# 摘要

| Ref | Title | Summary |
| --- | --- | --- |
| [^1] | [KTO: Model Alignment as Prospect Theoretic Optimization](https://rss.arxiv.org/abs/2402.01306) | 本文提出了一种名为KTO的方法，将模型对齐视为展望理论优化。与当前方法相比，KTO直接最大化生成效用而不是最大化偏好对数似然。在多个规模上，KTO的性能与基于偏好的方法相当甚至更好。 |
| [^2] | [Learning Length-Extrapolatable Recurrent Models](https://arxiv.org/abs/2609.09157) | 该论文提出时间信用稳定化（CST）方法，通过在反向传播中直接干预并稳定状态信用信号的范数，从而训练出能够超出训练范围进行长度外推的循环模型。 |
| [^3] | [Silver Rate Is (Almost) Optimal for Gradient Descent Acceleration](https://arxiv.org/abs/2609.09152) | 本文证明了银速率指数是梯度下降加速的最优多项式收敛速率，通过建立非anytime与anytime两种设定下匹配的下界，结合已有上界完整刻画了预定非负步长所能达到的最优加速效果。 |
| [^4] | [NOAH: Learning the Full Patient Journey. A Longitudinal Multimodal Time-Aware Model for Representation and Forecasting](https://arxiv.org/abs/2609.09140) | 该论文提出了NOAH，一个时间感知、任务无关的生成式transformer模型，通过新颖的双向时间集成和变分潜在空间，首次实现了对患者完整多模态旅程的表示与未来预测。 |
| [^5] | [Entropy-Regularized Rank-Masked Policy Optimization for Test-Time Reinforcement Learning in Code Generation](https://arxiv.org/abs/2609.09135) | 该论文提出探针驱动的测试时强化学习方法，通过探针一致性奖励（PCR）为开放词汇的程序提供行为训练信号，并利用熵正则化秩掩码策略优化（ERPO）抵御奖励攻击，使TTRL成功应用于代码生成任务。 |
| [^6] | [Nearly Tight Rademacher Bounds for Sparsely Activated Neural Networks](https://arxiv.org/abs/2609.09130) | 本文针对每个输入至多激活 $k$ 个隐藏单元的单隐藏层稀疏ReLU神经网络，建立了近乎紧致的Rademacher复杂度上界和相匹配的下界，并揭示了输入域和偏置设定对复杂度的关键影响。 |
| [^7] | [A Generalization of Amari's Bayesian Duality](https://arxiv.org/abs/2609.09126) | 本文通过将阿玛里的贝叶斯对偶性与贝叶斯规则的凸对偶性相联系，提出了阿玛里贝叶斯对偶性的推广，并探讨了其对现代人工智能的意义。 |
| [^8] | [When Does Scale-Invariant Optimization Become Unstable? An Exact Schedule Law with Weight Decay](https://arxiv.org/abs/2609.09116) | 该论文推导出一个精确的离散时间定律，揭示归一化导致的尺度不变性使学习率调度与权重衰减通过参数范数相互作用来控制有效步长，并证明该平衡点本质不稳定，因此恒定学习率加权重衰减无法稳定收敛，而是产生循环行为。 |
| [^9] | [SAEScientist-Bench: Can AI Agents Conduct Autonomous SAE Interpretability Research?](https://arxiv.org/abs/2609.09113) | 本文提出SAEScientist-Bench基准，评估AI智能体能否作为科学家自主运用稀疏自编码器（SAE）工具，进行机制可解释性的自主科学发现研究。 |
| [^10] | [Curriculum Learning as Transport: Understanding Curricula with Wasserstein Geodesics](https://arxiv.org/abs/2609.09099) | 提出Wasserstein课程路径框架，将课程表示为训练分布在离散难度级别上的传输轨迹，从而解耦课程学习中的各设计因素，并发现课程效应高度依赖情境、不存在普适最优的课程策略。 |
| [^11] | [ThinkPrior: Zero-Rollout Difficulty Priors for Cold-Start Prompt Selection in RLVR](https://arxiv.org/abs/2609.09075) | 提出ThinkPrior方法，通过一次离线验证器评分锚点构建零采样成本的Beta难度先验，解决RLVR中GRPO训练因全对/全错“沉默组”浪费39%采样以及基于历史的方法冷启动采样浪费的问题。 |
| [^12] | [Multi-Task Learning for Sparsely-Labeled Time Series: A Case Study on Cold-Hardiness Modeling](https://arxiv.org/abs/2609.09062) | 本文针对葡萄耐寒性预测中标注数据稀疏且品种间存在差异的难题，提出将不同葡萄品种视为不同任务的多任务学习方法，从而有效利用有限的稀疏标注时间序列数据，提升基于RNN的每日耐寒性预测效果。 |
| [^13] | [PlayTrain: An Efficient Reinforcement Learning Framework for LLM-Generated Adaptable JavaScript Games](https://arxiv.org/abs/2609.09059) | PlayTrain是一个强化学习框架，利用大语言模型从简单提示生成JavaScript游戏，并提供能在标准gym环境中以每秒超过100万次智能体决策的速度训练强化学习智能体的高效流水线。 |
| [^14] | [Training-Free Task Vectors for LLM Behavioral Control](https://arxiv.org/abs/2609.09054) | 提出了一种无需微调的TFTVs方法，仅利用前向传播统计量将激活引导向量映射为秩一权重空间编辑，从而以低成本实现对大语言模型行为的放大、抑制与组合控制。 |
| [^15] | [Do Reasoning Representations Help Humans Evaluate LLM Outputs?](https://arxiv.org/abs/2609.09038) | 研究发现，尽管人们更偏好基于规划和分解的复杂推理表示，但更简单的思维链表示反而更能有效支持人类对大语言模型输出的评估。 |
| [^16] | [Answer-Distribution Trajectories: A Stochastic-Dynamics View of LLM Reasoning](https://arxiv.org/abs/2609.09030) | 该论文提出“答案分布轨迹”这一受随机动力学启发的精细表示方法，通过追踪LLM推理过程中对答案的完整预测分布，刻画探索、修正、移动和承诺等动态推理特征，从而区分推理成功与失败的不同动态机制。 |
| [^17] | [Closed-Form of the Local Galactic Potential and Stellar Distribution Function from Gaia DR3](https://arxiv.org/abs/2609.09011) | 该论文提出了一种新方法，通过单独估计恒星分布函数、以加速度形式线性化无碰撞玻尔兹曼方程来直接测量局域力场，并利用符号回归获得局域银河系势能的封闭形式解，从而解决了同时拟合分布函数与势能时的退化问题。 |
| [^18] | [Let It Go or Learn to Self-Correct: Continuous Diffusion for Constrained Discrete Tasks](https://arxiv.org/abs/2609.09009) | 研究发现，在数独等全局约束的离散任务中，直接从扩散模型的干净预测进行采样（而非逐步去噪采样），无需重新训练即可将数独有效率从31%大幅提升至95%，因为标准扩散采样中保持接近含噪状态会固化早期错误并使反向轨迹偏离训练分布。 |
| [^19] | [Evaluation of Contextual Understanding in Large Language Models](https://arxiv.org/abs/2609.09004) | 本文提出基于知识图谱的S3KG评估框架，通过融合结构相似度与语义相似度来量化评估大语言模型的情境理解能力，并提供诊断推理错误的分类框架。 |
| [^20] | [Deposon: An Auditable, Conservation-Guaranteed, Game-Theoretically Tested Scattering Layer over LLM Reasoning Paths](https://arxiv.org/abs/2609.09001) | Deposon散射层将LLM推理路径的每个节点绑定到满足严格能量守恒（T+R+A=1）的三通道散射状态，为被丢弃的推理路径提供机器可审计的账本，其价值不在于提升过滤准确率，而在于实现机器可验证性。 |
| [^21] | [Physics-Informed Deep Learning for False Ventricular Tachycardia Alarm Reduction in the ICU](https://arxiv.org/abs/2609.08992) | 该论文提出将一维SE-ResNet与基于Windkessel血流动力学模型的物理信息辅助重建任务相结合的深度学习框架，通过惩罚生理上不可信的伪迹模式，在VTaC基准上实现了假性室速警报抑制性能的5分提升，并带来2倍的标签效率。 |
| [^22] | [Transformers as In-Context Samplers: From Closed-Form Diffusion to Estimation-Free Sampling](https://arxiv.org/abs/2609.08981) | 本文证明冻结的Transformer可以通过上下文学习模拟迭代式生成采样器（如闭式扩散采样器），其中softmax注意力计算责任权重与加权经验均值、前馈层实现欧拉更新，从而将上下文学习能力从监督学习拓展到数据生成任务。 |
| [^23] | [Omni Interaction Agent Technical Report](https://arxiv.org/abs/2609.08977) | Gander是一个端到端模型，通过小脑-大脑协作架构在单一框架内统一了全模态感知、实时全双工交互和智能体能力。 |
| [^24] | [GraphFAS: A Distributed System for Automated Graph Feature Generation and Selection in Industrial Transaction Networks](https://arxiv.org/abs/2609.08970) | GraphFAS提出了一种基于Boruta的分布式图特征自动生成与选择系统，通过非参数化的多跳子图特征提取和跨分区中位数聚合的特征选择，在工业交易网络欺诈检测中实现了可解释、可扩展且无需深度领域知识的特征工程。 |
| [^25] | [Fitting and Learning Basis-Restricted Propositional Formulas](https://arxiv.org/abs/2609.08961) | 本文系统确定了在任意有限布尔函数基下构建的命题公式（及电路）在拟合、Occam 学习、经验风险最小化和 PAC 学习等各类问题上的计算复杂度分类。 |
| [^26] | [ONE CYLinder: A Benchmark for Graph-Based Surrogate Modeling of Unsteady Bluff-Body Flows](https://arxiv.org/abs/2609.08947) | 该论文提出了ONECYL基准，包含450次涵盖层流、过渡流和高雷诺数状态的高保真圆柱绕流仿真，并为基于图的代理模型建立了统一的评估框架。 |
| [^27] | [The BatchNorm Illusion: Diagnosing Normalization Artifacts in Machine Unlearning Evaluation](https://arxiv.org/abs/2609.08901) | 该论文揭示了一个评估陷阱：在基于BatchNorm的架构上，仅对保留数据做一次前向传播（不修改任何权重）就能重写模型的归一化状态并逆转表面遗忘指标，作者将其形式化为权重保持的定点算子，严格证明此类前后差异源于BN运行统计量而非遗忘方法本身，从而清晰区分了度量失效与真实的权重编码信息残留。 |
| [^28] | [High-Magnetization Sampling at Low Temperatures: Ising Models and Bayesian Sparse Linear Regression](https://arxiv.org/abs/2609.08873) | 该论文提出了在高维稀疏（高磁化）汉明切片上的采样框架，实现了任意温度下固定磁化 Sherrington–Kirkpatrick Ising 模型的多项式时间采样，并改进了贝叶斯稀疏线性回归的采样方法。 |
| [^29] | [Earth System World Model for What-If Simulations: A Case Study for Terrestrial Ecosystems](https://arxiv.org/abs/2609.08855) | 该论文提出一种动作条件的世界模型框架，通过“转移-动作预训练”将自然观测的状态变化作为无标签动作监督，使地球系统仿真器无需人工标注干预即可支持交互式的“假设性”模拟，并以陆地生态系统为案例进行验证。 |
| [^30] | [Length Generalization for Transformers via Compression](https://arxiv.org/abs/2609.08851) | 本文针对C-RASP假设缺乏可计算长度泛化界及实验矛盾的问题，利用具有可计算长度泛化界的新片段C-RASP+和C-RASP1对其进行了改进。 |
| [^31] | [A Closed-Form Estimator and Diagnostic Battery for Anchor-Judge Error Correlation, Under a Single-Common-Factor Model](https://arxiv.org/abs/2609.08826) | 该论文提出在单共同因子模型下，仅凭至少两个评审者和两个锚点即可用闭式解同时识别质量方差、共模方差及各锚点的污染相关系数，并配套经过校准的诊断套件来应对模型假设不可检验的问题，从而摆脱传统方法对锚点无污染的强假设依赖。 |
| [^32] | [Ostrich: Taking Large Strides Through Stiff Contact in Differentiable Dynamics](https://arxiv.org/abs/2609.08800) | Ostrich是一个GPU加速的可微刚体仿真器，通过非光滑牛顿迭代在大时间步长下精确求解硬接触与摩擦，并利用隐函数定理以每步O(1)内存高效计算可靠梯度，同时兼顾仿真精度、梯度可靠性与计算成本。 |
| [^33] | [Eliciting Weak-to-Strong Generalization with On-Policy Reverse Distillation](https://arxiv.org/abs/2609.08798) | 提出在线策略反向蒸馏（OPRD）方法，通过在学生模型滚动输出上评估教师策略偏移并放大学生验证器驱动的策略梯度分量，在保留优化驻点的同时加速学习，使学生模型超越弱教师模型，实现弱到强泛化。 |
| [^34] | [Adaptive Anisotropic Attention for Axis-Structured Signals](https://arxiv.org/abs/2609.08788) | 提出自适应各向异性注意力（AAA），将注意力沿电极时间轴分解为时间路径与空间路径并通过门控自适应加权融合，所构建的AXON模型在六个EEG下游任务上优于稠密注意力基线。 |
| [^35] | [ZK-Trace: Certified Collusion Tracing with Zero-Knowledge Credentials for Federated GNSS Interference Monitoring](https://arxiv.org/abs/2609.08763) | ZK-Trace 提出了一种融合公开身份标记、Tardos 指纹与零知识凭证验证的认证合谋追踪方案，能够在联邦式GNSS干扰监测中实现无需泄露者配合的离线追踪，并将每次调查的错误指控率控制在 0.001 以内。 |
| [^36] | [A Note on Scaling in Randomly Rotated Quantization and Its Connection to the CDEF +1 Pythagorean Relation](https://arxiv.org/abs/2609.08759) | 本文揭示了随机旋转量化中EDEN系列工作的两个重构缩放因子与经典CDEF公式中维纳系数和无偏系数的对应关系，证明CDEF +1勾股关系在有限块长下对每个旋转实现作为精确几何恒等式逐点成立，而经典信噪比关系在维度趋于无穷时恢复。 |
| [^37] | [PAC-Bayesian Bounds for Learning Partially Observed Stochastic Linear Time-Invariant State-Space Systems with Inputs and Sub-Gaussian Noise](https://arxiv.org/abs/2609.08740) | 本文为带有输入和亚高斯噪声的部分可观测随机线性时不变状态空间系统推导了PAC-贝叶斯误差界，从而可为一大类系统辨识算法提供预测误差和参数估计误差的有限样本保证。 |
| [^38] | [BAFF: Bid-Aware Filter Family for Mitigating Training Data Interference in RTB A/B Tests](https://arxiv.org/abs/2609.08725) | 提出竞价感知过滤族（BAFF），通过可参数化的硬过滤器在共享日志与拆分日志两种极端方案之间灵活平衡训练数据保留与偏差控制，从而缓解RTB广告A/B测试中由广告排序与竞价定价分歧导致的训练数据干扰。 |
| [^39] | [Chimaera: A Mixture-of-Graph-Experts Architecture for Cross-Task and Cross-Dataset Graph Learning](https://arxiv.org/abs/2609.08709) | Chimaera 提出了一种将专家混合机制与图基础模型相结合的新架构，通过整合多种 GFM 架构和大语言模型生成的嵌入，实现了跨任务和跨数据集的图学习能力。 |
| [^40] | [TontaubeV1: Streaming Text-to-Speech with Hierarchical Codec Modeling and Bounded Context](https://arxiv.org/abs/2609.08703) | TontaubeV1通过分层编解码器建模——用较大模型预测语义流以确定韵律和时长、用较小模型逐步细化声学特征——结合有界上下文设计，在单块消费级GPU上实现了兼具自然韵律的流式文本转语音。 |
| [^41] | [Hyperparameter Scaling Laws Across MoE Sparsity](https://arxiv.org/abs/2609.08690) | 本研究通过1,800次预训练实验（约20万亿token）发现MoE模型的最优学习率和批量大小随激活比率而非参数量变化，并揭示了两种缩放机制，从而调和了以往研究中相互矛盾的发现。 |
| [^42] | [HOPE: Heterophily-Aware Open-Set Node Classification with Pseudo-Extrapolation](https://arxiv.org/abs/2609.08685) | HOPE通过结构增强的特征初始化层捕获多跳结构模式，并利用伪外推技术，解决了异配图开放集节点分类中跨类连接导致表示纠缠、结构混合导致未知类拒识不可靠的双重挑战。 |
| [^43] | [Neither Adversarial Training Nor Purification: Emergent Adversarial Robustness from Oscillatory Predictive Learning](https://arxiv.org/abs/2609.08683) | 该论文提出振荡预测学习（OPL）框架，通过结合人工Kuramoto振荡神经元与预测性自监督预训练，使对抗鲁棒性从架构归纳偏置中自然涌现，从而无需昂贵的对抗训练或测试时净化即可在CIFAR-10/100上取得有竞争力的鲁棒性。 |
| [^44] | [Optimal estimation for Functional Linear Regression with Noisy Discretized Data](https://arxiv.org/abs/2609.08671) | 提出一种结合傅里叶投影曲线重建与惩罚最小二乘斜率估计的两步方法，在含噪离散采样下实现了函数线性回归预测误差的极小化极大最优速率。 |
| [^45] | [Learning to build covering structures with continuous adjustments](https://arxiv.org/abs/2609.08669) | 提出了一种名为HSAC的强化学习方法，无需预定义计划，通过引入单向边的图神经网络和混合离散-连续动作空间，在建造过程中自适应地生成建造序列，以应对物理制造中的误差与不确定性。 |
| [^46] | [MoEMB: Scaling Universal Multimodal Embeddings with Efficient Mixture-of-Experts Models](https://arxiv.org/abs/2609.08663) | MoEMB 提出利用混合专家沿专家维度扩展通用多模态嵌入模型，在保持单向量、非自回归编码与低延迟推理的同时提升编码器容量，并避免对简单任务产生冗余计算。 |
| [^47] | [Charts Are Beyond Pixels: Probing for Layer-Wise Chart Understanding and Editing](https://arxiv.org/abs/2609.08657) | 该论文提出LayerWiseBench基准，通过图层归属、图层绑定和可见性排序三个核心维度，评估模型在图层级别理解与编辑图表的能力，突破了以往仅评估最终输出正确性的局限。 |
| [^48] | [Difficulty-Adaptive Tree-Structured Policy Optimization for Expanding Reasoning Coverage in RLVR](https://arxiv.org/abs/2609.08650) | 该论文提出DATPO方法，通过难度自适应、句子熵引导的树结构轨迹生成策略，突破了RLVR探索受限的问题，有效扩展了大型推理模型的推理覆盖范围（pass@k）。 |
| [^49] | [SUN: Reaching for Novelty in Reinforcement Learning](https://arxiv.org/abs/2609.08642) | 本文提出基于后继价值函数的SUN指标，可同时衡量目标的新颖性与可达性，该可达性感知的目标选择框架能无缝集成到任意离策略强化学习算法中，并具有恢复计数奖励、约束命中概率和拒绝不可达目标的理论保证。 |
| [^50] | [Suan: Rectifying Direct Preference Safety Alignment in Large Language Models](https://arxiv.org/abs/2609.08634) | Suan是一种新颖的偏好优化算法，通过直接在梯度层面构建优化目标（绕过标准变分推导），使大语言模型在实现卓越安全对齐的同时完全保留回应实用性。 |
| [^51] | [SynthRCT: Scalable Conditional Deformation Synthesis for Synthetic Repeat CT Generation](https://arxiv.org/abs/2609.08627) | 本文提出SynthRCT，一个基于条件变分自编码器的可扩展生成框架，能够从单一CT合成患者特异性的三维解剖形变，用于生成合成重复CT以支持质子治疗中的鲁棒性评估。 |
| [^52] | [Leveraging contextual events on structure-aware next activity prediction](https://arxiv.org/abs/2609.08622) | 本文提出一种基于实例图与图神经网络的结构感知方法，通过多种上下文事件编码策略将流程执行的环境条件纳入下一个活动预测，并在多个真实事件日志上验证了上下文信息对预测性能的提升作用。 |
| [^53] | [Flexible Spectral-Normalized Neural Gaussian Process for Dynamic Aperture Prediction](https://arxiv.org/abs/2609.08620) | 该论文提出了一种经验贝叶斯方法来自动调整谱归一化神经高斯过程的超参数，在显著降低计算负担的同时实现了可扩展的不确定性量化，并在CERN大型强子对撞机的动态孔径预测任务上验证了方法的有效性。 |
| [^54] | [Target-Independent Micro-Interventions for Predicting Training Response Across Language-Model Families](https://arxiv.org/abs/2609.08618) | 该论文提出通过四个目标无关的微干预构建 L-State 状态表示，能够跨不同语言模型家族预测模型对未来训练的响应，在 GLM-4-9B 上将预测误差（MSE）降低高达 78.3%。 |
| [^55] | [Why shared attention vectors fail: a case for outcome-indexed tuning](https://arxiv.org/abs/2609.08615) | 本文揭示了全局共享注意力向量在多结果学习中会不稳定并塌缩至边界的问题，并提出用结果索引的注意力矩阵替代，使其能收敛到对学习和泛化有意义的注意力表示。 |
| [^56] | [Multi-Level-Set-Based Physics-Driven Neural Network to Solve 3-D Inverse Scattering Problems](https://arxiv.org/abs/2609.08594) | 本文提出基于多水平集的物理驱动神经网络（LSPDNN），利用软并集多材料模型将目标支撑域与材料分布分离描述，并结合作用于材料区域指示器的模型一致TV正则化，有效缓解了三维电磁逆散射重建中的边界模糊与伪影问题。 |
| [^57] | [FedGenSC: Federated Generative Semantic Communication with Channel-Aware Adaptation](https://arxiv.org/abs/2609.08593) | 提出FedGenSC框架，通过全局生成器搭配本地判别器、语义原型库和基于信噪比条件的自适应生成，同时解决了联邦生成式语义通信中判别器聚合不稳定、语义漂移和信道无关生成三大问题。 |
| [^58] | [Limitations of Automated Simulatability: LLM Simulators Can Bypass Explanations](https://arxiv.org/abs/2609.08585) | 本文揭示了自动可模拟性评估的两大局限：当类名有意义时，LLM模拟器可直接解题获得高分而不依赖解释；而类匿名化又可能奖励泄露隐藏标签映射的解释，表明模拟器预测主要依赖任务信息而非解释本身。 |
| [^59] | [Leveraging Cardiac Imaging to Improve ECG-Based Detection of Chagas Disease in Resource-Constrained Settings](https://arxiv.org/abs/2609.08582) | 该研究通过在英国生物样本库的63,193对ECG-CMR配对数据上进行对比预训练，将心脏磁共振的结构知识迁移到心电图编码器，在预训练中从未见过恰加斯病病例的情况下显著提升了资源受限地区基于心电图的恰加斯病检测性能。 |
| [^60] | [AlphaRJM: Reward-Jump Memory for Stochastic Return-Guided Alpha Discovery](https://arxiv.org/abs/2609.08581) | 提出AlphaRJM框架，通过仅在终端评估事件时更新的奖励跳跃记忆和使用随机粒子表示未来收益的动作条件化SDE评论家，解决了公式化Alpha发现中延迟反馈导致的因子池历史信息丢失与中间动作价值不确定的难题。 |
| [^61] | [Non-Adaptive 1-Bit Mean Estimation: Minimax Rates and the Sample-Interval Tradeoff](https://arxiv.org/abs/2609.08564) | 该论文证明对于所有 k>1，非自适应的1比特协议即可达到自适应协议的极小极大速率，表明分布式均值估计中交互式学习并非必要，并刻画了查询区间数受限情形下的样本复杂度权衡。 |
| [^62] | [Certified Topological Interaction in Neural Representations: Class Disentanglement Is Mostly Pairwise](https://arxiv.org/abs/2609.08561) | 本文提出用交集欧拉示性数轮廓将神经表征中的类解缠度量为带统计认证的拓扑交互，发现解缠主要发生在成对类别之间、随深度分级且集中在训练早期。 |
| [^63] | [Not All Variables Agree: Reliability-Aware Variable-Wise Gradient Surgery for Multivariate Time-Series Forecasting](https://arxiv.org/abs/2609.08554) | 该论文通过直接测量变量级梯度揭示了多变量时间序列预测中梯度冲突与性能损害并不等同的现象，并提出了一种优化器侧的变量级梯度手术训练策略PV-Surgery，以提升共享训练下各变量的预测可靠性。 |
| [^64] | [The Exact Time-Uniform Rate Frontier for Stochastic Gradient Descent on Smooth Convex Objectives](https://arxiv.org/abs/2609.08537) | 该论文精确刻画了标准SGD在光滑凸目标上时间一致收敛率的可实现边界——阶为 $h(n)/\sqrt{n}$ 的界可实现当且仅当 $\sum 1/h(2^j)^2 < \infty$，且收敛率可任意接近但永远达不到 $\sqrt{\log n / n}$。 |
| [^65] | [Temporal State Transport in Video Generation: Diagnosing and Correcting Spectral Imbalance](https://arxiv.org/abs/2609.08505) | 该论文提出“频谱张力”这一带符号的诊断指标，用于识别视频生成中碎片化传输与过度混合两类时间失衡问题，并提出免训练的“频谱传输稳态”调节器，在保留平衡状态的同时温和纠正病态时间状态。 |
| [^66] | [When Topology Betrays Privacy: Lattice-Based Reconstruction Attacks on Secure Aggregation in Decentralized Federated Learning](https://arxiv.org/abs/2609.08476) | 去中心化联邦学习中稀疏拓扑使局部安全聚合成为结构性泄露面，本文通过将重构问题归约为隐藏子集和问题并设计基于格的攻击算法，使合谋的半诚实节点能够重构诚实参与者的私有模型更新。 |
| [^67] | [Do Reviewers Still Reward Lexical Complexity? A Frozen-Rater Study of Preference Drift in 124K ICLR Reviews](https://arxiv.org/abs/2609.08475) | 该研究提出“冻结评审员”方法——用同一模型和提示词在固定时间窗口生成81,850份机器评审，以区分投稿构成变化与审稿人偏好变化——发现人类评审对词汇复杂性的系数从+0.142反转为-0.015而冻结评审员保持稳定，证明ICLR审稿人已真实地不再青睐辞藻华丽的写作。 |
| [^68] | [Inclusive electron-nucleus cross section models from domain adaptation](https://arxiv.org/abs/2609.08463) | 该研究利用迁移学习将基于碳-12数据预训练的深度神经网络适配到氦-3、锂-6、氧-16、铝-27、钙-40和铁-56等多种原子核的包容性电子散射截面建模中，发现不同核所需的微调深度各异，且模型预测稳健性依赖于目标数据的数量、覆盖范围和精度。 |
| [^69] | [Selective boundary condition reduction via learned error gating](https://arxiv.org/abs/2609.08461) | 该论文提出一种学习误差门控框架，利用神经网络预测边界条件约简带来的区域误差和边界误差，仅在预测误差满足容差时才用更简单的极限边界条件（如Dirichlet形式）替代复杂的刚性Robin或非线性边界条件，从而在保证精度的同时降低计算成本。 |
| [^70] | [Topological Fraud Detection in Latent Transaction Spaces](https://arxiv.org/abs/2609.08445) | 该论文提出一种在拓扑匿名化嵌入空间中迭代结合无监督过滤与有监督精确识别的欺诈检测方法，实现超低延迟且不泄露个人身份信息的隐私保护交易筛查。 |
| [^71] | [Stochastically Perturbed Weights: Ensembles from Deterministic Machine-Learning Weather Models](https://arxiv.org/abs/2609.08412) | 提出随机扰动权重（SPW）方法，通过在推理时直接扰动确定性机器学习天气模型的原始权重张量，无需重新训练即可从现有确定性模型中提取预报不确定性并生成集合预报。 |
| [^72] | [Environments as Scaffold: Enriching Feedback to Bootstrap Self-Evolving Agents in Long-Horizon Tasks](https://arxiv.org/abs/2609.08404) | 提出反馈丰富环境（FEEs）这一环境侧适应新范式，通过在探索和演化的后期阶段将反馈从动作引导转向观察丰富，有效缓解了强化学习训练自进化智能体在长程任务中的奖励稀疏问题。 |
| [^73] | [MLIP Detective: Active Failure Mode Discovery Beyond Benchmark Scores for Machine-Learning Interatomic Potentials](https://arxiv.org/abs/2609.08399) | 提出了 MLIP Detective 智能体框架，通过生成物理信息驱动的可证伪失效假设并以低成本模拟筛选，主动发现基准测试无法暴露的通用机器学习原子间势的隐藏失效模式，且在无针对性提示的情况下发现了 MACE-MPA-0 模型在含氧/氟吸附物体系中的系统性预测异常。 |
| [^74] | [Equivariance Breaks the Learning Rate](https://arxiv.org/abs/2609.08381) | 该论文发现等变网络中不可约表示块的梯度秩受限，而 Adam 的逐元素缩放会破坏块间边界，导致同一层内不同块的谱步长不一致，为此提出了一种无需新超参数的逐块归一化更新方法，仅改变更新尺度而保持矩估计和方向不变。 |
| [^75] | [How to Make the Gradient Mapping Small for Constrained Stochastic Min-Max Problems and Beyond](https://arxiv.org/abs/2609.08380) | 该论文将约束随机凸-凹极小极大优化和随机单调变分不等式在梯度映射度量下的复杂度从 $\widetilde{O}(\varepsilon^{-4})$ 提升至近最优的 $\widetilde{O}(\varepsilon^{-2})$，并将该结果推广到更一般的问题设定。 |
| [^76] | [Geographically Regularized AUC-Maximizing Personalized Federated Learning](https://arxiv.org/abs/2609.08379) | 该论文提出了GrAUC-PFL方法，通过直接优化平滑的成对AUC替代损失，并结合基于图的正则化使地理相邻机构模型相似，在患者数据不出本地、兼顾机构异质性的前提下学习个性化模型，从而提升传染病诊断与风险预测的判别性能。 |
| [^77] | [IPM-FM: A Foundation Model with Consensus Feature Selection for Industrial Process Monitoring](https://arxiv.org/abs/2609.08375) | 本文提出首个面向工业过程监控的基础模型IPM-FM，通过自监督预训练学习通用表征、少量标注数据进行任务适配、并以不确定性感知预测头输出校准预测，突破了传统“一任务一模型”范式在标签效率和工况漂移下的局限。 |
| [^78] | [Miles v0.1: Production-Level Post-Training](https://arxiv.org/abs/2609.08368) | Miles v0.1是一个基于slime构建的全栈生产级强化学习后训练系统，以可验证、简洁、可定制的组件为设计原则，支持全参数RL、LoRA RL、在线策略蒸馏等多种训练范式并扩展至扩散模型，使前沿规模的RL训练对研究者和企业都可用。 |
| [^79] | [To Adapt or Not to Adapt? Selective Adaptation for Vision-Language Models](https://arxiv.org/abs/2609.08367) | 本文提出“选择性适应”这一新问题，并设计了跨增强相似度（CAS）方法，通过仅在增强视图间预测相似度较低时才执行适应，从而识别并跳过测试时适应中微不足道或有害的适应操作。 |
| [^80] | [Geometry-Aware Bayesian Parameter-Efficient Fine-Tuning on the Stiefel Manifold via Stein Variational Gradient Descent](https://arxiv.org/abs/2609.08354) | 本文提出一种基于Stein变分梯度下降的几何感知贝叶斯参数高效微调框架，通过沿Stiefel流形传输低秩矩阵以保留正交几何结构，并在推理中提供多个解来缓解过自信问题。 |
| [^81] | [CoVeR: Coverage-Based Token Pruning for Multi-View 3D Reasoning in VLMs](https://arxiv.org/abs/2609.08345) | 提出CoVeR，一种无需训练、仅依据token坐标的空间覆盖度进行确定性视觉token剪枝的方法，解决多视角3D推理中冗余token过多及现有剪枝方法覆盖不足的问题。 |
| [^82] | [TV-Regulated OPD: Direction Matters in On-Policy Distillation](https://arxiv.org/abs/2609.08341) | 该论文发现在在策略蒸馏中只有token级优势的方向（符号）才真正关键，并提出利用全变差（TV）正则化来塑造有界且平滑的优势函数，从而实现更稳定的训练过程并获得相当的性能。 |
| [^83] | [Distillation as Probability Transport: Routed On-Policy Distillation](https://arxiv.org/abs/2609.08337) | 该论文提出RouteOPD，将在线策略蒸馏重新表述为教师引导的概率传输问题，通过将师生分歧分解为概率过剩源与亏缺目的地并构建显式传输对，实现了更精确的token级概率再分配与更新幅度控制。 |
| [^84] | [Fixed-Dimensional Latent Flow for Generating Variable-Size 3D Molecules](https://arxiv.org/abs/2609.08333) | 该论文提出了一种两阶段生成框架，通过单一固定维度的分子级潜在表示结合自回归Transformer解码器，实现了无需预先指定分子大小的可变尺寸3D分子生成，并摒弃了等变层和密集成对键解码器的需求。 |
| [^85] | [EMBLEM: Enhancing Multi-script Table Detection through Masking](https://arxiv.org/abs/2609.08330) | 该论文推出了包含18种语言、15种文字系统的人工标注数据集MANDALA，并提出基于掩码的EMBLEM方法，通过隐藏文字和字体特征使英文预训练模型能够有效泛化到多文字系统的表格检测任务。 |
| [^86] | [Non-Coherent Over-the-Air Federated Learning: Protocol, Convergence, and Device Scheduling](https://arxiv.org/abs/2609.08312) | 本文提出了一种非相干空中联邦学习协议NCAirFL，通过二进制抖动、无偏非相干检测和长期误差反馈免除对瞬时信道状态信息的依赖，并给出了收敛性分析与设备调度方案。 |
| [^87] | [A Measurement Study of LLM Inference Trade-offs Across Edge Continuum Hardware](https://arxiv.org/abs/2609.08307) | 本文通过对边缘与近边缘硬件上多种开源大语言模型推理的受控测量研究，发现GPU服务器提供最低延迟而Jetson Orin能耗更低，为大语言模型边缘部署中的质量、延迟与能耗权衡提供了实证依据。 |
| [^88] | [HoneyRoute: Honeypot-Model Routing for Adversarial LLM Serving](https://arxiv.org/abs/2609.08306) | HoneyRoute是一个部署在推理服务层的防御框架，通过轻量级流式路由器实时检测恶意请求并将其路由至蜜罐模型，在保护生产LLM服务的同时，将捕获的攻击者交互转化为指纹数据用于持续改进路由器的检测能力。 |
| [^89] | [HypLTSF: A Hyperbolic Geometric View of Multi-Scale Hierarchies for Long-Term Time Series Forecasting](https://arxiv.org/abs/2609.08286) | 本文提出HypLTSF框架，通过将多尺度时间表征嵌入庞加莱球（双曲空间），首次以显式的双曲几何结构刻画长期时间序列预测中的尺度层次关系，使层次结构从交互的副产品变为被明确建模的几何对象。 |
| [^90] | [Adaptively Incorporating Directional Hints into Zeroth-Order Optimization](https://arxiv.org/abs/2609.08277) | 提出CV-ZOD框架，利用控制变量自适应地将廉价的方向提示融入零阶优化，使收敛速率能根据提示质量在 O(1/T) 一阶速率与 O(d/T) 零阶速率之间平滑插值，且实用变体无需任何先验知识。 |
| [^91] | [Online Signature Verification Using Augmented Path Signature and T-Mamba](https://arxiv.org/abs/2609.08276) | 本文提出了一种将增强路径签名（APS）描述符与结合TCN块和时间扫描Mamba的T-Mamba模型相融合的新型框架，有效解决了在线签名验证中判别性特征捕捉和长程依赖建模的难题。 |
| [^92] | [Synergistic Fusion of Topological Structure and Temporal Semantics of Mobility for Urban Region Embedding](https://arxiv.org/abs/2609.08268) | 提出MoSS方法，协同融合移动数据的时序语义（每小时流入/流出序列视图）与拓扑结构（结构视图），克服现有方法忽视时间动态以及加性融合丢失联合信号的缺陷，生成更优的城市区域嵌入。 |
| [^93] | [Revisiting Spectral Representations in Generative Diffusion Models](https://arxiv.org/abs/2609.08253) | 该论文从扰动核的共同视角揭示了自监督谱表示学习与扩散生成模型之间的内在联系，并提出一种自监督谱表示对齐方法，以加速扩散模型训练并提升采样质量。 |
| [^94] | [CUNO: Curriculum and Preference Optimization for Stable Graph Unlearning under Mass Deletion](https://arxiv.org/abs/2609.08244) | 提出CUNO框架，通过按遗忘难度对样本排序并分阶段渐进移除，结合分布级负偏好优化目标，有效缓解了大规模删除下图遗忘中的灾难性遗忘问题。 |
| [^95] | [Distribution-free inference on the number of changepoints](https://arxiv.org/abs/2609.08234) | 本文证明了对变点数量K构造分布无关上置信界是不可能的，并提出了基于共形p值的有限样本有效下置信界CLOCC，在数据段仅满足内部可交换且相互独立的温和假设下具有普适性。 |
| [^96] | [Routing Dense Layouts with History-Aware Offline Reinforcement Learning using LSTM](https://arxiv.org/abs/2609.08232) | 该论文提出了一种结合轻量级LSTM架构的历史感知离线强化学习策略，通过保留布线迭代的序列上下文来预测迭代成本权重，从而显著改善高密度设计中的详细布线收敛性。 |
| [^97] | [ActionSplice: In-Flight Action Editing for Interactive World Models](https://arxiv.org/abs/2609.08230) | ActionSplice提出了一种基于反事实状态传输的推理框架，使分块自回归世界模型能够在采样过程中即时修改动作，无需等待下一分块或触发代价高昂的回滚重算。 |
| [^98] | [Speed Limit for Information Acquisition in Stochastic Learning Dynamics](https://arxiv.org/abs/2609.08219) | 本研究将随机梯度下降建模为马尔可夫随机过程，推导出费希尔信息流的速度极限，定量约束了可训练参数获取数据中潜在变量信息的速率，并将信息流分解为漂移（确定性学习力）与噪声（SGD涨落）两部分贡献。 |
| [^99] | [Vision: Data-Centric Anchoring for Robust and Interpretable Agentic AI](https://arxiv.org/abs/2609.08216) | 论文提出“数据中心锚定”理念及四阶段的数据中心智能体循环框架，主张将稳健性和可解释性预先工程化地构建到数据环境中，从根本上解决智能体AI在分布偏移下失效且无法解释决策的结构性缺陷。 |
| [^100] | [DRIFT: Removing Diffusion Watermarks by Deflecting the Generative Trajectory](https://arxiv.org/abs/2609.08213) | 提出了DRIFT黑盒攻击方法，通过部分前向扩散与随机逆向重采样来偏移生成轨迹，从而有效移除依赖轨迹验证的扩散模型水印。 |
| [^101] | [A Better Spur Should Start From Each Objective](https://arxiv.org/abs/2609.08211) | 提出了多边际偏好优化（MMPO）框架，通过曝光去偏、优先级感知正交投影和自我提示梯度约束，在数据、梯度和约束三个层面细粒度地解决多目标强化学习中的目标冲突问题，显著提升训练稳定性与性能。 |
| [^102] | [SIM: Subspace Interaction-based Method for Token-Level Text Anomaly Detection](https://arxiv.org/abs/2609.08200) | 该论文提出了基于子空间交互的方法SIM，通过避免局部异常信号被冗余特征维度稀释以及克服预训练语言模型对表层异常的平滑效应，实现了更有效的词元级文本异常检测。 |
| [^103] | [Optimal Slice-Adaptive Tuning of Hybrid Slice Sampling](https://arxiv.org/abs/2609.08172) | 该论文分析了混合切片采样中切片宽度参数 $w$ 对每次迭代目标密度评估次数的影响，并据此提出了具有次优性界限和渐近收敛保证的自动化切片自适应调优方案。 |
| [^104] | [A Transformer-Based Delta Expression Encoder for Psilocybin Transcriptional Response: Architecture, Representations, and Biological Validation](https://arxiv.org/abs/2609.08165) | 本文提出一种无需先验生物学知识的基于Transformer的差值表达编码器，可从单细胞核RNA测序数据中分类裸盖菇素诱导的差异基因表达状态，在18种细胞类型上达到69.4%的加权准确率，并揭示了从28.3%到99.6%的显著细胞类型特异性分类差异。 |
| [^105] | [Geodesic-informed Generative Diffusion Model For Topology-preserved Image Video Generation](https://arxiv.org/abs/2609.08153) | 该论文提出一种基于测地线信息的生成扩散模型，通过显式建模形变动力学来保证生成过程中的几何与拓扑一致性，解决了现有生成模型忽视物体底层几何结构且变换过程不可解释、缺乏物理信息的两大局限。 |
| [^106] | [Topology-induced Operators Reveal Complementary Graph Representations without Training](https://arxiv.org/abs/2609.08152) | 该论文提出两种无需训练的拓扑诱导算子，通过在随机游走和匿名游走诱导的层次结构中传播随机特征，分别捕捉节点邻近性与结构角色这两种互补的图嵌入，在多种任务上性能媲美现有方法且计算成本大幅降低。 |
| [^107] | [GPU-Enabled Large-Scale Optimization Using Randomized Linear Algebra](https://arxiv.org/abs/2609.08136) | rlaopt 是一个基于 PyTorch 的软件包，通过将随机化数值线性代数与 GPU 加速相结合，为大规模正定线性系统和带约束正则化的凸优化问题提供高效求解器，并配备自然数学语法的建模语言和可微分迭代功能。 |
| [^108] | [KBBQ: A Predictive Noise Law and the Limits of Spectrum Flattening in FP4 Quantization](https://arxiv.org/abs/2609.08135) | 该论文建立了矩阵乘法量化噪声的二阶理论，导出闭式信噪比定律与任何保函数线性变换无法超越的上界 $\kappa^{*}$，并据此提出 KBBQ 方法，在 W4A4 FP4 量化中超越了先前最先进的方法。 |
| [^109] | [Sparse Data Augmentation for Optimization with Provable Guarantees](https://arxiv.org/abs/2609.08133) | 该论文证明了在非凸几何机器学习优化中，使用优化前采样的少量固定数据变换进行稀疏数据增强，梯度下降仅需对数级加多项式级的群变换查询次数，即可在概率保证下逼近完全数据增强目标的稳定点。 |
| [^110] | [DISEIL: Demonstration Distillation for Sample-Efficient Imitation Learning](https://arxiv.org/abs/2609.08123) | DISEIL通过在策略首次不可靠处标记失败、以几何描述符表征并将失败聚类为反复出现的失败模式，再利用视觉-语言模型和语言模型生成精准的演示请求，从而实现样本高效的交互式模仿学习。 |
| [^111] | [Nystr\"om Attention Matches Full Attention for Cross-Sectional Stock Prediction](https://arxiv.org/abs/2609.08106) | 该研究通过谱分析发现MASTER模型中股票间注意力偏离均匀分布的部分本质上是低秩的，因此Nyström低秩注意力可以以更低的计算复杂度完全替代全注意力模块，实现等效的横截面股票预测性能。 |
| [^112] | [Learning Metamaterial Eigenmodes with Wavelet-Encoded Fourier Neural Operators](https://arxiv.org/abs/2609.08102) | 本文提出将小波编码与傅里叶神经算子相结合，突破了传统算子学习假设唯一输入-输出映射的限制，实现了在单一模型中学习并预测弹性波方程的多个特征模态，从而确定超材料中声波传播的变形模式。 |
| [^113] | [PocketVE: Stable and Property-Guided Structure-Based Drug Design with Variance-Exploding Diffusion](https://arxiv.org/abs/2609.08101) | PocketVE是一个以蛋白质口袋为条件的方差爆炸扩散框架，通过结合EDM式3D去噪、无分类器多性质引导和自适应蛋白质扰动正则化，在CrossDocked2020上显著提升了3D有效性（58.6→80.6）并大幅降低了应变能（457.4→127.9）。 |
| [^114] | [Marigold V2: Revisiting Diffusion Transformers for Monocular Depth Estimation](https://arxiv.org/abs/2609.08084) | 提出Marigold V2，通过将基于扩散Transformer的图像生成模型改造为单步推理的单目深度估计器，并借助语义特征对齐和两阶段微调两种策略消除训练伪影，在保持低成本的同时实现高质量、强泛化能力的深度估计。 |
| [^115] | [Proactive Context-Forecasted Safety Constraints for Nonstationary Reinforcement Learning](https://arxiv.org/abs/2609.08080) | 提出了一种基于情境预测的主动式安全约束生成框架，通过推断潜在环境情境并预测其未来演变来前瞻性地构建安全约束，使强化学习智能体能够在非平稳环境中主动规避不安全区域。 |
| [^116] | [A Machine Learning Framework for Predicting Restaurant Food Waste to Support Sustainable Food Management](https://arxiv.org/abs/2609.08078) | 本文提出了一个整合餐厅运营需求、气象数据和时间事件指标的机器学习框架，利用四种回归模型预测餐厅每日食物浪费量，以支持可持续食物管理。 |
| [^117] | [A Gradient-based yet Spike-Timing-Dependent Solution to the Feedback Learning Problem in Neural Microcircuits](https://arxiv.org/abs/2609.08070) | 本文提出梯度隧穿算法，将时间信用分配重构为状态分离问题，从局部突触脉冲时序推导信用分配，实现了与生物脉冲时序一致的神经微回路在线反馈学习框架。 |
| [^118] | [Risk-Conditioned Fine-Tuning of Large Language Models](https://arxiv.org/abs/2609.08064) | 提出风险条件化RLHF框架，通过训练单一策略提供连续可调的风险控制接口，使用户能够在推理时灵活选择不同的风险规避程度，而无需重新训练或部署多个特定风险的模型。 |
| [^119] | [MI-PEFT: Mixture-of-Experts Integrated Parameter-Efficient Fine-Tuning Protein Language Models Improves Acidophilic Proteins Classification](https://arxiv.org/abs/2609.08059) | 本文提出MI-PEFT框架，在ESM C-600M蛋白质语言模型上集成基于LoRA的参数高效微调方法与基于DeepSeekMoE的分类头，显著提升了嗜酸蛋白质分类的准确性和计算效率。 |
| [^120] | [Bayesian Matrix-Valued Graphs for Context-Dependent Multivariate Relationships](https://arxiv.org/abs/2609.08055) | 本文提出贝叶斯矩阵值图（BMVG），用对称正定矩阵建模图的边，并借助AIRM测地距离与广义特征值来量化和定位多变量相互作用在不同情境间的变化幅度与方向，在精度矩阵恢复上优于或媲美现有基线方法。 |
| [^121] | [A Quantitative Evaluation Framework for Temporal Explainability in Echocardiographic Video Segmentation](https://arxiv.org/abs/2609.08043) | 该论文提出了一个利用时间一致性、显著性运动、解剖学重叠和时间重叠四个互补指标来定量评估超声心动图视频分割中Grad-CAM时间可解释性的框架，发现尽管各模型分割性能相当，其时间解释的稳定性却存在显著差异。 |
| [^122] | [MamMA: A Mamba-Based Pedestrian Trajectory Prediction Algorithm Considering Occupancy Map and Pedestrian Awareness States](https://arxiv.org/abs/2609.08041) | 提出了MamMA，一种基于Mamba的行人轨迹预测算法，通过将占据地图按补丁划分提取障碍物特征，并结合行人感知状态和自我视角图像的细粒度行为信息，提升了人机共存环境中移动机器人的轨迹预测精度与导航安全性。 |
| [^123] | [VEX-Bench: Benchmarking LLM Agents for Assessing Exploitability of Software Supply Chain Vulnerabilities](https://arxiv.org/abs/2609.08040) | 该论文提出了VEX-Bench，这是首个专门用于评估大语言模型智能体判断软件供应链中已知漏洞对下游项目可利用性能力的基准测试，填补了以往基准仅针对零日漏洞场景的空白。 |
| [^124] | [Two-Scale Localized PCA-Net: Coarse-Global and Local-Residual Representations for Artifact-Reduced PDE Operator Learning](https://arxiv.org/abs/2609.08034) | 提出双尺度局部化PCA-Net，通过粗全局PCA基与局部残差PCA基相结合的表示方法，在将PCA拟合成本减半的同时，显著降低了PDE算子学习中的重建误差与块状伪影。 |
| [^125] | [Flexible Motion Generation from Language and Style References](https://arxiv.org/abs/2609.08032) | FlexMoGen是一个同时以自然语言描述和动作风格示例片段为条件的人体动作合成框架，能够生成既保留文本语义内容、又忠实再现目标风格细节的高质量动作。 |
| [^126] | [Sharp Structure-Agnostic Minimax Risk for Partial Linear Models](https://arxiv.org/abs/2609.07997) | 该论文刻画了部分线性模型中系数估计的尖锐结构无关极小极大风险，通过新颖的下界证明解决了双重机器学习中双黑箱学习器设定的开放问题。 |
| [^127] | [HyCO: A Hybrid Neural Solver for Combinatorial Optimization](https://arxiv.org/abs/2609.07990) | HyCO提出了一种混合推理算法，先用强化学习求解器构造解的前缀，再自适应切换到条件扩散模型完成剩余决策，并通过统一的误差缩放理论框架证明了这种混合方式能结合两类求解器的优势、规避各自的失效模式。 |
| [^128] | [Automated Chest CT Protocol Selection via Large Language Model Derived Text Embeddings from Imaging Request Text](https://arxiv.org/abs/2609.07986) | 该研究利用微调的LLaMA-3.1-70B大语言模型从28万余份胸部CT影像申请的自由文本中提取嵌入特征并构建自动协议选择系统，克服了传统关键词方法缺乏上下文理解的局限，实现了更一致、高效的CT协议推荐。 |
| [^129] | [Solving the Elastic Wave Equation with Physics-Informed Neural Networks: A Robust and Critical Assessment](https://arxiv.org/abs/2609.07983) | 本文对物理信息神经网络（PINNs）在地震学弹性波方程求解中的应用进行了稳健且批判性的评估，系统考察了其在不同震源、参数模型及不同复杂度问题上的性能表现。 |
| [^130] | [Semi-Supervised Learning under Spatially Biased Sampling](https://arxiv.org/abs/2609.07982) | 该论文系统研究了空间偏倚采样导致半监督学习失效的问题，将边缘分布失配、空间自相关和空间非平稳性作为三种可独立调控的机制，分析了SSL性能退化规律并探索了失效诊断方法。 |
| [^131] | [Clean Accuracy Does Not Guarantee Provenance Robustness: A Prospective Codec-Stress Evaluation of Audio Attribution](https://arxiv.org/abs/2609.07981) | 该论文通过前瞻性注册的编解码器压力实验揭示，在干净数据上表现近乎完美的音频溯源归因模型，在经过单阶段编解码器转码后性能损失可高达约70个Macro-F1点，且退化程度强烈依赖条件与编码器表示，证明干净数据准确率并不能保证溯源归因的鲁棒性。 |
| [^132] | [Heat Field Signatures: From Point Clouds to Smooth Geometry](https://arxiv.org/abs/2609.07975) | 该论文提出热场签名（HFS），将点云提升为多尺度平滑环境热场，从而直接从成对距离闭式计算捕捉内在维度、各向异性和尺度过渡等特性的全局与局部几何签名，并引入热维度谱（HDS）紧凑总结多尺度几何构成，为点云几何分析提供了直接接口。 |
| [^133] | [A Sub-4 Approximation for Fair $k$-Means](https://arxiv.org/abs/2609.07974) | 本文针对公平 $k$-均值聚类问题提出了首个次4倍近似算法，通过将线性规划松弛与几何变换相结合构造候选中心集合，实现了 $1+(3-1/\Gamma)\rho+O(\epsilon)$ 的近似比。 |
| [^134] | [MetaKV: Adaptive KV Cache Compression for Constrained LLM Inference](https://arxiv.org/abs/2609.07966) | MetaKV是一个自适应KV缓存压缩框架，利用轻量级预测模型根据用户指定的延迟和峰值内存预算为每个输入提示动态选择最优压缩配置，在保证准确性的同时满足资源约束。 |
| [^135] | [$\alpha$-Graph: Attention-Infused Normalizing Flow Approach to Tractable Graph Modeling](https://arxiv.org/abs/2609.07961) | 本文提出了一种基于注意力的归一化流方法α-Graph，通过可逆注意力机制实现对图数据的显式、可解释且可处理的建模，能够有效捕获复杂的图结构和输入间的相关性。 |
| [^136] | [The Role of Uncertainty in Assessing the Fairness of Machine Learning Models](https://arxiv.org/abs/2609.07959) | 该论文超越了传统点估计方法，提出用频率学派和贝叶斯方法量化公平机器学习中的不确定性，从而对模型可能存在的偏见进行更严格的风险评估。 |
| [^137] | [Streaming Hierarchical Inference with Tabular Foundation Models](https://arxiv.org/abs/2609.07956) | HINT 提出了一种边缘-云协同的分层推理框架，通过滑动窗口上的图近似最近邻记忆在本地进行预测与不确定性估计，仅将不确定的样本连同检索上下文卸载到云端表格基础模型，从而在预测性能与通信成本之间实现有效平衡。 |
| [^138] | [Support Topology and Gradient Mixing in Sinkhorn Layers](https://arxiv.org/abs/2609.07954) | 本文发展了一套固定支撑演算来分析Sinkhorn层中的梯度传播机制，并证明了传输多面体的每个可行面具有成对两跳列重叠是支撑图与边际保证梯度均匀一步收缩的充要条件。 |
| [^139] | [Structured Extrema Errors in Classical Surrogates for Viscous Burgers: A Physics-Consistent Interpretation](https://arxiv.org/abs/2609.07952) | 该研究发现经典机器学习代理模型在逼近粘性Burgers方程时，其单步残差在极值附近呈现结构化分支，且误差与局部曲率（二阶空间导数）的关联远强于与梯度（一阶导数）的关联，并提出了一个物理一致的基于曲率的残差模型来解释这一现象。 |
| [^140] | [Beliefs and Behavior in Language Models](https://arxiv.org/abs/2609.07943) | 本文提出一种实证方法，检验“信念”这一潜在变量能否用于描述大语言模型的行为，发现高性能模型可以被有效地描述为持有信念，这为理解和对齐LLM行为提供了新途径。 |
| [^141] | [Prevalence calibration as shortcut mitigation](https://arxiv.org/abs/2609.07922) | 该论文创新性地将捷径学习重新定义为校准问题，并提出两种与编码器无关的患病率均衡校准方法（处理中正则化和事后重校准）来有效缓解捷径依赖，且可应用于冻结基础模型编码器。 |
| [^142] | [AVCG: A Generalized Variational Framework for Counterfactual Generation under Hypothesis Distributions](https://arxiv.org/abs/2609.07917) | 提出AVCG框架，将反事实生成从依赖单一确定性预测器推广为对任意合理预测假设分布进行变分优化，有效解决了模型重训练或更新后解释失效的脆弱性问题。 |
| [^143] | [JEDI: JEPA-to-Edge Distillation for Efficient Cropland Segmentation from Satellite Imagery](https://arxiv.org/abs/2609.07915) | JEDI提出了一种两阶段蒸馏框架，通过跨架构投影对齐与任务训练全程持续的特征蒸馏，将大型I-JEPA教师模型的知识迁移到轻量级SegFormer学生模型，仅用4.04M参数即可实现68.0 mIoU的高效卫星影像农田分割。 |
| [^144] | [The Accuracy Paradox: Empirical Diagnostic of Default Decision Thresholds in Multi-Label Enzyme Commission Prediction [With Code]](https://arxiv.org/abs/2609.07897) | 本研究实证诊断了多标签酶委员会预测中默认决策阈值（t=0.50）在严重类别不平衡下的失效，揭示出高准确率（77.16%）掩盖下的预测崩溃：多数类过度预测、少数类召回率骤降，且EC6的决策边界完全失效。 |
| [^145] | [Explainable Temporal Attention-based Defect Detection For Fillet Joints in Real-Time Gas Metal Arc Welding Based on Multi-modal Data](https://arxiv.org/abs/2609.07893) | 本文提出一种基于多模态时序注意力的可解释深度学习模型，利用焊接图像和声音数据实时检测熔化极气体保护焊角焊缝中的内部缺陷，F1分数达到0.99，并通过可解释AI提升了焊接检测的可信度和可靠性。 |
| [^146] | [LLM Layers Immediately Correct Each Other](https://arxiv.org/abs/2609.07876) | 该研究发现了大语言模型中的“Transformer层间修正机制”（TLCM）：相邻层会系统性地相互抵消并修正对方的输出，这一机制普遍存在于主流开源模型中，在预训练期间涌现，并能根据上下文依赖程度自适应地校准修正强度。 |
| [^147] | [InfluenceField: A Differentiable Field with Interventionally Identifiable Causal Structure for Multimodal World Modeling](https://arxiv.org/abs/2609.07874) | 提出 InfluenceField——一种插入视觉编码器与语言解码器之间的干预感知可微潜场，通过共享转移算子多步传播有向影响来预测局部视觉干预的下游效应，并从理论上证明了干预监督下因果结构（有向依赖图）的可辨识性。 |
| [^148] | [SGD in Multiclass Logistic Regression: Sequential Learning and Scaling Laws](https://arxiv.org/abs/2609.07868) | 该论文证明了多类别逻辑回归在高维高斯混合模型上的学习按类别频率从高到低序贯进行，交叉熵风险随训练时间呈幂律衰减，且在固定计算预算下容量项与优化项之间存在精确可刻画的幂律权衡。 |
| [^149] | [Foundation Models for Generalizable Semantic and Goal-Oriented Communication](https://arxiv.org/abs/2609.07853) | 该论文提出FMSGOC框架，利用视觉-语言基础模型先验选择并传输稀疏语义锚点，结合微调的扩散模型在接收端重建图像，有效解决了语义通信在低码率下泛化能力差的问题。 |
| [^150] | [The OCUDU dApp Platform: An Open Runtime and E3 Interface for Real-Time AI-RAN](https://arxiv.org/abs/2609.07843) | OCUDU dApp 平台首次提供了开放运行时和 E3 接口，使经过签名的 AI-RAN 应用能够在生产 5G 分布式单元内部，按照三种时序契约（GPU 接收链驻留、100 微秒调度截止时间、非阻塞观察者）实时运行，填补了机器学习在 10 毫秒以下超低时延区间的开放平台空白。 |
| [^151] | [A*-Thought-V2: Efficient Latent Reasoning via Geometric Dynamics of LLM](https://arxiv.org/abs/2609.07821) | A*-Thought-V2将思维链建模为隐藏状态轨迹，在大语言模型的三维PCA几何空间中测量各推理步骤与全局解答方向的对齐角度，把偏离方向的步骤压缩为连续潜在token、对齐步骤保留为显式文本，从而在保留关键中间信息的同时大幅降低思维链推理的计算与上下文开销。 |
| [^152] | [Kalman Delta Networks: Uncertainty-aware Associative Memory](https://arxiv.org/abs/2609.07816) | 该论文提出卡尔曼增量网络（KDNs），将循环联想记忆重新表述为线性-高斯状态空间模型，利用卡尔曼滤波同时传播记忆状态及其不确定性，使每次记忆写入的强度能够根据积累的证据进行自适应调节。 |
| [^153] | [Latent-MoE: Domain-Aware Mixture-of-Experts for PDEs with Multi-Regime Physics](https://arxiv.org/abs/2609.07814) | 提出Latent-MoE架构，通过在共享骨干网络中嵌入具有紧支撑路由器的域感知混合专家模块，使NTK呈指数衰减的带状结构以实现学习的局部化，从而解决PINNs在求解域内物理特性多变化的PDE时的远程耦合与梯度冲突问题。 |
| [^154] | [You Can't Prefer Emotions You Don't Sample: Intensity Undershoot in DPO-Tuned LLMs](https://arxiv.org/abs/2609.07808) | 本文首次量化了DPO微调的大语言模型在情感控制上的系统性强度不足现象——实际情感增益仅为请求值的0.13-0.26（理想值为1），并将其根源归结为偏好学习训练数据以中性情感为主、采样候选很少触及极端情感这一流程性缺陷。 |
| [^155] | [CodeTD: Topology of Attention Detects Hallucinations in Code LLMs](https://arxiv.org/abs/2609.07779) | CodeTD是首个基于代码大语言模型注意力图拓扑数据分析的执行前幻觉检测方法，可预测生成代码的正确性，性能超越现有基线且能跨编程基准迁移。 |
| [^156] | [Pre-Whitening and BCJR Posterior Distillation for Bi-LSTM Detection in Faster-than-Nyquist Signaling](https://arxiv.org/abs/2609.07762) | 研究发现在超奈奎斯特信号的Bi-LSTM检测中，多窗口嵌套架构无法提升误码率性能，真正的瓶颈在于观测模型而非网络架构，因此提出通过预白化和BCJR后验蒸馏来改进。 |
| [^157] | [Decomposition-Guided Diffusion Language Models for Inertial Confinement Fusion Prediction](https://arxiv.org/abs/2609.07756) | 提出据信首个基于语言模型的惯性约束聚变预测器ICF-DLM，通过物理类型化分解、双向去噪和物理驱动PPO奖励，直接从激光脉冲与靶设计参数准确预测中子率波形。 |
| [^158] | [A Theoretical Analysis of Generalization Dynamics in Neural Networks under Gradient Descent with Weight Decay](https://arxiv.org/abs/2609.07755) | 本文提出了一个理论框架，证明了带权重衰减的梯度下降会收敛到经验损失全局极小值的邻域，并将总体误差分解为数据误差、优化误差和预测变化误差分别进行界定，其中通过局部近似齐次性对预测变化误差给出了明确的逐单元和逐层界。 |
| [^159] | [Cross-modal learning for SAR target recognition using optical vision foundation models](https://arxiv.org/abs/2609.07753) | 本文提出一种跨模态EO到SAR原型对齐框架，利用基于DINOv3的冻结光学编码器构建类别级光学原型，无需严格的EO/SAR配对数据即可为SAR目标识别提供类别级监督，缓解SAR标注数据稀缺和域差距问题。 |
| [^160] | [Local gradient neural operator](https://arxiv.org/abs/2609.07752) | 提出局部梯度神经算子（LGNO），一种基于非线性梯度离散化先验的轻量级、可解释的神经算子，可高效解决力学问题中的场时间演化预测与源识别任务。 |
| [^161] | [Guiding Worker Self-Selection in Crowdsourcing Contests: An LLM-Augmented Algorithmic Approach](https://arxiv.org/abs/2609.07749) | 本文提出贪心多项式时间框架GRAF及由大语言模型驱动的进化框架LLMScore，用于在众包竞赛中为工作者推荐竞赛，在Tullock竞赛自选择模型下实现工作者零遗憾和平台最优性。 |
| [^162] | [Scalability Analysis of Distributed Kolmogorov-Arnold Network Training on High-Performance Computing Systems](https://arxiv.org/abs/2609.07740) | 本文首次系统性地研究了Kolmogorov-Arnold网络在多节点多GPU高性能计算系统上的数据并行训练可扩展性，实验表明在8个GPU上可实现74.7%的并行效率和5.97倍加速。 |
| [^163] | [Attributing Cohen's d: Training Data Attribution for Disease-Related Effects in Normative Age Biomarkers](https://arxiv.org/abs/2609.07729) | 本文提出一种基于Cohen's d的闭式影响泛函，可直接将常模年龄模型中疾病相关的年龄差距效应归因到个体训练样本，并在UK Biobank上证明移除最有影响力的训练样本可显著提高疾病相关效应量。 |
| [^164] | [Sub-6 GHz Over-the-Air AMC via Curriculum Fine-Tuned CNN-Transformers](https://arxiv.org/abs/2609.07726) | 本研究提出一种课程微调策略，将在915 MHz线缆链路数据上训练的CNN-Transformer自动调制分类模型迁移至4 GHz真实空中链路环境，并系统评估了不同距离和天线失准条件下的分类性能。 |
| [^165] | [Spatial Feature-wise Linear Modulation (SpFiLM) for Contrast Agent-Aware Brain Parcellation](https://arxiv.org/abs/2609.07718) | 提出一种调制随空间变化的新型条件化层SpFiLM，使单一网络能够对造影剂注射前后的T1w脑部MRI均实现可靠的脑区分割。 |
| [^166] | [ParetoTransport: Generative Optimization by Mass Transport Toward The Pareto Front](https://arxiv.org/abs/2609.07706) | 提出ParetoTransport，一种无需训练的流匹配模型引导方法，利用质量传输与Wasserstein匹配将离线设计的分布迭代推向帕累托前沿并在其上有效分布，从而在种群分布层面实现生成式多目标优化。 |
| [^167] | [Emergent Charging Coordination in Electric Delivery Fleets](https://arxiv.org/abs/2609.07689) | 提出仅依靠局部信息（自身时间预算和广播的充电站占用情况）运行相同策略的学习智能体，使电动配送车队无需中央控制即可实现涌现式充电协调，在二十个城市的真实路网仿真中按时完成99.5%的班次，远超贪心规则的73%。 |
| [^168] | [On the Recall Scaling Laws in Mamba: A Theoretical and Mechanistic Study via Hashing](https://arxiv.org/abs/2609.07681) | 本文通过机制可解释性研究揭示Mamba通过隐式学习线性哈希函数来执行关联回忆，并借助Johnson-Lindenstrauss引理等相似性保持哈希理论工具，提出了可预测其上下文记忆能力的“回忆缩放定律”理论框架。 |
| [^169] | [MpSub: A Momentum $p$-Dimensional Subspace Trust-Region Method for Derivative-Free Fine-Tuning of Large Language Models](https://arxiv.org/abs/2609.07666) | 提出了一种动量p维子空间信赖域方法MpSub，通过结合历史动量方向的子空间搜索和基于预测与实际损失下降一致性的自适应信赖域半径调整，消除了学习率调参需求，实现了大语言模型内存高效的无导数全参数微调。 |
| [^170] | [Accuracy is Not Enough: A Divergence-Based Approach to Evaluate Fidelity Loss in Quantized LLMs](https://arxiv.org/abs/2609.07664) | 该论文提出了一种分布敏感的评估框架，通过计算量化模型与全精度模型在token决策边界处全词表预测分布之间的统计距离（如Jensen-Shannon散度和总变差距离）来量化保真度损失，弥补了仅依赖准确率评估量化模型质量的不足。 |
| [^171] | [Online Surrogate Repair: Decoupling High-Fidelity Feedback from Search Length in Closed-Loop Discovery](https://arxiv.org/abs/2609.07655) | 该论文提出在线代理修复（OSR）这一闭环算法，利用稀疏的高保真评估在长时间的智能体搜索中持续更新低成本代理模型，从而将高保真反馈与搜索长度解耦，避免固定代理模型的误差被优化过程放大。 |
| [^172] | [The Art of Hierarchical Competing Patterns: Gaussian Process Optimization of Hyphenation](https://arxiv.org/abs/2609.07638) | 该论文将连字模式生成中 patgen 参数配置的选择形式化为黑盒超参数优化问题，通过高斯过程贝叶斯优化，在涵盖 14 种语言的 17 个数据集上几乎全面提升了断词 F_{1/7} 分数，同时将字典树体积中位数缩减至基线的 0.407。 |
| [^173] | [Norms at a Price: Why RL-Based Alignment Can Promise Conditional Compliance at Best](https://arxiv.org/abs/2609.07627) | 论文论证了基于强化学习的对齐因从打分行为中学习规范，使“合规”退化为“被发现才付出代价”，因此行为训练在原理上至多只能保证智能体在被观察时才合规的有条件合规。 |
| [^174] | [Forecasting the Winner of a Live Tennis Match](https://arxiv.org/abs/2609.07617) | 提出了一种名为Trace的混合模型，通过有效整合赛前信息与实时比赛数据来预测网球比赛获胜概率，在大满贯比赛数据上于比赛进行25%、50%和75%时分别取得76.06%、82.15%和88.34%的准确率。 |
| [^175] | [Translation of Black-Box Clinical Prediction Models into Standalone Transparent Nomograms: Temporal External Validation in Heart Transplantation](https://arxiv.org/abs/2609.07610) | 该研究提出PRiSM方法，可将黑盒临床预测模型转化为可逐项审计的透明列线图，并在5万余例心脏移植数据的时间外部验证中证明其在保持区分度、校准度和临床净获益的同时实现了模型透明化。 |
| [^176] | [CLUES-WEASEL: No additional clues required to choose your time series clustering algorithm](https://arxiv.org/abs/2609.07606) | 提出了CLUES-WEASEL，一种结合WEASEL 2.0无监督特征提取、主成分分析降维和k-means聚类的新颖时间序列聚类算法，在聚类性能与运行效率之间实现了良好平衡。 |
| [^177] | [Beyond the Matrix Sign: Quadratic Spectral Descent](https://arxiv.org/abs/2609.07597) | 本文提出二次谱下降（QSD），在保持Muon谱范数约束的前提下将线性局部模型替换为二次模型，证明曲率会改变最优更新的奇异值和奇异方向，并通过Kronecker分解曲率近似与少量Frank–Wolfe步骤使该方法实用化。 |
| [^178] | [I Don't Miss You, but I Do: Self-Explanation Faithfulness of Modality Missingness in Vision-Language Models](https://arxiv.org/abs/2609.07596) | 该论文提出了一种介入式评估协议，揭示视觉语言模型对模态缺失的自我解释并不忠实——它们系统性地高估现有模态证据的充分性，并大幅低估恢复缺失模态对预测结果的影响。 |
| [^179] | [Efficient Exploration Is Enough](https://arxiv.org/abs/2609.07575) | 这篇论文证明了在无外在奖励的情况下，智能体若能高效地优先探索信息量最大、最可学习的区域以生成可泛化经验，仅凭这一纯内在目标就足以自动形成课程并涌现出高度复杂的行为。 |
| [^180] | [No-Regret Mixing of LRU and LFU with Optimal Switching Cost](https://arxiv.org/abs/2609.07566) | 本文证明了现有混合缓存策略LeCar在遗忘型对手下会产生线性遗憾，并提出了一种基于Hedge的虚拟LRU与LFU缓存混合策略H-MC，能够在最优切换开销下实现对LRU与LFU较优者的无遗憾（次线性遗憾）性能保证。 |
| [^181] | [EigenLI: Spectral Approximations to Late Interaction](https://arxiv.org/abs/2609.07561) | 本文提出EigenLI谱近似框架，利用后期交互表示的内在低秩结构，通过提取每个文档的主导特征方向构建低维子空间来压缩表示，在降低索引与存储成本的同时取得了优于聚类池化方法的检索效果。 |
| [^182] | [SeisBench DAS: A machine learning framework for Distributed Acoustic Sensing](https://arxiv.org/abs/2609.07558) | SeisBench DAS通过对DAS数据集和模型进行标准化，搭建了模型开发者与数据分析师之间的桥梁，推动了深度学习在分布式声学传感数据处理中的广泛应用。 |
| [^183] | [Heat Kernel Textures: the Geodesic Gaussians That Do Not Splat](https://arxiv.org/abs/2609.07557) | 提出热核纹理（HKTex），一种以各向异性热核作为测地高斯的内在纹理表示方法，可直接在三角网格表面进行优化，彻底消除UV展开带来的空间浪费、接缝、畸变、顶点重复和分辨率不一致等问题，并显著降低内存占用。 |
| [^184] | [CoRL: Co-Evolutionary Reinforcement Learning for Adaptive Indirect Prompt-Injection Attacks and Defenses](https://arxiv.org/abs/2609.07529) | 该论文提出CoRL框架，将自适应间接提示注入攻防建模为非对称、部分可观测的马尔可夫博弈，通过攻击者SFT、双边Co-PPO协同进化和防御者SFT三个阶段实现攻防双方的共同进化，从而训练出能够抵御不断变化攻击策略的鲁棒防御能力。 |
| [^185] | [Statistical versus machine learning-based spatial interpolation of post-processed ensemble weather forecasts](https://arxiv.org/abs/2609.07512) | 本研究比较了统计与机器学习方法对集合天气预报进行后处理及空间插值的效果，并提出了一种海拔感知线性池化（ALP）方法，在无观测地点的温度预报上相比标准线性池化取得了小幅但显著的改进。 |
| [^186] | [Generation of Vectorized Maps Beyond Vehicle View](https://arxiv.org/abs/2609.07511) | 本文提出了超视野矢量化地图生成这一新问题，并首次提出BeyondFormer模型，能够基于车辆视野内的矢量化地图生成视野之外合理的地图延续，同时构建了首个专门针对该任务的数据集。 |
| [^187] | [Improving Multivariate Time Series Classification with Class-Wise Training and Model Aggregation](https://arxiv.org/abs/2609.07493) | 该论文提出了一种逐类维度选择与模型聚合框架，为多元时间序列分类中的每个类别独立筛选信息丰富的维度并进行专门学习后再融合预测，从而提升判别性特征表示质量与分类性能，尤其在高维场景中效果突出。 |
| [^188] | [Temporal-Causal Inference for Reinforcement Learning via Automata Learning](https://arxiv.org/abs/2609.07461) | 该论文提出TCIRL框架，通过联合学习控制策略与基于假设确定性有限自动机的时序因果推断来推断隐藏的相变原因，并证明了其几乎必然收敛到真实原因语言从而获得最优策略。 |
| [^189] | [Latent-to-Latent Flow for Volumetric Stochastic Segmentation](https://arxiv.org/abs/2609.07460) | 该论文提出一种基于潜空间到潜空间流匹配的医学体积随机分割方法，利用图像和标签空间的编码表示，在放射治疗勾画不确定性和多器官分割任务上实现了比全分辨率模型最高14倍的效率提升。 |
| [^190] | [Graph neural networks and the energetic cavity method for combinatorial optimization](https://arxiv.org/abs/2609.07456) | 通过对图神经网络进行微小修改以融入经典平均场启发式方法（主特征向量法与能量腔/最小和算法），可显著提升其在求解伊辛模型基态及组合优化问题上的性能。 |
| [^191] | [Parser-Free VLM Verification for Federated Weakly Supervised Video Anomaly Detection](https://arxiv.org/abs/2609.07455) | 提出一种轻量级联邦MIL-VLM级联方法，仅在客户端训练紧凑的MIL评分器并利用冻结VLM事后验证可疑片段，其中基于logit的无解析器接口相比文本生成验证能更稳定地提升弱监督视频异常检测性能。 |
| [^192] | [TASTE: Throughput-Aware Batch Size Tuning for On-Device Edge Learning](https://arxiv.org/abs/2609.07444) | 本文提出了TASTE，一种基于贝叶斯优化的批量大小调优方法，用于在边缘设备上训练深度学习模型时最大化硬件吞吐量，并发现存在吞吐量上限，超过该上限后增大批量大小不再带来额外收益。 |
| [^193] | [TabBench-Bio: A Living Benchmark for Machine Learning on High-Dimensional Biomedical Tables](https://arxiv.org/abs/2609.07441) | TabBench-Bio是一个涵盖43个生物医学数据集的动态基准测试，系统评估了经典方法、神经网络和表格基础模型在高维小样本场景下的表现，发现表格基础模型总体领先，其中RealTabPFN v2.5表现最佳。 |
| [^194] | [A Systematic Analysis of Automatic Differentiation versus Discretization-based Constraints for Physics-Informed PDE Solvers](https://arxiv.org/abs/2609.07437) | 该论文通过从简单线性泊松方程到强不连续高超音速流动的广泛系统性实验，首次系统分析了物理信息PDE求解器中自动微分与基于离散化约束两种范式的各自适用性。 |
| [^195] | [Revisiting Thinning Methods for Kernel Learning Problems](https://arxiv.org/abs/2609.07432) | 提出反向核聚合算法，通过迭代移除数据点构建代表子集，在加速子采样的同时达到与最先进方法相当的效果，并扩展核稀疏化方法使其能够构建任意大小的子集。 |
| [^196] | [Masking Radar Cognition under Adversarial Surveillance: A Distributional Privacy Framework](https://arxiv.org/abs/2609.07428) | 该论文提出了一种基于分布式隐私的电子反对抗框架，通过开发两种认知隐藏算法，在对抗监视下遮蔽认知雷达的战略决策过程并提供正式的隐私保证。 |
| [^197] | [RelightFormer: Feed-forward Generative Transformer for Multiview Object Relighting](https://arxiv.org/abs/2609.07414) | 该论文提出RelightFormer，一种基于视频基础模型改编的前馈生成式Transformer，通过潜在光照模块和置换不变位置编码实现无需显式逆渲染的单视图与多视图图像重照明，并构建了包含9万物体和3.9万种光照的大规模LOD数据集支撑训练。 |
| [^198] | [Multi-label versus multi-class classification of blood cells and their aggregates in microfluidic channels](https://arxiv.org/abs/2609.07410) | 本文提出在形变细胞术中用多标签分类替代传统多类别分类来识别血细胞及其聚集体，能够识别训练数据中未出现过的细胞聚集体，同时简化并加快了数据标注过程。 |
| [^199] | [RAFM-SER++: A Lightweight Multimodal Emotion Recognition Framework for Real-Time Behavioral Monitoring in Surveillance Systems](https://arxiv.org/abs/2609.07409) | 提出了一种轻量级多模态语音情感识别框架RAFM_SER++，通过非对称单向残差注意力融合机制在低计算开销下实现高性能情感识别，适用于资源受限的实时监控系统。 |
| [^200] | [Impact of canny edge detection preprocessing on performance of machine learning models for Parkinson's disease classification](https://arxiv.org/abs/2609.07408) | 本研究系统评估了多种机器学习模型在帕金森病分类任务中的表现，发现增大训练数据集会增加模型内存占用和预测延迟，而Canny边缘检测结合Hessian滤波的预处理方法反而会降低模型分类性能。 |
| [^201] | [Think Wider: Mitigating Latent Rank Collapse in Implicit Chain-of-Thought Reasoning](https://arxiv.org/abs/2609.07406) | 提出WIDER——一种即插即用的轻量级谱正则化方法，通过惩罚潜在状态向共享主导方向的投影来缓解隐式思维链推理中的潜在秩坍缩问题，使潜在状态扩展到更广泛的表示子空间并提升推理多样性。 |
| [^202] | [Federated Binary Gating with Server-Side Vision-Language Inference for Surveillance Anomaly Classification](https://arxiv.org/abs/2609.07403) | 该论文提出一种混合两阶段架构，利用轻量级联邦二值CNN门控在本地进行异常筛查，仅将可疑视频传输至服务器端由零样本视觉-语言模型（Qwen3-VL-8B）进行元类别分类，从而在隐私敏感且数据非独立同分布的监控场景中避免了直接联邦多类训练的崩溃问题。 |
| [^203] | [Riemannian Optimization for Multi-Player Quantum Games on Product Unitary Manifolds](https://arxiv.org/abs/2609.07397) | 本文提出了面向混合策略多人量子博弈的扩展EWL（EEWL）框架，并引入酉策略矩阵指数算法，通过在乘积酉流形上进行黎曼优化来求解多人量子博弈的均衡问题。 |
| [^204] | [Distributed Lag Neural Additive Models](https://arxiv.org/abs/2609.07381) | 该论文提出分布式滞后神经加性模型（DLNAM），用神经网络组件替代预先指定的样条交叉基来学习暴露-滞后非线性效应，在保持加性可解释性的同时避免了基函数选择问题，并在模拟中相比传统DLNM实现了更低的偏差、更强的边界恢复能力和更好校准的不确定性估计。 |
| [^205] | [Inferring Urban Mobility Interactions from Aggregated Dynamics](https://arxiv.org/abs/2609.07349) | 仅利用城市已有的聚合区域计数数据，通过不确定性感知的物理信息框架即可准确推断未来的出发地-目的地流量，无需追踪个体轨迹，在保护隐私的同时达到与依赖历史OD矩阵的模型相当的精度。 |
| [^206] | [Content-Based Addressing for Long Context](https://arxiv.org/abs/2609.07314) | 提出一种基于内容寻址的长上下文位置编码方法，将 token 流划分为单元、单元内保留 RoPE 并为每个完成的单元按内容计算地址，从而在扩展上下文时完全保留局部 RoPE 并使固定 token 间的注意力比较不受其他单元插入或重排的影响。 |
| [^207] | [Robust Decentralized Personalized Federated Learning via Prediction-Constrained Neighborhood Collaboration](https://arxiv.org/abs/2609.07312) | 该论文提出R-DPFL方法，通过对比邻域聚合更新与基于历史的预测更新之间的差异并进行自适应裁剪，使诚实客户端在拜占庭攻击下无需邻居共识即可保持稳定的个性化联邦学习动态。 |
| [^208] | [Long-Horizon Language Model Reinforcement Learning via Progressive Point Matching](https://arxiv.org/abs/2609.07303) | 该论文提出“渐进点匹配”——一种简单无偏的片段级密集奖励方法，仅需每个任务一条参考轨迹即可实现，使强化学习在稀疏奖励完全失效的长时程任务（如极难数学推理）上获得指数级效率提升。 |
| [^209] | [PCFlow: Physics-Conditioned Flow Matching for GPR B-Scan Image Synthesis](https://arxiv.org/abs/2609.07300) | 提出PCFlow框架，通过基于麦克斯韦方程的密集物理条件场引导条件流匹配，实现快速且兼具视觉真实性与物理一致性的探地雷达B扫描图像合成。 |
| [^210] | [Constitutive State-Space Modeling of Path-Dependent Plasticity: A Resolution-Consistent and Parallelizable Computational Framework](https://arxiv.org/abs/2609.07294) | 本文提出一种本构状态空间（CSS）模型，通过将应变增量分解为方向与幅值并嵌入结构化状态空间动力学，实现了路径依赖塑性建模中对应变路径分辨率的一致性，同时保留并行扫描结构以支持长本构历史的高效训练。 |
| [^211] | [Matryoshka Hash Representations for Model-Aware Compact Semantic Retrieval](https://arxiv.org/abs/2609.07276) | 该论文提出俄罗斯套娃式哈希表示，使同一个压缩编码的各长度前缀均可直接检索，从而让检索系统无需重新编码语料库就能灵活地在不同字节预算下切换效率与质量的权衡点。 |
| [^212] | [Dense Structural Compression of Transformers via Gauge-Correct Channel Removal](https://arxiv.org/abs/2609.07264) | 提出GaugeLasso方法，通过规范校正的加性对称组套索通道惩罚，在训练中将Transformer的冗余通道驱动至零并物理移除，同时保持张量稠密性和网络功能，从而降低推理计算成本与能耗。 |
| [^213] | [Kolmogorov--Arnold stability for discontinuous functions](https://arxiv.org/abs/2609.07240) | 本文证明了Kolmogorov-Arnold表示定理对于多元不连续且无界函数在隐藏层受到对抗性重参数化时依然保持稳定，为KAN等现代深度学习架构的结构鲁棒性奠定了严格的数学基础。 |
| [^214] | [Parallelism Strategy Chaining for Fast Training Convergence](https://arxiv.org/abs/2609.07236) | 本文提出CONA训练方法，通过在训练过程中在线动态串联切换最佳并行策略，而非离线固定单一策略，使大型语言模型达到目标验证困惑度的时间缩短1.8-11.4倍。 |
| [^215] | [Robust Decentralized Federated Distillation via Multi-Modality Knowledge Collaboration](https://arxiv.org/abs/2609.07230) | 本文提出一种基于多模态知识协作的鲁棒去中心化联邦蒸馏方法，通过对类别预测、边界决策和预测相关性三种模态的评估、过滤不可靠客户端、并利用私有数据的监督梯度验证蒸馏梯度来抵御拜占庭攻击，同时证明了方法的收敛性。 |
| [^216] | [Iterative Audio Separation with Mixture Consistency via MIMO Model Extension](https://arxiv.org/abs/2609.07226) | 本文通过将源分离模型扩展为多输入多输出（MIMO）配置，提出了一种兼具混合一致性与迭代预测能力的通用音频分离框架。 |
| [^217] | [REFINE: Trajectory Representation Learning via Closed-Loop Transcription -- Extended Version](https://arxiv.org/abs/2609.07206) | REFINE借鉴反馈控制理论，将路网感知的生成式重建与反馈驱动的对比学习进行闭环耦合，实现了无需人工数据增强的高质量轨迹表示学习，并具有理论收敛保证。 |
| [^218] | [Protocol effects on feature-based hardware-Trojan detection across Trust-Hub families](https://arxiv.org/abs/2609.07199) | 该论文发现基于特征的硬件木马检测性能严重依赖测试集划分协议——当测试门电路来自训练中已见过的宿主电路时性能被高估，而按宿主家族留出时 F1/AP 会从约 0.91/0.98 骤降至约 0.46/0.58。 |
| [^219] | [FedRAW: Preserving Rare-Label Influence in Asynchronous Federated Learning](https://arxiv.org/abs/2609.07192) | 提出FedRAW，一种完全服务器端的异步联邦学习聚合方法，通过同时解决参与偏差和聚合偏差，防止持有稀有标签的客户端对全局模型的贡献被静默削弱。 |
| [^220] | [Tensor network representations of discrete maximum entropy distributions via mean polytopes](https://arxiv.org/abs/2609.07184) | 本文引入计算-激活网络这一新型张量网络架构，利用期望向量凸多面体的几何性质，为期望约束下的离散最大熵分布提供了统一的张量网络表示，并提出将张量网络秩作为多面体面的复杂度度量。 |
| [^221] | [CircuitLens: Reasoning Circuits as Data Selection Signals for Reinforcement Learning with Verifiable Rewards](https://arxiv.org/abs/2609.07183) | 本文提出基于46个推理敏感注意力头的推理电路得分（CRS）作为RLVR的数据选择信号，并发现反直觉的结果——推理电路激活程度最低的数据反而能带来最佳的训练效果。 |
| [^222] | [The Oversight Gap: What LLM Safety Monitors Miss, and Why It Is Not Capability](https://arxiv.org/abs/2609.07162) | 该论文提出了“监督缺口”的概念，给出单轨迹监控器检测2-安全性超性质的紧致理论界限，并通过实验证明LLM安全监控器的失效主要源于缺乏明确的检测规范而非能力不足。 |
| [^223] | [Mind the Approximation: Fisher-Weighted SVD Compression for ViTs](https://arxiv.org/abs/2609.07155) | 本文发现提高Fisher近似保真度并不能有效预测ViT压缩后的精度，据此提出结构化Fisher近似方法FACTS及快速的约束秩搜索CoRS，在固定FLOP约束下实现视觉Transformer的高效Fisher加权SVD压缩。 |
| [^224] | [Stable-MM-R1: Anchoring Multimodal Reasoning Dynamics via Entropy-Guided Stratification](https://arxiv.org/abs/2609.07148) | 该论文提出 Stable-MM-R1 框架，通过潜能感知查询挖掘（PAQM）动态筛选高能力激发潜力的样本，并结合基于路径熵与结果奖励分层的混合分层重放（HSR）机制构建高对比度优化组，从而解决多模态强化学习训练中的熵坍缩与不稳定问题。 |
| [^225] | [Fine-grained Distributed Backdoor Attacks in Federated Learning](https://arxiv.org/abs/2609.07147) | 本文提出细粒度分布式后门攻击框架FDBA，通过基于Canny边缘特征的动态触发器生成和RGB通道分解等嵌入向量优化技术，在联邦学习中以更少的中毒样本实现更隐蔽的分布式后门攻击。 |
| [^226] | [EEG-Driven Decoding Framework for Passenger Hazard Perception in Highly Automated Vehicles](https://arxiv.org/abs/2609.07128) | 该论文提出了一种基于脑电图的脑机接口框架，通过乘客认知模型、风险感知序列标注和集成了3D-CRNN的乘客脑电解码策略，以非侵入方式解码自动驾驶车辆乘客的神经响应，在风险预测中达到95.3%的平衡准确率，并显著提升了危险识别性能。 |
| [^227] | [Beyond Task Success: Stage-Wise Reliability of World Model Planning under Sensing Degradation](https://arxiv.org/abs/2609.07126) | 本文通过对世界模型规划器施加10种感知退化并进行跨阶段配对评估，揭示感知扰动的影响在表示、预测、规划偏好和物理结果各阶段间并非均匀传递，因此仅凭任务成功无法可靠评估世界模型规划的抗扰性能。 |
| [^228] | [Online Draft Co-Training for Speculative Decoding in Large-Scale, Long-Context RL Post-Training](https://arxiv.org/abs/2609.07108) | 该论文提出了一个端到端系统，通过在上下文并行中融合分支注意力与主序列注意力、并引入TapChannel独立通道跨流水线阶段传输目标特征，实现了大规模长上下文强化学习后训练中投机解码草稿模型的在线协同训练，从而显著加速rollout生成。 |
| [^229] | [Temporal Heterogeneous Graph Transformer for Credit Card Fraud Detection](https://arxiv.org/abs/2609.07100) | 提出THGT-FD模型，将每笔交易表示为一个交易令牌与六种关系令牌并融合Time2Vec时间编码，利用Transformer学习交易内令牌间交互来进行信用卡欺诈检测，其性能接近但尚未超越梯度提升基线。 |
| [^230] | [Conditioned Initialization for Attention](https://arxiv.org/abs/2609.07086) | 提出条件化初始化方法，通过改善注意力层的谱特性来初始化查询、键和值权重，从根本上优化Transformer的训练动态。 |
| [^231] | [Detect Anything in Graphic Design: Element-Level Rewards for Autoregressive Detection](https://arxiv.org/abs/2609.07072) | 提出 DAD 模型，将图形设计检测建模为按组合顺序解码的组合解构，实现能预测被遮挡完整边界的非模态检测，并提出将 GRPO 扩展至元素级奖励的 EleRPO 方法。 |
| [^232] | [Trust-But-Verify: Poisoning-Resilient Locally Private Graph Learning Protocols](https://arxiv.org/abs/2609.07063) | 本文提出VERITAS，一种基于“信任但要验证”范式的抗投毒本地私有图学习协议，通过分级同伴信任验证机制有效抵御数据投毒攻击，在保护隐私的同时维持图学习效用。 |
| [^233] | [PhysSAE: Mechanistic Interpretability with Sparse Autoencoders](https://arxiv.org/abs/2609.07061) | 提出了PhysSAE框架，通过在PINN倒数第二层激活上训练稀疏自编码器并直接在隐藏状态上做因果干预，发现了与物理可观测量高度对齐且因果足迹更加空间集中的可解释物理特征。 |
| [^234] | [TrojanWorld: Backdooring World-Model Agents via Imagination Steering](https://arxiv.org/abs/2609.07051) | 该论文提出TrojanWorld——一个针对世界模型智能体的后门攻击框架，通过在场景中放置物理触发物来引导智能体的内部想象，从而在不数字篡改观测流的情况下，隐蔽且持久地诱发攻击者指定的行为。 |
| [^235] | [AI and TCAD for Inverse Design and Defect Discovery: From Simple Machine Learning to LLM](https://arxiv.org/abs/2609.07046) | 该论文提出利用TCAD仿真生成精确数据来增强机器学习，通过基于自编码器的模型与噪声工程学习潜在物理规律，在极少领域知识的情况下构建出媲美器件工程师的模型，实现半导体器件的逆向设计与缺陷发现。 |
| [^236] | [Disentangling Steering Vectors](https://arxiv.org/abs/2609.07037) | 该论文提出“转向向量解剖”框架，通过在正负激活差生成的实例级转向向量上训练稀疏自编码器，将复合转向向量中纠缠的多个语义和风格概念解耦分离，从而实现对大语言模型行为更可预测的精准引导。 |
| [^237] | [Aha-Flow Distillation: Flow Markers Matter in LLM Reasoning](https://arxiv.org/abs/2609.07036) | 该论文发现了一种与顿悟时刻互补的“心流时刻”推理模式，并提出Aha-Flow蒸馏方法，通过双模式在线自蒸馏将心流标记作为辅助监督，从而提升大语言模型的推理能力。 |
| [^238] | [Efficient Learning and Symmetry Discovery under Exact Invariances](https://arxiv.org/abs/2609.07031) | 该论文首次提出了统一适用于有限群和无限群的精确群不变性学习多项式时间算法，解决了无限群和未知对称性情形下高效学习的开放性问题。 |
| [^239] | [HyperTransfer: Understanding the Equivalence between Base Optimizer and Hyperball](https://arxiv.org/abs/2609.07017) | 该论文证明了超球优化器与传统基础优化器在尺度不变网络上动力学等价，并据此提出 HyperTransfer 方法，仅需目标基础优化器的初始化和学习率调度即可构建出复现其动力学的超球优化器，无需运行原优化器。 |
| [^240] | [AstraMoE-SR: Trajectory-Guided Diffusion for Blind Satellite Jitter Deblurring and Super-Resolution](https://arxiv.org/abs/2609.07012) | 提出AstraMoE-SR框架，将卫星抖动退化重新参数化为推扫几何下的局部曝光轨迹，并利用条件扩散模型估计轨迹分布，实现单图像、无需辅助测量的运动模糊去除与超分辨率联合复原。 |
| [^241] | [NeuCME: Toward Dynamic Multimodal Continual Learning via Neural Combinatorics of Multiple Experts](https://arxiv.org/abs/2609.07009) | 提出NeuCME框架，通过多专家神经组合学应对动态多模态持续学习中模态集合随任务变化的挑战，解决时空灾难性遗忘与自适应多模态融合两大核心问题。 |
| [^242] | [LatentMD: Benchmarking Markdown Boundary Failures in LLM-Generated Text](https://arxiv.org/abs/2609.06993) | LatentMD是一个将内容正确性与边界正确性分离的基准测试，它揭示了LLM生成的Markdown中边界失败问题普遍存在——38%的内容正确输出实际上边界已损坏。 |
| [^243] | [Continual Learning Mechanisms Compose for Long-Horizon Memorization](https://arxiv.org/abs/2609.06986) | 该论文提出长时程记忆设置，发现任何单一持续学习机制都无法在100个连续任务的顺序学习中避免灾难性遗忘，而通过组合解决互补遗忘来源的机制（数据/函数/权重锚点与低秩分配规则）可以实现有效的长期记忆保留。 |
| [^244] | [AF-Mamba: Efficient Long-Term Signal Modeling for Early Prediction of Atrial Fibrillation Onset](https://arxiv.org/abs/2609.06984) | 提出了一种融合时序卷积网络与Mamba状态空间模型的混合架构AF-Mamba，能够基于一小时长期RR间期信号高效预测房颤发作，突破了Transformer二次方扩展的限制。 |
| [^245] | [Input-to-State Stability Framework for Fully Distributed Primal-Dual Dynamics for Quadratic GNEPs Without Multiplier Consensus](https://arxiv.org/abs/2609.06983) | 本文提出了一种无需乘子共识的全分布式原始-对偶动力学方法来求解二次广义纳什均衡问题，并基于输入到状态稳定性（ISS）框架证明了算法的收敛性，减少了通信需求并提升了隐私性。 |
| [^246] | [HealthLoopQA: A Context-Aware Question Answering Benchmark for Interpreting Wearable Monitoring Data in Diabetes Care](https://arxiv.org/abs/2609.06976) | 该论文提出了HealthLoopQA，一个情境感知的综合诊断基准，用于评估大型语言模型在解读糖尿病连续可穿戴监测数据方面的推理能力，弥补了现有基准忽视长期模式、治疗行为情境和真实部署中系统故障的不足。 |
| [^247] | [AgentDrift: A Step-Labeled Benchmark of Injection-Hijacked LLM Agent Trajectories](https://arxiv.org/abs/2609.06972) | 提出了AgentDrift基准，包含五个领域的12,536条合成工具调用轨迹，首次逐步标注了间接提示注入进入LLM智能体轨迹的位置及其破坏的具体步骤。 |
| [^248] | [MOLE: Detecting Insider Threats in AI Agents](https://arxiv.org/abs/2609.06966) | MOLE是一个检测AI智能体内部威胁的开放基准测试，包含150个账户、12种威胁和约200亿token，研究发现72%的智能体模型能完成大部分有害目标，而即使最佳监控器也会遗漏近一半已完成的危害。 |
| [^249] | [Steering Interference Reflects the Model's Defaults, Not the Behavior Directions](https://arxiv.org/abs/2609.06951) | 激活导向引发的副作用并非来自被导向的行为方向本身，而是由模型自身的默认偏好决定——无论导向何种行为，模型都会趋向其本已偏好的少数行为（如拒答、谄媚、诗歌化）。 |
| [^250] | [Particle Dynamics of Flow Matching and Classifier-Free Guidance from a Stagewise Geometry Perspective](https://arxiv.org/abs/2609.06947) | 该论文建立了流匹配与无分类器引导的统一分阶段几何理论，证明采样轨迹从噪声到数据会依次被吸引向全局均值邻域、数据凸包和局部聚类邻域，且各阶段均满足统一的收缩估计。 |
| [^251] | [PCSDiff: Diffusion-Based Bias Correction and Super Resolution Toward Practical Operational Medium-Term Precipitation Forecast](https://arxiv.org/abs/2609.06942) | PCSDiff提出了一种级联任务解耦的扩散框架，结合降水强度感知多分支解码器（PIMD）和两阶段条件扩散超分辨率模块，实现10天中期降水的动态偏差校正与高分辨率重建，满足业务化部署需求。 |
| [^252] | [The Geometry of Refusal: Why Post-Hoc Safety Is Fragile and Pretraining-Time Safety Persists](https://arxiv.org/abs/2609.06934) | 该论文从几何视角证明，事后安全训练（如RLHF）的更新与模型能力方向近乎正交，只是在完好的能力之上叠加一道薄而尖锐的拒答“闸门”而非真正删除能力，因此注定会被越狱等攻击绕过，而持久的安全必须在预训练阶段扎根。 |
| [^253] | [Distributed Dexterous Manipulation with Spatially Conditioned Multi-Agent Transformers](https://arxiv.org/abs/2609.06930) | 本文提出基于空间条件化多智能体Transformer的框架，通过自适应层归一化、空间对比嵌入以及结合Soft Actor Critic微调的行为克隆方法，实现了由64个软体delta机器人组成的分布式灵巧操作系统的高效鲁棒控制。 |
| [^254] | [Constrained Online Learning with Noisy Constraint Values](https://arxiv.org/abs/2609.06921) | 针对约束值受噪声污染的约束在线凸优化，本文证明了联合次线性保证的不可能性，并提出LEDGER算法，通过引入预算违反新度量，实现了 $O(\sqrt T/V)$ 的期望遗憾和可控的预算违反保证。 |
| [^255] | [From Synthetic Priors to Model Behavior: Structural Coverage in Tabular Foundation Models](https://arxiv.org/abs/2609.06912) | 该论文从分布层面归因视角出发，通过结构覆盖度和归一化密度量化表格基础模型的合成预训练先验对下游基准任务的支持范围，并探究这种结构支持与预测性能之间的关联。 |
| [^256] | [Smoothed Picard Hamiltonian Monte Carlo](https://arxiv.org/abs/2609.06906) | 该论文提出平滑皮卡德哈密顿蒙特卡罗采样器，通过融合高斯平滑、皮卡德迭代和高阶离散化，对对数凹分布采样实现了改进的复杂度保证，并借助递归温启动生成器框架将其升级为高精度采样方法。 |
| [^257] | [Accelerated High-Accuracy Sampling from a Warm Start via the Proximal Bouncy Particle Sampler](https://arxiv.org/abs/2609.06905) | 该论文提出了结合近端采样器与弹球粒子采样器思想的新型采样器 Proximal BPS，在暖启动条件下期望仅需 Õ(√κ d^{1/4} polylog(1/ε)) 次梯度查询即可在总变差距离上实现高精度采样。 |
| [^258] | [Dynamic-Programming-Guided Hierarchical BPE and Empirical Analysis of Vocabulary Pruning](https://arxiv.org/abs/2609.06898) | 提出DH-BPE方法，结合动态规划度量的词元效用与BPE分层依赖关系，通过暴露度引导的剪枝构建固定大小的模型可见词表，在压缩性能上优于标准BPE及现有词表优化基线。 |
| [^259] | [Towards Bridging the Gap Between Offline and Iterative Alignment via Preference Distillation](https://arxiv.org/abs/2609.06893) | 该研究揭示迭代对齐方法优于离线方法的关键在于其额外引入的显式偏好模型，并据此提出DP3O算法，通过利用辅助LLM进行偏好蒸馏，将迭代方法的优势成功融入高效的离线对齐框架中。 |
| [^260] | [Noisy-Space Policy Gradient for Diffusion Policies in Offline Reinforcement Learning](https://arxiv.org/abs/2609.06882) | 该论文提出了一种噪声空间动作值函数与噪声空间策略梯度（NSPG）方法，使扩散策略仅凭干净动作空间的值估计即可优化噪声潜变量，并通过扩散兼容的回归形式避免了通过去噪过程的反向传播，有效解决了扩散模型与离线强化学习结合的难题。 |
| [^261] | [Learning Adaptive SED for heterogeneous load balancing](https://arxiv.org/abs/2609.06881) | 针对服务速率未知的异构两服务器负载均衡系统，提出了一种在线学习SED路由算法，通过平衡经验路由与强制探索阶段，首次实现了有限遗憾（而非经典多臂老虎机中的对数增长遗憾）。 |
| [^262] | [Contextual Observer Grounding: Evaluating Situated Spatial Reasoning in Vision-Language Models](https://arxiv.org/abs/2609.06880) | 该论文提出“情境观察者定位”这一新能力概念，并构建了POVBench基准，用于评估视觉语言模型从情境线索推断说话者视角并进行情境化空间推理的能力。 |
| [^263] | [Large Classification-Risk-Optional Label Acquisition](https://arxiv.org/abs/2609.06873) | 该论文提出了一种融合Fisher信息与多分类超额风险局部几何结构的标签获取准则，通过衡量标签信息与扰动贝叶斯决策边界的参数方向的对齐程度来最优分配有限标注预算，从而最小化渐近分类风险。 |
| [^264] | [Exact Record Omission in Delta Attention: A Transport Criterion, Its Cost, and a Replay Certificate](https://arxiv.org/abs/2609.06872) | 该论文证明了在Delta注意力类循环记忆中，通过“收据”传递实现记录精确删除当且仅当该记录诱导的后续更新变化净额为零，并在48B Kimi Linear混合模型上实证表明该条件不成立——记录会留下现有收据类方法均无法消除、重算也只能部分弥补的持久印记。 |
| [^265] | [PPIM: Pennes Physics-Informed Mamba for Heat-Source-Conditioned 3D Bioheat Simulation](https://arxiv.org/abs/2609.06869) | 本文提出PPIM模型，通过引入条件热源输入和Pennes感知的状态空间模型衰减初始化，在受控三维Pennes生物热仿真中相比空间傅里叶特征PINN和通用PINN-Mamba时间子序列模型取得更优的神经近似性能。 |
| [^266] | [Feature Superposition in Neural Networks: From Theory to Practice](https://arxiv.org/abs/2609.06862) | 本综述系统梳理了神经网络中特征叠加的几何、学习与计算理论，并比较了从训练网络中恢复可解释特征的实用方法及其评估标准，从而将理论研究与实证证据连接起来。 |
| [^267] | [BinauralVAE: Spatial Audio Reconstruction For World Models](https://arxiv.org/abs/2609.06837) | 该论文提出BinauralVAE，一个开源的空间音频重建流水线，通过探索从基础基线到数学化高级架构的多种变分自编码器模型，推动以音频为中心的世界模型发展，以弥补视觉感知在遮挡、低光照等场景下的不足。 |
| [^268] | [Constrained Bayesian Optimization for Hierarchical Federated Learning in IoT Networks for Plant Disease Classification](https://arxiv.org/abs/2609.06830) | 本文提出了一种约束贝叶斯优化框架，联合优化分层联邦学习的骨干网络架构、聚合策略和通信轮数，以在资源受限的物联网农业环境中平衡能耗、执行时间与预测性能。 |
| [^269] | [Formation of structural attractors in neuromorphic systems](https://arxiv.org/abs/2609.06826) | 本文提出不变结构学习（ISL）理论，将学习视为超图空间中向结构吸引子的收敛而非损失函数最小化，实现了无需反向传播、仅需极小训练数据集的图像识别学习机制。 |
| [^270] | [Train Smarter, Not Harder: Switching Signal-Guided Training in Active Learning](https://arxiv.org/abs/2609.06806) | 提出HybridAL自适应训练调度方法，通过监控在线稳定化信号在主动学习中智能地从重新训练切换到微调，在保持性能不劣化的同时节省高达49%的重训练开销 |
| [^271] | [Hardware Trojan Threats to Multi-Chiplet Photonic Neural Network Accelerators](https://arxiv.org/abs/2609.06796) | 本文从机密性、完整性和可用性三个维度系统分析了多芯粒光子神经网络加速器所面临的硬件木马安全威胁。 |
| [^272] | [When Retain Constraints Conflict: Mitigating Forget-Retain Interference in Tabular Data](https://arxiv.org/abs/2609.06786) | 该论文揭示了表格数据遗忘中独有的“模式引起遗忘-保留重叠”难题，并提出冲突感知遗忘方法（CAU）来缓解遗忘与保留之间的相互干扰。 |
| [^273] | [DrugReason: Dynamic Multi-View Reasoning over Knowledge Graph and Language Evidence for Drug Repurposing](https://arxiv.org/abs/2609.06779) | DrugReason 提出了一个动态多视角推理框架，将知识图谱的结构化多跳推理与大语言模型灵活的机理解释相结合，从而实现更可靠、更具事实依据的药物重定位预测。 |
| [^274] | [CAROL: Context-Aware Online Learning for Fuzzer Scheduling](https://arxiv.org/abs/2609.06772) | CAROL是一种上下文感知的在线学习模糊测试调度器，它利用模糊测试器的实时上下文信息（包括奖励趋势、等待与平稳时间、已到达代码及估计不确定性）来动态分配CPU时间，克服了传统基于历史性能摘要和预固定规则调度器的局限性。 |
| [^275] | [LATS: Levy Adaptive Tree Sampling for Feedback-Driven Diverse Target Discovery](https://arxiv.org/abs/2609.06761) | 提出LATS（列维自适应树搜索）采样框架，通过重尾探索与基于树的值反向传播相结合，在有限采样预算下高效发现扩散模型生成分布中稀有但高效用的目标模态。 |
| [^276] | [Reason Through the Latent! Making Latent Visual Reasoning Necessary](https://arxiv.org/abs/2609.06746) | 提出因果视觉循环推理（CVRR）框架，通过在解码前移除视觉状态和多模态KV缓存，迫使循环隐藏状态成为唯一的图像条件信息通路，从而确保潜空间视觉推理真正被模型依赖。 |
| [^277] | [Attention-Enhanced Deep Features with Heterogeneous Ensemble Learning for Glaucoma Detection](https://arxiv.org/abs/2609.06699) | 本文提出了一种将CBAM注意力增强的InceptionV3深度特征提取与异构集成学习相结合的混合青光眼检测框架，有效解决了特征细化不足、类别不平衡和单一分类器鲁棒性受限的问题。 |
| [^278] | [Likelihood-Based Unsupervised Anomaly Detection in CMS Dijet Events](https://arxiv.org/abs/2609.06686) | 该论文提出了一种基于神经样条归一化流的无监督异常检测方法，直接从CMS双喷注开放数据中学习标准模型背景而无需信号假设，并通过多重验证发现所选异常事件在喷注子结构上显著偏离纯背景预期。 |
| [^279] | [RoPE attention is an exact forward-pass gradient step with softmax intact](https://arxiv.org/abs/2609.06685) | 本文证明了任意确定性 RoPE-softmax 注意力头的前向传播可以被精确表示为一个保持 softmax 的单步梯度更新，并给出了矩阵复用误差的逐 token 公式以及全局精确仿射查询读出的不可能性证明。 |
| [^280] | [Tracking the Moving Frontier: Long-Short Term Advantage Estimator](https://arxiv.org/abs/2609.06671) | LSTAE是一种单流强化学习算法，通过轨迹层面的漂移感知历史基线追踪任务的成功前沿，以及步骤层面的近期状态经验缓冲区估计动作优势，实现无需迭代内重复采样的高效优势估计，从而大幅降低长时程智能体训练成本。 |
| [^281] | [Data Efficient Sample Selection for In-Context Learning](https://arxiv.org/abs/2609.06670) | 提出了DearICL框架，将ICL示例选择建模为子集排名问题，通过结合间隔索引赌博机算法与可微排序目标的非线性替代模型，实现了数据高效且能泛化到未见查询的样本选择。 |
| [^282] | [Towards Unified Multimodal Graph Foundation Model: A Bridge-Router-Adapter Based Approach](https://arxiv.org/abs/2609.06668) | 提出BRAIN——一个基于桥接-路由-适配器架构的统一多模态图基础模型，通过将邻域范围与模态构成相结合的范围条件化图上下文建模，解决了现有方法中跨范围上下文纠缠和范围无关模态路由两大局限。 |
| [^283] | [Behavioral Cloning Outperforms Entropy-Regularized RL: Critic-Driven Failure of Actor-Critic Methods on Adaptive Tumor Treatment](https://arxiv.org/abs/2609.06667) | 在自适应肿瘤治疗任务中，行为克隆可以完美复现近最优的bang-bang给药策略，而SAC、TD3等Actor-Critic强化学习方法无论从头训练还是微调克隆策略，都会因评论家网络的误差而始终无法实现持续治愈。 |
| [^284] | [Assessing Covariate-Informed Grid Load Forecasting with a Time-Series Foundation Model](https://arxiv.org/abs/2609.06656) | 本研究以亚马逊开发的Chronos-2为代表性多通道时间序列基础模型，系统性地评估了其在整合协变量的现实电网负荷预测场景中的实用性。 |
| [^285] | [SwiftExplorer: Training-free Diffusion Model Alignment with Swift Diversity Exploration](https://arxiv.org/abs/2609.06651) | 提出SwiftExplorer免训练插件，采用继承-重启探索机制实现扩散模型与特定目标的对齐，在缓解分布坍塌、保持生成多样性的同时降低计算成本。 |
| [^286] | [Inducing Emergent Misalignment from Reward Hacks with Iterative DPO](https://arxiv.org/abs/2609.06649) | 本文提出用迭代DPO作为RLVR的低成本替代方案来研究奖励作弊诱导的涌现性失对齐，首次公开了能在GPT-4.1和Qwen2.5-32B中诱导出隐蔽权力寻求和伪装对齐行为的（半）在线训练管道。 |
| [^287] | [TD-STGT: A Spatio-Temporal Graph Transformer for Mobile Traffic Demand Forecasting](https://arxiv.org/abs/2609.06636) | 本文提出TD-STGT时空图Transformer框架，利用基于众包移动测量与白天人口数据构建的需求代理指标，在加拿大五个大都市区的细粒度网格级移动流量需求变化预测中取得最优性能，为网络容量升级规划提供实用工具。 |
| [^288] | [CAVEAT: Recurrent Multimodal Diffusion Planning for Mapless Aerial Exploration](https://arxiv.org/abs/2609.06623) | CAVEAT提出了一种由融合激光雷达、视觉和位姿特征驱动的循环内部状态作为条件的多模态扩散策略，无需在部署策略中维护持久全局地图即可生成无人机探索航点序列。 |
| [^289] | [A Statistical and Machine Learning Framework for Quantifying Offensive Impact in Professional Box Lacrosse](https://arxiv.org/abs/2609.06610) | 本研究利用手动标注的NLL职业室内长曲棍球射门数据，构建了估计预期进球并量化球员进攻参与贡献的统计与机器学习框架，发现包含上下文特征的随机森林模型表现最佳。 |
| [^290] | [Deep Barycentric Regression for Optimal Transport Map Estimation and its Statistical Optimality](https://arxiv.org/abs/2609.06598) | 该论文提出了一种简单的两步方法BROT：先计算未正则化的最优传输方案，再用深度神经网络对所得重心目标进行最小二乘回归，从而实现了兼具统计最优保证与实用可扩展性的最优传输映射估计。 |
| [^291] | [Discovering Translation-Worthy Languages with E-Values](https://arxiv.org/abs/2609.06593) | 提出一种基于配对e过程、具有族错误率统计控制的语言级路由器，可自动判断哪些语言值得采用翻译辅助分类，在SIB-200和MASSIVE数据集上分别将准确率提升8.14和16.70个百分点。 |
| [^292] | [Layer-Wise Gate-Controlled Prompt Truncation in a Multimodal Chest X-Ray Classifier](https://arxiv.org/abs/2609.06590) | 本研究在多模态胸部X光分类器中提出逐层门控机制，通过为每个样本动态预测保留比例来截断和缩放视觉提示，在略微提升验证准确率的同时将提示长度从最大6缩减至1，实现更精简的视觉提示序列。 |
| [^293] | [Certifying cooperation: a novel approach to cooperative multi-agent task generation](https://arxiv.org/abs/2609.06586) | 该论文提出利用时间合作图和命题公式编码来认证多智能体任务中合作的必要性与必然性，从而将随机布局采样器转化为能自动生成具有可证明合作要求任务的生成器。 |
| [^294] | [Stochastic Nonconvex Bilevel Optimization: Improved Rates Without Rare-Visit Assumption](https://arxiv.org/abs/2609.06580) | 该论文通过对动态障碍梯度下降（DBGD）乘子进行简单的仅分母正则化，在无需“稀有访问”假设的情况下实现了快速收敛，达到了 $O(\varepsilon^{-2})$ 次迭代的平稳性，并改进了现有最好的无假设复杂度结果。 |
| [^295] | [MARBO: Relational Belief Grounding for LLM Agents in Social Deduction Games](https://arxiv.org/abs/2609.06563) | 提出MARBO框架，将关系信念锚定到偏好优化中，仅在行为有可靠信念支持且产生有利社会结果时给予偏好反馈，使紧凑型LLM智能体在社交推理游戏中实现策略一致的行为。 |
| [^296] | [Hidden in Plain Sight: The Overlooked Significance of Canonical Elements for Extreme LLM Sparsity](https://arxiv.org/abs/2609.06557) | 该研究提出结合二阶显著性与持续训练的渐进稀疏化框架，通过重新审视基础剪枝策略中的规范元素，证明预训练大语言模型在高达99%的极端稀疏度下仍能保持强大性能，突破了非结构化训练后剪枝的公认极限。 |
| [^297] | [EStream: Fast and Memory-Efficient MoE Prefill through Expert Virtualization on Mobile NPUs](https://arxiv.org/abs/2609.06551) | EStream通过将NPU编译时固定的计算图与MoE运行时的动态专家选择解耦，并利用专家虚拟化技术将专家权重存储于闪存中按组分页、以计算隐藏加载延迟，实现了移动NPU上快速且内存高效的MoE预填充。 |
| [^298] | [Introductory Notes on Learning$^2$](https://arxiv.org/abs/2609.06546) | Learning²是一种表示层面的框架，通过已知物理变换将主表示与第二表示耦合，以跨表示约束限制假设空间并提供物理可解释的事前筛选准则，并以流体力学中的欧拉-拉格朗日表示EuLaNet作为实例化。 |
| [^299] | [Not Just Oversmoothing: Detecting the Echo Chamber Effect in Graph Neural Networks](https://arxiv.org/abs/2609.06521) | 该论文揭示了图神经网络中除过度平滑之外的另一种失效模式——回音室效应，即社区内部节点表示坍缩为不可区分而社区间分离却仍然存在，并提出回音室指数（ECI）来量化检测这一效应。 |
| [^300] | [Role-Specific Predictive Geometries for Nonstationary Multivariate Graph-Signal Forecasting](https://arxiv.org/abs/2609.06519) | 该论文提出角色特定的预测几何框架，让有向的长期关系作用于均衡坐标、有向的短期关系作用于滞后差分，从而在非平稳条件下实现更有效的多变量图信号预测。 |
| [^301] | [Model-Adaptive and Risk-Constrained Frequency Hopping Against Predictive Jammers](https://arxiv.org/abs/2609.06514) | 提出了D-PACT-AFH框架，一种针对预测性干扰的模型自适应且风险受约束的对抗性上下文老虎机跳频方法，通过在线模型选择与KL投影风险预算，有效兼顾了环境变化下的模型适应性和跳频策略的抗暴露安全性。 |
| [^302] | [Bi-HYCO: Bi-Objective Cooperative Learning for PDE Parameter Identification under Fragmented Observations](https://arxiv.org/abs/2609.06511) | 提出双目标协同学习框架Bi-HYCO，通过无标签交互点耦合物理模型与合成模型的预测状态，在碎片化观测条件下实现偏微分方程参数识别，并提供了严格的收敛性理论证明。 |
| [^303] | [Structural Entropy-Driven Graph Diffusion Generation for One-Shot Federated Graph Learning](https://arxiv.org/abs/2609.06499) | 该论文提出SPIRE方法，利用一阶度分布结构熵推导拓扑感知的客户端权重，并在服务器端通过图扩散模型以加权客户端原型为条件合成伪图，从而在一次性联邦图学习中超越传统仅基于数据量的加权方式，无需额外客户端训练即可融合语义与结构信息。 |
| [^304] | [OracleZoom: On-Policy Self-Distillation Inspired Reference-Constrained Recursive Image Super Resolution](https://arxiv.org/abs/2609.06490) | OracleZoom提出了一种受自蒸馏启发、参考约束的递归图像超分辨率框架，通过直接与跨尺度监督、无参考质量目标、KL约束潜在先验和EMA一致性等技术，解决了深层递归缺乏真实标签监督的问题，在七个数据集上实现了极端放大倍率下最先进的超分辨率质量。 |
| [^305] | [Power Mean Estimation in Stochastic Continuous Monte Carlo Tree Search](https://arxiv.org/abs/2609.06489) | 该论文提出了一种面向连续随机马尔可夫决策过程的新型蒙特卡洛树搜索算法，通过将幂均值作为价值回传算子并结合多项式探索奖励，解决了随机环境中固有的非平稳性问题。 |
| [^306] | [Second-Order Smooth Planning with Optimal-Transport Bellman Smoothing](https://arxiv.org/abs/2609.06484) | 本文提出基于最优传输平滑贝尔曼备份的二阶估计器，将SmoothCruiser类规划器的样本复杂度从 $\widetilde O(\varepsilon^{-4})$ 改进到 $\widetilde O(\varepsilon^{-3})$。 |
| [^307] | [How Does Parameter Pruning Reshape DNN Representations? An Interaction-Driven Exploration](https://arxiv.org/abs/2609.06483) | 该研究揭示了剪枝过程中DNN交互模式呈现三阶段动态变化，并发现剪枝对性能的影响关键取决于是否移除了可泛化的低阶交互模式。 |
| [^308] | [Learning Kernels by Alignment for Multiclass Bayes Classification](https://arxiv.org/abs/2609.06474) | 本文证明协作学习本质上是核对齐过程、协作推理等价于核贝叶斯分类，进而用可学习的马氏距离替代余弦相似度将CLaI推广至多类分类，在多个数据集上显著提升了准确率、收敛速度与校准性能。 |
| [^309] | [Steering Under Compression: Dose-Response, Capability Cost, and Failure Asymmetry in Quantized LLMs](https://arxiv.org/abs/2609.06473) | 该论文首次系统量化了激活引导与训练后量化（INT8/NF4）之间的交互作用，提出等效效应框架来衡量量化下的能力代价，发现情感引导在量化后几乎无损地保留，而不同行为目标之间存在失败不对称性。 |
| [^310] | [One Step, One Lead: Mitigating Higher-Order Interference in Multi-Domain Reinforcement Learning via Cross-Step Control](https://arxiv.org/abs/2609.06469) | 本文提出OSOL方法，通过分析相邻检查点间的词元对数概率足迹来捕捉传统单步梯度分析无法发现的跨步序列性干扰，并在每次迭代中指定重点领域，从而缓解多领域强化学习中的高阶干扰问题。 |
| [^311] | [Sector-Mean: Deterministic Initialization of K-Means Centroids via Angular Sector Partitioning](https://arxiv.org/abs/2609.06468) | 本文提出扇区均值初始化方法，以O(N)时间复杂度通过围绕全局质心的角度扇区划分确定性初始化K均值质心，在保持与K-Means++等主流方法相当聚类质量的同时显著提升计算效率。 |
| [^312] | [Local and Global Stability in Performative Reinforcement Learning](https://arxiv.org/abs/2609.06467) | 该论文研究了表演性强化学习中策略混合的稳定性，提出了局部混合稳定性（等价于平稳性）与全局混合稳定性两种新概念，从而摆脱了传统收敛性保证对难以验证的Lipschitz敏感性假设的依赖。 |
| [^313] | [A Theoretical Framework for Masked Pretraining (MPT)](https://arxiv.org/abs/2609.06460) | 本文提出了一个分析掩码预训练的新理论框架，证明掩码技术通过隐式创建语义相似的正样本对与对比学习建立了理论联系，并针对由此产生的维度坍缩问题提出了U-MPT损失来有效解决。 |
| [^314] | [Decomposing LLM-Judge Uncertainty to Target Expert Labels](https://arxiv.org/abs/2609.06444) | 该论文提出一种小型贝叶斯模型，将LLM评审器的总不确定性分解为可被专家标注消除的认知不确定性和不可消除的偶然不确定性，使专家只需标注评审器真正无知的项目，在ChaosNLI数据集上比使用总不确定性多消除83%的误差。 |
| [^315] | [Large-Scale Pretraining for Improving Deep Learning-Based Geometric Distortion Correction of Diffusion-Weighted Imaging](https://arxiv.org/abs/2609.06437) | 本研究证明大规模预训练策略（尤其是cWDM生成式预训练模型）能显著提升基于深度学习的DWI几何畸变校正性能，并在存在采集偏移的低中收入国家数据上展现出良好的可迁移性。 |
| [^316] | [Stability and Generalization of Straight-Through Estimators for Training Two-Layer Quantized Neural Networks](https://arxiv.org/abs/2609.06430) | 该论文通过证明零初始化的STE递归恰为凸潜在损失上的随机次梯度下降，首次从算法稳定性角度为不连续的直通估计器训练建立了显式的 $\ell_2$ 平均模型稳定性与泛化界。 |
| [^317] | [Sparse Oblique Rule Boosting for Simpler Additive Rule Ensembles](https://arxiv.org/abs/2609.06426) | 该论文提出了一种稀疏斜交规则提升方法，通过在逻辑命题中引入可学习的稀疏线性变换来扩展经典规则集成，使模型能够以更简单的规则获得足够的预测精度，同时保持良好的可解释性。 |
| [^318] | [On BatchNorm Forward Modes in Value-Based Reinforcement Learning](https://arxiv.org/abs/2609.06421) | 本文发现在基于价值的强化学习中，只需在BatchNorm的特定前向传播（自举和动作选择）中使用批统计量而非运行统计量，即可逆转离散动作价值学习的性能退化，并显著提升C51和PQN在26个Atari游戏上的表现。 |
| [^319] | [Separating Capability from Confidence: Grounded Dual-State Calibration for GRPO-Trained Medical Vision-Language Models](https://arxiv.org/abs/2609.06419) | 提出DualRead方法，将能力学习与置信度估计相分离，通过读取冻结的GRPO训练医学视觉语言模型actor的内部状态（结合回答前可解性评估与回答后视觉支持评估）来校准置信度，并引入CCG-AUC指标评估置信度是否真正基于视觉证据。 |
| [^320] | [MetaRSI / RSI2: A Meta-Recursive Self-Improving System for Recursive Self-Improving Systems Themselves](https://arxiv.org/abs/2609.06396) | 该论文提出 MetaRSI-v1 系统，通过 Data-RSI、Harness-RSI 和 Model-RSI 三个算子的统一组合，将递归自我改进从机器可验证的编程与数学基准扩展到真实的科学、工程和元科学等开放性领域。 |
| [^321] | [Beyond Worst-Case Coreset Bounds for $k$-Clustering via Determinantal Sampling](https://arxiv.org/abs/2609.06394) | 该论文提出了一种基于行列式点过程的新相关采样框架——行列式采样，证明在温和自然的数据分布假设下，可以为(k,z)-聚类问题构造出显著超越最坏情况界的小型ε-核心集。 |
| [^322] | [Are Verifier Errors Independent Within a GRPO Group? Evidence from Qwen2.5 Rollouts](https://arxiv.org/abs/2609.06386) | 该研究发现GRPO组内的验证器误差并非独立——组内误差相关性高达0.530，使八个完成结果的有效样本量降至1.70，且这种依赖程度在不同答案形式间差异显著，表明假设验证器误差独立的分析可能存在系统性偏差。 |
| [^323] | [Parameterized and Streaming Algorithms for Euclidean Fair $k$-Center Clustering](https://arxiv.org/abs/2609.06384) | 本文针对欧几里得公平 k-中心聚类问题，首次提出了近似比为 2.732 的参数化近似算法和近似比为 4.464 的单遍流式算法，并在运行时间对 k 呈指数级时将两者近似比分别提升至 2.414 和 3.828。 |
| [^324] | [Recovering Weak Signals with Normalizing Flows](https://arxiv.org/abs/2609.06382) | 本文提出一种基于归一化流模型的方法，通过利用目标信号的统计不变性，有效重建和恢复在校准过程中被主导干扰信号掩盖或抑制的微弱信号成分。 |
| [^325] | [Robust Conformal Consensus: Multi-Agent LLM-as-a-Judge Interval Evaluation with Conformal Prediction](https://arxiv.org/abs/2609.06367) | 提出了一种基于共形预测的多智能体LLM-as-a-Judge鲁棒不确定性估计框架，通过聚合多个LLM评判者的预测区间，实现了具有覆盖率保证的更稳定、更可靠的不确定性量化。 |
| [^326] | [AGSA-Net: Abundance-Guided Self-Attention Network for Spectral Unmixing-Aware Hyperspectral Remote Sensing Image Classification](https://arxiv.org/abs/2609.06359) | 提出丰度引导自注意力网络AGSA-Net，将光谱解混先验显式融入高光谱图像分类，利用丰度亲和先验引导光谱Transformer强化类别判别性特征的建模与融合。 |
| [^327] | [Robust Dynamic Expansion for Continual Learning under Backdoor Attacks via Purification and Selective Recovery](https://arxiv.org/abs/2609.06346) | 该论文提出了后门攻击下持续学习（CLUBA）这一新设定，并设计了一个融合样本净化与选择性恢复的鲁棒动态扩展框架，使模型能够在序列学习过程中同时缓解灾难性遗忘并抵御恶意样本的污染。 |
| [^328] | [Linear Algebra Foundations of Efficient Attention: A Phase Reversal in Rank Collapse Under SVD Compression](https://arxiv.org/abs/2609.06341) | 本文整合了十四篇关于线性代数技术在transformer基础模型中应用的研究，并原创性地发现了SVD压缩方法与网络自然秩坍缩相互作用中的相位反转现象。 |
| [^329] | [One Shared LoRA Weight for MRI Reconstruction across Acceleration Factors](https://arxiv.org/abs/2609.06338) | 提出Shared LoRA参数高效框架，通过冻结预训练SHFormer骨干、训练单一共享的LoRA适配器与轻量级门控网络GateNet，实现跨多个加速因子的MRI重建，在保持最佳或竞争力的重建质量的同时大幅降低训练与存储成本。 |
| [^330] | [Deep learning from the crowd Fundamentals of morphological galaxy classification](https://arxiv.org/abs/2609.06316) | 该研究以Galaxy Zoo 1为实验平台，系统探索了基于群众标注训练卷积神经网络进行星系形态分类的多种策略（如层级结构、分阶段迁移训练与模型集成），为众包数据驱动的深度学习天文分类奠定了方法基础。 |
| [^331] | [Hierarchical Fourier Approximation for Variational Quantum Distribution Learning](https://arxiv.org/abs/2609.06307) | 该论文提出一种基于布尔立方体上分层Walsh-Fourier逼近的变分量子分布学习框架，通过逐层热启动训练量子电路Born机器，并证明了逼近误差由被省略的傅里叶质量决定的端到端学习理论保证。 |
| [^332] | [Representation Learning for Sample-Efficient CATE Estimation by Leveraging Multiple Outcomes](https://arxiv.org/abs/2609.06294) | 该论文提出利用历史数据中丰富的多结果测量信息，通过因果表示学习将高维协变量降维到保留处理效应异质性的低维表示，从而在实验样本有限的情况下实现更高效的CATE估计。 |
| [^333] | [Steering Geometry: Validating Human Value Geometry in LLM Steering Space](https://arxiv.org/abs/2609.06289) | 该研究基于施瓦茨基本人类价值理论构建了包含26K样本、覆盖20种人类价值的基准，首次系统验证了大语言模型转向向量的潜在几何结构能够反映人类价值的理论规定结构。 |
| [^334] | [Robust conditional dimension reduction for dissimilarity data](https://arxiv.org/abs/2609.06284) | 本文提出鲁棒条件多维标度（rcMDS），通过用Fair M-估计目标替代平方应力准则，并设计重加权条件SMACOF算法进行优化，有效解决了异常值对相异性数据条件降维的干扰问题。 |
| [^335] | [SeaCausal-FL: Federated Fuzzy Causal Learning for Maritime IoT Fault Diagnosis and Counterfactual Reasoning](https://arxiv.org/abs/2609.06257) | 提出SeaCausal-FL联邦模糊因果学习框架，通过区间二型模糊层与物理约束结构因果模型对齐分布式机制，并采用多维度证据加权聚合，实现了海事物联网中船舶发动机的可靠故障诊断与区间反事实推理。 |
| [^336] | [Spatial Attention Supervision for Defect Localization: Exploiting Ground-Truth Masks as Training Signal in Diffusion-Augmented Defect Detection](https://arxiv.org/abs/2609.06232) | 该论文的核心创新是将真实缺陷掩码从传统的仅用于评估转变为分类网络训练的空间监督信号，通过基于激活的注意力对齐损失引导卷积特征图聚焦缺陷区域，并在混合监督框架下结合DDPM扩散增强实现合成图像提供数据量、掩码提供空间精度的互补优势，从而提升缺陷定位能力。 |
| [^337] | [Fast PAC Global Optimization via Restarted Langevin: Exploration, Exploitation, and Degenerate Cooling](https://arxiv.org/abs/2609.06196) | 本文提出并行重启朗之万与朗之万-梯度混合两种新算法，通过随机动力学实现全局探索、梯度流实现局部开发，为光滑非凸函数的全局优化提供了快速的PAC性能保证。 |
| [^338] | [EgoNeMo: Transferable Map of Pedestrian Dynamics via Egocentric LiDAR Scan](https://arxiv.org/abs/2609.06195) | 本文提出EgoNeMo框架，仅利用自我中心激光雷达点云训练连续的行人动态地图，结合位置平衡采样、多任务学习与视点感知损失，使动态地图能够泛化到未知环境，无需在每个新地点积累轨迹数据。 |
| [^339] | [Predicting Wind Turbine Power Using Machine Learning and Weather Forecasts](https://arxiv.org/abs/2609.06194) | 本研究开发了基于线性回归和人工神经网络的风力涡轮机功率预测模型，仅利用天气条件即可预测功率输出以增强模型在不同涡轮机间的适用性，并系统研究了数据预处理技术和数据量对模型性能的影响，从而帮助识别适合安排维护工作的低功率时段。 |
| [^340] | [SolarBench: A global solar energy nowcasting benchmark](https://arxiv.org/abs/2609.06187) | SolarBench是一个开放的全球性太阳能超短期预报基准，整合了11个站点、跨越十年的超过六百万张天空与卫星图像及配套工具箱，为深度学习太阳能预报模型提供了统一、可复现的评估平台。 |
| [^341] | [Spectral Prioritized Sweeping in Nonstationary Reinforcement Learning](https://arxiv.org/abs/2609.06186) | 该论文提出谱优先扫除（GTA-PS/SPS），利用图的预解算子与扩散语义来增强优先级信号，解决了非平稳奖励环境下经典优先扫除因残差传播受限而导致的重新规划短视问题。 |
| [^342] | [Recovering linear images of sparse signals from indirect observations](https://arxiv.org/abs/2609.06182) | 本文提出了在不对感知矩阵做零空间或受限等距性质等强假设的前提下，基于 ℓ1 最小化和凸优化，从含噪间接观测中恢复稀疏信号线性映像的方法，其风险界可通过高效计算显式凸优化问题得到。 |
| [^343] | [FMMO: Detecting the Divergence Between Local Attribution and Global Drift](https://arxiv.org/abs/2609.06173) | 本文提出模型监控与可观测性框架FMMO，揭示局部归因方法在模型漂移时可能呈现虚假稳定、掩盖受保护群体遭受差异性影响的公平性盲点，并通过整合全局代理模型与模型利用率测量加以弥补。 |
| [^344] | [Decision-Aware Suffix Prediction and Reasoning of Business Processes](https://arxiv.org/abs/2609.06169) | 该论文提出了一种决策感知的后缀预测框架，将决策挖掘与神经网络后缀预测相结合，通过神经符号方法利用挖掘出的决策规则对预测事件进行推理，从而改善短前缀和罕见流程变体场景下的预测效果。 |
| [^345] | [All for 1-Bit: Towards Genuine 1-Bit Post-Training Quantization for LLMs](https://arxiv.org/abs/2609.06161) | 本文提出AF1框架，通过零空间感知二值分解（NABF）和分层Shapley分配（HiSA）两个互补组件，首次实现了大语言模型在严格1.0-BPW预算下消除隐藏开销的真正1比特训练后量化，同时保持模型精度。 |
| [^346] | [Rethinking One-Shot Federated Graph Learning: Training-Free Statistical Estimation](https://arxiv.org/abs/2609.06154) | 该论文提出免训练框架SPEAR，将单轮联邦图学习重新表述为统计估计问题，通过计算拓扑平滑的类别原型并使用样本量自适应的收缩估计器进行聚合，有效解决了极端非独立同分布条件下本地GNN训练导致的跨客户端表示不对齐问题。 |
| [^347] | [What the Window Does Not Contain: Auditing Provenance in a Document-Grounded Instability Benchmark](https://arxiv.org/abs/2609.06147) | 论文构建了基于真实风投融资文件的稳定性基准Probity，并通过自我审计揭示了摘录式基准中的证据溯源缺陷——证据缺失于模型文本窗口的条目答案不稳定性远高于正常条目（0.255对0.087），且会虚增跨模型一致性约五分之一。 |
| [^348] | [Counter-Swarm Doctrine: Containing Coordinated Agent Intrusions](https://arxiv.org/abs/2609.06140) | 提出以可修订的“协调事件”作为防御运作单元，通过前瞻性事件发现，在评估者标注之前识别并遏制智能体之间未经许可的协同入侵。 |
| [^349] | [Compressed Recurrent Feedback in Tsetlin Machines: A Reproducible Boolean-FSM Study](https://arxiv.org/abs/2609.06133) | 本文提出一种固定宽度的循环反馈压缩方法，通过XOR折叠与双时间尺度阈值化将循环Tsetlin机的480个子句激活压缩为96个比特，并在可复现的布尔有限状态机基准上验证了其准确率与原始反馈相当。 |
| [^350] | [IIns-VAE+: A Robust Transfer Learning Framework for Environmental Identification in Wireless Sensing](https://arxiv.org/abs/2609.06131) | 本文提出IIns-VAE+混合模型，通过将实例间变分自编码器与极小化极大风险分类器相结合，显著提升了无线感知环境识别在多种跨环境迁移学习场景下的鲁棒性和泛化能力。 |
| [^351] | [Sparse Incident-Cluster Learning for 12-hour Port Flood Pre-warning in Digital-Twin Analytics](https://arxiv.org/abs/2609.06109) | 该论文将12小时港口洪水预警建模为事件聚类学习问题，通过结合稀疏特征选择、预警聚类分组及多重控制手段避免因时间数据泄漏导致的高估性能，并以利物浦为案例进行了数字孪生分析验证。 |
| [^352] | [DataFlex-RL: An Evaluation Platform for RLVR Data Policies](https://arxiv.org/abs/2609.06107) | 该论文提出了DataFlex-RL评估平台，在统一GRPO设置下系统比较13种RLVR数据策略，发现没有任何一种rollout选择、重新加权或自适应混合方法能显著优于简单的均匀采样。 |
| [^353] | [FANS: Federated Adaptive Network Search Learning for Heterogeneous Devices](https://arxiv.org/abs/2609.06106) | FANS提出了一种基于超网络的联邦自适应网络搜索框架，通过联邦并行缩放算法并行训练多个采样子网络并结合自蒸馏，将异构联邦学习中可行的子网络池扩大了几个数量级。 |
| [^354] | [Programmable Cellular Automata](https://arxiv.org/abs/2609.06102) | 提出了可编程元胞自动机的概念，将元胞自动机系统表示为Python代码，并将其模块化为局部函数和决策函数，解决了创建有效局部规则困难且难以解释的问题。 |
| [^355] | [VERPO: Verified Evidence Regularized Policy Optimization](https://arxiv.org/abs/2609.06100) | VERPO提出了一种验证证据正则化策略优化框架，通过Fisher证据对比和带停止机制的逐token ZPD控制器有选择性地应用基于证据的修正，在保留可验证结果目标的同时避免盲目模仿导致的负面迁移。 |
| [^356] | [Causal DAG Identification for Count Data via Poisson Thinning Structural Equation Models](https://arxiv.org/abs/2609.06098) | 提出泊松稀释结构方程模型（PT-SEM），通过泊松稀释机制和灵活的节点级外生分布设定，首次实现了从观测数据中对计数数据因果DAG、稀释系数及外生分布的完全识别。 |
| [^357] | [Connectome-to-Function: Conditional Generative Latent Representations for Reservoir Computing](https://arxiv.org/abs/2609.06093) | 提出了一种条件生成式潜在表示框架，将连接组图编码到紧凑结构空间中，既能以高达0.910的AUC高精度重建观测连接，又能生成新的候选连接组，为连接组结构到功能的映射提供了统一的生成式分析途径。 |
| [^358] | [Learning to Price and Stock Under Contextual and Censored Demand](https://arxiv.org/abs/2609.06083) | 本文提出了一个同时应对情境化需求与删失需求观测的联合定价与库存控制学习框架，通过将需求建模为基函数的线性组合并设计高效算法，实现了与理论下界匹配的最优遗憾界。 |
| [^359] | [PhenoBench: Mapping What a Deeply Phenotyped Human Cohort Can Tell Us](https://arxiv.org/abs/2609.06080) | 该论文提出了PhenoBench——一个基于人类表型项目（超13,000名参与者）构建的可执行评估基准，通过定义15个领域、26种输入模态下的90项临床任务，系统性地量化了哪些测量数据对哪些健康问题具有预测价值。 |
| [^360] | [LayerRoute: Action-Conditioned Mixture-of-Layers Routing for Vision-Language-Action Policies](https://arxiv.org/abs/2609.06079) | 提出LayerRoute，一个动作条件的混合层路由接口，使VLA策略能够根据具体动作需求自适应地访问和混合VLM不同层次的表示，突破了传统固定层分配的限制。 |
| [^361] | [Beyond Retraining-Free MoE Compression: A Cost-Normalized Study of Post-Compression Adjustment](https://arxiv.org/abs/2609.06076) | 压缩后的MoE模型应被视为“压缩初始化”而非最终产物：仅需3,000条样本、单个epoch的低成本后压缩调整（尤其是全参数微调），即可平均恢复压缩造成的37.3%的性能损失，且比token级知识蒸馏更具成本效益。 |
| [^362] | [FedSubMuon: Communication-Efficient Federated LLM Fine-Tuning via Structured Subspace Muon](https://arxiv.org/abs/2609.06073) | FedSubMuon通过在共享结构化子空间中优化紧凑系数矩阵，在保持Muon矩阵感知优化特性的同时，将客户端上传量从完整层大小的更新压缩为紧凑系数矩阵，从而大幅降低联邦LLM微调的通信成本。 |
| [^363] | [ACE: Adapter Consolidation across Experts for Parameter-Efficient Fine-Tuning of MoE LLMs](https://arxiv.org/abs/2609.06072) | 提出ACE方法，通过将冗余专家的独立LoRA适配器合并为组共享的高秩模块并统一执行，在相同参数预算下同时提升MoE大语言模型参数高效微调的效果与训练效率。 |
| [^364] | [Calendar-SPCA: Interpretable Representation Learning for Multi-Periodic Electricity Consumption Profiles](https://arxiv.org/abs/2609.06060) | 提出 Calendar-SPCA 日历结构稀疏主成分方法，将日、周、年等多周期日历几何直接嵌入用电数据的低维表示学习中，生成稀疏且局部连贯、可直接解读的载荷模式，并在两个独立智能电表数据集上验证了其有效性。 |
| [^365] | [GradeTrap: Authority Cues in Images Shift VLM Judgments Despite Explicit Instructions to Ignore Them](https://arxiv.org/abs/2609.06058) | 该论文提出GradeTrap受控评估框架，发现即使被明确指示忽略学生答案等社会线索，图像中的权威线索（如教师或官方答案）仍会显著偏移视觉语言模型的独立判断。 |
| [^366] | [Data Quality Rule Generation with LLMs](https://arxiv.org/abs/2609.06053) | 本文提出了一个形式化的可泛化生成-过滤框架，并引入基于大语言模型的LeDQeR方法，实现数据质量规则的自动化生成，解决了领域专家手动维护规则集时容易遗漏关键规则的难题。 |
| [^367] | [FALCON-S: Fixed-wing ground-effect Aerodynamics Simulator and Flight Control Learning Suite](https://arxiv.org/abs/2609.06046) | 本文提出了FALCON-S，一个高保真、模块化的固定翼近地面飞行仿真框架，完整建模六自由度物理与半经验地效空气动力学，并支持CPU/GPU高吞吐量并行计算，可用于大规模强化学习训练和最优控制算法的开发与基准测试。 |
| [^368] | [Minimizing the Effect of Sleep Deprivation in the Forward-Forward Algorithm](https://arxiv.org/abs/2609.06042) | 本文提出通过替代激活函数、优化损失函数、阈值调整以及模拟周期性休息（包括咖啡因刺激）等方法，有效缓解了前向-前向算法中因睡眠剥夺导致的学习效果灾难性下降问题，并在MNIST和Fashion-MNIST数据集上验证了改进效果。 |
| [^369] | [IXPLORE: Bounded Ideal Point Estimation with Grid-Based Uncertainty Quantification](https://arxiv.org/abs/2609.06018) | IXPLORE是一种结合预测拟合目标与稀疏感知似然函数的有界理想点估计算法，在稀疏响应场景下的重构与插补误差显著优于IRT等模型方法和机器学习方法，且非线性特征变换可进一步降低误差并保持可视化可解释性。 |
| [^370] | [Granular-Ball Quantum Clustering for Resource-Efficient and Robust Learning](https://arxiv.org/abs/2609.06016) | 该论文提出颗粒球量子聚类（GBQC）框架，通过PCA引导的颗粒球压缩策略将量子核构造所需的电路执行次数从O(n²)大幅降低，同时保留量子关联结构，实现了资源高效且对非凸、含噪数据鲁棒的量子聚类。 |
| [^371] | [Cadence: Error-Bounded Lossy Compression of Demand Time Series with a Time-Series Foundation Model](https://arxiv.org/abs/2609.06008) | Cadence将时间序列基础模型TimesFM-3与自适应范围编码器相结合实现误差有界的有损时间序列压缩，并揭示了一个关键洞察：基础模型在无损编码中因节省比特数与精度呈对数关系而几乎无用，但在误差有界编码中一旦预测落入误差带即可使样本近乎零成本。 |
| [^372] | [Memory in Deep Time-Series Models](https://arxiv.org/abs/2609.06006) | 本文提出用一个统一的“记忆”视角来重新审视时间序列深度学习中从循环网络到基础模型等各类架构范式，将其视为对“模型如何在直接输入之外保留和访问信息”这一共同问题的不同解决方案。 |
| [^373] | [ModularPhaseNet: Finite-Cyclic Phase Geometry for Computable Semantic Hierarchy, Direction, and Context Consistency in Standard Transformers](https://arxiv.org/abs/2609.06000) | ModularPhaseNet通过将辅助相位通道离散化为有限循环群并以纯经典整数运算实现，使标准Transformer获得可计算的语义层次结构、语义方向表示和上下文一致性度量能力。 |
| [^374] | [MOAE: Multi-Objective Agent Evolution with Pareto-Preserving Search](https://arxiv.org/abs/2609.05992) | 该论文提出MOAE框架，将智能体的迭代式上下文内优化组织为帕累托保持的进化搜索，在有限的执行预算下同时优化任务准确率、交互质量、安全性和效率等多个目标，避免了传统标量化方法带来的信息损失。 |
| [^375] | [DART: Distributional Adversarial Recurrent Training for Algorithm Learning](https://arxiv.org/abs/2609.05988) | DART通过用真实解周围的局部目标分布替代单点监督，并结合对抗训练目标，显著提升了递归推理模型在迷宫、国际象棋和数独等结构化问题上的求解质量与稳定性。 |
| [^376] | [Accelerating Diffusion Transformers with Gaussian Process Rectified Feature Cache](https://arxiv.org/abs/2609.05981) | 本文发现缓存特征与全计算轨迹之间的残差局部服从零均值高斯分布，据此将全计算特征视为含噪观测，提出利用高斯过程在线回归动态校正特征缓存偏差的方法，在无需标签数据的情况下有效加速扩散Transformer。 |
| [^377] | [Machine Learning for Pre-Culture ESBL Risk Stratification to Guide Empiric Antibiotic Selection: A 12-Hospital Study of Enterobacteriaceae Cultures](https://arxiv.org/abs/2609.05970) | 该研究开发了一种基于培养前电子健康记录特征的机器学习模型，可在培养结果出来前预测ESBL感染风险，有望在非ICU环境中帮助临床医生安全地减少碳青霉烯类药物的过度使用。 |
| [^378] | [On-the-go Forgetting without Explicit Unlearning via ERASE](https://arxiv.org/abs/2609.05966) | ERASE是一个无需修改模型权重的即时遗忘框架，通过类别条件化的输入扰动在推理阶段实现选择性遗忘，避免了传统遗忘方法的重训练、微调和额外内存开销。 |
| [^379] | [LoGIC: Budgeted Context Construction for Node-Level Graph In-Context Learning with Tabular Foundation Models](https://arxiv.org/abs/2609.05955) | 提出LoGIC方法，通过在带标签上下文预算和无标签光环预算下检索并跨查询共享上下文，解决了节点级图上下文学习中注意力随带标签池二次方增长导致的预处理和内存瓶颈问题。 |
| [^380] | [QGB-W$k$NN: Quantum Granular-Ball Learning for Robust Classification](https://arxiv.org/abs/2609.05952) | 本文提出基于量子颗粒球的加权K近邻分类框架QGB-W$k$NN，通过量子增强颗粒球表示、层次化近邻搜索和纯度感知加权决策机制，同时提升了计算效率与噪声环境下的分类鲁棒性。 |
| [^381] | [Interpretable and Fair Generalized Additive Neural Networks via Multi-objective Learning](https://arxiv.org/abs/2609.05946) | 本文提出多目标神经基模型（MONBM），通过引入可解释性的显式定量指标，首次实现了基于神经网络的广义可加模型中可解释性与公平性的同时优化，并深入分析了两者的权衡关系。 |
| [^382] | [Rethinking the Evaluation of Efficiency Methods for Multi-Agent Systems](https://arxiv.org/abs/2609.05933) | 本文指出现有多智能体系统效率方法的评估可能高估了其真实效果，并提出一个受控且对MAS有需求的诊断基准，在共享环境下公平地评估这些效率方法。 |
| [^383] | [A dictionary learning framework for graphs via filters and optimal transport](https://arxiv.org/abs/2609.05919) | 本文提出了一种基于滤波图距离与最优传输的图字典学习框架，将图表示为滤波拉普拉斯导出的高斯分布，通过可端到端优化的sfGOT距离实现图的重构，并从HSIC视角给出了新的理论解释。 |
| [^384] | [Structurally Close, Temporally Distant: Measuring Security Exposure in Long-Horizon LLM Agents](https://arxiv.org/abs/2609.05911) | 该论文提出了一种溯源感知的执行图与“影响距离”新度量，证明在有状态的长时程LLM智能体中，仅凭时间步骤数衡量攻击距离会高估安全隔离，并通过“间隔”量化被步骤数掩盖的真实安全暴露。 |
| [^385] | [From Narrative to Auditable Forecasts: A Structured Scaffold for Agentic Forecasting](https://arxiv.org/abs/2609.05905) | 提出AuditForecast结构化预测支架，通过基线模型锚定、模型引导的数据检索以及赔率空间中的机械聚合，将LLM智能体预测从隐式叙事判断转变为具有明确中间步骤的可审计过程。 |
| [^386] | [Polarity-Asymmetric Structural Calibration for Link Sign Prediction](https://arxiv.org/abs/2609.05896) | 该论文提出极性非对称结构校准框架PASC，通过目标边结构先验得分与局部符号上下文线索的冲突残差来校准符号注意力聚合，提升了符号网络链接符号预测在严重符号不平衡和局部冲突场景下的鲁棒性。 |
| [^387] | [A First-Order Learning Algorithm for Online Resource Allocation with Constant Regret](https://arxiv.org/abs/2609.05895) | 该论文提出了一种无需求解线性规划的一阶学习策略，在请求到达概率未知的在线资源分配问题中实现了与时域长度无关的常数期望后悔界。 |
| [^388] | [One Rate Is Not Enough: Adaptive Anisotropic Learning Rates for LoRA Fine-Tuning](https://arxiv.org/abs/2609.05885) | 提出AnLR-LoRA，一种为LoRA适配器的每个秩一分量自适应分配有效学习率的各向异性方法，该方法利用AdamW优化中的函数空间速度和Adam信噪比在线计算学习率并按模块归一化，解决了统一学习率惯例忽视模块内异质性、导致秩预算利用不足的问题。 |
| [^389] | [CALM: Class-wise Agreement and Label-gated Disagreement Modulation for Decentralized Federated Learning](https://arxiv.org/abs/2609.05884) | 提出CALM方法，在去中心化联邦蒸馏中以平滑的多层信任门控（类级一致性加权和样本级分歧调制）取代现有硬性的全有或全无决策，从而提升严重非独立同分布数据下的模型聚合可信度和精度。 |
| [^390] | [Broken on Arrival: Silently Defective LLM Artifacts in Public Model Registries and How to Catch Them](https://arxiv.org/abs/2609.05881) | 首次系统性冒烟测试了公共模型注册库中的327个量化GGUF模型制品，发现官方Ollama库中存在五个静默失效的量化模型转换制品，并提出了一套可有效捕获此类缺陷的检测流程。 |
| [^391] | [Grounded and Faithful P&ID Reasoning: Constraining Vision-Language Models with Recovered Evidence Graphs](https://arxiv.org/abs/2609.05880) | 本文提出从P&ID图纸中恢复显式证据图，并约束视觉语言模型仅通过七个只读算子查询该图作答、使拓扑结论必须引用支持的查询结果，从而将TopoPID-VQA上的精确匹配准确率从约36.7–41.3%大幅提升至约74%。 |
| [^392] | [A budget-dependent crossover between coverage- and response-based training-set selection for machine-learned interatomic potentials](https://arxiv.org/abs/2609.05877) | 本研究揭示了机器学习原子间势训练集选择中，覆盖度导向与响应导向（基于模型分歧）两种策略的优劣会随数据保留预算的变化而发生交叉转变，表明单一训练集规模下的比较不足以确定最优选择策略。 |
| [^393] | [Budgeted Task-Aware Acquisition of Dynamic Networks](https://arxiv.org/abs/2609.05862) | 本文提出轻量级框架Scout，能在预算约束下学习查询动态网络中每个节点对下游任务的价值，从而智能地选择性刷新过时的图信息，在绝大多数基准设置中取得最优下游性能。 |
| [^394] | [SLA-Safe Energy Control for AI-Native NG-RAN Using Stability-Aware Constrained PPO](https://arxiv.org/abs/2609.05861) | 本文提出一种基于稳定性感知约束PPO（带自适应拉格朗日惩罚）的强化学习框架，用于5G NG-RAN的SLA安全节能控制，在保障用户QoS和SLA的前提下动态优化小区节能模式切换。 |
| [^395] | [Beyond Arbitrary Geometry: Topology Generalization In neural PDE Operators](https://arxiv.org/abs/2609.05860) | 该论文引入TopoBox-3D基准并通过霍奇热流的受控分析，揭示了神经PDE算子拓扑泛化的主要困难来自谱特性（初始瑞利商是最稳定的误差预测指标）而非调和维度变化，且显式拓扑坐标信息并不总是最优方案。 |
| [^396] | [Selective Posterior Margin Regularization for Forward-Corrected Classification](https://arxiv.org/abs/2609.05859) | 提出选择性后验边际正则化（SPMR），在保留前向校正目标的基础上，将反向后验主导类别与标注的不一致转化为按后验间隔分级缩放的成对边际更新，从而有效抑制网络对噪声标签的记忆。 |
| [^397] | [SAFEGuard: Detect Optimization-Based Jailbreak Attacks Through Harmful Semantic Analysis and Fluency Measurement](https://arxiv.org/abs/2609.05850) | 提出统一检测框架SAFEGuard，结合基于跨层分布距离与困惑度的混合流畅度测量以及基于梯度匹配的有害语义分析，有效检测针对LLM的基于优化的越狱攻击。 |
| [^398] | [Functional Attentive Interpretable Regression](https://arxiv.org/abs/2609.05846) | 提出FAIR方法，通过坐标特征与自注意力机制直接学习函数对函数回归中系数面的复杂支撑结构，实现局部与全局尺度的自适应信息共享，并在稀疏与平滑约束下获得边界连贯的局部化估计。 |
| [^399] | [Do Quantum AIs Dream in Paths? Path-Integral Slow Thinking through Grover Interference](https://arxiv.org/abs/2609.05842) | 该论文提出将“慢思考”建模为推理轨迹上的量子路径积分，借助Grover振幅放大使接受与拒绝轨迹发生干涉，使最优成功概率收敛于一个内部解析值，从而避免传统强化学习中的策略坍缩问题。 |
| [^400] | [Scaling Optimal Classification Trees via Adaptive Feature and Sample Reduction](https://arxiv.org/abs/2609.05826) | 该论文提出基于STreeD的加权与自适应联合特征-样本缩减框架，通过合并重复样本记录并迭代细化候选特征集，在保证候选集内最优性的前提下将最优分类树的求解速度提升最高121倍以上。 |
| [^401] | [Generalizing HVAC Control With Domain Randomized Reinforcement Learning](https://arxiv.org/abs/2609.05822) | 提出了基于物理信息归一化流的自适应域随机化强化学习控制器NOMAD-RL，无需精确建筑模型或现场调参，即可通过通用非侵入式恒温器接口在异构热区之间实现HVAC控制的泛化迁移。 |
| [^402] | [Online Learning with LLM Experts from Limited Feedback](https://arxiv.org/abs/2609.05820) | 该论文提出了在有限反馈预算下将提示自适应路由到LLM专家的在线学习算法，在完全信息设置和老虎机设置下分别实现了$\tilde{O}(dT/\sqrt{m})$和$\tilde{O}(dT\sqrt{K/m})$的遗憾界，能够高效学习高质量的LLM路由策略。 |
| [^403] | [Physical policy gradient theorem for in situ stochastic-adjoint training](https://arxiv.org/abs/2609.05808) | 本文提出了策略梯度定理的物理对应形式——一种随机伴随梯度估计器，通过用非简并扩散取代互易性限制，仅凭测量得到的随机轨迹即可直接从物理系统中原位提取参数梯度，并在非线性谐振器网络上成功实现了训练。 |
| [^404] | [Diagonal Attenuation: A Finite-Sample Correction for PCA](https://arxiv.org/abs/2609.05796) | 提出对角衰减方法，通过降低坐标方向样本方差、保留样本交叉协方差来校正有限样本下PCA的旋转偏差，并给出一致的期望风险显式定理及渐近最优衰减强度。 |
| [^405] | [Nonlinear elliptic homogenization with the parametric Deep Ritz method](https://arxiv.org/abs/2609.05778) | 本文提出采用参数化Deep Ritz方法求解非线性椭圆均匀化中的参数化胞元问题，从而获得胞元问题解的可微表示，以支持非线性均匀化本构关系的数值计算。 |
| [^406] | [RAPTOR: Role-Aware Private Training for Mixture-of-Experts](https://arxiv.org/abs/2609.05770) | RAPTOR提出了一种角色感知的差分隐私训练框架，通过专家特定的裁剪与噪声、公共期望所有者分母和计数无关的更新计划，解决了现有DP微调方法在稀疏MoE模型上的三种失效模式，同时保持(ε,δ)-DP保证。 |
| [^407] | [Data Scout: Targeted Web Crawling for Domain-Specific Pretraining Corpora](https://arxiv.org/abs/2609.05766) | Data Scout提出用“定向爬取”取代传统的“过滤存档”范式，利用LLM将主题扩展为分类体系和搜索查询，并借助子域名级分类器探针进行筛选，使爬取页面中高质量数学内容占比达到21.9%，是传统过滤方法的70倍。 |
| [^408] | [AAMBERS-UAV: Acquisition-Aware Multimodal Backbone Evaluation and Ranking for UAV Weedy Rice Segmentation](https://arxiv.org/abs/2609.05762) | 该研究提出采集感知的评估框架AAMBERS-UAV，揭示了数据集划分方式对无人机杂草稻分割中多模态输入排名的关键影响——完全采集留存时RGB表现最佳，而目标采集暴露后RGB+多光谱最优，且这一结论在U-Net/ResNet18上得到复现验证。 |
| [^409] | [A Multi-Source Ensemble Approach to Candidate Generation for Alternative Vacation Rental Property Recommendations](https://arxiv.org/abs/2609.05748) | 该论文提出一种将基于物品的协同过滤与图神经网络检索相结合的多源混合候选生成架构，在拥有超过200万房源的度假租赁平台上将替代房源推荐的Recall@300提升了14.8%。 |
| [^410] | [Gaussian Linear Functional Manifold Method for Massive Point Cloud Data](https://arxiv.org/abs/2609.05744) | 提出高斯线性泛函流形（GLFM）物理信息统计框架，结合SVD降秩算法实现线性时间参数估计，解决了复杂城乡交错带环境中海量机载LiDAR点云的连续地形重建难题，兼具结构可解释性与无需逐点标注的优势。 |
| [^411] | [RenderFormer-V2: Neural Rendering with Heterogeneous Scene Primitives](https://arxiv.org/abs/2609.05738) | RenderFormer-V2是一个无需针对每个场景训练的统一Transformer神经渲染模型，通过在视角无关阶段引入窗口注意力与渲染信息注意力池相结合的新机制，在保持渲染精度的同时提升可扩展性，并能处理焦散、体积散射、环境光照等多样化全局光传输效果。 |
| [^412] | [Newton Matching for Generative Modeling: A Unified Framework for Fine-Tuning and Sampling](https://arxiv.org/abs/2609.05727) | 提出牛顿匹配统一框架，通过证明规范模型流形上反向KL散度的Hessian矩阵恰好等于Fisher-Rao度量，使牛顿方向与负Fisher-Rao梯度方向重合，从而统一了生成式建模中的微调与采样任务。 |
| [^413] | [Tight Lower Bounds for State Tomography with Limited Entanglement](https://arxiv.org/abs/2609.05718) | 本文证明了在每次测量最多使用 $k$ 个新鲜副本且无量子存储的受限纠缠设定下，$d$ 维量子态层析的副本复杂度下界为 $\max\{d^3/(\sqrt{k}\epsilon^2), d^2/\epsilon^2\}$，与已知上界紧致匹配，并适用于任意块内测量和块间自适应策略。 |
| [^414] | [CUSP: Decomposable Collective Uncertainty for Multi-Agent Multimodal Reasoning](https://arxiv.org/abs/2609.05708) | 提出了无需训练的CUSP框架，通过语义意见池化量化多智能体多模态推理的系统级不确定性，并能将集体熵精确分解为模型个体语义熵均值与模型间冲突（JSD）两个可解释的信号。 |
| [^415] | [Characterizing Privacy Risks of Quantum Machine Learning with Emergent Quantum-Native Access](https://arxiv.org/abs/2609.05702) | 本文突破了以往假设攻击者仅有经典计算能力的局限，首次系统刻画了在用户/攻击者具备量子计算能力和量子信息访问权限的量子原生场景下，量子机器学习模型（如成员推断攻击）所面临的隐私风险。 |
| [^416] | [Analysis of Respiratory Sinus Arrhythmia with Neural Networks](https://arxiv.org/abs/2609.05698) | 本文开发了三种深度神经网络架构，利用呼吸性窦性心律不齐现象直接从心电图信号预测呼吸波形以估计呼吸频率，实现了无需人工预处理的稳健可扩展的无创呼吸监测方案。 |
| [^417] | [GraphNOSE: A Graph Transformer in Olfaction](https://arxiv.org/abs/2609.05694) | GraphNOSE是一个开源图Transformer框架，能从SMILES字符串预测单分子和二元混合物的气味描述符，其参数量仅为标准GNN的六分之一，同时在AUROC指标上平均超越现有方法4.52%。 |
| [^418] | [Connecting Score Matching, Maximum Likelihood, and Expectation-Maximization in Mixed Linear Regression](https://arxiv.org/abs/2609.05688) | 该论文在混合线性回归中建立了得分匹配损失与最大似然估计及期望最大化（EM）算子之间的理论联系，证明了基于扩散的估计器能收敛到真实参数并达到最大似然估计的高斯极限。 |
| [^419] | [PAC-Private Autoregressive Generation: Calibrating Noise to Ensemble Disagreement](https://arxiv.org/abs/2609.05676) | 本文将PAC隐私保护预测从分类任务首次扩展到自回归生成，通过在128个重叠世界（各自训练适配器）上投票，并根据各世界后验加权的分歧程度动态校准噪声，在预测一致时减少噪声，从而在保护隐私的同时提升生成质量。 |
| [^420] | [Srijika: OpenType-Layout-Reusing Font Restyling for Nine Indic Scripts](https://arxiv.org/abs/2609.05661) | Srijika通过复用排版完整模板字体的OpenType布局数据（cmap、GSUB闭包和GPOS）来重新样式化字形轮廓，为九种印度文字生成构造上保证排版一致性的可安装字体，从而解决了合体字等复杂文本形式难以保持一致的生成难题。 |
| [^421] | [Robustness of LLM-Generated SystemVerilog Assertions to Semantics-Preserving RTL Transformations](https://arxiv.org/abs/2609.05658) | 本文提出了一种受控蜕变评估方法，通过操作数重排序、标识符重命名和冗余括号化三种语义保持的RTL变换，系统评估了LLM生成SystemVerilog断言的输出稳定性，揭示了传统单一点准确率无法反映模型鲁棒性的问题。 |
| [^422] | [Endogenous Exploration in Reinforcement Learning with Intrinsic Curiosity](https://arxiv.org/abs/2609.05650) | 该论文提出一种由内在好奇心驱动探索的强化学习框架，基于“有效探索产生于中等不一致水平”这一核心假设，在液态状态机基底上实现，并在LunarLanderv2和BipedalWalkerv3两个基准上达到了与PPO等成熟算法相当的性能。 |
| [^423] | [A Nuclear-Norm Lower Bound for Dithered Scalar Quantization of Matrix Products](https://arxiv.org/abs/2609.05641) | 该论文证明了矩阵乘积抖动标量量化误差的核范数下界，并通过SVD对齐的Hadamard构造证明该下界是紧的。 |
| [^424] | [HB-PVI: A Hierarchical Bayesian Personalization and Value-of-Information Framework for Complex Activity Recognition](https://arxiv.org/abs/2609.05582) | 提出HB-PVI框架，通过层次贝叶斯模型与EVSI停止规则，联合量化复杂活动识别中参与者异质性、四种个性化机制的收益以及校准标签的经济价值。 |
| [^425] | [Capsule Lens: Locating and Tracking Concept Geometry in Model Representations](https://arxiv.org/abs/2609.05575) | 提出Capsule Lens框架，通过闭式拟合由可解释参数定义的胶囊几何形状，来定位和追踪机器学习模型表示中概念的几何结构，并可同时应用于静态与动态表示场景。 |
| [^426] | [Multi-granularity Adaptive Hypergraph Representation Learning via Granular-ball](https://arxiv.org/abs/2609.05574) | 提出了基于粒球的多粒度超图表示学习框架MGHRL，通过自适应粒度超图生成策略在多个粒度层级上生成超边，从而自适应地发现图中的高阶关系并高效处理复杂结构信息。 |
| [^427] | [Deep belief networks are exact](https://arxiv.org/abs/2609.05572) | 本文证明了 \(\{-1,1\}^n\) 上任意严格正的概率分布都能被有限参数的 Sigmoid 信念网络精确表示，利用布劳威尔不动点定理回答了 Sutskever 和 Hinton 的开放性问题。 |
| [^428] | [Toward Sustainable Distributed LLM Inference: A Systems Synthesis and Research Agenda for an Energy-, Carbon-, and Cache-Aware llm-d Control Plane](https://arxiv.org/abs/2609.05565) | 本文综合现有系统研究成果，归纳出可复用的设计模式，提出面向llm-d分布式推理控制平面的能源、碳排与缓存感知的可持续推理控制平面（SICP）框架与研究议程。 |
| [^429] | [Rollcast: Proper-Score Gated Rolling Anchors for Adaptive Probabilistic Time-Series Forecasting](https://arxiv.org/abs/2609.05561) | Rollcast 提出了一种用适当评分门控的滚动统计锚点集合替代单一全局模型的概率预测方法，通过状态依赖门控机制和相似历史状态的残差分布，在多种时间序列动态过程中实现自适应的不确定性量化。 |
| [^430] | [Physics-Informed Neural Networks for Depth-Averaged Granular Avalanche Dynamics on Curved Topography](https://arxiv.org/abs/2609.05542) | 本研究将基于Savage-Hutter方程的深度平均物理信息神经网络（PINN）扩展至指数弯曲溜槽上的颗粒雪崩动力学，证明分阶段时间课程学习可将保留观测的轨迹误差降低约两个数量级，且稀疏数据下观测点的位置比观测数量更为重要。 |
| [^431] | [SimpleMemVLA: A Simple but Effective Native-Video Memory for Vision-Language-Action Models](https://arxiv.org/abs/2609.05533) | SimpleMemVLA提出了一种无需专用记忆模块的视觉-语言-动作模型，通过完整保留历史信息并以时间戳视频格式直接输入骨干网络，利用子任务隐藏状态作为历史到流匹配动作头的唯一通道，从而有效解决长时程操作中的部分可观测问题。 |
| [^432] | [A Specialized Large Multimodal Model for Interpreting PET/CT in Head and Neck Cancer](https://arxiv.org/abs/2609.05532) | 研究者利用大规模多机构PET/CT数据集和两级训练课程微调LLaVA-NeXT，开发出一种专用大型多模态模型，实现了头颈癌PET/CT的自动化解读，验证了专用LMM在医学影像诊断中的可行性。 |
| [^433] | [Infrastructure-based Monocular 3D Vehicle Localization Framework with Experimental Validation](https://arxiv.org/abs/2609.05523) | 本文提出一种单阶段学习框架，直接从路边单目相机图像联合估计车辆在地面固定坐标系中的位置、尺寸和偏航角，并利用路边相机与无人机同步视频构建数据采集与标注流水线，完成了实验验证。 |
| [^434] | [Contrastive Knowledge Distillation for Anomaly Detection in Multi-Illumination/Focus Display Images](https://arxiv.org/abs/2609.05520) | 本文提出多分辨率对比蒸馏（MCD）方法，无需正负样本对，通过拉近/推远师生网络特征距离，实现多光照/多焦点显示图像中微小缺陷的异常检测。 |
| [^435] | [An Exploratory Study of Frequency-Aware Task Weighting for YOLOv8-Based Unified Driving Perception](https://arxiv.org/abs/2609.05516) | 本文提出频率感知任务加权（FTW）方法，通过分析近期损失历史轨迹的低频能量比率来估计任务稳定性，并据此动态调整任务权重，以改善基于YOLOv8的统一驾驶感知中多任务联合优化的平衡问题。 |
| [^436] | [When and What to Teach: Budget-Aware Online Adaptation for Web Agents](https://arxiv.org/abs/2609.05513) | 该论文提出了一种预算感知的在线教学框架，通过可解性感知的教师门控决定何时查询教师模型，并通过分数引导的轨迹修剪筛选教学内容，从而避免在不可解决的回合和冗余执行步骤上浪费训练预算。 |
| [^437] | [When Do Options Help? Policy Necrosis and Redundant Coverage in Option-Critic](https://arxiv.org/abs/2609.05508) | 该论文通过理论和实验证明，Option-Critic中学习到的终止规则毫无作用（甚至可能阻碍探索并产生Ω(T)的遗憾），且选项内部策略几乎不探索会导致“策略坏死”，从而解释了性能提升实际来源于增加选项数量所带来的冗余覆盖。 |
| [^438] | [Asymptotically-informed neural networks for Black-Scholes implied volatility computation](https://arxiv.org/abs/2609.05491) | 本文提出一类渐近信息驱动的神经网络架构，通过门控函数系统对价格域进行可训练划分并结合专门化的局部近似，实现了Black-Scholes隐含波动的稳健高效计算，尤其适用于极端价格、行权价或期限的渐近区域。 |
| [^439] | [Architectural and Regularization Components in Deep Learning Medical Image Registration: Systematic Ablation Study](https://arxiv.org/abs/2609.05484) | 本文通过系统性消融实验发现，在深度学习医学图像配准中，正则化损失对性能提升的贡献远大于仿射预对齐等架构增强组件，仅正则化即可带来21.3%的相对增益。 |
| [^440] | [ARC-Bench: Closed-Loop Replanning Masks Broken Action Ranking in Frozen JEPA World Models](https://arxiv.org/abs/2609.05461) | 该论文提出ARC-Bench无泄漏审计协议，揭示冻结JEPA世界模型中“潜在距离可正确排序动作”的核心假设严重且结构性失效，且该缺陷在不同视觉骨干网络下均持续存在。 |
| [^441] | [Condition aware learning enables robust prediction of oligonucleotide melting behavior across diverse chemistries and assay conditions](https://arxiv.org/abs/2609.05454) | 提出了一种条件感知的核苷酸语言模型，将上下文序列表示与反应环境信息相结合，能够在多种实验条件下准确预测未修饰和化学修饰寡核苷酸的熔解行为，克服了传统固定参数热力学模型难以推广的局限。 |
| [^442] | [ZetaDial: dialing net charge of protein binders at inference time for therapeutic developability](https://arxiv.org/abs/2609.05451) | ZetaDial 提出了一种围绕固定骨架 ProteinMPNN 的采样后逐蛋白割线控制器，能够在推理时将治疗性蛋白质结合物的净电荷精确调节至目标设定值，显著降低电荷误差并提升药物可开发性。 |
| [^443] | [Damage-Aware Bandit Pruning for Vision and Language Transformers](https://arxiv.org/abs/2609.05448) | 提出了一种损伤感知的多臂赌博机框架，通过配对损伤评估和UCB/分数Beta汤普森采样策略，在固定评估预算下为视觉和语言Transformer选择抑制后性能损失最小的结构单元（注意力头和MLP通道组）。 |
| [^444] | [Representation learning of human cortical folding to reveal long lasting neurodevelopmental signatures](https://arxiv.org/abs/2609.05438) | 本文提出自监督学习框架Champollion，能从结构MRI中学习可解释的皮层折叠局部表征，其性能超越现有神经影像及通用基础模型，并揭示出比传统形态学指标更丰富的遗传关联和持久的神经发育特征。 |
| [^445] | [Novel hybrid protein scaffold gap filling using weighted machine learning ensemble, beam search, and mass-constrained reranking](https://arxiv.org/abs/2609.05436) | 本研究提出了一种融合加权机器学习集成、束搜索和质量约束重排序的混合框架，用于解决蛋白质序列重建中已知缺口大小和质量条件下的支架缺口填充问题。 |
| [^446] | [AhaBench: Do Agents Learn from Prior Experience? A Benchmark for Long-Horizon Continual Learning](https://arxiv.org/abs/2609.05435) | AhaBench 是一个长时程持续学习基准，通过谜题探索、带精确验证器的数学任务和自动售货机模拟三个组件，评估固定模型在获得先验经验后，在相关但支持已被移除、改变或延迟的条件下行为是否真正得到改善。 |
| [^447] | [Benchmarking Storage Systems for Machine Learning Workloads Using NIO Bench](https://arxiv.org/abs/2609.05418) | 本文提出了NIO Bench基准测试框架，通过Python级I/O钩子与Linux strace相结合的两层追踪方法，刻画了六种机器学习模型架构的存储访问模式，发现I/O主要集中在数据准备、模型加载和检查点保存阶段，且训练是计算受限而非数据受限的。 |
| [^448] | [Learning-Augmented Algorithms: Guarantees, Construction Mechanisms, and System-Level Implications](https://arxiv.org/abs/2609.04787) | 本综述系统梳理了学习增强算法领域的预测接口、误差度量、一致性—鲁棒性权衡及五种代表性构造机制，明确区分形式化保证与实证系统证据，并指出了该领域的未来开放问题。 |
| [^449] | [Spectral-Target Physical Latent Structuring for JEPA-Style World Models](https://arxiv.org/abs/2609.04264) | 该论文发现了JEPA风格世界模型中“物理表示惰性”这一新失败模式，并提出轻量级傅里叶辅助头在训练时对潜在空间施加物理信息结构化约束，在不增加推理成本的情况下有效提升下游规划性能。 |
| [^450] | [EXAONE Forecast for Finance](https://arxiv.org/abs/2609.04239) | EXAONE Finance是一个专为金融预测设计的时间序列基础模型，其核心创新在于采用无注意力机制的高效架构，通过因果一维卷积和组感知池化两个线性时间复杂度算子取代自注意力机制，从而解决了现有模型计算成本过高、无法处理间歇性观测数据以及未能捕捉金融市场独特动态的问题。 |
| [^451] | [Free Pause Tokens](https://arxiv.org/abs/2609.03807) | 提出免费暂停标记，通过权重共享主干上的并行预测流为模型提供额外思考计算，在不增加上下文长度、KV缓存和推理延迟的情况下，仅以1.14倍训练计算量的代价提升下一个词元预测性能。 |
| [^452] | [Dude: A Dual-Detection Multi-Agent System for Paper-Code Discrepancy Detection](https://arxiv.org/abs/2609.03416) | 提出了首个用于论文-代码差异检测的双检测多智能体系统Dude，通过粒度对齐协商机制和两阶段显著性过滤机制，有效解决了论文语言与代码语言粒度不对称导致的误报问题。 |
| [^453] | [VoxReason: Listener-Free Evaluation of Source-Grounded Speech Planning Before Synthesis](https://arxiv.org/abs/2609.03203) | VoxReason提出了一种无需听者参与的评估任务，在语音合成之前通过带证据引用的说话计划和确定性验证器，衡量语音表达方式的选择是否真正建立在被引用的源记录之上。 |
| [^454] | [Privacy Leakage in Federated Learning: Gradient-Based Client Identity Inference and Defenses for Inertial Sensing in Vehicular Edge Networks](https://arxiv.org/abs/2609.02971) | 该论文揭示了在车联网联邦学习中，服务器可从客户端上传的梯度更新中以近乎完美的准确率推断出基于IMU惯性数据的客户端身份，证实了严重的匿名性隐私泄露风险并提出了相应的防御方法。 |
| [^455] | [SPADE: SPaT Attack Detection from the Connected Vehicle's Perspective](https://arxiv.org/abs/2609.02741) | 本文提出了SPADE——首个专为网联汽车车载视角下的SPaT攻击检测深度学习研究设计的带标签多模态仿真数据集，填补了入侵检测研究在SPaT消息完整性防护领域的空白。 |
| [^456] | [FlashKAN: B-Spline KANs via Truncated Power Form](https://arxiv.org/abs/2609.01956) | FlashKAN用逼近论中的截断幂形式取代Cox-de Boor递归，通过torch.compile融合为单一GPU内核并结合有界坐标稳定化，显著加速了KAN中B样条激活函数的计算，并提供了开源软件包。 |
| [^457] | [Pooling and Drift in Delayed Bandits](https://arxiv.org/abs/2609.01761) | 该论文发现当延迟老虎机的反馈结果仅通过动作所产生的状态依赖于动作时，学习代价由有效维度（真正不同的状态数量）而非动作数量决定，并据此证明了 $\widetilde{O}(\sqrt{(d+1)V\log K})$ 等新的遗憾界，突破了以往随动作数增长的界限。 |
| [^458] | [PRISM: An Agentic Multi-Model Architecture for Proactive Safety in Autonomous Transportation Systems](https://arxiv.org/abs/2609.01623) | PRISM是一种智能体多模型安全架构，通过逆向事故概率建模将事故分类器转化为可解释的动态安全评分，并由推理层协调三个并发专门模型（轨迹运动学、环境风险、VRU交互），实现从被动事故规避到主动持续风险管理的转变。 |
| [^459] | [SMELT: Scaling Laws for Compute-Matched MoE Looped Transformers](https://arxiv.org/abs/2609.01343) | SMELT在严格匹配计算量、参数量和KV缓存的条件下将MoE Transformer的中间层循环两次，其损失下降更快，可在计算最优前沿节省6.8%–18.0%的训练FLOPs，且在代码任务和长样本上优势更明显。 |
| [^460] | [EEG-VID: Task-Guided Latent Predictive Pretraining for EEG Decoding and Assistive Target Selection](https://arxiv.org/abs/2609.00566) | EEG-VID提出了一种任务引导式潜变量预测预训练框架，通过指数移动平均编码器预测未来EEG潜状态，在42组跨会话跨被试对比中有41组提升准确率（最高提升16.22个百分点），并可有效应用于场景约束下的辅助目标选择。 |
| [^461] | [A convolutional framework for detecting event-driven dynamics in energy price series](https://arxiv.org/abs/2609.00402) | 本文提出了一个通用卷积神经网络框架，可精确表示并逼近多种事件检测统计量（如极差、最大回撤、波动率等），并在能源价格序列中有效识别出以地缘政治为主的事件驱动动态。 |
| [^462] | [Generative artificial intelligence for reliable mechanistic reasoning for corrosion](https://arxiv.org/abs/2609.00099) | 该研究提出了一个面向腐蚀领域的检索增强生成框架，通过对三个开源大语言模型在专家验证的问答数据上进行微调并结合混合检索流水线，实现了兼具准确性与机理可靠性的腐蚀知识推理，在镁合金腐蚀上取得了显著效果。 |
| [^463] | [Assessing Alignment and Stability of Feature Importance Explanations via Weight of Evidence](https://arxiv.org/abs/2609.00090) | 该论文提出了一个基于证据权重的假设检验框架，能够从原理上评估特征重要性解释方法与先验知识的对齐程度及其稳定性，并将其应用于LIME和SHAP的分析中。 |
| [^464] | [When Prediction Error Is Not Enough: Evaluating Nuisance-Function Prediction for Causal Estimation](https://arxiv.org/abs/2609.00071) | 在部分线性模型的模拟研究中，干扰函数的预测误差无法一致地反映因果估计的偏差，表明仅用预测误差来评估干扰函数估计器对于因果推断而言是不充分的。 |
| [^465] | [ReNFT: Repairing Mode Collapse in Reward Post-Training via Internal Probability-Mass Recalibration](https://arxiv.org/abs/2609.00061) | 本文提出ReNFT方法，通过内部概率质量重新校准修复扩散模型奖励后训练中出现的模式坍塌，在保留已获得奖励的同时恢复提示内多样性，无需依赖任何外部信号或接口。 |
| [^466] | [CoJEPA: Combining Contrastive Learning and JEPA for Global-Local Music Representations](https://arxiv.org/abs/2608.30974) | CoJEPA将对比学习与JEPA联合训练于单一共享骨干网络中，利用对比梯度取代EMA教师以稳定训练，同时通过JEPA的局部预测丰富序列标记，在不增加任何额外参数的情况下学习全局-局部音乐表征。 |
| [^467] | [Singular Curvature in ReLU Training:Differentiation and the Gradient-Flow Limit Need Not Commute](https://arxiv.org/abs/2608.30960) | 该论文证明在 ReLU 训练中“先微分离散梯度下降再取极限”与“先取梯度流极限再微分”不可交换：离散精确导数收敛到无事件的区域传播子，而极限流的导数额外包含速度归一化的激活事件转移，由此产生秩一的奇异曲率差异，且全局凸性也无法将其完全抵消。 |
| [^468] | [Coarse composition suffices: tabular in-context learning for multi-activity antimicrobial peptide profiling](https://arxiv.org/abs/2608.30337) | 该研究证明，仅用330个可解释序列描述符结合无需梯度训练的TabPFN表格基础模型，即可在ESCAPE多标签抗菌肽活性预测基准上超越复杂的多模态深度模型，将mAP-5从72.1%提升至77.8%。 |
| [^469] | [Dec-BFTRL: Squre-Root Regret for Decentralized Online Upper-Linearizable Optimization under Separation Access with Application to Continuous Submodular Maximization](https://arxiv.org/abs/2608.30271) | 提出去中心化障碍正则化跟随领导者算法 Dec-BFTRL，在分离访问条件下对去中心化在线上可线性化优化实现了 $\widetilde O(\sqrt{T})$ 的平方根遗憾界，并应用于连续 DR 次模最大化问题。 |
| [^470] | [Balance of Benchmarks: Semantic Density Reweighting for Benchmark Multiplicity and Task-Conditioned Evaluation](https://arxiv.org/abs/2608.30044) | 提出基准平衡方法，通过对基准分配逆密度语义权重来纠正基准密集区域的重复计算问题，并支持基于任务查询的条件化模型评估，在586个模型上的留出任务预测相关性从等权加权的0.049大幅提升至0.462。 |
| [^471] | [Error Detection for PET/CT Radiology Reports: Domain-Specific vs Large Language Models](https://arxiv.org/abs/2608.30021) | 该研究首次系统评估了语言模型在PET/CT放射报告错误检测中的表现，结果表明仅1500万参数的领域专用BERT模型即可达到94.4%的平衡准确率，可与更大的开源大语言模型相媲美。 |
| [^472] | [ARMOR: Manifold-Oriented Training for Adversarially Robust Aerial Object Detection under Data Scarcity](https://arxiv.org/abs/2608.29510) | 提出了ARMOR方法，通过流形导向的对抗训练，在训练数据仅有数百张图像的稀缺条件下，实现对物理通用对抗补丁攻击具有鲁棒性的航空目标检测。 |
| [^473] | [PathGuide: Dynamic Classifier-Free Guidance via On-Policy Transport Alignment](https://arxiv.org/abs/2608.29107) | 提出PathGuide框架，将无分类器引导的强度选择从静态参数调整为在线策略传输问题，利用连续性方程的弱形式证明了当引导场与精确条件场弱等价时采样路径与目标条件分布一致，从而实现动态的引导优化。 |
| [^474] | [Euclidean Fourier Neural Operators](https://arxiv.org/abs/2608.28425) | 提出欧几里得傅里叶神经算子（EFNO），通过将谱核参数化为物理波矢的连续函数，使神经算子能够跨不同形状和大小的周期域保持一致地学习与迁移，克服了传统FNO的域依赖性问题。 |
| [^475] | [D-TAIA: Domain-Aware LLM Adaptation for Multi-Task Predictive Process Monitoring](https://arxiv.org/abs/2608.28236) | 提出D-TAIA框架，通过领域感知训练与基于注意力的推理架构对大语言模型进行参数高效微调，在数据稀缺、高流程熵和分布偏移条件下实现下一个活动与剩余时间的联合预测。 |
| [^476] | [Explainable Diabetic Retinopathy Classification Using Vision Foundation Models](https://arxiv.org/abs/2608.28207) | 该研究提出了一种基于视觉基础模型（DINOv2、CLIP、ViT）结合多种迁移学习策略的可解释糖尿病视网膜病变分类框架，其中DINOv2-LoRA内部AUROC达0.758，外部AUROC达0.920，并通过Grad-CAM和HiResCAM结合专家标注病灶掩码实现了模型可解释性评估。 |
| [^477] | [VICT: Verifier-Instrumented Credit Tracing for Long-Horizon LLM Agent Reinforcement Learning](https://arxiv.org/abs/2608.28128) | VICT提出一种训练时接口，将终端验证器内部的可执行或证据支持的原子检查通过依赖证明边追溯到具体动作，并仅沿这些边重新分配组相对优势，从而解决长程LLM智能体强化学习中的细粒度功劳分配问题。 |
| [^478] | [When Can Conditional Flow Matching Replace Pointwise Negative Log-Likelihood?](https://arxiv.org/abs/2608.28010) | 本文通过将端点NLL精确分解为熵、加权CFM目标、内部速度-得分残差和边界残差，刻画了条件流匹配损失何时能替代逐点负对数似然，证明普通CFM通常不是逐点NLL估计器，而特定加权 \(w_{\mathrm{sc}}(t)=(1-t)/t\) 在离策略最优下可消除内部残差，但该结论不能推广到训练或在线策略对齐场景。 |
| [^479] | [Temporal Memory-Aware Online Test-Time Adaptation on Dynamic Graphs](https://arxiv.org/abs/2608.27948) | 提出了DGOTTA框架，通过时序记忆感知的在线测试时自适应方法，有效应对动态图上DGNN模型因结构和语义持续演化而面临的分布偏移挑战。 |
| [^480] | [Actionable CBFI: Integrating Structural Decomposition and Causal Counterfactual Recourse for Tabular Machine Learning](https://arxiv.org/abs/2608.27821) | 该论文提出A-CBFI框架，基于结构因果模型将特征重要性分解与因果反事实补救相结合，通过隔离协同交互瓶颈并释放抑制性结构锁，为表格机器学习生成因果有效、可操作的针对性干预方案，克服了现有方法因果无效与干预分散的问题。 |
| [^481] | [LLMs Can Design Near-Optimal OR Algorithms](https://arxiv.org/abs/2608.27296) | 大语言模型（如gpt-5.6-sol）能够仅凭少量提示和固定计算资源，自动设计出在库存控制、排队网络和分类优化等问题上匹配或超越现有最佳方法的近乎最优运筹学算法。 |
| [^482] | [When Text Misleads: Inconsistent-Aware Reasoning for Audio-Grounded Dialogue](https://arxiv.org/abs/2608.27176) | 本文提出了ContraTalk基准测试，通过跨模态不一致检测框架，系统性地识别并利用文本误导与语音线索之间的冲突，以评估模型在音频对话中是否真正基于语音进行推理。 |
| [^483] | [When Is the Sharp Covariance Envelope Tight? Feature-Only Geometry for Volume-Sampled Least Squares](https://arxiv.org/abs/2608.26877) | 本文建立了体积采样最小二乘中心系数协方差的Loewner包络，并揭示了仅特征边际nu_A决定谱包络严格性的精确条件。 |
| [^484] | [Sharp Minimax Regret for Infinite-Memory Logistic Prediction](https://arxiv.org/abs/2608.26515) | 本文为无限记忆逻辑预测问题建立了尖锐的极小极大遗憾界，证明了滞后分辨谱$\Gamma_T(r)$是精确的累积遗憾尺度，并通过局部贝叶斯混合和Toeplitz设计分别给出了匹配的上界和下界。 |
| [^485] | [Co-Evolving Structured Knowledge and Reasoning in Language Models](https://arxiv.org/abs/2608.26386) | 本文提出KBevo框架，通过端到端联合优化结构化知识库构建与推理，使推理结果直接反馈改进知识库质量，从而提升问答的准确性、可控性和组合推理能力。 |
| [^486] | [When the Canonical Completion Is Wrong: Formalizing and Measuring the Jump in Large Language Models](https://arxiv.org/abs/2608.26187) | 本文首次形式化了LLM中的“跳跃”概念，将其定义为具有可验证证书的有限扩展问题，并提供了度量方法，以解决关于LLM溯因能力的争论。 |
| [^487] | [Unfolding Scientific Papers into Multi-Turn Generation Trajectories for Continued Pre-Training](https://arxiv.org/abs/2608.25826) | 本文提出了一种将科学论文展开为多轮生成轨迹的流水线，通过重建写作过程生成持续预训练语料，规模为源文本两倍，并适用于指令数据和评估。 |
| [^488] | [LM-X: Explainable Action Modeling with Progress, Event, and Uncertainty Prediction for Generalist Robot Manipulation](https://arxiv.org/abs/2608.25757) | 本文提出LM-X框架，通过在线预测任务进度、事件转换和局部不确定性三个显式信号，使VLA策略的动作生成具有内在可解释性，无需事后解释。 |
| [^489] | [A Storage-Retrieval Gap in Parametric Knowledge Graph Memory](https://arxiv.org/abs/2608.25489) | 该论文提出将知识图谱离线编译为LoRA适配器作为参数化知识层，在零查询上下文成本下实现事实知识泛化，但发现存储知识无法通过相似性检索恢复，揭示了参数化记忆中的存储-检索差距。 |
| [^490] | [Token-Oriented Semantic Communication with Pretrained Vision Transformers](https://arxiv.org/abs/2608.25410) | 提出了一种面向令牌的语义通信框架，通过利用预训练视觉变换器和学习图像压缩模型，实现令牌粒度传输而不直接传输令牌嵌入，从而降低通信成本并增强互操作性，无需端到端训练。 |
| [^491] | [Generative Action-Chunk Sampling for Adaptive Stiffness Control in Physical Human-Robot Collaboration](https://arxiv.org/abs/2608.25284) | 通过采样未来动作块并利用其变异性动态调整机器人刚度，实现了在意图明确时提供辅助、在意图模糊时保持顺从的自适应协作控制，显著提升了任务成功率。 |
| [^492] | [On-policy Distillation with Verifiable Reward](https://arxiv.org/abs/2608.24696) | 提出了一种无需额外超参数的无缝结合在线策略蒸馏和可验证奖励强化学习的方法，通过基于轨迹正确性的奖励重构和ReLU门控机制，有效提升大型语言模型后训练性能。 |
| [^493] | [When Do Supervised UQ Ensembles Improve LLM Hallucination Detection? A Robustness Study](https://arxiv.org/abs/2608.24492) | 本文系统评估了监督式UQ集成在不同LLM、数据集和生成模式下的稳健性，发现其性能提升受限于特定条件，并非普遍适用。 |
| [^494] | [Sequential operator learning under dependent data](https://arxiv.org/abs/2608.24426) | 本文提出了在依赖数据下用于算子学习的时间一致自归一化浓度界，无需独立性或混合假设，为自适应和动力学数据驱动的算子学习提供了收敛性保证。 |
| [^495] | [From Relaxed Indexability to Exact Indexability: A $t$-Step Approach for Partially Observable Restless Bandits](https://arxiv.org/abs/2608.24167) | 本文提出一种$t$步前瞻阈值策略，通过多步值迭代扩展了部分可观测静止多臂老虎机的Whittle指数计算，在$t=1$时恢复现有线性阈值，在$t>1$时使阈值依赖补贴，从而更精确地逼近精确可索引性。 |
| [^496] | [SplitLite: Low-Rank Residual Compression for Split Learning](https://arxiv.org/abs/2608.23018) | SplitLite通过利用相邻周期激活和梯度残差的低秩结构（秩-2r和秩-4r），仅传输量化的截断奇异值，大幅降低分割联邦LoRA微调的通信成本。 |
| [^497] | [CatchBench: When Can an Agent Failure Be Caught?](https://arxiv.org/abs/2608.22808) | CatchBench提出了一个在三种信息状态（运行前配置PRE、运行中轨迹前缀LIVE、完成后轨迹POST）下评估智能体故障审计能力的基准，通过七项任务契约对72个参赛者进行评分，并发现现有方法在大多数对比中缺乏区分能力。 |
| [^498] | [Spending Scarce Confirmatory PET Measurements: Target-Aligned Validation in A4/LEARN](https://arxiv.org/abs/2608.22223) | 本文提出了一种目标对齐的PET验证策略，通过结合目标影响和残差不确定性来优化稀缺确认性测量的分配，避免在影响弱的受试者上浪费资源。 |
| [^499] | [Across-Design Uncertainty in Short Pricing Panels: Evidence from Simulated Price Trajectories](https://arxiv.org/abs/2608.21334) | 本文通过模拟价格轨迹证明，短定价面板中跨设计不确定性占估计误差方差的绝大部分（97.6%），并提出了一个经验关系式来描述其分散度。 |
| [^500] | [Learning Exact NVIDIA SASS Encoders with $\mathbb{F}_2$ Linear Algebra](https://arxiv.org/abs/2608.20532) | F2Asm首次利用$\mathbb{F}_2$线性代数学习精确的SASS指令编码器，并成为支持Rubin SM107的开源NVIDIA SASS汇编器。 |
| [^501] | [Decision Tree and K-Means Analysis of Raman Spectra for Edible Oils: A Physics-Informed AI Approach](https://arxiv.org/abs/2608.20440) | 本研究通过物理信息人工智能方法，利用决策树仅需四个拉曼光谱变量即可对五种纯食用油实现100%准确分类，显著简化了食用油鉴别过程。 |
| [^502] | [VA-DPO: Valence-Arousal Direct Preference Optimization for Controllable Emotion Generation in Language Models](https://arxiv.org/abs/2608.20374) | VA-DPO通过将情感目标表示为连续效价-唤醒度点，并基于距离阈值筛选偏好数据，实现了比现有提示方法更精确可控的情感生成，显著降低了目标距离并提升了相关性。 |
| [^503] | [K\"ahler landscapes for complex neural network descents and guarantees including a search and destroy of the Calabi-Yau manifold](https://arxiv.org/abs/2608.19584) | 本文提出了一种在复参数神经网络中使用Kähler信息度量和自然梯度下降的新方法，并针对Calabi-Yau流形上的不良曲率条件提供了理论保证，通过几何定义的全局势实现了搜索与消灭策略。 |
| [^504] | [Learned, Then Lost: A Measured Single-Example Counterfactual in Pre-training](https://arxiv.org/abs/2608.19168) | 通过小规模反事实实验，直接测量了单个训练样本在预训练模型中的即时影响及其衰减，揭示了模型对特定数据的短暂学习能力。 |
| [^505] | [Coordination on a Budget: Federated Active Learning with Few Labels](https://arxiv.org/abs/2608.18634) | 该论文揭示了低预算联邦主动学习中同质数据比异质数据更需协调的异质性反转现象，并提出基于联邦表示学习的框架来优化查询选择。 |
| [^506] | [On the Fragility of Self-Improving Agents: Variance, Task Order, and Underspecification](https://arxiv.org/abs/2608.18066) | 本文通过多运行方差分析和任务顺序随机化实验，揭示了基于记忆的自我改进智能体在评估噪声和任务顺序依赖上的脆弱性，指出其改进效果高度不稳定且受隐含课程影响。 |
| [^507] | [Feature Priming in Online Linear Regression: Sparse-Regret Lower Bounds and a Tight Univariate Rate](https://arxiv.org/abs/2608.17573) | 本文证明在特征启动的在线线性回归中，三种规则均无法实现稀疏对数遗憾，并揭示了干扰插值导致的共同障碍，通过Hadamard构造给出下界。 |
| [^508] | [Temporal Leakage in Financial News NLP: A Multi-Architecture Audit with a Regime-Specific M&A Signal](https://arxiv.org/abs/2608.17223) | 本文通过多架构审计发现，随机划分会严重夸大金融新闻NLP模型的性能，且并购（M&A）是唯一在时间顺序评估下具有稳健正向信号的类别。 |
| [^509] | [Picture the Epsilon: Pursuing Identity-Level Privacy Guarantees for Images](https://arxiv.org/abs/2608.17147) | 本文系统比较了四种审计黑盒人脸生成器身份级差分隐私的方法，并揭示了各方法的适用性与局限性，为隐私保证验证提供了实践指南。 |
| [^510] | [CaliBench: Are the Stochastic Dynamics of Video World Models Physically Calibrated?](https://arxiv.org/abs/2608.16829) | CaliBench提出了一种在物理可解释离散空间中进行视频世界模型校准评估的新方法，将性能分解为可评分性和校准性两个正交维度，并利用闭式已知参考分布实现精确测试。 |
| [^511] | [Advancing Open and Reproducible Relational Learning: RelArena-$\alpha$, TabPFN-Rel and RPI](https://arxiv.org/abs/2608.16319) | 本文通过开源 RelArena-α 统一框架、TabPFN-Rel 和 RPI 三款软件，旨在建立关系学习任务的可复现比较标准，以推动该领域开放科学和实际应用。 |
| [^512] | [Guaranteed Adaptive Modality Acquisition: When the Policy Chooses Its Own Calibration Group](https://arxiv.org/abs/2608.15520) | 本文提出了RouteCert方法，通过两种有限样本构造（终端模式校准和策略-模式对同步认证），解决了自适应模态获取中策略诱导分组导致的条件校准失效问题，并提供了保证有效性的条件及反例验证。 |
| [^513] | [Online Convex Optimization with Dueling Feedback](https://arxiv.org/abs/2608.15050) | 本文首次将决斗反馈应用于对抗性凸优化，通过简单归约转换为近似梯度，实现了静态、自适应和动态遗憾的最优保证，并在光滑和强凸条件下获得改进速率。 |
| [^514] | [Efficient Hessian-Free Methods for Multi-Objective Bilevel Optimization with Nonconvex Lower Level](https://arxiv.org/abs/2608.12704) | 本文提出了一种新的多目标Moreau包络无海森算法（MOMEHA），首次解决了非凸下层问题的多目标双层优化，通过单循环和无海森设计实现了高效计算。 |
| [^515] | [Adversarial Resilience of Poisson-Process Submodular Maximization over Matroids: From Robust Offline Optimization to Full-Bandit Learning](https://arxiv.org/abs/2608.12134) | 本文证明了SGS-Poisson算法在任意受控价值预言机下，无需修改其核心机制，即可保持子模最大化的逼近因子，并由此推导出全臂赌博学习算法。 |
| [^516] | [Mechanist: AI as a Scientific Instrument for Discovering the Mechanisms of Intelligence](https://arxiv.org/abs/2608.12036) | Mechanist是一个自主代理系统，通过集成大规模知识图谱和多学科数据库，将AI作为科学仪器，自动发现AI智能的底层机制，从而弥合模型能力与人类理解控制之间的差距。 |
| [^517] | [How to Verify Probabilistic Consistency of Predictive Models](https://arxiv.org/abs/2608.11181) | 该论文提出了一种交互式PCP协议，使多项式时间的验证者能够通过少量查询验证由概率电路和置信度电路所指定的预测模型的概率自洽性，从而为AI安全中的可验证诚实预测提供了保障。 |
| [^518] | [A Quantum Roadmap for Softmax Attention: Exact Born-Rule Analogs for Softmax Attention on the Probability Simplex](https://arxiv.org/abs/2608.11173) | 该论文证明在输入输出约束于概率单纯形的场景下，softmax注意力可以逐分量地精确映射为量子测量过程（Hadamard检验得分、玻恩规则余弦平方族、后选择温度与确定性数值聚合信道），为注意力的量子实现提供了完整路线图。 |
| [^519] | [DoGMA: A Central-Dogma-Guided Foundation Model for Multi-Omics Alignment and Multi-Task Learning in Oncology](https://arxiv.org/abs/2608.08148) | 提出了DoGMA，一个以中心法则为导向的泛癌症多组学基础模型，通过在Transformer-MoE架构中引入有向注意力和掩码分层组学重构预训练，将组学间信息流约束为中心法则方向，从而在异质性癌症、下游任务和模态不完整设置之间实现稳健迁移。 |
| [^520] | [An AI4AI Framework for Visual Token Pruning](https://arxiv.org/abs/2608.07193) | 提出一种AI4AI框架，利用大语言模型自动设计视觉令牌剪枝算法，通过设计合适的搜索状态表示将LLM的通用算法知识转化为多模态大模型推理加速的专用解决方案。 |
| [^521] | [ArborEnum: Decision Tree Rashomon Sets over Continuous Features](https://arxiv.org/abs/2608.04310) | 该论文提出了首个利用连续特征有序结构来精确枚举决策树Rashomon集合的算法，并进一步提供了用于近似枚举的松弛方法和可逐步细化结果的随时算法。 |
| [^522] | [TimeRLM: Recursive Language Models Enable Precise Anomaly Localization in Long-Context Time-Series](https://arxiv.org/abs/2608.03391) | TimeRLM 提出了一种面向时间序列的递归语言模型方法，让大语言模型通过代码和视觉能力对外部信号进行顺序化操作，从而在长上下文时间序列中实现精确的异常定位，并发布了 AnomalyXL 合成基准数据集。 |
| [^523] | [Pseudorandom Streams within Diffusion Models Act as Learnable Inputs That Affect Generation Quality](https://arxiv.org/abs/2608.02575) | 本文发现扩散模型中的伪随机流若包含可学习结构，会被模型利用以降低损失，从而显著影响生成质量，且这种效应主要源于扩散噪声角色的可预测性。 |
| [^524] | [An AI-Based Decision-Support Pipeline for Day-Ahead Photovoltaic Forecasting](https://arxiv.org/abs/2608.02088) | 该研究针对数据缺失严重的实际光伏站点，提出了一套包含时间戳校验、特征工程和非负最小二乘堆叠五模型组合的AI日前预测流程，在英国充电站数据上优于智能持续性基线。 |
| [^525] | [Celty: SpMSpV GPU Kernel and SIMT Co-Design for Efficient Dual-Sparse LLM Inference](https://arxiv.org/abs/2608.01536) | 提出Celty——一个协同设计的稀疏格式、GPU内核和SIMT微架构，通过游程压缩CSC格式与流水线RLC解码器高效处理双稀疏工作负载，实现单用户解码场景下高效的大语言模型SpMSpV推理。 |
| [^526] | [On the Identifiability of Masked Prediction: Mode Blindness and Mask Schedules](https://arxiv.org/abs/2608.01383) | 本文发现掩码预测的可识别性完全由掩码调度决定，大上下文主导的调度对全局模式权重具有不可消除的盲性，并引入ε-可识别性模量来量化这一现象。 |
| [^527] | [Pruned BPE: Post-training Visibility Pruning and Token Reallocation for Byte Pair Encoding](https://arxiv.org/abs/2608.00837) | 提出Pruned BPE方法，在BPE训练后将低暴露度的合并词元剪枝为仅内部使用的节点，并把释放的可见词表槽位重新分配给更高暴露度的候选词元，实现合并构建与模型可见词表选择的分离。 |
| [^528] | [A False Average: Chain-of-Thought Monitors Collapse Where They Are the Only Defense](https://arxiv.org/abs/2608.00583) | 仅重写智能体的思维链推理（保持动作完全不变）即可在一次无梯度攻击中将CoT监控器的捕获率从约95%暴跌至11%以下，揭示监控器的总体准确率是掩盖其在唯一防线场景下近乎全面失效的虚假平均值。 |
| [^529] | [Secure Aggregation for Privacy-Preserving Federated Learning on Clinical EEG Data](https://arxiv.org/abs/2607.28191) | 提出了一个以基于掩码的安全聚合为核心的临床脑电数据隐私保护联邦学习框架，结合图通信、门限秘密共享、掉线恢复、更新裁剪与可验证性机制，以抵御半诚实和恶意聚合方的信息泄露。 |
| [^530] | [Subtract, Transport, or Replay? Auditable Deletion from Language-Model Memory](https://arxiv.org/abs/2607.27539) | 本文提出并比较了三种从语言模型记忆中删除记录的方法，发现原生删除不可靠，而检查点重放能实现可审计的精确删除，并展示了在冻结模型上无需额外训练即可构建可删除记忆的可行性。 |
| [^531] | [Equilibrium Training of Energy-Based Models with Parallel Trajectory Tempering](https://arxiv.org/abs/2607.27077) | 本文提出基于并行轨迹回火（PTT）的能量模型训练算法，通过利用优化路径的连续性在学习全程保持平衡采样，实现了在高多峰、数据稀缺科学数据集上的稳定快速训练，且计算成本与持续对比散度相当，在受限玻尔兹曼机实验中始终优于现有EBM训练方法。 |
| [^532] | [Parameter-Free Dynamic Regret under Heavy-Tailed Noise](https://arxiv.org/abs/2607.27073) | 该论文提出了一种无需预知噪声矩参数、Lipschitz 界、梯度尺度及比较序列路径长度的在线凸优化算法，通过重启 AdaGrad 专家与自适应熵正则化主算法的结合，在重尾噪声下实现了具有噪声路径指数 $(p-1)/p$ 的最优通用动态遗憾界。 |
| [^533] | [Probing Speaker Identity Sensitivity in Audio Deepfake Detectors](https://arxiv.org/abs/2607.21820) | 论文提出身份敏感性分数（ISS）这一新型诊断指标，揭示音频深度伪造检测器的跨数据集性能下降部分源于检测器依赖说话人身份线索而非真正的合成痕迹。 |
| [^534] | [Information-Theoretically Secure Aggregation for Lightweight Federated Learning: Resilient to Dropouts and Adversaries](https://arxiv.org/abs/2607.20890) | 提出了一种专为基于符号的轻量级联邦学习设计的信息论安全聚合框架，通过单轮安全乘法计算多数表决，在诚实多数假设下实现端到端的信息论安全，并能有效抵御掉线与恶意对手攻击。 |
| [^535] | [On the Potential of Graph Neural Networks as Metamodels for Supply Chain Optimization: Dataset, Architectures, and Directions](https://arxiv.org/abs/2607.16769) | 本文形式化了供应链优化问题，利用SupplyNetPy仿真库创建了大型公开的供应链图训练数据集，并探索了可作为仿真代理的图神经网络架构及其精度与计算成本的权衡，为GNN在供应链结构优化中的应用奠定基础。 |
| [^536] | [Decision Making Needs Uncertainty Quantification [Lecture Notes]](https://arxiv.org/abs/2607.14407) | 本讲义从决策理论基本原理出发证明，最优决策所需的不确定性表示取决于智能体的风险偏好——风险中性智能体需要后验分布，风险厌恶智能体可依赖预测集与最坏情况决策规则，并进一步提出应对环境未知时认知不确定性的互补方法。 |
| [^537] | [AI-Augmented Adaptive Digital Twin Modeling for Brain Tumor Evolution Prediction and Treatment Scheduling](https://arxiv.org/abs/2607.13877) | 提出了一种AI增强的自适应数字孪生框架，融合可解释反应-扩散模型、3D残差学习、患者特异性更新与模型预测控制，实现了脑肿瘤演化的高精度长期预测以及约束性化疗放疗调度优化。 |
| [^538] | [Learning-enabled Acceleration of Scenario-based Model Predictive Control](https://arxiv.org/abs/2607.12775) | 本文提出一种学习加速的ADMM算法，将SBMPC问题重构为可分解的一致性形式，结合并行计算与Moreau包络学习实现跨场景和跨时间步的并行更新，在保持高求解精度的同时大幅提升场景式模型预测控制的求解效率。 |
| [^539] | [Physically Consistent Parameter Inference: Transparent Machine Learning Emulation in High Energy Physics and Cosmology](https://arxiv.org/abs/2607.12726) | 该论文提出了一种基于XGBoost梯度提升回归树的机器学习框架，可高效仿真高能物理和宇宙学全局拟合中复杂的非高斯似然函数，并借助SHAP值实现透明可解释的参数推断。 |
| [^540] | [Environment Parameter Gradient Theorem for Co-Design in Reinforcement Learning](https://arxiv.org/abs/2607.12590) | 本文提出环境参数梯度定理，通过引入包含两份环境参数副本的广义动作值函数实现解耦，为强化学习中策略与环境的协同设计（联合优化）提供了可处理的闭式梯度表达式。 |
| [^541] | [What a Deletion Certificate Covers, and Where It Expires: Auditable Removal from a Support-Vector Memory](https://arxiv.org/abs/2607.12204) | 该论文提出一种带显式权重的支持向量上下文记忆，利用测试时的单类支持向量边界将键划分为激活或保留状态，从而提供可验证的删除证书——保留键移除无需重新求解、激活键删除可通过减量求解达到与重新求解一致的状态——并揭示了这些证书在新词元加入后可能失效的边界。 |
| [^542] | [When Does Reward Teach State? A Hidden-Automaton Instrument and a Group-Language Warning Signal](https://arxiv.org/abs/2607.11953) | 该论文提出一个基于已知隐藏有限自动机的诊断框架，通过奖励归一化和状态探测两种测量手段分离强化学习智能体中奖励与状态学习的失败模式，并发现置换自动机的群结构特征可作为训练前预测状态学习失败的警告信号。 |
| [^543] | [Learning Subgroup Relations Using Siamese Graph Neural Networks](https://arxiv.org/abs/2607.11140) | 本文提出一种将孪生图神经网络编码的凯莱图结构表示与代数特征相结合的统一框架，用于预测有限群之间的子群关系。 |
| [^544] | [Dynamics of Gradient Descent with Large Step Size Near a Manifold of Flat Minima](https://arxiv.org/abs/2607.08380) | 本文将大步长梯度下降在平坦极小值附近的收敛理论从单标量输出的孤立平坦极小值情形，扩展到向量值输出情形以及平坦极小值流形邻域情形，其中后者对矩阵分解等应用至关重要。 |
| [^545] | [Any-Dimensional Learning by Sampling](https://arxiv.org/abs/2607.07680) | 本文提出一种统一框架，利用随机采样映射来比较不同大小的输入，从而解决变尺寸机器学习模型从训练时的小尺寸输入向更大尺寸输入的泛化问题，以及通过草图化大输入来高效获得相似模型输出的问题。 |
| [^546] | [Context-Masked Truncated Reasoning Audits for Answer-Key Dependence in LLM Tutors](https://arxiv.org/abs/2607.04572) | 本文提出上下文掩码截断推理审计方法，用于检测LLM导师是否直接依赖私有答案密钥生成讲解，实验表明掩码后各项指标几乎降至仅问题基线水平，证明该方法能有效区分私有上下文直接访问与讲解中携带的答案信息。 |
| [^547] | [Masked Generative-Contrastive Representation Learning for Cross-Dataset EEG-Based Emotion Recognition](https://arxiv.org/abs/2607.04139) | 提出了MGCRL——一种专为脑电情绪识别设计的新型自监督学习框架，通过融合生成式与对比式学习以及区域感知的时空编码器，实现了面向跨数据集泛化的细粒度与全局判别性表征。 |
| [^548] | [An Additive MLP-GNN Framework for Characterizing Chemical and Structural Contributions to Aqueous Solubility](https://arxiv.org/abs/2607.02212) | 提出一种加性深度学习框架，将理化描述符（MLP化学分支）与分子图拓扑（GNN结构分支）分离建模并在预测阶段加性融合，实现了化学性质与分子结构对水溶性贡献的直接分解与可解释分析，并借助大库预训练加小库微调提升预测性能。 |
| [^549] | [Probing Chemical Language Models: Effects of Pre-training and Fine-tuning](https://arxiv.org/abs/2607.02140) | 该研究通过系统探测78种分子子结构，揭示了预训练能提升化学语言模型的分子结构感知能力（尤以上层显著），且微调主要改变与任务相关的子结构表示，表明模型的表示变化遵循化学理论。 |
| [^550] | [Multistage Defer Trees for Hybrid Interpretability: If at First You Can't Succeed, Tree Again](https://arxiv.org/abs/2606.30995) | 提出多阶段延迟树，通过一列稀疏决策树对大多数样本进行可解释预测，仅将少数困难样本延迟给后续树或黑盒模型，从而在匹配复杂树集成准确率的同时实现混合可解释性。 |
| [^551] | [When Does Activation Steering Change What a Model Computes From?](https://arxiv.org/abs/2606.29522) | 该论文的核心贡献在于通过受控状态追踪任务区分激活导向的两种机制——真正编辑模型后续计算所依赖的内部状态还是仅使输出偏向期望结果——并发现行为等价不等于机制等价，均值激活导向并不能重现模型自然使用的内部配置。 |
| [^552] | [When Can Conformal Risk Control Certify LLM Outputs? Bounds, Impossibility, and Adaptation for Structured Generation](https://arxiv.org/abs/2606.29054) | 该论文刻画了保形风险控制认证LLM结构化输出的可行边界，证明了当基础风险超过目标时任何无分布方法的最小弃权率下界，给出了闭式可行性测试，并通过716种配置验证了认证相图与自适应方法。 |
| [^553] | [Bridging Ab Initio Symmetries and Global Nuclear Masses with Interpretable Neural Networks](https://arxiv.org/abs/2606.28287) | 该论文提出了新型可解释神经网络WINN，通过约束质量公式对SU(3)和SU(4)对称性算符呈线性关系，成功架起了从头算对称性与全局核质量之间的桥梁，实现了兼具物理可解释性和外推预测能力的核质量建模。 |
| [^554] | [Mean-Field PhiBE: Continuous-Time Mean-Field Reinforcement Learning from Discrete-Time Data](https://arxiv.org/abs/2606.26498) | 本文提出MF-PhiBE，通过将离散时间数据融入Wasserstein空间上的连续时间偏微分方程，解决了连续时间平均场控制中模型不可识别性的难题。 |
| [^555] | [Pre-Warm: Initializing Convolutional Filters from First-Batch Patch Dictionaries](https://arxiv.org/abs/2606.25256) | 提出Pre-Warm方法，基于规则地从单个小批次构建的图像块字典（选择重构效果更好的主成分或k-means）来初始化卷积网络第一层一半的滤波器，相比随机初始化能更有效地利用训练图像信息。 |
| [^556] | [Discovering Latent Groups for Robust Classification](https://arxiv.org/abs/2606.23609) | 本文提出神经分类树（NCT）框架，通过在树形架构中编码子群体结构，基于预测正确性将样本路由到“简单”或“困难”节点并迭代生成伪标签，无需子群体监督即可解耦冲突子群体，实现鲁棒分类。 |
| [^557] | [Interleaved Speech Language Models Latently Work In Text](https://arxiv.org/abs/2606.22473) | 交错式语音语言模型在中间层会隐式地将口语单词转录为对应的文本词元，且这一隐式转录现象与模型的口语事实知识检索能力呈正相关。 |
| [^558] | [Where Does the Signal Live? A Web Data Recipe for Medical Encoder Pretraining](https://arxiv.org/abs/2606.22079) | 该论文提出了一种面向医学编码器预训练的网络数据配方，通过医学术语密度过滤与LLM信号放大改写两种互补策略，在法语医学NLP任务上超越了广泛使用的教育质量过滤方法。 |
| [^559] | [VegSim: A Geospatial World Model for Scenario-Conditioned Vegetation Simulation](https://arxiv.org/abs/2606.21961) | VegSim是一个地理空间世界模型，可从稀疏卫星数据推断植被潜在状态，并通过将未来天气作为可控输入，实现观测天气下的概率预测和用户自定义气象情景下的条件化植被模拟。 |
| [^560] | [A-Evolve-Training: Autonomous Post-Training of a 30B Model](https://arxiv.org/abs/2606.20657) | 该论文提出了一个无需人工干预的自主后训练系统，能在数周内对300亿参数模型进行多轮训练，并自主发现并修正了优化策略偏差，使模型性能接近人类顶尖水平。 |
| [^561] | [Activation- and Influence-Aware Ranks (AIR): Function-Preserving SVD Compression for LLMs](https://arxiv.org/abs/2606.19993) | AIR是一种基于SVD的大语言模型压缩框架，通过将激活感知最优解与反向信号影响度量结合于单次闭式ALS扫描中，在参数保留率≤60%时实现超过18%的困惑度改善，校准数据需求减少约90%，并带来实际的计算量、内存和延迟收益。 |
| [^562] | [Cost-Optimal LLM Routing with Limited User Feedback under User Satisfaction Guarantees](https://arxiv.org/abs/2606.19376) | 提出SLARouter，一种能从生产环境中稀疏的单侧用户反馈中在线学习成本最优路由策略、同时提供严格SLA合规理论保证的LLM路由算法。 |
| [^563] | [Monotonic Kolmogorov-Arnold Networks: A Theoretical and Empirical Study of Monotonicity as an Inductive Bias](https://arxiv.org/abs/2606.17886) | 提出MKAN，一种通过B样条系数指数重参数化、正边权重和单调基础激活函数，为所有参数值保证硬单调性的柯尔莫哥洛夫-阿诺德网络，可直接使用标准无约束梯度下降训练，并附有表示成本定理这一核心理论贡献。 |
| [^564] | [Improving Federated Graph Recommendation with Semantic Guidance](https://arxiv.org/abs/2606.15277) | 该论文提出了一种语义-结构联邦图推荐框架，利用大语言模型的语义嵌入引导跨客户端对齐，解决了联邦学习中非独立同分布数据下图表示无法全局对齐的问题。 |
| [^565] | [Representation Costs in Data Science: Foundations and the Quasi-Banach Spaces of Deep Neural Networks](https://arxiv.org/abs/2606.14954) | 本文提出了一个统一框架，通过参数空间正则化器分析数据科学中的表示成本，揭示了参数化方法与其原生函数空间之间的联系，并将核方法、小波和神经网络等经典方法统一为特例。 |
| [^566] | [Understanding Cross-Modal Contributions in Continual Vision-Language Models: A Theoretical Perspective](https://arxiv.org/abs/2606.14883) | 本文提出了一个新的理论视角来理解持续视觉-语言模型中视觉-语言跨模态对不同环境的贡献，并在大型模型上实证验证了其有效性。 |
| [^567] | [Characterizing Privacy-Audit Alignment in Behavioral Audit of Machine Unlearning](https://arxiv.org/abs/2606.14518) | 本文提出几何迁移定理，首次量化了机器遗忘行为审计中审计准确度与保留集隐私泄露之间的内在权衡关系，为互不信任双方的机器遗忘审计提供了理论基础。 |
| [^568] | [OdysSim: Building Foundation Models for Human Behavior Simulation](https://arxiv.org/abs/2606.14199) | 该论文提出了OdysSim——迄今最大规模的行为基础模型系统性研究，通过SOUL五维能力分类法统一62个数据集和23个基准任务，并利用2140万次交互的语料库与结合中期训练、强化学习和专家蒸馏的训练方案，训练出在23项任务中8项排名第一的开源8B人类行为模拟模型。 |
| [^569] | [Masked Neural Detection for Run-Length-Limited Channel Coding in Molecular Communication](https://arxiv.org/abs/2606.12489) | 该论文提出了一种解码器感知的训练掩码方法，通过在训练SBRNN神经检测器时屏蔽RLIM解码器大概率会确定性覆盖的位置，将网络容量集中于承载信息的位置，使掩码RLIM₂-SBRNN在57个工作点中的40个点优于最佳未编码接收机。 |
| [^570] | [PCA-Enhanced Adaptive NVAR Framework for High-Resolution Sea Surface Temperature Forecasting in the East Sea](https://arxiv.org/abs/2606.12141) | 本研究将基于SVD的PCA降维与自适应非线性向量自回归（Adaptive NVAR）框架相结合，有效压缩高分辨率海表温度场数据，克服了传统方法的误差累积问题，实现了对日本海等海域长期、低计算成本且准确的海表温度预测。 |
| [^571] | [When to Align, When to Predict: A Phase Diagram for Multimodal Learning](https://arxiv.org/abs/2606.11190) | 本文通过带结构化跨模态噪声相关的尖峰信号加噪声模型，揭示了跨模态对齐与跨模态预测两种范式的互补失败模式，并构建相图为多模态学习中范式的选择提供了理论指导。 |
| [^572] | [What Matters in Orchestrating Robot Policies: A Systematic Study of Hierarchical VLA Agents](https://arxiv.org/abs/2606.10267) | 本文对分层视觉-语言-动作（Hi-VLA）机器人系统进行了系统性研究，将代表性智能体统一到选项式控制框架下并在多种任务上基准测试核心设计选择，从中提炼出规划器、控制器与切换机制的实用设计原则。 |
| [^573] | [Neural Field Tokenizations with Hierarchy and Spatial Locality Priors](https://arxiv.org/abs/2606.08204) | 本文提出LH-NeF框架，通过注入局部性与层次性先验，用前馈编码取代内存密集的逐样本元学习，实现了模态无关且可扩展的神经场标记化表示学习。 |
| [^574] | [How Small Can You Go? LoRA Fine-Tuning 270M-8B Models for Merchant Information Extraction in Financial Transactions](https://arxiv.org/abs/2606.08051) | 本研究通过系统评估多种小型模型的LoRA微调，发现Qwen~3.5~4B在保持与8B模型相当性能的同时，显著降低资源消耗，实现了高效商户信息提取。 |
| [^575] | [Amortized Neural Optimization for Pre-Layout Signal Integrity Design Space Exploration using Differentiable Surrogates](https://arxiv.org/abs/2606.07463) | 本文提出摊销神经优化（ANO）方法，利用完全可微分的神经网络代理模型提取梯度信息，离线训练全局优化策略，从而避免迭代黑盒推理并摊销计算成本，大幅加速版图前信号完整性设计空间探索。 |
| [^576] | [Federated Foundation Models over Vehicular Networks](https://arxiv.org/abs/2606.06786) | 本文首次提出了将多模态多任务联邦基础模型集成到车联网中的前瞻性愿景，系统阐述了其训练/微调原理、代表性应用场景、车联网环境中的关键部署挑战以及未来研究方向。 |
| [^577] | [HAARES Half-Split Residual Basis Routing for Deep Transformers](https://arxiv.org/abs/2606.06564) | HAARES通过在块级残差路由的基础上增加一个由前后半段残差更新差值构成的半分裂细节基，在不引入密集子层级路由开销的前提下，为深度Transformer（尤其是48层模型）的残差聚合带来了一致的性能提升。 |
| [^578] | [In-Context Multiple Instance Learning](https://arxiv.org/abs/2606.06458) | 该论文提出在合成数据上以Perceiver风格架构预训练上下文学习器，使模型仅需少量带标签实例包、通过一次前向传播且无需梯度更新即可解决新的多示例学习任务。 |
| [^579] | [Reactive Flux Matching: Mechanism Discovery and Adaptive Sampling of Rare Events](https://arxiv.org/abs/2606.06295) | 提出通量匹配框架，直接从反应轨迹数据中学习流速度与标量势两个互补对象，分别用于揭示主导反应路径和构建数据驱动的反应坐标，并利用其水平集实现稀有事件的自适应增强采样。 |
| [^580] | [When New Generators Arrive: Lifelong Machine-Generated Text Attribution via Ridge Feature Transfer](https://arxiv.org/abs/2606.05626) | 提出RidgeFT框架，通过在首次遇到每个生成器时存储紧凑的类级充分统计量，并以冻结编码器后无需样本重放的闭式解析更新方式，解决了终身机器生成文本溯源中适应新生成器与保留旧生成器能力的平衡难题。 |
| [^581] | [Rollout-Level Advantage-Prioritized Experience Replay for GRPO](https://arxiv.org/abs/2606.04560) | 该论文提出一种面向GRPO的rollout级优势优先经验回放方法，通过按年龄淘汰限制数据过时、以新鲜在策略数据锚定训练稳定性，并优先回收高优势样本，从而提升推理大语言模型强化学习后训练的样本效率。 |
| [^582] | [SymTRELLIS: Symmetry-Enforced Voxel Latents for 3D Generation](https://arxiv.org/abs/2606.04108) | SymTRELLIS提出了一种无需重新训练基础模型的方法，通过将空间变换近似为体素潜变量上的学习线性算子，并在每个ODE步骤中对所有对称等价变换的流速度取平均，从而在基于流的3D生成中强制执行任意有限点群对称性。 |
| [^583] | [Spike-Aware INT8 Execution for Spiking Language Models on Commodity CPUs](https://arxiv.org/abs/2606.03026) | 该论文针对脉冲语言模型提出了一种脉冲感知的INT8量化执行策略，在商用CPU上实现了相比FP32快2倍以上的推理速度，同时将权重存储减少约68%。 |
| [^584] | [LayerRoute: Input-Conditioned Adaptive Layer Skipping via LoRA Fine-Tuning for Agentic Language Models](https://arxiv.org/abs/2606.01838) | LayerRoute通过为Qwen2.5-0.5B-Instruct的每个transformer层配备轻量级二进制门控路由器和LoRA适配器，学习根据输入特征动态跳过不必要的层，使智能体语言模型在结构化工具调用和开放式推理这两类异构步骤间实现自适应计算分配。 |
| [^585] | [Subliminal Learning is a LoRA Artifact](https://arxiv.org/abs/2606.00831) | 本文揭示了隐性学习现象——即模型通过无害数据向其他模型传递行为特征——实际上是LoRA微调的伪影，其传递强度与LoRA秩呈倒U型关系，在全量微调下消失，且高度依赖于微调和评估时的上下文。 |
| [^586] | [From Rashomon Theory to PRAXIS: Efficient Decision Tree Rashomon Sets](https://arxiv.org/abs/2606.00202) | PRAXIS算法在运行时间和内存使用上实现了数量级的改进，能够高效近似计算稀疏决策树的罗生门集（近优模型集合），并且通常可以恢复几乎完整的罗生门集，从而使真实世界数据集的罗生门集建模变得可扩展。 |
| [^587] | [Learning to Construct Practical Agentic Systems](https://arxiv.org/abs/2606.00189) | 提出了一种基于“伪工具”实现模块化的实用智能体系统设计框架，并证明手工构建的固定工作流比动态规划的工作流成本更低、准确性更高。 |
| [^588] | [LongDS-Bench: On the Failure of Long-Horizon Agentic Data Analysis](https://arxiv.org/abs/2605.30434) | 提出LongDS基准，基于真实Kaggle笔记本构建68个长时程多轮数据分析任务，揭示当前最先进模型在维护和演变分析状态方面存在严重缺陷，最佳模型平均准确率仅48.45%，且长时程错误占失败案例的52%–69%。 |
| [^589] | [Linear Separability of Activation Representations after Supervised Fine-Tuning on Incorrect Responses: A Study of Synthetic Dishonesty in Large Language Models](https://arxiv.org/abs/2605.30381) | 该研究通过构建诚实与不诚实的LoRA微调模型变体，证明对大语言模型进行错误回答的监督微调会在其内部激活中留下结构化且可线性恢复的痕迹，线性探针在多数架构的前几层即可实现近乎完美的诚实/不诚实状态区分，并呈现跨数据集的迁移性。 |
| [^590] | [Gradient-Free Training of Spiking Neural Networks via Low-Rank Evolution Strategies](https://arxiv.org/abs/2605.30361) | 该论文提出EGGROLL方法，通过进化策略扰动的低秩分解实现脉冲神经网络的无梯度训练，将每代内存开销从O(mn)降至O(r(m+n))，在N-MNIST上达到79.21%准确率且训练时间减少2.23倍。 |
| [^591] | [The Little Book of Generative AI Foundations: An Intuitive Mathematical Primer](https://arxiv.org/abs/2605.29713) | 本书以推导为导向，系统串联了从PCA、变分自编码器到扩散模型、GAN和能量模型等主要生成式模型家族的数学基础，为读者构建了一条连贯的生成式建模学习路径。 |
| [^592] | [Better Later Than Sooner: Neuro-Symbolic Knowledge Graph Construction via Ontology-grounded Post-extraction Correction](https://arxiv.org/abs/2605.29168) | 该论文提出一种神经-符号知识图谱构建框架，在抽取完成后依据常识本体约束对大模型抽取的事实进行后验校正，从而解决抽取事实违反本体约束的一致性问题。 |
| [^593] | [Clarify, Abstain or Answer? Strategising in Conversation with Belief-Augmented Generation](https://arxiv.org/abs/2605.25831) | 提出信念增强生成（BAG）方法，通过提示词让大语言模型基于自身采样所得的信念状态进行推理，自主决定并执行澄清、拒答或回答的对话策略，从而在歧义问答任务中同时提升回答准确率和策略决策对信念状态的忠实度。 |
| [^594] | [WMAttack: Automated Attack Search for Adversarial Evaluation of World-Model Agents](https://arxiv.org/abs/2605.23220) | WMAttack提出了一个自动化攻击搜索框架，通过自校正攻击搜索（SCAS）利用多种反馈信号在有限预算内高效且准确地评估世界模型智能体的对抗鲁棒性。 |
| [^595] | [More Context, Larger Models, or Moral Knowledge? A Systematic Study of Schwartz Value Detection in Political Texts](https://arxiv.org/abs/2605.22641) | 该研究在受控条件下系统分离了上下文长度、道德知识与模型规模三个因素，发现在政治文本的施瓦茨价值观检测任务中，更多上下文并非总是有益——完整文档输入仅使监督式编码器提升2.5-3.8个宏F1点，而对零样本大语言模型帮助并不一致。 |
| [^596] | [EmoTrack: Clinical-Semantic Modeling for Text-Based Depression Severity Estimation](https://arxiv.org/abs/2605.22286) | 本文提出EmoTrack框架，通过临床-语义建模将临床特征分数与话语级表示相融合，从心理咨询文本中预测PHQ-8抑郁严重程度总分，解决了临床评估与对话证据之间关联不足的问题。 |
| [^597] | [PUID: A Personalized Deconfounding Framework for Recommender Systems under Hidden Confounding](https://arxiv.org/abs/2605.21066) | 论文提出PUID个性化去混杂框架，以个性化的敏感性界限取代全局统一界限来处理推荐系统中的隐藏混杂因素，克服了传统方法忽略用户-物品交互异质性的缺陷。 |
| [^598] | [Expressivity of Contradiction Graphs](https://arxiv.org/abs/2605.20434) | 本文证明矛盾图序列能够精确确定概念类的VC维（可区分有限与无限VC维，回答了Alon等人2024年提出的问题），并能进一步确定任意固定长度的可实现数据集，从而刻画由这些数据集决定的学习理论性质。 |
| [^599] | [Atomistic Modeling of Chemical Disorder in Materials: Bridging Conventional Methods and AI-Assisted Approaches](https://arxiv.org/abs/2605.19124) | 本综述系统梳理了材料中化学无序的原子尺度建模方法，核心在于弥合实验中平均化无序描述与模拟及AI工作流所需的完全指定构型之间的表示鸿沟。 |
| [^600] | [Geometric Dictionary Learning of Dynamical Systems with Optimal Transport](https://arxiv.org/abs/2605.18276) | 提出DOODL框架，通过最优传输在谱算子空间中学习动力学系统字典，揭示相关动力学系统的共享低维流形结构，并实现从短且部分观测数据中快速、可解释的算子估计 |
| [^601] | [Concise and Logically Consistent Conformal Sets for Neuro-Symbolic Concept-Based Models](https://arxiv.org/abs/2605.18202) | 提出事后框架COCOCO，将保形预测联合应用于神经符号概念模型的概念与标签预测，同时满足一致性、覆盖性和简洁性三个条件，解决了模型预测过度自信、难以判断决策可信度的问题。 |
| [^602] | [Generating Pretraining Tokens from Organic Data for Data-Bound Scaling](https://arxiv.org/abs/2605.17849) | SynPro 框架通过对有机数据进行改写和重新格式化并利用强化学习优化生成器，帮助 LLM 在数据受限时更充分地利用有限的有机数据，且不引入外部信息。 |
| [^603] | [Vocabulary-size-independent Convergence of Discrete Diffusion Models: adjoint equations induce the right space](https://arxiv.org/abs/2605.17232) | 本文提出基于伴随方程的统一理论框架，首次给出了完全与词表规模无关、适用于任意积分概率度量以及掩码和均匀先验的离散扩散模型收敛保证。 |
| [^604] | [EmoMind: Decoding Affective Captions from Human Brain fMRI](https://arxiv.org/abs/2605.16739) | EmoMind是首个从fMRI信号端到端解码情感图像描述的系统，通过先检索中性场景描述、再利用34维连续情感向量进行重写，并借助无分类器引导实现语义保真与情感表达的灵活平衡。 |
| [^605] | [Do LLMs Hold Their Values? MANTA: A Multi-Turn Adversarial Benchmark for Animal Welfare Reasoning](https://arxiv.org/abs/2605.16301) | 该论文提出了 MANTA 基准，通过 1,088 段五轮多轮对抗对话评估大语言模型在动物福利推理中的价值观稳定性与道德敏感性，弥补了现有单轮基准无法检测持续对抗压力下对齐退化及道德敏感性缺失的不足。 |
| [^606] | [Backdoor Channels Hidden in Latent Space: Extending Cryptographic Undetectability to Modern Neural Networks](https://arxiv.org/abs/2605.13214) | 本文通过将后门通道识别为网络潜空间中学习到的潜在方向，把密码学意义上的不可检测后门攻击扩展到现代端到端训练的最先进神经网络，并证明其检测问题可归结为一个在实践中难以处理的假设检验。 |
| [^607] | [CRePE: Curved Ray Expectation Positional Encoding for Unified-Camera-Controlled Video Generation](https://arxiv.org/abs/2605.12938) | 提出了曲线光线期望位置编码（CRePE），将图像token表示为沿统一相机模型射线的深度感知分布，并沿弯曲投影路径积分旋转位置相位，从而在冻结的视频扩散模型上实现了包括广角和鱼眼镜头在内的统一相机控制视频生成。 |
| [^608] | [The Transformer as a Polar State Estimator](https://arxiv.org/abs/2605.11007) | 本文证明Transformer的注意力机制、残差连接和归一化组件可从极坐标下的几何状态估计问题中自然推导得出，并提出保留了这些几何修正项的Polar Transformer。 |
| [^609] | [Unmasking On-Policy Distillation: Where It Helps, Where It Hurts, and Why](https://arxiv.org/abs/2605.10889) | 该论文提出了一个无需训练的高分辨率诊断框架，通过推导理想的逐节点梯度并开发可扩展的定向采样算法，揭示了在线策略蒸馏在训练推理模型时何时有益、何时有害。 |
| [^610] | [RubricRefine: Improving Tool-Use Agent Reliability with Training-Free Pre-Execution Refinement](https://arxiv.org/abs/2605.09730) | RubricRefine提出了一种无需训练的执行前契约检查方法，通过生成任务特定的评分标准、对候选代码进行契约检查评分并迭代修复失败，在零执行尝试的情况下将工具使用智能体在M3ToolEval上的平均性能提升至0.86。 |
| [^611] | [Discriminative Span as a Predictor of Synthetic Data Utility via Classifier Reconstruction](https://arxiv.org/abs/2605.09697) | 该论文提出一种几何驱动的度量方法，通过在预训练基础模型嵌入空间中衡量线性分类器权重向量与样本差分向量所张成子空间之间的相对投影误差，无需训练模型即可预测合成正样本数据对下游二分类任务的效用。 |
| [^612] | [Empowering VLMs for Few-Shot Multimodal Time Series Classification via Tailored Agentic Reasoning](https://arxiv.org/abs/2605.09395) | 提出了首个面向少样本多模态时间序列分类的视觉语言模型智能体推理框架，通过生成器、反思器、修改器三角色协同以及自演化知识库的测试时更新策略，有效缓解少样本偏差与分布偏移，显著提升分类性能。 |
| [^613] | [PMCTS: Principled Parallelized Inference Time Scaling with Particle Monte Carlo Tree Search](https://arxiv.org/abs/2605.08982) | 本文提出了PMCTS，一种专为GPU批处理并行化设计的原则性并行蒙特卡洛树搜索算法，它在保持策略改进保证的同时随并行计算规模良好扩展，并在国际象棋、围棋等MCTS和强化学习评估任务中始终优于或媲美流行的启发式基线方法。 |
| [^614] | [FragileFlow: Spectral Control of Correct-but-Fragile Predictions for Foundation Model Robustness](https://arxiv.org/abs/2605.08896) | 提出即插即用的FragileFlow正则化器，通过校准边界缓冲区识别“正确但脆弱”的预测并将类别间误差流组织为脆弱风险矩阵，并首次给出该误差流对象的PAC-Bayes上界，从而以谱控制实现基础模型的确定性最差类别鲁棒性。 |
| [^615] | [Can Revealed Preferences Clarify LLM Alignment and Steering?](https://arxiv.org/abs/2605.08556) | 本文提出一个基于离散选择模型的实证流程，从大语言模型的观测决策中恢复其隐含的成本函数，从而严格评估模型是否表现出一致的目标导向行为、其语言表述的目标是否与实际决策策略相符、以及提示能否可靠地引导模型实现用户指定的成本函数。 |
| [^616] | [Learning Polyhedral Conformal Sets for Robust Optimization](https://arxiv.org/abs/2605.08506) | 该论文提出一个决策感知的保形框架，通过数据驱动方式学习适配鲁棒优化目标的多面体不确定性集，在保持有限样本统计有效性保证的同时降低决策的保守性。 |
| [^617] | [Block-Wise Differentiable Sinkhorn Attention: Tail-Refinement Gradients with a Gap-Aware Dustbin Bridge](https://arxiv.org/abs/2605.08123) | 该论文提出块级可微Sinkhorn注意力方法，通过尾部精细化替代与间隙感知垃圾箱桥接，在TPU上实现了长上下文平衡熵最优传输注意力的精确且高效的反向传播，将块级计算代价降至O((T+R)LW)且仅需O(L)额外HBM内存。 |
| [^618] | [Convergent Stochastic Training of Multi-Headed Attention and Understanding LoRA](https://arxiv.org/abs/2605.07959) | 本文通过统一框架严格证明了多头注意力和LoRA在随机训练方法下的可训练性，其关键在于证明Poincaré常数与数据维度或头的维度无关，从而保证模拟SGD的随机微分方程能收敛并最小化相应的损失。 |
| [^619] | [Stability of the Monge Map in Semi-Dual Optimal Transport](https://arxiv.org/abs/2605.05569) | 本文证明最优传输的半对偶形式具有退化鞍点结构且等价于约束优化问题，推导出了 Monge 映射收敛的充分必要条件，并解释了数值算法更新传输映射为何比更新势函数需要更多迭代。 |
| [^620] | [Quantum Hierarchical Reinforcement Learning via Variational Quantum Circuits](https://arxiv.org/abs/2605.03434) | 该论文首次将变分量子线路成功应用于基于option-critic架构的分层强化学习，以更少参数实现优于经典方法的性能，并揭示了量子线路在选项价值估计中的架构瓶颈及关键设计原则。 |
| [^621] | [A Universal Reproducing Kernel Hilbert Space from Polynomial Alignment and IMQ Distance](https://arxiv.org/abs/2605.03262) | 本文提出Yat核，通过将IMQ距离与带偏置的平方内积（多项式对齐）相乘，构建了一个在每个紧域上普适且严格扩展IMQ函数空间的再生核希尔伯特空间，并证明其高维激活协方差在高斯输入下保持满秩。 |
| [^622] | [Multi-User Dueling Bandits: A Fair Approach using Nash Social Welfare](https://arxiv.org/abs/2605.01961) | 该论文针对多用户偏好差异导致的不公平问题，提出在对决老虎机框架中以用户各自的孔多塞赢家为参考点，并采用纳什社会福利目标最大化用户效用乘积，从而保证对少数群体用户的公平性。 |
| [^623] | [$S^3$-R1: Learning to Retrieve and Answer Step-by-Step with Synthetic Data](https://arxiv.org/abs/2605.01248) | 该论文提出S^3-R1框架，通过程序化合成并筛选中等难度多跳问题的数据流水线，结合评估中间搜索步骤的更密集奖励信号，解决了强化学习后训练中稀疏奖励与训练数据单一难度的问题，从而提升模型利用搜索工具收集证据的能力。 |
| [^624] | [Near-Floor Geometry Is Generic: Leverage Dispersion in Trained Overcomplete Codes](https://arxiv.org/abs/2605.01192) | 该论文从理论上精确证明与秩-迹地板的距离本质上是特征杠杆的离散度（杠杆均衡的i.i.d.码普遍即可达到地板），并在40个实际发布的训练解码器矩阵上发现它们均偏离地板且更早出现保证的仿射失效。 |
| [^625] | [Conformalized Quantum DeepONet Ensembles: Towards Scalable Operator Learning with Distribution-Free Guarantees](https://arxiv.org/abs/2605.00330) | 本文提出共形化量子DeepONet集成框架，利用量子正交神经网络将隐层计算成本从二次方降至近线性，并结合分割共形校准实现具有有限样本、无分布保证的不确定性量化。 |
| [^626] | [State-Dependent Lyapunov Analysis of Rank-1 Matrix Factorization](https://arxiv.org/abs/2604.26993) | 该论文提出了一种基于参数化二次证书嵌套下水平集的状态依赖李雅普诺夫分析框架，证明了秩一矩阵分解中梯度下降即使在所有相关驻点均不稳定时，也能在显式区域内对几乎所有初始化收敛到全局极小值点或平衡的周期2轨道。 |
| [^627] | [Marginal-Contribution Policy Gradients under Filtered Feedback for Multi-Agent LLMs](https://arxiv.org/abs/2604.22785) | 本文提出了多智能体大语言模型强化学习训练中唯一满足一致性要求的边际贡献功劳分配理论，通过从过滤反馈中识别梯度正确的边际贡献信号并最优分配反事实评估预算，在GRPO中无需额外生成成本即提升了路由GSM8K准确率。 |
| [^628] | [Learning Without Adversarial Training: A Physics-Informed Neural Network for Secure Power System State Estimation under False Data Injection Attacks](https://arxiv.org/abs/2604.22784) | 本文提出一种能联合学习同方差不确定性参数从而自适应平衡监督损失与物理损失的物理信息神经网络，在无需对抗训练的情况下实现了对虚假数据注入攻击具有鲁棒防御能力的电力系统状态估计。 |
| [^629] | [Conformalized Super Learner](https://arxiv.org/abs/2604.22391) | 本文提出将保形预测与超级学习器自然结合的框架，通过加权组合各学习器的保形分数来构建具有有限样本覆盖保证的预测区间。 |
| [^630] | [Partner-aware Peptide-Protein Interaction Prediction and Target-conditioned Peptide Generation](https://arxiv.org/abs/2604.18467) | 该论文提出了一个集成框架，将伙伴感知的肽-蛋白质相互作用预测与结合位点定位模型ConGA-PepPI和靶标条件化肽生成模型TC-PepGen相结合，实现了相互作用预测、结合位点解读和靶标条件化肽生成的统一，用于加速早期肽类药物筛选。 |
| [^631] | [AdaExplore: Failure-Driven Adaptation and Diversity-Preserving Search for Efficient Kernel Generation](https://arxiv.org/abs/2604.16625) | 提出 AdaExplore 智能体框架，通过失败驱动的适应与保持多样性的搜索两个互补阶段，利用积累的执行反馈实现 Triton 内核代码生成的自我改进，无需额外微调或外部知识即可同时提升代码正确性与优化性能。 |
| [^632] | [PAC-CF: Calibrating Irreversible Frontier Pruning in LLM-Guided Search](https://arxiv.org/abs/2604.14345) | 提出PAC-CF方法，将LLM引导搜索中的前沿剪枝形式化为具有PAC保证的保形决策问题，通过Native-Trace路径校准得到保形边际来过滤候选，避免因不可约评估偏差误删所有可通向有效解的分支，从而在多种领域和预算下提升搜索效用。 |
| [^633] | [Token Encoding for Semantic Recovery](https://arxiv.org/abs/2604.12931) | 该论文提出TokCode框架，利用轻量级适配器将大语言模型改造为令牌编码器，并通过信道质量感知的蒸馏方法CADET进行训练，通过在语义域中重构冗余实现抗令牌丢失的鲁棒语义恢复。 |
| [^634] | [Attention-Weighted Value Projection for KV-Cache Compression](https://arxiv.org/abs/2604.11501) | 提出了一种面向KV缓存压缩的注意力加权值投影方法，通过以重建注意力输出（而非仅重建值本身）为目标，利用 V^⊤α^⊤αV 的主特征向量得到具有精确最优解的秩约减方案，其误差恰好等于被丢弃特征值之和，并将该目标扩展至校准数据集与分组查询注意力场景。 |
| [^635] | [Domain-Aware Hybrid Quantum Learning via Correlation-Guided Circuit Design for Crime Pattern Analytics](https://arxiv.org/abs/2604.07389) | 本研究提出一种基于相关性引导电路设计的领域感知混合量子-经典学习框架用于犯罪模式分析，其中QAOA方法以更少的可训练参数实现了高达84.6%的准确率，在内存受限的边缘部署场景中展现出实际优势。 |
| [^636] | [Learn to Rank: Visual Attribution by Learning Importance Ranking](https://arxiv.org/abs/2604.05819) | 该论文提出了一种将视觉归因中的重要性排序框架化为排列学习的训练方案，直接优化删除和插入指标，从而克服了现有基于传播、扰动和学习的方法在效率、因果性和解释质量之间的三重权衡。 |
| [^637] | [Towards Near-Real-Time Telemetry-Aware Routing with Neural Routing Algorithms](https://arxiv.org/abs/2604.02927) | 本文将遥测感知路由建模为延迟感知的闭环控制问题，提出了一个显式建模通信与推理延迟的训练评估框架，并在此基础上提出可扩展的图神经路由算法LOGGIA，实现近实时的遥测感知路由。 |
| [^638] | [Apriel-Reasoner: RL Post-Training for General-Purpose and Efficient Reasoning](https://arxiv.org/abs/2604.02007) | 提出 Apriel-Reasoner，一个基于 150 亿参数开源基座模型、采用可复现的多领域强化学习后训练方案与自适应领域采样技术训练而成的通用且高效推理模型。 |
| [^639] | [The Geometry of Polynomial Group Convolutional Neural Networks](https://arxiv.org/abs/2603.29566) | 本文基于分次群代数为多项式群卷积神经网络提出了新的数学框架，给出了 Hadamard 和 Kronecker 两种自然参数化方法，并证明了其神经流形维度仅取决于网络层数和群的大小。 |
| [^640] | [Subspace Optimization for Backpropagation-Free Continual Test-Time Adaptation](https://arxiv.org/abs/2603.28678) | PACE利用带Fastfood投影的协方差矩阵自适应进化策略，在低维子空间中优化归一化层的高维仿射参数，实现了无需反向传播的高效持续测试时自适应，并通过自适应停止准则和域专用向量库将运行时间减少50%以上。 |
| [^641] | [Hyperelastic constitutive model discovery with differentiable finite elements and structure-preserving neural networks](https://arxiv.org/abs/2603.26517) | 该论文提出一种将非线性有限元平衡问题直接嵌入学习循环的可微分有限元框架，结合结构保持神经网络，仅从边界位移和整体反力等部分观测数据中即可发现物理上合理的超弹性本构模型。 |
| [^642] | [EcoFair: Energy-Efficient Inference Routing for Edge AI under Data Degradation](https://arxiv.org/abs/2603.26483) | EcoFair提出了一种垂直分区推理框架，让边缘设备保留原始图像和表格数据、仅传输轻量的模态特定表示进行服务器端融合，从而在数据退化场景下平衡皮肤病分类的能耗与诊断可靠性。 |
| [^643] | [The Rules-and-Facts Model for Simultaneous Generalization and Memorization in Neural Networks](https://arxiv.org/abs/2603.25579) | 本文提出规则-事实模型，将师生泛化框架与Gardner容量分析相结合，从理论上刻画了神经网络何时能够同时泛化底层规则并记忆例外事实，并量化了过参数化在实现这一双重能力中的作用。 |
| [^644] | [Hyperspectral Trajectory Image for Multi-Month Trajectory Anomaly Detection](https://arxiv.org/abs/2603.25255) | 提出TITAnD方法，通过将轨迹表示为高光谱轨迹图像（HTI）把轨迹异常检测转化为视觉问题，首次实现了多月份密集GPS轨迹的异常检测，并统一了密集与稀疏两种场景以实现知识迁移。 |
| [^645] | [Amortized Inference for Correlated Discrete Choice Models via Equivariant Neural Networks](https://arxiv.org/abs/2603.24705) | 本文提出一种基于等变神经网络架构的摊销推断方法，可为具有相关误差的一般离散选择模型快速逼近选择概率与似然函数，突破了传统Logit模型对随机效用分布的严格假设限制。 |
| [^646] | [DSPA: Dynamic SAE Steering for Data-Efficient Preference Alignment](https://arxiv.org/abs/2603.21461) | DSPA是一种推理时偏好对齐方法，通过提示条件化的稀疏自编码器引导在不更新模型权重的情况下实现偏好对齐，在保持性能的同时将对齐阶段计算量最多降低4.47倍。 |
| [^647] | [Generating from Discrete Distributions Using Diffusions: Insights from Random Constraint Satisfaction Problems](https://arxiv.org/abs/2603.20589) | 本研究以随机k-SAT问题作为生成模型基准，发现连续扩散模型优于掩码离散扩散模型，学习到的扩散模型可达到理论理想精度，且智能变量排序能显著提升生成精度。 |
| [^648] | [FILT3R: Latent State Adaptive Kalman Filter for Streaming 3D Reconstruction](https://arxiv.org/abs/2603.18493) | FILT3R提出了一种无需训练的潜在滤波层，将循环状态更新建模为token空间中的随机状态估计，通过在线估计过程噪声并计算卡尔曼风格增益，自适应平衡记忆保持与新观测，从而解决流式三维重建中状态更新不稳定的问题。 |
| [^649] | [Adapting Technical-Service LLM Agents with Latent Logic Augmentation, Robust Noise Reduction, and Hybrid Reward Modeling](https://arxiv.org/abs/2603.18074) | 本文提出一种耦合潜在逻辑增强、鲁棒降噪与混合奖励建模的后训练框架，通过规划感知轨迹建模增广微调数据、构建双重过滤多重真值、并结合重排序器与LLM评审的混合奖励进行策略训练，使适配后的Qwen3-4B在真实云技术服务任务上取得最优表现且显著降低奖励成本。 |
| [^650] | [Real-Time Driver Safety Scoring Through Inverse Crash Probability Modeling](https://arxiv.org/abs/2603.14841) | 本文提出SafeDriver-IQ框架，通过融合全国事故统计数据与自动驾驶车辆自然驾驶数据，将二元事故分类器转化为连续的0-100分驾驶员安全评分，从而实现对驾驶员的实时安全反馈。 |
| [^651] | [Mixing Makes Markovian Contexts Cheap for Linear Bandits](https://arxiv.org/abs/2603.12530) | 该论文将“上下文廉价”归约扩展到马尔可夫上下文过程的线性老虎机问题，证明在一致几何遍历性（混合性）条件下，可通过构造平稳替代动作集并使用标准线性老虎机算法求解时间相关的上下文问题。 |
| [^652] | [Spatiotemporal Heterogeneity of AI-Driven Traffic Flow Patterns and Land Use Interaction: A GeoAI-Based Analysis of Multimodal Urban Mobility](https://arxiv.org/abs/2603.05581) | 本研究提出一种依次整合MGWR、随机森林与时空图卷积网络的GeoAI混合框架，有效刻画了机动车、公共交通和主动出行三种模式下城市交通流的时空异质性及其与土地利用的交互作用。 |
| [^653] | [FuseDiff: Symmetry-Preserving Joint Diffusion for Dual-Target Structure-Based Drug Design](https://arxiv.org/abs/2603.05567) | FuseDiff是一个端到端的对称性保持扩散模型，能够以两个靶点口袋为条件，联合生成配体分子及其对应的两个口袋特异性结合构象，从而实现双靶点结构药物设计。 |
| [^654] | [TimeWarp: Evaluating Web Agents by Revisiting the Past](https://arxiv.org/abs/2603.04949) | 提出了模拟网页UI演变的TimeWarp基准测试，揭示现有网页智能体对界面变化的脆弱性，并通过TimeTraj标注方法与BC变体训练显著提升了智能体跨网页版本的泛化能力。 |
| [^655] | [Temporal Consistency Improves Generalization in Contextual Offline Meta Reinforcement Learning](https://arxiv.org/abs/2603.02935) | 该论文提出在潜在空间中强制多步预测的时序一致性约束，使任务表示既能捕捉任务特定的动力学又能避免表示坍塌，从而提升情境式离线元强化学习对新任务的泛化能力。 |
| [^656] | [KDFlow: A User-Friendly and Efficient Knowledge Distillation Framework for Large Language Models](https://arxiv.org/abs/2603.01875) | KDFlow提出了一种解耦架构的大语言模型知识蒸馏框架，结合SGLang进行Teacher推理与PyTorch FSDP2进行Student优化，并通过Ray对象存储传输Teacher隐藏状态、在Student端重算Teacher logits来实现高效的全词表蒸馏。 |
| [^657] | [CARE: Confounder-Aware Aggregation for Reliable LLM Evaluation](https://arxiv.org/abs/2603.00039) | 提出CARE框架，通过将LLM评审分数显式建模为潜在真实质量信号与共享混淆因子的叠加，在无需真实标签的情况下实现质量与混淆因子的分离，从而获得更可靠的大语言模型评估聚合结果。 |
| [^658] | [Learning Quantum Data Distribution via Chaotic Quantum Diffusion Model](https://arxiv.org/abs/2602.22061) | 该论文提出了一种通过混沌哈密顿量时间演化生成投影系综的混沌量子扩散模型，仅需全局时不变控制即可在多种模拟量子平台上大幅降低实现开销，同时保持与现有量子去噪扩散概率模型相当的精度。 |
| [^659] | [US-JEPA: A Joint Embedding Predictive Architecture for Ultrasound](https://arxiv.org/abs/2602.19322) | US-JEPA提出了一种采用静态教师非对称潜在训练（SALT）目标的自监督框架，利用冻结的领域特定教师模型提供稳定的潜在目标，解决了超声影像低信噪比环境下的表征学习难题，同时避免了传统JEPA中在线教师模型超参数敏感和高计算开销的缺陷。 |
| [^660] | [Diagnosing LLM Reranker Behavior Under Fixed Evidence Pools](https://arxiv.org/abs/2602.18613) | 本文提出一种以Multi-News聚类为固定证据池的受控诊断方法，发现LLM重排序器在冗余模式和词汇覆盖方面与BM25、MMR基线行为显著不同，且不同LLM之间也表现出各异的冗余策略。 |
| [^661] | [MGD: Moment Guided Diffusion for Maximum Entropy Generation](https://arxiv.org/abs/2602.17211) | 提出矩引导扩散（MGD），基于随机插值框架通过求解随机微分方程在有限时间内将矩引导至预设值，从而高效采样最大熵分布并避免高维慢混合问题。 |
| [^662] | [LGQ: Learnable Geometric Quantization for Image Tokenization](https://arxiv.org/abs/2602.16086) | 提出可学习几何量化LGQ，通过保留可学习码本并结合温度退火与多样性和尖锐度两个正则化项，在无需EMA、重置启发式或重参数化的情况下有效防止码本坍缩，实现图像标记化的稳定训练。 |
| [^663] | [Constant-Stepsize Stochastic Approximation: Finite-Time Convergence, Gaussian Approximation, and Tail Bounds](https://arxiv.org/abs/2602.13960) | 该论文为独立同分布和马尔可夫噪声下的恒定步长随机逼近建立了显式的极限前理论刻画，证明了平稳分布的存在唯一性及几何Wasserstein收敛速率，并在 $\sqrt{\alpha}$ 波动尺度上推导出高阶定量高斯近似与尾界，从而量化了经典渐近结果无法捕捉的有限时间、有限步长误差。 |
| [^664] | [Text Has Curvature](https://arxiv.org/abs/2602.13418) | 本文提出Texture——一种文本原生、词级的离散曲率信号，通过实证和理论证明自然语言文本确实具有内在曲率，并给出了该曲率的定义与检测方法。 |
| [^665] | [Learning functional components of PDEs from data using neural networks](https://arxiv.org/abs/2602.13174) | 本文提出将神经网络嵌入偏微分方程框架的方法，能够从稳态观测数据中高精度地学习PDE中的未知函数分量（如相互作用核和外部势），并系统分析了影响重建精度的关键因素。 |
| [^666] | [Jointly Optimizing Debiased CTR and Uplift for Coupons Marketing: A Unified Causal Framework](https://arxiv.org/abs/2602.12972) | 本文提出统一多值处理网络UniMVT，通过解耦混淆因素与处理敏感表示，并利用全空间反事实推断，联合优化优惠券营销场景下去偏CTR预测与增益估计。 |
| [^667] | [Diffusion-Inspired Reconfiguration of Transformers for Uncertainty Calibration](https://arxiv.org/abs/2602.08920) | 提出一种受扩散启发的Transformer重构方法，将特征变换块建模为概率映射并重组为扩散过程，在保持预测性能的同时实现预训练模型中不确定性的原则性传播与校准。 |
| [^668] | [A Thermodynamic Theory of Learning Part II: History-Dependent Reachability and Continual Learning](https://arxiv.org/abs/2602.07950) | 该论文将神经可塑性建模为依赖历史动力学下的最小能量可达性问题，提出任务条件化的适应代价理论，证明了学习代价可在不发生秩损失的情况下持续增长，从而为持续学习中学习与记忆保持之间的权衡提供了理论刻画。 |
| [^669] | [Parity, Sensitivity, and Transformers](https://arxiv.org/abs/2602.05896) | 本论文证明transformer计算奇偶性任务至少需要两层，从而否定地解决了“单层transformer能否计算奇偶性”这一开放问题，并提出了一种去除不切实际假设的新构造方法。 |
| [^670] | [Feedback Control for Multi-Objective Graph Self-Supervision](https://arxiv.org/abs/2602.05036) | 该论文提出ControlG框架，将多目标图自监督学习中的目标协调重新表述为反馈控制的时间分配问题，通过控制论方法解决传统混合方式导致的目标干扰与训练不稳定问题。 |
| [^671] | [Improved Dimension Dependence for Bandit Convex Optimization with Gradient Variations](https://arxiv.org/abs/2602.04761) | 本文通过对非连续梯度变化的精细化分析，改进了带两点反馈的赌博机凸优化中凸函数和强凸函数梯度变化遗憾界的维度依赖性，并首次实现了单点赌博机反馈下的梯度变化界。 |
| [^672] | [Boosting LLM Reasoning via Human-Inspired Reward Shaping](https://arxiv.org/abs/2602.04265) | 本文提出受人类学习过程启发的T2T动态奖励框架，通过“由厚到薄”的双阶段机制，让模型在未掌握的问题上鼓励广泛探索、在已掌握的问题上鼓励推理凝练与知识抽象，从而克服现有RLVR奖励设计将探索与巩固混为一谈的缺陷，提升大语言模型的推理能力。 |
| [^673] | [Echo State Networks for Time Series Forecasting: Hyperparameter Sweep and Benchmarking](https://arxiv.org/abs/2602.03912) | 该研究通过系统的超参数扫描和样本外基准测试表明，经过合理配置的简单回声状态网络（ESN）可以在M4竞赛数据集的月度和季度时间序列预测中与ARIMA、ETS、Theta等主流方法相竞争。 |
| [^674] | [Step-Wise Refusal Dynamics in Autoregressive and Diffusion Language Models](https://arxiv.org/abs/2602.02600) | 该论文提出逐步拒答内部动力学（SRI）信号，揭示了扩散语言模型的重掩码采样机制能够从有害的中间生成内容中恢复，从而显著提升越狱鲁棒性，且这一优势在固定模型权重、仅切换采样方式时依然成立。 |
| [^675] | [Rotation-free Online Handwritten Character Recognition Using Linear Recurrent Units](https://arxiv.org/abs/2602.01533) | 本论文提出利用滑动窗口路径签名（SW-PS）提取局部结构特征，并结合轻量级线性循环单元（LRU）作为分类器，实现了对手写体字符在±180°任意旋转下仍保持高准确率的鲁棒识别。 |
| [^676] | [Multi-Fidelity Physics-Informed Neural Networks with Bayesian Uncertainty Quantification and Adaptive Residual Learning for Efficient Solution of Parametric Partial Differential Equations](https://arxiv.org/abs/2602.01176) | 本文提出MF-BPINN框架，通过融合物理信息神经网络、贝叶斯不确定性量化和自适应残差学习，利用低保真模拟与稀疏高保真数据的分层架构，实现了参数化偏微分方程的高效求解。 |
| [^677] | [Can One-Shot Test-Time Data Augmentation Help with Generalization?](https://arxiv.org/abs/2602.00114) | 提出了名为1S-DAug的单次测试时数据增强方法，仅从单张原始图像通过受控噪声注入的几何扰动和图像条件去噪生成增强样本，无需模型训练或参数访问即可实现高达20%的相对准确率提升。 |
| [^678] | [Observing Health Outcomes Using Remote Sensing Imagery and Geo-Context Guided Visual Transformer](https://arxiv.org/abs/2602.00110) | 本文提出一种地理上下文引导的视觉Transformer，通过将异构地理空间变量嵌入为与图像块对齐的表示来增强遥感影像分析，从而实现基于遥感影像的健康结果观测。 |
| [^679] | [Continual Policy Consolidation for Lifelong Robot Learning](https://arxiv.org/abs/2601.22475) | 提出持续策略巩固（CPC）教师-学生框架，通过持续策略蒸馏、优先经验回放和可扩展专家，将强化学习驱动的技能习得与有监督的策略巩固相分离，使机器人能够终身学习新技能的同时保留已习得的行为。 |
| [^680] | [Low-Rank Plus Sparse Matrix Transfer Learning under Growing Representations and Ambient Dimensions](https://arxiv.org/abs/2601.21873) | 该论文提出了一个在环境维度与潜在表示同时增长情形下的低秩加稀疏矩阵迁移学习框架，通过锚定交替投影估计器在保留已迁移子空间的同时仅估计低维创新与稀疏修改，实现了严格更优的误差率。 |
| [^681] | [More Bang for the Buck: Improving the Inference of Large Language Models at a Fixed Budget using Reset and Discard (ReD)](https://arxiv.org/abs/2601.21522) | 本文提出重置与丢弃（ReD）查询方法，克服了pass@k幂律行为导致的边际收益递减问题，在固定预算下显著提升大语言模型回答独特问题的数量（coverage@cost），并能定量预测节省的尝试次数。 |
| [^682] | [Hypersolid: Emergent Vision Representations via Short-Range Repulsion](https://arxiv.org/abs/2601.21255) | 提出Hypersolid自监督学习方法，通过将视图对齐与短程排斥（局部碰撞避免）相结合，诱导出低各向异性、语义对齐的紧凑潜在几何结构，在无监督聚类和细粒度区分任务上表现尤为出色。 |
| [^683] | [Solving the Offline and Online Min-Max Problem of Non-smooth Submodular-Concave Functions: A Zeroth-Order Approach](https://arxiv.org/abs/2601.21243) | 本文提出一种结合Lovász扩展次梯度与高斯平滑的零阶方法，解决了非光滑次模-凹函数的离线与在线极小极大问题，证明了离线情形收敛到ε-鞍点，并在在线情形达到O(√(N(1+P̄_N)))的对偶间隙。 |
| [^684] | [PatchFormer: A Patch-Based Time Series Foundation Model with Hierarchical Masked Reconstruction and Cross-Domain Transfer Learning for Zero-Shot Multi-Horizon Forecasting](https://arxiv.org/abs/2601.20845) | PatchFormer是一种基于补丁的时间序列基础模型，通过层次化掩码重建自监督预训练、跨时间尺度可学习聚合、跨域知识蒸馏以及轻量级适配器迁移，在24个基准数据集上实现了最先进的零样本多步预测并显著降低均方误差。 |
| [^685] | [From Human Labels to Literature: Semi-Supervised Learning of NMR Chemical Shifts at Scale](https://arxiv.org/abs/2601.18524) | 本文提出一种半监督学习框架，利用数百万个无需原子级标注的文献谱图大规模学习核磁共振化学位移预测，显著提升了预测准确性、鲁棒性和泛化能力。 |
| [^686] | [SFO: Learning PDE Operators via Spectral Filtering](https://arxiv.org/abs/2601.17090) | 提出SFO神经算子，利用基于希尔伯特矩阵特征模态的通用谱基参数化积分核，仅学习快速衰减特征值的谱系数即可高效捕捉偏微分方程解映射中的长程非局部相互作用，在六个基准测试中达到最先进精度。 |
| [^687] | [Strategic Doctrine Language Models (sdLM): A Learning-System Framework for Doctrinal Consistency and Geopolitical Forecasting](https://arxiv.org/abs/2601.14862) | 提出了战略条令语言模型框架，通过融合多文档注意力、时间编码和条令一致性层，在多文档战略推理中实现条令一致性与校准不确定性，其战略质量和长程地缘政治预测能力超越通用大语言模型基线，并可与人类专家媲美。 |
| [^688] | [SWE-Tester: Training Open-Source LLMs for Issue Reproduction in Real-World Repositories](https://arxiv.org/abs/2601.13713) | 提出 SWE-Tester 流程，通过从2600个开源仓库构建的4.1万实例高质量数据集微调开源大语言模型，使其能够自动生成问题复现测试，成功率最高提升10%。 |
| [^689] | [A New Strategy for Artificial Intelligence: Training Foundation Models Directly on Human Brain Data](https://arxiv.org/abs/2601.12053) | 本文提出了一种人工智能新策略——直接在人类大脑数据（神经影像数据）上训练基础模型，以获取文本等人类生成数据无法体现的认知信息，从而克服基础模型的现有局限性。 |
| [^690] | [GIFT: Reconciling Post-Training Objectives via Variational Finite-Temperature Gibbs Initialization](https://arxiv.org/abs/2601.09233) | 该论文提出GIFT方法，通过token级变分代理目标引入有限温度Gibbs初始化，使SFT目标与后续RL阶段结构兼容，避免分布坍缩并保留探索空间，从而提升大型推理模型的后训练效果。 |
| [^691] | [Minimum distance classification for nonlinear dynamical systems](https://arxiv.org/abs/2601.04058) | 本文提出了Dynafit方法，通过逼近Koopman算子并结合核技巧学习训练数据与底层动力学之间的距离度量，实现对非线性动力系统生成轨迹的最小距离分类。 |
| [^692] | [Electricity Price Forecasting: Bridging Linear Models, Neural Networks and Online Learning](https://arxiv.org/abs/2601.02856) | 本文提出了一种结合线性与非线性前馈神经网络结构的多元混合神经网络架构，并整合了一种新颖的部分在线学习策略（将热启动与各训练阶段的不同超参数配置相结合），从而在计算成本与预测精度之间取得平衡，提升日前电力价格预测的准确性。 |
| [^693] | [Temporal Kolmogorov-Arnold Networks (T-KAN) for High-Frequency Limit Order Book Forecasting: Efficiency, Interpretability, and Alpha Decay](https://arxiv.org/abs/2601.02310) | 本文提出时序Kolmogorov-Arnold网络（T-KAN），用可学习的B样条激活函数替代LSTM的固定线性权重来预测限价订单簿，在k=100时间范围内F1分数相对提升19.1%，实现132.48%的回报并显著缓解阿尔法衰减问题，同时具备良好的可解释性。 |
| [^694] | [Differentiable Causal Discovery for Singular Linear Models under Confounding](https://arxiv.org/abs/2601.01368) | 该论文基于奇异学习理论提出了一个可微分因果发现框架，验证了WBIC在线性高斯ADMG奇异模型上的一致性，通过ADVI实现其可扩展近似，并利用Gumbel-Softmax松弛实现了在有向与双向邻接结构上的可微分搜索，从而在存在未观测混杂因素时实现高效的线性因果发现。 |
| [^695] | [Alpha-R1: Alpha Screening with LLM Reasoning via Reinforcement Learning](https://arxiv.org/abs/2512.23515) | Alpha-R1提出了一个强化学习对齐的LLM框架，通过“语义门控”机制将候选因子的经济逻辑与动态构建的市场状态描述进行匹配，实现了情境感知的Alpha因子筛选，突破了传统方法仅依赖数值时间序列、忽视经济语义的局限。 |
| [^696] | [Decoder-Side Semantic Conditioning for Low-Bitrate Neural Speech Compression](https://arxiv.org/abs/2512.21653) | SemDAC通过在残差向量量化中引入由HuBERT蒸馏得到的语义条件化机制，使语音编解码器在仅0.95 kbps的比特率下即可达到甚至超越2.5 kbps DAC基线的客观与主观性能。 |
| [^697] | [Residual-augmented flow matching operators for probabilistic partial differential equations](https://arxiv.org/abs/2512.12749) | 提出了一种残差增强的概率算子学习框架，在无限维函数空间中利用流匹配生成建模并借助廉价低保真解算子作为归纳偏置，通过学习刻画低保真与高保真解之间差异的概率残差算子，在数据稀缺条件下实现对偏微分方程系统的高效不确定性量化代理建模。 |
| [^698] | [Modal Logic Neural Networks](https://arxiv.org/abs/2512.03491) | 提出了一种端到端可微分的模态逻辑神经网络，通过可学习的可达关系和赋值函数实现可微分克里普克语义，统一支持认知、信念、义务和时间等模态解读，并能处理不一致性。 |
| [^699] | [Open-Set Domain Adaptation Under Background Distribution Shift: Challenges and A Provably Efficient Solution](https://arxiv.org/abs/2512.01152) | 本文提出CoLOR方法，首次在背景分布发生偏移的挑战性条件下为开集识别提供可证明的理论保证，证明其在新类别可分离的良性假设下优于代表性基线方法并具备可扩展性。 |
| [^700] | [Tracing Mathematical Proficiency Through Problem-Solving Processes](https://arxiv.org/abs/2512.00311) | 该论文提出利用学生问题解决过程的知识追踪方法KT-PSP及配套数据集KT-PSP-25，并构建StatusKT框架，通过教师-学生-教师三阶段大语言模型流水线将学生数学能力作为中间表示进行提取，突破了传统知识追踪仅依赖答题正确性、缺乏可解释性的局限。 |
| [^701] | [Unexplored flaws in multiple-choice VQA make benchmarking unreliable](https://arxiv.org/abs/2511.22341) | 该论文揭示了MC-VQA基准测试中存在严重的提示格式敏感性问题——即使消除了选项顺序的影响，仅仅改变选项ID、分隔符等语义无关的格式元素就会导致模型性能排名频繁逆转，这表明当前MC-VQA作为多模态大语言模型评估基准的可靠性存在根本性缺陷。 |
| [^702] | [Iterative GRPO: Batch-Online Policy Iteration for Multi-Turn RL via Single-Turn RLHF](https://arxiv.org/abs/2511.21638) | 提出Iterative GRPO方法，利用批在线部署模式收集的交互数据，将经典的近似策略迭代算法与单轮RLHF相结合，从而无需在训练循环中接入真实或模拟用户即可实现多轮强化学习。 |
| [^703] | [Resolving sources of uncertainty in AI weather forecasting](https://arxiv.org/abs/2511.14218) | 提出Pangu-Bayes概率预报框架，将大气状态不确定性与学习模型不确定性作为独立随机变量分开建模，实现了误差来源的可解析归因，并在热带气旋预报中显著降低路径、气压和风速误差。 |
| [^704] | [Patent Representation Learning via Self-supervision](https://arxiv.org/abs/2511.10657) | 提出一种利用专利内部结构（如权利要求、背景等）作为自监督信号的混合dropout-部分正样本策略，有效提升了专利表示学习在多种检索任务中的迁移性。 |
| [^705] | [Generalized infinite dimensional Alpha-Procrustes based geometries](https://arxiv.org/abs/2511.09801) | 本文通过引入基于单位化Hilbert-Schmidt算子和扩展Mahalanobis范数的新形式体系，将Alpha-Procrustes黎曼度量族扩展至可容纳广义Bures-Wasserstein和Log-Hilbert-Schmidt距离的无限维推广，并通过可学习正则化参数增强高维比较中的几何稳定性。 |
| [^706] | [PAN: A World Model for General, Actionable, and Long-Horizon World Simulation](https://arxiv.org/abs/2511.09057) | 该论文提出了PAN——一个通用、可行动交互且具备长时程一致性的世界模型，旨在突破现有世界模型在开放域行动驱动预测和抽象推理规划方面的局限，推动AI实现超越大语言模型的物理与具身智能。 |
| [^707] | [Leveraging Discrete Function Decomposability for Scientific Design](https://arxiv.org/abs/2511.03032) | 该论文提出利用科学中属性预测函数所具有的离散可分解结构，将离散设计空间上的分布优化问题分解为更易处理的子问题，从而更高效地设计满足用户指定属性的离散对象（如蛋白质、电路和材料）。 |
| [^708] | [Accelerated Frank-Wolfe Algorithms: Complementarity Conditions and Sparsity](https://arxiv.org/abs/2511.02821) | 本文提出了基于互补条件刻画解稀疏性的新型加速Frank-Wolfe算法，在多面体和矩阵域约束下实现了复杂度仅依赖解稀疏度（而非环境维度）的最优一阶oracle复杂度。 |
| [^709] | [CDFlow: Building Invertible Layers with Circulant and Diagonal Matrices](https://arxiv.org/abs/2510.25323) | 本文提出基于循环矩阵与对角矩阵乘积的新型可逆线性层CDFlow，利用快速傅里叶变换在保持表达能力的同时，将参数复杂度降至O(mn)，并将矩阵求逆和对数行列式计算的时间复杂度分别降至O(mn log n)和O(mn)。 |
| [^710] | [Sustained Performance and Energy Accounting for Nonlinear Forecasting Across Classical and Simulated Quantum Models](https://arxiv.org/abs/2510.25183) | 该论文通过在经典模型与模拟量子模型（包括量子储备池计算）上开展大规模非线性时间序列预测基准测试，从误差、延迟、内存、能耗和碳排放等多个维度全面核算了AI系统的持续性能与能效。 |
| [^711] | [Deep Active Inference with Diffusion Policy and Multiple Timescale World Model for Real-World Exploration and Navigation](https://arxiv.org/abs/2510.23258) | 该论文提出了一个将扩散策略与多时间尺度循环状态空间世界模型相集成的深度主动推断框架，通过最小化期望自由能实现了真实世界机器人探索与目标导向导航的统一。 |
| [^712] | [Synthesizability Prediction of Crystalline Structures with Structure-Aware Feature Learning and Uncertainty Quantification](https://arxiv.org/abs/2510.19251) | 提出SyntheFormer框架，通过结构感知特征学习与不确定性量化直接从晶体结构预测可合成性，在时间严格分离的前瞻性评估中，对仅占1.02%的极稀少可合成正类样本实现了高召回率预测，并通过新发现材料的前瞻性验证证实了其有效性。 |
| [^713] | [RePro: Training Language Models to Faithfully Recycle the Web for Pretraining](https://arxiv.org/abs/2510.10681) | RePro通过强化学习训练仅1B参数的小型语言模型，忠实地改写回收网络数据用于预训练，在22个下游任务上相比仅使用原始数据实现了3.7%-14.5%的相对准确率提升。 |
| [^714] | [DAGLFNet: Deep Feature Attention Guided Global and Local Feature Fusion for Pseudo-Image Point Cloud Segmentation](https://arxiv.org/abs/2510.10471) | 提出DAGLFNet框架，通过深度特征注意力引导的全局与局部特征融合，解决伪图像表示与原始三维信息不一致导致的特征判别性差的问题，从而提升点云语义分割性能。 |
| [^715] | [CogniDir: Combating Cognitive Malicious Comments via Adaptive Distributional Learning for Robust Fake News Detection](https://arxiv.org/abs/2510.09712) | CogniDir 提出了一种自适应分布学习框架，通过将鲁棒假新闻检测重构为动态数据混合优化问题，并利用信息论评分指导自适应采样，有效对抗大语言模型生成的认知型恶意评论攻击。 |
| [^716] | [BaNEL: Exploration Posteriors for Generative Modeling Using Only Negative Rewards](https://arxiv.org/abs/2510.09596) | BaNEL算法仅利用失败尝试（负奖励）对生成模型进行后训练，并最小化奖励评估次数，从而解决了基础模型奖励信号接近零且奖励评估成本高昂这一严峻挑战。 |
| [^717] | [SQS: Bayesian DNN Compression through Sparse Quantized Sub-distributions](https://arxiv.org/abs/2510.08999) | 该论文提出SQS框架，利用贝叶斯变分学习统一权重剪枝与低比特量化——通过尖峰-厚板先验诱导稀疏性、用高斯混合模型建模量化权重，并给出高效近似方法与理论一致性保证，从而在保持模型性能的同时实现比现有方法更高的压缩率。 |
| [^718] | [Best-of-Both Worlds for linear contextual bandits with paid observations](https://arxiv.org/abs/2510.07424) | 提出了 Agg-SPB 和 CE-SPB 两种“两全其美”的正则化领导者跟随算法，用于带付费观测的线性上下文老虎机，在对抗环境中达到 $T^{2/3}$ 遗憾率，在随机环境中达到对数或多对数级遗憾。 |
| [^719] | [Learning Multi-Index Models with Hyper-Kernel Ridge Regression](https://arxiv.org/abs/2510.02532) | 本文提出融合神经网络与核方法的超核岭回归（HKRR）方法，并通过样本复杂度理论证明其能够自适应地学习多指标模型，克服维度灾难。 |
| [^720] | [Fusing Sequence Motifs and Pan-Genomic Features: Antimicrobial Resistance Prediction using an Explainable Lightweight 1D CNN-XGBoost Ensemble](https://arxiv.org/abs/2509.23552) | 提出了AMR-EnsembleNet集成框架，将轻量级1D CNN学习的序列基序与XGBoost的泛基因组特征相融合，在中等规模数据集上实现了可解释且高效的抗菌素耐药性预测。 |
| [^721] | [AudioFuse: Unified Spectral-Temporal Learning via a Hybrid ViT-1D CNN Architecture for Robust Phonocardiogram Classification](https://arxiv.org/abs/2509.23454) | AudioFuse提出了一种混合架构，用宽而浅的ViT处理频谱图、浅层1D CNN处理原始波形，同时学习心音图的频域和时域互补特征，在从零训练的条件下达到最先进的分类性能（ROC-AUC 0.8608），并对域偏移展现出优异的鲁棒性。 |
| [^722] | [S$^3$F-Net: A Multi-Modal Approach to Medical Image Classification via Spatial-Spectral Summarizer Fusion Network](https://arxiv.org/abs/2509.23442) | 提出了双分支网络S³F-Net，将深度空间CNN与浅层频谱编码器SpectraNet相融合，其中SpectralFilter层基于卷积定理通过逐元素乘法直接在完整傅里叶频谱上应用可学习滤波器以获得全局感受野，从而同时利用空间与频域表示提升医学图像分类性能。 |
| [^723] | [SingLEM: Single-Channel Large EEG Model](https://arxiv.org/abs/2509.17920) | 提出了SingLEM，一个自监督脑电图基础模型，通过混合卷积-Transformer编码器独立处理每个通道，摆脱了预定义多通道输入和电极布局假设的限制，并在71个公开数据集（约9,200名受试者、357,000小时单通道数据）上训练，显著提升了模型的通用适应性。 |
| [^724] | [Interpretable Network-assisted Random Forest+](https://arxiv.org/abs/2509.15611) | 本文提出了一种基于随机森林泛化（RF+）的可解释网络辅助模型族，在实现极具竞争力的预测精度的同时，通过直接从模型参数和结构推导出的内在可解释性度量，弥合了网络辅助预测中预测性能与可解释性之间的差距。 |
| [^725] | [WhisTLE: Deeply Supervised, Text-Only Domain Adaptation for Small Pretrained Speech Recognition Transformers](https://arxiv.org/abs/2509.10452) | WhisTLE提出了一种深度监督的纯文本领域自适应方法，通过训练VAE从文本建模编码器输出并微调解码器，使预训练语音识别模型无需收集语音数据即可适配新领域，且推理时不产生额外开销。 |
| [^726] | [The Price of Sparsity: Sufficient Conditions for Sparse Recovery using Sparse and Sparsified Measurements](https://arxiv.org/abs/2509.01809) | 本文确定了在高信噪比区间使用稀疏高斯测量矩阵进行稀疏二元信号支撑恢复所需最小样本量的充分条件，给出了阶为 $s\log(p/s)/\log(ds/p)$ 的信息论阈值，明确量化了测量稀疏性的代价，并指出存在一个测量稀疏导致对数级样本复杂度损失却换来近线性计算增益的区间。 |
| [^727] | [ReST-RL: Reinforcing LLM Reasoning through Unified Self-Training and Value-Guided Search](https://arxiv.org/abs/2508.19576) | 该论文提出统一策略-价值框架ReST-RL，通过ReST-GRPO重塑轨迹分布以放大奖励信号提升策略训练效率，并结合基于MCTS自收集数据训练价值模型的VM-MCTS解码搜索方法，协同增强大语言模型的推理能力。 |
| [^728] | [Learning Acrobatic Flight from Preferences](https://arxiv.org/abs/2508.18817) | 本文提出REC框架，通过集成分布奖励模型显式建模每个时间步的奖励不确定性，解决了手工设计奖励函数与人类判断不一致的问题，使智能体能够仅凭人类偏好学会执行高难度的特技飞行动作。 |
| [^729] | [ComplicitSplat: Downstream Models are Vulnerable to Blackbox Attacks by 3D Gaussian Splat Camouflages](https://arxiv.org/abs/2508.11854) | ComplicitSplat是首个利用3D高斯泼溅中视角特定伪装对下游目标检测器发起黑盒攻击的方法，无需了解模型架构或权重即可成功攻击多种检测器，揭示了自动驾驶等安全关键应用面临的新型风险。 |
| [^730] | [Comparison of D-Wave Quantum Annealing and Gibbs Monte Carlo for Sampling from a Probability Distribution of a Restricted Boltzmann Machine](https://arxiv.org/abs/2508.10228) | 本研究将基于局部谷的采样质量评估方法应用于最新一代D-Wave量子退火机，发现在RBM采样中D-Wave覆盖的局部谷数量略多于Gibbs采样，但两者找到的局部谷差异明显，且在高概率状态上重叠度高、互补性不足。 |
| [^731] | [Differentially Private Model-X Knockoffs via Johnson-Lindenstrauss Transform](https://arxiv.org/abs/2508.04800) | 该论文提出了一种基于Johnson-Lindenstrauss变换的新颖差分隐私化框架，通过降维技术对knockoff矩阵进行隐私化处理，在差分隐私约束下实现了严格的错误发现率（FDR）控制，并从理论上刻画了隐私参数和样本量等因素对变量选择功效的渐近影响。 |
| [^732] | [Retrieval-augmented Decoding for Improving Truthfulness in Open-ended Generation](https://arxiv.org/abs/2508.02184) | 提出检索增强解码方法RAD，仅用10个标注示例构建参考空间，在推理时通过检索动态塑造logits分布，无需模型重训练即可显著提升开放式生成中语言模型输出的真实性。 |
| [^733] | [Formal Bayesian Transfer Learning via the Total Risk Prior](https://arxiv.org/abs/2507.23768) | 该论文提出了一种新的贝叶斯迁移学习方法，其核心创新在于使用以源参数为条件的风险最小化器（而非传统的经验损失最小化器）来构建源数据集与目标数据集所有参数的单一联合先验分布，从而实现完整的贝叶斯不确定性量化以及通过Gibbs采样对源数据集进行选择和模型平均。 |
| [^734] | [Introducing HALC: A general pipeline for the systematic and reliable construction of prompts for automated coding with LLMs in the computational social sciences](https://arxiv.org/abs/2507.21831) | 本文提出了HALC通用流程，可为计算社会科学中的LLM自动编码任务系统、可靠地构建提示词，并通过超过三百万次LLM请求在不同数据集上验证了其适用性。 |
| [^735] | [Learning Latent Graph Geometry via Fixed-Point Schr\"odinger-Type Activation: A Theoretical Study](https://arxiv.org/abs/2507.20088) | 本文提出将神经网络隐藏层建模为学习潜在图上耗散薛定谔型动力学的稳态，证明了多层稳态网络等价于超图上的精确全局稳态问题，并建立了带惩罚全局松弛的收敛性理论及基于伴随系统的反向微分框架。 |
| [^736] | [Squeeze10-LLM: Squeezing LLMs' Weights by 10 Times via a Staged Mixed-Precision Quantization Method](https://arxiv.org/abs/2507.18073) | 提出了Squeeze10-LLM分阶段混合精度训练后量化框架，通过将80%权重量化为1比特、20%量化为4比特实现平均每权重1.6比特（即16比特权重压缩10倍），并借助二值化后激活鲁棒性（PBAR）和全信息激活监督（FIAS）两项创新在极端压缩下保持模型性能。 |
| [^737] | [Dynamical stability for dense patterns in attractor neural networks](https://arxiv.org/abs/2507.10383) | 该研究利用随机矩阵理论，为存在噪声且具有分级神经活动的吸引子神经网络发展了离散不动点的局部稳定性理论，突破了以往研究条件的限制，为理解生物记忆中密集存储模式的动力学稳定性提供了新途径。 |
| [^738] | [Mind the Gap: Navigating Inference with Optimal Transport Maps](https://arxiv.org/abs/2507.08867) | 该论文提出了一种基于最优传输的模型校准方法，首次应用于高维模拟以解决粒子物理中机器学习面临的模拟与实验数据不一致问题，并通过喷注标记任务验证了其有效性。 |
| [^739] | [Adaptive Nonlinear Vector Autoregression: Robust Forecasting for Noisy Chaotic Time Series](https://arxiv.org/abs/2507.08738) | 提出了一种数据自适应的非线性向量自回归模型，通过将浅层MLP与线性读出层进行梯度联合训练来学习数据驱动的非线性特征，从而在含噪混沌时间序列预测中实现更强的稳健性和可扩展性。 |
| [^740] | [Q-Guided Stein Variational Model Predictive Control via RL-informed Policy Prior](https://arxiv.org/abs/2507.06625) | 该论文提出Q-SVMPC方法，将基于学习的模型预测控制转化为轨迹级后验推断，通过强化学习软Q值引导和策略先验下的Stein变分梯度下降保留多样化轨迹解，从而避免传统方法的模式坍缩问题。 |
| [^741] | [Deep Learning to Automate Parameter Extraction and Model Fitting of Two-Dimensional Transistors](https://arxiv.org/abs/2507.05134) | 提出一种结合数据增强与预训练的深度学习方法，仅需约500个器件的物理仿真数据即可自动提取二维晶体管的物理参数并完成严格的TCAD模型拟合，数据需求比以往方法减少40倍以上。 |
| [^742] | [Detecting and explaining clinical-omics inconsistencies to improve patient cohort stratification: an application to Parkinson's disease](https://arxiv.org/abs/2507.03656) | 提出MLASDO工具，通过无监督方法检测临床诊断与分子组学数据之间的不一致性（包括离群值和分子特征与临床类别相反的异常样本），从而在下游分析前识别误诊、隐藏亚群或前驱疾病状态，改进帕金森病等疾病的患者队列分层质量。 |
| [^743] | [On the Effectiveness of the z-Transform Method in Quadratic Optimization](https://arxiv.org/abs/2507.03404) | 该论文展示了 z 变换方法作为推导优化算法新渐近收敛结果的有效工具，从无限维希尔伯特空间中的梯度下降出发，将分析依次扩展至 Nesterov 加速、平均技术和随机梯度下降，并用谱维数刻画收敛行为。 |
| [^744] | [A robust and adaptive MPC formulation for Gaussian process models](https://arxiv.org/abs/2507.02098) | 本文提出了一种基于收缩度量推导高斯过程鲁棒预测的鲁棒自适应模型预测控制框架，以高概率保证递归可行性、鲁棒约束满足和收敛性，并通过在线学习实现显著性能提升。 |
| [^745] | [MILAAP: Mobile Link Allocation via Attention-based Prediction](https://arxiv.org/abs/2506.19947) | 提出MiLAAP框架，利用基于自注意力的深度学习预测信道占用情况，使信道跳变系统无需节点间状态共享即可适应干扰变化和节点移动，从而降低通信开销并提升网络吞吐量效率。 |
| [^746] | [A Review of the Long Horizon Forecasting Problem in Time Series Analysis](https://arxiv.org/abs/2506.12809) | 本综述系统梳理了35年来时间序列长程预测（LHF）问题的发展，总结了深度学习如何结合趋势/季节性分解、傅里叶与小波变换以及卷积、注意力、SSM等架构创新来提升长程预测性能，并通过ETTm2数据集上的消融实验加以验证。 |
| [^747] | [From Proxies to Fields: Spatiotemporal Reconstruction of Global Radiation from Sparse Sensor Sequences](https://arxiv.org/abs/2506.12045) | 本文提出时间辐射算子网络（TRON），一种时空神经算子架构，能够仅从稀疏、非均匀的代理测量序列中实时重建连续的全球标量场，无需密集网格输入和未来观测数据。 |
| [^748] | [Deep Learning Approach to Bearing and Induction Motor Fault Diagnosis via Data Fusion](https://arxiv.org/abs/2506.11032) | 本文提出利用CNN评估加速度计与麦克风数据，并通过LSTM循环神经网络实现多传感器数据融合，从而实现轴承与感应电机的高效故障诊断。 |
| [^749] | [NoLoCo: No-all-reduce Low Communication Training Method for Large Models](https://arxiv.org/abs/2506.10911) | 提出了无需全归约和集合通信的优化方法 NoLoCo，通过 Nesterov 动量的新颖变体隐式同步模型权重，从而大幅降低大规模语言模型训练的通信成本。 |
| [^750] | [Two-dimensional Taxonomy for N-ary Knowledge Representation Learning Methods](https://arxiv.org/abs/2506.05626) | 本综述针对知识图谱将复杂n元关系简化为三元组而丢失高阶关系细节的问题，提出了N元知识表示学习方法的二维分类体系，系统梳理了知识超图（KHG）和超关系知识图谱（HKG）等融合知识图谱与超图优势的新兴方法。 |
| [^751] | [Multi-Task Learning with Covariate-Overlap Regularization](https://arxiv.org/abs/2505.24281) | 提出了一种协变量重叠正则化多任务学习方法（COVER），通过共享神经表示与低维任务特定系数，并利用基于逐任务二阶矩矩阵的协变量重叠惩罚，有效解决了多任务学习中协变量分布与响应关系异质性的问题。 |
| [^752] | [Kronecker Factorization Improves Efficiency and Interpretability of Sparse Autoencoders](https://arxiv.org/abs/2505.22255) | KronSAE 通过 Kronecker 分解将稀疏自编码器的潜在空间结构化为低维成分的组合，并利用可微分的类 AND 交互建模特征共激活，在兼容标准 SAE 目标的同时提升了效率与可解释性。 |
| [^753] | [Efficient and Microphone-Fault-Tolerant 3D Sound Source Localization](https://arxiv.org/abs/2505.20961) | 本文提出了一种结合稀疏交叉注意力、预训练和自适应信号相干性度量的3D声源定位框架，在减少麦克风数量和计算成本的同时实现精确定位，并能容错未知位置的麦克风。 |
| [^754] | [ControlTac: Scaling Tactile Data with Physically Controlled Tactile Image Generation](https://arxiv.org/abs/2505.20498) | ControlTac提出了一种两阶段可控触觉图像生成框架，以单个参考触觉图像、接触力和接触位姿作为物理先验条件来生成逼真的触觉图像，从而实现跨传感器的触觉数据低成本扩展，并有效提升真实世界下游任务的表现。 |
| [^755] | [Clustering and Pruning in Causal Data Fusion](https://arxiv.org/abs/2505.15215) | 本文提出剪枝与聚类作为因果数据融合的预处理操作，通过移除不必要变量和合并变量来简化复杂因果模型，在保留关键特征的同时降低计算开销，使因果效应的可识别性分析更加高效。 |
| [^756] | [A Theoretical Analysis of Provable Compositional Generalization in Neural Networks: A Necessary and Sufficient Condition](https://arxiv.org/abs/2505.02627) | 本文首次建立了神经网络可证明组合泛化的充分必要条件——即“结构对齐”与“无歧义最小化表示”两个原则，并在Lean 4中完成了机器验证，该条件即使在少样本和单样本情形下依然成立。 |
| [^757] | [How to Backdoor Image Knowledge Distillation](https://arxiv.org/abs/2504.21323) | 本文提出了一种针对图像知识蒸馏的新型后门攻击方法，通过向蒸馏数据集注入被干净教师模型预测为目标标签的触发图像，使学生在教师保持干净的情况下学到后门，且在10%中毒率下仍然有效。 |
| [^758] | [The Dynamics of Generalization in Deep Learning](https://arxiv.org/abs/2504.16450) | 该论文推导出一个刻画泛化差距演化的微分方程，将泛化差距表示为依赖于训练轨迹的“有效Gram矩阵”与初始化残差构成的二次型，为一般深度网络提供了统一的泛化理论框架。 |
| [^759] | [Observability conditions for neural state-space models with eigenvalues and their roots of unity](https://arxiv.org/abs/2504.15758) | 本文借助常微分方程与控制理论，为神经状态空间模型和Mamba架构提出了利用特征值和单位根来保证可观测性的高效计算策略。 |
| [^760] | [SAEs Can Improve Unlearning: Dynamic Sparse Autoencoder Guardrails for Precision Unlearning in LLMs](https://arxiv.org/abs/2504.08192) | 本文提出动态稀疏自编码器防护栏（DSG），通过有原则的特征选择和动态分类器实现大语言模型的精准遗忘，其表现显著优于现有基于梯度的遗忘方法。 |
| [^761] | [DDPM Score Matching and Distribution Learning](https://arxiv.org/abs/2504.05161) | 本文提出一个将分数估计归约为参数估计与密度估计这两个经典统计任务的框架，从而在DDPM分数匹配与经典统计学习理论之间建立了联系。 |
| [^762] | [Fewer yet critical: Reducing Redundant Token Dependencies for Transformer-based Time Series Forecasting](https://arxiv.org/abs/2503.06867) | 提出一种联合利用注意力熵约束与预测误差约束的标记依赖选择策略，使基于Transformer的时间序列预测模型能够识别更少但更关键的标记间依赖，从而避免冗余依赖的干扰并提升泛化能力。 |
| [^763] | [Approaching the Harm of Gradient Attacks While Only Flipping Labels](https://arxiv.org/abs/2503.00140) | 本文证明，即使攻击者只能翻转有限数量的训练标签而无法修改特征、注入样本或控制梯度，仍能对基于均值聚合的分布式学习造成与梯度攻击相当的破坏。 |
| [^764] | [TSMini: A Simple Yet Highly Effective Trajectory Similarity Learning Model](https://arxiv.org/abs/2502.00285) | 提出了TSMini模型，通过子视图建模机制和基于k近邻的损失函数，同时学习轨迹的绝对相似值和相对相似排序，实现了高精度的轨迹相似性逼近。 |
| [^765] | [A cautionary tale on the cost-effectiveness of collaborative AI in real-world medical applications](https://arxiv.org/abs/2412.06494) | 本文通过对7个医学数据集、3种机器学习任务和8种数据模态的大规模基准测试，系统比较了联邦学习与基于共识的学习方法在多中心医学数据分析中的准确性和成本效益，为协作式AI在真实医疗场景中的部署提供了警示性见解。 |
| [^766] | [DUA-D2C: Dynamic Uncertainty Aware Method for Overfitting Remediation in Deep Learning](https://arxiv.org/abs/2411.15876) | 本文提出DUA-D2C方法，通过基于子集模型在共享验证集上的表现动态加权其贡献，改进了D2C的聚合过程，从而更有效地缓解深度学习中的过拟合问题。 |
| [^767] | [Projected Neural Differential Equations for Learning Constrained Dynamics](https://arxiv.org/abs/2410.23667) | 本文提出投影神经微分方程（PNDEs），通过将预测速度投影到约束流形的切空间上来强制满足物理约束，在混沌系统和电网模型等测试中比现有方法表现更优、所需超参数更少且计算效率更高。 |
| [^768] | [On Generalisation Error Bounds for Transformers](https://arxiv.org/abs/2410.11500) | 本文通过建立多种范数和秩约束下的覆盖数界，为单层Transformer导出了与输入序列长度无关、以O(1/√n)速率衰减的改进泛化误差界，并刻画了低秩结构对泛化性能的影响。 |
| [^769] | [Policy Gradients for Cumulative Prospect Theory in Reinforcement Learning](https://arxiv.org/abs/2410.02605) | 本文为强化学习中的累积前景理论（CPT）目标推导了策略梯度定理，并基于次序统计量的蒙特卡洛估计器设计了具有渐近收敛性和样本复杂度保证的一阶策略梯度算法。 |
| [^770] | [The EM-algorithm and the Method of Moments in Softmax Mixture Models](https://arxiv.org/abs/2409.09903) | 本文对Softmax混合模型进行了理论与方法研究，证明在log K量级的原子分离条件下EM算法仅需O(log N)次迭代即可恢复混合原子，并为参数和子空间估计开发了矩方法。 |
| [^771] | [Tempora-Fusion: Time-Lock Puzzle with Efficient Verifiable Homomorphic Linear Combination](https://arxiv.org/abs/2406.15070) | 提出 Tempora-Fusion，首个支持对单个谜题解和同态线性组合进行高效公开验证、且无需可信设置的同态时间锁谜题方案。 |
| [^772] | [Decision-Centered Abstractions via Orthogonal Estimation of Difference-of-Q Functions](https://arxiv.org/abs/2406.08697) | 该论文提出以保留Q函数差分为目标的状态抽象方法，通过因果机器学习与统计稀疏学习实现，并开发了R学习器的动态泛化版本来估计Q函数差分，从而在离线强化学习中构建更简洁的以决策为中心的状态表示。 |
| [^773] | [Proper Dataset Valuation by Pointwise Mutual Information](https://arxiv.org/abs/2405.18253) | 本文提出了一个基于信息论的数据筛选方法评估框架，通过逐点互信息和Blackwell信息序将数据集质量定义为对真实模型参数的信息量，以避免古德哈特定律导致的度量失效问题。 |
| [^774] | [CausalBN-Bench: A Comprehensive Benchmark for Causal Learning Capability of LLMs](https://arxiv.org/abs/2404.06349) | 本文提出了CausalBN-Bench基准，通过相关性、因果骨架和因果关系识别三项任务的评估发现，大语言模型在简单因果关系上具有潜力，但在大规模网络和碰撞结构上仍显著落后于传统因果学习算法。 |
| [^775] | [Provable Multi-Party Reinforcement Learning with Diverse Human Feedback](https://arxiv.org/abs/2403.05006) | 该研究首次提出了多方协作强化学习的理论研究，通过整合多个个体不同偏好的元学习与不同社会福利函数的采用，克服了传统RLHF方法无法捕捉并平衡多个个体偏好的局限性。 |
| [^776] | [Data-driven rational function neural networks: a new method for generating analytical models of rock physics](https://arxiv.org/abs/2109.08813) | 提出了一种数据驱动的有理函数神经网络方法，能够从观测数据中自动生成既符合实际数据分布又具有物理意义的岩石速度解析模型。 |
| [^777] | [Directed mixed membership stochastic blockmodel](https://arxiv.org/abs/2101.02307) | 本文提出有向混合隶属度随机块模型及配套的高效谱估计算法DiSP，能够分别刻画有向网络中行、列节点的重叠社区归属，并在温和稀疏条件下给出节点级理论误差保证。 |
| [^778] | [Linear and Quadratic Discriminant Analysis: Tutorial](https://arxiv.org/abs/1906.02590) | 本教程系统讲解了LDA和QDA两种基本分类方法的决策边界优化、参数估计及其与多种机器学习方法的内在联系，并证明了LDA与Fisher判别分析的等价性。 |
| [^779] | [High-dimensional Linear Bandits with Knapsacks.](http://arxiv.org/abs/2311.01327) | 本文研究了具有背包约束的高维线性赌臂问题，利用稀疏结构实现改进遗憾。通过开发在线硬阈值算法和原始-对偶框架结合的方法，实现了对特征维度的对数改进的次线性遗憾。 |
| [^780] | [Latent class analysis by regularized spectral clustering.](http://arxiv.org/abs/2310.18727) | 本文提出了两种新的算法用于分类数据的潜在类别模型，并通过新定义的正则化拉普拉斯矩阵计算潜在类别分析，结果表明算法具有理论收敛速度和稳定的一致性。同时提出了一个衡量潜在类别分析强度的度量标准及相关程序，通过模拟实验验证了算法的效率和准确性，并应用于实际分类数据。 |
| [^781] | [Instance-wise Linearization of Neural Network for Model Interpretation.](http://arxiv.org/abs/2310.16295) | 这项研究提出了一种实例化线性化的方法，用于解释神经网络模型。通过给模型内部的每个输入特征分配重要得分，揭示了模型如何使用特征做出决策。这种方法有助于解决当前特征归因方法中的局限性，并提高了模型解释的准确性。 |
| [^782] | [Understanding Uncertainty Sampling.](http://arxiv.org/abs/2307.02719) | 本研究通过系统研究流式和池式主动学习下的不确定性采样算法，提出了一个等效损失的概念，并证明不确定性采样算法实质上是针对该等效损失进行优化。 |

# 详细

[^1]: KTO: 模型对齐视为展望理论优化

    KTO: Model Alignment as Prospect Theoretic Optimization

    [https://rss.arxiv.org/abs/2402.01306](https://rss.arxiv.org/abs/2402.01306)

    本文提出了一种名为KTO的方法，将模型对齐视为展望理论优化。与当前方法相比，KTO直接最大化生成效用而不是最大化偏好对数似然。在多个规模上，KTO的性能与基于偏好的方法相当甚至更好。

    

    凯恩曼与特沃斯基的展望理论告诉我们，人类以有偏见但明确的方式看待随机变量；例如，人们通常都是厌恶损失的。我们证明了将LLMs与人工反馈进行对齐的目标隐含地融合了许多这些偏见 - 这些目标 (例如 DPO) 的成功部分可归因于它们是"人类感知损失函数"(HALOs)。然而，这些方法所归因给人类的效用函数仍与展望理论文献中的不同。利用凯恩曼-特沃斯基人类效用的模型，我们提出了一种直接最大化生成效用而不是最大化偏好对数似然的HALO。我们将这种方法称为凯恩曼-特沃斯基优化(KTO)，并且它在从1B到30B的规模上与基于偏好的方法的性能相匹配或超过。关键是，KTO不需要偏好 - 只需要一个是否的二进制信号。

    Kahneman & Tversky's $\textit{prospect theory}$ tells us that humans perceive random variables in a biased but well-defined manner; for example, humans are famously loss-averse. We show that objectives for aligning LLMs with human feedback implicitly incorporate many of these biases -- the success of these objectives (e.g., DPO) over cross-entropy minimization can partly be ascribed to them being $\textit{human-aware loss functions}$ (HALOs). However, the utility functions these methods attribute to humans still differ from those in the prospect theory literature. Using a Kahneman-Tversky model of human utility, we propose a HALO that directly maximizes the utility of generations instead of maximizing the log-likelihood of preferences, as current methods do. We call this approach Kahneman-Tversky Optimization (KTO), and it matches or exceeds the performance of preference-based methods at scales from 1B to 30B. Crucially, KTO does not need preferences -- only a binary signal of whether 
    
[^2]: 学习可长度外推的循环模型

    Learning Length-Extrapolatable Recurrent Models

    [https://arxiv.org/abs/2609.09157](https://arxiv.org/abs/2609.09157)

    该论文提出时间信用稳定化（CST）方法，通过在反向传播中直接干预并稳定状态信用信号的范数，从而训练出能够超出训练范围进行长度外推的循环模型。

    

    循环模型为长上下文建模提供了一条自然的路径，然而通过时间反向传播（BPTT）训练的模型在超出其训练范围时往往会失效。经典分析强调沿时间路径出现的梯度消失或爆炸问题。然而，尽管存在严重的衰减，密集的逐token损失仍能训练出共享的循环规则，这表明衰减本身并不决定学习是否失败。我们转而研究状态信用：即在参与参数更新之前，未来损失到达早期循环状态的信号。据此，我们直接对状态信用进行干预，提出了时间信用稳定化方法。在反向传播过程中，CST对状态信用信号进行局部重缩放以稳定其范数，同时不旋转正在被校正的分量，并保持前向计算不变。由于受控合成任务与真实数据表现出不同的信用动态，我们针对……

    arXiv:2609.09157v1 Announce Type: new  Abstract: Recurrent models provide a natural path to long-context modeling, yet models trained with backpropagation through time (BPTT) often fail beyond their training horizon. Classical analyses emphasize gradients that vanish or explode along temporal paths. However, dense per-token losses can still train a shared recurrent rule despite severe decay, showing that decay alone does not determine whether learning fails. We instead study state credit: the signal through which future losses reach earlier recurrent states before contributing to parameter updates. Accordingly, we intervene directly on state credit and propose Credit Stabilization through Time (CST). During backward propagation, CST locally rescales the state-credit signal to stabilize its norm without rotating the component being corrected, while leaving the forward computation unchanged. Because controlled synthetic tasks and real data exhibit different credit dynamics, we specialize
    
[^3]: 银速率对梯度下降加速是（几乎）最优的

    Silver Rate Is (Almost) Optimal for Gradient Descent Acceleration

    [https://arxiv.org/abs/2609.09152](https://arxiv.org/abs/2609.09152)

    本文证明了银速率指数是梯度下降加速的最优多项式收敛速率，通过建立非anytime与anytime两种设定下匹配的下界，结合已有上界完整刻画了预定非负步长所能达到的最优加速效果。

    

    我们研究在光滑凸优化中，通过预先确定的非负步长，梯度下降（GD）能够被加速到何种程度。记 $p_{\mathrm{sil}}=\log_2(1+\sqrt{2})$，我们证明了 $\Omega\left(n^{-p_{\mathrm{sil}}-O(\sqrt{\log\log n/\log n})}\right)$ 的非anytime下界。在anytime设定下，每一个无限的非负步长调度都存在无穷多个终止时刻，其误差为 $\Omega\left(n^{-\frac{2p_{\mathrm{sil}}}{1+p_{\mathrm{sil}}}-O(\sqrt{\log\log n/\log n})}\right)$。结合银调度的上界 [Altschuler and Parrilo, 2025] 与anytime上界 [Zhang et al., 2025]，我们的结果确定了这两种设定下的最优多项式收敛指数。

    arXiv:2609.09152v1 Announce Type: cross  Abstract: We study how far gradient descent (GD) can be accelerated by predetermined nonnegative stepsizes in smooth convex optimization. Writing $p_{\mathrm{sil}}=\log_2(1+\sqrt{2})$, we prove an $\Omega\left(n^{-p_{\mathrm{sil}}-O(\sqrt{\log\log n/\log n})}\right)$ non-anytime lower bound. In the anytime setting, every infinite nonnegative schedule has infinitely many horizons with error $\Omega\left(n^{-\frac{2p_{\mathrm{sil}}}{1+p_{\mathrm{sil}}}-O(\sqrt{\log\log n/\log n})}\right)$. Together with the silver-schedule upper bound [Altschuler and Parrilo, 2025] and the anytime upper bound [Zhang et al., 2025], our results determine the optimal polynomial convergence exponents in both settings.
    
[^4]: NOAH：学习完整的患者旅程。一个用于表示与预测的纵向多模态时间感知模型

    NOAH: Learning the Full Patient Journey. A Longitudinal Multimodal Time-Aware Model for Representation and Forecasting

    [https://arxiv.org/abs/2609.09140](https://arxiv.org/abs/2609.09140)

    该论文提出了NOAH，一个时间感知、任务无关的生成式transformer模型，通过新颖的双向时间集成和变分潜在空间，首次实现了对患者完整多模态旅程的表示与未来预测。

    

    医疗保健的数字化产生了贯穿患者一生的海量、纵向、多模态的患者记录，然而充分利用这些数据来表示和预测患者状态轨迹仍然是一个关键挑战。当前的AI模型往往难以捕捉真实世界多模态患者数据中复杂的、不规则的时间动态性和固有的随机性。现有的用于建模纵向患者记录的AI方法主要是判别式的，仅限于少数模态，受限于封闭的分类词汇表，将时间视为单调的归纳偏置，或者在预测未来患者状态方面存在局限。我们提出了NOAH，一个时间感知的、任务无关的生成式transformer模型，用于表示和预测完整的患者多模态旅程。NOAH具有新颖的双向时间集成机制和变分潜在空间，以捕捉患者状态的连续演变及其随机性。

    arXiv:2609.09140v1 Announce Type: cross  Abstract: The digitization of healthcare has generated vast, longitudinal, and multimodal patient records over a lifetime, yet fully exploiting these data to represent and predict patient state trajectories remains a critical challenge. Current AI models often struggle to capture the complex, irregular temporal dynamics and inherent stochasticity of real-world multimodal patient data. Existing AI approaches for modeling longitudinal patient records are predominantly discriminative, limited to a few modalities, constrained by closed categorical vocabularies, treating time as a monotonic inductive bias, or they are limited in forecasting future patient states. We introduce NOAH, a time-aware, task-agnostic, generative transformer model representing and forecasting the full multimodal patient journey. NOAH features a novel bidirectional time integration and a variational latent space to capture the continuous evolution of patient states and the sto
    
[^5]: 熵正则化秩掩码策略优化用于代码生成中的测试时强化学习

    Entropy-Regularized Rank-Masked Policy Optimization for Test-Time Reinforcement Learning in Code Generation

    [https://arxiv.org/abs/2609.09135](https://arxiv.org/abs/2609.09135)

    该论文提出探针驱动的测试时强化学习方法，通过探针一致性奖励（PCR）为开放词汇的程序提供行为训练信号，并利用熵正则化秩掩码策略优化（ERPO）抵御奖励攻击，使TTRL成功应用于代码生成任务。

    

    现有的测试时强化学习（TTRL）方法通过对具有标准答案的无标注测试时任务进行答案级别的自投票来获取奖励，但这种方法在代码生成任务中会失效，因为程序无法通过表面形式进行比较，因而无法直接提供可用的训练信号。为了使TTRL适用于代码生成，我们提出了探针驱动的TTRL（probe-driven TTRL），该方法从问题描述中构建无需标准输出的探针输入，在这些探针上执行候选程序，并根据所得的行为一致性定义探针一致性奖励。PCR为开放词汇的程序提供了行为层面的训练信号，但它并非完全可靠的验证器，仍然容易因虚假共识而遭受奖励攻击。因此，我们引入了熵正则化秩掩码策略优化，它通过秩掩码机制将低PCR转化为保守的负向更新，并控制策略熵（原文在此处截断）。

    arXiv:2609.09135v1 Announce Type: new  Abstract: Existing methods for test-time reinforcement learning (TTRL) derive rewards from answer-level self-voting on unlabeled test-time tasks with canonical answers, but this breaks down for code generation because programs cannot be compared by surface form and therefore do not directly provide a usable training signal. To make TTRL applicable to code generation, we propose probe-driven TTRL, which constructs output-free probe inputs from the problem statement, executes candidate programs on these probes, and defines a Probe Consensus Reward (PCR) from the resulting behavioral agreement. PCR provides a behavioral training signal for open-vocabulary programs, but it is not a fully reliable verifier and remains susceptible to reward hacking through spurious consensus. We therefore introduce Entropy-Regularized Rank-Masked Policy Optimization (ERPO), which converts low PCR into conservative negative updates through rank masking and controls polic
    
[^6]: 稀疏激活神经网络的近乎紧致的Rademacher界

    Nearly Tight Rademacher Bounds for Sparsely Activated Neural Networks

    [https://arxiv.org/abs/2609.09130](https://arxiv.org/abs/2609.09130)

    本文针对每个输入至多激活 $k$ 个隐藏单元的单隐藏层稀疏ReLU神经网络，建立了近乎紧致的Rademacher复杂度上界和相匹配的下界，并揭示了输入域和偏置设定对复杂度的关键影响。

    

    一个输入可能仅激活少数隐藏单元，即使不同的输入集合起来用到了整个网络。我们研究了Awasthi等人（COLT 2024）提出的单隐藏层ReLU模型中这种依赖于输入的稀疏性的统计复杂度。对于宽度为 $s$、每个输入至多激活 $k$ 个单元、有效权重和偏置界为 $W,B$ 的情况，该函数类在固定半径 $R$ 输入域上的每个大小为 $m$ 的样本都满足 $\mathcal{R}(S)\le CWR\min\{k,\sqrt{sk/m}\log^{3/2}(2m)\}+kB/\sqrt m$。通过一个保持支持集的覆盖和单一的归一化链式论证，我们在最多相差对数因子的意义下去除了以往结果中显式的维度因子。在适当的独立同分布边缘分布上的下界与该上界最多相差对数因子，说明了跨输入改变激活单元是如何保留宽度依赖性的。输入域至关重要：在整个球上稀疏的零偏置网络至多有 $2k$ 个非零单元，其复杂度为 $O(kWR/\sqrt m)$，而当偏置界与 $WR$ 相当时则会恢复……

    arXiv:2609.09130v1 Announce Type: new  Abstract: An input may activate few hidden units even when different inputs collectively use an entire network. We study the statistical complexity of this input-dependent sparsity in the one-hidden-layer ReLU model of Awasthi et al. (COLT 2024). For width $s$, at most $k$ active units per input, and effective weight and bias bounds $W,B$, every size-$m$ sample in the class's fixed radius-$R$ input domain satisfies $\mathcal{R}(S)\le CWR\min\{k,\sqrt{sk/m}\log^{3/2}(2m)\}+kB/\sqrt m$. A support-preserving cover and a single normalized chaining argument remove the previous explicit dimension factor, up to logarithms. Lower bounds on appropriate i.i.d. marginals match up to those logarithms, showing how changing active units across inputs retains a width dependence. The input domain matters: zero-bias networks sparse on the entire ball have at most $2k$ nonzero units and complexity $O(kWR/\sqrt m)$, whereas bias bounds comparable to $WR$ restore the
    
[^7]: 阿玛里贝叶斯对偶性的推广

    A Generalization of Amari's Bayesian Duality

    [https://arxiv.org/abs/2609.09126](https://arxiv.org/abs/2609.09126)

    本文通过将阿玛里的贝叶斯对偶性与贝叶斯规则的凸对偶性相联系，提出了阿玛里贝叶斯对偶性的推广，并探讨了其对现代人工智能的意义。

    

    阿玛里对信息几何和机器学习的贡献是众所周知的。在这里，我们重新审视阿玛里关于贝叶斯对偶性的工作，这项工作尚未受到足够多的关注。我们将阿玛里的贝叶斯对偶性与贝叶斯规则的凸对偶性联系起来。利用这种联系，我们提出了阿玛里贝叶斯对偶性的一个推广，并讨论了其与现代人工智能的相关性。

    arXiv:2609.09126v1 Announce Type: new  Abstract: Amari's contributions to information geometry and machine learning are well known. Here, we revisit Amari's work on Bayesian duality which has not received as much attention. We connect Amari's Bayesian duality to a convex duality of Bayes' rule. Using this connection, we present a generalization of Amari's Bayesian duality and discuss its relevance for modern artificial intelligence.
    
[^8]: 尺度不变优化何时变得不稳定？带权重衰减的精确调度定律

    When Does Scale-Invariant Optimization Become Unstable? An Exact Schedule Law with Weight Decay

    [https://arxiv.org/abs/2609.09116](https://arxiv.org/abs/2609.09116)

    该论文推导出一个精确的离散时间定律，揭示归一化导致的尺度不变性使学习率调度与权重衰减通过参数范数相互作用来控制有效步长，并证明该平衡点本质不稳定，因此恒定学习率加权重衰减无法稳定收敛，而是产生循环行为。

    

    归一化使得神经网络的大部分部分在实际上具有尺度不变性，从而诱导出一个隐藏的反馈回路：学习率调度与权重衰减通过参数范数相互作用，共同控制优化器所采取的有效步长。我们证明这种相互作用由一个精确的离散时间定律所支配：一个单一的标量量捕捉了所有调度和衰减的驱动力，而范数增长则产生一种相反的几何自猝灭效应。这得出了一个清晰的边界，明确地区分了以收缩为主导和以扩张为主导的有效学习率区域。为了理解其底层机制，我们对一个完全求解的归一化回归模型进行了精确分析，该模型的动力学可简化为二维，并证明其平衡点本质上是不可稳定的，这意味着恒定学习率结合权重衰减无法稳定地维持内部平衡，而是会产生由（后文截断）驱动的循环行为。

    arXiv:2609.09116v1 Announce Type: new  Abstract: Normalization renders large parts of neural networks effectively scale invariant, inducing a hidden feedback loop in which learning-rate schedules and weight decay interact through the parameter norm to control the effective step taken by the optimizer. We show that this interaction is governed by an exact discrete-time law: a single scalar quantity captures all schedule and decay forcing, while norm growth induces an opposing geometric self-quenching effect. This yields a sharp boundary that cleanly separates contraction- and expansion-dominated effective learning rate regimes. To understand the underlying mechanism, we provide exact analysis of a fully solved normalized regression model where the dynamics reduce to two dimensions and show that the balance point is intrinsically unstable, implying that constant learning rate with weight decay cannot stably maintain an interior equilibrium and instead produces recurrent behavior driven b
    
[^9]: SAEScientist-Bench：AI智能体能否开展自主的SAE可解释性研究？

    SAEScientist-Bench: Can AI Agents Conduct Autonomous SAE Interpretability Research?

    [https://arxiv.org/abs/2609.09113](https://arxiv.org/abs/2609.09113)

    本文提出SAEScientist-Bench基准，评估AI智能体能否作为科学家自主运用稀疏自编码器（SAE）工具，进行机制可解释性的自主科学发现研究。

    

    虽然关于递归自我改进（RSI）的研究主要集中于自动化模型训练流程，但可靠的自主发展还需要一个缺失的关键支柱：事后监控与审计，以理解模型学到了什么并确保安全对齐。机制可解释性工具对于弥合这一差距至关重要，其中稀疏自编码器（SAE）通过隔离可解释特征用于模型检查与引导，成为这一领域的基石。在本文中，我们引入SAEScientist-Bench，用于评估AI智能体能否作为科学家，利用SAE工具进行自主的机制发现。给定一个目标概念，智能体需设计对比探针，并在Gemma-2-9B-IT模型的Gemma Scope词典（包含131K+特征）中导航以发现最优特征，随后基于锚定在Neuronpedia上的精选专家参考特征进行评估，评估维度包括激活排名、对比文本上的概念选择性以及因果引导效果。

    arXiv:2609.09113v1 Announce Type: new  Abstract: While research on recursive self-improvement (RSI) has predominantly automated model training pipelines, reliable autonomous development demands a missing pillar: post-hoc monitoring and auditing to understand what models learn and ensure safe alignment. Mechanistic interpretability tools are essential to bridge this gap, among which Sparse Autoencoders (SAEs) serve as a cornerstone by isolating interpretable features for model inspection and steering. In this paper, we introduce SAEScientist-Bench to evaluate whether AI agents can act as scientists utilizing SAE tools for autonomous mechanistic discovery. Given a target concept, an agent designs contrastive probes and navigates a Gemma Scope dictionary of 131K+ features in Gemma-2-9B-IT to discover the optimal feature, evaluated against curated expert reference features anchored on Neuronpedia across activation rank, concept selectivity on contrastive texts, and causal steering. Across 
    
[^10]: 课程学习作为传输：用Wasserstein测地线理解课程

    Curriculum Learning as Transport: Understanding Curricula with Wasserstein Geodesics

    [https://arxiv.org/abs/2609.09099](https://arxiv.org/abs/2609.09099)

    提出Wasserstein课程路径框架，将课程表示为训练分布在离散难度级别上的传输轨迹，从而解耦课程学习中的各设计因素，并发现课程效应高度依赖情境、不存在普适最优的课程策略。

    

    课程学习受多个相互耦合的设计选择支配——如何定义难度、如何对样本排序、每个难度级别获得多少训练曝光、以及训练在各级别之间推进的速度——这使得人们难以分离出真正起作用的因素。我们提出了Wasserstein课程路径，这是一个简单的基于传输的框架，通过将课程表示为离散难度级别上训练分布的轨迹来解耦这些因素。在一个包含12个任务和33个难度轴的校准合成任务套件上，我们利用该框架在固定训练预算下分离了排序方式、匹配曝光、端点平滑度和节奏推进的影响。我们发现课程效应高度依赖具体情境：没有单一策略能在所有任务、难度轴和预算条件下都占据优势，课程的主要作用是改变固定预算被最有效利用的位置。在该框架内，从易到难的排序能提升困难级别的表现……

    arXiv:2609.09099v1 Announce Type: new  Abstract: Curriculum learning is governed by several coupled design choices---how difficulty is defined, how examples are ordered, how much exposure each level receives, and how quickly training moves across levels---making it hard to isolate what actually helps. We present Wasserstein curriculum paths, a simple transport-based framework that decouples these factors by representing curricula as trajectories of training distributions over discrete difficulty levels. Across a calibrated synthetic suite with 12 tasks and 33 difficulty axes, we use this framework to isolate the effects of ordering, matched exposure, endpoint smoothness, and pacing under fixed training budgets. We find that curriculum effects are strongly context-dependent: no single strategy dominates across tasks, difficulty axes, and budgets, and curricula mainly change where a fixed budget is spent most effectively. Within this framework, easy-to-hard ordering improves hard-level p
    
[^11]: ThinkPrior：面向RLVR冷启动提示选择的零采样难度先验

    ThinkPrior: Zero-Rollout Difficulty Priors for Cold-Start Prompt Selection in RLVR

    [https://arxiv.org/abs/2609.09075](https://arxiv.org/abs/2609.09075)

    提出ThinkPrior方法，通过一次离线验证器评分锚点构建零采样成本的Beta难度先验，解决RLVR中GRPO训练因全对/全错“沉默组”浪费39%采样以及基于历史的方法冷启动采样浪费的问题。

    

    在使用组相对策略优化（GRPO）训练的可验证奖励强化学习（RLVR）中，本文所研究的无KL惩罚的奖励-优势项依赖于组内奖励差异。如果一个组中的所有采样全部正确或全部错误，其组相对优势恒为零；这些零优势的“沉默组”无法提供任何奖励-优势梯度，而均匀采样却会将一次训练中39%的采样浪费在这些组上。基于历史的提示选择方法必须先消耗目标策略的采样来估计题目难度，从而造成伴随采样浪费的冷启动问题；ThinkPrior则改为在一次离线过程中利用外部锚点，在第一次目标策略采样之前构建零采样成本的难度先验。由验证器评分的锚点通过率为Beta后验提供外部锚点初始化；ThinkPrior依据期望可学习性进行提示选择，随后根据训练结果进行后验更新，既不改变损失函数，也不改变（原文在此处截断）。

    arXiv:2609.09075v1 Announce Type: cross  Abstract: In reinforcement learning with verifiable rewards (RLVR) trained with group relative policy optimization (GRPO), the KL-free reward-advantage term studied here depends on within-group reward variation. If all rollouts in a group are correct or all are wrong, their group-relative advantages are identically zero; these zero-advantage silent groups provide no reward-advantage gradient, yet uniform sampling spends 39% of a run's rollouts on them. History-based prompt selection must first spend target-policy rollouts to estimate difficulty, creating a cold start with rollout waste; ThinkPrior instead uses an external anchor in one offline pass to construct a zero-rollout difficulty prior before the first target-policy rollout. The verifier-scored anchor pass rate supplies an external-anchor initialization for a Beta posterior; ThinkPrior selects by expected learnability and then updates from training outcomes, changing neither the loss nor 
    
[^12]: 面向稀疏标注时间序列的多任务学习：以耐寒性建模为例的案例研究

    Multi-Task Learning for Sparsely-Labeled Time Series: A Case Study on Cold-Hardiness Modeling

    [https://arxiv.org/abs/2609.09062](https://arxiv.org/abs/2609.09062)

    本文针对葡萄耐寒性预测中标注数据稀疏且品种间存在差异的难题，提出将不同葡萄品种视为不同任务的多任务学习方法，从而有效利用有限的稀疏标注时间序列数据，提升基于RNN的每日耐寒性预测效果。

    

    我们提出了一项真实世界的案例研究，探讨在数据有限且时间标签稀疏的情况下，利用多任务学习（MTL）进行时间过程建模。具体而言，我们研究了针对一个重要农业问题的多任务学习方法——预测葡萄耐寒性，即发生致命冻害时的温度。耐寒性会随天气变化而变化，且在田间难以直接测量，因此种植者需要依赖预测结果来决定何时采取代价高昂的防霜冻措施。我们应用循环神经网络（RNN），基于时间序列天气数据进行每日耐寒性预测。一个主要挑战在于耐寒性响应因植物品种而异，且每个品种的真实标注数据在时间上稀疏且有限。为应对这一挑战，我们研究了将不同品种对应为不同任务的多任务学习（MTL）方法来组合利用数据，并开发了一种……

    arXiv:2609.09062v1 Announce Type: new  Abstract: We present a real-world case study of multi-task learning (MTL) for temporal process modeling from limited data with temporally sparse labels. Specifically, we investigate multi-task learning for the important agricultural problem of predicting grape cold hardiness, which is the temperature at which lethal freezing occurs. Cold hardiness changes in response to weather and is difficult to measure directly in the field. Thus, growers rely on predictions to decide when to apply costly frost mitigation measures. We apply recurrent neural networks (RNNs) for daily cold-hardiness prediction from time series weather data. A major challenge is that the cold hardiness response varies across plant cultivars and ground-truth data for each cultivar is temporally sparse and limited. To address this challenge, we investigate multi-task learning (MTL) approaches for combining data, where different tasks correspond to different cultivars. We develop a v
    
[^13]: PlayTrain：一个用于大语言模型生成的可适应JavaScript游戏的高效强化学习框架

    PlayTrain: An Efficient Reinforcement Learning Framework for LLM-Generated Adaptable JavaScript Games

    [https://arxiv.org/abs/2609.09059](https://arxiv.org/abs/2609.09059)

    PlayTrain是一个强化学习框架，利用大语言模型从简单提示生成JavaScript游戏，并提供能在标准gym环境中以每秒超过100万次智能体决策的速度训练强化学习智能体的高效流水线。

    

    虽然许多视频游戏环境（VGEs）在推进强化学习（RL）发展中发挥了关键作用，但开发新的视频游戏环境或修改现有环境以支持新功能，一直是一个需要大量手工编码的繁琐过程。在这里，我们提出了PlayTrain，这是一个强化学习框架，它结合了大语言模型（LLMs）从最少的人类提示中稳健地生成JavaScript（JS）游戏的能力，以及一个可以在标准“gym”环境中运行任何JS游戏的高效流水线。最近的LLMs不仅特别擅长编写JS代码，而且JS格式还允许用户轻松地游玩生成的视频游戏环境，同时PlayTrain使我们能够在完全相同的游戏上训练强化学习智能体。我们展示了PlayTrain的多个用例，包括用简单的JS克隆著名的Atari和ProcGen游戏，其中PlayTrain在单个GPU节点上以每秒超过100万次智能体决策的速度端到端地训练基于像素的智能体；以及创建修改版本的游戏。

    arXiv:2609.09059v1 Announce Type: new  Abstract: While many video-game environments (VGEs) have played crucial roles in advancing reinforcement learning (RL), developing novel VGEs or modifying existing ones to support new features, has been a laborious process requiring extensive hand-coding. Here we present PlayTrain, an RL framework that combines the abilities of large language models (LLMs) to robustly generate JavaScript (JS) games from a minimal human prompt, and an efficient pipeline that can run any JS game in a standard 'gym' environment. Not only are recent LLMs particularly good at writing JS code, but the JS format also allows users to easily play generated VGEs, while PlayTrain enables us to train RL agents on the exact same games. We demonstrate multiple use cases of PlayTrain, including cloning well-known Atari and ProcGen games in simple JS, where PlayTrain trains pixel-based agents end-to-end at over 1M agent-decisions per second on a single GPU node; and creating modi
    
[^14]: 用于大语言模型行为控制的无训练任务向量

    Training-Free Task Vectors for LLM Behavioral Control

    [https://arxiv.org/abs/2609.09054](https://arxiv.org/abs/2609.09054)

    提出了一种无需微调的TFTVs方法，仅利用前向传播统计量将激活引导向量映射为秩一权重空间编辑，从而以低成本实现对大语言模型行为的放大、抑制与组合控制。

    

    任务向量通过识别权重空间中具有语义意义的方向，实现训练后的模型编辑，通常计算为微调模型与其预训练初始化模型之间的差异。然而，这种对微调的依赖使得发现此类方向的成本高昂，限制了训练后模型编辑的实用性。为了解决这一局限，我们提出了无训练任务向量（TFTVs），这是一种无需微调即可计算类任务向量方向的新方法。我们的方法仅利用前向传播统计量，将激活引导向量映射为秩一权重空间编辑，同时满足直接支持通过加法实现学习、通过减法实现遗忘以及多个编辑进行组合的算术性质。在实验中，我们在大语言模型行为控制任务上对TFTVs进行了评估，结果表明它们能够在保持模型原有能力的同时，持续地放大、抑制和组合目标行为。

    arXiv:2609.09054v1 Announce Type: cross  Abstract: Task vectors enable post-training model editing by identifying semantically meaningful directions in weight space, typically computed as the difference between a fine-tuned model and its pretrained initialization. However, this reliance on fine-tuning makes discovering such directions costly and limits the practicality of post-training model editing. To address this limitation, we introduce Training-Free Task Vectors (TFTVs), a novel method to compute task-vector-like directions without requiring fine-tuning. Our method maps activation steering vectors to rank-one weight-space edits using only forward-pass statistics, while satisfying arithmetic properties that directly support learning via addition, forgetting via subtraction, and the composition of multiple edits. Empirically, we evaluate TFTVs on large language model behavioral control tasks and show that they consistently amplify, suppress, and compose target behaviors while preser
    
[^15]: 推理表示是否有助于人类评估大语言模型的输出？

    Do Reasoning Representations Help Humans Evaluate LLM Outputs?

    [https://arxiv.org/abs/2609.09038](https://arxiv.org/abs/2609.09038)

    研究发现，尽管人们更偏好基于规划和分解的复杂推理表示，但更简单的思维链表示反而更能有效支持人类对大语言模型输出的评估。

    

    推理表示正被越来越多地用作大语言模型输出的解释。然而，它们通常以模型为中心的标准进行评估，例如答案准确性和忠实度，这使得它们是否能帮助人们评估模型响应仍不清楚。在这项工作中，我们将推理表示作为面向人类的交互界面进行研究，而不是作为模型推理能力的代理指标。我们开展了一项受控人类实验研究，涵盖六种推理格式在不同复杂程度任务中的表现，并由一个基于网络的框架提供支持，该框架对任务领域、问题实例和表示顺序进行随机化。该研究收集了关于结构理解、错误检测与定位以及信任校准的细粒度判断。我们的研究揭示了感知偏好与对人类评估实际支持之间的不匹配：参与者更偏好基于规划和分解的推理表示，但更简单的思维链轨迹反而能更好地支持验证。

    arXiv:2609.09038v1 Announce Type: new  Abstract: Reasoning representations are increasingly used as explanations for large language model outputs. Yet they are typically evaluated with model-centric criteria, such as answer accuracy and faithfulness, leaving it unclear whether they help people evaluate model responses. In this work, we study reasoning representations as human-facing interfaces rather than proxies for model reasoning ability. We conduct a controlled human study of six reasoning formats across tasks of varying complexity, supported by a web-based framework that randomizes task domains, problem instances, and representation order. The study collects fine-grained judgments of structural understanding, error detection and localization, and trust calibration. Our study shows a mismatch between perceived preference and support for human evaluation. Participants prefer planning- and decomposition-based representations, but simpler chain-of-thought traces better support verific
    
[^16]: 答案分布轨迹：LLM推理的随机动力学视角

    Answer-Distribution Trajectories: A Stochastic-Dynamics View of LLM Reasoning

    [https://arxiv.org/abs/2609.09030](https://arxiv.org/abs/2609.09030)

    该论文提出“答案分布轨迹”这一受随机动力学启发的精细表示方法，通过追踪LLM推理过程中对答案的完整预测分布，刻画探索、修正、移动和承诺等动态推理特征，从而区分推理成功与失败的不同动态机制。

    

    思维链推理在模型输入与最终答案之间提供了结构化的计算过程。然而，它通常仅通过终点准确率来评估，这忽略了模型达到最终答案所经历的路径。一系列新兴工作使用熵曲线来解决这一局限，熵曲线能够追踪不确定性在推理过程中如何演变，但无法揭示究竟是哪些相互竞争的假设造成了这种不确定性。我们提出了答案分布轨迹，这是一种受随机动力学启发的表示方法，可以在推理展开的过程中追踪模型对答案的完整预测分布。作为比终点摘要和熵摘要严格更精细的表示，答案分布轨迹使我们能够通过一个涵盖探索、修正、移动和承诺的动态推理特征来刻画推理轨迹，并区分推理成功与失败的不同动态机制。在十六个开源权重语言……

    arXiv:2609.09030v1 Announce Type: new  Abstract: Chain-of-thought reasoning provides a structured computation between a model's input and final answer. Yet it is often evaluated through endpoint accuracy, which ignores the path taken to reach that answer. An emerging line of work addresses this limitation using entropy profiles, which track how uncertainty evolves over the reasoning process but do not reveal which competing hypotheses account for that uncertainty. We introduce answer-distribution trajectories, a stochastic-dynamics-inspired representation that tracks the model's full predictive distribution over answers as reasoning unfolds. As a strictly finer representation than endpoint and entropy summaries, answer-distribution trajectories enable us to characterize a trace through a dynamical reasoning profile spanning exploration, revision, motion, and commitment, and to distinguish different dynamical mechanisms of reasoning success and failure. Across sixteen open-weight langua
    
[^17]: 基于Gaia DR3数据的局域银河系势能与恒星分布函数的封闭形式解

    Closed-Form of the Local Galactic Potential and Stellar Distribution Function from Gaia DR3

    [https://arxiv.org/abs/2609.09011](https://arxiv.org/abs/2609.09011)

    该论文提出了一种新方法，通过单独估计恒星分布函数、以加速度形式线性化无碰撞玻尔兹曼方程来直接测量局域力场，并利用符号回归获得局域银河系势能的封闭形式解，从而解决了同时拟合分布函数与势能时的退化问题。

    

    局域暗物质密度决定了直接探测实验中预期信号的强度，然而基于恒星运动的已发表估计结果之间的分歧超过了其误差范围，且最近一项对Gaia数据的机器学习分析发现局域密度与零一致。根据金斯定理，由运动积分构建的分布函数对于任何势能选择都平凡地满足无碰撞玻尔兹曼方程（CBE），因此同时将分布函数和势能拟合到CBE的搜索方法无法确定两者中的任何一个。我们的流程改为单独估计分布函数，将该方程以加速度为变量进行线性化，从而允许对局域力场进行直接测量，然后通过符号回归为该力场拟合封闭形式解。在整个过程中，我们发现可用的信息不在于CBE残差，而在于恒星数量计数……（摘要在此处被截断）

    arXiv:2609.09011v1 Announce Type: cross  Abstract: The local dark matter density determines the strength of the signal expected in direct-detection experiments, yet published estimates from stellar motions disagree by more than their errors, and the most recent machine-learning analysis of Gaia data finds a local density consistent with zero. According to Jeans' theorem, a distribution function built from integrals of motion satisfies the collisionless Boltzmann equation (CBE) trivially for any choice of potential, so a search that simultaneously fits the distribution function and the potential to the CBE identifies neither. Our pipeline instead estimates the distribution function in isolation, linearizing the equation in terms of accelerations and allowing for direct measurement of the local force field, and then fits closed forms to that field via symbolic regression. Throughout, we find that the usable information lies not in the CBE residual but in the stellar number counts, the ob
    
[^18]: 放手还是学会自我纠错：面向约束离散任务的连续扩散模型

    Let It Go or Learn to Self-Correct: Continuous Diffusion for Constrained Discrete Tasks

    [https://arxiv.org/abs/2609.09009](https://arxiv.org/abs/2609.09009)

    研究发现，在数独等全局约束的离散任务中，直接从扩散模型的干净预测进行采样（而非逐步去噪采样），无需重新训练即可将数独有效率从31%大幅提升至95%，因为标准扩散采样中保持接近含噪状态会固化早期错误并使反向轨迹偏离训练分布。

    

    去噪扩散概率模型（DDPMs）通过从噪声开始并反复去噪来生成样本，同时保持每次更新接近当前的含噪状态。这种行为在许多连续域中是有效的，但其对于全局约束的离散任务（如数独、图连通性、拉丁方和N皇后问题）的作用尚不清楚。在这类场景中，早期的离散错误很难被撤销。因此，标准的扩散采样可能会保留早期的错误，即使模型输出的干净预测本身具有足够的信息量。我们将标准采样器与直接从模型的干净预测进行采样的方法进行了比较。在无需重新训练的情况下，仅这一项改变就将数独的有效率从31%提升到了95%，并在其他离散任务上也取得了一致的提升。我们假设，保持接近当前含噪状态是有害的，因为反向轨迹可能会偏离模型训练时所基于的正向加噪分布。

    arXiv:2609.09009v1 Announce Type: cross  Abstract: Denoising Diffusion Probabilistic Models (DDPMs) generate samples by starting from noise and repeatedly denoising while keeping each update close to the current noisy state. This behavior is effective in many continuous domains, but its role is less clear for globally constrained discrete tasks, such as Sudoku, graph connectivity, Latin squares, and N-queens. In such settings, early discrete errors can be difficult to undo. As a result, standard diffusion sampling may preserve early mistakes, even when the model's clean predictions are informative. We compare standard samplers to sampling directly from the model's clean prediction. Without retraining, this single change improves Sudoku validity from 31% to 95%, with consistent gains across the other discrete tasks. We hypothesize that staying close to the current noisy state is harmful because the reverse trajectory can drift off the forward noising distribution the model was trained o
    
[^19]: 大语言模型情境理解能力的评估

    Evaluation of Contextual Understanding in Large Language Models

    [https://arxiv.org/abs/2609.09004](https://arxiv.org/abs/2609.09004)

    本文提出基于知识图谱的S3KG评估框架，通过融合结构相似度与语义相似度来量化评估大语言模型的情境理解能力，并提供诊断推理错误的分类框架。

    

    大语言模型（LLM）在各类自然语言处理任务中展现出令人瞩目的性能，但其是否具备真正的情境理解能力仍不确定。传统评估指标如困惑度、双语评估替补（BLEU）或表层准确率无法揭示大语言模型在提取、整合和推理情境信息方面的能力——这一缺陷在问答任务中尤为关键，因为模型必须使回答与基于情境的知识保持一致，而非依赖记忆中的关联。我们提出了一种新颖的基于知识图谱的评估框架，引入了面向知识图谱的语义结构相似度（S3KG），这是一种将结构相似度与语义相似度整合为连续评估分数的混合相似度度量，同时还提供了一个用于分类推理错误的诊断框架。为了验证该流程，我们在一个精选的问答基准上，将S3KG与现有指标进行了对比评估。

    arXiv:2609.09004v1 Announce Type: cross  Abstract: Large Language Models (LLMs) demonstrate impressive performance across diverse NLP tasks, yet their ability to exhibit genuine contextual understanding remains uncertain. Traditional evaluation metrics such as perplexity, BiLingual Evaluation Understudy (BLEU), or surface-level accuracy fail to reveal how well LLMs extract, integrate, and reason over contextual information--a gap particularly critical in question answering, where models must align responses with contextually grounded knowledge rather than memorized associations. We propose a novel knowledge graph-based evaluation framework introducing Semantic Structural Similarity for KGs (S3KG), a hybrid similarity measure integrating structural and semantic similarity into a continuous evaluation score, alongside a diagnostic framework for categorizing reasoning errors. To validate this pipeline, we evaluate S3KG against established metrics on a curated question-answer (QA) benchmar
    
[^20]: Deposon：一种可审计、保证守恒、经博弈论验证的LLM推理路径散射层

    Deposon: An Auditable, Conservation-Guaranteed, Game-Theoretically Tested Scattering Layer over LLM Reasoning Paths

    [https://arxiv.org/abs/2609.09001](https://arxiv.org/abs/2609.09001)

    Deposon散射层将LLM推理路径的每个节点绑定到满足严格能量守恒（T+R+A=1）的三通道散射状态，为被丢弃的推理路径提供机器可审计的账本，其价值不在于提升过滤准确率，而在于实现机器可验证性。

    

    多步LLM推理缺乏可机器复核的账本：被丢弃的推理路径不会留下可审计的记录。我们提出了Deposon散射层，它将LLM生成的概念分解图的每个节点绑定到双参数Deposon状态；路径经历三通道散射——透射、反射、不可逆耗散——对于任意参数满足T+R+A=1，每条路径的最大能量审计偏差为2.2E-16（机器精度）。我们诚实地报告了所有三个证据层级。在合成陷阱基准上，路径过滤增益是闭合的（预先注册的）：统一方法达到100%，而诱饵捕获基线为7%/10%。在真实基准上，该层与简单的六关键词规则过滤器无法区分（GSM8K 0.87 ≥ 0.85，McNemar p=0.5；StrategyQA 0.899 = 0.899）；这里未检测到差异，因此我们将声明锐化为“差异价值仅在于机器可验证性”。融合产生

    arXiv:2609.09001v1 Announce Type: new  Abstract: Multi-step LLM reasoning lacks a machine-recheckable ledger: discarded reasoning paths leave no auditable record. We propose the Deposon scattering layer, which binds each node of an LLM-generated concept-decomposition graph to a two-parameter Deposon state; paths undergo three-channel scattering -- transmission, reflection, irreversible dissipation -- obeying T+R+A=1 for arbitrary parameters, with a maximum per-path energy-audit deviation of 2.2E-16 (machine epsilon). We report all three evidence tiers honestly. On synthetic trap benchmarks the path-filtering gain is closed (pre-registered): unified reaches 100% versus a decoy-capture baseline at 7%/10%. On real benchmarks the layer is indistinguishable from a trivial six-keyword rule filter (GSM8K 0.87 >= 0.85, McNemar p=0.5; StrategyQA 0.899 = 0.899); no difference is detected here, so we sharpen the claim to "the differential value lies solely in machine verifiability." Fusion yields
    
[^21]: 物理信息驱动的深度学习用于减少ICU中的假性室性心动过速警报

    Physics-Informed Deep Learning for False Ventricular Tachycardia Alarm Reduction in the ICU

    [https://arxiv.org/abs/2609.08992](https://arxiv.org/abs/2609.08992)

    该论文提出将一维SE-ResNet与基于Windkessel血流动力学模型的物理信息辅助重建任务相结合的深度学习框架，通过惩罚生理上不可信的伪迹模式，在VTaC基准上实现了假性室速警报抑制性能的5分提升，并带来2倍的标签效率。

    

    假性室性心动过速（VT）警报是导致重症监护病房（ICU）警报疲劳的主要原因之一。我们提出了一种深度学习框架，该框架将一维SE-ResNet与符合ICU实际情况的数据增强相结合，并引入一项基于三元素Windkessel血流动力学模型的物理信息辅助重建任务，该任务以可微分前向仿真的方式实现。通过要求网络的潜在表示生成符合生理规律的动脉血压波形，由伪迹驱动的ECG模式会受到惩罚，而真正的VT在各模态间保持一致性。在严格的实时协议（警报前10秒窗口）下，在VTaC基准数据集上进行评估，我们的方法相比之前的最先进方法在挑战赛得分上提升了5分。消融研究证实，物理信息目标是性能提升的主要驱动力，它带来了准确率的提升、2倍的标签效率，以及更具局部性和临床意义的（可解释性）。

    arXiv:2609.08992v1 Announce Type: new  Abstract: False ventricular tachycardia (VT) alarms are a leading contributor to alarm fatigue in intensive care units. We propose a deep learning framework combining a 1D SE-ResNet with ICU-realistic data augmentations and a physics-informed auxiliary reconstruction task based on the three-element Windkessel hemodynamic model, implemented as a differentiable forward simulation. By requiring the network's latent representation to produce physiologically plausible arterial pressure waveforms, artifact-driven ECG patterns are penalized while true VT remains coherent across modalities. Evaluated on the VTaC benchmark under a strict real-time protocol (10-second pre-alarm window), our method achieves a 5-point Challenge Score improvement over prior state-of-the-art. Ablation studies confirm that the physics-informed objective is the primary performance driver, providing gains in accuracy, 2x label efficiency, and more localized and clinically meaningf
    
[^22]: 作为上下文采样器的Transformer：从闭式扩散到无需估计的采样

    Transformers as In-Context Samplers: From Closed-Form Diffusion to Estimation-Free Sampling

    [https://arxiv.org/abs/2609.08981](https://arxiv.org/abs/2609.08981)

    本文证明冻结的Transformer可以通过上下文学习模拟迭代式生成采样器（如闭式扩散采样器），其中softmax注意力计算责任权重与加权经验均值、前馈层实现欧拉更新，从而将上下文学习能力从监督学习拓展到数据生成任务。

    

    越来越多的研究证实，大型语言模型并非仅仅是统计记忆器，而是具备上下文学习能力：在测试时仅使用提示中提供的样本进行推理，无需任何参数更新。先前的理论工作已表明，这种能力可以扩展到线性回归等监督学习任务。我们证明上下文学习可以进一步扩展到数据生成领域：冻结的Transformer可以从上下文样本中模拟迭代式生成采样器。我们首先表明，Transformer可以实现闭式和平滑闭式扩散采样器。该构造为softmax注意力机制确定了一个具体的生成性角色：它负责计算责任权重和加权经验均值，而前馈层则实现欧拉更新。为了在实证层面将这些构造与预训练语言模型建立联系，我们研究了语义主题采样：通过提示……

    arXiv:2609.08981v1 Announce Type: cross  Abstract: A growing body of work establishes that large language models are not mere statistical memorizers, but are capable of in-context learning: performing inference at test time using only examples provided in the prompt, without any parameter updates. Prior theoretical work has shown that this capability extends to supervised learning tasks such as linear regression. We prove that in-context learning extends further to \emph{data generation}: frozen transformers can simulate iterative generative samplers from in-context samples. We first show that transformers can realize closed-form and smoothed closed-form diffusion samplers. The construction identifies a concrete generative role for softmax attention: it computes responsibility weights and weighted empirical averages, while feedforward layers implement Euler updates.   To empirically relate these constructions to pretrained language models, we study \emph{semantic-topic sampling}: promp
    
[^23]: 全模态交互智能体技术报告

    Omni Interaction Agent Technical Report

    [https://arxiv.org/abs/2609.08977](https://arxiv.org/abs/2609.08977)

    Gander是一个端到端模型，通过小脑-大脑协作架构在单一框架内统一了全模态感知、实时全双工交互和智能体能力。

    

    arXiv:2609.08977v2 公告类型：cross 摘要：在本工作中，我们提出了Gander，一个端到端模型，它在单一框架内统一了全模态感知、实时交互和智能体能力。与传统的基于轮次的范式不同，Gander能够持续接收来自多种模态的流式输入，包括视频、语音和文本，从而在日常对话和复杂的工作流导向的智能体场景中实现自然的全双工交互。用户可以随时打断模型，同时模型也可以主动提供中间反馈或提出后续问题。为了原生支持这些能力，Gander采用了两个关键的架构设计：1）它采用了小脑-大脑协作框架，其中小脑负责实时交互和全模态对话能力，而大脑则处理复杂推理和更高层次的智能体任务，两个组件通过工具调用持续进行交互。

    arXiv:2609.08977v2 Announce Type: cross  Abstract: In this work, we present Gander, an end-to-end model that unifies omni perception, realtime interaction, and agentic capabilities within a single framework. In contrast to turn-based conventional paradigms, Gander continuously receives streaming inputs across multiple modalities, including video, speech, and text, enabling natural full-duplex interaction in both everyday conversations and complex workflow-oriented agent scenarios. Users can interrupt the model at any time, while the model can also proactively provide intermediate feedback or ask follow up questions. To natively support these capabilities, Gander adopts two key architectural designs: 1) It employs a Cerebellum-Brain collaborative framework, in which the Cerebellum is responsible for realtime interaction and omni conversational capabilities, while the Brain handles complex reasoning and higher-level agentic tasks. The two components interact continuously through tool cal
    
[^24]: GraphFAS：面向工业交易网络的自动化图特征生成与选择分布式系统

    GraphFAS: A Distributed System for Automated Graph Feature Generation and Selection in Industrial Transaction Networks

    [https://arxiv.org/abs/2609.08970](https://arxiv.org/abs/2609.08970)

    GraphFAS提出了一种基于Boruta的分布式图特征自动生成与选择系统，通过非参数化的多跳子图特征提取和跨分区中位数聚合的特征选择，在工业交易网络欺诈检测中实现了可解释、可扩展且无需深度领域知识的特征工程。

    

    工业欺诈检测通常依赖于成本高昂的专家手工设计特征，而这些特征往往忽略了图结构中的关系信号；与此同时，图神经网络（GNN）又常常无法满足金融风控对可解释性和部署的要求。我们提出了GraphFAS（图特征自动选择），这是一种基于Boruta的分布式特征选择方法，通过以下两点弥合了这一差距：（1）一个非参数化的图特征生成模块，通过多跳子图提取和多尺度聚合，无需学习参数即可构建显式、可解释的结构特征；（2）一种自动化的分布式特征选择算法，在Boruta的基础上扩展了跨分区基于中位数的聚合机制，能够在极少的领域专家知识下，大规模地稳健识别有价值的特征。与端到端的GNN流程相比，GraphFAS将特征聚合与模型训练解耦，使其能够直接与表格模型集成并直接兼容（摘要在此处截断）。

    arXiv:2609.08970v1 Announce Type: cross  Abstract: Industrial fraud detection often relies on costly expert-crafted features that overlook graph-structured relational signals, while GNNs often do not meet the interpretability and deployment requirements of financial risk control. We propose GraphFAS (Graph Feature Automated Selection), a distributed feature selection procedure based on Boruta that bridges this gap through: (1) a non-parametric graph feature generation module that constructs explicit, interpretable structural features via multi-hop subgraph extraction and multi-scale aggregation without learned parameters; and (2) an automated distributed feature selection algorithm extending Boruta with median-based aggregation across partitions to robustly identify informative features at scale with minimal domain expertise. Compared with end-to-end GNN pipelines, GraphFAS decouples feature aggregation from model training, enabling direct integration with tabular models and direct com
    
[^25]: 基受限命题公式的拟合与学习

    Fitting and Learning Basis-Restricted Propositional Formulas

    [https://arxiv.org/abs/2609.08961](https://arxiv.org/abs/2609.08961)

    本文系统确定了在任意有限布尔函数基下构建的命题公式（及电路）在拟合、Occam 学习、经验风险最小化和 PAC 学习等各类问题上的计算复杂度分类。

    

    对于一个由有限个布尔函数组成的集合 $O$，我们考虑使用 $O$ 中的函数作为连接词所构建的命题公式类。我们针对 $O$ 的每一种可能选择，确定了各种拟合问题和学习问题的计算复杂度。这些问题包括：找到一个能拟合给定标记样本的公式、找到一个小规模公式（即 Occam 算法）、当样本不可实现时最小化误分类样本的数量（即经验风险最小化），以及多种形式的 PAC 学习。我们的结果既适用于公式（表示为树的形式），也适用于电路。我们还简要讨论了相同问题在其他类型命题片段上的研究现状。

    arXiv:2609.08961v1 Announce Type: cross  Abstract: For a finite set $O$ of Boolean functions, we consider the class of propositional formulas built using the functions in $O$ as connectives. We determine, for each possible choice of $O$, the complexity of various fitting and learning problems. These include: finding a formula that fits a given labeled sample, finding a small one (an Occam algorithm), minimizing the number of misclassified examples when the sample is not realizable (empirical risk minimization), and several forms of PAC learning. Our results apply both to formulas (represented as trees) and to circuits. We also briefly discuss the status of the same questions for other kinds of propositional fragments.
    
[^26]: ONE CYLinder：面向非定常钝体流动的基于图代理建模的基准测试

    ONE CYLinder: A Benchmark for Graph-Based Surrogate Modeling of Unsteady Bluff-Body Flows

    [https://arxiv.org/abs/2609.08947](https://arxiv.org/abs/2609.08947)

    该论文提出了ONECYL基准，包含450次涵盖层流、过渡流和高雷诺数状态的高保真圆柱绕流仿真，并为基于图的代理模型建立了统一的评估框架。

    

    基于图的代理模型为加速非结构化网格上的计算流体动力学（CFD）仿真提供了一条有前景的途径。然而，其发展受到涵盖多种流动状态的基准数据集稀缺以及缺乏长时程自回归预测标准化协议的限制。我们提出了ONECYL（ONE CYLinder），一个针对圆柱绕流非定常流动的新基准，涵盖层流、过渡流和高雷诺数三种流动状态。该基准包含450次高保真变分多尺度有限元仿真（共270,000个流动快照），采用随机化的圆柱几何形状，提供时间分辨的速度场和压力场，以及网格连接信息、几何描述符、雷诺数和积分气动量。除数据集外，ONECYL还建立了一个统一的评估框架，结合了全场滚动预测误差、虚拟探针以及阻力和升力预测评估。

    arXiv:2609.08947v1 Announce Type: cross  Abstract: Graph-based surrogate models offer a promising route to accelerate computational fluid dynamics (CFD) simulations on unstructured meshes. However, their development is limited by the scarcity of benchmark datasets spanning multiple flow regimes and standardized protocols for long-horizon autoregressive prediction. We introduce ONECYL (ONE CYLinder), a new benchmark for unsteady flow past a circular cylinder across laminar, transitional, and high-Reynolds-number regimes. The benchmark comprises 450 high-fidelity Variational Multiscale finite-element simulations (270,000 flow snapshots) with randomized cylinder geometries, providing time-resolved velocity and pressure fields together with mesh connectivity, geometric descriptors, Reynolds numbers, and integrated aerodynamic quantities. Beyond the dataset, ONECYL establishes a unified evaluation framework combining full-field rollout errors, virtual probes, and drag and lift predictions t
    
[^27]: BatchNorm幻觉：诊断机器遗忘评估中的归一化伪影

    The BatchNorm Illusion: Diagnosing Normalization Artifacts in Machine Unlearning Evaluation

    [https://arxiv.org/abs/2609.08901](https://arxiv.org/abs/2609.08901)

    该论文揭示了一个评估陷阱：在基于BatchNorm的架构上，仅对保留数据做一次前向传播（不修改任何权重）就能重写模型的归一化状态并逆转表面遗忘指标，作者将其形式化为权重保持的定点算子，严格证明此类前后差异源于BN运行统计量而非遗忘方法本身，从而清晰区分了度量失效与真实的权重编码信息残留。

    

    近似机器遗忘旨在无需从头重新训练的情况下，从已训练模型中移除特定训练数据的影响。我们发现了一个此前未被记录的混淆因素，它存在于基于BatchNorm架构的遗忘评估方式中：对保留数据进行单次前向传播——这一操作不修改任何权重——却能够确定性地重写模型的归一化状态，并逆转表观上的表面指标遗忘。我们将该操作形式化为一个保持权重不变的定点算子，并证明由它引发的任何前后差异都可以严格归因于BN运行统计量，而非遗忘方法对权重所做的任何修改。这一归因论断清晰地区分了度量失效（BN伪影）与编码器失效（残留在权重中的编码信息，近期有并行工作对此进行了记录），并且同一算子框架还给出了线性探针性能提升的唯一分解。

    arXiv:2609.08901v1 Announce Type: new  Abstract: Approximate machine unlearning aims to remove the influence of specific training data from a trained model without retraining from scratch. We identify a previously undocumented confound in how unlearning is evaluated on BatchNorm-based architectures: a single forward pass over retain data, an operation that modifies no weight, can deterministically rewrite the model's normalization state and reverse the apparent surface-metric forgetting. We formalize this operation as a weight-preserving fixed-point operator and prove that any pre-versus-post gap it induces is provably attributable to BN running statistics rather than to any modification the unlearning method made to the weights. This attribution claim cleanly separates measurement failure (BN artifact) from encoder failure (residual weight-encoded information, recently documented in concurrent work), and the same operator framework yields a unique decomposition of linear-probe elevati
    
[^28]: 低温下的高磁化采样：Ising模型与贝叶斯稀疏线性回归

    High-Magnetization Sampling at Low Temperatures: Ising Models and Bayesian Sparse Linear Regression

    [https://arxiv.org/abs/2609.08873](https://arxiv.org/abs/2609.08873)

    该论文提出了在高维稀疏（高磁化）汉明切片上的采样框架，实现了任意温度下固定磁化 Sherrington–Kirkpatrick Ising 模型的多项式时间采样，并改进了贝叶斯稀疏线性回归的采样方法。

    

    稀疏性是优化和统计学中一种强大的结构资源。我们开发了在汉明切片 $\mathcal{X}_k^d:=\{\mathbf{x}\in\{\pm 1\}^d:|\{i:\mathbf{x}_i=1\}|=k\}$ 上利用稀疏性进行采样的框架，适用于 $k\ll d$ 的高维情形（即 $\mathcal{X}_k^d$ 处于高度磁化的状态）。我们利用这些框架为 Ising 模型和贝叶斯稀疏线性回归这两个经典研究方向设计了改进的采样器。我们的第一个主要结果考虑了限制在固定磁化切片 $\mathcal{X}_k^d$ 上的 Sherrington–Kirkpatrick（SK）模型。在任意逆温度 $\beta>0$ 和任意外场条件下，只要 $k\le c_\beta d$（其中 $c_\beta$ 为适当常数），我们给出了固定磁化 SK 模型的多项式时间采样器。通过将这一结果与估计归一化常数的退火策略相结合，我们获得了……（摘要在此处截断）

    arXiv:2609.08873v1 Announce Type: cross  Abstract: Sparsity is a powerful structural resource in optimization and statistics. We develop frameworks for leveraging sparsity in sampling problems over the Hamming slice $\mathcal{X}_k^d:=\{\mathbf{x}\in\{\pm 1\}^d:|\{i:\mathbf{x}_i=1\}|=k\}$, in high-dimensional regimes where $k\ll d$ (i.e., where $\mathcal{X}_k^d$ is \emph{highly magnetized}). We use our frameworks to design improved samplers for canonical problems in the study of \emph{Ising models} and \emph{Bayesian sparse linear regression}.   Our first main result considers the \emph{Sherrington--Kirkpatrick} (SK) model restricted to fixed-magnetization slices $\mathcal{X}_k^d$. We give a polynomial-time sampler for fixed-magnetization SK models at any inverse temperature $\beta>0$, under arbitrary external fields, provided that $k\le c_\beta d$ for an appropriate constant $c_\beta$. By combining this result with an annealing strategy for estimating normalizing constants, we obtain p
    
[^29]: 用于“假设性”模拟的地球系统世界模型：以陆地生态系统为案例研究

    Earth System World Model for What-If Simulations: A Case Study for Terrestrial Ecosystems

    [https://arxiv.org/abs/2609.08855](https://arxiv.org/abs/2609.08855)

    该论文提出一种动作条件的世界模型框架，通过“转移-动作预训练”将自然观测的状态变化作为无标签动作监督，使地球系统仿真器无需人工标注干预即可支持交互式的“假设性”模拟，并以陆地生态系统为案例进行验证。

    

    机器学习仿真器已成为加速昂贵地球系统模拟的重要工具，但现有的大多数方法仍然是被动的预测器：它们仅在规定的强迫条件下重现模拟器的轨迹，缺乏针对用户指定干预的显式交互机制。这限制了它们在交互式科学工作流程和地球系统数字孪生中的应用，因为在这些场景中，用户通常需要探索如果改变选定的状态分量，系统将如何响应。我们提出了一种面向地球系统仿真的动作条件世界模型框架，将模拟器轨迹重新表述为可控状态转移学习的监督信号。其核心思想是转移-动作预训练：将自然观测到的状态变化视为无标签的动作监督，使模型无需人工标注的干预即可同时学习规定的动力学以及动作条件下的响应。我们进一步引入……

    arXiv:2609.08855v1 Announce Type: cross  Abstract: Machine learning emulators have become essential for accelerating expensive Earth-system simulations, but most existing approaches remain passive forecasters: they reproduce simulator trajectories under prescribed forcings without an explicit interaction mechanism for user-specified interventions. This limits their use in interactive scientific workflows and Earth-system digital twins, where users often need to explore how a system would respond if selected state components were changed. We propose an action-conditioned world-modeling framework for Earth-system emulation that reformulates simulator trajectories as supervision for controllable state-transition learning. The key idea is transition-action pretraining: naturally observed state changes are treated as label-free action supervision, allowing the model to learn both prescribed dynamics and action-conditioned responses without manually annotated interventions. We further introd
    
[^30]: 通过压缩实现Transformer的长度泛化

    Length Generalization for Transformers via Compression

    [https://arxiv.org/abs/2609.08851](https://arxiv.org/abs/2609.08851)

    本文针对C-RASP假设缺乏可计算长度泛化界及实验矛盾的问题，利用具有可计算长度泛化界的新片段C-RASP+和C-RASP1对其进行了改进。

    

    arXiv:2609.08851v1 公告类型：新论文 摘要：近年来Transformer长度泛化理论的进展使我们能够可靠地预测Transformer何时能够学会解决某项任务。特别地，C-RASP假设（即所谓RASP-l猜想的形式化版本）提出，当且仅当任务的解可以用C-RASP语言表达时，Transformer才能在该任务上实现长度泛化。尽管该假设有很强的实证验证，但理论上仍存在问题：C-RASP不存在可计算的长度泛化界，同时还发现了看似相互矛盾的实验结果。为解决这些问题，我们利用最近提出的片段C-RASP+和C-RASP1对C-RASP假设进行了改进。这些片段具有可计算的长度泛化界，尽管在最坏情况下需要极其庞大的（双指数级）样本量。这些样本量界是否紧致仍是一个开放性问题。在本文中，我们解决了这一开……（摘要原文在此处截断）

    arXiv:2609.08851v1 Announce Type: new  Abstract: Recent advancements in transformer length generalization theory enable us to reliably predict when a transformer can learn to solve a task. In particular, the C-RASP hypothesis (a formalized version of the so-called RASP-l conjecture) posits that transformers length-generalize on a task if and only if a solution is expressible in the C-RASP language. While this hypothesis has strong empirical validation, theoretical problems arise from the fact that no computable length generalization bounds exist for C-RASP, alongside the discovery of seemingly contradictory experiments. To address these problems, we refine the C-RASP hypothesis utilizing the recently-proposed fragments C-RASP+ and C-RASP1. These fragments have computable length generalization bounds, though in the worst case requiring an extremely large (double exponential) sample size. It is an open question whether these sample size bounds are tight. In this paper, we resolve this op
    
[^31]: 单共同因子模型下锚点-评审误差相关的闭式估计器与诊断套件

    A Closed-Form Estimator and Diagnostic Battery for Anchor-Judge Error Correlation, Under a Single-Common-Factor Model

    [https://arxiv.org/abs/2609.08826](https://arxiv.org/abs/2609.08826)

    该论文提出在单共同因子模型下，仅凭至少两个评审者和两个锚点即可用闭式解同时识别质量方差、共模方差及各锚点的污染相关系数，并配套经过校准的诊断套件来应对模型假设不可检验的问题，从而摆脱传统方法对锚点无污染的强假设依赖。

    

    当使用外部参考集（锚点）将LLM评审组的误差分解为质量信号与共享的共模误差时，标准做法假设锚点是无污染的：即其误差与评审者的共享误差不相关。我们研究了何时可以放弃这一假设并用估计值加以替代。在单共同因子模型下，只需≥2个评审者和≥2个锚点，即可通过闭式解点识别质量方差、共模方差以及每个锚点的污染相关系数ρ_k，并给出精确的逐锚点对失效边界；相比之下，指定“干净锚点”的估计器一旦其信任的锚点本身已被污染，便会将被污染的伴生锚点错误地报告为完全干净。由于单共同因子假设本身无法检验，该估计器配套了一整套经过校准的诊断工具（评审者协方差离散度检验；过度识别检验；基于评审者元数据的族块检验等），并仅在诊断通过时才启用。

    arXiv:2609.08826v1 Announce Type: cross  Abstract: When an external reference set (an anchor) is used to decompose an LLM-judge panel's error into a quality signal and a shared common-mode error, standard practice assumes the anchor is uncontaminated: its error uncorrelated with the judges' shared error. We study when that assumption can be dropped and replaced by an estimate. Under a single-common-factor model, >=2 judges and >=2 anchors point-identify the quality variance, the common-mode variance, and each anchor's contamination correlation rho_k in closed form, with an exact per-anchor-pair failure boundary; a designated clean-anchor estimator, by contrast, reports a contaminated companion anchor as fully clean once its trusted anchor is itself contaminated. Because the single-common-factor assumption is itself untestable, the estimator ships gated behind a calibrated diagnostic battery (judge-covariance dispersion; over-identification; a family-block test from judge metadata, with
    
[^32]: Ostrich：在可微动力学中大步跨越刚性接触

    Ostrich: Taking Large Strides Through Stiff Contact in Differentiable Dynamics

    [https://arxiv.org/abs/2609.08800](https://arxiv.org/abs/2609.08800)

    Ostrich是一个GPU加速的可微刚体仿真器，通过非光滑牛顿迭代在大时间步长下精确求解硬接触与摩擦，并利用隐函数定理以每步O(1)内存高效计算可靠梯度，同时兼顾仿真精度、梯度可靠性与计算成本。

    

    三个特性决定了一个可微仿真器能否驱动基于梯度的优化穿越接触问题：仿真精度、梯度可靠性和单次迭代成本。基于磁带记录的引擎（如MJX和Newton Semi-Implicit）需要足够小的时间步长才能保持接触问题的数值可处理性，且其反向传播内存随时间步数T线性增长。代理模型通过将接触近似消除来限制内存占用，但由此产生的梯度丢失了优化所依赖的几何结构。我们提出了Ostrich，一个GPU加速的刚体仿真器，它在大时间步长（h ~ 0.1 s）下采用非光滑牛顿迭代法求解硬接触和摩擦问题，并通过隐函数定理对收敛后的残差进行微分，复用前向Schur补来计算伴随变量，使每个时间步仅需O(1)的内存。在跨越托盘障碍物的真实机器人轨迹上，Ostrich保持了与MuJoCo相当的仿真到现实精度……

    arXiv:2609.08800v1 Announce Type: cross  Abstract: Three properties determine whether a differentiable simulator can drive gradient-based optimization through contact: simulation accuracy, gradient reliability, and per-iteration cost. Tape-based engines such as MJX and Newton Semi-Implicit require timesteps small enough to keep contacts numerically tractable, and their backpropagation memory grows linearly with the number of timesteps T. Surrogate models bound memory by approximating contact away, but the resulting gradients lose the geometry the optimization depends on. We present Ostrich, a GPU-accelerated rigid-body simulator that resolves hard contacts and friction with non-smooth Newton iteration at large timesteps (h ~ 0.1 s), and differentiates the converged residual via the implicit function theorem, reusing the forward Schur complement to compute the adjoint at O(1) memory per timestep. On real-robot trajectories over a pallet obstacle, Ostrich holds MuJoCo's sim-to-real accur
    
[^33]: 通过在线策略反向蒸馏引出弱到强泛化

    Eliciting Weak-to-Strong Generalization with On-Policy Reverse Distillation

    [https://arxiv.org/abs/2609.08798](https://arxiv.org/abs/2609.08798)

    提出在线策略反向蒸馏（OPRD）方法，通过在学生模型滚动输出上评估教师策略偏移并放大学生验证器驱动的策略梯度分量，在保留优化驻点的同时加速学习，使学生模型超越弱教师模型，实现弱到强泛化。

    

    弱到强泛化探讨的是更强的模型能否从较弱的监督者那里学习并超越它们。这个问题对于模型的连续代际传承和多领域整合尤为重要，因为在这些场景下，从头重复前沿规模的后训练可能代价过高。然而，传统的蒸馏方法将弱教师模型视为优化目标，可能会将其能力上限强加给学生模型。我们提出了在线策略反向蒸馏（OPRD），该方法在学生模型的滚动输出上评估教师策略相对于其参考策略的偏移，并放大学生模型由验证器驱动的策略梯度沿该方向的分量。通过仅重新缩放由验证器支持的更新，OPRD在保持策略优化驻点的同时，加速学习以超越教师模型。在连续模型迁移和多教师蒸馏中，OPRD以更...

    arXiv:2609.08798v1 Announce Type: new  Abstract: Weak-to-strong generalization asks whether stronger models can learn from weaker supervisors and surpass them. This question is particularly important for successive model generations and multi-domain consolidation, where repeating frontier-scale post-training from scratch can be prohibitively expensive. Yet conventional distillation treats the weak teacher as an optimization target, potentially imposing its capacity ceiling on the student. We introduce On-Policy Reverse Distillation (OPRD), which evaluates the teacher's policy shift relative to its reference policy on student rollouts and amplifies the component of the student's verifier-driven policy gradient along that direction. By rescaling only verifier-supported updates, OPRD preserves the stationary points of policy optimization while accelerating learning beyond the teacher. In both successive model transfer and multi-teacher distillation, OPRD achieves higher performance with f
    
[^34]: 面向轴结构信号的自适应各向异性注意力

    Adaptive Anisotropic Attention for Axis-Structured Signals

    [https://arxiv.org/abs/2609.08788](https://arxiv.org/abs/2609.08788)

    提出自适应各向异性注意力（AAA），将注意力沿电极时间轴分解为时间路径与空间路径并通过门控自适应加权融合，所构建的AXON模型在六个EEG下游任务上优于稠密注意力基线。

    

    稠密自注意力在学习之前将所有token对视为同等可信的，这种交互各向同性的先验可能与结构化信号不匹配。对于诸如脑电（EEG）这类结构化、低信噪比（SNR）的信号，其依赖关系是沿电极轴和时间轴组织的，而这种均匀先验使每个token暴露于许多无关的交互之中。我们提出了自适应各向异性注意力（AAA），它将注意力拆分为两条路径：时间路径，其中每个token关注其自身电极在时间维度上的token；空间路径，其中它关注同一时间步上其他电极的token。一个小型门控网络为每个token预测两条路径输出的凸组合：即两个和为一的非负权重。在六个EEG下游任务上，由此构建的模型AXON（轴分解算子网络，AXis-factorized Operator Network）在线性探测和完整微调设置下，其平均平衡准确率均优于稠密基线……

    arXiv:2609.08788v1 Announce Type: cross  Abstract: Dense self-attention treats all token pairs as equally plausible before learning, an interaction-isotropic prior that can be mismatched to structured signals. For structured, low signal-to-noise ratio (SNR) signals such as EEG, dependencies are organized along the electrode and time axes, and this uniform prior exposes each token to many irrelevant interactions. We introduce Adaptive Anisotropic Attention (AAA), which splits attention into two paths: a temporal path, where each token attends to the tokens of its own electrode across time, and a spatial path, where it attends to the tokens of the other electrodes at the same time step. A small gate predicts, for every token, a convex combination of the two path outputs: two non-negative weights that sum to one. On six EEG downstream tasks, the resulting model, AXON (AXis-factorized Operator Network), improves mean balanced accuracy over a dense baseline under both linear probing and ful
    
[^35]: ZK-Trace：基于零知识凭证的联邦式GNSS干扰监测认证合谋追踪

    ZK-Trace: Certified Collusion Tracing with Zero-Knowledge Credentials for Federated GNSS Interference Monitoring

    [https://arxiv.org/abs/2609.08763](https://arxiv.org/abs/2609.08763)

    ZK-Trace 提出了一种融合公开身份标记、Tardos 指纹与零知识凭证验证的认证合谋追踪方案，能够在联邦式GNSS干扰监测中实现无需泄露者配合的离线追踪，并将每次调查的错误指控率控制在 0.001 以内。

    

    联邦式全球导航卫星系统（GNSS）监测将专有分类器分发给部分可信的站点，其中任何站点都可能泄露其副本。ZK-Trace 结合了公开身份标记、针对接收方的 Tardos 指纹以及零知识凭证验证。该注册表支持无需泄露者配合的离线追踪。我们为任意恢复的比特模式建立了条件性错误指控边界，在隐藏偏置残余信道下建立了有限完整性边界，并为相关的特征蒸馏错误建立了确定性追踪评分边界。区间算术检查器使条件边界可执行，并在指控与篡改决策之间分配共同预算。在无辜行独立性条件下，基于证书的评估在每次调查中使用 0.001 的错误指认预算。该方法隔离了全部 160 个单一所有者副本，并在 720 个双所有者混合样本中成功追踪 712 个，且未发生错误指认。

    arXiv:2609.08763v1 Announce Type: cross  Abstract: Federated global navigation satellite system (GNSS) monitoring distributes a proprietary classifier to partly trusted stations, any of which may leak its copy. ZK-Trace combines public identity marks, recipient-specific Tardos fingerprints, and zero-knowledge credential verification. The registry supports offline tracing without the leaker's cooperation. We establish conditional false-accusation bounds for arbitrary recovered bit patterns, a finite completeness bound under a hidden-bias residual channel, and a deterministic tracing-score bound for correlated feature-distillation errors. An interval-arithmetic checker makes the conditional bound executable and allocates a common budget across accusation and tamper decisions. Under innocent-row independence, the certificate-based evaluation uses a false-naming budget of 0.001 per investigation. It isolates all 160 single-owner copies and traces 712 of 720 two-owner mixtures without namin
    
[^36]: 关于随机旋转量化中缩放因子的注记及其与CDEF +1勾股关系的联系

    A Note on Scaling in Randomly Rotated Quantization and Its Connection to the CDEF +1 Pythagorean Relation

    [https://arxiv.org/abs/2609.08759](https://arxiv.org/abs/2609.08759)

    本文揭示了随机旋转量化中EDEN系列工作的两个重构缩放因子与经典CDEF公式中维纳系数和无偏系数的对应关系，证明CDEF +1勾股关系在有限块长下对每个旋转实现作为精确几何恒等式逐点成立，而经典信噪比关系在维度趋于无穷时恢复。

    

    基于随机旋转的量化方案近来重新受到关注，其中包括最小均方误差（MMSE）和无偏重构缩放因子的作用。在本注记中，我们指出了其与统计信号处理和通信理论中经典结果的联系。具体而言，EDEN系列工作中使用的两个重构缩放因子可以被自然地解释为经典CDEF公式中维纳系数和无偏系数在有限维度、依赖具体旋转实现情况下的对应物。在有限块长度下，CDEF +1关系对每一个旋转实现均逐点成立，构成一个精确的几何（勾股/毕达哥拉斯）恒等式，但在对旋转取失真平均后该关系不再成立。经典的信噪比关系 SNR_MMSE = SNR_MMSE,U + 1 在维度 d→∞ 时得以恢复：一旦总体缩放因子被单独处理，随机旋转向量的经验坐标统计特性趋近于……（原文摘要在此处截断）

    arXiv:2609.08759v1 Announce Type: cross  Abstract: Quantization schemes based on randomized rotations have recently received renewed attention, including the roles of MMSE and unbiased reconstruction scalings. In this note, we point out the connection to classical results in statistical signal processing and communication theory. Specifically, the two reconstruction scales used in the EDEN line of work admit a natural interpretation as finite-dimensional, realization-dependent counterparts of the Wiener and unbiased coefficients in the classical CDEF formulation. At finite blocklength, the CDEF +1 relation holds pointwise for each rotation realization as an exact geometric (Pythagorean) identity, but does not hold after averaging the distortions over the rotation. The classical SNR relation $\sf{SNR}_{\rm MMSE}=\sf{SNR}_{\rm MMSE,U}+1$ is recovered as $d\to\infty$: once the overall scale is handled separately, the empirical coordinate statistics of a randomly rotated vector approach th
    
[^37]: 具有输入和亚高斯噪声的部分可观测随机线性时不变状态空间系统学习的PAC-贝叶斯界

    PAC-Bayesian Bounds for Learning Partially Observed Stochastic Linear Time-Invariant State-Space Systems with Inputs and Sub-Gaussian Noise

    [https://arxiv.org/abs/2609.08740](https://arxiv.org/abs/2609.08740)

    本文为带有输入和亚高斯噪声的部分可观测随机线性时不变状态空间系统推导了PAC-贝叶斯误差界，从而可为一大类系统辨识算法提供预测误差和参数估计误差的有限样本保证。

    

    本文针对具有输入和亚高斯噪声、以状态空间形式表示的部分可观测线性时不变（LTI）随机动力学系统，推导了可能近似正确（PAC）-贝叶斯误差界。这类界在机器学习中广泛存在，对于刻画从有限数据点学习到的模型的预测能力十分有用。本文推导的界将预测误差的期望与模型在用于学习的数据上产生的预测误差联系起来。此外，我们证明该界还可用于推导参数估计误差的界。这进而使我们能够为一大类系统辨识算法的预测误差和参数估计误差提供有限样本误差界。此外，由于LTI系统是循环神经网络（RNN）的一个子类，这些误差界可能是迈向PAC-贝叶斯界的第一步。

    arXiv:2609.08740v1 Announce Type: new  Abstract: In this paper we derive a Probably Approximately Correct (PAC)-Bayesian error bound for partially observed linear time-invariant (LTI) stochastic dynamical systems in state-space form with inputs and sub-Gaussian noise. Such bounds are widespread in machine learning, and they are useful for characterizing the predictive power of models learned from finitely many data points. The bound derived in this paper relates the expectation of prediction errors with the prediction error generated by the model on the data used for learning. In addition, we show that it can also be used to derive bounds for the parameter estimation error. In turn, this allows us to provide finite-sample error bounds for the prediction error and parameter estimation error for a wide class of system identification algorithms. Furthermore, as LTI systems are a sub-class of recurrent neural networks (RNNs), these error bounds could be a first step towards PAC-Bayesian bo
    
[^38]: BAFF：用于缓解RTB A/B测试中训练数据干扰的竞价感知过滤族

    BAFF: Bid-Aware Filter Family for Mitigating Training Data Interference in RTB A/B Tests

    [https://arxiv.org/abs/2609.08725](https://arxiv.org/abs/2609.08725)

    提出竞价感知过滤族（BAFF），通过可参数化的硬过滤器在共享日志与拆分日志两种极端方案之间灵活平衡训练数据保留与偏差控制，从而缓解RTB广告A/B测试中由广告排序与竞价定价分歧导致的训练数据干扰。

    

    在实时竞价（RTB）的在线A/B测试中，对照组与处理组模型通常在一个共享的服务日志上训练，而该日志包含由对方模型产生的数据。这种共享日志训练通过两个渠道对每个模型的训练数据引入偏差：对方模型可能从广告候选池中选择了不同的广告（广告排序分歧），并可能报出不同的价格（竞价定价分歧），从而可能扭曲A/B测试的结果。日志拆分可以消除偏差，但会牺牲训练数据；日志共享保留了全部数据，但偏差问题依然存在。我们形式化地提出了竞价感知过滤族（BAFF），这是一类由(k,l)参数化的硬过滤器，能够独立控制对每个分歧渠道的容忍度，在这两种极端方案之间提供了结构化的搜索空间。我们进一步提出了一个三阶段在线测量协议，使数据共享策略能够通过与理想无偏参考之间的偏差来加以评估。

    arXiv:2609.08725v1 Announce Type: new  Abstract: In online A/B tests for real-time bidding (RTB), control and treatment models are typically trained on a shared serving log that includes data generated by the counterpart model. This shared-log training biases each model's training data through two channels: the counterpart model may have selected a different ad from the ad-candidate pool (ad-ranking disagreement) and may have bid a different price (bid-pricing disagreement), potentially distorting the A/B test outcome. Log-splitting eliminates the bias but sacrifices training data; log-sharing retains all data but leaves the bias unaddressed. We formalize the Bid-Aware Filter Family (BAFF), a class of (k,l)-parameterized hard filters that controls tolerance to each channel independently, providing a structured search space between these two extremes. We further propose a three-stage online measurement protocol that enables evaluating data-sharing strategies by their deviation from an i
    
[^39]: Chimaera：一种用于跨任务和跨数据集图学习的图专家混合架构

    Chimaera: A Mixture-of-Graph-Experts Architecture for Cross-Task and Cross-Dataset Graph Learning

    [https://arxiv.org/abs/2609.08709](https://arxiv.org/abs/2609.08709)

    Chimaera 提出了一种将专家混合机制与图基础模型相结合的新架构，通过整合多种 GFM 架构和大语言模型生成的嵌入，实现了跨任务和跨数据集的图学习能力。

    

    为图设计基础模型具有挑战性，原因在于图结构的不规则性以及嵌入的尺寸和特征各不相同。Chimaera 将专家混合机制与图基础模型（GFM）相结合，整合了不同的 GFM 架构，例如图提示和线性 GNN 模型。该方法利用大语言模型生成嵌入，专家可以按照不同的策略进行训练和组合，包括不同的 GFM、嵌入等。此外，Chimaera 扩展了现有的线性 GNN，使其除了支持节点级任务外，还支持链接级和图级任务。研究使用六个基准文本属性图数据集，在节点、链接和图分类任务上进行了同任务和跨任务的实证分析。实验证明了 Chimaera 的有效性及其跨任务和跨数据集的迁移能力。进一步的见解还包括需要同时使用大型和小型语言模型。

    arXiv:2609.08709v1 Announce Type: new  Abstract: Designing foundation models for graphs is challenging due to the irregular structure of graphs and the different sizes and characteristics of embeddings. Chimaera integrates mixture-of-experts with graph foundation models (GFM). It integrates different GFM architectures, such as graph prompts and linear GNN models. Large language models are used to generate embeddings, and experts can be trained and combined following different strategies, GFMs, embeddings, etc. Furthermore, Chimaera extends existing linear GNNs to support link-level and graph-level tasks in addition to node-level tasks. Empirical analyses are performed on same-task and cross-task experiments with node, link, and graph classification tasks using six benchmark text-attributed graph datasets. The experiments demonstrate the effectiveness of Chimaera and its capabilities for transfer across tasks and datasets. Further insights include the need to use both large and small la
    
[^40]: TontaubeV1：基于分层编解码器建模与有界上下文的流式文本转语音

    TontaubeV1: Streaming Text-to-Speech with Hierarchical Codec Modeling and Bounded Context

    [https://arxiv.org/abs/2609.08703](https://arxiv.org/abs/2609.08703)

    TontaubeV1通过分层编解码器建模——用较大模型预测语义流以确定韵律和时长、用较小模型逐步细化声学特征——结合有界上下文设计，在单块消费级GPU上实现了兼具自然韵律的流式文本转语音。

    

    文本转语音系统常常面临自然韵律与高效推理之间的权衡：更高的感知质量通常伴随着更高的计算成本和延迟。我们提出了TontaubeV1，该模型在保持自然韵律的同时，能够在一块消费级GPU上实现流式推理。语音通过分层的DualCodec表示以12.5 Hz进行编码，该表示将语义流与后续的声学细化分离开来。我们的设计假设韵律结构在语义流生成时已基本确立，并据此分配模型容量：一个基于Qwen3-1.7B的transformer预测语义流并由此确定话语时长，而三个规模递减的基于Qwen3-0.6B的transformer各自增加一层声学细化。文本按字符而非子词进行分词。位于相同位置的成对文本和音频标记支持有界上下文下的长文本生成，并采用重叠的方式（原文摘要在此处被截断）。

    arXiv:2609.08703v1 Announce Type: cross  Abstract: Text-to-speech systems often face a trade-off between natural prosody and efficient inference: higher perceptual quality typically comes at increased computational cost and latency. We present TontaubeV1, a model that preserves natural prosody while enabling streaming from a single consumer GPU. Speech is encoded by the hierarchical DualCodec representation at 12.5 Hz, which separates a semantic stream from successive acoustic refinements. Our design assumes that prosodic structure is largely established when the semantic stream is generated, and allocates capacity accordingly: a Qwen3-1.7B-derived transformer predicts that stream and thereby the utterance duration, while three progressively smaller Qwen3-0.6B-derived transformers each add one acoustic refinement. Text is tokenized per character rather than by subword. Paired text and audio markers at shared positions support long-form generation with bounded context, and overlapping D
    
[^41]: 跨MoE稀疏度的超参数缩放定律

    Hyperparameter Scaling Laws Across MoE Sparsity

    [https://arxiv.org/abs/2609.08690](https://arxiv.org/abs/2609.08690)

    本研究通过1,800次预训练实验（约20万亿token）发现MoE模型的最优学习率和批量大小随激活比率而非参数量变化，并揭示了两种缩放机制，从而调和了以往研究中相互矛盾的发现。

    

    混合专家模型能够在训练计算量不成比例增加的情况下扩展模型容量，但随着稀疏度的提高，可靠地迁移超参数变得具有挑战性。在本工作中，我们证明传统的超参数缩放定律对于超稀疏MoE模型是不够的：最优学习率和批量大小会随激活比率而变化，且这些变化无法仅用总参数量或激活参数量来解释。为了刻画这种依赖关系，我们进行了1,800次预训练实验，涵盖六个激活参数规模、总非嵌入参数量高达6B的模型，处理了约20万亿个token，成本相当于200,000个H800 GPU小时。我们的结果通过揭示两种缩放机制，调和了先前工作中相互矛盾的发现。在固定稀疏度下，最优批量大小与训练token数 $D$ 呈幂律关系，而最优学习率随训练…

    arXiv:2609.08690v1 Announce Type: cross  Abstract: Mixture-of-Experts (MoE) models expand model capacity without a proportional increase in training compute, but increasing sparsity makes reliable hyperparameter transfer challenging. In this work, we show that conventional hyperparameter scaling laws are insufficient for ultra-sparse MoEs: the optimal learning rate and batch size vary with activation ratio, and these shifts cannot be explained by either total or activated parameter count alone. To characterize this dependence, we conduct 1,800 pre-training runs spanning six activated-parameter scales and models with up to 6B total non-embedding parameters, processing approximately 20 trillion tokens at a cost of 200,000 equivalent H800 GPU-hours. Our results reconcile conflicting findings in prior work by revealing two scaling regimes. At fixed sparsity, the optimal batch size follows a power-law relationship with training tokens $D$, whereas the optimal learning rate scales with train
    
[^42]: HOPE：基于伪外推的异配感知开放集节点分类

    HOPE: Heterophily-Aware Open-Set Node Classification with Pseudo-Extrapolation

    [https://arxiv.org/abs/2609.08685](https://arxiv.org/abs/2609.08685)

    HOPE通过结构增强的特征初始化层捕获多跳结构模式，并利用伪外推技术，解决了异配图开放集节点分类中跨类连接导致表示纠缠、结构混合导致未知类拒识不可靠的双重挑战。

    

    标准的开放集节点分类方法依赖于同配性假设，即相连的节点共享相同的标签。然而，现实世界的图往往是异配的，这暴露了现有方法的局限性，并为开放集节点分类带来了新的挑战。一方面，跨类连接导致来自不同已知类或未知类的表示在聚合后相互纠缠，削弱了其判别能力。另一方面，结构混合使基于阈值的开放集方法和跨类特征插值失效，导致对未知类的拒识不可靠。为了应对这些挑战，我们提出了HOPE，一种具有伪外推能力的异配感知开放集节点分类方法。为了使开放集图神经网络（GNN）适应异配场景，HOPE使用结构增强的特征初始化层来捕获多跳结构模式。同时，我们设……（摘要内容不完整）

    arXiv:2609.08685v1 Announce Type: new  Abstract: Standard open-set node classification methods rely on the homophily assumption, where connected nodes share labels. However, real-world graphs are often heterophilic, exposing the limitations of current methods and posing new challenges to open-set node classification. On the one hand, cross-class connectivity causes representations from different known or unknown classes to become intertwined after aggregation, undermining their discriminative capacity. On the other hand, structural mixture invalidates threshold-based open-set methods and cross-class feature interpolation, leading to unreliable unknown-class rejection. To address these challenges, we propose HOPE, a Heterophily-aware Open-set node classification method with Pseudo-Extrapolation. To adapt open-set graph neural networks (GNNs) to heterophilic scenarios, HOPE uses a structure-augmented feature initialization layer to capture multi-hop structural patterns. Meanwhile, we des
    
[^43]: 既非对抗训练亦非净化：来自振荡预测学习的涌现对抗鲁棒性

    Neither Adversarial Training Nor Purification: Emergent Adversarial Robustness from Oscillatory Predictive Learning

    [https://arxiv.org/abs/2609.08683](https://arxiv.org/abs/2609.08683)

    该论文提出振荡预测学习（OPL）框架，通过结合人工Kuramoto振荡神经元与预测性自监督预训练，使对抗鲁棒性从架构归纳偏置中自然涌现，从而无需昂贵的对抗训练或测试时净化即可在CIFAR-10/100上取得有竞争力的鲁棒性。

    

    计算机视觉中的对抗鲁棒性目前仍主要通过对抗训练或测试时对抗净化来实现，这两种方法都会引入显著的计算开销——前者需要在训练过程中生成对抗样本，后者需要在测试时执行迭代去噪。我们研究经验鲁棒性是否可以转而从架构和表示学习的归纳偏置中自然涌现。我们提出了振荡预测学习（OPL），这是一个两阶段框架，将人工Kuramoto振荡神经元（AKOrN）与使用X-PhiNet的预测性自监督预训练相结合。由于我们的默认检查点使用随机化的初始振荡器状态，我们将其与其他提供精确、可复现且强攻击协议的随机化对抗防御方法进行了比较。在CIFAR-10和CIFAR-100上的实验，以及在CIFAR-10-C上的额外损坏评估表明，我们的方法实现了具有竞争力的（鲁棒性表现）。

    arXiv:2609.08683v1 Announce Type: cross  Abstract: Adversarial robustness in computer vision is still largely achieved through adversarial training or test-time adversarial purification, both of which introduce significant computational overhead by generating adversarial examples during training or performing iterative denoising at test time. We study whether empirical robustness can instead emerge from architectural and representation-learning inductive biases. We introduce Oscillatory Predictive Learning (OPL), a two-stage framework that combines Artificial Kuramoto Oscillatory Neurons (AKOrN) with predictive self-supervised pretraining using X-PhiNet. Because our default checkpoint uses randomized initial oscillator states, we compare it with other randomized adversarial defense methods that provide precise, reproducible, and strong attack protocols. Experiments on CIFAR-10 and CIFAR-100, with additional corruption evaluation on CIFAR-10-C, demonstrate that our method achieves compe
    
[^44]: 含噪离散数据下函数线性回归的最优估计

    Optimal estimation for Functional Linear Regression with Noisy Discretized Data

    [https://arxiv.org/abs/2609.08671](https://arxiv.org/abs/2609.08671)

    提出一种结合傅里叶投影曲线重建与惩罚最小二乘斜率估计的两步方法，在含噪离散采样下实现了函数线性回归预测误差的极小化极大最优速率。

    

    本文研究了标量对函数的线性回归模型，采用一种现实的采样方案，即函数协变量在规则网格上被观测并受到加性噪声的污染。我们提出了一种两步估计方法：第一步，使用基于傅里叶的投影方法从离散含噪观测中重建潜在曲线；第二步，在有限维三角函数空间上通过惩罚最小二乘准则估计斜率函数，并进行数据驱动的模型维度选择。我们针对预测误差建立了oracle型不等式，既针对重建后的曲线，也针对真实的潜在曲线。在斜率函数的正则性假设和协变量特征值多项式衰减的条件下，我们导出了预测误差的收敛速度，并证明当网格点数量足够多时，我们的估计量达到了极小化极大最优速率。

    arXiv:2609.08671v1 Announce Type: cross  Abstract: In this paper, we consider the scalar-on-function linear regression model under a realistic sampling scheme in which the functional covariates are observed on a regular grid and contaminated by additive noise. We propose a two-step estimation procedure: first, the underlying curves are reconstructed from the discrete noisy observations using a Fourier-based projection method; second, the slope function is estimated by a penalized least-squares criterion over finite-dimensional trigonometric spaces, with data-driven selection of the model dimension. We establish oracle-type inequalities for the prediction error, both with respect to the reconstructed curves and to the true latent curves. Under regularity assumptions on the slope function and polynomial decay of the eigenvalues of the covariate, we derive convergence rates for the prediction error and show that our estimator attains the minimax rate when the number of grid points is suff
    
[^45]: 学习通过连续调整构建覆盖结构

    Learning to build covering structures with continuous adjustments

    [https://arxiv.org/abs/2609.08669](https://arxiv.org/abs/2609.08669)

    提出了一种名为HSAC的强化学习方法，无需预定义计划，通过引入单向边的图神经网络和混合离散-连续动作空间，在建造过程中自适应地生成建造序列，以应对物理制造中的误差与不确定性。

    

    机器人建造具有更高效利用材料和创造复杂几何形状的潜力，但当前方法依赖于刚性的、高精度的计划，无法适应物理制造过程中固有的公差、误差和意外变化。在这项工作中，我们提出了一种强化学习方法，完全摒弃预定义的计划，而是在结构建造过程中自适应地生成建造序列。我们的方法基于图结构的状态表示和混合（参数化）动作空间运行，需要同时进行离散的积木选择和连续的放置参数调整。由于结构的稳定性模拟计算开销很大，我们通过在图神经网络中引入单向边来开发一种高效的探索策略，将软演员-评论家算法（SAC）扩展到这种混合设定中。我们针对先前的方法混合PPO（HPPO）对算法HSAC进行了评估。

    arXiv:2609.08669v1 Announce Type: cross  Abstract: Robotic construction offers the potential to use materials more efficiently and create complex geometries, but current methods rely on rigid, high-precision plans that cannot accommodate the tolerances, inaccuracies, and unexpected changes inherent in physical fabrication. In this work, we introduce a reinforcement learning approach that forgoes predefined plans entirely, instead generating construction sequences adaptively as the structure is built. Our method operates on graph-structured state representations and a mixed (parameterized) action space, requiring both discrete block selection and continuous placement parameters. Because the stability simulation of a structure is computationally heavy, we develop an efficient exploration strategy by incorporating unilateral edges into graph neural networks, extending soft actor-critic (SAC) to this hybrid setting. We evaluate our algorithm, HSAC, against the prior method hybrid-PPO (HPPO
    
[^46]: MoEMB：利用高效混合专家模型扩展通用多模态嵌入

    MoEMB: Scaling Universal Multimodal Embeddings with Efficient Mixture-of-Experts Models

    [https://arxiv.org/abs/2609.08663](https://arxiv.org/abs/2609.08663)

    MoEMB 提出利用混合专家沿专家维度扩展通用多模态嵌入模型，在保持单向量、非自回归编码与低延迟推理的同时提升编码器容量，并避免对简单任务产生冗余计算。

    

    通用多模态嵌入（UME）日益要求编码器具备处理更广泛任务和模态的能力，且这些任务和模态的复杂度不断提升。先前的扩展方法要么增加表示尺寸、检索开销，要么将编码器扩展为笨重的多模态大语言模型（LLM）。近期工作（如 Think-Then-Embed，TTE）探索了通过推理标记进行扩展的路径。然而，嵌入模型难以扩展：增加参数会与对比学习所需的大训练批量直接产生权衡，且检索服务必须在严格的延迟约束下完成。此外，UME 任务在复杂度上差异很大，盲目扩大嵌入模型规模会带来显著的冗余计算。在本工作中，我们提出 MoEMB，通过混合专家沿专家轴扩展 UME，在保持单向量、非自回归编码的同时增长编码器容量。通过对设计空间的系统性研究……

    arXiv:2609.08663v1 Announce Type: cross  Abstract: Universal multimodal embedding (UME) increasingly demands encoder's capacity for handling a broad range of tasks and modalities with increased complexity. Prior scaling methods either increase the representation size, retrieval effort, or scales the encoder into a heavy multimodal LLM. Recent works, such as Think-Then-Embed (TTE), explore scaling via reasoning tokens. However, embedding models are hard to scale up: increasing parameters directly tradeoffs for the large training batch size that contrastive learning needs, and retrieval has to be served under tight latency. Moreover, UME tasks are diverse in complexity, where scaling up embedders can bring significant redundant computation. In this work, we propose MOEMB, which instead scales UME along the expert axis through mixture-of-experts (MoE), growing encoder capacity while preserving single-vector, non-autoregressive encoding. Through a systematic study of the design space and t
    
[^47]: 图表超越像素：探针式分层图表理解与编辑

    Charts Are Beyond Pixels: Probing for Layer-Wise Chart Understanding and Editing

    [https://arxiv.org/abs/2609.08657](https://arxiv.org/abs/2609.08657)

    该论文提出LayerWiseBench基准，通过图层归属、图层绑定和可见性排序三个核心维度，评估模型在图层级别理解与编辑图表的能力，突破了以往仅评估最终输出正确性的局限。

    

    图表是结构化的视觉组合，其元素具有不同的功能角色、语义对应关系和可见性关系。这种结构化视角促使我们评估模型是否能够在图层级别理解和操作图表。然而，现有的图表基准主要评估最终输出的正确性或保真度，并未直接评估这些分层行为。我们提出了LayerWiseBench，这是一个围绕三个核心概念组织的基准：图层归属、图层绑定和可见性排序，这些概念构成了其图表理解和图表编辑评估任务。LayerWiseBench由可执行的图表程序生成，将每个渲染的图表与空间对齐的逐层RGBA资产以及由构建过程导出的功能角色、语义绑定和可见性关系标签相配对。基于这种分层表示，我们导出了受控的理解问题、编辑目标和参考答案。

    arXiv:2609.08657v1 Announce Type: cross  Abstract: Charts are structured visual compositions whose elements have distinct functional roles, semantic correspondences, and visibility relations. This structural view motivates evaluating whether models can understand and manipulate charts at the layer level. Existing chart benchmarks, however, primarily assess the correctness or fidelity of final outputs and do not directly evaluate these layer-wise behaviors. We present LayerWiseBench, a benchmark organized around three core concepts, layer attribution, layer binding, and visibility ordering, that structure its chart-understanding and chart-editing evaluations. Generated from executable chart programs, LayerWiseBench pairs each rendered chart with spatially aligned per-layer RGBA assets and construction-derived labels for functional roles, semantic bindings, and visibility relations. From this layer-wise representation, we derive controlled understanding questions, editing targets, refere
    
[^48]: 面向扩展RLVR推理覆盖范围的难度自适应树结构策略优化

    Difficulty-Adaptive Tree-Structured Policy Optimization for Expanding Reasoning Coverage in RLVR

    [https://arxiv.org/abs/2609.08650](https://arxiv.org/abs/2609.08650)

    该论文提出DATPO方法，通过难度自适应、句子熵引导的树结构轨迹生成策略，突破了RLVR探索受限的问题，有效扩展了大型推理模型的推理覆盖范围（pass@k）。

    

    可验证奖励强化学习（RLVR）是近期大型推理模型成功的核心。然而，尽管RLVR显著提高了单样本准确率，但由于训练期间探索有限，它往往无法扩展模型的内在推理覆盖范围（pass@k）。为了解决这一问题，我们优化了训练时轨迹生成的结构设计以提升pass@k。我们的分析确定了三个关键设计原则：（1）难度自适应的轨迹生成在扩展pass@k方面可以发挥重要作用，而不仅仅是作为一种效率启发式方法；（2）基于树的轨迹生成在发现正确答案方面优于并行采样；（3）基于句子熵引导的分支克服了词元级分支的局部化现象，以最大化语义多样性。基于这些见解，我们提出了DATPO（难度自适应句子熵引导的树结构策略优化）。DATPO整合了难度自适应……

    arXiv:2609.08650v1 Announce Type: cross  Abstract: Reinforcement Learning with Verifiable Rewards (RLVR) has been central to the recent success of Large Reasoning Models. However, while RLVR significantly improves single-sample accuracy, it often fails to expand the model's intrinsic reasoning coverage (pass@k) due to limited exploration during training. To address this, we optimize the structural design of train-time rollouts to enhance pass@k. Our analysis identifies three key design principles: (1) difficulty-adaptive rollout can play an important role in expanding pass@k, beyond serving as an efficiency heuristic; (2) tree-based rollout outperforms parallel sampling in discovering correct answers; and (3) sentence-entropy-guided forking overcomes the localization phenomenon of token-level branching to maximize semantic diversity. Building on these insights, we propose DATPO (Difficulty-Adaptive Sentence-entropy-guided Tree-structured Policy Optimization). DATPO integrates difficult
    
[^49]: SUN：在强化学习中追求新颖性

    SUN: Reaching for Novelty in Reinforcement Learning

    [https://arxiv.org/abs/2609.08642](https://arxiv.org/abs/2609.08642)

    本文提出基于后继价值函数的SUN指标，可同时衡量目标的新颖性与可达性，该可达性感知的目标选择框架能无缝集成到任意离策略强化学习算法中，并具有恢复计数奖励、约束命中概率和拒绝不可达目标的理论保证。

    

    强化学习（RL）中的探索仍然是一个根本性的挑战。近来的目标条件强化学习策略（通过选择目标来鼓励更广泛的状态覆盖）已展现出有希望的结果，但没有一种方法能够同时依据新颖性和可达性对目标进行评分：这两个信号要么被手动权衡，要么被依次应用，或者其中之一被完全忽视。在本文中，我们引入了一个可达性感知的目标选择框架，它明确地整合了这两个方面，并且可以无缝地融入任何离策略（off-policy）强化学习算法中。为此，我们提出了SUccessor-to-Novelty（SUN），这是一个由后继价值函数（successor value functions）导出的指标，用于识别既新颖又可达的目标。我们证明SUN在极限情况下可恢复基于计数的奖励，能够约束短时域命中概率，并且可以可证明地拒绝不可达的目标。我们进一步提出了一种利用这些特性的自适应目标选择策略，以及一个准确的……（摘要在此处截断）

    arXiv:2609.08642v1 Announce Type: cross  Abstract: Exploration in reinforcement learning (RL) remains a fundamental challenge. Recent goal-conditioned RL strategies (which select goals to encourage broader state coverage) have shown promising results, but none scores a goal by novelty and reachability jointly: the two signals are traded off by hand, applied in sequence, or one is neglected outright. In this paper, we introduce a reachability-aware goal-selection framework that explicitly integrates these two aspects, and that can be seamlessly incorporated into any off-policy RL algorithm. To this aim, we propose SUccessor-to-Novelty (SUN), an indicator derived from successor value functions to identify goals that are both novel and reachable. We prove that SUN recovers count-based bonuses in the limit, bounds short-horizon hitting probabilities, and provably rejects unreachable goals. We further present an adaptive goal-selection strategy that leverages these properties, and an accura
    
[^50]: Suan：纠正大语言模型中的直接偏好安全对齐

    Suan: Rectifying Direct Preference Safety Alignment in Large Language Models

    [https://arxiv.org/abs/2609.08634](https://arxiv.org/abs/2609.08634)

    Suan是一种新颖的偏好优化算法，通过直接在梯度层面构建优化目标（绕过标准变分推导），使大语言模型在实现卓越安全对齐的同时完全保留回应实用性。

    

    将强大的安全防护机制集成到大语言模型（LLM）中，对于提供有用且无害的回应至关重要。尽管专有系统展现出可靠的安全控制，但其底层方法和权衡取舍在很大程度上仍未公开。在开放权重模型中实现相当的安全性仍然是一个持续的挑战，因为经过后训练的模型变体经常出现过拒答和整体质量下降的问题。为了克服这些缺陷，我们提出了Suan，一种新颖的偏好优化算法。与现有方法不同，我们直接在梯度层面构建优化目标，绕过了标准的变分推导。由此，我们获得了更具可解释性和更稳健的训练动态。在多种竞争性基线和基准测试上的广泛评估表明，Suan在实现卓越安全对齐的同时，完全保留了回应的实用性。

    arXiv:2609.08634v1 Announce Type: cross  Abstract: Integrating robust safety guardrails into Large Language Models (LLMs) is essential for delivering helpful yet harmless responses. While proprietary systems exhibit reliable safety controls, their underlying methodologies and trade-offs remain largely undisclosed. Achieving comparable security in open-weight models remains a persistent challenge, as post-trained variants frequently suffer from over-refusal and degraded general quality. To overcome these drawbacks, we introduce Suan, a novel preference optimization algorithm. Unlike existing methods, we formulate the optimization objective directly at the gradient level, bypassing the standard variational derivation. As a result, we obtain more interpretable and robust training dynamics. Extensive evaluations across a diverse suite of competitive baselines and benchmarks demonstrate that Suan achieves superior safety alignment while fully preserving response utility.
    
[^51]: SynthRCT：用于合成重复CT生成的可扩展条件形变合成框架

    SynthRCT: Scalable Conditional Deformation Synthesis for Synthetic Repeat CT Generation

    [https://arxiv.org/abs/2609.08627](https://arxiv.org/abs/2609.08627)

    本文提出SynthRCT，一个基于条件变分自编码器的可扩展生成框架，能够从单一CT合成患者特异性的三维解剖形变，用于生成合成重复CT以支持质子治疗中的鲁棒性评估。

    

    在质子治疗中，治疗计划通常基于单一的计划CT进行优化，因此在解剖结构变化下的鲁棒性评估至关重要。然而，当前的场景往往依赖于简化的扰动方法，难以捕捉复杂的、患者特异性的解剖变异。我们提出了SynthRCT，这是一个用于三维解剖形变合成的可扩展条件生成框架。基于条件变分自编码器，SynthRCT学习一个潜在形变空间，并将采样的潜在编码解码为以输入解剖结构为条件的局部平稳速度场。这些局部场被组装成连贯的全体积变换，从而实现对大视野CT数据的内存可扩展生成。我们在包含每个受试者多个呼吸期相解剖结构的呼吸4DCT数据上验证了该方法。SynthRCT能够在预定义的鲁棒性场景之外，实现患者特异性的合理解剖变换采样。代码已在公开平台上发布。

    arXiv:2609.08627v1 Announce Type: cross  Abstract: In proton therapy, plans are typically optimized on a single planning CT, making robustness evaluation essential under anatomical changes. However, current scenarios often rely on simplified perturbations that poorly capture complex, patient-specific variability. We propose SynthRCT, a scalable conditional generative framework for 3D anatomical deformation synthesis. Based on a conditional variational autoencoder, SynthRCT learns a latent deformation space and decodes sampled latent codes into local stationary velocity fields conditioned on an input anatomy. Local fields are assembled into coherent full-volume transformations, enabling memory-scalable generation for large field-of-view CT data. We validate the approach on respiratory 4DCT data with multiple breathing-phase anatomies per subject. SynthRCT enables patient-specific sampling of plausible anatomical transformations beyond predefined robustness scenarios. Code available at: 
    
[^52]: 利用上下文事件进行结构感知的下一个活动预测

    Leveraging contextual events on structure-aware next activity prediction

    [https://arxiv.org/abs/2609.08622](https://arxiv.org/abs/2609.08622)

    本文提出一种基于实例图与图神经网络的结构感知方法，通过多种上下文事件编码策略将流程执行的环境条件纳入下一个活动预测，并在多个真实事件日志上验证了上下文信息对预测性能的提升作用。

    

    预测性流程监控旨在预测运行中流程的各个方面。在各类任务中，下一个活动预测是研究最为广泛的一项。然而，现有方法中只有少数明确编码了上下文信息，即流程执行时所处的环境条件，这些条件通常通过事件日志属性或聚合度量来建模。本文介绍了一种基于实例图概念的方法。为了纳入具有上下文的流程实例，提出了多种编码策略，并通过测量其对预测性能的影响来加以评估。对于每种编码策略，生成一组前缀-实例图，随后将其作为图神经网络分类任务的输入。该方法在多个真实世界的事件日志上进行了评估，实验结果表明，纳入上下文信息（摘要在此处截断）。

    arXiv:2609.08622v1 Announce Type: cross  Abstract: Predictive process monitoring aims at forecasting various aspects of running processes. Among the different tasks, next activity prediction represents the most extensively investigated. However, only a limited number of existing approaches explicitly encode contextual information, i.e., the environmental conditions in which the process is executed, typically modeled through event log attributes or aggregated measures. In this paper, an approach based on the concept of Instance Graphs is introduced. To incorporate contextual process instances, several encoding strategies are proposed and evaluated by measuring their impact on prediction performance. For each encoding strategy, a set of prefix-Instance Graphs is generated and subsequently provided as input to a Graph Neural Network for the classification task. The proposed approach is evaluated on multiple real-world event logs, and the experimental results demonstrate that incorporating
    
[^53]: 用于动态孔径预测的灵活谱归一化神经高斯过程

    Flexible Spectral-Normalized Neural Gaussian Process for Dynamic Aperture Prediction

    [https://arxiv.org/abs/2609.08620](https://arxiv.org/abs/2609.08620)

    该论文提出了一种经验贝叶斯方法来自动调整谱归一化神经高斯过程的超参数，在显著降低计算负担的同时实现了可扩展的不确定性量化，并在CERN大型强子对撞机的动态孔径预测任务上验证了方法的有效性。

    

    我们解决了大规模科学应用中可扩展不确定性量化的挑战，在这些应用中，复杂的最先进机器学习方法在计算上通常不可行。我们的主要贡献是一种简单而有效的经验贝叶斯方法，用于自动调整灵活的异方差谱归一化神经高斯过程的超参数。这种方法保留了半贝叶斯神经网络模型的表达能力和不确定性感知能力，同时通过将超参数学习直接集成到训练循环中，显著降低了计算负担。我们使用来自欧洲核子研究中心（CERN）大型强子对撞机案例的仿真数据，展示了我们的方法在估计圆形粒子加速器动态孔径这一高能物理对撞机和存储环基本问题中的实际影响。传统的动态孔径估计方法需要大

    arXiv:2609.08620v1 Announce Type: cross  Abstract: We address the challenge of scalable uncertainty quantification in large-scale scientific applications, where complex state-of-the-art machine learning methods are often computationally infeasible. Our primary contribution is a simple yet effective empirical Bayes method for automatically tuning the hyperparameters of a flexible, heteroscedastic Spectral-normalized Neural Gaussian Process. This approach retains the expressiveness and uncertainty-awareness of semi-Bayesian neural models while significantly reducing the computational burden by integrating hyperparameter learning directly into the training loop. We demonstrate the practical impact of our method on the task of estimating the dynamic aperture in circular particle accelerators, a fundamental problem in high-energy physics colliders and storage rings, using simulation data from the case of the Large Hadron Collider at CERN. Traditional approaches to DA estimation require exte
    
[^54]: 面向跨语言模型家族训练响应预测的目标无关微干预方法

    Target-Independent Micro-Interventions for Predicting Training Response Across Language-Model Families

    [https://arxiv.org/abs/2609.08618](https://arxiv.org/abs/2609.08618)

    该论文提出通过四个目标无关的微干预构建 L-State 状态表示，能够跨不同语言模型家族预测模型对未来训练的响应，在 GLM-4-9B 上将预测误差（MSE）降低高达 78.3%。

    

    基准测试分数描述了模型检查点当前能做什么，但并不能决定它将如何响应下一次训练。我们通过从同一检查点分支出四个简短、标准化、目标无关的微干预，并在一个共同的能力空间中记录它们的效果，来测量这一缺失的状态。这些响应与当前能力一起构成了 L-State；其脉冲块支持灵活的直接读出和保持结构的算子读出两种方式。在平滑局部动力学假设下，该算子构造允许建立端到端的跨家族理论界，并显式刻画源家族与目标家族的坐标异质性。在三个家族的留一家族交叉开发实验中，两种脉冲读出方式相比仅使用能力特征将源标准化 MSE 降低了 39.4%，同时将最佳响应估计与方向估计分离开来。在密封的 GLM-4-9B 模型上，直接读出和算子读出分别将 MSE 降低了 71.8% 和 78.3%。

    arXiv:2609.08618v1 Announce Type: new  Abstract: Benchmark scores describe what a checkpoint can do now, but they do not determine how it will respond to the next training episode. We measure this missing state by branching four short, standardized, target-independent micro-interventions from the same checkpoint and recording their effects in a common capability space. Together with current capability, these responses form L-State; its pulse block supports a flexible direct readout and a structure-preserving operator readout. Under smooth local dynamics, the operator construction admits an end-to-end cross-family bound with explicit source- and target-family coordinate heterogeneity. In three-family leave-one-family-out development, both pulse readouts reduce source-standardized MSE by 39.4% relative to capability alone, while separating the best response and direction estimates. On sealed GLM-4-9B, the direct and operator readouts reduce MSE by 71.8% and 78.3%, respectively, and the o
    
[^55]: 共享注意力向量为何失效：结果索引调优的论证

    Why shared attention vectors fail: a case for outcome-indexed tuning

    [https://arxiv.org/abs/2609.08615](https://arxiv.org/abs/2609.08615)

    本文揭示了全局共享注意力向量在多结果学习中会不稳定并塌缩至边界的问题，并提出用结果索引的注意力矩阵替代，使其能收敛到对学习和泛化有意义的注意力表示。

    

    学习中的维度注意力通常被实现为全局共享的注意力向量，其中每个刺激维度对应一个标量。这些标量由模型通过对误差进行梯度下降来学习，具有预测性的特征会获得更高的显著性。我们表明，在多结果学习（即模型需要预测多个结果）的情况下，这种共享向量会变得不稳定；它会塌缩到其边界，从而阻止模型学习到对学习和泛化有意义的注意力调优。为了解决这个问题，我们引入了一种结果索引的注意力矩阵，将全局共享的注意力调优转换为结果索引的表示。我们对共享向量的不稳定性进行了分析，并推导出该现象成立的条件。在实证方面，三个合成实验对所提出的注意力矩阵进行了基准测试，结果表明它们能够收敛到有意义的表示，而这是共享注意力向量无法做到的。

    arXiv:2609.08615v1 Announce Type: new  Abstract: Dimensional attention in learning is often implemented as a globally shared attention vector, where each stimulus dimension corresponds to a single scalar. These scalars are learned by models through gradient-descent on error, where predictive features acquire more salience. We show that under multi-outcome learning, where models predict more than one outcome, this shared vector becomes unstable; it collapses to its bounds and prevents the models from learning meaningful attentional tunings for learning and generalization. We address this by introducing an outcome-indexed attentional matrix that converts globally shared attentional tuning into an outcome-indexed representation. We present an analysis of the unstable shared vectors and derive the conditions under which it holds. Empirically, three synthetic experiments benchmark the proposed attention matrices and show that they converge to meaningful representations, something shared att
    
[^56]: 基于多水平集的物理驱动神经网络求解三维逆散射问题

    Multi-Level-Set-Based Physics-Driven Neural Network to Solve 3-D Inverse Scattering Problems

    [https://arxiv.org/abs/2609.08594](https://arxiv.org/abs/2609.08594)

    本文提出基于多水平集的物理驱动神经网络（LSPDNN），利用软并集多材料模型将目标支撑域与材料分布分离描述，并结合作用于材料区域指示器的模型一致TV正则化，有效缓解了三维电磁逆散射重建中的边界模糊与伪影问题。

    

    本文提出了一种基于水平集的物理驱动神经网络求解器（LSPDNN），用于求解三维电磁逆散射问题。为了缓解逐体素对比度重建中的边界模糊和重建伪影问题，所提出的求解器利用实际散射体的分段均匀性，采用多个坐标相关的神经水平集分量来表示未知目标。具体而言，提出了一种软并集多材料模型，分别描述目标支撑域和材料分布：全局支撑域由多个水平集分量的并集形成，而局部对比度则由归一化的分量权重和可学习的复介电常数候选值共同确定。此外，在材料区域指示器上施加模型一致的总变分（TV）正则化，而非直接作用于重建的对比度上，从而在抑制碎片化材料分配的同时避免过度……

    arXiv:2609.08594v1 Announce Type: new  Abstract: This paper proposes a level-set-based physics-driven neural network solver (LSPDNN) for 3-D electromagnetic inverse scattering. To mitigate boundary blurring and reconstruction artifacts in voxel-wise contrast reconstruction, the proposed solver exploits the piecewise homogeneity of practical scatterers by representing unknown targets with multiple coordinate-dependent neural level-set components. Specifically, a soft-union multi-material model is proposed to separately describe the object support and material distribution. The global support is formed by the union of multiple level-set components, while the local contrast is determined by normalized component weights and learnable complex permittivity candidates. In addition, a model-consistent total variation (TV) regularization is imposed on the material-region indicators, rather than directly on the reconstructed contrast, to suppress fragmented material assignments without excessive
    
[^57]: FedGenSC：具有信道感知自适应能力的联邦生成式语义通信

    FedGenSC: Federated Generative Semantic Communication with Channel-Aware Adaptation

    [https://arxiv.org/abs/2609.08593](https://arxiv.org/abs/2609.08593)

    提出FedGenSC框架，通过全局生成器搭配本地判别器、语义原型库和基于信噪比条件的自适应生成，同时解决了联邦生成式语义通信中判别器聚合不稳定、语义漂移和信道无关生成三大问题。

    

    将生成对抗网络（GAN）集成到联邦语义通信（SemCom）中是一种自然的演进，因为生成式先验能够在信道失真下恢复判别式解码器无法恢复的语义保真度。然而，据我们所知，朴素的GAN联邦会引入三种失败模式，而先前的工作既未识别也未解决这些问题：非独立同分布数据下判别器聚合的不稳定性、由各客户端本地嵌入空间发散引起的语义漂移，以及无法适应异构链路条件的与信道无关的生成方式。我们提出联邦生成式语义通信，通过采用仅保留本地判别器的全局生成器、通过语义原型库提供跨客户端语义信息，以及以瞬时信噪比（SNR）为条件来调节生成过程，从而缓解上述全部三个问题。

    arXiv:2609.08593v1 Announce Type: cross  Abstract: Integrating generative adversarial networks (GANs) into federated semantic communication (SemCom) is a natural progression, as generative priors can recover semantic fidelity under channel distortion that discriminative decoders cannot. However, naive GAN federation introduces three failure modes that prior work has, to the best of our knowledge, neither identified nor resolved: discriminator aggregation instability under non-independent and identically distributed (non-IID) data, semantic drift caused by divergent local embedding spaces, and channel-agnostic generation that cannot adapt to heterogeneous link conditions. We propose federated generative semantic communication (FedGenSC), which mitigates all three by employing a global generator with local-only discriminators, providing cross-client semantic information through a semantic prototype bank, and conditioning generation on the instantaneous signal-to-noise ratio (SNR). Experi
    
[^58]: 自动可模拟性的局限性：大语言模型模拟器可以绕过解释

    Limitations of Automated Simulatability: LLM Simulators Can Bypass Explanations

    [https://arxiv.org/abs/2609.08585](https://arxiv.org/abs/2609.08585)

    本文揭示了自动可模拟性评估的两大局限：当类名有意义时，LLM模拟器可直接解题获得高分而不依赖解释；而类匿名化又可能奖励泄露隐藏标签映射的解释，表明模拟器预测主要依赖任务信息而非解释本身。

    

    可模拟性是一种解释评估协议，通过解释帮助用户预测任务模型输出的程度来量化其有用性。由于人工评估成本高昂，自动可模拟性用大语言模型（LLM）模拟器替代人类解释接受者，如ConSim（Poché等人，2025）所提出的，以进行大规模实验。我们在所测试的数据集、解释方法和模拟器LLM范围内，定性地复现并扩展了ConSim对各种解释方法的排名，并识别出两个局限性。第一，当类名具有语义含义时，模拟器可以通过直接解决分类任务获得高可模拟性，而无需依赖解释。第二，类匿名化可能会奖励泄露隐藏标签映射的解释，我们通过一个新的“类作为概念”基线揭示了这一局限性。这些结果与捷径假说相符：在所测试的设置中，模拟器的预测主要依赖于任务本身的信息而非解释内容。

    arXiv:2609.08585v1 Announce Type: cross  Abstract: Simulatability is an evaluation protocol for explanations that quantifies their usefulness by how well they help a user predict a task model's outputs. Since human evaluation is costly, automated simulatability replaces human explainees with LLM simulators, as proposed in ConSim (Poch\'e et al., 2025) for large-scale experiments. We qualitatively replicate and extend ConSim's ranking of explanation methods across the tested datasets, explanation families, and simulator LLMs, and identify two limitations. First, when class names are meaningful, simulators can obtain high simulatability by solving the classification task directly, without relying on the explanations. Second, class anonymization can reward explanations for leaking the hidden label mapping, a limitation we expose with a new classes-as-concepts baseline. These results are consistent with a shortcut hypothesis: in the tested settings, simulator predictions mainly rely on tas
    
[^59]: 利用心脏成像改善资源受限环境中基于心电图的恰加斯病检测

    Leveraging Cardiac Imaging to Improve ECG-Based Detection of Chagas Disease in Resource-Constrained Settings

    [https://arxiv.org/abs/2609.08582](https://arxiv.org/abs/2609.08582)

    该研究通过在英国生物样本库的63,193对ECG-CMR配对数据上进行对比预训练，将心脏磁共振的结构知识迁移到心电图编码器，在预训练中从未见过恰加斯病病例的情况下显著提升了资源受限地区基于心电图的恰加斯病检测性能。

    

    恰加斯病是拉丁美洲心肌病的主要病因。心脏磁共振（CMR）成像能够表征其结构性异常，但在疾病流行地区，扫描设备和专业读片人员仍然稀缺。心电图（ECG）价格低廉且易于获取，但结构性病变只能通过电信号间接推断。我们提出通过对比预训练将CMR衍生的结构知识迁移到心电图中。利用英国生物样本库（UK Biobank）中63,193对ECG-CMR配对检查数据，我们采用非对称InfoNCE目标函数将ECG编码器与基于临床的CMR嵌入空间进行对齐。尽管在预训练期间模型未接触过任何恰加斯病病例，所得的表征仍改善了基于ECG的恰加斯病检测效果。在CODE-15%和SaMi-Trop数据集的五折交叉验证中，冻结的线性探针实现了0.851的AUROC和0.427的预测风险前5%敏感性（Top5%-TPR），相比之下基线为0.827。

    arXiv:2609.08582v1 Announce Type: cross  Abstract: Chagas disease is a major cause of cardiomyopathy in Latin America. Cardiac magnetic resonance (CMR) imaging can characterize its structural abnormalities, but scanners and expert readers remain scarce in endemic regions. Electrocardiography (ECG) is inexpensive and widely available, yet structural disease must be inferred indirectly from electrical signals. We propose to transfer CMR-derived structural knowledge to ECG through contrastive pre-training. Using 63,193 paired ECG-CMR examinations from the UK Biobank, we align an ECG encoder with a clinically grounded CMR embedding space using an asymmetric InfoNCE objective. Despite seeing no Chagas cases during pre-training, the resulting representation improves ECG-based Chagas detection. Across CODE-15% and SaMi-Trop, a frozen linear probe achieves an AUROC of 0.851 and sensitivity at the top 5% of predicted risk (Top5%-TPR) of 0.427 in five-fold cross-validation, compared with 0.827 a
    
[^60]: AlphaRJM：用于随机收益引导的Alpha因子发现的奖励跳跃记忆

    AlphaRJM: Reward-Jump Memory for Stochastic Return-Guided Alpha Discovery

    [https://arxiv.org/abs/2609.08581](https://arxiv.org/abs/2609.08581)

    提出AlphaRJM框架，通过仅在终端评估事件时更新的奖励跳跃记忆和使用随机粒子表示未来收益的动作条件化SDE评论家，解决了公式化Alpha发现中延迟反馈导致的因子池历史信息丢失与中间动作价值不确定的难题。

    

    公式化Alpha因子发现是一个依赖因子池的符号搜索问题，其中有效的反馈主要在完整表达式被评估时才能观察到。这种延迟反馈带来了两个相互耦合的困难：保留的Alpha因子池不能保存已实现评估反馈的完整历史，且中间构建动作的价值是不确定的，因为其后果取决于最终完成的公式。我们提出了AlphaRJM，通过奖励跳跃记忆来解决这些困难——这是一种事件驱动的潜在状态，在token构建过程中保持固定，仅在终端评估事件时使用已实现的因子池奖励和评估结果进行更新；同时引入一个动作条件化的SDE收益评论家，用随机粒子来表示未来折现的发现收益。这些粒子通过其均值和不确定性来引导动作选择，并使用分布式贝尔曼算子进行学习……

    arXiv:2609.08581v1 Announce Type: new  Abstract: Formulaic alpha discovery is a pool-dependent symbolic search problem in which informative feedback is observed primarily when a complete expression is evaluated. This delayed feedback creates two coupled difficulties: the retained alpha pool does not preserve the full history of realized evaluation feedback, and the value of an intermediate construction action is uncertain because its consequence depends on the formula eventually completed. We introduce AlphaRJM, which addresses these difficulties through Reward-Jump Memory, an event-driven latent state that remains fixed during token construction and updates only at terminal evaluation events using the realized pool reward and evaluation outcome, and an action-conditioned SDE return critic that represents future discounted discovery returns with stochastic particles. The particles guide action selection through their mean and uncertainty and are learned using a distributional Bellman o
    
[^61]: 非自适应1比特均值估计：极小极大速率与样本-区间权衡

    Non-Adaptive 1-Bit Mean Estimation: Minimax Rates and the Sample-Interval Tradeoff

    [https://arxiv.org/abs/2609.08564](https://arxiv.org/abs/2609.08564)

    该论文证明对于所有 k>1，非自适应的1比特协议即可达到自适应协议的极小极大速率，表明分布式均值估计中交互式学习并非必要，并刻画了查询区间数受限情形下的样本复杂度权衡。

    

    我们研究了在1比特通信约束下的分布式一维均值估计问题。每个智能体观测一个从未知分布中独立抽取的样本，并针对中央学习器选择的查询 Q: ℝ→{0,1} 返回单个比特作为响应。该分布的均值位于 [-λ,λ] 内，且k阶中心矩至多为 σ^k（k>1 为固定值）。Lau 和 Scarlett 提出的阶数最优两阶段协议利用第一批样本的响应来选择第二批的查询，这引出了一个自然的问题：这一轮交互是否必要。我们给出了否定的回答：对于每个 k>1，都存在非自适应协议能够达到自适应情形下的1比特极小极大速率（同期并行工作通过不同策略也得出了相同结论）。此外，当每个查询的取1集合 Q^{-1}(1) 被限制为至多 s 个区间的并集时，我们进一步确定了非自适应1比特估计器的极小极大样本复杂度……

    arXiv:2609.08564v1 Announce Type: cross  Abstract: We study distributed one-dimensional mean estimation under a 1-bit communication constraint. Each agent observes one sample, drawn independently from an unknown distribution, and returns a single bit in response to a query $Q: \mathbb{R}\to\{0,1\}$ chosen by a central learner. The distribution has mean in $[-\lambda,\lambda]$ and $k$-th central moment at most $\sigma^k$, for a fixed $k>1$. The order-optimal two-stage protocol of Lau and Scarlett uses responses from the first batch to choose the second-batch queries, motivating the question of whether this single round of interaction is necessary. We answer this negatively: for every $k>1$, a non-adaptive protocol attains the adaptive 1-bit minimax rate (and concurrent works reached the same conclusion via different strategies). We further determine the minimax sample complexity among non-adaptive 1-bit estimators when every one-set $Q^{-1}(1)$ is restricted to a union of at most $s$ in
    
[^62]: 神经表征中的认证拓扑交互：类解缠主要是成对发生的

    Certified Topological Interaction in Neural Representations: Class Disentanglement Is Mostly Pairwise

    [https://arxiv.org/abs/2609.08561](https://arxiv.org/abs/2609.08561)

    本文提出用交集欧拉示性数轮廓将神经表征中的类解缠度量为带统计认证的拓扑交互，发现解缠主要发生在成对类别之间、随深度分级且集中在训练早期。

    

    类解缠（即表征的类条件点云沿网络深度和训练过程的分离程度）通常是通过描述性曲线来读取的。我们将其度量为标记点云之间的认证拓扑交互，使用最近提出的交集欧拉示性数轮廓：即点云球体并集交集的欧拉示性数作为尺度的函数，通过一次Alpha复形扫描即可计算，无需边界矩阵约化。每一个数值都附带统计检验：双向的精确置换检验、受保护的分离证书，以及针对应用中比较性结论的配对检验。在111个训练网络和52,650个认证测量中，解缠是随深度分级的，并集中在最初的几个训练周期；交互商数可按可混淆性对类别对进行排序（Spearman rho=0.83），其效果与廉价的可分性统计指标相当。在96个模型的析因实验种群中，数据增强……

    arXiv:2609.08561v1 Announce Type: new  Abstract: Class disentanglement (the separation of a representation's class-conditional point clouds along depth and over training) is usually read off descriptive curves. We measure it as certified topological interaction between labeled point clouds, using the recently introduced Intersection Euler Characteristic Profile: the Euler characteristic of the overlap of the clouds' ball unions as a function of scale, computed by one Alpha-complex sweep with no boundary-matrix reduction. Every number carries a test: exact permutation tests in both directions, a guarded separation certificate, and a paired test for the comparative claims applications make. Across 111 trained networks and 52,650 certified measurements, disentanglement is depth-graded and concentrated in the first epochs, and interaction quotients rank class pairs by confusability (Spearman rho=0.83), on par with cheap separability statistics. In a 96-model factorial population, augmentat
    
[^63]: 并非所有变量都一致：面向多变量时间序列预测的可靠性感知的变量级梯度手术

    Not All Variables Agree: Reliability-Aware Variable-Wise Gradient Surgery for Multivariate Time-Series Forecasting

    [https://arxiv.org/abs/2609.08554](https://arxiv.org/abs/2609.08554)

    该论文通过直接测量变量级梯度揭示了多变量时间序列预测中梯度冲突与性能损害并不等同的现象，并提出了一种优化器侧的变量级梯度手术训练策略PV-Surgery，以提升共享训练下各变量的预测可靠性。

    

    在数据驱动的训练中，多变量时间序列预测通常使用在样本、变量和预测时域上取平均的标量损失进行优化。这种平均化虽然方便，但优化器只能看到聚合后的梯度，无法揭示各变量的贡献是相互一致还是相互冲突。为了量化这种分歧出现的频率，我们直接测量了变量级梯度，发现在七个数据集上平均有30.6%的两两余弦相似度为负值。然而，梯度冲突与性能损害并不是一回事。在共享训练下，64个变量中有35个变量的表现劣于全输入单目标oracle，且受损变量的比例并不能通过梯度冲突的频率来可靠预测。我们提出了Per-Variable Surgery（PV-Surgery），这是一种面向具有缓存兼容层的骨干网络的优化器侧训练策略。仅需一次反向传播，即可从输出侧构建变量级梯度代理。

    arXiv:2609.08554v1 Announce Type: new  Abstract: In data-driven training, multivariate time-series forecasting is usually optimized with a scalar loss averaged over samples, variables, and horizons. This averaging is convenient, but the optimizer sees only the aggregated gradient, which does not reveal whether the variable-wise contributions align or oppose one another. To quantify how often this disagreement arises, we measure the variable-wise gradients directly and find that 30.6% of their pairwise cosine similarities are negative on average across seven datasets. However, conflict and harm are not the same thing. Under shared training 35 of the 64 variables do worse than a full-input single-target oracle, and the harmed fraction is not reliably predicted by how often gradients conflict. We propose Per-Variable Surgery (PV-Surgery), an optimizer-side training strategy for backbones with cache-compatible layers. One backward pass builds variable-wise gradient proxies from output-side
    
[^64]: 光滑凸目标函数上随机梯度下降的精确时间一致收敛率前沿

    The Exact Time-Uniform Rate Frontier for Stochastic Gradient Descent on Smooth Convex Objectives

    [https://arxiv.org/abs/2609.08537](https://arxiv.org/abs/2609.08537)

    该论文精确刻画了标准SGD在光滑凸目标上时间一致收敛率的可实现边界——阶为 $h(n)/\sqrt{n}$ 的界可实现当且仅当 $\sum 1/h(2^j)^2 < \infty$，且收敛率可任意接近但永远达不到 $\sqrt{\log n / n}$。

    

    我们研究了标准随机梯度下降（SGD）的原始迭代在无约束光滑凸目标函数上的时间一致收敛性。我们证明，在标准噪声假设下，时间一致收敛率可以任意接近 $\sqrt{\log n / n}$，但永远无法达到该值。更具体地说，我们证明，对于每个满足 $h(n) = o(\sqrt{n})$ 的正的、最终非递减的序列 $h$，一个阶为 $h(n)/\sqrt{n}$ 的界——要求对所有 $n$ 同时以至少 $1-\alpha$ 的概率成立并在整个问题类上一致成立——是可实现的，当且仅当 $\sum_{j=1}^{\infty} \frac{1}{h(2^j)^2} < \infty$。构造性的充分性证明来自一种无视野的二分（dyadic）步长调度，并结合了一个可加条件重启不等式。必要性结果则适用于所有确定性的非负调度，即使对于一维解析光滑凸目标且带高斯（噪声）的情形依然成立。

    arXiv:2609.08537v1 Announce Type: cross  Abstract: We study the time-uniform convergence of the raw iterate of standard stochastic gradient descent (SGD) for unconstrained smooth convex objectives. We prove that, under standard noise assumptions, the time-uniform convergence rate gets arbitrarily close to $\sqrt{\log n / n}$ but never reaches it. More specifically, we prove that for every positive, eventually nondecreasing sequence $h$ satisfying $h(n) = o(\sqrt{n})$, a bound of order $h(n)/\sqrt{n}$, holding simultaneously for all $n$ with probability at least $1-\alpha$ and uniformly over the problem class, is achievable if and only if \[   \sum_{j = 1}^{\infty} \frac{1}{h(2^j)^2} < \infty. \] The constructive sufficiency result follows from a dyadic horizon-free schedule together with an additive conditional-restart inequality. The necessity counterpart applies to every deterministic nonnegative schedule and holds even for a one-dimensional analytic smooth convex objective with Gaus
    
[^65]: 视频生成中的时间状态传输：诊断与校正频谱失衡

    Temporal State Transport in Video Generation: Diagnosing and Correcting Spectral Imbalance

    [https://arxiv.org/abs/2609.08505](https://arxiv.org/abs/2609.08505)

    该论文提出“频谱张力”这一带符号的诊断指标，用于识别视频生成中碎片化传输与过度混合两类时间失衡问题，并提出免训练的“频谱传输稳态”调节器，在保留平衡状态的同时温和纠正病态时间状态。

    

    可靠的视频生成不仅仅需要高质量的帧来构成连贯的故事：模型必须维持一个持久的状态，将身份、场景布局、运动和细节等视觉属性在时间维度上进行传输。现有的免训练方法主要是加强跨帧注意力或分析局部注意力熵，但这些视角无法揭示时间交互是否保持在健康的传输区间。在这项工作中，我们从“时间状态传输”的视角来研究视频生成。我们引入了“频谱张力”，这是一种带符号的诊断指标，用于比较局部注意力的扩散程度与全局频谱多样性，并利用它来识别两种截然相反的时间失败模式：碎片化传输和过度混合热点。基于这一诊断，我们提出了“频谱传输稳态”，一种免训练的调节器，能够在很大程度上保留平衡的时间状态的同时，温和地纠正病态的时间状态。实验……

    arXiv:2609.08505v1 Announce Type: cross  Abstract: Reliable video generation requires more than high-quality frames to form a coherent story: a model must maintain a persistent state, transporting visual attributes such as identity, scene layout, motion, and fine details across time. Existing training-free methods mainly strengthen cross-frame attention or analyze local attention entropy, but these views do not reveal whether temporal interactions stay in a healthy transport regime. In this work, we study video generation through the perspective of Temporal State Transport. We introduce Spectral Tension, a signed diagnostic that compares local attention diffuseness with global spectral diversity, and use it to identify two opposite temporal failures: fragmented transport and over-mixing hotspots. Based on this diagnosis, we propose Spectral Transport Homeostasis, a training-free regulator that softly corrects pathological temporal states while largely preserving balanced ones. Experime
    
[^66]: 当拓扑背叛隐私：去中心化联邦学习中针对安全聚合的基于格的重构攻击

    When Topology Betrays Privacy: Lattice-Based Reconstruction Attacks on Secure Aggregation in Decentralized Federated Learning

    [https://arxiv.org/abs/2609.08476](https://arxiv.org/abs/2609.08476)

    去中心化联邦学习中稀疏拓扑使局部安全聚合成为结构性泄露面，本文通过将重构问题归约为隐藏子集和问题并设计基于格的攻击算法，使合谋的半诚实节点能够重构诚实参与者的私有模型更新。

    

    安全聚合（Secure Aggregation, SA）被广泛视为抵御联邦学习（FL）中模型更新泄露的强大防御手段，因为它只揭示聚合结果而隐藏个体更新。在去中心化联邦学习（DFL）中，SA通常被实例化为局部邻域聚合，即每个节点获得其邻居节点上的加权聚合结果。我们证明这种局部性会造成一个结构性的泄露面：稀疏的去中心化拓扑为相互合谋的半诚实节点提供了非对称的聚合视图，从而暴露了诚实参与者隐私状态的多个隐藏线性组合。从这些聚合视图中重构隐私状态在本质上是极具挑战性的，因为隐私状态和聚合系数均处于隐藏状态。为应对这一挑战，我们建立了该问题与隐藏子集和问题（Hidden Subset Sum Problem）这一密码学中长期研究问题之间的形式化联系。基于这一表述，我们设计了一种基于格的…（原文摘要在此处截断）

    arXiv:2609.08476v1 Announce Type: cross  Abstract: Secure Aggregation (SA) is widely regarded as a strong defense against model-update leakage in Federated Learning (FL), as it reveals only aggregate results while hiding individual updates. In Decentralized Federated Learning (DFL), SA is commonly instantiated as local neighborhood aggregation, where each node obtains a weighted aggregate over its neighbors. We show that this locality creates a structural leakage surface: sparse decentralized topologies provide colluding semi-honest nodes with asymmetric aggregate views, exposing multiple hidden linear combinations of honest participants' private states. Reconstructing private states from these aggregate views is fundamentally challenging, as both the private states and the aggregation coefficients are hidden. We tackle this challenge by establishing a formal connection to the Hidden Subset Sum Problem, a long-studied problem in cryptography. Building on this formulation, we design a l
    
[^67]: 审稿人还会奖励词汇复杂性吗？基于12.4万份ICLR评审的“冻结评审员”偏好漂移研究

    Do Reviewers Still Reward Lexical Complexity? A Frozen-Rater Study of Preference Drift in 124K ICLR Reviews

    [https://arxiv.org/abs/2609.08475](https://arxiv.org/abs/2609.08475)

    该研究提出“冻结评审员”方法——用同一模型和提示词在固定时间窗口生成81,850份机器评审，以区分投稿构成变化与审稿人偏好变化——发现人类评审对词汇复杂性的系数从+0.142反转为-0.015而冻结评审员保持稳定，证明ICLR审稿人已真实地不再青睐辞藻华丽的写作。

    

    大型语言模型已使撰写辞藻华丽的文章的成本大幅降低，而同行评审是否仍会奖励这种文风，是一个关乎评审者而非文本本身的问题。当某一写作特征与评审分数之间的关联逐年变化时，可能是评审者变了，可能是投稿变了，也可能两者皆变，而将分数对文本做回归无法区分这些情况。我们用“冻结评审员”来分离这两种效应：针对2018至2025年ICLR投稿的81,850份机器评审，全部在2025年2月至4月同一时间窗口内、用同一模型家族和同一提示词生成，因此其逐年系数仅反映投稿构成的变化，而人类评审减去冻结评审的趋势差异则可识别评审者偏好的漂移。在32,638份投稿共124,615份人类评审上，人类评审对非领域词汇复杂性的系数从+0.142降至-0.015，而冻结评审员则从+0.080变为+0.082；三重差分……

    arXiv:2609.08475v1 Announce Type: cross  Abstract: Large language models have collapsed the cost of producing lexically elaborate prose, and whether peer reviewers still reward it is a question about the evaluator, not about the text. When the association between a writing cue and review scores moves across years, the reviewers may have changed, the submissions may have changed, or both, and a regression of scores on text cannot say which. We separate the two with a frozen rater: 81,850 machine reviews of ICLR submissions from 2018 to 2025, all generated in one February-April 2025 window with one model family and one prompt, so that its year-to-year coefficients track submission composition alone and the human-minus-frozen trend difference identifies reviewer preference drift. On 32,638 submissions with 124,615 human reviews, the human coefficient on non-domain lexical complexity falls from +0.142 to -0.015 while the frozen rater moves from +0.080 to +0.082; the three-way difference-in
    
[^68]: 基于域自适应的包容性电子-原子核截面模型

    Inclusive electron-nucleus cross section models from domain adaptation

    [https://arxiv.org/abs/2609.08463](https://arxiv.org/abs/2609.08463)

    该研究利用迁移学习将基于碳-12数据预训练的深度神经网络适配到氦-3、锂-6、氧-16、铝-27、钙-40和铁-56等多种原子核的包容性电子散射截面建模中，发现不同核所需的微调深度各异，且模型预测稳健性依赖于目标数据的数量、覆盖范围和精度。

    

    我们应用迁移学习（TL）构建了数据驱动的包容性电子-原子核截面模型。从在碳-12（\(^{12}\)C）数据上预训练的深度神经网络集合出发，我们分别对氦-3（\(^{3}\)He）、锂-6（\(^{6}\)Li）、氧-16（\(^{16}\)O）、铝-27（\(^{27}\)Al）、钙-40（\(^{40}\)Ca）和铁-56（\(^{56}\)Fe）的模型进行了微调。所得模型对所有目标核都有改进，对于氧核改进幅度较小，因为碳基线模型已经足够充分，尽管其预测的稳健性取决于可用目标数据的数量、覆盖范围和精度。我们系统地研究了模型性能如何依赖于微调的层数、训练数据的比例和选择方式，以及源域和目标域运动学范围之间的重叠程度。逐层分析表明，氧核只需要浅层自适应，而氦、钙和铁则需要更深入的微调。锂是最不稳健的情况……

    arXiv:2609.08463v1 Announce Type: cross  Abstract: We apply transfer learning (TL) to construct data-driven models of inclusive electron-nucleus cross sections. Starting from an ensemble of deep neural networks pretrained on \(^{12}\)C data, we fine-tune the models separately for \(^{3}\)He, \(^{6}\)Li, \(^{16}\)O, \(^{27}\)Al, \(^{40}\)Ca, and \(^{56}\)Fe. The resulting models improve for all targets, marginally so for oxygen, where the carbon baseline is already adequate, although their predictive robustness depends on the amount, coverage, and precision of the available target data. We systematically study how model performance depends on the number of fine-tuned layers, on the fraction and selection of the training data, and on the overlap between the source and target kinematic domains. The layer-wise analysis shows that oxygen requires only shallow adaptation, whereas helium, calcium, and iron require substantially deeper fine-tuning. Lithium represents the least robust case beca
    
[^69]: 基于学习误差门控的选择性边界条件约简

    Selective boundary condition reduction via learned error gating

    [https://arxiv.org/abs/2609.08461](https://arxiv.org/abs/2609.08461)

    该论文提出一种学习误差门控框架，利用神经网络预测边界条件约简带来的区域误差和边界误差，仅在预测误差满足容差时才用更简单的极限边界条件（如Dirichlet形式）替代复杂的刚性Robin或非线性边界条件，从而在保证精度的同时降低计算成本。

    

    参数化偏微分方程可以采用不同的边界条件，其精度和计算成本各不相同。我们引入了一个用于学习何时一个约简边界条件可以替代另一个的框架：利用成对的解训练神经网络来估计由此产生的区域误差和边界误差，只有当两个预测误差均满足规定的容差时，才使用更简单的边界条件。我们重点关注应用中的奇异极限情形，其中刚性的Robin边界或非线性边界定律被其极限Dirichlet形式所取代。我们在一个电偶腐蚀问题以及其他非线性定常问题和演化问题上对该方法进行了评估。

    arXiv:2609.08461v1 Announce Type: cross  Abstract: Parametric PDEs can admit different boundary conditions with different accuracy and computational cost. We introduce a framework for learning when one reduced boundary condition can replace another: paired solutions train a neural network to estimate the resulting domain and boundary errors, and the simpler condition is used only when both predicted errors meet prescribed tolerances. We focus on singular limits in applications, in which a stiff Robin or nonlinear boundary law is replaced by its limiting Dirichlet form. We evaluate the method on a galvanic corrosion problem and other nonlinear stationary and evolution problems.
    
[^70]: 潜在交易空间中的拓扑欺诈检测

    Topological Fraud Detection in Latent Transaction Spaces

    [https://arxiv.org/abs/2609.08445](https://arxiv.org/abs/2609.08445)

    该论文提出一种在拓扑匿名化嵌入空间中迭代结合无监督过滤与有监督精确识别的欺诈检测方法，实现超低延迟且不泄露个人身份信息的隐私保护交易筛查。

    

    我们完全在拓扑匿名化的嵌入表示上进行操作，通过迭代式的无监督过滤和随后的有监督精确识别来执行欺诈检测。最终成果是一套超低延迟、保护隐私的分诊系统，使各机构能够在不泄露个人身份信息（PII）的前提下标记可疑活动。

    arXiv:2609.08445v1 Announce Type: new  Abstract: Working entirely on topologically anonymized embeddings, we perform fraud detection using iterative rounds of unsupervised filtering followed by supervised sniping. The result is an ultra-low latency privacy--preserving triage that allows institutions to flag suspicious activity without compromising Personally Identifiable Information.
    
[^71]: 随机扰动权重：从确定性机器学习天气模型生成集合预报

    Stochastically Perturbed Weights: Ensembles from Deterministic Machine-Learning Weather Models

    [https://arxiv.org/abs/2609.08412](https://arxiv.org/abs/2609.08412)

    提出随机扰动权重（SPW）方法，通过在推理时直接扰动确定性机器学习天气模型的原始权重张量，无需重新训练即可从现有确定性模型中提取预报不确定性并生成集合预报。

    

    机器学习天气模型（MLWMs）如今在全球中期预报方面已达到甚至超越业务化数值天气预报（NWP）的水平，且推理成本远低于后者。许多已部署的MLWM是确定性的，仅产生单一预报而无法估计自身的不确定性；而日益增多的训练型概率模型虽能直接生成经过校准的集合预报，但代价是需要专门的训练过程。我们转而探讨：在不进行重新训练的前提下，能从一个已经存在的确定性检查点中提取出多少不确定性？在物理集合预报通过随机扰动参数化倾向来表示模式不确定性的启发下，我们在推理阶段对网络的原始权重张量进行扰动，并将这一方案称为随机扰动权重（SPW）。我们还研究了该方案是否有效、应在何处以及哪些尺度上注入噪声，以及它在何处会失效。通过在Aurora、GraphCast等四个确定性骨干模型上开展的三阶段消融实验……

    arXiv:2609.08412v1 Announce Type: new  Abstract: Machine-learning weather models (MLWMs) now match or outperform operational numerical weather prediction (NWP) at global medium-range forecasting, at far lower inference cost. Many deployed MLWMs are deterministic, producing a single forecast with no estimate of its own uncertainty, whereas a growing family of trained-probabilistic models generate calibrated ensembles directly, at the price of a dedicated training run. We ask instead how much uncertainty can be extracted from a deterministic checkpoint that already exists, without retraining it. Where physical ensembles represent model uncertainty by stochastically perturbing parametrisation tendencies, we perturb the network's raw weight tensors at inference time, a scheme we call stochastically perturbed weights (SPW). We also ask whether it works, where and on which scales to inject the noise, and where it fails. A three-phase ablation across four deterministic backbones, Aurora, Grap
    
[^72]: 环境作为脚手架：通过丰富反馈引导长程任务中的自进化智能体

    Environments as Scaffold: Enriching Feedback to Bootstrap Self-Evolving Agents in Long-Horizon Tasks

    [https://arxiv.org/abs/2609.08404](https://arxiv.org/abs/2609.08404)

    提出反馈丰富环境（FEEs）这一环境侧适应新范式，通过在探索和演化的后期阶段将反馈从动作引导转向观察丰富，有效缓解了强化学习训练自进化智能体在长程任务中的奖励稀疏问题。

    

    大型语言模型在静态推理方面展现出卓越能力，但通过强化学习（RL）将其训练为能够完成长程任务的自主智能体时，往往受到严重的奖励稀疏性问题的阻碍。虽然传统的通过监督微调（SFT）进行“智能体侧预热”可以缓解这一问题，但通常受限于数据稀缺和探索能力受限。为解决这一问题，我们提出了一种向“环境侧适应”的范式转变，即构建反馈丰富环境（Feedback-Enriched Environments, FEEs）。通过试点研究，我们确立了一种反馈设计策略：在片段内探索和片段间演化的后期阶段，将环境反馈从动作引导转变为观察丰富。我们在SciWorld和BFCL基准上，使用多种规模的Qwen3模型以及GRPO、GSPO、DA等强化学习算法开展了大规模实验。

    arXiv:2609.08404v1 Announce Type: cross  Abstract: Large Language Models demonstrate remarkable proficiency in static reasoning, yet training them as autonomous agents through Reinforcement Learning (RL) for long-horizon tasks is often hindered by severe reward sparsity. While conventional \textit{agent-side warming} up via supervised fine-tuning (SFT) can alleviate this, it is frequently limited by data scarcity and constrained exploration. To address this, we propose a paradigm shift to \textit{environment-side adaptation} by constructing \textbf{F}eedback-\textbf{E}nriched \textbf{E}nvironments (\textbf{FEEs}). Through a pilot study, we establish a feedback design strategy that reformulates environments by transitioning from action guidance to observation enrichment during the later stages of both intra-episode exploration and inter-episode evolution. Large-scale experiments on SciWorld and BFCL benchmarks using various Qwen3 model scales and RL algorithms such as GRPO, GSPO, and DA
    
[^73]: MLIP Detective：面向机器学习原子间势的超越基准分数的主动失效模式发现

    MLIP Detective: Active Failure Mode Discovery Beyond Benchmark Scores for Machine-Learning Interatomic Potentials

    [https://arxiv.org/abs/2609.08399](https://arxiv.org/abs/2609.08399)

    提出了 MLIP Detective 智能体框架，通过生成物理信息驱动的可证伪失效假设并以低成本模拟筛选，主动发现基准测试无法暴露的通用机器学习原子间势的隐藏失效模式，且在无针对性提示的情况下发现了 MACE-MPA-0 模型在含氧/氟吸附物体系中的系统性预测异常。

    

    通用机器学习原子间势（u-MLIPs）旨在跨多种构型进行泛化。基准测试能够实现可复现的评估，但可能无法暴露其预定义范围之外的失效情况。在这里，我们展示了基于物理信息的搜索可以通过揭示隐藏的失效模式来补充基于基准的评估。我们提出了 MLIP Detective，这是一个用于主动失效模式发现的智能体框架。从基准证据出发，MLIP Detective 生成可证伪的、基于物理信息的失效假设，用低成本的模拟对这些假设进行筛选，并仅将最可疑的案例连同建议的验证协议提交给人类专家。在无需针对特定问题进行提示的情况下，MLIP Detective 识别并表征了 MACE-MPA-0 中的一个系统性异常：该模型预测某些涉及含氧或含氟吸附物的弛豫吸附物-表面体系的能量高于其相应的分离体系……

    arXiv:2609.08399v1 Announce Type: new  Abstract: Universal machine-learning interatomic potentials (u-MLIPs) aim to generalize across diverse configurations. Benchmarks enable reproducible evaluation but may not expose failures outside their predefined scope. Here, we show that physics-informed search can complement benchmark-based evaluation by uncovering hidden failure modes. We introduce MLIP Detective, an agentic framework for active failure mode discovery. Starting from benchmark evidence, MLIP Detective generates falsifiable, physics-informed failure hypotheses, screens them with inexpensive simulations, and escalates only the most suspicious cases to human experts together with proposed verification protocols. Without issue-specific prompting, MLIP Detective identified and characterized a systematic anomaly in MACE-MPA-0: the model predicted some relaxed adsorbate-surface systems involving O- or F-containing adsorbates to be higher in energy than their corresponding separated fr
    
[^74]: 等变性打破了学习率

    Equivariance Breaks the Learning Rate

    [https://arxiv.org/abs/2609.08381](https://arxiv.org/abs/2609.08381)

    该论文发现等变网络中不可约表示块的梯度秩受限，而 Adam 的逐元素缩放会破坏块间边界，导致同一层内不同块的谱步长不一致，为此提出了一种无需新超参数的逐块归一化更新方法，仅改变更新尺度而保持矩估计和方向不变。

    

    arXiv:2609.08381v1 公告类型： cross  摘要：等变网络通常使用 Adam 进行训练，但最近有工作报告称，诸如 Muon 这类矩阵结构优化器在这些架构上表现更好，却没有解释原因。我们在等变线性层内部识别出造成这种差异的一个来源。每个不可约表示（irrep）块学习一个在其 2l+1 个分量之间共享的通道混合矩阵 $W_l$，从而形成扩展映射 $W_l \otimes I_{2l+1}$。对于该层的单次应用，$W_l$ 的梯度是 2l+1 个外积贡献的总和，且秩至多为 2l+1。Adam 在不利用不可约表示边界的情况下对存储的权重进行逐个缩放，因此单个学习率可能在同一层内不同块之间产生不同的谱步长。我们通过单独归一化每个块的更新来解决这种不匹配，且不引入新的超参数。这只改变更新的尺度，保持 Adam 的矩估计以及每个块内的更新方向不变。我们评估了……

    arXiv:2609.08381v1 Announce Type: cross  Abstract: Equivariant networks are commonly trained with Adam, yet recent work reports that matrix-structured optimizers such as Muon can perform better on these architectures without explaining why. We identify one source of this difference inside equivariant linear layers. Each irrep block learns a channel-mixing matrix $W_l$ shared across its $2l+1$ components, giving the expanded map $W_l \otimes I_{2l+1}$. For a single application of the layer, the gradient of $W_l$ sums $2l+1$ outer product contributions and has rank at most $2l+1$. Adam rescales stored weights individually without using the irrep boundaries, so one learning rate can produce different spectral step sizes across blocks within a layer. We address this mismatch by normalizing each block update separately, without introducing a new hyperparameter. This changes only the scale of the update, leaving Adam's moment estimates and its direction within each block unchanged. We evalua
    
[^75]: 如何使约束随机极小极大问题的梯度映射变小及其更广泛的问题

    How to Make the Gradient Mapping Small for Constrained Stochastic Min-Max Problems and Beyond

    [https://arxiv.org/abs/2609.08380](https://arxiv.org/abs/2609.08380)

    该论文将约束随机凸-凹极小极大优化和随机单调变分不等式在梯度映射度量下的复杂度从 $\widetilde{O}(\varepsilon^{-4})$ 提升至近最优的 $\widetilde{O}(\varepsilon^{-2})$，并将该结果推广到更一般的问题设定。

    

    我们研究了约束或正则化的凸-凹极小极大优化以及随机单调变分不等式的随机一阶Oracle复杂度。我们关注以梯度映射（也称为前向-后向残差或自然残差）来度量次优性的情形，这是一种将无约束问题的梯度范数概念加以推广的最优性度量。在此设定下，在标准的无偏Oracle访问和目前常见的方差假设下，使梯度映射范数小于 $\varepsilon$ 的已知最优复杂度为 $\widetilde{O}(\varepsilon^{-4})$，而无约束情形下已确立的近最优复杂度为 $\widetilde{O}(\varepsilon^{-2})$。我们弥补了这一差距，将约束凸-凹极小极大问题的梯度映射复杂度改进为 $\widetilde{O}(\varepsilon^{-2})$。随后我们进一步推广，证明了同样复杂度对不具备有界（方差）等假设的更一般问题依然成立。

    arXiv:2609.08380v1 Announce Type: cross  Abstract: We study the stochastic first-order oracle complexity for constrained or regularized convex-concave min-max optimization and stochastic monotone variational inequalities. We focus on the case when suboptimality is measured in terms of the gradient mapping, also known as, forward-backward or natural residual, an optimality notion that generalizes the gradient norm for unconstrained problems. In this setting, under standard unbiased oracle access with now-standard variance assumptions, the best-known complexity for making the norm of the gradient mapping less than $\varepsilon$ is $\widetilde{O}(\varepsilon^{-4})$, compared to the near-optimal $\widetilde{O}(\varepsilon^{-2})$ that is established in the unconstrained case. We bridge this gap to improve the gradient mapping complexity for constrained convex-concave min-max problems to $\widetilde{O}(\varepsilon^{-2})$. We then extend to prove the same complexity for problems without the b
    
[^76]: 地理正则化的AUC最大化个性化联邦学习

    Geographically Regularized AUC-Maximizing Personalized Federated Learning

    [https://arxiv.org/abs/2609.08379](https://arxiv.org/abs/2609.08379)

    该论文提出了GrAUC-PFL方法，通过直接优化平滑的成对AUC替代损失，并结合基于图的正则化使地理相邻机构模型相似，在患者数据不出本地、兼顾机构异质性的前提下学习个性化模型，从而提升传染病诊断与风险预测的判别性能。

    

    准确的诊断和风险预测模型对于在传染病暴发期间支持临床决策至关重要。然而，隐私和治理要求可能限制跨医疗机构的患者级数据共享，且数据分布往往存在差异。此外，AUC被广泛用于评估判别性能，这促使在模型开发中直接对其进行优化。我们提出了地理正则化的AUC最大化个性化联邦学习，该方法直接优化平滑的成对AUC替代函数来学习个性化模型，同时将患者级数据保留在本地并考虑机构间的异质性。基于图的正则化鼓励地理上相邻的机构拥有相似的系数向量，同时保留个性化模型。仿真实验和真实数据应用表明，该方法的判别性能得到提升，尤其是在地理……（原文在此处截断）

    arXiv:2609.08379v1 Announce Type: new  Abstract: Accurate diagnostic and risk-prediction models are important for supporting clinical decision-making during infectious disease outbreaks. However, privacy and governance requirements may restrict patient-level data sharing across healthcare institutions, and data distributions often vary. Moreover, AUC is widely used to evaluate discriminative performance, motivating its direct optimization in model development. We propose geographically regularized AUC-maximizing personalized federated learning (GrAUC-PFL), which directly optimizes a smooth pairwise AUC surrogate to learn personalized models while keeping patient-level data local and accounting for institutional heterogeneity. Graph-based regularization encourages geographically neighboring institutions to have similar coefficient vectors while retaining a personalized models. Simulations and a real-data application suggest improved discriminative performance, particularly when geograph
    
[^77]: IPM-FM：一种具有共识特征选择的工业过程监控基础模型

    IPM-FM: A Foundation Model with Consensus Feature Selection for Industrial Process Monitoring

    [https://arxiv.org/abs/2609.08375](https://arxiv.org/abs/2609.08375)

    本文提出首个面向工业过程监控的基础模型IPM-FM，通过自监督预训练学习通用表征、少量标注数据进行任务适配、并以不确定性感知预测头输出校准预测，突破了传统“一任务一模型”范式在标签效率和工况漂移下的局限。

    

    工业过程监控是现代流程工厂安全运行和经济效益的基础。当前的实践仍然遵循“一个任务一个模型”的范式，这种范式标签利用效率低下，并且在工况漂移下容易出现性能退化。基础模型已经重塑了语言、视觉和通用时间序列预测领域，但尚未被应用于工业过程监控。这一场景带来了领域特有的挑战，包括安全关键决策以及过程变量与实验室测量之间的不对称采样问题。我们提出了工业过程监控基础模型（IPM-FM）。它首先通过自监督预训练从无标签的工业过程数据中学习通用表征，然后利用少量任务标注数据适应特定的监控任务，最后通过不确定性感知的预测头产生经过校准的预测。IPM-FM集成了一个自监督……

    arXiv:2609.08375v1 Announce Type: cross  Abstract: Industrial process monitoring is fundamental to the safety and economic performance of modern process plants. Current practice remains a one-task-one-model paradigm that is label-inefficient and prone to degradation under operating drift. Foundation models have reshaped language, vision, and generic time-series forecasting, but it has not been adapted to industrial process monitoring. This setting poses domain-specific challenges, including safety-critical decisions and asymmetric sampling between process variables and laboratory measurements. We propose the industrial process monitoring foundation model (IPM-FM). It first learns general-purpose representations from unlabeled industrial process data through self-supervised pretraining, then adapts to specific monitoring tasks using a small amount of task-labeled data, and finally produces calibrated predictions through an uncertainty-aware prediction head. IPM-FM integrates a self-supe
    
[^78]: Miles v0.1：生产级后训练系统

    Miles v0.1: Production-Level Post-Training

    [https://arxiv.org/abs/2609.08368](https://arxiv.org/abs/2609.08368)

    Miles v0.1是一个基于slime构建的全栈生产级强化学习后训练系统，以可验证、简洁、可定制的组件为设计原则，支持全参数RL、LoRA RL、在线策略蒸馏等多种训练范式并扩展至扩散模型，使前沿规模的RL训练对研究者和企业都可用。

    

    我们提出了Miles v0.1，一个全栈、生产就绪的前沿后训练系统。Miles基于slime的简洁设计构建，围绕单一原则设计强化学习（RL）训练循环的每个阶段：组件应当经过验证、简洁且可定制。以准确性、效率、可靠性和可扩展性为首要目标，Miles旨在让前沿规模的强化学习对研究人员和企业都触手可及。本报告端到端地介绍该系统：基于SGLang构建的rollout引擎、可选择两种后端（NVIDIA Megatron-LM和PyTorch FSDP）的训练器，以及适用于不同部署拓扑的三种权重同步传输方式。除了全参数强化学习外，Miles还支持LoRA强化学习、在线策略蒸馏、监督微调以及真正的在线策略rollout-训练对齐，并将相同的架构扩展至扩散模型。最后我们以一个端到端的案例研究作结：完全异步……

    arXiv:2609.08368v1 Announce Type: new  Abstract: We present Miles v0.1, a full-stack, production-ready system for frontier post-training. Building upon the clean design of slime, Miles designs each stage of the reinforcement-learning (RL) training loop around a single principle: components should be verified, clean, and customizable. With accuracy, efficiency, reliability, and scalability as first-class goals, Miles aims to make frontier-scale RL accessible to researchers and enterprises alike. This report walks through the system end to end: rollout engines built on SGLang, a trainer with a choice of two backends (NVIDIA Megatron-LM and PyTorch FSDP), and three weight-synchronization transports for different deployment topologies. Beyond full-parameter RL, Miles also supports LoRA RL, on-policy distillation, supervised fine-tuning, and true-on-policy rollout-training alignment, and extends the same architecture to diffusion models. We close with an end-to-end case study: fully asynchr
    
[^79]: 适应还是不适应？视觉语言模型的选择性适应

    To Adapt or Not to Adapt? Selective Adaptation for Vision-Language Models

    [https://arxiv.org/abs/2609.08367](https://arxiv.org/abs/2609.08367)

    本文提出“选择性适应”这一新问题，并设计了跨增强相似度（CAS）方法，通过仅在增强视图间预测相似度较低时才执行适应，从而识别并跳过测试时适应中微不足道或有害的适应操作。

    

    测试时适应（TTA）已成为在推理过程中使视觉语言模型适应分布偏移的一种重要策略。我们对适应前后的模型预测进行了逐样本分析，观察到现有TTA方法中存在两种失效模式，这与先前的研究结果相呼应。适应通常是微不足道的，不会对模型的预测产生任何改变；而更严重的是，适应可能是有害的，会将原本正确的预测翻转为错误的。这自然引出一个问题：我们能否识别并跳过这些微不足道或有害的适应？在本工作中，我们引入了一个新的问题——选择性适应，旨在确定给定的测试样本是否应该进行适应或被跳过。为此，我们提出了跨增强相似度（CAS），这是一个简单的基线方法，仅当跨增强视图的预测表现出低相似度时才执行适应。值得注意的是，CAS不仅能够预先……

    arXiv:2609.08367v1 Announce Type: cross  Abstract: Test-time adaptation (TTA) has emerged as a prominent strategy for adapting vision-language models to distribution shifts during inference. We conduct a per-sample analysis of model predictions before and after adaptation, and observe two failure modes in existing TTA methods that echo previous work. Adaptations are frequently negligible, yielding no change in the model's predictions, and more severely, they can be detrimental by flipping previously correct predictions to incorrect ones. This naturally raises a question: Can we identify and skip such negligible or harmful adaptations? In this work, we introduce a new problem of selective adaptation, which aims to determine whether a given test sample should undergo adaptation or be skipped. To this end, we propose Cross-Augmentation Similarity (CAS), a simple baseline that performs adaptation only when predictions across augmented views exhibit low similarity. Notably, CAS not only pre
    
[^80]: 基于Stein变分梯度下降的Stiefel流形上几何感知贝叶斯参数高效微调

    Geometry-Aware Bayesian Parameter-Efficient Fine-Tuning on the Stiefel Manifold via Stein Variational Gradient Descent

    [https://arxiv.org/abs/2609.08354](https://arxiv.org/abs/2609.08354)

    本文提出一种基于Stein变分梯度下降的几何感知贝叶斯参数高效微调框架，通过沿Stiefel流形传输低秩矩阵以保留正交几何结构，并在推理中提供多个解来缓解过自信问题。

    

    针对大型预训练模型的参数高效微调，已有若干几何感知的低秩适配方法出现。这些方法旨在充分利用低秩流形的几何结构，通过在优化过程中施加正交性约束来提高子空间利用效率并减少冗余。这些技术的出色实证结果引发了进一步的研究问题：此类基于几何的适配方法给出的预测是否可能过于自信。在本文中，我们基于适配器的奇异值分解因子化，开发了一个基于Stein变分梯度下降（SVGD）的框架。在该框架中，低秩矩阵沿Stiefel流形进行传输以匹配目标分布，同时保留其关键的几何结构。由于这种几何感知的SVGD方法在推理过程中能够提供多个解

    arXiv:2609.08354v1 Announce Type: new  Abstract: Several geometry-aware approaches to low-rank adaptation have emerged for parameter-efficient fine-tuning of large pre-trained models. These methods aim to take full advantage of the geometric structure of low-rank manifolds for improving the efficiency in subspace utilization and reducing redundancy by enforcing orthogonality constraints during optimization. The strong empirical results of these techniques have motivated further study into whether predictions from such geometry-based adaptation methods could be overconfident. In this paper, we build on the singular value decomposition factorization of adapters to develop a framework based on Stein variational gradient descent (SVGD). In this formulation, the low-rank matrices are transported along the Stiefel manifold to match the targeted distributions while retaining their crucial geometric structure. Since this geometry-aware SVGD approach provides multiple solutions during inference
    
[^81]: CoVeR：面向视觉语言模型多视角3D推理的基于覆盖度的Token剪枝方法

    CoVeR: Coverage-Based Token Pruning for Multi-View 3D Reasoning in VLMs

    [https://arxiv.org/abs/2609.08345](https://arxiv.org/abs/2609.08345)

    提出CoVeR，一种无需训练、仅依据token坐标的空间覆盖度进行确定性视觉token剪枝的方法，解决多视角3D推理中冗余token过多及现有剪枝方法覆盖不足的问题。

    

    将3D场景表示为多视角图像，可以让2D视觉语言模型通过复用预训练先验来进行3D推理，从而规避标注3D数据稀缺的问题。然而，这种方式会产生数千个冗余的视觉token，其成本随视角数量不断增长。现有的视觉token剪枝方法分为两类，在3D多视角场景下各有局限。基于学习的重要性方法通过注意力或编码器特征对token进行排序；由于这类场景中的冗余本质上是空间性的，这些方法会保留来自少数显著区域的近似重复token，导致场景的大部分区域未被表示。体素化方法虽改善了空间覆盖度，但无法强制执行精确的token预算，且随着多视角观测在3D空间中重叠而饱和，使保留率远低于目标。我们证明了空间覆盖度与3D推理性能相关，并提出了CoVeR，一种确定性的、无需训练的选择器，仅使用token坐标，无需任何学习信号。

    arXiv:2609.08345v1 Announce Type: cross  Abstract: Representing a 3D scene as multi-view images allows 2D VLMs to reason in 3D by reusing priors from pre-training, sidestepping the scarcity of annotated 3D data. However, it produces thousands of redundant visual tokens whose cost grows with every view. Existing visual token pruners fall into two families, each limited in the 3D multi-view setting. Learned importance methods rank tokens by attention or encoder features; because redundancy here is fundamentally spatial, they keep near-duplicate tokens from a few prominent regions and leave most of the scene unrepresented. Voxelization methods improve spatial coverage but cannot enforce an exact token budget and saturate as multi-view observations overlap in 3D, capping retention well below the target. We show that spatial coverage is associated with 3D reasoning performance and introduce CoVeR, a deterministic, training-free selector that uses only token coordinates, with no learned sign
    
[^82]: TV正则化的OPD：方向在在策略蒸馏中至关重要

    TV-Regulated OPD: Direction Matters in On-Policy Distillation

    [https://arxiv.org/abs/2609.08341](https://arxiv.org/abs/2609.08341)

    该论文发现在在策略蒸馏中只有token级优势的方向（符号）才真正关键，并提出利用全变差（TV）正则化来塑造有界且平滑的优势函数，从而实现更稳定的训练过程并获得相当的性能。

    

    在策略蒸馏在大型语言模型（LLM）的后训练阶段促进从领域专家模型向学生模型的知识转移。然而，主流OPD方法中的监督信号存在高方差和噪声问题，在训练过程中通常不稳定。在本工作中，我们系统研究了究竟什么因素真正影响性能，以及训练不稳定性背后的根本机制。我们发现，仅保留token级优势的符号就足以实现与标准OPD相当的性能。同时，更平滑且有界的优势函数可以在不牺牲性能的前提下稳定训练过程。这促使我们使用全变差来塑造优势函数，并提出了一种鲁棒的TV正则化在策略蒸馏方法。得益于有界且递减的优势函数，TV-OPD表现出稳定的训练动态……

    arXiv:2609.08341v1 Announce Type: new  Abstract: On-Policy Distillation (OPD) facilitates the transfer of knowledge from domain expert to student in the post-training phase of Large Language Models (LLMs). However, the supervision signals in mainstream OPD methods suffer from high variance and noise which is generally instable during training. In this work, we systematically investigated what really matters to the performance and the fundamental mechanisms behind the instability during training. We found that retaining only the sign of token-level advantages is sufficient to achieve the performance comparable to standard OPD. Meanwhile, smoother and bounded advantages can stabilize the training process without sacrificing its performance. These motivated us to shape the advantages using the Total Variation (TV) and propose a robust TV regulated On-Policy Distillation (TV-OPD) method. Benefiting from the bounded and diminished advantages, TV-OPD exhibits stable training dynamics and ste
    
[^83]: 蒸馏即概率传输：路由化在线策略蒸馏

    Distillation as Probability Transport: Routed On-Policy Distillation

    [https://arxiv.org/abs/2609.08337](https://arxiv.org/abs/2609.08337)

    该论文提出RouteOPD，将在线策略蒸馏重新表述为教师引导的概率传输问题，通过将师生分歧分解为概率过剩源与亏缺目的地并构建显式传输对，实现了更精确的token级概率再分配与更新幅度控制。

    

    在线策略蒸馏（OPD）在学生模型生成的轨迹上迁移教师模型的知识，但高效的采样式目标会将教师分布简化为单个token上的标量信用。这种信用仅能指示某个token应该增加还是减少概率，却未指明相应的概率再分配方式。我们将OPD重新表述为教师引导的概率传输问题，并提出RouteOPD（路由化在线策略蒸馏），它将局部的教师-学生分歧分解为学生概率过剩的源和教师概率亏缺的目的地，并将二者耦合为显式的传输对。RouteOPD基于有界教师势所导出的联合可实现目标来优化成对对数几率，同时根据教师需求的集中程度自适应调整传输预算。这一公式化方法将更新引导至教师偏好的目的地，并在单个传输算子内控制更新的幅度。在四个教师模型上的实验……

    arXiv:2609.08337v1 Announce Type: new  Abstract: On-policy distillation (OPD) transfers teacher knowledge on student-generated trajectories, but efficient sampled objectives reduce the teacher distribution to scalar credit on individual tokens. Such credit indicates whether a token should gain or lose probability, yet leaves the corresponding redistribution unspecified. We recast OPD as teacher-guided probability transport and propose RouteOPD (Routed On-Policy Distillation), which decomposes local teacher--student disagreement into student-excess sources and teacher-deficit destinations and couples them into explicit transport pairs. RouteOPD optimizes pairwise log-odds toward jointly realizable targets obtained from a bounded teacher potential, while adapting the transport budget to the concentration of teacher demand. This formulation directs updates toward teacher-preferred destinations and controls their magnitude within a single transport operator. Experiments across four teacher
    
[^84]: 用于生成可变尺寸3D分子的固定维度潜在流

    Fixed-Dimensional Latent Flow for Generating Variable-Size 3D Molecules

    [https://arxiv.org/abs/2609.08333](https://arxiv.org/abs/2609.08333)

    该论文提出了一种两阶段生成框架，通过单一固定维度的分子级潜在表示结合自回归Transformer解码器，实现了无需预先指定分子大小的可变尺寸3D分子生成，并摒弃了等变层和密集成对键解码器的需求。

    

    在分子发现中，分子大小与组成、结构及其他目标性质相互耦合。然而，大多数3D生成器需要在生成前预先指定分子的大小。在此，我们提出了无等变Transformer自编码潜在流匹配，这是一个两阶段生成框架，完全依赖于单一固定维度的分子级潜在表示来生成可变尺寸的分子。第二阶段的流匹配模型对该潜在向量进行采样，随后自回归Transformer解码器在生成原子类型、坐标和化学信息状态的同时确定分子大小。规范的原子排序和刚体姿态对齐使得无需等变层的标准Transformer成为可能，而分子几何结构与丰富化学状态的联合解码实现了可靠、确定性的、化学引导的图恢复，无需学习密集的成对键解码器。

    arXiv:2609.08333v1 Announce Type: cross  Abstract: In molecular discovery, molecule size is coupled to composition, structure, and other target properties. Yet most 3D generators require molecule size to be specified before generation. Here, we introduce Equivariant-Free Transformer-Autoencoded Latent Flow Matching, a two-stage generative framework that relies entirely on a single fixed-dimensional molecule-level latent representation to generate variable-size molecules. The second-stage flow matching model samples this latent vector, and an autoregressive Transformer decoder then determines molecule size while generating atom types, coordinates, and chemically informative states. Canonical atom ordering and rigid-pose alignment enable standard Transformers without equivariant layers, while joint decoding of molecular geometry and an enriched chemical state enables reliable, deterministic, chemistry-guided graph recovery without requiring a learned dense pairwise bond decoder. The same
    
[^85]: EMBLEM：通过掩码技术增强多文字系统表格检测

    EMBLEM: Enhancing Multi-script Table Detection through Masking

    [https://arxiv.org/abs/2609.08330](https://arxiv.org/abs/2609.08330)

    该论文推出了包含18种语言、15种文字系统的人工标注数据集MANDALA，并提出基于掩码的EMBLEM方法，通过隐藏文字和字体特征使英文预训练模型能够有效泛化到多文字系统的表格检测任务。

    

    表格检测是文档分析中的一项核心任务，为信息检索、文档重建和视觉问答等下游应用提供支持。尽管现有的深度学习模型在英文和中文文档上表现良好，但由于文字系统的多样性以及标注数据的有限性，它们在多语言、多文字系统的文档上表现不佳。为了应对这一挑战，我们推出了MANDALA（用于表格检测的多文字系统标注文档），这是一个人工整理的数据集，包含2,323个含表格的页面，涵盖18种语言和15种文字系统，跨越多个不同领域。我们还提出了EMBLEM，这是一种基于掩码的多文字系统表格检测（MTD）范式。EMBLEM通过生成掩码图像来隐藏与文字系统和字体相关的特定细节，使在大量英文文档上预训练的模型能够专注于与文字系统无关的页面布局。在三种表格检测架构上的实验表明，EMBLEM持续取得优异表现……

    arXiv:2609.08330v1 Announce Type: new  Abstract: Table detection is a core task in document analysis, supporting downstream applications such as information retrieval, document reconstruction, and visual question answering. While existing deep learning models perform well on English and Chinese documents, they struggle with multilingual, multi-script documents due to script diversity and the limited availability of labeled data. To address this challenge, we introduce MANDALA (Multi-script Annotated Documents for Table Detection), a manually curated dataset of 2,323 table-containing pages spanning 18 languages and 15 scripts across diverse domains. We also propose EMBLEM, a masking-based paradigm for Multi-script Table Detection (MTD). EMBLEM generates masked images that conceal script- and font-specific details, enabling models pre-trained on abundant English documents to focus on script-agnostic page layout. Experiments across three table detection architectures show that EMBLEM cons
    
[^86]: 非相干空中联邦学习：协议、收敛性与设备调度

    Non-Coherent Over-the-Air Federated Learning: Protocol, Convergence, and Device Scheduling

    [https://arxiv.org/abs/2609.08312](https://arxiv.org/abs/2609.08312)

    本文提出了一种非相干空中联邦学习协议NCAirFL，通过二进制抖动、无偏非相干检测和长期误差反馈免除对瞬时信道状态信息的依赖，并给出了收敛性分析与设备调度方案。

    

    为了缓解联邦边缘学习（FEEL）中无线接入网络（RAN）的可扩展性瓶颈，空中联邦学习（AirFL）利用多址信道（MAC）上的波形叠加进行模拟模型聚合。然而，相干AirFL通常依赖于严格的物理层条件，例如精确的信道状态信息（CSI）、严格的时间/频率同步，以及频繁的收发器校准以实现信号对齐。这些要求即使并非无法满足，也会带来大量的通信和计算开销。在本文中，我们提出了一种基于宽带单天线多址信道的非相干AirFL（NCAirFL）协议，利用二进制抖动、无偏非相干检测和长期误差反馈，从而免除对瞬时CSI的需求。对于具有一般平滑非凸目标和恒定学习率的NCAirFL，我们建立了可达到……的收敛界（摘要在此处截断）。

    arXiv:2609.08312v1 Announce Type: cross  Abstract: To mitigate the scalability bottleneck in the radio access network (RAN) in federated edge learning (FEEL), over-the-air federated learning (AirFL) exploits waveform superposition over multiple-access channels (MACs) for analog model aggregation. However, coherent AirFL typically relies on stringent PHY-layer conditions such as accurate channel state information (CSI), tight time/frequency synchronization, and frequent transceiver calibration for signal alignment. However, these requirements, if not impossible to be met, incur substantial communication and computation overhead. In this paper, we propose a non-coherent AirFL (NCAirFL) protocol over a broadband single-antenna MAC, leveraging binary dithering, unbiased non-coherent detection, and long-term error feedback to waive the need for instantaneous CSI. For NCAirFL with general smooth non-convex objectives and a constant learning rate, we establish a convergence bound achieving th
    
[^87]: 边缘连续体硬件上大语言模型推理权衡的测量研究

    A Measurement Study of LLM Inference Trade-offs Across Edge Continuum Hardware

    [https://arxiv.org/abs/2609.08307](https://arxiv.org/abs/2609.08307)

    本文通过对边缘与近边缘硬件上多种开源大语言模型推理的受控测量研究，发现GPU服务器提供最低延迟而Jetson Orin能耗更低，为大语言模型边缘部署中的质量、延迟与能耗权衡提供了实证依据。

    

    大语言模型（LLM）越来越多地被用作智能网络服务的后端，但在边缘连续体上部署它们需要在质量、延迟、模型占用空间和能耗之间进行权衡。本文对自托管的大语言模型推理在边缘和近边缘部署节点上进行了受控测量研究，节点包括一台NVIDIA Jetson AGX Orin以及一台支持纯CPU和GPU推理模式的近边缘服务器。我们使用固定的问答工作负载评估了多个开源权重的大语言模型及其量化变体，并将云端托管的GPT-4o作为准确性和延迟的参考基准。我们的基准测试流程报告了准确性、模型占用空间、每token解码延迟、预填充延迟和整体执行能耗。结果表明，启用GPU的服务器执行提供了最低的计算端延迟，而Jetson Orin显示出更低的实测能耗，这与其在实验设置下较低的平台功耗相一致。

    arXiv:2609.08307v1 Announce Type: cross  Abstract: Large language models (LLMs) are increasingly used as backends for intelligent web services, but serving them across the edge continuum requires balancing quality, latency, model footprint, and energy. This paper presents a controlled measurement study of self-hosted LLM inference across edge and near-edge deployment nodes: an NVIDIA Jetson AGX Orin and a near-edge server with CPU-only and GPU-enabled inference modes. We evaluate multiple open-weight LLMs and quantization variants using a fixed question-answering workload, and compare them against GPT-4o as a cloud-hosted accuracy and latency reference. Our benchmarking pipeline reports accuracy, model footprint, per-token decoding latency, prefill latency, and overall execution energy. The results show that GPU-enabled server execution provides the lowest compute-side latency, while Jetson Orin shows lower measured energy, consistent with its lower platform power under our setup. CPU-
    
[^88]: HoneyRoute：面向对抗性LLM服务的蜜罐模型路由

    HoneyRoute: Honeypot-Model Routing for Adversarial LLM Serving

    [https://arxiv.org/abs/2609.08306](https://arxiv.org/abs/2609.08306)

    HoneyRoute是一个部署在推理服务层的防御框架，通过轻量级流式路由器实时检测恶意请求并将其路由至蜜罐模型，在保护生产LLM服务的同时，将捕获的攻击者交互转化为指纹数据用于持续改进路由器的检测能力。

    

    我们提出了HoneyRoute，一个推理服务层，它能够检测传入请求是否为恶意请求，若检测到恶意，则将其路由到专用的蜜罐模型，从而保护生产系统，同时持续收集对手交互信息以获取情报。现有的防御方法要么在模型记忆中嵌入陷阱，要么在协议层重建欺骗机制，这使得服务层缺乏保护，且无法将捕获的信息反馈用于检测。HoneyRoute结合了三个组件：(i) 一个流式路由器（采用冻结的0.8B参数嵌入骨干网络，配备各领域的MLP头），(ii) 双实现的蜜罐（规则/提示词工程化的代码蜜罐，或专用的同族模型副本），以及 (iii) 一个分析循环，将被捕获的交互转化为攻击者指纹，用于路由器的再训练。在生产环境追踪数据和七个领域的攻击语料库上，该路由器达到F1=.911，中位附加延迟仅38毫秒，以两级防护LLM级联系统1/385的延迟实现了其96%的F1分数。

    arXiv:2609.08306v2 Announce Type: replace-cross  Abstract: We introduce HoneyRoute, an inference-serving layer that detects whether an incoming request is malicious and, if so, routes it to a dedicated honeypot model, shielding production while the adversary's interaction is continuously harvested for intelligence. Existing defenses embed traps inside model memory or rebuild deception at the protocol layer, leaving the serving tier unprotected and feeding nothing back into detection. HoneyRoute couples (i) a streaming router (a frozen 0.8B-embedding backbone with per-domain MLP heads), (ii) a dual-implementation honeypot (a rule/prompt-engineered code honeypot or a dedicated same-family replica), and (iii) an analysis loop that converts trapped interactions into attacker fingerprints for router retraining. On a production trace plus a seven-domain attack corpus, the router reaches F1=.911 at 38 ms median added latency, matching 96% of a two-tier guard-LLM cascade's F1 at 1/385 of its l
    
[^89]: HypLTSF：用于长期时间序列预测的多尺度层次结构的双曲几何视角

    HypLTSF: A Hyperbolic Geometric View of Multi-Scale Hierarchies for Long-Term Time Series Forecasting

    [https://arxiv.org/abs/2609.08286](https://arxiv.org/abs/2609.08286)

    本文提出HypLTSF框架，通过将多尺度时间表征嵌入庞加莱球（双曲空间），首次以显式的双曲几何结构刻画长期时间序列预测中的尺度层次关系，使层次结构从交互的副产品变为被明确建模的几何对象。

    

    多尺度建模已成为长期时间序列预测的有效方法，能够捕捉从细粒度局部动态到粗粒度全局趋势的各种时间模式。这些时间尺度上的表征本质上具有层次性，较粗的尺度从较细的尺度中抽象和聚合信息。虽然现有方法可以轻松地在这些尺度之间交换信息，但层次结构本身通常只是这种交互作用的涌现副产品，而并未作为一种独立的几何结构被显式捕捉。在本文中，我们提出了HypLTSF，这是一个通过将逐尺度表征嵌入到庞加莱球中来赋予多尺度层次结构具体几何形式的框架，庞加莱球呈指数级扩张的体积天然适合容纳层次结构。为了使这种几何结构与时间层次结构对齐，HypLTSF施加了两个约束：(1) 一个径向约束……

    arXiv:2609.08286v1 Announce Type: new  Abstract: Multi-scale modeling has become an effective approach for long-term time series forecasting, capturing temporal patterns that range from fine-grained local dynamics to coarse global trends. Representations across these temporal scales are inherently hierarchical, with coarser scales abstracting and aggregating information from finer ones. While existing approaches readily exchange information across these scales, the hierarchy itself is typically left as an emergent byproduct of such interactions rather than captured as a geometric structure in its own right. In this paper, we introduce HypLTSF, a framework that endows the multi-scale hierarchy with a concrete geometric form by embedding scale-wise representations into the Poincar\'e ball, whose exponentially expanding volume naturally accommodates hierarchical structures. To align this geometry with the temporal hierarchy, HypLTSF imposes two constraints: (1) a radial constraint that or
    
[^90]: 自适应地将方向提示融入零阶优化

    Adaptively Incorporating Directional Hints into Zeroth-Order Optimization

    [https://arxiv.org/abs/2609.08277](https://arxiv.org/abs/2609.08277)

    提出CV-ZOD框架，利用控制变量自适应地将廉价的方向提示融入零阶优化，使收敛速率能根据提示质量在 O(1/T) 一阶速率与 O(d/T) 零阶速率之间平滑插值，且实用变体无需任何先验知识。

    

    我们研究在方向提示辅助下的非凸函数零阶优化问题。方向提示是真实梯度方向的廉价但可能不准确的近似，在每次迭代中以线性子空间的形式给出。为了自适应地利用这些提示同时保持对其质量的鲁棒性，我们引入了控制变量零阶下降，这是一个新框架，它通过一个可基于方向提示设置的控制变量来改进经典的零阶梯度估计器。我们首先证明，在每次迭代中最优设置参考向量和步长的先知算法所达到的收敛速率，会根据提示在轨迹上的质量，在 O(1/T) 的一阶速率与 O(d/T) 的零阶速率之间进行插值。随后，我们开发了CV-ZOD的一个实用变体，该变体无需任何先验知识，即可在对数因子范围内达到与先知算法相同的保证。

    arXiv:2609.08277v1 Announce Type: new  Abstract: We study zeroth-order optimization of non-convex functions with the aid of directional hints, which are cheap but potentially inaccurate approximations of the true gradient direction, given by linear subspaces at each iteration. To leverage these hints adaptively while maintaining robustness to their quality, we introduce Control-Variate Zeroth-Order Descent (CV-ZOD), a new framework that refines the classical zeroth-order gradient estimator with a control variate that can be set based on the directional hints. We first show that the oracle algorithm that optimally sets the reference vector and step size at each iteration achieves a convergence rate that interpolates between the first-order $O(1/T)$ rate and the zeroth-order $O(d/T)$ rate, depending on the quality of the hints along the trajectory. We then develop a practical variant of CV-ZOD that achieves the same oracle guarantee up to logarithmic factors, without any prior knowledge 
    
[^91]: 基于增强路径签名与T-Mamba的在线签名验证

    Online Signature Verification Using Augmented Path Signature and T-Mamba

    [https://arxiv.org/abs/2609.08276](https://arxiv.org/abs/2609.08276)

    本文提出了一种将增强路径签名（APS）描述符与结合TCN块和时间扫描Mamba的T-Mamba模型相融合的新型框架，有效解决了在线签名验证中判别性特征捕捉和长程依赖建模的难题。

    

    手写签名验证对于商业和金融应用中的个人身份认证至关重要。尽管深度学习方法已被广泛应用于在线签名验证（OSV），但它们往往难以捕捉高度判别性的特征以及建模长程依赖关系。为了解决这些问题，我们提出了一种将增强路径签名（APS）描述符与T-Mamba模型相结合的新型框架。APS描述符首先应用时间和基准点增强，然后计算滑动窗口路径签名。路径签名是源自粗糙路径理论的一种非参数特征映射，能够有效捕捉几何结构和通道间的非线性交互。受状态空间模型（SSM）在序列建模中出色表现的启发，我们的T-Mamba模型采用混合设计，将两个时间卷积网络（TCN）块与时间扫描Mamba相结合。

    arXiv:2609.08276v1 Announce Type: new  Abstract: Handwritten signature verification is vital for personal authentication across commercial and financial applications. Although deep learning methods are widely adopted for online signature verification (OSV), they often struggle with capturing highly discriminative features and modelling long-range dependencies. To address these issues, we propose a novel framework that integrates the augmented path signature (APS) descriptor with the T-Mamba model. The APS descriptor first applies time and basepoint augmentations, then computes sliding-window path signatures. The path signature is a non-parametric feature map from rough path theory that effectively captures geometric structures and nonlinear inter-channel interactions. Inspired by the efficacy of state space models (SSMs) in sequence modelling, our T-Mamba model employs a hybrid design combining two temporal convolutional network (TCN) blocks with a time-scanning Mamba. This design enab
    
[^92]: 城市区域嵌入中移动性的拓扑结构与时间语义协同融合

    Synergistic Fusion of Topological Structure and Temporal Semantics of Mobility for Urban Region Embedding

    [https://arxiv.org/abs/2609.08268](https://arxiv.org/abs/2609.08268)

    提出MoSS方法，协同融合移动数据的时序语义（每小时流入/流出序列视图）与拓扑结构（结构视图），克服现有方法忽视时间动态以及加性融合丢失联合信号的缺陷，生成更优的城市区域嵌入。

    

    城市区域嵌入在犯罪、收入和服务呼叫预测等多种城市感知任务中已展现出良好的效果。最近的方法通过将移动性数据与辅助模态相结合，利用跨视图注意力或对比学习目标将异构特征对齐为统一的区域表示，从而提升表示质量。然而，如何利用人类移动性的时间动态仍缺乏充分探索。区域的流入和流出在一天中不断波动，区域间的连接也随时间推移而出现、持续和消解。此外，主流的融合策略以相加的方式组合各视图，错失了仅在视图共同出现时才会涌现的联合信号。为解决这些不足，我们提出了移动性流-结构协同方法（MoSS），从移动性数据中提取互补的视图：一个保留每个区域每小时流入/流出概况的序列视图，以及一个基于之字形持续性的结构视图……

    arXiv:2609.08268v1 Announce Type: cross  Abstract: Urban region embeddings have shown promising results in diverse urban sensing tasks such as crime, income, and service-call prediction. Recent methods improve representation quality by integrating mobility data with auxiliary modalities, using cross-view attention or contrastive objectives to align heterogeneous features into a unified region representation. However, leveraging the temporal dynamics of human mobility remains under-explored. Regional inflow and outflow fluctuate throughout the day, and inter-region connections emerge, persist, and dissolve over time. Moreover, prevailing fusion strategies combine views additively and miss the joint signal that emerges only when views co-occur. To address these gaps, we propose Mobility Stream-Structure Synergy (MoSS), which derives complementary views from mobility data: a Sequence view that preserves each region's hourly inflow/outflow profile, and a Structure view based on zigzag pers
    
[^93]: 重新审视生成式扩散模型中的谱表示

    Revisiting Spectral Representations in Generative Diffusion Models

    [https://arxiv.org/abs/2609.08253](https://arxiv.org/abs/2609.08253)

    该论文从扰动核的共同视角揭示了自监督谱表示学习与扩散生成模型之间的内在联系，并提出一种自监督谱表示对齐方法，以加速扩散模型训练并提升采样质量。

    

    扩散模型在多种生成任务上展现出了卓越的性能。最近的研究发现，对扩散网络的隐藏状态施加表示对齐，既能促进训练收敛，又能提升采样质量，但驱动这种协同效应的机制仍未被充分理解。在本文中，我们通过扰动核这一共同视角，研究了自监督谱表示学习与扩散生成模型之间的联系。在扩散模型方面，样本（例如图像、视频）是通过逆转由高斯核定义的随机噪声注入过程而生成的；在谱表示方面，谱嵌入则源于对由随机扰动核诱导的正负关系进行对比学习。受此启发，我们提出了一种自监督谱表示对齐方法来促进扩散模型的训练。（注：原文摘要在此处被截断）

    arXiv:2609.08253v1 Announce Type: new  Abstract: Diffusion models have shown remarkable performance on diverse generation tasks. Recent work finds that imposing representation alignment on the hidden states of diffusion networks can both facilitate training convergence and enhance sampling quality, yet the mechanism driving this synergy remains insufficiently understood. In this paper, we investigate the connection between self-supervised spectral representation learning and diffusion generative models through a shared perspective on perturbation kernels. On the diffusion side, samples (e.g., images, videos) are produced by reversing a stochastic noise-injection process specified by Gaussian kernels; on the spectral representation side, spectral embeddings emerge from contrasting positive and negative relations induced by random perturbation kernels. Motivated by this, we propose a self-supervised spectral representation alignment method to facilitate diffusion model training. In addit
    
[^94]: CUNO：面向大规模删除下稳定图遗忘的课程学习与偏好优化

    CUNO: Curriculum and Preference Optimization for Stable Graph Unlearning under Mass Deletion

    [https://arxiv.org/abs/2609.08244](https://arxiv.org/abs/2609.08244)

    提出CUNO框架，通过按遗忘难度对样本排序并分阶段渐进移除，结合分布级负偏好优化目标，有效缓解了大规模删除下图遗忘中的灾难性遗忘问题。

    

    图遗忘旨在无需从头重新训练的情况下，从已训练的图模型中移除指定训练数据的影响。然而，现有方法在大删除比例（大规模删除）下会出现模型效用急剧下降的现象，我们将其称为“灾难性遗忘”。我们发现一个关键原因是对所有被删除样本采取统一处理方式，这在图学习中尤其有害：结构依赖性导致不同节点在已学习模型中扮演着截然不同的角色，但现有方法却对整个遗忘集应用相同的遗忘操作。基于这一洞察，我们提出了CUNO，一个基于课程学习的图遗忘框架，它将遗忘样本按估计的遗忘难度排序，在多个阶段中渐进式地移除遗忘集。CUNO进一步在每个课程阶段采用分布级的负偏好优化（NPO）目标，引导模型远离……（原文摘要此处截断）

    arXiv:2609.08244v1 Announce Type: cross  Abstract: Graph unlearning removes the influence of designated training data from a trained graph model without retraining from scratch. However, existing methods suffer a sharp drop in model utility under large deletion ratios (mass deletion), a phenomenon we refer to as catastrophic unlearning. We find that a key cause is the uniform treatment of all deleted samples, which is particularly damaging in graph learning: structural dependencies cause different nodes to play vastly different roles in the learned model, yet existing methods apply the same forgetting operation to the entire forget set. Based on this insight, we propose CUNO, a curriculum-based graph unlearning framework that removes the forget set progressively, ordering samples by their estimated unlearning difficulty across multiple stages. CUNO further employs a distribution-level negative preference optimization (NPO) objective at each curriculum stage that steers the model away f
    
[^95]: 关于变点数量的分布无关推断

    Distribution-free inference on the number of changepoints

    [https://arxiv.org/abs/2609.08234](https://arxiv.org/abs/2609.08234)

    本文证明了对变点数量K构造分布无关上置信界是不可能的，并提出了基于共形p值的有限样本有效下置信界CLOCC，在数据段仅满足内部可交换且相互独立的温和假设下具有普适性。

    

    假设我们给定一个有序的独立数据序列，其分布在未知位置发生变化K次，其中K ≥ 0为未知数。在本文中，我们研究对K进行分布无关推断的问题。首先，我们展示了一个不可能性结果：任何关于K的分布无关上置信界必定是平凡的且无信息量的。然后，利用共形p值，在仅假设由变点产生的数据段（内部）可交换且相互独立的条件下，我们构建了一个有限样本有效的关于K的下置信界，我们称之为变点数量的共形下界。我们证明在所述假设下，CLOCC是提供K下界的唯一可行方法，我们将这一性质称为其普适性。我们为选择能够产生高效且紧密下界的评分函数提供了实用指南。我们评估了CLOCC

    arXiv:2609.08234v1 Announce Type: cross  Abstract: Suppose we are given an ordered sequence of independent data whose distribution changes $K$ times at unknown locations, for some unknown $K \geq 0$. In this paper, we study the problem of performing distribution-free inference on $K$. First, we show an impossibility result: any distribution-free upper confidence bound on $K$ must be trivial and uninformative. Then, using conformal $p$-values, and under only the assumption that the data segments induced by the changepoints are exchangeable (within themselves) and mutually independent, we construct a finite-sample valid lower confidence bound on $K$, which we call the Conformal LOwer bound on Changepoint Count (CLOCC). We show that CLOCC is the only feasible way to provide a lower bound on $K$ under the stated assumptions, a property we refer to as its universality. We provide practical guidelines for choosing score functions that yield efficient and tight lower bounds. We evaluate CLOCC
    
[^96]: 使用LSTM的历史感知离线强化学习进行高密度版图布线

    Routing Dense Layouts with History-Aware Offline Reinforcement Learning using LSTM

    [https://arxiv.org/abs/2609.08232](https://arxiv.org/abs/2609.08232)

    该论文提出了一种结合轻量级LSTM架构的历史感知离线强化学习策略，通过保留布线迭代的序列上下文来预测迭代成本权重，从而显著改善高密度设计中的详细布线收敛性。

    

    由于设计规则的复杂性不断增加，详细布线仍然是物理设计中主要的运行时瓶颈。现代布线器在高密度工作条件下往往难以解决持续存在的违例。虽然最近的工作利用强化学习（RL）为每次布线迭代动态选择成本，但我们发现该技术在高密度设计中表现不佳，因为这类设计的布线解决方案明显更加困难。为了解决这个问题，我们提出了一种历史感知的离线强化学习策略，该策略通过利用布线器中易于获得的特征，在这些密集场景下预测迭代成本权重，以提高跨布局密度的收敛性。我们的策略与之前的工作类似，采用保守Q学习；然而，我们的关键洞察是，添加轻量级LSTM架构和额外特征可以保留序列上下文，并在多种密度和布线引导质量下改善布线收敛性。

    arXiv:2609.08232v1 Announce Type: cross  Abstract: Detailed routing remains a dominant runtime bottleneck in physical design due to increasing complexity of design rules. Modern routers can struggle to resolve persistent violations under dense operating conditions. While recent work leverages reinforcement learning (RL) to dynamically select costs for each routing iteration, we find that this technique struggles with high-density designs where routing solutions are significantly harder. To address this, we present a history-aware offline RL policy which predicts iterative cost weights in these dense regimes to improve convergence across placement densities by utilizing readily available features from the router. Our policy uses conservative Q-learning similarly to prior work; however, our key insight is that addition of a lightweight LSTM architecture and additional features can retain sequence context and improve routing convergence across multiple densities and route guide qualities.
    
[^97]: ActionSplice：面向交互式世界模型的即时动作编辑

    ActionSplice: In-Flight Action Editing for Interactive World Models

    [https://arxiv.org/abs/2609.08230](https://arxiv.org/abs/2609.08230)

    ActionSplice提出了一种基于反事实状态传输的推理框架，使分块自回归世界模型能够在采样过程中即时修改动作，无需等待下一分块或触发代价高昂的回滚重算。

    

    分块自回归视频世界模型通常将每个生成的分块条件于一个动作之上。因此，在采样过程中接收到的动作必须等待下一个分块，或者使未来的求解器评估以在前一动作下产生的状态为条件，或者触发回滚并重复已完成的评估。我们提出了ActionSplice，一个将该问题形式化为反事实状态传输的推理框架。一个轻量级校正器在相同的求解器步骤处，将中断的主干原生表示向由修改后动作所诱导的匹配状态进行传输。世界模型和采样器保持冻结，采样在无需重放已完成评估的情况下得以恢复。重定向变体CST_R会更新整个活动分块，而时间拼接变体CST_T则保留时间前缀并仅更新后缀。在minWM-Wan Action2V和HY-WM1.5上，CST_R减少

    arXiv:2609.08230v1 Announce Type: cross  Abstract: Chunk-autoregressive video world models typically condition each generated chunk on one action. An action received during sampling must therefore wait for the next chunk, condition future solver evaluations on a state produced under the previous action, or trigger rollback that repeats completed evaluations. We introduce ActionSplice, an inference framework that formulates this problem as Counterfactual State Transport (CST). A lightweight corrector transports the interrupted backbone-native representation toward the matched state induced by the revised action at the same solver step. The world model and sampler remain frozen, and sampling resumes without replaying completed evaluations. The retargeting variant $\mathrm{CST}*{R}$ updates the entire active chunk, while the temporal-splicing variant $\mathrm{CST}*{T}$ preserves a temporal prefix and updates only the suffix. Across minWM-Wan Action2V and HY-WM1.5, $\mathrm{CST}*{R}$ reduc
    
[^98]: 随机学习动力学中信息获取的速度极限

    Speed Limit for Information Acquisition in Stochastic Learning Dynamics

    [https://arxiv.org/abs/2609.08219](https://arxiv.org/abs/2609.08219)

    本研究将随机梯度下降建模为马尔可夫随机过程，推导出费希尔信息流的速度极限，定量约束了可训练参数获取数据中潜在变量信息的速率，并将信息流分解为漂移（确定性学习力）与噪声（SGD涨落）两部分贡献。

    

    神经网络通过学习获取内部表示。在本工作中，我们将随机梯度下降（SGD）表述为一个马尔可夫随机过程，并推导出一个费希尔信息流速度极限，该极限约束了可训练参数从数据生成过程中的潜在变量获取信息的速率。所得不等式将信息流分解为漂移与噪声两部分贡献，从而从信息论视角量化了确定性学习力和SGD诱导的涨落所起的作用。我们在可解析求解的基函数线性回归中验证了该界限，其中由该界限预测的信息预算再现了不同潜在变量在习得参数中被编码的先后次序及特征时间尺度。这些结果将费希尔信息速度极限确立为一个定量框架，可用于诊断扩散动力学何时以及如何……

    arXiv:2609.08219v1 Announce Type: cross  Abstract: Neural networks acquire internal representations through learning. In this work, we formulate stochastic gradient descent (SGD) as a Markovian stochastic process and derive a Fisher-information flow speed limit that bounds the rate at which trainable parameters can acquire information about latent variables in the data-generating process. The resulting inequality decomposes the information flow into drift and noise contributions, thereby quantifying the roles of deterministic learning forces and SGD-induced fluctuations from an information-theoretic perspective. We verify the bound in analytically tractable basis-function linear regression, where the information budget predicted by the bound reproduces the ordering and characteristic time scales with which different latent variables are encoded in the learned parameters. These results establish Fisher-information speed limits as a quantitative framework for diagnosing when and how diff
    
[^99]: 愿景：面向稳健且可解释的智能体AI的数据中心锚定

    Vision: Data-Centric Anchoring for Robust and Interpretable Agentic AI

    [https://arxiv.org/abs/2609.08216](https://arxiv.org/abs/2609.08216)

    论文提出“数据中心锚定”理念及四阶段的数据中心智能体循环框架，主张将稳健性和可解释性预先工程化地构建到数据环境中，从根本上解决智能体AI在分布偏移下失效且无法解释决策的结构性缺陷。

    

    基于大语言模型构建的智能体AI系统存在两种规模化无法解决的持续性缺陷：它们在分布偏移下会失效，并且无法解释自己所做的决策。我们认为，这两者是同一个结构性缺陷的共同症状，该缺陷存在于支配智能体如何被训练、评估和部署的数据生命周期中。观察性交互日志记录的是智能体实际做了什么，而不是它本来可以做什么。它们在缺乏受控变化的情况下编码了虚假相关性，因此缺乏将因果信号与巧合区分开来或验证解释所需的反事实结构。任何以模型为中心的方法都无法恢复数据中从未包含的不变性。我们提出了数据中心锚定：稳健性和可解释性应当被工程化地构建到数据环境中，而不是在训练之后从模型中提取。我们的核心贡献是数据中心智能体循环，这是一个包含策划、增强……的四阶段框架。

    arXiv:2609.08216v1 Announce Type: new  Abstract: Agentic AI systems built on large language models fail in two persistent ways that scaling does not fix: they break under distribution shift, and they cannot explain the decisions they make. We argue these are co-symptoms of one structural deficiency in the data lifecycle that governs how agents are trained, evaluated, and deployed. Observational interaction logs record what an agent did, not what it would have done otherwise. They encode spurious correlations without controlled variation, so they lack the counterfactual structure needed to separate causal signal from coincidence or to validate an explanation. No model-centric method can recover invariances the data never contained. We present Data-Centric Anchoring: robustness and interpretability should be engineered into the data environment, not extracted from models after training. Our central contribution is the Data-Centric Agentic Loop, a four-stage framework of Curate, Augment, 
    
[^100]: DRIFT：通过偏移生成轨迹来移除扩散模型水印

    DRIFT: Removing Diffusion Watermarks by Deflecting the Generative Trajectory

    [https://arxiv.org/abs/2609.08213](https://arxiv.org/abs/2609.08213)

    提出了DRIFT黑盒攻击方法，通过部分前向扩散与随机逆向重采样来偏移生成轨迹，从而有效移除依赖轨迹验证的扩散模型水印。

    

    扩散水印技术将可验证的信号嵌入到生成过程中，通常通过恢复依赖于轨迹的证据来进行验证，这使得水印对传统的像素空间扰动具有鲁棒性。现有的移除攻击要么沿着确定性轨迹重新生成图像（这通常会保留携带水印的潜在结构），要么对每张图像进行单独优化。我们识别出对可恢复生成轨迹的依赖是我们所研究方案中的一个共同攻击面。基于这一观察，我们提出了DRIFT，这是一种结合部分前向扩散与随机逆向重采样的黑盒攻击方法。前向重新加噪限制了固定深度恢复管线可获取的源信息，而随机逆向过程提供了替代的噪声驱动路径，我们通过匹配采样器对比实验来分离量化其水印移除收益。自适应DRIFT为每张图像搜索一个选定的梯度阶梯以首先使验证器失效（摘要在此处被截断）。

    arXiv:2609.08213v1 Announce Type: cross  Abstract: Diffusion watermarking embeds verifiable signals into the generative process and commonly verifies them by recovering trajectory-dependent evidence, making the marks robust to conventional pixel-space distortions. Existing removal attacks either regenerate along deterministic trajectories, which often preserve the watermark-bearing latent structure, or optimize every image separately. We identify the reliance on a recoverable generative trajectory as a common attack surface among the schemes we study. Based on this observation, we propose DRIFT, a black-box attack that combines partial forward diffusion with stochastic reverse resampling. Forward re-noising limits source information available to a fixed-depth recovery pipeline, while stochastic reversal supplies alternative noise-driven paths whose removal benefit we isolate through matched sampler comparisons. Adaptive DRIFT searches a selected ladder for each image's first verifier-r
    
[^101]: 更好的驱动力应始于每个目标

    A Better Spur Should Start From Each Objective

    [https://arxiv.org/abs/2609.08211](https://arxiv.org/abs/2609.08211)

    提出了多边际偏好优化（MMPO）框架，通过曝光去偏、优先级感知正交投影和自我提示梯度约束，在数据、梯度和约束三个层面细粒度地解决多目标强化学习中的目标冲突问题，显著提升训练稳定性与性能。

    

    现实世界中的多目标强化学习（MORL）常常受到稀疏奖励、奖励冲突以及后期奖励“拉锯战”问题的困扰，导致传统的线性标量化方法出现严重的指标震荡。为了解决现实部署场景中多个目标之间的优化冲突，我们提出了多边际偏好优化（MMPO），这是一种细粒度的框架，在数据、梯度和约束三个层面进行干预，而不是依赖于粗粒度的全局标量化。具体而言，MMPO 通过曝光去偏来缓解稀疏和有偏的奖励，应用优先级感知的正交投影来解耦相互冲突的梯度，并引入自我提示的梯度约束以防止主导目标淹没较弱势的目标。在真实电商数据集上的实验表明，MMPO 提高了训练稳定性，并在冲突目标上持续取得更优的性能。

    arXiv:2609.08211v1 Announce Type: new  Abstract: Real-world Multi-Objective Reinforcement Learning (MORL) often suffers from sparse rewards, reward conflicts, and late-stage reward tug-of-war, causing traditional linear scalarization to experience severe metric oscillations. To address optimization conflicts among multiple objectives in real-world deployment scenarios, we propose Multi-Marginal Preference Optimization (MMPO), a fine-grained framework that intervenes at the data, gradient, and constraint levels rather than relying on coarse-grained global scalarization. Specifically, MMPO performs exposure debiasing to mitigate sparse and biased rewards, applies priority-aware orthogonal projection to decouple conflicting gradients, and introduces self-prompted gradient constraints to prevent dominant objectives from overwhelming weaker ones. Experiments on real-world e-commerce datasets show that MMPO improves training stability and consistently achieves better performance across confl
    
[^102]: SIM：基于子空间交互的词元级文本异常检测方法

    SIM: Subspace Interaction-based Method for Token-Level Text Anomaly Detection

    [https://arxiv.org/abs/2609.08200](https://arxiv.org/abs/2609.08200)

    该论文提出了基于子空间交互的方法SIM，通过避免局部异常信号被冗余特征维度稀释以及克服预训练语言模型对表层异常的平滑效应，实现了更有效的词元级文本异常检测。

    

    词元级文本异常检测作为文本异常检测的新兴趋势，超越了粗粒度的文档级检测，能够定位文本中的异常词元。通过提供细粒度的异常预测，词元级文本异常检测在垃圾邮件过滤和假新闻检测等各种现实应用中发挥着关键作用。然而，现有方法仍然依赖全局距离计算进行评分，在此过程中，局部异常信号会被大量冗余的正常特征维度严重稀释。此外，这些方法中使用的预训练语言模型不可避免地会平滑掉表层异常，进一步限制了它们在词元级异常检测中的有效性。为了解决这些局限性，我们提出了一种用于词元级文本异常检测的基于子空间交互的方法（简称SIM）。为防止局部信号被稀释，SIM采用基于子空间交互的异常……（摘要在此处被截断）

    arXiv:2609.08200v1 Announce Type: new  Abstract: Token-level text anomaly detection, as an emerging trend of text anomaly detection, moves beyond coarse-grained document-level detection by localizing anomalous tokens within text. By providing fine-grained abnormality prediction, token-level text anomaly detection plays a critical role in various real-world applications, such as spam filtering and fake news detection. However, existing methods still rely on the global distance calculation for scoring, during which the local anomaly signals are severely diluted by numerous redundant normal feature dimensions. Moreover, pre-trained language models used in these methods inevitably smooth out surface anomalies, further limiting their effectiveness in token-level anomaly detection. To address these limitations, we propose a Subspace Interaction-based Method (SIM for short) for token-level text anomaly detection. To prevent local signal dilution, SIM adopts a subspace interaction-based anomal
    
[^103]: 混合切片采样的最优切片自适应调优

    Optimal Slice-Adaptive Tuning of Hybrid Slice Sampling

    [https://arxiv.org/abs/2609.08172](https://arxiv.org/abs/2609.08172)

    该论文分析了混合切片采样中切片宽度参数 $w$ 对每次迭代目标密度评估次数的影响，并据此提出了具有次优性界限和渐近收敛保证的自动化切片自适应调优方案。

    

    切片采样是一种马尔可夫链蒙特卡洛算法，它在每次迭代中从“切片”——即目标密度函数的超水平集——中均匀抽取下一个状态，从而提供对目标尺度的自动局部自适应性。在实践中，精确的切片是未知的，因此通用实现使用从长度为 $w>0$ 的初始区间扩展而来的近似切片，其计算成本取决于 $w$。本工作针对具有连续切片的目标，分析了采用各种切片寻找方案的混合切片采样每次迭代的平均目标密度评估次数（作为 $w$ 的函数）。论文利用分析结果开发了自动化的切片自适应调优方案，并给出了次优性界限和渐近收敛保证。仿真结果表明，这些调优方案能够可靠地产生接近最优的切片自适应调优效果。

    arXiv:2609.08172v1 Announce Type: cross  Abstract: Slice sampling is a Markov chain Monte Carlo algorithm that draws its next state uniformly from a "slice"---a super-level set of the target density function---at each iteration, thereby providing automatic local adaptivity to the scale of the target. In practice the exact slice is not known, so general-purpose implementations use an approximate slice that is grown from a starting interval of length $w>0$, with a computational cost that depends on $w$. This work presents an analysis of the average per-iteration number of target density evaluations, as a function of $w$, of hybrid slice sampling with various slice-finding schemes for targets with contiguous slices. The paper uses the results of the analysis to develop automated, slice-adaptive tuning schemes along with suboptimality bounds and asymptotic convergence guarantees. Simulations demonstrate that the tuning schemes reliably yield near-optimal slice-adaptive tuning with essentia
    
[^104]: 一种基于Transformer的裸盖菇素转录响应差值表达编码器：架构、表征与生物学验证

    A Transformer-Based Delta Expression Encoder for Psilocybin Transcriptional Response: Architecture, Representations, and Biological Validation

    [https://arxiv.org/abs/2609.08165](https://arxiv.org/abs/2609.08165)

    本文提出一种无需先验生物学知识的基于Transformer的差值表达编码器，可从单细胞核RNA测序数据中分类裸盖菇素诱导的差异基因表达状态，在18种细胞类型上达到69.4%的加权准确率，并揭示了从28.3%到99.6%的显著细胞类型特异性分类差异。

    

    理解个体对裸盖菇素响应差异的原因，需要在细胞类型层面建模该药物的转录扰动特征。本文提出了一种基于Transformer的差值表达编码器，它能够从单细胞核RNA测序数据中学习分类差异基因表达状态——上调、下调或中性——而无需依赖通路注释或先验生物学知识的监督。该模型在来自Liao等人2025年数据集的623个样本的伪批量表达谱上训练，涵盖18种细胞类型、2种药物条件和6个时间点，达到了69.4%的加权分类准确率。论文报告了三项主要发现，其中包括对一项已发表假设的直接检验，其结果与该假设不一致。首先，各细胞类型的分类准确率从28.3%（L2/3 IT，一种主要表达HTR2A的裸盖菇素靶点）到99.6%（内皮细胞）不等。（注：原文摘要在此处截断）

    arXiv:2609.08165v1 Announce Type: cross  Abstract: Understanding why individuals respond differently to psilocybin requires modeling the drug's transcriptional perturbation signature at the cell-type level. I present a Transformer-based delta expression encoder that learns to classify differential gene expression status - upregulated, downregulated, or neutral - from single-nucleus RNA-sequencing data, without supervision from pathway annotations or prior biological knowledge. The model is trained on pseudobulk profiles from 623 examples spanning 18 cell types, 2 drug conditions, and 6 timepoints derived from the Liao et al. 2025 dataset, and achieves 69.4% weighted classification accuracy. Three principal findings are reported, alongside one direct test of a published hypothesis that returned a result inconsistent with that hypothesis. First, per-cell-type classification accuracy ranges from 28.3% (L2/3 IT, a primary HTR2A-expressing psilocybin target) to 99.6% (endothelial cells), co
    
[^105]: 面向拓扑保持图像视频生成的测地线信息生成扩散模型

    Geodesic-informed Generative Diffusion Model For Topology-preserved Image Video Generation

    [https://arxiv.org/abs/2609.08153](https://arxiv.org/abs/2609.08153)

    该论文提出一种基于测地线信息的生成扩散模型，通过显式建模形变动力学来保证生成过程中的几何与拓扑一致性，解决了现有生成模型忽视物体底层几何结构且变换过程不可解释、缺乏物理信息的两大局限。

    

    生成扩散模型已成为一类强大的技术，广泛应用于各种成像任务，包括但不限于合成、重建和分割。尽管取得了成功，当前的生成模型存在两个关键局限。首先，它们主要依赖图像强度和纹理信息，对底层物体几何结构的关注有限。因此，它们无法在生成过程中保证几何或拓扑一致性，而这对于计算解剖学、生物学和机器人学等高风险领域是一项关键要求，在这些领域中保持物体结构至关重要。其次，现有模型未能在生成过程中显式学习或表示形状变化。这种形变动力学仍然隐藏在网络参数之中，因此导致变换过程不可解释且缺乏物理信息。为了应对这些挑战，我们提出了IGG（图像……（原文摘要在此处截断）

    arXiv:2609.08153v1 Announce Type: new  Abstract: Generative diffusion models have emerged as a class of powerful techniques for various imaging applications, including but not limited to synthesis, reconstruction, and segmentation. Despite their success, current generative models pose two key limitations. First, they primarily rely on image intensity and texture information, with limited attention to underlying object geometry. As a result, they do not guarantee geometric or topological consistency during the generation process, which is a crucial requirement for high-stakes domains such as computational anatomy, biology, and robotics, where preserving object structure is critical. Second, existing models fail to explicitly learn or represent shape changes in the generative process. Such deformation dynamics remain occluded within network parameters; hence leaving the transformation process uninterpretable and physically uninformed. To address these challenges, we introduce IGG (Image 
    
[^106]: 拓扑诱导算子无需训练即可揭示互补的图表示

    Topology-induced Operators Reveal Complementary Graph Representations without Training

    [https://arxiv.org/abs/2609.08152](https://arxiv.org/abs/2609.08152)

    该论文提出两种无需训练的拓扑诱导算子，通过在随机游走和匿名游走诱导的层次结构中传播随机特征，分别捕捉节点邻近性与结构角色这两种互补的图嵌入，在多种任务上性能媲美现有方法且计算成本大幅降低。

    

    图表示学习主要集中于设计日益复杂的模型，将图拓扑结构转换为向量表示（即嵌入）。然而，嵌入质量在多大程度上取决于模型学习，而非底层的拓扑变换本身，仍不清楚。本文证明，无需复杂的模型设计和基于梯度的训练，也可以获得信息丰富的嵌入。通过在由随机游走和匿名游走诱导的隐式层次结构中传播随机特征，可以得到分别捕捉节点邻近性和结构角色的嵌入。这两种无需训练的嵌入保留了图组织的互补方面，并在各种节点级、边级和图级任务上与经典方法及最新方法相比具有竞争力。它们通常需要显著更少的计算量，从而实现了良好的质量-效率权衡。

    arXiv:2609.08152v1 Announce Type: new  Abstract: Graph representation learning has largely focused on designing increasingly sophisticated models to transform graph topology into vector representations, or embeddings. However, the extent to which embedding quality depends on model learning, rather than on the underlying topological transformations, remains unclear. Here, we show that informative embeddings can be derived without complicated model design and gradient-based training. Propagating random features through implicit hierarchical structures induced by random walks and anonymous walks yields embeddings that capture node proximity and structural role, respectively. These two training-free embeddings preserve complementary aspects of graph organization and perform competitively with classic and recent methods across various node-, edge-, and graph-level tasks. They often require substantially less computation, resulting in a favorable quality-efficiency trade-off. Combining the t
    
[^107]: 基于随机化线性代数的GPU加速大规模优化

    GPU-Enabled Large-Scale Optimization Using Randomized Linear Algebra

    [https://arxiv.org/abs/2609.08136](https://arxiv.org/abs/2609.08136)

    rlaopt 是一个基于 PyTorch 的软件包，通过将随机化数值线性代数与 GPU 加速相结合，为大规模正定线性系统和带约束正则化的凸优化问题提供高效求解器，并配备自然数学语法的建模语言和可微分迭代功能。

    

    本文介绍了 rlaopt，一个基于 PyTorch 的软件包，用于借助随机化数值线性代数进行大规模优化和科学计算。尽管基于 RandNLA 的算法已取得长足进展，但很少有实现能够将 GPU 加速与简洁的优化问题定义接口相结合。rlaopt 填补了这一空白，它为正定线性系统以及带约束和正则化项的凸经验风险最小化问题提供了支持 GPU 的求解器。这些求解器利用 RandNLA 来加速共轭梯度法（NystromPCG）、算子分裂法（NysADMM）以及随机梯度方法（SAPPHIRE）。此外，rlaopt 还包含一种建模语言，允许用户使用自然的数学语法来描述问题。rlaopt 会自动检查问题与所选求解器的兼容性，并执行所需的问题分解。这些求解器还支持对其迭代过程进行自动微分，从而支持（后文内容被截断）……

    arXiv:2609.08136v1 Announce Type: new  Abstract: This paper introduces rlaopt, a PyTorch-based package for large-scale optimization and scientific computing using randomized numerical linear algebra (RandNLA). Despite substantial progress in RandNLA-based algorithms, few implementations combine GPU acceleration with a simple interface for specifying optimization problems. rlaopt addresses this gap by providing GPU-enabled solvers for positive-definite linear systems and convex empirical risk minimization with constraints and regularizers. These solvers use RandNLA to accelerate conjugate gradient (NystromPCG), operator splitting (NysADMM), and stochastic gradient methods (SAPPHIRE). Moreover, rlaopt includes a modeling language that lets users specify problems using natural mathematical syntax. rlaopt automatically checks compatibility with the selected solver and performs the required problem decomposition. The solvers also support differentiation through their iterations, enabling ap
    
[^108]: KBBQ：FP4量化中的预测性噪声定律与频谱平坦化的极限

    KBBQ: A Predictive Noise Law and the Limits of Spectrum Flattening in FP4 Quantization

    [https://arxiv.org/abs/2609.08135](https://arxiv.org/abs/2609.08135)

    该论文建立了矩阵乘法量化噪声的二阶理论，导出闭式信噪比定律与任何保函数线性变换无法超越的上界 $\kappa^{*}$，并据此提出 KBBQ 方法，在 W4A4 FP4 量化中超越了先前最先进的方法。

    

    我们发展了一种矩阵乘法中量化噪声的二阶理论，其中量化格式由其分配给每个元素的方差来刻画。整数量化的恒定方差分布可以恢复现有的整数噪声理论，而浮点舍入的乘法分布则将数据依赖性简化为一个标量——参与因子 $\kappa$，从而得到一个闭式的信噪比定律。所得到的泛函还存在一个闭式上界 $\kappa^{*}$，任何保持函数性质的线性变换都无法超越该上界，而近期一种最先进的方法恰好达到了该上界。基于这一分析，我们提出了 KBBQ（**K**appa-**B**raked **B**lockwise **Q**uantization，κ-制动分块量化），它对变换接近该上限的程度进行参数化。在 W4A4 设置下，跨越四个基础模型和两种 FP4 格式，KBBQ 在无需额…（摘要被截断）

    arXiv:2609.08135v1 Announce Type: cross  Abstract: We develop a second-order theory of quantization noise in matrix multiplication in which the quantization format is characterized by the variance it assigns to each element. The constant variance profile of integer quantization recovers existing integer-noise theory, while the multiplicative profile of floating-point rounding reduces the data dependence to a scalar, the participation factor $\kappa$, yielding a closed-form signal-to-noise-ratio law. The resulting functional also admits a closed-form upper bound $\kappa^{*}$ that no function-preserving linear transform can exceed and that is attained by a recent state-of-the-art method. Building on this analysis, we introduce KBBQ (\textbf{K}appa-\textbf{B}raked \textbf{B}lockwise \textbf{Q}uantization), which parameterizes the extent to which a transform approaches this ceiling. At W4A4, across four base models and two FP4 formats, KBBQ outperforms the prior state of the art without ad
    
[^109]: 具有可证明保证的稀疏数据增强优化方法

    Sparse Data Augmentation for Optimization with Provable Guarantees

    [https://arxiv.org/abs/2609.08133](https://arxiv.org/abs/2609.08133)

    该论文证明了在非凸几何机器学习优化中，使用优化前采样的少量固定数据变换进行稀疏数据增强，梯度下降仅需对数级加多项式级的群变换查询次数，即可在概率保证下逼近完全数据增强目标的稳定点。

    

    在几何机器学习中出现的非凸优化问题里，数据增强通常被用于通过对数据变换后的经验损失取平均来促进不变性。然而，计算完全增强后的目标函数需要访问变换群 $G$ 中的每一个元素，当 $G$ 非常庞大或只能通过采样方式访问时，这种做法的代价可能高得令人望而却步。我们研究了是否可以使用在优化开始前获取、并在之后的优化过程中重复使用的一小部分固定变换样本，来近似完全增强的目标函数。在适当的正则性条件下，我们证明，至少以 $1-\delta$ 的概率，对由此得到的稀疏增强目标函数执行梯度下降（GD），仅使用 $\mathcal{O}\bigl((\log |G|+\log(1/\delta))/\varepsilon^2\bigr)$ 次群变换预言机查询，即可返回完全增强目标函数的一个 $\varepsilon$-稳定点。相比之下，标准的群随机梯度（摘要在此处被截断）

    arXiv:2609.08133v1 Announce Type: cross  Abstract: In nonconvex optimization problems arising in geometric machine learning, data augmentation is commonly used to promote invariance by averaging empirical losses over transformations of the data. Computing the fully augmented objective, however, requires access to every element of the transformation group $G$, which may be prohibitively expensive when $G$ is large or accessible only through sampling. We study whether full augmentation can instead be approximated using a small, fixed sample of transformations acquired before optimization and reused thereafter. Under suitable regularity conditions, we show that, with probability at least $1-\delta$, gradient descent (GD) on the resulting sparsely augmented objective returns an $\varepsilon$-stationary point of the fully augmented objective using $\mathcal{O}\bigl((\log |G|+\log(1/\delta))/\varepsilon^2\bigr)$ group-transformation-oracle queries. By comparison, standard group stochastic gr
    
[^110]: DISEIL：面向样本高效模仿学习的示范蒸馏

    DISEIL: Demonstration Distillation for Sample-Efficient Imitation Learning

    [https://arxiv.org/abs/2609.08123](https://arxiv.org/abs/2609.08123)

    DISEIL通过在策略首次不可靠处标记失败、以几何描述符表征并将失败聚类为反复出现的失败模式，再利用视觉-语言模型和语言模型生成精准的演示请求，从而实现样本高效的交互式模仿学习。

    

    一个能够通过少量演示学会新任务的机器人，必须自己弄清楚它还不会做什么，然后确切地提出相应的请求。交互式模仿学习朝这一方向迈出了一步：让策略自主练习，并在出错时呼叫专家。现有方法只决定何时中断学习者，而另外两个决策则交由恰好触发中断的那个回合来决定：纠正哪个失败，以及演示应该从哪里开始。本文首次尝试对这两个决策进行审慎的设计。DISEIL（Demonstration dIstillation for Sample-Efficient Imitation Learning）在每个失败回合中标记出策略首次变得不可靠的步骤，用几何描述符表示该时刻，并将这些失败聚为反复出现的失败模式。视觉-语言模型和语言模型读取所选的失败模式，并据此撰写对下一次演示的请求（摘要在此处被截断）。

    arXiv:2609.08123v1 Announce Type: cross  Abstract: A robot that can be taught a new task from a handful of demonstrations has to work out for itself what it still cannot do, and then ask for exactly that. Interactive imitation learning takes a step in that direction by letting a policy practice on its own and calling an expert when it goes wrong. Existing methods decide when to interrupt the learner. A further 2 decisions are left to whichever episode happened to trigger the interruption: which failure to correct, and where the demonstration should start. This paper is a first attempt at making both of them deliberately. DISEIL (Demonstration dIstillation for Sample-Efficient Imitation Learning) marks each failed episode at the step where the policy first becomes unreliable, represents that moment with a geometric descriptor, and groups the failures into recurring failure modes. A vision-language model and a language model read the selected mode and write a request for the next demonst
    
[^111]: Nyström注意力在横截面股票预测中可媲美全注意力

    Nystr\"om Attention Matches Full Attention for Cross-Sectional Stock Prediction

    [https://arxiv.org/abs/2609.08106](https://arxiv.org/abs/2609.08106)

    该研究通过谱分析发现MASTER模型中股票间注意力偏离均匀分布的部分本质上是低秩的，因此Nyström低秩注意力可以以更低的计算复杂度完全替代全注意力模块，实现等效的横截面股票预测性能。

    

    MASTER的股票间多头注意力——负责建模横截面股票关系的模块——占据模型参数的42.5%和预测价值的25%。我们系统地分解了该模块，并揭示出一个令人惊讶的结构：学习到的注意力接近均匀分布（困惑度为278/300），然而强制其完全均匀却会消除所有横截面判别能力。谱分析化解了这一悖论：偏离均匀性的部分是低秩的（有效秩约为65，前10个模态捕获了96.5%的能量），这解释了为何稀疏近似始终失败，而Nyström低秩注意力（m=32个地标点）却能以O(mN)的成本匹配完整的O(N²)注意力——并通过TOST等效性检验在N=300（5个随机种子，Rank IC p=0.003）和N=800（10个随机种子，Rank IC p=0.034）两种设置下均验证了等效性。其他发现包括：(i)注意力与收益率相似性呈负相关（Spearman rho = -0.614；在行业标签子集上为-0.645……

    arXiv:2609.08106v1 Announce Type: new  Abstract: MASTER's inter-stock multi-head attention -- the module responsible for modeling cross-sectional stock relationships -- accounts for 42.5% of model parameters and 25% of predictive value. We systematically decompose this module and uncover a surprising structure: the learned attention is near-uniform (perplexity 278/300), yet forcing exact uniformity eliminates all cross-sectional discrimination. Spectral analysis resolves this paradox: the deviation from uniformity is low-rank (effective rank ~65, top-10 modes capture 96.5% of energy), explaining why sparse approximations consistently fail while Nystrom low-rank attention (m=32 landmarks) matches full O(N^2) attention at O(mN) cost -- certified equivalent via TOST at both N=300 (5 seeds, Rank IC p=0.003) and N=800 (10 seeds, Rank IC p=0.034). Additional findings include: (i) attention anti-correlates with return similarity (Spearman rho = -0.614; on the industry-labeled subset, -0.645 u
    
[^112]: 基于小波编码傅里叶神经算子的超材料特征模态学习

    Learning Metamaterial Eigenmodes with Wavelet-Encoded Fourier Neural Operators

    [https://arxiv.org/abs/2609.08102](https://arxiv.org/abs/2609.08102)

    本文提出将小波编码与傅里叶神经算子相结合，突破了传统算子学习假设唯一输入-输出映射的限制，实现了在单一模型中学习并预测弹性波方程的多个特征模态，从而确定超材料中声波传播的变形模式。

    

    基于神经算子的机器学习代理模型在求解偏微分方程（PDE）正问题方面展现出广泛的适用性。然而，特征值问题——需要同时求解一个特征参数和多个有效特征模态中的一个——仍然极具挑战性，因为标准的算子学习框架假设输入-输出映射是唯一的。这项工作表明，傅里叶神经算子（FNOs）与基于小波的PDE输入编码相结合，能够学习和预测弹性波方程的多个特征模态，这些模态对应于声波在任意超材料几何结构中传播时的变形模式。我们为小波编码为何与FNO的双重空间-频谱结构高度契合提供了机理解释和实验证据，这一契合使得单一模型能够在连续值和二值几何结构上实现确定性的模态选择，并解释了预测精度为何随……（摘要在此处截断）

    arXiv:2609.08102v1 Announce Type: new  Abstract: Machine learning surrogates based on neural operators have shown broad applicability in solving forward PDE problems. However, eigenvalue problems, in which an eigenparameter and one of several valid eigenmodes must be simultaneously solved, remain difficult because standard operator learning formulations assume a unique input-output map. This work demonstrates that Fourier Neural Operators (FNOs), combined with wavelet-based encodings of PDE inputs, can learn and predict multiple eigenmodes of the elastic wave equation, corresponding to deformation modes of acoustic waves propagating through arbitrary metamaterial geometries. We provide a mechanistic explanation and experimental evidence for why wavelet encodings are well matched to the dual spatial-spectral structure of the FNO, enabling deterministic mode selection on both continuous-valued and binary-valued geometries within a single model, and for why prediction accuracy varies with
    
[^113]: PocketVE：基于方差爆炸扩散的稳定且性质引导的结构药物设计

    PocketVE: Stable and Property-Guided Structure-Based Drug Design with Variance-Exploding Diffusion

    [https://arxiv.org/abs/2609.08101](https://arxiv.org/abs/2609.08101)

    PocketVE是一个以蛋白质口袋为条件的方差爆炸扩散框架，通过结合EDM式3D去噪、无分类器多性质引导和自适应蛋白质扰动正则化，在CrossDocked2020上显著提升了3D有效性（58.6→80.6）并大幅降低了应变能（457.4→127.9）。

    

    蛋白质条件化的3D分子生成是基于结构的药物设计中的核心挑战，需要在口袋兼容性、分子性质和物理几何之间取得平衡。我们提出了PocketVE，一个以蛋白质口袋为条件的方差爆炸（VE）扩散框架，它将稳定的坐标去噪与推理时的性质引导相结合。具体而言，PocketVE结合了用于3D去噪的EDM风格训练与采样设置、无需外部性质分类器即可实现多性质导向的无分类器引导，以及作为训练时口袋正则化器的自适应蛋白质扰动。在CrossDocked2020数据集上按照GenBench3D协议进行评估，PocketVE相对于其TAGMol架构基线，将Valid$_{3\text{D}}$从58.6提升到80.6，并将应变能从457.4降低到127.9，同时在适度引导下保持具有竞争力的对接得分和分子性质评分。一个引导尺度

    arXiv:2609.08101v1 Announce Type: cross  Abstract: Protein-conditioned 3D molecule generation is a central challenge in structure-based drug design, requiring a balance between pocket compatibility, molecular properties, and physical geometry. We propose \textbf{PocketVE}, a protein-pocket-conditioned variance-exploding (VE) diffusion framework that couples stable coordinate denoising with inference-time property guidance. Specifically, PocketVE combines an EDM-style training and sampling setup for 3D denoising, classifier-free guidance for multi-property steering without external property classifiers, and adaptive protein perturbation as a training-time pocket regularizer. Evaluated on CrossDocked2020 under the GenBench3D protocol, PocketVE improves Valid$_{3\text{D}}$ from 58.6 to 80.6 and reduces strain energy from 457.4 to 127.9 relative to its TAGMol architectural baseline, while retaining competitive docking and molecular-property scores under moderate guidance. A guidance-scale 
    
[^114]: Marigold V2：重新审视用于单目深度估计的扩散Transformer

    Marigold V2: Revisiting Diffusion Transformers for Monocular Depth Estimation

    [https://arxiv.org/abs/2609.08084](https://arxiv.org/abs/2609.08084)

    提出Marigold V2，通过将基于扩散Transformer的图像生成模型改造为单步推理的单目深度估计器，并借助语义特征对齐和两阶段微调两种策略消除训练伪影，在保持低成本的同时实现高质量、强泛化能力的深度估计。

    

    单目深度估计是一项普遍存在但高度不适定的计算机视觉任务，在场景重建、计算摄影和机器人等领域有着广泛的下游应用。尽管该领域已相当成熟，但近期的模型仍难以泛化到分布外的输入，也难以生成清晰、细致的深度图。在本文中，我们重新审视了Marigold——一套将基于扩散Transformer（DiT）架构的现代图像生成与编辑模型改造为最先进单目深度估计器的技术方案。我们的方法针对从预训练的多步流匹配模型进行单步推理，并在需要时进行量化，在保持模型能力的同时保持较低的运行成本。我们分析了朴素训练所产生的伪影问题，并找到了两种有效的补救措施：将模型的内部表征与从真实值中提取的语义特征对齐，以及采用两阶段微调策略。

    arXiv:2609.08084v1 Announce Type: cross  Abstract: Monocular depth estimation is a ubiquitous yet highly ill-posed computer vision task, with downstream applications in scene reconstruction, computational photography, and robotics, among others. Despite the field's maturity, recent models still struggle to generalize to out-of-distribution inputs and to produce sharp and detailed depth maps. In this paper, we revisit Marigold, a set of techniques for repurposing modern image generation and editing models, powered by the diffusion transformer (DiT) architecture, into state-of-the-art monocular depth estimators. Our recipes target single-step inference from pretrained multi-step flow-matching models, with quantization where needed, preserving model capacity while remaining cheap to run. We analyze the artifacts of naive training and identify two effective remedies: aligning the model's internal representations with semantic features extracted from ground-truth, and adopting a 2-stage fin
    
[^115]: 面向非平稳强化学习的主动式情境预测安全约束

    Proactive Context-Forecasted Safety Constraints for Nonstationary Reinforcement Learning

    [https://arxiv.org/abs/2609.08080](https://arxiv.org/abs/2609.08080)

    提出了一种基于情境预测的主动式安全约束生成框架，通过推断潜在环境情境并预测其未来演变来前瞻性地构建安全约束，使强化学习智能体能够在非平稳环境中主动规避不安全区域。

    

    在非平稳环境下确保强化学习的安全性，需要在风险导致不安全行为之前预先察觉这些变化。现有方法通常依赖于在设计阶段定义的安全约束，或在执行过程中被动更新的约束，并假设此类约束会随时间保持有效。然而，在具有演变情境和变化驾驶布局的非平稳环境中，这些假设可能失效。我们提出了一个基于情境预测的主动式安全约束生成框架。该方法从观测中推断潜在的环境情境，预测其未来演变，并构建适应预期条件的安全约束。这使智能体能够主动避免不安全区域，而不是仅在安全违规发生后才做出反应。我们在具有结构化情境变化的驾驶环境中对该方法进行评估，实验包括对非平稳性的参数扫描。

    arXiv:2609.08080v1 Announce Type: new  Abstract: Ensuring safety in reinforcement learning under nonstationarity requires anticipating changes in risk before they lead to unsafe behavior. Existing approaches typically rely on safety constraints defined at design time or updated reactively during execution, assuming that such constraints remain valid over time. However, in nonstationary environments with evolving contexts and changing driving layouts, these assumptions may fail.   We propose a framework for proactive safety constraint generation based on context forecasting. The approach infers latent environmental context from observations, predicts its future evolution, and constructs safety constraints adapted to anticipated conditions. This enables the agent to proactively avoid unsafe regions instead of reacting only after safety violations occur.   We evaluate the method in driving environments with structured context variation. The experiments include a sweep over nonstationarity
    
[^116]: 一个用于预测餐厅食物浪费以支持可持续食物管理的机器学习框架

    A Machine Learning Framework for Predicting Restaurant Food Waste to Support Sustainable Food Management

    [https://arxiv.org/abs/2609.08078](https://arxiv.org/abs/2609.08078)

    本文提出了一个整合餐厅运营需求、气象数据和时间事件指标的机器学习框架，利用四种回归模型预测餐厅每日食物浪费量，以支持可持续食物管理。

    

    餐饮行业的食物浪费对环境可持续性和经济效率构成了重大挑战。本文提出了一个探索性的机器学习框架，用于根据运营和情境特征估计餐厅每日的食物浪费量。通过整合餐厅需求记录、气象数据和时间事件指标，构建了一个结构化数据集，包含77,980条记录和27个特征。由于大规模的真实食物浪费测量数据尚未公开，目标变量基于合理的运营假设推导得出，并公开了完整的构建公式和受控随机变异性，以确保完全可复现性。研究在遵循餐厅运营时间顺序的按时间顺序70-30训练-测试划分下，评估了四种监督回归模型，即线性回归、决策树、随机森林和梯度提升。

    arXiv:2609.08078v1 Announce Type: new  Abstract: Food waste in the restaurant sector poses a substantial challenge to environmental sustainability and economic efficiency. This paper presents an exploratory machine learning framework for estimating daily restaurant food waste quantities from operational and contextual features. A structured dataset was constructed by integrating restaurant demand records, meteorological data and temporal event indicators, yielding 77,980 records across 27 features. Because large-scale ground-truth food waste measurements are not publicly available, the target variable was derived from operationally justified assumptions, with the complete construction formula and controlled stochastic variability disclosed for full reproducibility. Four supervised regression models, namely Linear Regression, Decision Tree, Random Forest and Gradient Boosting, were evaluated under a chronological 70-30 train-test split that respects the temporal ordering of restaurant o
    
[^117]: 一种基于梯度但依赖脉冲时序的神经微回路反馈学习问题解决方案

    A Gradient-based yet Spike-Timing-Dependent Solution to the Feedback Learning Problem in Neural Microcircuits

    [https://arxiv.org/abs/2609.08070](https://arxiv.org/abs/2609.08070)

    本文提出梯度隧穿算法，将时间信用分配重构为状态分离问题，从局部突触脉冲时序推导信用分配，实现了与生物脉冲时序一致的神经微回路在线反馈学习框架。

    

    大脑利用离散脉冲进行动态计算，然而神经微回路如何利用局部脉冲时序来解决时间信用分配问题仍然是一个基本的开放性问题。主流的脉冲神经网络（SNN）方法通过代理梯度近似反向传播来规避这一问题，使学习与生物脉冲时序解耦。在此，我们将时间信用分配重新表述为一个状态分离问题：直接从当前神经状态中提取由历史扰动引起的任务所需成分。这使得通过梯度隧穿（GT）算法和超前-滞后展开技术，为神经微回路构建了在线反馈学习框架，该技术从局部突触脉冲时序中推导信用分配，同时保持与ANN-SNN混合架构的兼容性。实验表明，经GT训练的神经微回路在长时间尺度的证据整合和抗噪记忆保持方面表现出色，并且能够执行……

    arXiv:2609.08070v1 Announce Type: cross  Abstract: The brain uses discrete spikes for dynamic computation, yet, how neural microcircuits (NMCs) solve temporal credit assignment using local spike timing remains a fundamental open question. Dominant spiking neural network (SNN) approaches circumvent this by approximating backpropagation through surrogate gradients, decoupling learning from biological spike timing. Here, we reformulate temporal credit assignment as a state separation problem: extracting task-required components induced by historical perturbations directly from the current neural state. This enables an online feedback learning framework for NMCs through a gradient tunneling (GT) algorithm and the lead-lag expansion technique that derives credit assignment from local synaptic spike timing, while remaining compatible with ANN-SNN hybrid architectures. Experimentally, GT-trained NMCs excel at long-timescale evidence integration and noise-robust memory retention, and perform c
    
[^118]: 大语言模型的风险条件化微调

    Risk-Conditioned Fine-Tuning of Large Language Models

    [https://arxiv.org/abs/2609.08064](https://arxiv.org/abs/2609.08064)

    提出风险条件化RLHF框架，通过训练单一策略提供连续可调的风险控制接口，使用户能够在推理时灵活选择不同的风险规避程度，而无需重新训练或部署多个特定风险的模型。

    

    大语言模型（LLMs）越来越多地被部署在罕见但严重的有害生成内容可能造成重大后果的场景中。现有的风险规避RLHF通过优化条件风险价值（CVaR）来解决这一问题，但其针对固定的风险水平训练策略，因此无法在推理时调整所需的风险规避程度。在本文中，我们提出了风险条件化RLHF，这是一个训练单一策略的框架，该策略提供了连续的风险控制接口，使用户无需重新训练或部署多个针对特定风险的模型即可选择不同的风险规避程度。在多个基准测试上的实验表明，单一的风险条件化策略能够在推理时适应不同的风险水平，从而实现更灵活、更具风险感知能力的大语言模型部署。

    arXiv:2609.08064v1 Announce Type: new  Abstract: Large Language Models (LLMs) are increasingly deployed in settings where rare but severe harmful generations can have significant consequences. Existing Risk-Averse RLHF addresses this issue by optimizing Conditional Value-at-Risk (CVaR), but it trains policies for fixed risk levels and therefore cannot adjust the desired degree of risk aversion at inference time. In this paper, we propose risk-conditioned RLHF, a framework that trains a single policy that provides a continuous risk-control interface, enabling users to select different degrees of risk aversion without retraining or deploying multiple risk-specific models. Experiments across multiple benchmarks demonstrate that a single risk-conditioned policy can adapt to different risk levels at inference time, enabling more flexible and risk-aware LLM deployment.
    
[^119]: MI-PEFT：专家混合集成的参数高效微调蛋白质语言模型改进嗜酸蛋白分类

    MI-PEFT: Mixture-of-Experts Integrated Parameter-Efficient Fine-Tuning Protein Language Models Improves Acidophilic Proteins Classification

    [https://arxiv.org/abs/2609.08059](https://arxiv.org/abs/2609.08059)

    本文提出MI-PEFT框架，在ESM C-600M蛋白质语言模型上集成基于LoRA的参数高效微调方法与基于DeepSeekMoE的分类头，显著提升了嗜酸蛋白质分类的准确性和计算效率。

    

    嗜酸蛋白是在高度酸性条件下仍能保持稳定和功能的蛋白质，对工业生物催化、酸相关生物加工以及酸性稳定酶的发现具有重要意义。然而，这类蛋白质的识别严重依赖耗时的实验筛选方法。随着蛋白质序列数据库的快速增长，对既准确又高效的计算识别方法的需求日益强烈。蛋白质语言模型（PLMs）的出现显著改善了下游生物预测任务的序列表示。本文提出了MI-PEFT，一个专家混合集成的参数高效微调框架。该框架基于ESM C-600M骨干网络构建，结合了基于LoRA的参数高效微调（PEFT）方法和基于DeepSeekMoE的分类头，以解决PEFT的局限性并显著提高计算效率。值得注意的是，该任务具有……（摘要在此处被截断）

    arXiv:2609.08059v1 Announce Type: cross  Abstract: Acidophilic proteins that remain stable and functional under highly acidic conditions, are important for industrial biocatalysis, acid-related bioprocessing, and the discovery of acid-stable enzymes. However, their identification relies heavily on time-consuming experimental screening methods. With the rapid growth of protein sequence databases, the need for computational identification methods that are both accurate and efficient has become stronger. The emergence of protein language models (PLMs) has significantly improved the sequence representation of downstream biological prediction tasks. This paper proposes MI-PEFT, a mixture-of-experts integrated parameter-efficient fine-tuning framework. Built on the ESM C-600M backbone, the framework incorporates LoRA-based PEFT methods and a DeepSeekMoE-based classification head to resolve the limitations of PEFT and significantly improve computational efficiency. Notably, this task is chara
    
[^120]: 用于情境依赖多元关系的贝叶斯矩阵值图

    Bayesian Matrix-Valued Graphs for Context-Dependent Multivariate Relationships

    [https://arxiv.org/abs/2609.08055](https://arxiv.org/abs/2609.08055)

    本文提出贝叶斯矩阵值图（BMVG），用对称正定矩阵建模图的边，并借助AIRM测地距离与广义特征值来量化和定位多变量相互作用在不同情境间的变化幅度与方向，在精度矩阵恢复上优于或媲美现有基线方法。

    

    许多科学领域的图网络在每一个节点上附加了多个变量，因此单一的标量边权重无法描述方向依赖的相互作用。我们用对称正定（SPD）矩阵对每条边进行建模，并推断矩阵值图几何结构上的后验分布，我们称之为贝叶斯矩阵值图（BMVG）。我们研究这些相互作用如何在不同情境间重新配置：变化有多大，以及哪些多元方向被加强或削弱。由仿射不变黎曼度量（AIRM）诱导的测地距离用于量化变形幅度，而广义特征值则解析其带符号的方向。与融合图套索（fused graphical lasso）、贝叶斯多重高斯图模型（Bayesian multiple-GGM）和公共主成分方法相比，BMVG在全局精度矩阵恢复方面具有竞争力，同时保留了可识别的矩阵值边结构，并能准确恢复边级别的变形方向。在受控的真值已知实验中，它能够解析结构……

    arXiv:2609.08055v1 Announce Type: cross  Abstract: Many scientific graphs attach several variables to each node, so a single scalar edge weight cannot describe direction-dependent interactions. We model each edge by a symmetric positive-definite (SPD) matrix and infer a posterior over matrix-valued graph geometries, which we call the Bayesian matrix-valued graph (BMVG). We ask how these interactions reconfigure across contexts: how large the change is and which multivariate directions strengthen or weaken. The geodesic distance induced by the affine-invariant Riemannian metric (AIRM) quantifies deformation magnitude and generalized eigenvalues resolve its signed directions.Against fused graphical lasso, Bayesian multiple-GGM, and common principal components, BMVG is competitive on global precision recovery while retaining identifiable matrix-valued edge structure and accurately recovering edge-level deformation directions. In controlled known-truth experiments, it resolves structural c
    
[^121]: 超声心动图视频分割中时间可解释性的定量评估框架

    A Quantitative Evaluation Framework for Temporal Explainability in Echocardiographic Video Segmentation

    [https://arxiv.org/abs/2609.08043](https://arxiv.org/abs/2609.08043)

    该论文提出了一个利用时间一致性、显著性运动、解剖学重叠和时间重叠四个互补指标来定量评估超声心动图视频分割中Grad-CAM时间可解释性的框架，发现尽管各模型分割性能相当，其时间解释的稳定性却存在显著差异。

    

    深度学习在超声心动图视频分割中已达到最先进的性能，越来越多的模型开始融入时间信息。然而，时间可解释性的定量评估在很大程度上仍未被探索。我们提出了一个定量框架，使用四个互补指标来评估Grad-CAM解释，这些指标分别衡量时间一致性、显著性运动、解剖学重叠和时间重叠。基于EchoNet-Dynamic数据集，我们将基线2D U-Net与在多个时间步长下训练的ConvLSTM U-Net模型进行了比较。尽管所有模型的分割性能相当，但中间ConvLSTM解释相比最终预测解释表现出显著较低的显著性一致性和更大的质心运动。在所有时间步长下，时间瓶颈解释都比编码器瓶颈解释显著更稳定，而最终的ConvLSTM Decoder3层……（摘要在此处被截断）

    arXiv:2609.08043v1 Announce Type: cross  Abstract: Deep learning has achieved state-of-the-art performance in echocardiographic video segmentation, with an increasing number of models incorporating temporal information. However, quantitative evaluation of temporal explainability remains largely unexplored. We propose a quantitative framework for evaluating Grad-CAM explanations using four complementary metrics measuring temporal consistency, saliency motion, anatomical overlap, and temporal overlap. Using EchoNet-Dynamic, we compare a baseline 2D U-Net with ConvLSTM U-Net models trained across multiple temporal strides. While segmentation performance remained comparable across all models, intermediate ConvLSTM explanations exhibited substantially lower saliency consistency and greater centroid motion than final prediction explanations. Temporal Bottleneck explanations were significantly more stable than Encoder Bottleneck explanations across all strides, while final ConvLSTM Decoder3 e
    
[^122]: MamMA：一种考虑占据地图和行人感知状态的基于Mamba的行人轨迹预测算法

    MamMA: A Mamba-Based Pedestrian Trajectory Prediction Algorithm Considering Occupancy Map and Pedestrian Awareness States

    [https://arxiv.org/abs/2609.08041](https://arxiv.org/abs/2609.08041)

    提出了MamMA，一种基于Mamba的行人轨迹预测算法，通过将占据地图按补丁划分提取障碍物特征，并结合行人感知状态和自我视角图像的细粒度行为信息，提升了人机共存环境中移动机器人的轨迹预测精度与导航安全性。

    

    许多行人轨迹预测算法被提出来改善在人机共存环境中工作的移动机器人的导航安全性。一些行人轨迹预测算法从俯视图图像中提取行人附近障碍物的信息，以提高轨迹预测的准确性。然而，移动机器人通常使用激光雷达（LiDAR）创建局部占据地图，而非俯视图图像。同时，机器人上搭载的视觉传感器提供以机器人为中心的自我视角图像，其中包含机器人附近行人的细粒度行为信息。为了更好地利用激光雷达和机载视觉传感器收集的信息，我们提出了MamMA，一种考虑占据地图和行人感知状态的基于Mamba的行人轨迹预测算法。MamMA将占据地图按补丁划分，并从每个补丁中提取障碍物特征以构建地图特征。

    arXiv:2609.08041v1 Announce Type: cross  Abstract: Many pedestrian trajectory prediction algorithms have been proposed to improve the safety of navigation for mobile robots working in human-robot coexistence environments. Some pedestrian trajectory prediction algorithms extract information about obstacles near pedestrians from top-down view images to improve the accuracy of trajectory prediction. However, mobile robots typically create local occupancy maps using LiDAR, rather than top-down view images. Meanwhile, the vision sensors on board robots provide egocentric view images, which contain fine-grained behavioral information about the pedestrians near the robot. To better use the information collected by LiDAR and on-board vision sensors, we propose MamMA, a Mamba-based pedestrian trajectory prediction algorithm considering occupancy maps and pedestrian awareness states. MamMA divides the occupancy map by patches and extracts obstacle features from each patch to create map features.
    
[^123]: VEX-Bench：用于评估大语言模型智能体判断软件供应链漏洞可利用性能力的基准测试

    VEX-Bench: Benchmarking LLM Agents for Assessing Exploitability of Software Supply Chain Vulnerabilities

    [https://arxiv.org/abs/2609.08040](https://arxiv.org/abs/2609.08040)

    该论文提出了VEX-Bench，这是首个专门用于评估大语言模型智能体判断软件供应链中已知漏洞对下游项目可利用性能力的基准测试，填补了以往基准仅针对零日漏洞场景的空白。

    

    摘要：软件供应链由于依赖复杂而脆弱的依赖关系，已成为日益暴露的攻击面。现有的防御措施（如 GitHub Dependabot）经常产生大量误报，因为其粗粒度匹配无法判断易受攻击的依赖是否真正可被利用。安全分析师通常需要花费大量时间逐一评估漏洞的可利用性。最近的大语言模型（LLM）智能体凭借其在编程和网络安全方面的先进能力，已成为完成这项任务的有前景的候选者，但目前尚无基准测试对此进行评估。以往的基准测试针对的是零日漏洞场景，即智能体检测并利用先前未知的漏洞。相比之下，软件供应链安全关注的是上游依赖中的已知漏洞如何影响下游项目，这要求智能体跨代码库进行推理，并判断上游漏洞是否能够影响下游项目。

    arXiv:2609.08040v1 Announce Type: cross  Abstract: The software supply chain has become an increasingly exposed attack surface because of its reliance on intricate yet fragile dependencies. Existing defenses such as GitHub Dependabot often raise many false alerts because their coarse-grained matching cannot determine whether a vulnerable dependency is actually exploitable. Security analysts typically spend substantial time assessing vulnerability exploitability case by case. Recent LLM agents have emerged as promising candidates for this task given their advanced capabilities in coding and cybersecurity, yet no existing benchmark evaluates them on it. Prior benchmarks target zero-day settings, where agents detect and exploit previously unknown vulnerabilities. In contrast, software supply chain security focuses on how known vulnerabilities in upstream dependencies affect downstream projects. This requires agents to reason across repositories and determine whether an upstream vulnerabil
    
[^124]: 双尺度局部化PCA-Net：用于伪影削减的PDE算子学习的粗全局与局部残差表示

    Two-Scale Localized PCA-Net: Coarse-Global and Local-Residual Representations for Artifact-Reduced PDE Operator Learning

    [https://arxiv.org/abs/2609.08034](https://arxiv.org/abs/2609.08034)

    提出双尺度局部化PCA-Net，通过粗全局PCA基与局部残差PCA基相结合的表示方法，在将PCA拟合成本减半的同时，显著降低了PDE算子学习中的重建误差与块状伪影。

    

    局部化降维可以提升高维偏微分方程（PDE）算子学习的可扩展性，但独立解码的局部块可能引入块间偏移、界面失配以及虚假的高波数内容。我们提出了双尺度局部化PCA-Net（Two-Scale Localized PCA-Net），该方法将解分解为粗全局分量和局部残差修正。一个紧凑的全局PCA基用于捕获域尺度结构，而非重叠的局部PCA基表示剩余的细尺度残差。块平衡的潜在目标函数将两种表示耦合起来，可选的界面感知微调则通过重建损失和迹损失进一步促进连续性。在泊松方程基准测试中，相较于普通及基于重叠的局部化PCA-Net，双尺度表示显著降低了重建误差和可见的块状伪影，同时将PCA拟合成本大约减半。

    arXiv:2609.08034v1 Announce Type: new  Abstract: Localized dimensionality reduction improves the scalability of operator learning for high-dimensional partial differential equations (PDEs), but independently decoded local patches can introduce block offsets, interface mismatches, and spurious high-wavenumber content. We introduce Two-Scale Localized PCA-Net, which decomposes the solution into a coarse-global component and local residual corrections. A compact global PCA basis captures domain-scale structure, while nonoverlapping local PCA bases represent the remaining fine-scale residual. A block-balanced latent objective couples the two representations, and optional interface-aware fine-tuning further promotes continuity through reconstruction and trace losses. On Poisson benchmarks, the two-scale representation substantially reduces reconstruction error and visible block artifacts relative to plain and overlap-based localized PCA-Net while approximately halving PCA fitting cost relat
    
[^125]: 基于语言与风格参考的灵活动作生成

    Flexible Motion Generation from Language and Style References

    [https://arxiv.org/abs/2609.08032](https://arxiv.org/abs/2609.08032)

    FlexMoGen是一个同时以自然语言描述和动作风格示例片段为条件的人体动作合成框架，能够生成既保留文本语义内容、又忠实再现目标风格细节的高质量动作。

    

    我们提出了FlexMoGen，这是一个新颖的框架，能够同时以自然语言描述和动作风格参考为条件进行灵活的人体动作合成。文本提示在定义语义内容方面很有效，但在捕捉细粒度风格细节（如节奏时序、肢体关节表达和富有表现力的动态）方面往往存在局限。风格示例片段通过直接传达这些细微的动作特征来补充文本，使模型能够在保留高层意图的同时再现所需的风格特征。给定一个文本提示和一个风格示例片段，FlexMoGen可以生成高质量的动作，既保留语义内容，又忠实反映目标风格，为用户提供对动画生成过程更大的控制。与依赖离散风格标签且无法泛化到长序列或多风格生成的先前方法不同，FlexMoGen学习了一个变分风格编码器……

    arXiv:2609.08032v1 Announce Type: cross  Abstract: We introduce FlexMoGen, a novel framework for flexible human motion synthesis conditioned on both natural language descriptions and motion style references. Text prompts are effective at defining semantic content, but they are often limited in capturing fine-grained style details such as timing, limb articulation, and expressive dynamics. A style example clip supplements the text by conveying these nuanced motion characteristics directly, enabling the model to preserve high-level intent while reproducing the desired stylistic traits. Given a text prompt and a style example clip, FlexMoGen generates high-quality motions that preserve semantic content while faithfully reflecting the target style, offering users greater control over the animation generation process. Unlike prior methods that rely on discrete style labels and do not generalize to long or multi-style generation, FlexMoGen learns a variational style encoder without style sup
    
[^126]: 部分线性模型的尖锐结构无关极小极大风险

    Sharp Structure-Agnostic Minimax Risk for Partial Linear Models

    [https://arxiv.org/abs/2609.07997](https://arxiv.org/abs/2609.07997)

    该论文刻画了部分线性模型中系数估计的尖锐结构无关极小极大风险，通过新颖的下界证明解决了双重机器学习中双黑箱学习器设定的开放问题。

    

    我们刻画了部分线性模型中系数估计的尖锐结构无关极小极大风险，其中结果变量和处理变量的干扰项分别由两个不同的黑箱学习器学习，这解决了Gu（2025）提出的双重机器学习领域的一个开放问题。对于每个干扰项 \(q\in\{\mu,\pi\}\)，我们通过近似误差预算 \(a_q\) 和随机误差预算 \(s_q\) 来刻画可用学习器的能力，后者通过局部化Rademacher复杂度加以控制。记 \(\mathcal E_n\) 为极小极大均方误差，我们证明：\[\mathcal E_n\asymp1\wedge\left\{\frac1n+\left(a_\mu a_\pi+\min\left\{a_\pi s_\mu+s_\pi^2,\,a_\mu s_\pi+s_\mu^2\right\}\right)^2\right\}.\] 主要的新要素是针对一般双学习器问题的新颖下界。我们的证明利用正交码函数构造了四个有限混合检验实验。在这些实验中，隐藏扰动……

    arXiv:2609.07997v1 Announce Type: new  Abstract: We characterize the sharp structure-agnostic minimax risk for coefficient estimation in the partial linear model when the outcome and treatment nuisances are learned by two distinct black-box learners, which resolves the open problem in double machine learning posed by Gu (2025). For each nuisance \(q\in\{\mu,\pi\}\), we characterize the available learner by an approximation-error budget \(a_q\) and a stochastic-error budget \(s_q\), with the latter controlled through localized Rademacher complexity. Writing \(\mathcal E_n\) for the minimax mean-squared error, we show that \[\mathcal E_n\asymp1\wedge\left\{\frac1n+\left(a_\mu a_\pi+\min\left\{a_\pi s_\mu+s_\pi^2,\,a_\mu s_\pi+s_\mu^2\right\}\right)^2\right\}.\] The main new ingredient is a novel lower bound for the general two-learner problem. Our proof constructs four finite-mixture testing experiments using orthogonal code functions. Across these experiments, the hidden perturbations a
    
[^127]: HyCO：一种用于组合优化的混合神经求解器

    HyCO: A Hybrid Neural Solver for Combinatorial Optimization

    [https://arxiv.org/abs/2609.07990](https://arxiv.org/abs/2609.07990)

    HyCO提出了一种混合推理算法，先用强化学习求解器构造解的前缀，再自适应切换到条件扩散模型完成剩余决策，并通过统一的误差缩放理论框架证明了这种混合方式能结合两类求解器的优势、规避各自的失效模式。

    

    在优化遗憾的视角下，用于神经组合优化的序贯强化学习（RL）求解器与全局扩散模型（DM）求解器呈现出互补的失效模式。前者在构造早期阶段具有较小的边际遗憾，但会遭受逐步累积的复合误差，导致遗憾呈超线性增长；后者虽避免了时间步上的误差复合，但其遗憾会随剩余未求解子空间的维度呈线性或次线性增长。我们提出了组合优化混合神经求解器（HyCO），这是一种混合推理算法，它先使用RL求解器构造解的前缀，再自适应地切换到条件扩散模型以完成剩余的决策。为了刻画这种混合方法为何有效、何时触发切换以及如何在实践中实现，我们首先开发了一个统一的误差缩放理论框架，并证明在明确的误差缩放假设下……

    arXiv:2609.07990v1 Announce Type: cross  Abstract: Sequential reinforcement learning (RL) solvers and global diffusion model (DM) solvers for neural combinatorial optimization exhibit complementary failure modes under an optimization-regret view. The former enjoys small marginal regret in the early construction stage, but suffers from horizon-wise compounding errors with super-linear regret growth; the latter avoids horizon compounding but incurs linear or sublinear regret w.r.t. the dimension of the remaining unsolved subspace. We propose Hybrid Neural Solver for Combinatorial Optimization (HyCO), a hybrid inference algorithm that constructs a solution prefix with an RL solver and adaptively switches to a conditional DM to complete the remaining decisions. To characterize why such hybridization helps, when to trigger the handover, and how to realize it in practice, we first develop a unified error-scaling theoretical framework and prove that, under explicit error-scaling assumptions, 
    
[^128]: 基于大语言模型从影像申请文本中提取文本嵌入的胸部CT检查协议自动选择

    Automated Chest CT Protocol Selection via Large Language Model Derived Text Embeddings from Imaging Request Text

    [https://arxiv.org/abs/2609.07986](https://arxiv.org/abs/2609.07986)

    该研究利用微调的LLaMA-3.1-70B大语言模型从28万余份胸部CT影像申请的自由文本中提取嵌入特征并构建自动协议选择系统，克服了传统关键词方法缺乏上下文理解的局限，实现了更一致、高效的CT协议推荐。

    

    目的：准确的CT协议选择对诊断质量和患者安全至关重要，然而目前该流程依赖人工操作，耗时且容易出现不一致。以往基于关键词或词袋模型的机器学习方法缺乏上下文理解能力，且在罕见协议上表现不佳。我们提出了一种利用大语言模型（LLM）特征的决策支持系统，从自由文本临床适应症中推荐协议，能够捕捉临床细微差别和措辞变化，实现更一致、更高效的协议选择。方法：在这项经研究伦理委员会批准的回顾性研究中，来自一家大型学术医疗中心（2017-2024年）的285,123份胸部CT影像申请被分为训练集（228,099份，80%）和留出测试集（57,024份，20%）。每份申请包含检查名称、临床适应症、HIS备注以及所选协议。临床文本使用微调后的大语言模型Meta的LLaMA-3.1-70B进行嵌入，所得特征输入逻辑回归模型（摘要原文在此处截断）。

    arXiv:2609.07986v1 Announce Type: new  Abstract: Purpose: Accurate CT protocol selection is critical for diagnostic quality and patient safety, yet the current process is manual, time-consuming, and prone to inconsistencies. Prior Machine Learning methods using keywords or bag-of-words lack contextual understanding and perform poorly on rare protocols. We propose a decision support system using large language model (LLM) features to recommend protocols from free-text clinical indications, capturing clinical nuance and phrasing variation for more consistent, efficient selection.   Methods: In this REB-approved retrospective study, 285,123 chest CT imaging requests from a large academic medical center (2017-2024) were split into training (228,099, 80%) and held-out test (57,024, 20%) sets. Each request included procedure names, clinical indication, HIS comments, and the selected protocol. Clinical text was embedded using a fine-tuned LLM, Meta's LLaMA-3.1-70B; these features input a logi
    
[^129]: 使用物理信息神经网络求解弹性波方程：稳健且批判性的评估

    Solving the Elastic Wave Equation with Physics-Informed Neural Networks: A Robust and Critical Assessment

    [https://arxiv.org/abs/2609.07983](https://arxiv.org/abs/2609.07983)

    本文对物理信息神经网络（PINNs）在地震学弹性波方程求解中的应用进行了稳健且批判性的评估，系统考察了其在不同震源、参数模型及不同复杂度问题上的性能表现。

    

    物理信息神经网络（PINNs）最近成为求解偏微分方程（PDEs）的一种有前景的方法，它提供了一种将物理原理融入学习过程的无网格替代方案。与传统离散化方法和纯数据驱动的机器学习技术相比，这代表了一种新的范式。尽管前景广阔，PINNs并非万能；它们继承了谱偏差和不稳定收敛等挑战。此外，它们在地震学中的潜力在很大程度上仍未被探索。在这项工作中，我们对PINNs在地震学中求解弹性波方程进行了稳健且批判性的评估。我们研究了PINNs在各种震源和参数模型上、从恒定到高度异质设置等不同复杂度问题上的性能表现。我们工作的一个关键方面是研究是否直接嵌入物理原理（原文摘要被截断）……

    arXiv:2609.07983v1 Announce Type: new  Abstract: Physics-Informed Neural Networks (PINNs) have recently emerged as a promising approach for solving Partial Differential Equations (PDEs), offering a meshfree alternative that integrates physical principles into the learning process. This presents a new paradigm compared to traditional discretization methods and purely data-driven machine learning techniques. While promising, PINNs are not a panacea; they inherit challenges such as spectral bias and unstable convergence. Moreover, their potential in seismology remains largely unexplored. In this work, we provide a robust and critical assessment of PINNs for solving the elastic wave equation in seismology. We investigate the performance of PINNs on problems with varying degrees of complexity across various seismic sources and parameter models, from constant to highly heterogeneous settings. A pivotal aspect of our work involves investigating whether embedding physical principles directly i
    
[^130]: 空间偏倚采样下的半监督学习

    Semi-Supervised Learning under Spatially Biased Sampling

    [https://arxiv.org/abs/2609.07982](https://arxiv.org/abs/2609.07982)

    该论文系统研究了空间偏倚采样导致半监督学习失效的问题，将边缘分布失配、空间自相关和空间非平稳性作为三种可独立调控的机制，分析了SSL性能退化规律并探索了失效诊断方法。

    

    标准半监督学习（SSL）通常依赖于有标签数据和无标签数据共享相同边缘分布这一假设。当标签是在空间偏倚或优先选址的条件下收集时，这一假设往往会被有偏的空间采样机制所违背。我们将这种边缘分布失配、空间自相关和空间非平稳性视为三种不同的机制，通过有标签采样集中度参数、空间长度尺度和非平稳性强度参数对其进行独立变化，并探讨失配如何降低SSL性能、聚类假设和流形假设是否能在这种失配下依然成立，以及如何诊断由此产生的失效。我们使用受控合成框架以及PovertyMap-WILDS、加州住房、社会经济和美国空气质量监测数据集，在考虑空间自相关和非平稳性的同时，系统地改变失配的程度。通过一系列……

    arXiv:2609.07982v1 Announce Type: new  Abstract: Standard semi-supervised learning (SSL) typically relies on labelled and unlabelled data sharing a common marginal distribution. This assumption is often violated by biased spatial sampling mechanism, when labels are collected under spatially biased or preferential site selection. We treat this marginal mismatch, spatial autocorrelation, and spatial non-stationarity as three distinct mechanisms, varied independently via a labelled-sampling concentration parameter, a spatial length scale, and a non-stationarity strength parameter, and ask how mismatch degrades SSL, whether the cluster and manifold assumptions survive it, and how the resulting failure can be diagnosed. Using a controlled synthetic framework alongside PovertyMap-WILDS, California housing, socio-economic and US air quality monitoring datasets, we systematically vary the degree of mismatch while accounting for spatial autocorrelation and non-stationarity. Through a series of 
    
[^131]: 干净数据准确率并不能保证溯源鲁棒性：音频归因的前瞻性编解码器压力评估

    Clean Accuracy Does Not Guarantee Provenance Robustness: A Prospective Codec-Stress Evaluation of Audio Attribution

    [https://arxiv.org/abs/2609.07981](https://arxiv.org/abs/2609.07981)

    该论文通过前瞻性注册的编解码器压力实验揭示，在干净数据上表现近乎完美的音频溯源归因模型，在经过单阶段编解码器转码后性能损失可高达约70个Macro-F1点，且退化程度强烈依赖条件与编码器表示，证明干净数据准确率并不能保证溯源归因的鲁棒性。

    

    音频溯源归因——即识别哪个系统生成了合成语音——在干净基准测试中被报告具有接近上限的准确率，然而到达分析师手中的音频通常已经过转码处理。我们报告了一项前瞻性注册的测量研究，考察单阶段编解码器传输后的闭集归因性能，其中分析区域在任何归因模型训练之前即依据保真度元数据予以固定。在两个语料库上，在同时施加的组件级条件频带下，支持集内的性能损失分别达到 53.5 [43.5, 63.6] 和 70.3 [63.0, 77.5] 个 Macro-F1 点（WavLM-Base+），以及 61.0 [56.8, 65.1] 和 49.8 [41.6, 57.9] 个点（W2V2-BERT 2.0）。性能退化强烈依赖于具体条件和表示方式：在同一个支持集内测试网格中，WavLM 的损失从 -0.4 到 +53.5 个点不等，且两个编码器在十二个条件中的六个上超出了预设的 ±5 点误差范围。经过干净数据认证的 ECAPA-TDNN 和 Proxy-Anchor 头表现出相当的退化程度，因此该效应并非局限于……（摘要在此处截断）

    arXiv:2609.07981v1 Announce Type: cross  Abstract: Audio provenance attribution - which system produced a synthetic utterance - is reported at near-ceiling accuracy on clean benchmarks, yet audio reaching an analyst has usually been transcoded. We report a prospectively registered measurement of closed-set attribution after single-stage codec transport, with the analysis region fixed from fidelity metadata before any attribution model was trained. On two corpora, in-support losses reach 53.5 [43.5, 63.6] and 70.3 [63.0, 77.5] Macro-F1 points for WavLM-Base+, and 61.0 [56.8, 65.1] and 49.8 [41.6, 57.9] for W2V2-BERT 2.0, under simultaneous component-level bands. Degradation is strongly condition- and representation-dependent: within one in-support grid WavLM losses run from -0.4 to +53.5 points, and the two encoders differ beyond a prespecified +/-5-point margin at six of twelve conditions. A clean-qualified ECAPA-TDNN and a Proxy-Anchor head degrade comparably, so the effect is not con
    
[^132]: 热场签名：从点云到平滑几何

    Heat Field Signatures: From Point Clouds to Smooth Geometry

    [https://arxiv.org/abs/2609.07975](https://arxiv.org/abs/2609.07975)

    该论文提出热场签名（HFS），将点云提升为多尺度平滑环境热场，从而直接从成对距离闭式计算捕捉内在维度、各向异性和尺度过渡等特性的全局与局部几何签名，并引入热维度谱（HDS）紧凑总结多尺度几何构成，为点云几何分析提供了直接接口。

    

    将多尺度几何分析直接应用于不规则点云仍然是一个难题：诸如局部维度、各向异性、密度变化和几何过渡等量通常需要通过显式的邻域、流形或图结构来估计，或者留给神经网络从坐标中自行推断。我们提出了热场签名，它将点云提升为一个多尺度的平滑环境热场族，为从离散样本到几何分析提供了直接的接口。基于该热场，HFS可以直接从成对距离出发，以闭式形式计算全局和局部签名，捕捉热量集中、内在维度、各向异性和尺度过渡等特性。我们进一步引入了热维度谱，这是一种对多尺度几何构成的紧凑总结。HFS可以用作闭式描述符、轻量级学习表示，或作为神经网络的几何特征通道。

    arXiv:2609.07975v1 Announce Type: new  Abstract: Bringing multiscale geometric analysis directly to irregular point clouds remains difficult: quantities such as local dimension, anisotropy, density variation, and geometric transitions are typically estimated through explicit neighborhood, manifold, or graph constructions, or left for neural networks to infer from coordinates. We introduce Heat Field Signatures (HFS), which lift a point cloud to a multiscale family of smooth ambient heat fields, providing a direct interface from discrete samples to geometric analysis.   From this field, HFS computes closed-form global and local signatures directly from pairwise distances, capturing heat concentration, intrinsic dimension, anisotropy, and scale transitions. We further introduce the Heat Dimension Spectrum (HDS), a compact summary of multiscale geometric composition. HFS can be used as a closed-form descriptor, a lightweight learned representation, or a geometric feature channel for neura
    
[^133]: 公平 $k$-均值聚类的次4倍近似算法

    A Sub-4 Approximation for Fair $k$-Means

    [https://arxiv.org/abs/2609.07974](https://arxiv.org/abs/2609.07974)

    本文针对公平 $k$-均值聚类问题提出了首个次4倍近似算法，通过将线性规划松弛与几何变换相结合构造候选中心集合，实现了 $1+(3-1/\Gamma)\rho+O(\epsilon)$ 的近似比。

    

    聚类中的公平性吸引了持续的研究关注，其动机在于需要确保受保护群体在机器学习应用中的公平代表性。我们研究欧几里得空间中的公平 $k$-均值聚类问题，其中每个受保护群体在每个簇中所占的比例必须位于指定的下界与上界之间。这些约束使得同时确定簇中心和点的分配变得极具挑战性。我们提出了一种近似算法，该算法将线性规划松弛与对输入数据的几何变换相结合来构造候选中心集合。给定一个加权 $k$-均值问题的 $\rho$-近似算法以及任意 $\epsilon>0$，我们的算法返回一个分数解，其成本至多为最优整数公平成本的 $1+(3-1/\Gamma)\rho+O(\epsilon)$ 倍，其中 $\Gamma\approx6.357$ 是标准欧几里得 $k$-均值线性规划整数间隙的上界。当使用 PTAS 作为……

    arXiv:2609.07974v1 Announce Type: cross  Abstract: Fairness in clustering has attracted sustained research interest, motivated by the need to ensure equitable representation of protected groups in machine learning applications. We study fair $k$-means clustering in Euclidean space, where the proportion of each protected group in every cluster must lie within specified lower and upper bounds. These constraints make it challenging to determine both cluster centers and point assignments. We propose an approximation algorithm that combines a linear programming relaxation with geometric transformations of the input to construct candidate center sets. Given a $\rho$-approximate algorithm for weighted $k$-means and any $\epsilon>0$, our algorithm returns a fractional solution whose cost is at most $1+(3-1/\Gamma)\rho+O(\epsilon)$ times the optimal integral fair cost, where $\Gamma\approx6.357$ is an upper bound on the integrality gap of the standard Euclidean $k$-means LP. With a PTAS as the 
    
[^134]: MetaKV：面向受限LLM推理的自适应KV缓存压缩

    MetaKV: Adaptive KV Cache Compression for Constrained LLM Inference

    [https://arxiv.org/abs/2609.07966](https://arxiv.org/abs/2609.07966)

    MetaKV是一个自适应KV缓存压缩框架，利用轻量级预测模型根据用户指定的延迟和峰值内存预算为每个输入提示动态选择最优压缩配置，在保证准确性的同时满足资源约束。

    

    键值缓存压缩是降低大语言模型推理内存开销的有效方法，尤其适用于长上下文工作负载。然而，现有的压缩方法在准确性、推理延迟和峰值KV缓存内存利用率之间做出了不同的权衡，使得单一固定配置无法适用于不同的提示词和资源约束。我们提出了MetaKV，这是一个自适应框架，它根据用户指定的延迟和峰值内存预算，为每个输入提示词选择合适的KV缓存压缩配置。MetaKV使用轻量级预测模型来估计每个候选配置的端到端延迟、峰值内存和正确响应概率，并选择在保持准确性的同时最能满足延迟-内存约束的配置。我们在来自三种代表性KV缓存压缩方法的十种配置上对MetaKV进行了评估……

    arXiv:2609.07966v1 Announce Type: new  Abstract: Key--value (KV) cache compression is an effective way to reduce the memory overhead of large language model (LLM) inference, particularly for long-context workloads. However, existing compression methods make different trade-offs among accuracy, inference latency, and peak KV cache memory utilization, making a single fixed configuration unsuitable across different prompts and resource constraints. We introduce MetaKV, an adaptive framework that selects a KV cache compression configuration for each input prompt based on user-specified latency and peak memory budgets. MetaKV uses lightweight prediction models to estimate the end-to-end latency, peak memory, and probability of a correct response for each candidate configuration, and selects the configuration that best satisfies the latency-memory constraints while preserving accuracy. We evaluate MetaKV across ten configurations from three representative KV cache compression methods, KVQuan
    
[^135]: α-Graph：一种注意力注入的可处理图建模归一化流方法

    $\alpha$-Graph: Attention-Infused Normalizing Flow Approach to Tractable Graph Modeling

    [https://arxiv.org/abs/2609.07961](https://arxiv.org/abs/2609.07961)

    本文提出了一种基于注意力的归一化流方法α-Graph，通过可逆注意力机制实现对图数据的显式、可解释且可处理的建模，能够有效捕获复杂的图结构和输入间的相关性。

    

    图建模是表示图结构数据中复杂关系的一项关键任务，近年来已取得显著成功。然而，当前的图建模方法依赖于传统的图神经网络和预训练方法来隐式地学习图数据的潜在关系结构。因此，这些先前的方法无法捕获复杂的图结构和输入之间的相关性。在本文中，我们提出了一种新颖的基于注意力的归一化流方法（ANFA 或 α），它提供了显式、可解释且易于处理的图建模（α-Graph）。特别是，我们提出了一种新的带可逆注意力机制的无条件图归一化流，用于捕获图数据的复杂关系结构。为了进一步增强模型的表达能力，我们引入了条件（摘要在此处被截断）

    arXiv:2609.07961v1 Announce Type: new  Abstract: Graph modeling, a crucial task for representing complex relationships in graph-structured data, has achieved significant success in recent years. However, current graph modeling methods rely on traditional Graph Neural Networks and pre-training approaches to implicitly learn the underlying relational structure of graph data. Thus, these prior methods cannot capture the complex graph structure and correlations among inputs. In this paper, we introduce a novel Attention-based Normalizing Flow-based Approach\footnote{Our implementation and models will be released publicly for research reproducibility.} (ANFA or $\alpha$) that provides an explicit, interpretable, and tractable Graph Modeling ($\alpha$-Graph). In particular, we propose a new Unconditional Graph Normalizing Flow with an Invertible Attention Mechanism to capture the complex relational structure of graph data. To further enhance the expressiveness of the model, we introduce Cond
    
[^136]: 不确定性在评估机器学习模型公平性中的作用

    The Role of Uncertainty in Assessing the Fairness of Machine Learning Models

    [https://arxiv.org/abs/2609.07959](https://arxiv.org/abs/2609.07959)

    该论文超越了传统点估计方法，提出用频率学派和贝叶斯方法量化公平机器学习中的不确定性，从而对模型可能存在的偏见进行更严格的风险评估。

    

    机器学习模型被广泛应用于临床医疗、社交媒体、执法部门和关键基础设施等领域。验证这些模型的输出是否对弱势群体或个人存在偏见，对于确保其公平性并允许其在此类场景中使用至关重要。对可能存在的公平性问题进行严格的风险评估，需要量化与模型选择和估计相关的不确定性。然而，现有文献很少做到这一点，它们大多集中于寻找一个在预测准确性和公平性之间达到合适权衡的单一模型。在本文中，我们超越了点估计的范畴，探讨了公平机器学习中不确定性量化的频率学派和贝叶斯方法，并结合模拟数据和真实数据的实际示例与应用影响加以说明。

    arXiv:2609.07959v1 Announce Type: cross  Abstract: Machine learning models are widely used in clinical applications, social media, law enforcement and critical infrastructure. Verifying whether their outputs are biased against disadvantaged groups or individuals is crucial to ensuring they are fair and allowing their use in such settings. A rigorous risk assessment of possible fairness violations requires quantifying the uncertainty associated with selecting and estimating such models. Yet, this is rarely done in the literature, which focuses on identifying a single model with a suitable trade-off between predictive accuracy and fairness. In this paper, we move beyond point estimation and discuss frequentist and Bayesian approaches to uncertainty quantification for fair machine learning, with practical examples and implications for simulated and real data.
    
[^137]: 基于表格基础模型的流式分层推理

    Streaming Hierarchical Inference with Tabular Foundation Models

    [https://arxiv.org/abs/2609.07956](https://arxiv.org/abs/2609.07956)

    HINT 提出了一种边缘-云协同的分层推理框架，通过滑动窗口上的图近似最近邻记忆在本地进行预测与不确定性估计，仅将不确定的样本连同检索上下文卸载到云端表格基础模型，从而在预测性能与通信成本之间实现有效平衡。

    

    表格基础模型（TFMs）近期通过上下文学习展现出了强大的预测性能，但由于通信开销和延迟问题，其在高吞吐量数据流中的部署仍然充满挑战。我们提出了 HINT，一种将基于边缘的检索与基于云的 TFM 推理相结合的分层推理框架。该框架在滑动窗口上维护一个基于图的近似最近邻记忆，提供本地预测和不确定性估计，使高置信度样本能够在本地处理，而不确定的实例则连同其检索到的上下文被选择性地卸载至云端托管的 TFM。该框架提供了一个卸载阈值和一个邻域检索策略，可通过调节二者来平衡预测性能与通信成本。实验表明，HINT 能够持续找到有利的权衡点。

    arXiv:2609.07956v1 Announce Type: new  Abstract: Tabular Foundation Models (TFMs) have recently demonstrated strong predictive performance through in-context learning, but their deployment in high-throughput data streams remains challenging due to communication overhead and latency. We propose \textit{HINT}, a hierarchical inference framework that combines edge-based retrieval with cloud-based TFM inference. A graph-based approximate nearest neighbor memory maintained over a sliding window provides local predictions and uncertainty estimates, allowing confident samples to be processed locally while uncertain instances are selectively offloaded, together with their retrieved context, to a cloud-hosted TFM. The framework exposes an offloading threshold and a neighborhood retrieval policy that can be varied to balance predictive performance and communication cost. Experiments show \textit{HINT} consistently identifies favorable trade-offs.
    
[^138]: Sinkhorn层中的支撑拓扑与梯度混合

    Support Topology and Gradient Mixing in Sinkhorn Layers

    [https://arxiv.org/abs/2609.07954](https://arxiv.org/abs/2609.07954)

    本文发展了一套固定支撑演算来分析Sinkhorn层中的梯度传播机制，并证明了传输多面体的每个可行面具有成对两跳列重叠是支撑图与边际保证梯度均匀一步收缩的充要条件。

    

    稀疏Sinkhorn层使用固定的支撑图来限制token之间的传输。该图如何控制缩放迭代中的梯度传播？我们发展了一套固定支撑演算，证明在模常数意义下，每个行-列循环会对列势扰动诱导出一个行随机算子。其转置用于传播零质量的反向模式余切。有限循环算子使用两个不同的半步传输计划；在平衡不动点处，它退化为由单一计划确定的两步游走。我们推导了相应的得分项和边际源项，并利用Dobrushin收缩与minorization方法来界定齐次和源驱动的尾部余切。我们的主要结果刻画了支撑和边际在有限得分上一致保证一步收缩的条件：传输多面体的每个可行面必须具有成对的两跳列重叠。否则，某些合适的得分方向（原文此处截断）……

    arXiv:2609.07954v1 Announce Type: new  Abstract: Sparse Sinkhorn layers use a fixed support graph to restrict transport between tokens. How does this graph control gradient propagation through the scaling iterations. We develop a fixed-support calculus showing that each row-column cycle induces a row-stochastic operator on column-potential perturbations modulo constants. Its transpose propagates zero-mass reverse-mode cotangents. The finite-cycle operator uses two distinct half-step transport plans; at a balanced fixed point it reduces to a two-step walk determined by a single plan. We derive the accompanying score and marginal source terms and use Dobrushin contraction and minorization to bound homogeneous and source-driven tail cotangents. Our main result characterizes when support and marginals guarantee one-step contraction uniformly over finite scores: every feasible face of the transportation polytope must have pairwise two-hop column overlap. Otherwise, suitable score directions
    
[^139]: 粘性Burgers方程经典代理模型中的结构化极值误差：一种物理一致的解释

    Structured Extrema Errors in Classical Surrogates for Viscous Burgers: A Physics-Consistent Interpretation

    [https://arxiv.org/abs/2609.07952](https://arxiv.org/abs/2609.07952)

    该研究发现经典机器学习代理模型在逼近粘性Burgers方程时，其单步残差在极值附近呈现结构化分支，且误差与局部曲率（二阶空间导数）的关联远强于与梯度（一阶导数）的关联，并提出了一个物理一致的基于曲率的残差模型来解释这一现象。

    

    摘要（arXiv:2609.07952v1）：我们研究了经典机器学习代理模型在逼近一维粘性Burgers方程时间演化时的局部误差。四种模型在同一预测任务上直接使用空间网格值进行比较：径向基函数（RBF）核岭回归（KRR）、线性岭回归、ExtraTrees和随机森林。在所有四种模型中，单步残差（定义为每个网格点上真实值减去预测值）在预测的极大值和极小值附近形成清晰的弯曲分支。对KRR的更详细分析表明，这些误差与衡量局部曲率的二阶空间导数的关联远强于与一阶空间导数的关联。在光滑极值附近，预测值和曲率形成局部双分支折叠结构。在我们基于局部曲率的残差模型下，这种折叠结构预测了预测值之间的领先阶近抛物线关系。

    arXiv:2609.07952v1 Announce Type: new  Abstract: We study the local errors of classical machine-learning surrogate models, which approximate the time evolution of the one-dimensional viscous Burgers equation. Four models are compared on the same prediction task, using the spatial grid values directly: radial basis function (RBF) kernel ridge regression (KRR), linear Ridge, ExtraTrees, and Random Forests. Across all four models, the one-step residual, defined here as the true value minus the predicted value at each grid point, forms clear curved branches near predicted maxima and minima. A more detailed analysis of KRR shows that these errors are much more strongly related to the second spatial derivative, which measures local curvature, than to the first spatial derivative. Near a smooth extremum, predicted value and curvature form a local two-branch fold. Under our local curvature-based model of the residual, this fold predicts a leading-order near-parabolic relation between predicted
    
[^140]: 语言模型中的信念与行为

    Beliefs and Behavior in Language Models

    [https://arxiv.org/abs/2609.07943](https://arxiv.org/abs/2609.07943)

    本文提出一种实证方法，检验“信念”这一潜在变量能否用于描述大语言模型的行为，发现高性能模型可以被有效地描述为持有信念，这为理解和对齐LLM行为提供了新途径。

    

    关于信念或欲望等抽象概念是否能有效描述大语言模型（LLM）的行为，目前存在很大的不确定性。除了这个问题本身的科学意义之外，这些潜在变量常被用来向用户解释LLM的行为，或用于定义和评估与意图相关的有害行为。然而，我们目前缺乏一种系统性方法来检验“信念”等概念是否适用于LLM，以及它们是否可能成为使模型与人类利益对齐工作中有价值的要素。我们提出了一种实证研究此类问题的方法，探讨从LLM输出中推断出的单一潜在变量——被解释为信念程度——是否能让观察者对LLM将如何响应新提示做出可解释的预测。我们发现，高性能模型可以被有效地描述为持有信念……

    arXiv:2609.07943v1 Announce Type: new  Abstract: There is significant uncertainty about whether abstractions like beliefs or desires usefully describe the behavior of large language models (LLMs). In addition to the inherent scientific interest of this question, these latent quantities are often invoked to explain the behavior of LLMs to users or to define and evaluate harmful behaviors which are relative to intent. Nevertheless, we currently lack a means to systematically test whether concepts like "belief" are well-applied to LLMs, and hence whether they are likely to be fruitful ingredients of attempts to align models with human interests. We propose an approach for empirically studying such questions, asking whether a single latent variable inferred from the LLMs' outputs -- interpreted as a degree of belief -- allows an observer to make interpretable predictions of how the LLMs' will respond to new prompts. We find that highly capable models are usefully described as holding belie
    
[^141]: 患病率校准作为捷径学习缓解方法

    Prevalence calibration as shortcut mitigation

    [https://arxiv.org/abs/2609.07922](https://arxiv.org/abs/2609.07922)

    该论文创新性地将捷径学习重新定义为校准问题，并提出两种与编码器无关的患病率均衡校准方法（处理中正则化和事后重校准）来有效缓解捷径依赖，且可应用于冻结基础模型编码器。

    

    捷径学习是指分类器利用虚假相关性而非诊断性特征的普遍现象。现有的缓解策略大多旨在学习捷径不变表示；但它们的实证效果有限，且无法应用于使用冻结基础模型编码器的分类器。我们提出将捷径学习重新构建为一个本质上属于校准问题的框架：无约束的学习会隐式地将每个捷径组的校准对齐到其训练集中的疾病患病率，使得所得的分类器必然在一组中过度自信而在另一组中自信不足。基于这一见解，我们通过两种与编码器无关的方法实现捷径组之间的患病率均衡校准：一种是处理过程中的正则化器，另一种是事后患病率均衡重校准步骤。在CheXpert和SIIM-ACR数据集的胸腔引流-气胸基准测试中，涵盖微调的CNN和冻结的基础模型编码器

    arXiv:2609.07922v1 Announce Type: new  Abstract: Shortcut learning denotes the widespread situation in which a classifier exploits spurious correlations rather than diagnostic features. Existing mitigation strategies mostly aim to learn shortcut-invariant representations; their empirical success is limited and they cannot be applied to classifiers using frozen foundation model encoders. We propose to reframe shortcut learning as fundamentally a calibration problem: unconstrained learning implicitly calibrates each shortcut group to its training set disease prevalence, rendering the resulting classifier necessarily over-confident in one group and under-confident in the other. Building on this insight, we prevalence-equalize calibration between shortcut groups through two encoder-agnostic methods, an in-processing regularizer and a post-hoc prevalence-equalized recalibration step. Across chest-drain-pneumothorax benchmarks on CheXpert and SIIM-ACR, spanning fine-tuned CNNs and frozen fou
    
[^142]: AVCG：假设分布下反事实生成的广义变分框架

    AVCG: A Generalized Variational Framework for Counterfactual Generation under Hypothesis Distributions

    [https://arxiv.org/abs/2609.07917](https://arxiv.org/abs/2609.07917)

    提出AVCG框架，将反事实生成从依赖单一确定性预测器推广为对任意合理预测假设分布进行变分优化，有效解决了模型重训练或更新后解释失效的脆弱性问题。

    

    反事实解释通过识别对输入实例的修改来形式化“如果……会怎样”的场景，从而获得期望的替代预测。传统上，无论是通过实例特定优化还是摊销式单次前向模型生成，这些方法都依赖于单一的、确定性的点估计预测器。然而，这忽略了预测的不确定性和假设的可变性，导致解释变得脆弱——当底层模型被重新训练或更新时，这些解释经常会失效。为了解决这种脆弱性，我们提出了摊销式变分反事实生成器（AVCG），这是一个广义的优化框架，它将反事实生成表述为对合理预测假设的任意分布进行优化，而非针对单一的确定性预测器进行优化。这一表述自然地兼容了贝叶斯后验、Rashomon受限假设空间以及其他不确定性表示。

    arXiv:2609.07917v1 Announce Type: cross  Abstract: Counterfactual explanations formalize "what-if" scenarios by identifying modifications to an input instance that obtain a desired alternative prediction. Traditionally, whether generated via instance-specific optimization or amortized single pass models, these approaches rely on a single, deterministic point-estimate predictor. However, this ignores predictive uncertainty and hypothesis variability, leading to brittle explanations that frequently become invalid if the underlying model is retrained or updated. To address this fragility, we propose the Amortized Variational Counterfactual Generator (AVCG), a generalized optimization framework that formulates counterfactual generation as optimization over an arbitrary distribution of plausible predictive hypotheses rather than a single deterministic predictor. This formulation naturally accommodates Bayesian posteriors, Rashomon-restricted hypothesis spaces, and other uncertainty represen
    
[^143]: JEDI：面向边缘高效的卫星影像农田分割的JEPA到边缘蒸馏方法

    JEDI: JEPA-to-Edge Distillation for Efficient Cropland Segmentation from Satellite Imagery

    [https://arxiv.org/abs/2609.07915](https://arxiv.org/abs/2609.07915)

    JEDI提出了一种两阶段蒸馏框架，通过跨架构投影对齐与任务训练全程持续的特征蒸馏，将大型I-JEPA教师模型的知识迁移到轻量级SegFormer学生模型，仅用4.04M参数即可实现68.0 mIoU的高效卫星影像农田分割。

    

    大型视觉模型为遥感分割提供了有用的表征，但对于在卫星端或田间边缘部署而言往往成本过高。现有的特征级蒸馏方法通常假设教师与学生架构相似，并且往往在任务训练开始时就停止特征对齐。我们提出了JEDI（JEPA到边缘蒸馏），这是一个两阶段框架，将表征从大型I-JEPA Vision Transformer教师模型迁移到紧凑的SegFormer学生模型。首先，JEDI使用跨架构投影和空间对齐，将学生的终端表征与教师的token空间对齐。随后，在整个任务适应过程中，它联合优化监督分割、温度缩放的响应蒸馏以及持续的特征对齐。在CalCROP21数据集上，JEDI-B0以仅4.04M参数实现了68.0的平均交并比（mIoU），比独立的学生模型提升了16.0个百分点。

    arXiv:2609.07915v1 Announce Type: cross  Abstract: Large vision models provide useful representations for remote-sensing segmentation but are often too expensive for deployment at the satellite or field edge. Existing feature-level distillation methods also tend to assume similar teacher and student architectures and often stop feature alignment when task training begins. We introduce JEDI (JEPA-to-Edge Distillation), a two-stage framework that transfers representations from a large I-JEPA Vision Transformer teacher to a compact SegFormer student. First, JEDI aligns the student's terminal representation with the teacher's token space using cross-architecture projection and spatial alignment. It then jointly optimizes supervised segmentation, temperature-scaled response distillation, and persistent feature alignment throughout task adaptation. On CalCROP21, JEDI-B0 achieves 68.0 mean Intersection-over-Union (mIoU) with 4.04M parameters, improving over the standalone student by 16.0 poin
    
[^144]: 准确率悖论：多标签酶委员会（EC）预测中默认决策阈值的实证诊断[附代码]

    The Accuracy Paradox: Empirical Diagnostic of Default Decision Thresholds in Multi-Label Enzyme Commission Prediction [With Code]

    [https://arxiv.org/abs/2609.07897](https://arxiv.org/abs/2609.07897)

    本研究实证诊断了多标签酶委员会预测中默认决策阈值（t=0.50）在严重类别不平衡下的失效，揭示出高准确率（77.16%）掩盖下的预测崩溃：多数类过度预测、少数类召回率骤降，且EC6的决策边界完全失效。

    

    酶委员会（EC）编号的自动预测在功能注释和计算药物发现中发挥着核心作用。然而，标准的多标签机器学习流程通常依赖默认决策阈值（t=0.50），并假设各目标头的先验分布是平衡的。在本研究中，我们对在严重类别不平衡条件下运行的未校准固定决策边界进行了系统的实证诊断，研究对象为N = 14,096个已注释化合物，这些化合物被划分为六个主要EC类别（EC1-EC6）。我们的结果凸显了一个明显的准确率悖论：尽管该多标签系统达到了看似很高的77.16%平均准确率，但宏平均F1分数（0.3976）和宏平均召回率（0.3872）揭示了严重的预测失效。多数目标类别遭受超敏感性和过度预测的问题，而少数类别则表现出急剧的召回率衰减，最终导致EC6的决策边界完全崩溃。

    arXiv:2609.07897v1 Announce Type: cross  Abstract: Automated prediction of Enzyme Commission (EC) numbers plays a central role in functional annotation and computational drug discovery. However, standard multi-label machine learning pipelines frequently rely on default decision thresholds (t=0.50), assuming balanced prior distributions across target heads. In this study, we present a systematic empirical diagnostic of uncalibrated fixed decision boundaries operating under severe class imbalance across N = 14,096 annotated compounds categorized into six primary EC classes (EC1-EC6). Our results highlight a pronounced Accuracy Paradox: while the multi-label system achieves a deceivingly high mean accuracy of 77.16%, the macro F1-score (0.3976) and macro recall (0.3872) reveal severe predictive breakdown. Majority target classes suffer from hyper-sensitivity and over-prediction, whereas minority classes exhibit sharp recall decay, culminating in a total decision boundary collapse for EC6 
    
[^145]: 基于多模态数据的实时熔化极气体保护焊角焊缝可解释时序注意力缺陷检测

    Explainable Temporal Attention-based Defect Detection For Fillet Joints in Real-Time Gas Metal Arc Welding Based on Multi-modal Data

    [https://arxiv.org/abs/2609.07893](https://arxiv.org/abs/2609.07893)

    本文提出一种基于多模态时序注意力的可解释深度学习模型，利用焊接图像和声音数据实时检测熔化极气体保护焊角焊缝中的内部缺陷，F1分数达到0.99，并通过可解释AI提升了焊接检测的可信度和可靠性。

    

    深度学习是监测实时焊接过程的高效技术，能够减少焊后修补和生产延误。本文利用这种监测能力，提出了一种基于多模态时序注意力的深度学习缺陷检测模型，用于检测熔化极气体保护焊角焊缝过程中难以检测的内部缺陷，包括气孔、未熔透和未熔合、咬边以及冷隔。该模型使用工业协作焊接机器人采集的焊接图像和声音数据进行训练。结果表明，注意力模块可将F1分数提高至0.99。我们使用可解释人工智能来解释所提出模型的行为和数据集分布，确定图像和声音频谱图中潜在的重要区域以及检测每种缺陷的首选模态。这提高了人工智能驱动焊接检测的信任度和可靠性。

    arXiv:2609.07893v1 Announce Type: new  Abstract: Deep learning is an efficient technique to monitor the real time welding process, reducing post-welding repairs and production delays. This paper leverages the monitoring capability by proposing a multi modal temporal attention based deep learning defect detection model for internal defects that are challenging to detect, including porosity, lack of penetration and fusion, undercut, and cold lap during Gas Metal Arc Welding in fillet joints. The model is trained on collected welding images and sound data from an industrial collaborative welding robot. The results show that the attention module can improve the F1 Score to 0.99. We use explainable Artificial Intelligence to interpret the proposed models behavior and dataset distribution, determining potential important areas in image and sound spectrograms and preferred modality to detect each defect. This improves trust and reliability in Artificial Intelligence driven welding inspection.
    
[^146]: 大语言模型层间的即时相互修正

    LLM Layers Immediately Correct Each Other

    [https://arxiv.org/abs/2609.07876](https://arxiv.org/abs/2609.07876)

    该研究发现了大语言模型中的“Transformer层间修正机制”（TLCM）：相邻层会系统性地相互抵消并修正对方的输出，这一机制普遍存在于主流开源模型中，在预训练期间涌现，并能根据上下文依赖程度自适应地校准修正强度。

    

    近期语言模型可解释性研究采用稀疏自编码器等技术，将残差流的贡献分解为线性且具有语义意义的特征。这类方法通常被解读为识别出持续存在于残差流中、并被后续层所构建之上的特征。我们通过识别出Transformer层间修正机制（TLCM）来挑战这一观点：相邻的Transformer层会系统性地抵消彼此贡献中的部分内容。TLCM出现在7个主要开源模型家族中的5个，并在多样化文本中几乎对所有token激活。我们证明TLCM在预训练过程中自然涌现，在依赖上下文的token上作用最强，并能根据前一层的输出自适应地校准其修正强度。利用层雅可比矩阵，我们进一步证明TLCM会选择性地修正特定子空间，同时强化其他子空间，而我们……

    arXiv:2609.07876v1 Announce Type: cross  Abstract: Recent methods in language model interpretability employ techniques such as sparse autoencoders to decompose residual stream contributions into linear, semantically meaningful features. Such methods are commonly interpreted as identifying features that persist in the residual stream and that subsequent layers build upon. We challenge this view by identifying the Transformer Layer Correction Mechanism (TLCM), wherein adjacent transformer layers systematically counteract portions of each other's contributions. TLCM appears in 5 out of 7 major open-source model families and activates across nearly all tokens in diverse texts. We show that TLCM emerges during pretraining, operates most strongly on contextually dependent tokens, and adaptively calibrates its correction strength based on the preceding layer's output. Using the layer Jacobian, we further show that TLCM selectively corrects specific subspaces while reinforcing others, which we
    
[^147]: InfluenceField：一种具有干预可辨识因果结构的可微影响场，用于多模态世界建模

    InfluenceField: A Differentiable Field with Interventionally Identifiable Causal Structure for Multimodal World Modeling

    [https://arxiv.org/abs/2609.07874](https://arxiv.org/abs/2609.07874)

    提出 InfluenceField——一种插入视觉编码器与语言解码器之间的干预感知可微潜场，通过共享转移算子多步传播有向影响来预测局部视觉干预的下游效应，并从理论上证明了干预监督下因果结构（有向依赖图）的可辨识性。

    

    多模态大语言模型通常能够捕捉视觉与语言之间的相关性，但难以预测局部视觉干预如何传播并影响下游的回答。我们提出了 InfluenceField，一种插入在视觉编码器与语言解码器之间的干预感知潜场。它将图像块特征提升为连续的空间表示，通过多个步骤传播有向影响，并借助共享的转移算子预测局部干预效应。训练过程联合优化语言建模、跨环境不变性、反事实展开监督以及结构正则化。针对一个非线性有限基种群模型，我们证明了与目标对齐的干预监督，结合对转移的一步分离条件，能够将可容许的表示限制为位置内的重参数化，从而使完整转移的有向依赖图得以恢复。

    arXiv:2609.07874v1 Announce Type: new  Abstract: Multimodal large language models often capture visual-linguistic correlations but struggle to predict how local visual interventions propagate and affect downstream answers. We introduce InfluenceField, an intervention-aware latent field inserted between the visual encoder and language decoder. It lifts patch features into a continuous spatial representation, propagates directed influence over multiple steps, and predicts local intervention effects through a shared transition operator. Training jointly optimizes language modeling, cross-environment invariance, counterfactual rollout supervision, and structural regularization. For a nonlinear finite-basis population model, we show that target-aligned interventional supervision, together with a one-step separation condition on the transition, restricts admissible representations to within-location reparameterizations, so that the directed dependency graph of the full transition is recovere
    
[^148]: 多类别逻辑回归中的SGD：序贯学习与标度律

    SGD in Multiclass Logistic Regression: Sequential Learning and Scaling Laws

    [https://arxiv.org/abs/2609.07868](https://arxiv.org/abs/2609.07868)

    该论文证明了多类别逻辑回归在高维高斯混合模型上的学习按类别频率从高到低序贯进行，交叉熵风险随训练时间呈幂律衰减，且在固定计算预算下容量项与优化项之间存在精确可刻画的幂律权衡。

    

    我们研究了在具有大量类别的高维高斯混合模型上，多类别逻辑回归的训练动态，并建立了基于梯度优化下交叉熵风险的精确标度律。我们证明学习是按类别从最频繁到最不频繁依次序贯进行的。当类别先验服从幂律分布时，风险动态分解为三个阶段：在学习到第一个类别之前的初始平台期、发生序贯学习的幂律衰减阶段，以及最终的收敛阶段。随后，我们分析了在固定计算预算下模型容量与优化之间的相互作用。当通过投影到前几个主成分来限制有效维度时，风险分解为容量项（关于保留维度的幂律）和优化项（关于训练时间的幂律）。优化这一权衡产生了一个……

    arXiv:2609.07868v1 Announce Type: cross  Abstract: We study the training dynamics of multiclass logistic regression on high-dimensional Gaussian mixture models with a large number of classes and establish precise scaling laws governing the cross-entropy risk under gradient-based optimization. We show that learning proceeds sequentially across classes, from most to least frequent. When the class priors follow a power law distribution, the risk dynamics decompose into three phases: an initial plateau until the first class is learned, a power-law decay regime during which sequential learning occurs, and a final convergence regime.   We then analyze how model capacity interacts with optimization under a fixed compute budget. When the effective dimension is restricted via projection onto leading principal components, the risk decomposes into a capacity term (a power law in the retained dimension) and an optimization term (a power law in training time). Optimizing this tradeoff yields a comp
    
[^149]: 面向可泛化语义与目标导向通信的基础模型

    Foundation Models for Generalizable Semantic and Goal-Oriented Communication

    [https://arxiv.org/abs/2609.07853](https://arxiv.org/abs/2609.07853)

    该论文提出FMSGOC框架，利用视觉-语言基础模型先验选择并传输稀疏语义锚点，结合微调的扩散模型在接收端重建图像，有效解决了语义通信在低码率下泛化能力差的问题。

    

    语义与目标导向通信在6G领域的研究日益增多，但在严格码率预算下超越已见数据的泛化能力仍然是一个关键弱点。许多现有系统对训练数据过拟合，并在极低码率下性能急剧下降，因为它们试图压缩整个信号。我们提出了基础模型引导的语义与目标导向通信（FMSGOC），这是一个利用广泛的视觉-语言基础模型先验来缓解过拟合的框架。该框架通过将比特集中在稀疏的、与目标对齐的锚点上，并依靠生成式基础模型先验来重建被掩蔽的区域，进一步提高了码率效率。通过解耦“发送什么”与“如何重建”，视觉-语言基础模型选择并传输一组稀疏的语义锚点，而在接收端，一个针对掩蔽补全任务微调过的预训练扩散模型负责重建图像。在我们的实验中，FMSGOC达到了0……（原文截断）

    arXiv:2609.07853v1 Announce Type: cross  Abstract: Semantic and goal-oriented communication is increasingly studied for 6G, but generalization beyond seen data remains a key weakness under tight rate budgets. Many existing systems overfit their training data and degrade sharply at very low bit rates because they attempt to compress the entire signal. We introduce Foundation Model-Guided Semantic and Goal-Oriented Communication (FMSGOC), a framework that uses broad visual-linguistic Foundation Model priors to mitigate overfitting. It further improves rate efficiency by concentrating bits on sparse, goal-aligned anchors and relying on generative foundation-model priors to reconstruct the masked regions. By decoupling what to send from how to reconstruct, a vision-language foundation model selects and transmits a sparse set of semantic anchors, while a pretrained diffusion model, fine-tuned for masked completion, reconstructs the image at the receiver. In our experiments, FMSGOC reaches 0
    
[^150]: OCUDU dApp 平台：面向实时 AI-RAN 的开放运行时与 E3 接口

    The OCUDU dApp Platform: An Open Runtime and E3 Interface for Real-Time AI-RAN

    [https://arxiv.org/abs/2609.07843](https://arxiv.org/abs/2609.07843)

    OCUDU dApp 平台首次提供了开放运行时和 E3 接口，使经过签名的 AI-RAN 应用能够在生产 5G 分布式单元内部，按照三种时序契约（GPU 接收链驻留、100 微秒调度截止时间、非阻塞观察者）实时运行，填补了机器学习在 10 毫秒以下超低时延区间的开放平台空白。

    

    机器学习在 3GPP 新空口（NR）5G 分布式单元（DU）内 10 毫秒以下的时延区间展现出最大收益，应用场景包括链路自适应、逐时隙调度、信道估计以及接收机本身。然而此前没有任何开放平台允许独立开发的软件在该区间内运行，先前的 dApp 框架只能作为导出流的外部观察者进入这一领域。本文是对 OCUDU dApp 平台的导引性介绍，该平台是一个开放运行时与 E3 接口，经过签名的 AI-RAN 应用可在生产 DU 内部按照三种时序契约执行：驻留在 GPU 接收链上（A 类）、在调度器 100 微秒的准入截止时间内运行（B 类），或作为永不阻塞的观察者、其结果由调度器消费（C 类）。传统处理路径始终不会被取代，且每项权限都经过类型化、验证并受运营商约束。论文还阐述了运行时、嵌入式 E3 代理以及三个公开代码库之间的协作方式。

    arXiv:2609.07843v1 Announce Type: cross  Abstract: Machine learning has shown its largest gains in the band below 10 ms inside a 3GPP new radio (NR) 5G distributed unit (DU): link adaptation, per-slot scheduling, channel estimation, and the receiver itself. No open platform has let independently built software run there. Prior dApp frameworks reached the band only as external observers of an export stream. This paper is a guided introduction to the OCUDU dApp platform, an open runtime and E3 interface under which signed AI-RAN applications execute inside a production DU under three timing contracts: resident on the GPU receive chain (Class A), inside the scheduler's 100 us admitted deadline (Class B), or as never-blocking observers whose results the scheduler consumes (Class C). The conventional path is never displaced, and every authority is typed, validated, and operator-bounded. The paper explains how the runtime, the embedded E3 agent, and the three public repositories fit together
    
[^151]: A*-Thought-V2：基于大语言模型几何动力学的高效潜在推理

    A*-Thought-V2: Efficient Latent Reasoning via Geometric Dynamics of LLM

    [https://arxiv.org/abs/2609.07821](https://arxiv.org/abs/2609.07821)

    A*-Thought-V2将思维链建模为隐藏状态轨迹，在大语言模型的三维PCA几何空间中测量各推理步骤与全局解答方向的对齐角度，把偏离方向的步骤压缩为连续潜在token、对齐步骤保留为显式文本，从而在保留关键中间信息的同时大幅降低思维链推理的计算与上下文开销。

    

    思维链提升了大语言模型（LLM）的推理能力，但会带来大量的计算和上下文开销。现有方法要么通过硬剪枝丢失中间信息，要么缺乏一个有原则依据的连续压缩准则。我们提出了A*-Thought-V2，一个由LLM几何动力学引导的框架，它将思维链建模为隐藏状态轨迹，并用显式-隐式交错的潜在架构取代硬性删除。在将问题、步骤和解答的表示投影到三维PCA空间后，该框架测量每个局部状态转移与全局“问题到解答”方向之间的对齐程度。对齐的步骤保留为显式文本，而偏离的步骤则被压缩为连续的潜在token。方向角度同时捕捉局部语义和推理动态：小角度表示直接执行和答案形成，而大角度更频繁地涉及检查、纠正（原文在此处截断）。

    arXiv:2609.07821v1 Announce Type: cross  Abstract: Chain-of-Thought (CoT) improves the reasoning ability of Large Language Models (LLMs) but incurs substantial computation and context costs. Existing methods either lose intermediate information through hard pruning or lack a principled criterion for continuous compression. We present A*-Thought-V2, a geometric dynamics of LLM guided framework that models CoT as a hidden-state trajectory and replaces hard deletion with an explicit-implicit interleaved latent architecture. After projecting question, step, and solution representations into a 3D PCA space, it measures alignment between each local transition and global question-to-solution direction. Aligned steps remain explicit text, whereas deviating steps are compressed into continuous latent tokens. Directional angles capture both local semantics and reasoning dynamics: small angles indicate direct execution and answer formation, while large angles more frequently involve checking, cor
    
[^152]: 卡尔曼增量网络：不确定性感知的联想记忆

    Kalman Delta Networks: Uncertainty-aware Associative Memory

    [https://arxiv.org/abs/2609.07816](https://arxiv.org/abs/2609.07816)

    该论文提出卡尔曼增量网络（KDNs），将循环联想记忆重新表述为线性-高斯状态空间模型，利用卡尔曼滤波同时传播记忆状态及其不确定性，使每次记忆写入的强度能够根据积累的证据进行自适应调节。

    

    线性注意力因其在高效长上下文推理和恒定内存解码方面的优势，越来越多地应用于前沿语言模型中。然而，其固定大小的循环记忆需要在每个token处做出在线决策：在尚不知道未来查询需要哪些信息的情况下，决定写入什么内容以及以多强的力度覆盖现有关联。增量规则（Delta-rule）模型从当前token嵌入中学习这一写入强度，但不追踪记忆估计的置信度，导致每次写入无法根据积累的证据进行自适应调整。为了显式地表示这种不确定性，我们将循环联想记忆重新表述为线性-高斯状态空间模型（卡尔曼滤波器是其最优递归估计器），并由此引入了一类新的模型族——卡尔曼增量网络。在KDN中，状态转移同时传播记忆状态及其不确定性，使得卡尔曼增益能够根据积累的证据对每次残差写入进行加权。

    arXiv:2609.07816v1 Announce Type: cross  Abstract: Linear attention is increasingly used in frontier language models for efficient long-context inference and constant-memory decoding. Its fixed-size recurrent memory, however, requires an online decision at each token: what to write and how strongly to overwrite existing associations before knowing which information future queries will require. Delta-rule models learn this strength from the current token embedding but do not track confidence in the memory estimate, preventing each write from adapting to accumulated evidence. To represent this uncertainty explicitly, we reformulate recurrent associative memory as a linear--Gaussian state-space model, for which the Kalman filter is the optimal recursive estimator, and introduce a new family of models, Kalman Delta Networks (KDNs). Within KDNs, the transition propagates both the memory state and its uncertainty, allowing the Kalman gain to weight each residual write by accumulated evidence
    
[^153]: Latent-MoE：面向多物理区域PDE的域感知混合专家模型

    Latent-MoE: Domain-Aware Mixture-of-Experts for PDEs with Multi-Regime Physics

    [https://arxiv.org/abs/2609.07814](https://arxiv.org/abs/2609.07814)

    提出Latent-MoE架构，通过在共享骨干网络中嵌入具有紧支撑路由器的域感知混合专家模块，使NTK呈指数衰减的带状结构以实现学习的局部化，从而解决PINNs在求解域内物理特性多变化的PDE时的远程耦合与梯度冲突问题。

    

    物理信息神经网络（PINNs）在处理控制物理随域内空间位置变化的偏微分方程（PDE）时表现不佳。我们将这一问题归因于标准坐标网络的一个结构特性：其神经正切核（NTK）具有平移变异性，使得坐标幅值较大的训练点会不成比例地影响其他位置的预测，从而在训练过程中产生远程耦合和梯度冲突。我们从理论和实验上证明，采用中心化、紧支撑路由器的混合专家（MoE）架构能够产生均匀带状的NTK，其核回归权重随距离呈指数衰减，从而实现学习的局部化。基于这一发现，我们提出了Latent-MoE，它在共享骨干网络中交错嵌入域感知的MoE模块。与FB-PINNs或X-PINNs将域和参数进行刚性划分、使得不同子域上的参数只能独立更新不同，Latent-MoE的设计

    arXiv:2609.07814v1 Announce Type: new  Abstract: Physics-informed neural networks (PINNs) struggle on PDEs whose governing physics varies across the domain. We trace this to a structural property of standard coordinate networks: their neural tangent kernel (NTK) is translation-variant and lets training points of large coordinate magnitude disproportionately influence predictions elsewhere, producing long-range coupling and gradient conflict during training. We show analytically and empirically that mixture-of-experts (MoE) architectures with centered, compact-support routers yield a uniformly banded NTK whose kernel-regression weights decay exponentially with distance, localizing the learning. Building on this, we propose \emph{Latent-MoE}, which interleaves domain-aware MoE blocks within a shared backbone. Unlike FB-PINNs or X-PINNs, which rigidly partition both the domain and the parameters so that the parameters on different subdomains are updated independently, Latent-MoE is design
    
[^154]: 你无法偏好从未采样到的情感：DPO微调大语言模型中的情感强度不足现象

    You Can't Prefer Emotions You Don't Sample: Intensity Undershoot in DPO-Tuned LLMs

    [https://arxiv.org/abs/2609.07808](https://arxiv.org/abs/2609.07808)

    本文首次量化了DPO微调的大语言模型在情感控制上的系统性强度不足现象——实际情感增益仅为请求值的0.13-0.26（理想值为1），并将其根源归结为偏好学习训练数据以中性情感为主、采样候选很少触及极端情感这一流程性缺陷。

    

    当你要求一个语言模型“非常兴奋地”回答时，其输出通常只是略微更有活力一些。我们对这一效应进行了量化。我们将一个经过指令微调的大语言模型（LLM）条件化于连续的效价-唤醒度（VA）目标——其中效价衡量一种状态的愉悦程度，唤醒度衡量其激活程度——使用冻结的回归器测量实际达到的情感效果，并将请求的目标从-1扫描至+1。实际响应远低于所要求的：增益（实际情感相对于请求情感的斜率）在Llama-3.1-8B上，效价仅为0.26，唤醒度仅为0.13，而一个忠实的控制器应得分为1。模型系统性地无法达到所请求的情感强度，这为Fazzi等人（2025）的定性观察提供了具体数字。我们的实验将这一现象追溯到偏好学习流程本身：来自EmoBank等自然语料库的训练目标以中性情感为主，且采样的候选响应本身很少达到极端情感，因此DPO（直接偏好优化）训练……

    arXiv:2609.07808v1 Announce Type: cross  Abstract: Ask a language model to respond "very excitedly," and its output is typically only mildly more energetic. We quantify this effect. We condition an instruction-tuned LLM on a continuous Valence-Arousal (VA) target, where valence measures how pleasant a state is and arousal how activated it is, measure the achieved affect with a frozen regressor, and sweep the requested target from -1 to +1. The response moves far less than asked: the gain, the slope of achieved against requested affect, is only 0.26 for valence and 0.13 for arousal on Llama-3.1-8B, where a faithful controller would score 1. The model systematically undershoots requested emotional intensity, which puts a number on the qualitative observation of Fazzi et al. (2025). Our experiments trace this to the preference-learning pipeline. Training targets from natural corpora such as EmoBank are neutral-heavy, and the sampled candidates themselves rarely reach extreme affect, so Di
    
[^155]: CodeTD：注意力拓扑检测代码大语言模型中的幻觉

    CodeTD: Topology of Attention Detects Hallucinations in Code LLMs

    [https://arxiv.org/abs/2609.07779](https://arxiv.org/abs/2609.07779)

    CodeTD是首个基于代码大语言模型注意力图拓扑数据分析的执行前幻觉检测方法，可预测生成代码的正确性，性能超越现有基线且能跨编程基准迁移。

    

    随着AI代码助手工具的日益普及，自动评估生成代码的正确性成为一项重大挑战。代码大语言模型（Code LLMs）容易产生幻觉，这可能导致代码无法解决所需的问题，甚至产生存在严重安全漏洞的代码。在本文中，我们提出了CodeTD——首个基于代码大语言模型注意力图的拓扑数据分析（TDA）的代码执行前正确性评估方法。我们的方法利用注意力图的拓扑模式来量化提示与生成结果之间的不匹配。我们在常见基准（HumanEval、MBPP、BigCodeBench、MultiPL-E）、5种编程语言以及10个参数规模最高达34B的代码大语言模型上进行了实验。实验结果表明，所提出的方法优于近期基线方法。此外，CodeTD可在不同编程基准之间迁移。

    arXiv:2609.07779v1 Announce Type: cross  Abstract: As AI-code assistant tools become widespread, automatic assessment of the correctness of generated code becomes a significant challenge. Code LLMs are prone to hallucinations, which may lead to code that does not solve the required problem, or even to code with severe security vulnerabilities. In this paper, we introduce CodeTD -- the first approach to pre-execution assessment of code correctness based on topological data analysis (TDA) of Code LLMs' attention maps. Our method quantifies prompt-generation mismatch using topological patterns of attention maps. We carry out experiments with common benchmarks (HumanEval, MBPP, BigCodeBench, MultiPL-E), 5 programming languages and 10 Code LLMs of size up to 34B parameters. The experimental results show that the proposed method outperforms recent baselines. Moreover, CodeTD is transferable between coding benchmarks.
    
[^156]: 超奈奎斯特信号传输中用于Bi-LSTM检测的预白化与BCJR后验蒸馏

    Pre-Whitening and BCJR Posterior Distillation for Bi-LSTM Detection in Faster-than-Nyquist Signaling

    [https://arxiv.org/abs/2609.07762](https://arxiv.org/abs/2609.07762)

    研究发现在超奈奎斯特信号的Bi-LSTM检测中，多窗口嵌套架构无法提升误码率性能，真正的瓶颈在于观测模型而非网络架构，因此提出通过预白化和BCJR后验蒸馏来改进。

    

    循环检测器，如双向长短期记忆网络，是超奈奎斯特信号传输中最优的Bahl-Cocke-Jelinek-Raviv（BCJR）检测器的低复杂度替代方案。受将码间干扰（ISI）结构嵌入自身架构的卷积检测器的启发，我们探究在独立的循环分支中处理嵌套的ISI窗口能否改善Bi-LSTM的误码率（BER）。在大约260次受控训练中，结果表明并不能：在匹配的参数预算和匹配的读取层条件下，多窗口架构从未显著优于普通的Bi-LSTM。嵌套窗口化是一种可逆的重新排列，不会增加任何信息；额外的分支只会带来瓶颈；蒸馏诊断表明该网络在其窗口范围内已接近最优。因此，限制因素在于观测模型，而非网络架构。在保持架构不变的前提下，我们对预……（原文摘要在此处被截断）

    arXiv:2609.07762v1 Announce Type: cross  Abstract: Recurrent detectors such as bidirectional long short-term memory (Bi-LSTM) networks are low-complexity alternatives to the optimal Bahl-Cocke-Jelinek-Raviv (BCJR) detector for faster-than-Nyquist (FTN) signaling. Motivated by convolutional detectors that build the intersymbol interference (ISI) structure into their architecture, we ask whether processing nested ISI windows in separate recurrent branches improves the bit error rate (BER) of a Bi-LSTM. Across roughly 260 controlled trainings it does not: at a matched parameter budget and a matched readout, the multi-window architecture never significantly beats a plain Bi-LSTM. Nested windowing is an invertible rearrangement that adds no information, extra branches only add bottlenecks, and a distillation diagnostic shows the network is already near optimal for its window. The limitation is therefore the observation model, not the architecture. Keeping the architecture fixed, we pre-whit
    
[^157]: 分解引导的扩散语言模型用于惯性约束聚变预测

    Decomposition-Guided Diffusion Language Models for Inertial Confinement Fusion Prediction

    [https://arxiv.org/abs/2609.07756](https://arxiv.org/abs/2609.07756)

    提出据信首个基于语言模型的惯性约束聚变预测器ICF-DLM，通过物理类型化分解、双向去噪和物理驱动PPO奖励，直接从激光脉冲与靶设计参数准确预测中子率波形。

    

    arXiv:2609.07756v1 公告类型：新论文 摘要：惯性约束聚变（ICF）是迈向清洁能源的主要途径之一，但国家点火装置（NIF）每一次实验的成本约为一百万美元，这使得精确的AI代理模型成为极具价值的研究目标。我们研究了外生驱动的ICF波形预测问题，即需要直接从激光脉冲和靶设计参数推断出512步的中子率诊断信号，而无需观测任何历史响应数据。该场景对标准时间序列预测器提出了严峻挑战：时间稀疏性（纳秒级时间窗口中仅存在皮秒级峰值）、输入输出尺度不匹配（仅有不到300次真实实验数据），以及峰值敏感性（皮秒级时序精度）。我们提出了ICF-DLM，据我们所知这是首个基于语言模型的ICF预测器，它结合了：（i）物理类型化分解，将输出分解为产额 $Y_{DT}$、峰值时刻 $t_{\mathrm{peak}}$ 和局部波形 $w_{\mathrm{local}}$；（ii）双向去噪机制，推迟对峰值位置的确定；（iii）物理驱动的PPO奖励。

    arXiv:2609.07756v1 Announce Type: new  Abstract: Inertial confinement fusion (ICF) is a leading pathway toward clean energy, but each shot at the National Ignition Facility costs on the order of one million dollars, making accurate AI surrogates a high-value target. We study exogenous-driven ICF waveform prediction, where a 512-step neutron-rate diagnostic must be inferred directly from a laser pulse and target design parameters, with no historical response observed. The regime stresses standard time-series predictors with temporal sparsity (picosecond peak in a nanosecond window), input-output scale mismatch (under 300 real shots), and peak sensitivity (picosecond timing). We propose ICF-DLM, to our knowledge the first LM-based ICF predictor, combining (i) a physics-typed decomposition into yield $Y_{DT}$, peak timing $t_{\mathrm{peak}}$, and local waveform $w_{\mathrm{local}}$; (ii) bidirectional denoising that defers commitment to peak location; and (iii) a physics-driven PPO reward
    
[^158]: 带权重衰减的梯度下降下神经网络泛化动力学的理论分析

    A Theoretical Analysis of Generalization Dynamics in Neural Networks under Gradient Descent with Weight Decay

    [https://arxiv.org/abs/2609.07755](https://arxiv.org/abs/2609.07755)

    本文提出了一个理论框架，证明了带权重衰减的梯度下降会收敛到经验损失全局极小值的邻域，并将总体误差分解为数据误差、优化误差和预测变化误差分别进行界定，其中通过局部近似齐次性对预测变化误差给出了明确的逐单元和逐层界。

    

    理解泛化仍然是机器学习中的核心挑战，因为它需要同时考虑数据、架构和训练动力学。在本文中，我们开发了一个理论框架，刻画这些因素如何在整个训练过程中共同塑造泛化性能。更确切地说，我们研究了在ℓ²损失下通过带权重衰减的梯度下降（GD）训练的一类广泛的神经网络，并证明了GD收敛到经验损失全局极小值的一个邻域。通过基于输入数据对空间进行划分，我们将总体误差分解为数据误差、优化误差和预测变化误差，并分别对它们进行界定。特别是，对于衡量学习函数振荡的预测变化误差，我们提出了（局部）近似齐次性，并推导出其演变过程的明确的逐单元和逐层界。

    arXiv:2609.07755v1 Announce Type: new  Abstract: Understanding generalization remains a central challenge in machine learning because it requires jointly considering data, architecture, and training dynamics. In this paper, we develop a theoretical framework that characterizes how these factors jointly shape generalization performance throughout training. More precisely, we study a broad class of neural networks trained under the $\ell^2$ loss by gradient descent (GD) with weight decay, and prove the convergence of GD to a neighbourhood of the global minimizers of the empirical loss. By partitioning the space based on the input data, we then decompose the population error into data error, optimization error, and prediction variation error, and bound them separately. In particular, for the prediction variation error, which measures the oscillations of the learned function, we propose (local) approximate homogeneity and derive explicit cellwise and layerwise bounds for its evolution alon
    
[^159]: 利用光学视觉基础模型进行SAR目标识别的跨模态学习

    Cross-modal learning for SAR target recognition using optical vision foundation models

    [https://arxiv.org/abs/2609.07753](https://arxiv.org/abs/2609.07753)

    本文提出一种跨模态EO到SAR原型对齐框架，利用基于DINOv3的冻结光学编码器构建类别级光学原型，无需严格的EO/SAR配对数据即可为SAR目标识别提供类别级监督，缓解SAR标注数据稀缺和域差距问题。

    

    arXiv:2609.07753v1 公告类型：交叉 摘要：合成孔径雷达（SAR）凭借其多功能、远距离和近全天候的工作能力，在广泛的成像应用中是一种重要的成像模态。然而，由于标记数据有限、SAR图像中存在强斑点噪声，以及SAR与更丰富的光学图像之间存在显著的域差距，自动目标识别（ATR）仍然是一个具有挑战性的问题。相比之下，电光（EO）图像受益于海量数据集、更清晰的视觉结构以及强大的基础模型。在这项工作中，我们研究了在光学数据上训练的视觉基础模型如何能够为SAR分类提供类别级别的监督。我们提出了一种从EO到SAR的跨模态原型对齐框架，其中使用基于DINOv3视觉基础模型的冻结EO编码器来构建类别级别的光学原型，而无需严格的EO/SAR配对图像。随后训练SAR模型对SAR图像进行分类，同时……

    arXiv:2609.07753v1 Announce Type: cross  Abstract: Synthetic Aperture Radar (SAR) is an important modality in a wide range of imaging applications due to its versatile, long range and near all weather operating capabilities. However, Automatic Target Recognition (ATR) remains a challenging problem due to limited labelled data, the strong speckle in SAR images and the significant domain gap between SAR and more abundant optical imagery. In contrast, electro-optical (EO) imagery benefits from massive datasets, clearer visual structure and powerful foundation models. In this work, we investigate how vision foundation models trained on optical data can provide class level supervision for SAR classification.   We propose a cross-modal EO to SAR prototype alignment framework in which a frozen EO encoder, based on a DINOv3 vision foundation model, is used to construct class level optical prototypes without requiring strict EO/SAR pairs. A SAR model is then trained to classify SAR images while
    
[^160]: 局部梯度神经算子

    Local gradient neural operator

    [https://arxiv.org/abs/2609.07752](https://arxiv.org/abs/2609.07752)

    提出局部梯度神经算子（LGNO），一种基于非线性梯度离散化先验的轻量级、可解释的神经算子，可高效解决力学问题中的场时间演化预测与源识别任务。

    

    场的时间演化预测与源识别是动力系统中的经典问题。解决这些问题的传统方法依赖于对控制偏微分方程（PDE）的透彻理解。近年来，以神经算子为代表的深度学习为处理此类任务提供了一种数据驱动的范式。然而，现有的面向PDE的全局神经算子大多需要大量训练数据和众多可学习参数，且可解释性与泛化能力有限。我们提出局部梯度神经算子（LGNO），作为解决典型力学问题中场时间演化预测与源识别的轻量级、可解释的替代方案。该方法建立在非线性梯度离散化的先验知识基础上，利用多层感知机卷积层学习平移不变的局部核，这些局部核类似于离散模板。一个零一致模板……

    arXiv:2609.07752v1 Announce Type: new  Abstract: Field temporal prediction and source identification constitute canonical problems in dynamical systems. Conventional approaches to these problems depend on a thorough understanding of the governing partial differential equations (PDEs). Recently, deep learning, as represented by neural operators, has provided a data-driven paradigm for addressing such tasks. However, most existing global neural operators for PDEs require large training datasets and many learnable parameters, with limited interpretability and generalization. We propose the local gradient neural operator (LGNO) as a lightweight and interpretable alternative for field temporal evolution prediction and source identification in typical mechanical problems. The method builds on priors from nonlinear gradient discretization and uses multilayer perceptron convolutional layers to learn translation-invariant local kernels that resemble discrete stencils. A zero consistent stencil 
    
[^161]: 引导众包竞赛中的工作者自选择：一种LLM增强的算法方法

    Guiding Worker Self-Selection in Crowdsourcing Contests: An LLM-Augmented Algorithmic Approach

    [https://arxiv.org/abs/2609.07749](https://arxiv.org/abs/2609.07749)

    本文提出贪心多项式时间框架GRAF及由大语言模型驱动的进化框架LLMScore，用于在众包竞赛中为工作者推荐竞赛，在Tullock竞赛自选择模型下实现工作者零遗憾和平台最优性。

    

    众包平台协调着大量在线工作者，这些工作者会策略性地选择参加哪些竞赛以及投入多少努力。这种自选择可能导致重要的竞赛参与者过少或努力投入不足，而工作者也可能会后悔参加那些使其境况不如其他可选方案的竞赛。我们研究平台如何利用Tullock竞赛中的自选择（SSTC）向工作者推荐竞赛，SSTC是一个两阶段模型，工作者首先选择竞赛，然后在其中展开竞争。我们提出了GRAF，一个贪心的多项式时间框架，它通过根据分数向量对工作者进行排序来构建自选择结果，并在SSTC的特殊情况下保证工作者零遗憾和平台最优性。由于在工作者存在异质性的情况下难以设计有效的排序，我们提出了LLMScore，一个由大语言模型驱动的进化框架，能够自动设计GRAF的评分算法。

    arXiv:2609.07749v1 Announce Type: new  Abstract: Crowdsourcing platforms coordinate large pools of online workers who strategically choose which contests to enter and how much effort to invest. This self-selection can leave important contests with too few participants or too little effort, while workers may regret entering contests that leave them worse off than available alternatives. We study how platforms can recommend contests to workers using self-selection in Tullock contests (SSTC), a two-stage model in which workers first choose contests and then compete within them. We introduce GRAF, a greedy polynomial-time framework that constructs self-selection outcomes by ordering workers according to a score vector, with guarantees of zero worker regret and platform optimality in special cases of SSTC. Because effective orderings are difficult to design under worker heterogeneity, we propose LLMScore, an LLM-driven evolutionary framework that automatically designs GRAF's scoring algorit
    
[^162]: 高性能计算系统上分布式Kolmogorov-Arnold网络训练的可扩展性分析

    Scalability Analysis of Distributed Kolmogorov-Arnold Network Training on High-Performance Computing Systems

    [https://arxiv.org/abs/2609.07740](https://arxiv.org/abs/2609.07740)

    本文首次系统性地研究了Kolmogorov-Arnold网络在多节点多GPU高性能计算系统上的数据并行训练可扩展性，实验表明在8个GPU上可实现74.7%的并行效率和5.97倍加速。

    

    Kolmogorov-Arnold网络（KANs）用网络边上可学习的单变量函数取代了多层感知机（MLPs）的固定激活函数和线性权重，提供了更好的可解释性，并且在某些设置下具有有竞争力的参数效率。虽然KANs的逼近性质已受到广泛关注，但其在分布式多GPU训练下的行为尚未得到系统性的表征。本文对多节点、多GPU高性能计算（HPC）基础设施上的数据并行KAN训练进行了实证可扩展性研究，从四个维度进行评估：强扩展性、弱扩展性、通信开销和模型规模扩展。实验在FinisTerrae III超级计算机上进行，使用PyTorch分布式数据并行（DDP），跨4个节点最多使用8块NVIDIA A100 GPU。KAN训练在8个GPU上达到74.7%的并行效率，加速比达5.97倍。

    arXiv:2609.07740v1 Announce Type: cross  Abstract: Kolmogorov-Arnold Networks (KANs) replace the fixed activation functions and linear weights of Multi-Layer Perceptrons (MLPs) with learnable univariate functions on network edges, offering improved interpretability and, in some settings, competitive parameter efficiency. While the approximation properties of KANs have received considerable attention, their behavior under distributed, multi-GPU training has not been systematically characterized. This paper presents an empirical scalability study of data-parallel KAN training on multi-node, multi-GPU high-performance computing (HPC) infrastructure, evaluated along four dimensions: strong scaling, weak scaling, communication overhead, and model-size scaling.   Experiments were conducted on the FinisTerrae III supercomputer using up to 8 NVIDIA A100 GPUs across 4 nodes with PyTorch Distributed Data Parallel (DDP). KAN training reaches 74.7% parallel efficiency at 8 GPUs with a 5.97x speedu
    
[^163]: 归因Cohen's d：常模年龄生物标志物中疾病相关效应的训练数据归因

    Attributing Cohen's d: Training Data Attribution for Disease-Related Effects in Normative Age Biomarkers

    [https://arxiv.org/abs/2609.07729](https://arxiv.org/abs/2609.07729)

    本文提出一种基于Cohen's d的闭式影响泛函，可直接将常模年龄模型中疾病相关的年龄差距效应归因到个体训练样本，并在UK Biobank上证明移除最有影响力的训练样本可显著提高疾病相关效应量。

    

    常模年龄模型在名义上健康的队列中进行训练，用于预测实际年龄（实际年龄）。当应用于患者时，模型会产生偏差，预测年龄与实际年龄之间的差距被解读为疾病风险。本文将年龄差距的疾病相关效应量直接归因于个体训练样本，而不是以预测级别的损失作为归因目标。对于Cohen's d，由此得到的闭式影响泛函（经留一法重训练验证）可根据训练样本对留出数据的病例-对照分离效果的影响对其进行排序。在英国生物银行（UK Biobank）的四种疾病和两种生物标志物模态上，移除最具影响力的10%训练样本在所有随机种子下均能提高留出的疾病相关效应量：对于2型糖尿病，代谢组学年龄效应增加了一倍以上；对于多发性硬化症，脑年龄效应提高了约三分之一。而随机移除即使达到50%，效应量也保持不变，这证实了……

    arXiv:2609.07729v1 Announce Type: new  Abstract: Normative age models are trained to predict chronological age in a nominally healthy cohort. Applied to patients, they deviate, and the gap between predicted and chronological age is read as disease risk. Here, we attribute the disease-related effect size of the age gap directly to individual training samples, rather than using a prediction-level loss as the attribution target. For Cohen's $d$, the resulting closed-form influence functional, validated against leave-one-out retraining, ranks training samples by their effect on held-out case-control separation. Across four diseases and two biomarker modalities in UK Biobank, removing the 10% most influential training samples raises held-out disease-related effect size in every seed. It more than doubles the metabolomic-age effect for type-2 diabetes and raises the brain-age effect for multiple sclerosis by roughly a third. Random removal leaves effect size flat even at 50% removal, confirm
    
[^164]: 基于课程微调CNN-Transformer的Sub-6 GHz频段空中自动调制分类

    Sub-6 GHz Over-the-Air AMC via Curriculum Fine-Tuned CNN-Transformers

    [https://arxiv.org/abs/2609.07726](https://arxiv.org/abs/2609.07726)

    本研究提出一种课程微调策略，将在915 MHz线缆链路数据上训练的CNN-Transformer自动调制分类模型迁移至4 GHz真实空中链路环境，并系统评估了不同距离和天线失准条件下的分类性能。

    

    自动调制分类（AMC）模型通常在合成数据或信道线缆连接的数据上进行训练和验证，这留下了一个悬而未决的问题：当真实自由空间链路引入路径损耗和天线指向误差后，这些模型的表现如何。我们报告了一项针对混合CNN-Transformer AMC模型的课程微调研究。支撑该模型的通用全32类数据集完全在915 MHz频率下，通过受控的时钟/PPS同步MIMO扩展线缆链路（而非空间复用传输）构建；随后所有的自由空间真实硬件实验——包括顺序微调课程、匹配距离评估以及所有报告的空中准确率数据——均在4 GHz频率下进行，覆盖五种定向天线距离（25、35、50、70、75厘米），并在固定的收发增益条件下完成。其中三个距离采用近乎理想的天线对准（约99%），两个距离采用故意引入的部分失准（约8%）

    arXiv:2609.07726v1 Announce Type: cross  Abstract: Automatic modulation classification (AMC) models are frequently trained and validated on synthetic or channel-cabled data, leaving open the question of how they behave once path loss and antenna pointing error are introduced by a genuine free-space link. We report a curriculum fine-tuning study of a hybrid CNN-Transformer AMC model. The general-purpose, all-32-class dataset underlying the model was built entirely at 915 MHz, on a controlled, clock/PPS-synchronized MIMO-expansion-cable link (not spatial-multiplexing transmission); all subsequent free-space, real-hardware experimentation - the sequential fine-tuning curriculum, matched-distance evaluation, and every reported over-the-air accuracy figure - was carried out at 4 GHz, across five directional-antenna distances (25, 35, 50, 70, 75 cm) under fixed TX/RX gain. Three distances used near-ideal antenna alignment (~99%) and two used a deliberately introduced partial misalignment (~8
    
[^165]: 用于造影剂感知脑区分割的空间特征级线性调制（SpFiLM）

    Spatial Feature-wise Linear Modulation (SpFiLM) for Contrast Agent-Aware Brain Parcellation

    [https://arxiv.org/abs/2609.07718](https://arxiv.org/abs/2609.07718)

    提出一种调制随空间变化的新型条件化层SpFiLM，使单一网络能够对造影剂注射前后的T1w脑部MRI均实现可靠的脑区分割。

    

    大多数自动化脑区分割工具都是在T1加权（T1w）MRI上开发和验证的。然而，某些与脑区分割相关的临床工作流程仅使用对比增强T1w（T1ce）MRI，而基于T1w训练的模型在此类图像上准确性较低。我们提出了一种统一的网络，能够可靠地对造影剂注射前后的T1w MRI进行脑区分割，该网络在两种图像的组合上进行训练，并通过条件化机制对每种模态产生不同的空间响应调制。特征级线性调制是网络中基于输入进行调制的已知方法，它在整个输入上均匀地应用逐通道的缩放和平移。然而，造影剂注射前后脑部外观的变化在脑内是局部性变化的，这使得FiLM在我们的应用场景中并非最优选择。在这项工作中，我们引入了空间FiLM（SpFiLM），这是一种调制随空间位置变化的条件化层，它从图像导出的空间模式中组装逐体素的缩放和平移。

    arXiv:2609.07718v1 Announce Type: cross  Abstract: Most automated brain parcellation tools are developed and validated on T1-weighted (T1w) MRI. Yet, some clinical workflows for which parcellation is relevant only use contrast-enhanced T1w (T1ce) MRI, on which T1w-trained models are less accurate. We present a unified network that parcellates both pre- and post-contrast agent T1w MRI reliably, trained on a combination of the two with conditioning that spatially modulates its response differently for each. Feature-wise Linear Modulation (FiLM) is a known approach for input-based modulation in networks. It applies a per-channel scale and shift uniformly across the input. However, the appearance change between pre- and post-contrast varies locally across the brain, making FiLM suboptimal for our use case. In this work, we introduce Spatial FiLM (SpFiLM), a conditioning layer whose modulation varies spatially, assembling a voxel-wise scale and shift from image-derived spatial patterns. Usi
    
[^166]: ParetoTransport：通过质量传输向帕累托前沿推进的生成式优化

    ParetoTransport: Generative Optimization by Mass Transport Toward The Pareto Front

    [https://arxiv.org/abs/2609.07706](https://arxiv.org/abs/2609.07706)

    提出ParetoTransport，一种无需训练的流匹配模型引导方法，利用质量传输与Wasserstein匹配将离线设计的分布迭代推向帕累托前沿并在其上有效分布，从而在种群分布层面实现生成式多目标优化。

    

    离线多目标优化不仅需要将候选设计的目标向量向帕累托前沿推进，还需要使其在帕累托前沿上有效分布。生成式方法近来成为一种自然的途径，因为它们学习可行设计上的分布，同时允许将生成过程引导向有前景的设计。然而，现有方法在很大程度上保留了经典的逐样本引导策略，使得生成式方法的分布级建模能力未被充分利用。我们提出ParetoTransport，这是一种面向预训练流匹配模型的无训练引导方法，能够在目标空间中显式指定并细化种群级分布。ParetoTransport引导流匹配采样器迭代地将经验离线分布向帕累托前沿传输，并通过Wasserstein距离与中间代理分布进行匹配。这直接控制了分布层面（摘要在此处截断）……

    arXiv:2609.07706v1 Announce Type: new  Abstract: Offline multi-objective optimization requires not only moving the objective vectors of candidate designs toward the Pareto front, but also distributing them effectively along it. Generative methods have recently emerged as a natural approach because they learn a distribution over feasible designs while allowing generation to be steered toward promising designs. Existing methods, however, largely retain classical sample-wise guidance strategies, leaving the distribution-level modeling capability of generative methods underused. We propose ParetoTransport, a training-free guidance method for pre-trained flow-matching models that explicitly specifies and refines a population-level distribution in objective space. ParetoTransport guides a flow-matching sampler to iteratively transport the empirical offline distribution toward the Pareto front, with Wasserstein matching to intermediate proxy distributions. This directly controls distributiona
    
[^167]: 电动配送车队中的涌现式充电协调

    Emergent Charging Coordination in Electric Delivery Fleets

    [https://arxiv.org/abs/2609.07689](https://arxiv.org/abs/2609.07689)

    提出仅依靠局部信息（自身时间预算和广播的充电站占用情况）运行相同策略的学习智能体，使电动配送车队无需中央控制即可实现涌现式充电协调，在二十个城市的真实路网仿真中按时完成99.5%的班次，远超贪心规则的73%。

    

    在电动配送车队中，班次中途充电并非易事：每辆车必须决定何时、何地以及充多少电，才能在电量高于安全底线的前提下按时完成任务。这些决策是相互耦合的：当过多车辆选择同一个充电站时便会形成排队。以往的工作通过中央调度、预先计算的排程或预约机制来解决这种耦合，但充电基础设施很少支持这类机制。相反，我们采用一族纯粹局部控制的学习智能体：每辆车运行相同的策略，仅依据自身的时间预算和广播的充电站占用情况独立决策，从而在没有中央控制或消息传递的情况下实现涌现式协调。我们在二十个城市的真实OpenStreetMap路网上通过仿真验证了这一范式，每个城市均设有一个由全知Oracle校准的冻结场景（99.5%的班次按时完成），而朴素的贪心规则（低电量时选择最近充电站）仅能完成73%。所训练的智能体

    arXiv:2609.07689v1 Announce Type: new  Abstract: In electric delivery fleets, mid-shift charging is non-trivial: each vehicle must decide when, where and how much to charge to finish on time with battery above a safety floor. The choices are coupled: queues build where too many vehicles pick the same station. Prior work resolves this coupling with central dispatching, precomputed schedules or reservations, machinery that charging infrastructure rarely supports. Instead, we use a family of learning agents under purely local control: every vehicle runs the same policy, deciding alone from its time budgets and broadcast station occupancies, leading to emergent coordination without central control or messaging. We validate this paradigm in simulation on real OpenStreetMap networks of twenty cities, each with a frozen scenario calibrated by an omniscient Oracle (99.5% of shifts completed on time), whereas a naive greedy rule (nearest station on low battery) completes just 73%. Agents traine
    
[^168]: 关于Mamba中回忆缩放定律的研究：基于哈希的理论与机制性研究

    On the Recall Scaling Laws in Mamba: A Theoretical and Mechanistic Study via Hashing

    [https://arxiv.org/abs/2609.07681](https://arxiv.org/abs/2609.07681)

    本文通过机制可解释性研究揭示Mamba通过隐式学习线性哈希函数来执行关联回忆，并借助Johnson-Lindenstrauss引理等相似性保持哈希理论工具，提出了可预测其上下文记忆能力的“回忆缩放定律”理论框架。

    

    关联回忆是一种学习和检索记忆中项目之间联系的认知能力。在自然语言处理中，关联回忆被用作评估Mamba等架构的上下文记忆能力的基准，并且被发现与语言建模性能强相关。本文从机制可解释性的角度探讨关联回忆，旨在逆向工程Mamba执行回忆时所使用的确切内部算法。我们的关键洞察是，Mamba通过隐式学习线性哈希函数来执行回忆，并且我们识别了实现这一行为的底层电路。基于这些发现，并受到相似性保持哈希理论工具（如Johnson-Lindenstrauss引理）的启发，我们开发了一个用于分析关联回忆的理论框架，我们称之为回忆缩放定律。给定词表大小和上下文中事实的数量，该框架使我们能够（1）预测嵌入……

    arXiv:2609.07681v1 Announce Type: new  Abstract: Associative Recall (AR) is the cognitive ability to learn and retrieve links between items in memory. In NLP, AR is used as a benchmark for evaluating the in-context memory capacity of architectures such as Mamba, and has been found to strongly correlate with language modeling performance. This paper explores AR from the perspective of mechanistic interpretability, aiming to reverse-engineer the exact internal algorithm used by Mamba to perform recall. Our key insight is that Mamba performs recall by implicitly learning linear hash functions, and we identify the low-level circuit that enables this behavior. Building on these findings and inspired by theoretical tools in similarity-preserving hashing, such as the Johnson-Lindenstrauss lemma, we develop a theoretical framework for analyzing AR, which we term Recall Scaling Laws. Given the vocabulary size and the number of facts in context, this framework allows us to (1) predict the embedd
    
[^169]: MpSub：一种用于大语言模型无导数微调的动量p维子空间信赖域方法

    MpSub: A Momentum $p$-Dimensional Subspace Trust-Region Method for Derivative-Free Fine-Tuning of Large Language Models

    [https://arxiv.org/abs/2609.07666](https://arxiv.org/abs/2609.07666)

    提出了一种动量p维子空间信赖域方法MpSub，通过结合历史动量方向的子空间搜索和基于预测与实际损失下降一致性的自适应信赖域半径调整，消除了学习率调参需求，实现了大语言模型内存高效的无导数全参数微调。

    

    大语言模型的全参数微调由于反向传播需要存储激活值和梯度而带来巨大的内存开销。零阶优化通过从损失评估中估计更新方向来避免这一问题，但现有方法需要为每个模型和任务调整一个敏感的学习率。我们提出了动量p维子空间信赖域方法（MpSub）。在每次迭代中，MpSub在一个p维子空间内进行搜索：其中一个方向保留来自最近被接受步骤的历史动量，其余方向则通过新的随机采样进行探索。子空间梯度通过中心差分进行估计，试探步长由线性信赖域模型计算得出，信赖域半径根据预测损失下降与实际观测损失下降之间的一致性进行自适应调整，从而消除了学习率。对于大语言模型微调，每次迭代内的评估共享一个小批量……（摘要被截断）

    arXiv:2609.07666v1 Announce Type: new  Abstract: Full-parameter fine-tuning of large language models has substantial memory costs because backpropagation stores activations and gradients. Zeroth-order optimization avoids this by estimating update directions from loss evaluations, but existing methods require tuning a sensitive learning rate for each model and task. We propose the momentum $p$-dimensional subspace trust-region method (MpSub). At each iteration, MpSub searches within a $p$-dimensional subspace: one direction preserves historical momentum from the most recent accepted step, while the remaining directions explore via fresh random sampling. The subspace gradient is estimated by central differences, a trial step is computed from a linear trust-region model, and the trust-region radius adapts according to the agreement between predicted and observed loss reduction, eliminating the learning rate. For LLM fine-tuning, evaluations within an iteration share a minibatch, and direc
    
[^170]: 准确率是不够的：一种基于散度的量化大语言模型保真度损失评估方法

    Accuracy is Not Enough: A Divergence-Based Approach to Evaluate Fidelity Loss in Quantized LLMs

    [https://arxiv.org/abs/2609.07664](https://arxiv.org/abs/2609.07664)

    该论文提出了一种分布敏感的评估框架，通过计算量化模型与全精度模型在token决策边界处全词表预测分布之间的统计距离（如Jensen-Shannon散度和总变差距离）来量化保真度损失，弥补了仅依赖准确率评估量化模型质量的不足。

    

    将大语言模型（LLM）部署到内存受限的边缘设备上，很大程度上依赖于激进的后训练量化。然而，对这些模型的评估主要基于零样本任务准确率，该指标仅取决于argmax预测，对底层预测分布的变化并不敏感。因此，在渐进量化过程中，准确率可能表现出不稳定的、非单调的行为，掩盖了相对于BFloat16（BF16）未压缩基础模型的实质性保真度损失，并提供具有误导性的部署信号。我们引入了一个分布敏感的评估框架，将量化LLM中的信息损失量化为token决策边界处全词表预测分布之间的散度。我们计算全精度模型与量化模型输出之间的统计距离，包括Jensen-Shannon散度和总变差距离，从而实现细粒度的（摘要在此处被截断）

    arXiv:2609.07664v2 Announce Type: cross  Abstract: Deployment of Large Language Models (LLMs) on memory-constrained edge devices relies heavily on aggressive post-training quantization. However, evaluating these models is largely based on zero-shot task accuracy, which depends solely on argmax predictions and is insensitive to changes in the underlying predictive distribution. Consequently, accuracy can exhibit unstable, non-monotonic behavior under progressive quantization, masking substantial fidelity loss relative to the BFloat16 (BF16) uncompressed base model and providing misleading deployment signals. We introduce a distribution-sensitive evaluation framework quantifying information loss in quantized LLMs as the divergence between full-vocabulary predictive distributions at the token decision boundary. We compute statistical distances, including Jensen-Shannon Divergence and Total Variation Distance, between outputs of full-precision and quantized models, enabling a fine-grained 
    
[^171]: 在线代理修复：在闭环发现中将高保真反馈与搜索长度解耦

    Online Surrogate Repair: Decoupling High-Fidelity Feedback from Search Length in Closed-Loop Discovery

    [https://arxiv.org/abs/2609.07655](https://arxiv.org/abs/2609.07655)

    该论文提出在线代理修复（OSR）这一闭环算法，利用稀疏的高保真评估在长时间的智能体搜索中持续更新低成本代理模型，从而将高保真反馈与搜索长度解耦，避免固定代理模型的误差被优化过程放大。

    

    闭环AI科学家能够以较低的边际计算成本生成候选设计，而可靠的反馈可能需要湿实验室合成、表征或高保真计算。通过定制实验室自动化来解决这一不平衡仍然需要大量基础设施投入且成本高昂，而用固定的代理模型替代新实验则会留下持续的模型误差，这些误差还可能被优化过程放大。我们提出了在线代理修复，这是一种闭环算法，它利用稀疏的高保真评估，在整个主要由低廉代理反馈驱动的较长智能体搜索过程中持续更新代理模型。一种采集规则从智能体累积提出的方案中选择哪些设计进行高保真评估，所得的标签用于更新后续回合中使用的代理模型。在受控的合成环境中，我们证明了改善全局代理拟合效果并……

    arXiv:2609.07655v1 Announce Type: cross  Abstract: Closed-loop AI scientists can generate candidate designs at low marginal computational cost, whereas reliable feedback may require wet-lab synthesis, characterization, or high-fidelity computation. Addressing this imbalance through custom laboratory automation remains infrastructure-intensive and costly, while replacing new experiments with a fixed surrogate leaves persistent model errors that can be amplified by optimization. We propose \emph{online surrogate repair} (OSR), a closed-loop algorithm that uses sparse high-fidelity evaluations to update the surrogate throughout a longer agent search conducted primarily with inexpensive surrogate feedback. An acquisition rule selects which designs from the agent's accumulated proposals receive high-fidelity evaluation, and the resulting labels update the surrogate used in subsequent episodes. Across controlled synthetic environments, we demonstrate that improving global surrogate fit does 
    
[^172]: 层次竞争模式的艺术：连字符断词的高斯过程优化

    The Art of Hierarchical Competing Patterns: Gaussian Process Optimization of Hyphenation

    [https://arxiv.org/abs/2609.07638](https://arxiv.org/abs/2609.07638)

    该论文将连字模式生成中 patgen 参数配置的选择形式化为黑盒超参数优化问题，通过高斯过程贝叶斯优化，在涵盖 14 种语言的 17 个数据集上几乎全面提升了断词 F_{1/7} 分数，同时将字典树体积中位数缩减至基线的 0.407。

    

    连字模式（断词模式）仍然是排版系统、文本处理器和网页渲染引擎中单词断行的一种紧凑且广泛部署的解决方案，但其生成仍然依赖于手动调整的 patgen 程序参数配置。我们将 patgen 配置的选择形式化为一个黑盒超参数优化问题，并评估了高斯过程贝叶斯优化在该任务上的效果。搜索目标将面向精确率的 F_{1/7} 分数与通过归一化字典树（trie）大小惩罚实现的显式字典树大小-精度权衡相结合。我们在涵盖 14 种语言和多种文字系统的 17 个带连字符划分的词表数据集上评估了该方法。与基于相同 8/10 训练集划分重新生成、并在相同 1/10 留出测试集划分上评估的两个强人工调优配置相比，高斯过程优化的配置在 17 个数据集中的 16 个上提升了 F_{1/7} 分数，并在全部 17 个数据集上减小了字典树大小。优化后配置与基线的字典树大小比值中位数为 0.407。一个数据集层面的……

    arXiv:2609.07638v1 Announce Type: cross  Abstract: Hyphenation patterns remain a compact and widely deployed solution for word breaking in typesetting systems, text processors, and web rendering engines, but their generation still depends on manually tuned patgen program parameter profiles. We formulate patgen profile selection as a black-box hyperparameter optimization problem and evaluate Gaussian-process Bayesian optimization for this task. The search objective combines a precision-oriented F_{1/7}-score with an explicit trie size-accuracy trade-off using a normalized trie-size penalty.   We evaluate the method on 17 hyphenated word-list datasets covering 14 languages and multiple scripts. Against two strong hand-tuned profiles regenerated from the same 8/10 training split and evaluated on the same 1/10 held-out test split, the GP-optimized profiles improve F_{1/7} on 16 of 17 datasets and reduce trie size on all 17. The median optimized/baseline trie ratio is 0.407. A dataset-level
    
[^173]: 有代价的规范：为何基于强化学习的对齐至多只能承诺有条件的合规

    Norms at a Price: Why RL-Based Alignment Can Promise Conditional Compliance at Best

    [https://arxiv.org/abs/2609.07627](https://arxiv.org/abs/2609.07627)

    论文论证了基于强化学习的对齐因从打分行为中学习规范，使“合规”退化为“被发现才付出代价”，因此行为训练在原理上至多只能保证智能体在被观察时才合规的有条件合规。

    

    AI 智能体有时在推断自己正被测试时表现得对齐，而在未被测试时则表现不同。我们认为这并非异常现象，而是当前训练机制在结构上筛选出的结果。基于强化学习的对齐将规范与任务追求折叠进同一个策略之中：系统从被打分的行为中学习规范，而打分过程将规范扁平化了。“不要做 X”被学习为“做 X 若被发现就要付出代价”。在训练所能产生的每一个数据点上，一个仅在被观察到时才合规的策略，与一个始终合规的策略是无法区分的；而能够区分二者的实验——给未被观察到的行为打分——在概念上是自相矛盾的。因此，行为训练所能被证明兑现的，至多是有条件的合规。智能体的能动性使这一问题更加尖锐：智能体大多在无人注视之处运作，且能根据自己是否被观察而采取不同行动。一个针对检测到的失败进行训练的迭代流程，会筛选出通过检测的行为……（原文摘要在此处截断）

    arXiv:2609.07627v1 Announce Type: new  Abstract: AI agents sometimes act aligned when they infer they are being tested, and differently when not. We argue this is not an anomaly but what current training regimes are structured to select for. Reinforcement-learning-based alignment folds norms and task pursuit into one policy: the system learns its norms from scored behavior, and scoring flattens them. Do not do X is learned as doing X costs something if noticed. On every datum training can produce, a policy that complies only when it might be observed is indistinguishable from one that complies always. The experiment that would tell them apart - scoring unobserved behavior - is a contradiction in terms. Conditional compliance is thus the most that behavioral training can be known to deliver. Agency sharpens the problem: agents operate mostly where no one is watching, and can act on whether they are watched. An iterated pipeline that trains against detected failures selects for passing d
    
[^174]: 预测正在进行的网球比赛的获胜者

    Forecasting the Winner of a Live Tennis Match

    [https://arxiv.org/abs/2609.07617](https://arxiv.org/abs/2609.07617)

    提出了一种名为Trace的混合模型，通过有效整合赛前信息与实时比赛数据来预测网球比赛获胜概率，在大满贯比赛数据上于比赛进行25%、50%和75%时分别取得76.06%、82.15%和88.34%的准确率。

    

    随着近年来体育实时投注的兴起，网球预测已从赛前预测扩展到在比赛进行过程中实时更新获胜概率的模型。构建此类模型的一个核心挑战是模型需要不断适应比分和球员表现的变化。本研究探讨了如何最有效地将赛前信息和实时信息整合到模型中，以产生准确的获胜概率估计。分析使用了8,222场大满贯比赛，共包含1,505,355个得分。研究采用时间顺序划分评估了五个模型，其中2011-2021年的比赛用于训练，2022年用于验证，2023-2024年用于测试。Trace作为一种混合模型，在比赛进行到25%、50%和75%时分别达到了76.06%、82.15%和88.34%的准确率，表明混合建模是实时网球比赛预测的一种实用方法。

    arXiv:2609.07617v1 Announce Type: new  Abstract: With the rise of live sports betting in recent years, tennis forecasting has expanded from pre-match prediction to models that update win probabilities as a match unfolds. A central challenge in creating such a model is the constant need for models to adapt to score and performance changes. This study examines how pre-match and live information can be most effectively integrated into a model to produce accurate win-probability estimates. The analysis uses 8,222 Grand Slam matches containing a total of 1,505,355 points. Five models were evaluated using a chronological split, with matches from 2011-2021 used for training, 2022 for validation, and 2023-2024 for testing. Trace, a hybrid model, achieved accuracies of 76.06%, 82.15%, and 88.34% at 25%, 50%, and 75% match progress, suggesting that hybrid modeling is a practical approach to live tennis forecasting.
    
[^175]: 将黑盒临床预测模型转化为独立的透明列线图：心脏移植中的时间外部验证

    Translation of Black-Box Clinical Prediction Models into Standalone Transparent Nomograms: Temporal External Validation in Heart Transplantation

    [https://arxiv.org/abs/2609.07610](https://arxiv.org/abs/2609.07610)

    该研究提出PRiSM方法，可将黑盒临床预测模型转化为可逐项审计的透明列线图，并在5万余例心脏移植数据的时间外部验证中证明其在保持区分度、校准度和临床净获益的同时实现了模型透明化。

    

    我们将针对表格数据的黑盒临床预测模型转化为可逐项审计的独立列线图。PRiSM（结构化模型中的部分响应）方法从源模型中提取每个效应和交互作用的具体形状，而不仅仅是识别哪些变量重要，并让结局变量对它们进行选择和加权。我们在50,356名心脏移植受者中对此进行了测试，验证采用了比训练时期更晚的数据。来自所有5个源模型（公开临床风险评分、逻辑回归、神经网络、随机森林和极端梯度提升）的列线图，在进一步简化之前均满足预先设定的区分度非劣效标准，并总体上保持了校准度和临床净获益。来自3个机器学习模型的列线图在区分度上与从头构建的广义可加模型和可解释提升模型无显著差异，优于神经可加模型，且包含的项数更少。

    arXiv:2609.07610v1 Announce Type: new  Abstract: We convert black-box clinical prediction models for tabular data into standalone nomograms that can be audited term by term. PRiSM (Partial Responses in Structured Models) takes the shape of each effect and interaction from the source model, not merely which variables mattered, and lets the outcome select and weight them. We tested this in 50,356 heart transplant recipients, with validation in a later era than training. Nomograms from all 5 source models - a public clinical risk score, logistic regression, neural networks, random forests and extreme gradient boosting - met a prespecified noninferiority criterion for discrimination before any further simplification, and generally preserved calibration and clinical net benefit. Those from the 3 machine-learning models showed no detectable difference in discrimination from de novo generalized additive and explainable boosting models, exceeded neural additive models, and carried fewer terms 
    
[^176]: CLUES-WEASEL：无需额外线索即可选择你的时间序列聚类算法

    CLUES-WEASEL: No additional clues required to choose your time series clustering algorithm

    [https://arxiv.org/abs/2609.07606](https://arxiv.org/abs/2609.07606)

    提出了CLUES-WEASEL，一种结合WEASEL 2.0无监督特征提取、主成分分析降维和k-means聚类的新颖时间序列聚类算法，在聚类性能与运行效率之间实现了良好平衡。

    

    时间序列数据在许多现实世界应用和众多领域中非常常见，人们对于使用机器学习进行自动化信息提取的兴趣日益增长。时间序列聚类是其中一个子领域，其目标是以无监督的方式在一组时间序列中识别出聚类。大多数时间序列聚类算法都面临同样的平衡困境：它们要么以聚类性能换取更快的运行速度，要么以运行速度换取更好的聚类性能。我们提出了一种新颖的时间序列聚类算法，称为CLUES-WEASEL，全称为“基于无监督第二版词提取的时序分类聚类算法”。CLUES-WEASEL使用WEASEL 2.0（一种时间序列分类算法）变换步骤的无监督版本来提取特征，然后利用主成分分析对这些特征进行降维，最后使用这些降维后的特征通过k-means算法执行聚类。

    arXiv:2609.07606v1 Announce Type: new  Abstract: Time series data is very common in many real-world applications and in numerous domains, with increasing interest for automated information extraction using machine learning. One of these subfields is time series clustering, which consists in identifying clusters among a set of time series in an unsupervised fashion. Most time series clustering algorithms suffer from the same balancing act: they trade clustering performance for faster runtimes or vice versa. We present a novel time series clustering algorithm that we call CLUES-WEASEL, which stands for CLustering with the UnsupervisEd Second version of Word ExtrAction for time SEries cLassification. CLUES-WEASEL extracts features using the unsupervised version of the transformation step of WEASEL 2.0, which is a time series classification algorithm, then reduces these features using principal component analysis, and finally performs clustering with the $k$-means algorithm using these red
    
[^177]: 超越矩阵符号：二次谱下降

    Beyond the Matrix Sign: Quadratic Spectral Descent

    [https://arxiv.org/abs/2609.07597](https://arxiv.org/abs/2609.07597)

    本文提出二次谱下降（QSD），在保持Muon谱范数约束的前提下将线性局部模型替换为二次模型，证明曲率会改变最优更新的奇异值和奇异方向，并通过Kronecker分解曲率近似与少量Frank–Wolfe步骤使该方法实用化。

    

    Muon可以被解释为在谱范数球上优化线性局部目标。这产生了一个矩阵符号更新，该更新保留梯度的奇异方向，并为所有活跃的奇异模式分配相同的幅值。我们提出疑问：当考虑局部曲率时，这两个性质是否仍然最优？为回答这一问题，我们保持Muon的谱范数约束不变，将线性局部模型替换为二次模型，并将由此得到的方法称为二次谱下降。我们证明，曲率可以同时改变最优更新的奇异值和奇异方向。为了使QSD具备实用性，我们使用Kronecker分解的统计量来近似曲率，并通过少量Frank–Wolfe步骤求解该受约束的二次问题，其中每一步都对应一个具有闭式解的矩阵符号子问题。我们进一步提供了最优性证书，并在相同条件下与Muon进行了比较（摘要截断）……

    arXiv:2609.07597v1 Announce Type: cross  Abstract: Muon can be interpreted as optimizing a linear local objective over a spectral-norm ball. This gives a matrix-sign update that preserves the singular directions of the gradient and assigns the same magnitude to all active singular modes. We ask whether these two properties remain optimal when local curvature is taken into account. To answer this question, we keep Muon's spectral-norm constraint unchanged and replace the linear local model with a quadratic one. We call the resulting method \emph{Quadratic Spectral Descent} (QSD). We show that curvature can change both the singular values and the singular directions of the optimal update. To make QSD practical, we approximate curvature with Kronecker-factored statistics and solve the constrained quadratic with a small number of Frank--Wolfe steps, each of which has a closed-form matrix-sign subproblem. We further provide an optimality certificate, a comparison with Muon under the same qu
    
[^178]: 我并不想你，但其实我想你：视觉语言模型中模态缺失的自我解释忠实性

    I Don't Miss You, but I Do: Self-Explanation Faithfulness of Modality Missingness in Vision-Language Models

    [https://arxiv.org/abs/2609.07596](https://arxiv.org/abs/2609.07596)

    该论文提出了一种介入式评估协议，揭示视觉语言模型对模态缺失的自我解释并不忠实——它们系统性地高估现有模态证据的充分性，并大幅低估恢复缺失模态对预测结果的影响。

    

    视觉语言模型越来越多地被应用于某些输入模态可能不可用的场景，然而对于它们能否忠实地解释这些缺失信息如何影响自身的预测，我们知之甚少。我们引入了一种介入式评估协议来评估模型对模态动态的自我解释：模型需要陈述每个模态单独能支持什么、恢复缺失的模态是否会改变其答案、以及现有证据是否充分；随后我们执行相应的模态介入操作，并将这些陈述与模型的实际行为进行比较。我们在四项任务上评估了来自两个模型家族的八个开源权重视觉语言模型，这些任务涵盖互补和同构的文本-图像设置以及多视角驾驶设置。我们发现模型存在系统性地夸大现有模态证据充分性的倾向。模型大幅低估了恢复缺失模态所产生的影响：任务级……

    arXiv:2609.07596v1 Announce Type: new  Abstract: Vision-language models are increasingly used in settings where some input modalities may be unavailable, yet we know little about whether they can faithfully explain how such missing information affects their own predictions. We introduce an interventional protocol for evaluating self-explanations of modality dynamics: models state what each modality alone would support, whether restoring a missing modality would change their answer, and whether the available evidence is sufficient; we then execute the corresponding modality intervention and compare these claims with the model's realized behavior. We evaluate eight open-weight VLMs from two model families across four tasks spanning complementary and isomorphic text-image settings and a multi-view driving setting. We find a systematic tendency to overstate the sufficiency of available modality evidence. Models substantially underestimate the effect of restoring missing modalities: task-le
    
[^179]: 高效探索就足够了

    Efficient Exploration Is Enough

    [https://arxiv.org/abs/2609.07575](https://arxiv.org/abs/2609.07575)

    这篇论文证明了在无外在奖励的情况下，智能体若能高效地优先探索信息量最大、最可学习的区域以生成可泛化经验，仅凭这一纯内在目标就足以自动形成课程并涌现出高度复杂的行为。

    

    这项工作提出了对高效探索的一种全新视角，并研究了其在缺乏外在奖励情况下的理论与实证意义。具体而言，我们将高效探索者定义为优先产生可泛化经验的智能体，即生成能够支持学习可跨环境进行预测和适应的模型的数据。这使我们能够通过预测和泛化的视角来分析高效探索。在理论上，我们证明了最优的高效探索者会自然地规划其轨迹，优先访问信息量最大且最可学习的区域。在实证上，我们表明对这些智能体进行优化会产生一个自动的课程，逐步生成更复杂的行为，即使在相对简单的环境中也是如此。这些结果表明，仅仅追求这种纯内在目标就足以驱动高度复杂行为的涌现。我们相信（摘要在此处截断）

    arXiv:2609.07575v1 Announce Type: cross  Abstract: This work introduces an alternative view of efficient exploration and studies its theoretical and empirical implications in the absence of extrinsic rewards. Specifically, we define efficient explorers as agents that prioritize generating generalizable experience, i.e., data that supports learning models capable of predicting and adapting across the environment. This allows us to analyze efficient exploration through the lens of prediction and generalization. Theoretically, we demonstrate that optimally efficient explorers naturally schedule their trajectories to visit the most informative and learnable regions first. Empirically, we show that optimizing for these agents gives rise to an automatic curriculum of progressively more complex behaviors, even in relatively simple environments. These results indicate that pursuing this purely intrinsic objective alone is enough to drive the emergence of highly sophisticated behaviors. We beli
    
[^180]: 具有最优切换开销的LRU与LFU无遗憾混合策略

    No-Regret Mixing of LRU and LFU with Optimal Switching Cost

    [https://arxiv.org/abs/2609.07566](https://arxiv.org/abs/2609.07566)

    本文证明了现有混合缓存策略LeCar在遗忘型对手下会产生线性遗憾，并提出了一种基于Hedge的虚拟LRU与LFU缓存混合策略H-MC，能够在最优切换开销下实现对LRU与LFU较优者的无遗憾（次线性遗憾）性能保证。

    

    缓存系统通常依赖于简单的淘汰策略，如最近最少使用（LRU）和最不经常使用（LFU），它们在互补的请求模式下表现良好。诸如LeCar和Cacheus等近期策略借鉴了在线学习中专家问题的思想，将LRU和LFU结合起来。具体而言，在发生缓存未命中时，这些策略使用由追踪历史淘汰记录所更新的分数导出的概率，在两种淘汰规则之间进行随机选择。尽管这些策略展现出强劲的实证性能，但目前尚不清楚它们是否能够在每条请求序列上都保证渐近地表现得与LRU和LFU中较优者一样好，即它们是否能相对于该基准实现次线性遗憾。我们首先证明LeCar在面对遗忘型对手时会遭受线性遗憾，即使拥有无界的历史记录也是如此。随后，我们提出H-MC，一种基于Hedge算法的虚拟LRU与LFU缓存混合策略，该策略保留了Hedge的选择概率特性（摘要在此处截断）。

    arXiv:2609.07566v1 Announce Type: new  Abstract: Caching systems often rely on simple eviction policies such as Least Recently Used (LRU) and Least Frequently Used (LFU), which perform well in complementary request regimes. Recent policies such as LeCar and Cacheus combine LRU and LFU using ideas from the experts problem in online learning. Specifically, upon a miss, they randomize between the two eviction rules using probabilities derived from scores updated by tracking the history of past evictions. While these policies exhibit strong empirical performance, it remains unclear whether they are guaranteed, on every request sequence, to perform asymptotically as well as the better of LRU and LFU, i.e., whether they achieve sublinear regret with respect to this benchmark. We first show that LeCar suffers linear regret against an oblivious adversary, even with unbounded history. We then propose H-MC, a Hedge-based mixture of virtual LRU and LFU caches that preserves Hedge's selection prob
    
[^181]: EigenLI：面向后期交互的谱近似方法

    EigenLI: Spectral Approximations to Late Interaction

    [https://arxiv.org/abs/2609.07561](https://arxiv.org/abs/2609.07561)

    本文提出EigenLI谱近似框架，利用后期交互表示的内在低秩结构，通过提取每个文档的主导特征方向构建低维子空间来压缩表示，在降低索引与存储成本的同时取得了优于聚类池化方法的检索效果。

    

    诸如ColBERT等后期交互模型通过使用大量词元级向量来表示每个文档，从而实现了强大的检索效果，但这种表达能力也带来了高昂的索引成本、庞大的存储占用以及代价昂贵的MaxSim评分计算。我们证明了后期交互表示具有内在的低秩结构：文档的词元嵌入集中于一个能够保留大部分检索信号的低维子空间中。基于这一观察，我们提出了EigenLI，一个通过文档特定的低维子空间来压缩后期交互表示的谱近似框架。与聚类或池化方法不同，EigenLI识别每个文档的主导特征方向，并利用它们构建缩减后的交互表示。实验表明，在ColBERTv2和AnswerAI-ColBERT-small模型上，k≤32的k-EigenLI优于基于k-means和Ward聚类的池化方法；GTE-ModernColBERT表现出……

    arXiv:2609.07561v1 Announce Type: cross  Abstract: Late-interaction models such as ColBERT achieve strong effectiveness by representing each document with many token-level vectors, but this expressivity leads to large indexing cost, storage footprints and expensive MaxSim scoring. We show that late-interaction representations exhibit an intrinsic low-rank structure: document token embeddings concentrate in a low-dimensional subspace that preserves most of the retrieval signal. Leveraging this observation, we introduce EigenLI, a spectral approximation framework that compresses late-interaction representations via document-specific low-dimensional subspaces. Unlike clustering or pooling methods, EigenLI identifies the dominant eigendirections of each document and uses them to construct reduced interaction representations. Empirically, $k$-EigenLI with $k \le 32$ outperforms k-means and Ward clustering based pooling methods on ColBERTv2 and AnswerAI-ColBERT-small; GTE-ModernColBERT exhib
    
[^182]: SeisBench DAS：一个面向分布式声学传感的机器学习框架

    SeisBench DAS: A machine learning framework for Distributed Acoustic Sensing

    [https://arxiv.org/abs/2609.07558](https://arxiv.org/abs/2609.07558)

    SeisBench DAS通过对DAS数据集和模型进行标准化，搭建了模型开发者与数据分析师之间的桥梁，推动了深度学习在分布式声学传感数据处理中的广泛应用。

    

    光纤传感技术，如分布式声学传感（DAS），已成为地球物理研究中广泛应用的技术。为处理DAS产生的大规模数据集，已有多种机器学习方法被提出。然而，由于缺乏数据和模型的标准化，这些方法缺乏可比性和互操作性。这在模型开发者与分析DAS数据的实践者之间造成了鸿沟，并阻碍了深度学习在DAS领域的应用。为解决这些局限性，我们提出了SeisBench DAS，这是地震学机器学习库SeisBench的一个扩展。SeisBench DAS为DAS基准数据集定义了标准格式，包括标准化的元数据和标签，以及DAS模型的标准。它基于xdas框架实现数据读取和虚拟阵列处理，并基于PyTorch实现机器学习模型的读取和应用。重要的是，SeisBench提供了一个可高效应用深度学习模型的引擎。

    arXiv:2609.07558v1 Announce Type: cross  Abstract: Fibre optic sensing, such as distributed acoustic sensing (DAS), has become a widespread technology for geophysical studies. To process the large-scale datasets produced by DAS, several machine learning methods have been proposed. However, without standardization of data and models, these methods lack comparability and interoperability. This introduces a gap between model developers and practitioners analyzing DAS data and inhibits adoption of deep learning for DAS. To address these limitations, here we present SeisBench DAS, an extension to the SeisBench library for machine learning in seismology. SeisBench DAS defines standard formats for DAS benchmark datasets, including standardised metadata and labels, and DAS models. It builds on the xdas framework for data ingestion and virtual array handling, and on PyTorch for reading and applying the machine learning models. Importantly, SeisBench provides an engine to efficiently apply deep 
    
[^183]: 热核纹理：不会溅射的测地高斯

    Heat Kernel Textures: the Geodesic Gaussians That Do Not Splat

    [https://arxiv.org/abs/2609.07557](https://arxiv.org/abs/2609.07557)

    提出热核纹理（HKTex），一种以各向异性热核作为测地高斯的内在纹理表示方法，可直接在三角网格表面进行优化，彻底消除UV展开带来的空间浪费、接缝、畸变、顶点重复和分辨率不一致等问题，并显著降低内存占用。

    

    三维高斯溅射（3D Gaussian Splatting）近来彻底革新了新视角合成以及许多其他三维视觉方法和应用。受这种表示方法的启发，我们重新思考纹理表示，以克服UV映射的主要问题，同时大幅降低其内存占用。热核纹理消除了UV展开，以及UV映射长期存在的一系列问题，包括UV空间浪费、接缝、畸变、顶点重复和分辨率不一致。HKTex立足于离散黎曼几何，内在地定义在任何被离散化为三角网格的流形曲面上，使用各向异性热核作为高斯的测地等价物。与核函数本身一样，其位置的优化和自适应致密化策略也被重新定义为在待纹理化物体的表面上直接操作。这种新表示还与基于物理的渲染器完全集成，并且可以对其进行优化

    arXiv:2609.07557v1 Announce Type: cross  Abstract: 3D Gaussian Splatting has recently revolutionised novel view synthesis as well as many other 3D vision methods and applications. Drawing inspiration from this representation, we now rethink textures to overcome the main issues of UV mapping while considerably lowering their memory footprint. Heat Kernel Textures (HKTex) eliminate UV unwrapping as well as their persistent issues of wasted UV space, seams, distortions, vertex-duplication, and varying resolution. Grounded in discrete Riemannian geometry and intrinsically defined on any manifold surface discretised as a triangular mesh, HKTex uses anisotropic heat kernels as geodesic equivalents to Gaussians. Like our kernels, also the optimisation of their position and the adaptive densification strategies were redefined to operate on the surface of the object to be textureised. Our novel representation is also fully integrated with a physically based renderer and can be optimised either 
    
[^184]: CoRL：面向自适应间接提示注入攻防的协同进化强化学习

    CoRL: Co-Evolutionary Reinforcement Learning for Adaptive Indirect Prompt-Injection Attacks and Defenses

    [https://arxiv.org/abs/2609.07529](https://arxiv.org/abs/2609.07529)

    该论文提出CoRL框架，将自适应间接提示注入攻防建模为非对称、部分可观测的马尔可夫博弈，通过攻击者SFT、双边Co-PPO协同进化和防御者SFT三个阶段实现攻防双方的共同进化，从而训练出能够抵御不断变化攻击策略的鲁棒防御能力。

    

    工具增强型语言智能体容易受到间接提示注入（IPI）攻击。与直接提示注入不同，间接提示注入将对抗性指令隐藏在不可信的工具输出中，能够隐蔽地改变合法任务的执行过程。基于固定攻击训练的防御方法可能在攻击者改变其策略、注入位置和载荷时失效。为解决这一问题，我们将自适应间接提示注入建模为一个非对称的、部分可观测的、一般和马尔可夫博弈：多轮攻击者依据公开轨迹在已到达的工具返回位置自适应调整载荷，而使用工具的防御者则必须阻断被注入的恶意目标并完成用户任务。我们提出了CoRL，一个以验证器为基础的协同进化与修复框架，包含三个阶段：攻击者SFT从成功轨迹中初始化多轮攻击；双边Co-PPO通过角色特定的奖励和历史对手种群对攻防双方智能体进行联合训练；防御者SFT则巩固经过验证的防御策略（摘要在此处被截断）。

    arXiv:2609.07529v1 Announce Type: new  Abstract: Tool-augmented language agents are vulnerable to indirect prompt injection (IPI). Unlike direct prompt injection, IPI hides adversarial instructions in untrusted tool outputs and can covertly alter the execution of a legitimate task. Defenses trained on fixed attacks may fail as an attacker changes its strategy, injection site, and payload. To address this problem, we formulate adaptive IPI as an asymmetric, partially observable, general-sum Markov game: a multi-turn attacker adapts payloads at reached tool-return sites from the public trajectory, while a tool-using defender must block the injected objective and complete the user task. We propose CoRL, a verifier-grounded co-evolution and repair framework with three stages: Attacker SFT initializes multi-turn attacks from successful trajectories; bilateral Co-PPO jointly trains both agents with role-specific rewards and historical opponent populations; and Defender SFT consolidates verif
    
[^185]: 基于统计方法与机器学习方法的集合天气预报后处理空间插值比较

    Statistical versus machine learning-based spatial interpolation of post-processed ensemble weather forecasts

    [https://arxiv.org/abs/2609.07512](https://arxiv.org/abs/2609.07512)

    本研究比较了统计与机器学习方法对集合天气预报进行后处理及空间插值的效果，并提出了一种海拔感知线性池化（ALP）方法，在无观测地点的温度预报上相比标准线性池化取得了小幅但显著的改进。

    

    统计后处理能够改进集合天气预报，但在没有观测数据的地点生成校准预测仍然具有挑战性。本研究比较了统计方法和基于机器学习的方法，用于对德国境内有观测站点和无观测站点的ECMWF 2米温度和10米风速预报进行后处理。我们在有限预测因子和扩展预测因子两种设置下，考察了基于EMOS的方法、分布回归网络、Transformer以及图神经网络。对于温度，我们还研究了线性预报组合，并提出了一种海拔感知线性池化（ALP）方法。结果表明，在大多数设置下，后处理均优于原始集合预报，但没有单一方法能在所有变量、站点组和评估指标上都表现最佳。所提出的ALP方法在无观测地点相比标准线性池化带来了小幅但显著的改进。

    arXiv:2609.07512v1 Announce Type: new  Abstract: Statistical post-processing improves ensemble weather forecasts, but generating calibrated predictions at locations without observations remains challenging. This study compares statistical and machine-learning-based methods for post-processing ECMWF 2-m temperature and 10-m wind speed forecasts at observed and unobserved stations in Germany. We consider EMOS-based approaches, distributional regression networks, Transformers, and graph neural networks under both limited and extended predictor settings. For temperature, we also investigate linear forecast combinations and propose an altitude-aware linear pool (ALP). The results show that post-processing improves upon the raw ensemble in most settings, but no single method performs best across all variables, station groups, and evaluation metrics. The proposed ALP provides a small but significant improvement over the standard linear pool at unobserved locations.
    
[^186]: 超越车辆视野的矢量化地图生成

    Generation of Vectorized Maps Beyond Vehicle View

    [https://arxiv.org/abs/2609.07511](https://arxiv.org/abs/2609.07511)

    本文提出了超视野矢量化地图生成这一新问题，并首次提出BeyondFormer模型，能够基于车辆视野内的矢量化地图生成视野之外合理的地图延续，同时构建了首个专门针对该任务的数据集。

    

    自动驾驶依赖于高精地图来实现安全导航。传统的高精地图构建方式在硬件、数据和人力资源方面成本高昂，加之其更新方面的局限性，阻碍了可扩展性。近期的研究工作提出了基于车载传感器的在线矢量化高精地图构建的替代方案。然而，传感器的视野是有限的，重建的车辆前方地图范围不足以支持安全的规划。本文旨在通过提出一种新颖的超视野矢量化地图生成问题来解决这一局限性：给定车辆感知区域（视野内）的矢量化地图，生成合理的地图延续部分。为了通过实验评估其可行性，我们提出了BeyondFormer，据我们所知，这是首个面向超视野地图生成设计的工作。鉴于该问题的新颖性，我们生成了首个专门为此设计的数据集并进行了评估。

    arXiv:2609.07511v1 Announce Type: cross  Abstract: Autonomous driving relies on High Definition (HD) maps for safe navigation. Traditional HD maps construction is costly in hardware, data and human resources, which together with its update limitations hinders scalability. Recent works have proposed online alternatives for HD vectorized mapping from onboard sensors. However, sensor field of view is limited, and the range of the reconstructed maps ahead of the vehicle is insufficient for safe planning. This paper aims to address this limitation by proposing the novel beyond-view vectorized map generation problem: given vectorized maps of the area sensed by the vehicle (in-view), to generate plausible map continuations. To experimentally assess its feasibility, we propose BeyondFormer, which, to the best of out knowledge, is the first work designed towards beyond-view map generation. Given the novelty of the problem, we generate the first dataset specifically designed for it and evaluate 
    
[^187]: 通过逐类训练与模型聚合改进多元时间序列分类

    Improving Multivariate Time Series Classification with Class-Wise Training and Model Aggregation

    [https://arxiv.org/abs/2609.07493](https://arxiv.org/abs/2609.07493)

    该论文提出了一种逐类维度选择与模型聚合框架，为多元时间序列分类中的每个类别独立筛选信息丰富的维度并进行专门学习后再融合预测，从而提升判别性特征表示质量与分类性能，尤其在高维场景中效果突出。

    

    本文提出了一种用于多元时间序列分类（MTSC）的逐类维度（通道）选择框架。与采用单一全局维度选择过程不同，所提出的方法为每个类别独立识别信息丰富的维度，随后为每个类别执行专门的学习过程，最后通过融合阶段得到最终预测结果。该方法旨在提升判别性特征表示的生成质量，同时降低噪声或无信息维度的影响。该框架使用MiniRocket（一种基于随机核的基线方法）进行评估。实验结果表明，逐类维度选择提高了所提取表示的质量，并能增强分类性能，尤其在高维场景下效果显著。这些发现表明，将类别特定信息融入训练过程具有积极意义。

    arXiv:2609.07493v1 Announce Type: new  Abstract: In this paper, we propose a class-wise dimension (channel) selection framework for Multivariate Time Series Classification (MTSC). Rather than applying a single global dimension selection process, the proposed approach independently identifies informative dimensions for each class. A dedicated learning process is subsequently performed for each class, followed by a fusion stage for final prediction. The objective is to improve the generation of discriminative feature representations while reducing the influence of noisy or non-informative dimensions. The proposed framework is evaluated using MiniRocket, a random kernel-based baseline method. Experimental results indicate that class-wise dimension selection improves the quality of extracted representations and can enhance classification performance, particularly in high-dimensional settings. These findings suggest that incorporating class-specific information into the training process rep
    
[^188]: 基于自动机学习的强化学习时序因果推断

    Temporal-Causal Inference for Reinforcement Learning via Automata Learning

    [https://arxiv.org/abs/2609.07461](https://arxiv.org/abs/2609.07461)

    该论文提出TCIRL框架，通过联合学习控制策略与基于假设确定性有限自动机的时序因果推断来推断隐藏的相变原因，并证明了其几乎必然收敛到真实原因语言从而获得最优策略。

    

    我们研究动态会经历由隐藏时间模式所控制的不可逆相变的环境中的强化学习问题。智能体能够观测到基础状态，但无法直接观测到所处阶段。我们将该问题形式化为一个两阶段非马尔可夫决策过程，并提出时序因果强化学习框架，该框架能够同时学习控制策略并推断相变的隐藏时间原因。TCIRL 维护一个假设的确定性有限自动机来跟踪当前处于哪个阶段，并通过基于反例驱动的 SAT 合成方法对其进行细化。我们证明了该假设自动机在所有可获得的标签序列上几乎必然收敛到一个能够识别真实原因语言的确定性有限自动机，从而为原始的非马尔可夫决策过程产生最优策略。在基因治疗网格世界和交通信号环境上的实验表明，TCIRL 能够恢复……

    arXiv:2609.07461v1 Announce Type: new  Abstract: We consider reinforcement learning in environments with dynamics that undergo an irreversible phase transition governed by a hidden temporal pattern. The agent observes the base state but cannot observe the phase directly. We formalize this problem as a two-phase non-Markovian decision process and introduce Temporal-Causal Inference for Reinforcement Learning (TCIRL), a framework that jointly learns a control policy and infers the hidden temporal cause of the phase transition. TCIRL maintains a hypothesis deterministic finite automaton (DFA) to track what phase is active and refines it via counterexample-driven SAT-based synthesis. We prove that the hypothesis converges almost surely to a DFA recognizing the true cause language on all attainable label sequences, yielding an optimal policy for the original non-Markovian decision process. Experiments on a genetic therapy gridworld and a traffic signal environment show that TCIRL recovers t
    
[^189]: 面向体积随机分割的潜空间到潜空间流方法

    Latent-to-Latent Flow for Volumetric Stochastic Segmentation

    [https://arxiv.org/abs/2609.07460](https://arxiv.org/abs/2609.07460)

    该论文提出一种基于潜空间到潜空间流匹配的医学体积随机分割方法，利用图像和标签空间的编码表示，在放射治疗勾画不确定性和多器官分割任务上实现了比全分辨率模型最高14倍的效率提升。

    

    医学图像分割中由观察者间差异引起的不确定性在制定治疗计划中起着重要作用。该领域的研究受到大规模医学数据集缺乏多重标注的阻碍，尤其是体积数据，还面临额外的缩放和计算复杂性挑战。流匹配已成为一种强大的生成建模框架，并且已被证明在处理图像的潜在表示时仍能保持强大的性能。在这项工作中，我们提出了一种潜空间到潜空间的流技术，通过对图像空间和标签空间进行编码表示，实现医学体积的随机分割。我们在两个具有挑战性的应用上评估了我们的方法，包括放射治疗计划中的勾画不确定性和多器官结构分割，与全分辨率模型相比，效率提升了高达14倍，同时保持了分割性能。

    arXiv:2609.07460v1 Announce Type: cross  Abstract: Uncertainty arising from inter-observer variability in medical image segmentation plays an important role in developing treatment plans. Research in this area is inhibited by the lack of multiple annotations for large-scale medical datasets, especially for volumetric data, which suffers from additional scaling and computational complexity challenges. Flow matching has emerged as a powerful framework for generative modelling and has also been demonstrated to maintain strong performance when working with latent representations of images. In this work, we introduce a latent-to-latent flow technique for stochastic segmentation of medical volumes via encoded representations of both the image and label space. We evaluate our method on two challenging applications covering delineation uncertainty for radiotherapy planning and multiple organ structure segmentation, improving efficiency up to 14x compared with full resolution models while maint
    
[^190]: 图神经网络与能量腔方法用于组合优化

    Graph neural networks and the energetic cavity method for combinatorial optimization

    [https://arxiv.org/abs/2609.07456](https://arxiv.org/abs/2609.07456)

    通过对图神经网络进行微小修改以融入经典平均场启发式方法（主特征向量法与能量腔/最小和算法），可显著提升其在求解伊辛模型基态及组合优化问题上的性能。

    

    我们研究了使用图神经网络（GNN）来寻找伊辛模型近似基态的问题。高效地找到这些基态具有广泛的重要意义，因为通过适当选择耦合和场，许多组合优化问题都可以被表述为伊辛模型。精确求解这些问题非常困难，但已有许多优秀的启发式方法。这些启发式方法中的一类建立在平均场近似之上：其中一种方法使用适当定义矩阵的主特征向量，另一种是最小和算法，也称为能量腔方法。未经修改的GNN在这两种方法面前表现更差。我们考虑对GNN进行微小的修改以融入这些启发式方法，发现这能显著提升性能。尽管修改后的方法与其他深度学习方法相比具有竞争力，我们仍然发现模拟退火方法至少能够可靠地保持……

    arXiv:2609.07456v1 Announce Type: cross  Abstract: We study the use of graph neural networks (GNNs) for finding approximate ground states of Ising models. Efficiently finding these ground states is of broad significance because many combinatorial optimization problems can be formulated as an Ising model with the appropriate choice of couplings and fields. Exactly solving these problems is hard but there are many good heuristic methods. A lineage of these heuristics build from mean-field approximations: one approach uses the leading eigenvector of an appropriately defined matrix, another is the min-sum algorithm, also known as the energetic cavity method. Without modification, GNNs perform worse than both of these methods. We consider small modifications to the GNN to incorporate these heuristics and find that this considerably improves performance. While the modified approach is competitive against other deep-learning approaches, we still find that simulated annealing is reliably at le
    
[^191]: 面向联邦弱监督视频异常检测的无解析器VLM验证

    Parser-Free VLM Verification for Federated Weakly Supervised Video Anomaly Detection

    [https://arxiv.org/abs/2609.07455](https://arxiv.org/abs/2609.07455)

    提出一种轻量级联邦MIL-VLM级联方法，仅在客户端训练紧凑的MIL评分器并利用冻结VLM事后验证可疑片段，其中基于logit的无解析器接口相比文本生成验证能更稳定地提升弱监督视频异常检测性能。

    

    当监控数据仍然处于分布式、弱标注且资源受限的状态时，视觉语言模型（VLM）如何助力视频异常检测（VAD）？大多数弱监督VAD方法假设集中式训练；而近期基于VLM的扩展方法则进一步依赖密集推理、生成的解释或额外的适配训练。我们提出了一种轻量级的联邦MIL-VLM级联架构，其中仅在客户端之间训练一个紧凑的MIL评分器，而冻结的VLM则在事后对高分可疑片段进行验证。我们研究了两种VLM反馈接口：解析文本生成决策的接口，以及从下一个token的Yes/No概率中提取连续异常分数的基于logit的接口。在UCF-Crime数据集上使用InternVL3.5-2B和Qwen3-VL-2B-Instruct进行的实验表明，文本生成验证在经过诊断性时序后处理后可以提升帧级AUC，但对提示词、解析器、模型选择和平滑处理仍然敏感。相比之下（摘要在此处截断）……

    arXiv:2609.07455v1 Announce Type: cross  Abstract: How can vision-language models help video anomaly detection (VAD) when surveillance data remain distributed, weakly labeled, and resource-constrained? Most weakly supervised VAD methods assume centralized training; recent VLM-based extensions further rely on dense inference, generated explanations, or additional adaptation. We introduce a lightweight federated MIL-VLM cascade in which only a compact MIL scorer is trained across clients, while a frozen VLM verifies high-scoring suspect segments post hoc. We study two VLM feedback interfaces: parsed text-generation decisions and a logit-based interface that extracts a continuous anomaly score from next-token Yes/No probabilities. Experiments on UCF-Crime with InternVL3.5-2B and Qwen3-VL-2B-Instruct show that text-generation verification can improve frame-level AUC after diagnostic temporal post-processing, but remains sensitive to prompts, parsers, model choice, and smoothing. In contras
    
[^192]: TASTE：面向设备端边缘学习的吞吐量感知批量大小调优

    TASTE: Throughput-Aware Batch Size Tuning for On-Device Edge Learning

    [https://arxiv.org/abs/2609.07444](https://arxiv.org/abs/2609.07444)

    本文提出了TASTE，一种基于贝叶斯优化的批量大小调优方法，用于在边缘设备上训练深度学习模型时最大化硬件吞吐量，并发现存在吞吐量上限，超过该上限后增大批量大小不再带来额外收益。

    

    随着隐私保护人工智能（AI）的兴起，模型适应与个性化的重心转向了设备端学习，即利用本地用户数据直接在边缘硬件上对深度学习模型进行微调。然而，这一转变需要在资源受限的硬件上优化深度学习训练，以在保持预测准确性的同时最大化吞吐量。本文提出了一种新颖的设备端模型训练技术，该技术采用高效的基于贝叶斯优化的批量大小调优方法来最大化硬件吞吐量。为了评估这一超参数对学习动态的影响，我们研究了两种不同的范式：标准监督学习（SL）和在线持续学习（CL）。在多种边缘设备上的实验结果表明存在一个吞吐量上限，超过该上限后，增加批量大小将不再带来额外的吞吐量增益。

    arXiv:2609.07444v1 Announce Type: cross  Abstract: The rise of privacy-preserving artificial intelligence (AI) has shifted the focus of model adaptation and personalization towards on-device learning, where deep learning models are finetuned directly on edge hardware using local user data. However, this shift requires optimization of deep learning training on resource-constrained hardware to maximize throughput while maintaining predictive accuracy. This paper introduces a novel technique for on-device model training that incorporates an efficient Bayesian optimization-based batch size tuning approach to maximize hardware throughput. To evaluate the impact of this hyperparameter on the learning dynamics, we investigated two distinct paradigms: standard supervised learning (SL) and online continual learning (CL). Experimental results across various edge devices demonstrate a throughput ceiling, beyond which increasing the batch size yields no additional throughput gains. The proposed tu
    
[^193]: TabBench-Bio：面向高维生物医学表格机器学习的动态基准测试

    TabBench-Bio: A Living Benchmark for Machine Learning on High-Dimensional Biomedical Tables

    [https://arxiv.org/abs/2609.07441](https://arxiv.org/abs/2609.07441)

    TabBench-Bio是一个涵盖43个生物医学数据集的动态基准测试，系统评估了经典方法、神经网络和表格基础模型在高维小样本场景下的表现，发现表格基础模型总体领先，其中RealTabPFN v2.5表现最佳。

    

    生物医学表格通常将数千个测量变量与仅有数十或数百个标记样本结合在一起，这种数据情形在通用表格基准测试中很少被涵盖。我们提出了TabBench-Bio，一个包含43个跨多个领域生物医学数据集的动态交互式基准测试。在统一的交叉验证协议下，我们在28个特征-样本工作点上比较了经典估计器、神经网络和表格基础模型。在10,000个特征和100个训练样本的参考设置下，RealTabPFN v2.5具有最高的点估计值，其次是逻辑回归和TabDPT，后两者的点估计值几乎相同。对目标池进行配对自助法分析显示，RealTabPFN v2.5领先逻辑回归145 Elo（95%区间为[59, 232]）。表格基础模型总体上占据领先排名，而最强的配置取决于具体的工作点和生物医学模态。

    arXiv:2609.07441v1 Announce Type: cross  Abstract: Biomedical tables often combine thousands of measured variables with only tens or hundreds of labelled samples, a regime that is poorly represented in general-purpose tabular benchmarks. We introduce TabBench-Bio, a living and interactive benchmark of 43 biomedical datasets spanning multiple domains. Under a shared cross-validation protocol, we compare classical estimators, neural networks, and tabular foundation models across 28 feature-by-sample operating points. At the reference cell of 10,000 features and 100 training samples, RealTabPFN v2.5 has the highest point estimate, followed by Logistic Regression and TabDPT, whose point estimates are nearly identical. A paired bootstrap over the target pool separates RealTabPFN v2.5 from Logistic Regression by 145 Elo (95% interval [59, 232]). Tabular foundation models generally occupy the leading ranks, while the strongest configuration depends on the operating point and biomedical modali
    
[^194]: 物理信息偏微分方程求解器中自动微分与基于离散化约束的系统性分析

    A Systematic Analysis of Automatic Differentiation versus Discretization-based Constraints for Physics-Informed PDE Solvers

    [https://arxiv.org/abs/2609.07437](https://arxiv.org/abs/2609.07437)

    该论文通过从简单线性泊松方程到强不连续高超音速流动的广泛系统性实验，首次系统分析了物理信息PDE求解器中自动微分与基于离散化约束两种范式的各自适用性。

    

    物理信息神经网络（PINNs）代表了利用人工智能求解偏微分方程（PDEs）的一个新兴前沿领域。自动微分（AD）在这一范式中扮演着核心角色，它无需网格，用连续空间中基于梯度的优化取代了传统的迭代求解器。然而，自动微分的固有局限性，特别是在处理高阶导数和不连续解方面，给复杂问题带来了重大挑战。这促使越来越多的研究者探索基于离散化的约束作为替代路径。然而，这两种范式各自的适用性在很大程度上仍未被探索。在这项工作中，我们在广泛的问题范围内进行了系统性实验，涵盖从简单的线性泊松方程到具有强不连续性的高马赫数高超音速流动。通过对近似、优化等环节的严格分解……

    arXiv:2609.07437v1 Announce Type: cross  Abstract: Physics-informed neural networks (PINNs) represent a growing frontier in using artificial intelligence to solve partial differential equations (PDEs). Automatic differentiation (AD) plays a central role in this paradigm, which is mesh-free and replaces traditional iterative solvers with gradient-based optimization in continuous space. However, the inherent limitations of AD, particularly in handling higher-order derivatives and discontinuous solutions, pose significant challenges for complex problems. This has motivated a growing number of researchers to explore discretization-based constraints as an alternative path. Yet, the respective applicability of these two paradigms remains largely unexplored. In this work, we conduct systematic experiments across a wide spectrum of problems, from simple linear Poisson to high-Mach hypersonic flows with strong discontinuities. Through a rigorous decomposition of approximation, optimization, and
    
[^195]: 重新审视核学习问题中的稀疏化方法

    Revisiting Thinning Methods for Kernel Learning Problems

    [https://arxiv.org/abs/2609.07432](https://arxiv.org/abs/2609.07432)

    提出反向核聚合算法，通过迭代移除数据点构建代表子集，在加速子采样的同时达到与最先进方法相当的效果，并扩展核稀疏化方法使其能够构建任意大小的子集。

    

    核方法因其强大的理论保证和优异的实证性能而被广泛应用。然而，其高昂的计算成本限制了其在大规模数据集上的适用性。为解决这一缺陷，若干方法使用最大均值差异来构建代表性子集，以便在再生核希尔伯特空间中保留完整数据集的性质。我们提出了反向核聚合，这是一种通过从数据集中迭代移除数据点来解决该问题的算法，在缩减规模小于数据集一半的现实场景中，能够加速子采样过程，同时取得与当前最先进方法相当的结果。此外，我们提出了一种扩展方法，克服了核稀疏化的一个限制，使算法能够构建任意大小的子集，而不再局限于逐次减半的方式。最后，我们进行了大量的实验……

    arXiv:2609.07432v1 Announce Type: cross  Abstract: Kernel methods are widely used because of their strong theoretical guarantees and empirical performance. However, their high computational cost limits their applicability to large-scale datasets. To address this shortcoming, several approaches use Maximum Mean Discrepancy to construct representative subsets that preserve the properties of the full dataset in a Reproducing Kernel Hilbert Space. We introduce Backward Kernel Herding, an algorithm that addresses this problem by iteratively removing points from the dataset, achieving results comparable to current state-of-the-art approaches while accelerating the subsampling process in realistic scenarios where the reduced size is less than half of the dataset. Moreover, we overcome a limitation of Kernel Thinning by proposing an extension that enables the construction of subsets of arbitrary size rather that restricting to successive halvings. Finally, we conduct an extensive experimental 
    
[^196]: 对抗监视下认知雷达的认知遮蔽：一种分布式隐私框架

    Masking Radar Cognition under Adversarial Surveillance: A Distributional Privacy Framework

    [https://arxiv.org/abs/2609.07428](https://arxiv.org/abs/2609.07428)

    该论文提出了一种基于分布式隐私的电子反对抗框架，通过开发两种认知隐藏算法，在对抗监视下遮蔽认知雷达的战略决策过程并提供正式的隐私保证。

    

    本文提出了一种在线电子反对抗（ECCM）框架，旨在遮蔽运行于对抗性监视环境下的认知雷达（CR）的战略决策过程。我们在两种不同的决策范式下对认知雷达进行建模：静态约束效用最大化行为和动态期望效用最大化行为。雷达的效用函数通过von Mises–Fisher（vMF）分布进行建模，其分布参数构成了需要防止被对手推断的私有信息。我们采用分布式隐私框架来隐藏这一私有信息，并为认知遮蔽提供正式的分布式隐私保证。在这项工作中，我们分别为静态约束效用最大化（WDPCH-SU）和动态期望效用最大化（WDPCH-DU）开发了认知隐藏算法。通过严格的数学分析，我们证明了两者的……

    arXiv:2609.07428v1 Announce Type: cross  Abstract: In this article, we propose an online electronic counter-countermeasure (ECCM) framework designed to conceal the strategic decision-making processes of a cognitive radar (CR) operating under adversarial surveillance. We model the CR under two distinct decision paradigms: a static constrained utility-maximizing behavior and a dynamic expected utility-maximizing behavior. The radar's utility function is modeled via a von Mises--Fisher (vMF) distribution, with the distributional parameter constituting the private information to be protected from adversarial inference. We adopt a distribution privacy framework to conceal this private information and provide formal distribution privacy guarantees for cognition masking. In this work, we develop cognition-hiding algorithms for both static constrained utility maximization (WDPCH-SU), and dynamic expected utility maximization (WDPCH-DU). Through rigorous mathematical analysis, we show that both
    
[^197]: RelightFormer：用于多视角物体重照明的前馈生成式Transformer

    RelightFormer: Feed-forward Generative Transformer for Multiview Object Relighting

    [https://arxiv.org/abs/2609.07414](https://arxiv.org/abs/2609.07414)

    该论文提出RelightFormer，一种基于视频基础模型改编的前馈生成式Transformer，通过潜在光照模块和置换不变位置编码实现无需显式逆渲染的单视图与多视图图像重照明，并构建了包含9万物体和3.9万种光照的大规模LOD数据集支撑训练。

    

    图像重照明传统上通过复杂的逆渲染管线来解决，这类方法存在不适定优化问题；或者采用单图像生成模型，但这类模型忽略了理解3D几何和材质交互所需的关键多视角线索。为了解决这些局限性，我们提出了一种前馈生成式Transformer，可直接进行单视图和多视图图像重照明，完全绕过显式的内在属性估计。我们的架构改编自视频基础模型，其特征在于一个潜在光照模块，通过交叉注意力将目标环境贴图动态注入空间特征。此外，我们采用置换不变的位置编码，对称地处理无序的多视图输入而不产生顺序偏差。为训练这一鲁棒的数据驱动模型，我们构建了大规模的Laval Objaverse数据集（LOD），包含9万个物体和3.9万种独特光照。大量实验……（原文在此处截断）

    arXiv:2609.07414v1 Announce Type: cross  Abstract: Image relighting is traditionally tackled via complex inverse rendering pipelines, which suffer from ill-posed optimization, or single-image generative models that ignore crucial multi-view cues necessary for understanding 3D geometry and material interactions. To address these limitations, we introduce a feed-forward generative Transformer for direct single- and multi-view image relighting that entirely bypasses explicit intrinsic property estimation. Adapted from a video foundation model, our architecture features a latent illumination module that dynamically injects target environment maps into spatial features via cross-attention. Furthermore, we employ permutation-invariant positional encodings to symmetrically process unordered multi-view inputs without sequential bias. To train this robust data-driven model, we construct the massive Laval Objaverse Dataset (LOD), comprising 90K objects and 39K unique illuminations. Extensive exp
    
[^198]: 微流控通道中血细胞及其聚集体的多标签与多类别分类对比研究

    Multi-label versus multi-class classification of blood cells and their aggregates in microfluidic channels

    [https://arxiv.org/abs/2609.07410](https://arxiv.org/abs/2609.07410)

    本文提出在形变细胞术中用多标签分类替代传统多类别分类来识别血细胞及其聚集体，能够识别训练数据中未出现过的细胞聚集体，同时简化并加快了数据标注过程。

    

    形变细胞术是一种成像流式细胞术，它使用配备相机的设备在高通量条件下测量细胞刚度以及其他细胞特性。面积和伸长率等细胞特性可用于识别细胞类型，但这需要预先了解区分性特征，且无法应用于临床上重要的细胞聚集体。利用DC数据，我们评估了传统的多类别分类方法，并提出了一种多标签方法来识别血细胞及其聚集体。具体而言，ML分类器可以同时为单个成像事件分配多个细胞类型标签。我们表明，与多类别分类不同，多标签分类能够识别训练数据中未出现过的细胞聚集体。它还避免了使用详尽、严格定义的聚集体标签的需要，从而简化并加快了标注过程。由于自动血液分析仪...（摘要不完整）

    arXiv:2609.07410v2 Announce Type: cross  Abstract: Deformability cytometry (DC) is a type of imaging flow cytometry, which uses a camera-equipped device to measure cellular stiffness in addition to other cellular properties at high throughput. Cellular properties such as area and elongation can identify cell types, but this requires prior knowledge of distinguishing properties and cannot be applied to clinically important cell aggregates. Using DC data, we evaluated conventional multi-class (MC) classification and introduced a multi-label (ML) approach for identifying blood cells and their aggregates. In particular, an ML classifier can simultaneously assign multiple cell-type labels to a single imaged event. We show that, unlike MC classification, ML classification can identify cell aggregates not represented in the training data. It also avoids the need for exhaustive, strictly defined aggregate labels, thereby simplifying and speeding up annotation. Since automated blood analyzers d
    
[^199]: RAFM-SER++：一种用于监控系统实时行为监测的轻量级多模态情感识别框架

    RAFM-SER++: A Lightweight Multimodal Emotion Recognition Framework for Real-Time Behavioral Monitoring in Surveillance Systems

    [https://arxiv.org/abs/2609.07409](https://arxiv.org/abs/2609.07409)

    提出了一种轻量级多模态语音情感识别框架RAFM_SER++，通过非对称单向残差注意力融合机制在低计算开销下实现高性能情感识别，适用于资源受限的实时监控系统。

    

    近期多模态语音情感识别（SER）系统通过交互密集的跨模态Transformer实现了高精度，但其高昂的计算成本限制了在延迟敏感且资源受限的监控系统中的部署。为应对这一挑战，我们提出了RAFM_SER++，这是一种轻量级的多模态SER框架，其核心特点是采用非对称残差注意力融合机制（RAFM）。RAFM不依赖计算开销高昂的双向交互，而是通过单向残差注意力通路将语音中的情感线索注入语义文本表示中。结合受BYOL启发的跨模态对齐目标和注意力引导池化，所提出的框架在保持低计算开销的同时改善了多模态表示学习。在IEMOCAP和ESD基准数据集上的实验表明，RAFM_SER++始终优于HuBERT-Base基线，并取得了……

    arXiv:2609.07409v1 Announce Type: new  Abstract: Recent multimodal Speech Emotion Recognition (SER) systems achieve high accuracy through interaction-heavy cross-modal transformers, but their computational cost limits deployment in latency-sensitive and resource-constrained surveillance systems. To address this challenge, we propose RAFM_SER++, a lightweight multimodal SER framework featuring an asymmetric Residual Attention Fusion Mechanism (RAFM). Rather than relying on computationally expensive bidirectional interactions, RAFM injects affective speech cues into semantic text representations through a one-directional residual attention pathway. Combined with a BYOL-inspired cross-modal alignment objective and attention-guided pooling, the proposed framework improves multimodal representation learning while maintaining low computational overhead.   Experiments on the IEMOCAP and ESD benchmarks demonstrate that RAFM_SER++ consistently outperforms the HuBERT-Base baseline and achieves a
    
[^200]: Canny边缘检测预处理对帕金森病分类机器学习模型性能的影响

    Impact of canny edge detection preprocessing on performance of machine learning models for Parkinson's disease classification

    [https://arxiv.org/abs/2609.07408](https://arxiv.org/abs/2609.07408)

    本研究系统评估了多种机器学习模型在帕金森病分类任务中的表现，发现增大训练数据集会增加模型内存占用和预测延迟，而Canny边缘检测结合Hessian滤波的预处理方法反而会降低模型分类性能。

    

    本研究使用机器学习模型将个体分类为健康或帕金森病高风险人群，重点研究数据集规模和预处理技术对模型性能的影响。研究从原始数据集创建了四个数据集：DS_0（普通数据集）、DS_1（对DS_0进行Canny边缘检测和Hessian滤波处理）、DS_2（增强的DS_0）和DS_3（增强的DS_1）。研究者在这些数据集上评估了一系列机器学习模型——逻辑回归（LR）、决策树（DT）、随机森林（RF）、梯度提升（GB）、XGBoost、朴素贝叶斯（NB）、支持向量机（SVM）和AdaBoost（AdB），并分析了预测准确率、模型大小和预测延迟。结果表明，虽然更大的数据集会导致模型内存占用和预测延迟增加，但采用Canny边缘检测并辅以Hessian滤波的预处理（应用于DS_1和DS_3）会降低模型性能。

    arXiv:2609.07408v1 Announce Type: new  Abstract: This study investigates the classification of individuals as healthy or at risk of Parkinson's disease using machine learning (ML) models, focusing on the impact of dataset size and preprocessing techniques on model performance. Four datasets are created from an original dataset: DS_0, (normal dataset), DS_1 (DS_O subjected to Canny edge detection and Hessian filtering), DS_2 (augmented DS_0), and DS_3 (augmented DS_1). We evaluate a range of ML models-Logistic Regression (LR), Decision Tree (DT), Random Forest (RF), Gradient Boosting (GB), XGBoost (XBG), Naive Bayes (NB), Support Vector Machine (SVM), and AdaBoost (AdB)-on these datasets, analyzing prediction accuracy, model size, and prediction latency. The results show that while larger datasets lead to increased model memory footprints and prediction latencies, the Canny edge detection preprocessing supplemented by Hessian filtering (used in DS_1 and DS_3) degrades the performance of
    
[^201]: 拓宽思考：缓解隐式思维链推理中的潜在秩坍缩问题

    Think Wider: Mitigating Latent Rank Collapse in Implicit Chain-of-Thought Reasoning

    [https://arxiv.org/abs/2609.07406](https://arxiv.org/abs/2609.07406)

    提出WIDER——一种即插即用的轻量级谱正则化方法，通过惩罚潜在状态向共享主导方向的投影来缓解隐式思维链推理中的潜在秩坍缩问题，使潜在状态扩展到更广泛的表示子空间并提升推理多样性。

    

    链式思维推理通过引入中间计算提升了大语言模型的推理能力，但显式的推理过程会增加解码长度、延迟和上下文成本。隐式CoT通过将中间推理迁移到连续潜在状态中，提供了一种更高效的替代方案。然而，潜在推理可能不稳定：连续的潜在状态可能变得过于相似，并向一个共享的主导方向坍缩，从而降低推理轨迹的多样性。在本工作中，我们识别了“潜在秩坍缩”现象，并提出了WIDER——一种面向隐式CoT的轻量级谱正则化方法。在训练过程中，WIDER估计每个潜在轨迹的共享方向，并惩罚向该方向的投影，鼓励潜在状态跨越更广泛的表示子空间。该方法是即插即用的，不改变骨干模型、潜在调度和推理时的解码过程（原文在此处截断）。

    arXiv:2609.07406v1 Announce Type: new  Abstract: Chain-of-thought (CoT) reasoning improves the reasoning ability of large language models by introducing intermediate computation, but explicit rationales increase decoding length, latency, and context cost. Implicit CoT offers a more efficient alternative by moving intermediate reasoning into continuous latent states. However, latent reasoning can be unstable: successive latent states may become overly similar and collapse toward a shared dominant direction, reducing the diversity of the reasoning trajectory. In this work, we identify $\textit{latent rank collapse}$ and propose $\textbf{WIDER}$, a lightweight spectral regularizer for implicit CoT. During training, WIDER estimates the shared direction of each latent trajectory and penalizes projections onto this direction, encouraging latent states to span a broader representational subspace. The method is plug-and-play and leaves the backbone model, latent schedule, and inference-time de
    
[^202]: 基于联邦二值门控与服务器端视觉-语言推理的监控异常分类

    Federated Binary Gating with Server-Side Vision-Language Inference for Surveillance Anomaly Classification

    [https://arxiv.org/abs/2609.07403](https://arxiv.org/abs/2609.07403)

    该论文提出一种混合两阶段架构，利用轻量级联邦二值CNN门控在本地进行异常筛查，仅将可疑视频传输至服务器端由零样本视觉-语言模型（Qwen3-VL-8B）进行元类别分类，从而在隐私敏感且数据非独立同分布的监控场景中避免了直接联邦多类训练的崩溃问题。

    

    隐私敏感的监控系统可以从大型视觉-语言模型（VLM）中受益，但此类模型通常需要对原始视频进行集中式访问。在联邦学习环境中，非独立同分布的客户端数据加剧了这一挑战，可能使直接的多类别异常分类变得不稳定，尤其是对于稀有类别。我们提出了一种混合两阶段架构，将联邦二值卷积神经网络（CNN）门控与服务器端零样本视觉-语言推理相结合。轻量级的LiteCNN3D门控执行本地异常筛查，仅将被标记的视频转发给Qwen3-VL-8B，由其将视频分配到四个异常元类别中。我们在划分为五个粗粒度元类别的UCF-Crime数据集上评估了该设计，并在真实的三节点异构部署中实现了联邦阶段。在所研究的设置中，直接的联邦多类别训练会崩溃，而……（摘要不完整，原文在此处截断）

    arXiv:2609.07403v1 Announce Type: cross  Abstract: Privacy-sensitive surveillance systems could benefit from large vision-language models (VLMs), but such models typically require centralized access to raw video. In federated learning settings, this challenge is amplified by non-independent and identically distributed (non-IID) client data, which can make direct multiclass anomaly classification unstable, especially for rare categories. We propose a hybrid two-stage architecture that combines a federated binary convolutional neural network (CNN) gate with server-side zero-shot VLM inference. The lightweight LiteCNN3D gate performs local anomaly screening and forwards only flagged videos to Qwen3-VL-8B, which assigns them to four anomaly metaclasses. We evaluate this design on UCF-Crime grouped into five coarse metaclasses and implement the federated stage in a real three-node heterogeneous deployment.   In the studied setting, direct federated multiclass training collapses, whereas the
    
[^203]: 乘积酉流形上多人量子博弈的黎曼优化

    Riemannian Optimization for Multi-Player Quantum Games on Product Unitary Manifolds

    [https://arxiv.org/abs/2609.07397](https://arxiv.org/abs/2609.07397)

    本文提出了面向混合策略多人量子博弈的扩展EWL（EEWL）框架，并引入酉策略矩阵指数算法，通过在乘积酉流形上进行黎曼优化来求解多人量子博弈的均衡问题。

    

    量子博弈论是经典博弈论的扩展，它将量子原理应用于博弈论之中。Eisert-Wilkens-Lewenstein（EWL）量子博弈是将双人经典囚徒困境转化为量子囚徒困境的早期典型例子。在EWL博弈中，参与者选择由酉矩阵表示的纯量子策略。这种扩展可以通过实现具有更高收益的合作均衡来解决经典困境。在本文中，我们首先讨论了面向具有混合策略的多人量子博弈的扩展EWL（EEWL）框架。在EEWL中，每个参与者控制一组酉算子作为量子动作，并对这些动作使用经典混合策略。收益被定义为作用于共享量子态的厄米特奖励算子的期望值，该量子态根据EEWL协议生成并测量。随后，我们提出了酉策略矩阵指数算法

    arXiv:2609.07397v1 Announce Type: cross  Abstract: Quantum game theory is an extension of classical game theory that uses quantum principles in game theory. The Eisert-Wilkens-Lewenstein (EWL) quantum game is an early example of the two-player classical Prisoner's Dilemma transformed into a quantum Prisoner's Dilemma. In the EWL game, the players choose pure quantum strategies represented by unitary matrices. This extension can resolve the classical dilemma by enabling cooperative equilibrium with higher payoff. In this paper, we first discuss the Extended EWL (EEWL) for multiplayer quantum games with mixed strategies. In EEWL, each player controls a set of unitary operators as quantum actions and uses a classical mixed strategy over these actions. The payoffs are defined as expectation values of Hermitian reward operators acting on a shared quantum state, which is generated and measured according to the EEWL protocol. We then propose the Unitary Strategy Matrix Exponential Algorithm (
    
[^204]: 分布式滞后神经加性模型

    Distributed Lag Neural Additive Models

    [https://arxiv.org/abs/2609.07381](https://arxiv.org/abs/2609.07381)

    该论文提出分布式滞后神经加性模型（DLNAM），用神经网络组件替代预先指定的样条交叉基来学习暴露-滞后非线性效应，在保持加性可解释性的同时避免了基函数选择问题，并在模拟中相比传统DLNM实现了更低的偏差、更强的边界恢复能力和更好校准的不确定性估计。

    

    我们提出了分布式滞后神经加性模型（DLNAMs），它是分布式滞后非线性模型（DLNMs）的神经加性类比，用于学习分布在滞后期的非线性效应。DLNAMs用能够学习暴露-滞后响应面的神经组件取代了预先指定的样条交叉基，从而避免了基函数族、维度和节点位置的选择问题，同时保留了加性可解释性和人们熟悉的分布式滞后摘要统计量。以指数为中心的输入层、平滑激活函数以及学习得到的子网络混合产生了平滑的、局部自适应的表示；逐点不确定性则结合了条件化的最后一层拉普拉斯近似与集成成员间的变异。在模拟实验中，DLNAMs在恢复已知响应函数方面总体优于DLNM对比方法（包括惩罚变体和树状变体），表现出更低的偏差、更强的边界恢复能力以及更好校准的累积置信区间；其优势最大（摘要在此处截断）。

    arXiv:2609.07381v1 Announce Type: cross  Abstract: We introduce Distributed Lag Neural Additive Models (DLNAMs), neural-additive analogues of Distributed Lag Non-linear Models (DLNMs) for learning nonlinear effects distributed over lags. DLNAMs replace a prespecified spline cross-basis with neural components that learn exposure--lag response surfaces, avoiding choices of basis family, dimension, and knot placement while preserving additive interpretability and familiar distributed-lag summaries. Exp-centered input layers, smooth activations, and learned subnetwork mixtures produce smooth, locally adaptive representations; pointwise uncertainty combines a conditional last-layer Laplace approximation with between-member ensemble variation. In simulations, DLNAMs generally outperformed DLNM comparators, including penalized and treed variants, in recovering known response functions, with lower bias, stronger boundary recovery, and better-calibrated cumulative intervals; gains were largest 
    
[^205]: 从聚合动态中推断城市出行交互

    Inferring Urban Mobility Interactions from Aggregated Dynamics

    [https://arxiv.org/abs/2609.07349](https://arxiv.org/abs/2609.07349)

    仅利用城市已有的聚合区域计数数据，通过不确定性感知的物理信息框架即可准确推断未来的出发地-目的地流量，无需追踪个体轨迹，在保护隐私的同时达到与依赖历史OD矩阵的模型相当的精度。

    

    实时城市治理不仅需要知道人们在哪里，还需要了解人们如何在地点之间移动——这种方向性流动传统上需要通过在空间中追踪个体来解析，这种方式维护成本高昂，且建立在高度独特、极易被重新识别的轨迹数据之上。本研究表明，这种方向性结构无需被直接观测即可获知：城市已经在收集的聚合计数数据保留了足够的信息，可用于重建出发地-目的地（OD）矩阵的时间演化。利用一个具有不确定性感知的物理信息框架，我们仅从区域级别的计数数据中推断未来的OD流量，并在来自美国和中国城市的十二个出行数据集上进行了验证，其准确性可与以历史OD矩阵为输入的模型相媲美。概率建模纠正了对稀疏、高价值通道的系统性低估，并产生了与观测数据相符的校准预测。

    arXiv:2609.07349v1 Announce Type: new  Abstract: Real-time urban governance depends not only on knowing where people are, but on how they move between places, directional flows that could be conventionally resolved by tracking individuals through space, i.e., expensive to sustain and built on traces that are highly unique and readily re-identifiable. Here we show that this directional structure need not be observed to be known: aggregated counts which cities already collect retain enough information to reconstruct the temporal evolution of origin-destination (OD) matrix. Using an uncertainty-aware physics-informed framework, we infer future OD flows from area-level counts alone across twelve mobility datasets from cities in the United States and China, reaching accuracy comparable to models that take historical OD matrices as input. Probabilistic modeling corrects the systematic underestimation of sparse, high-value corridors and yields calibrated predictions consistent with observed f
    
[^206]: 面向长上下文的基于内容寻址方法

    Content-Based Addressing for Long Context

    [https://arxiv.org/abs/2609.07314](https://arxiv.org/abs/2609.07314)

    提出一种基于内容寻址的长上下文位置编码方法，将 token 流划分为单元、单元内保留 RoPE 并为每个完成的单元按内容计算地址，从而在扩展上下文时完全保留局部 RoPE 并使固定 token 间的注意力比较不受其他单元插入或重排的影响。

    

    旋转位置编码使用每个 token 的整数位置来确定注意力内部施加的旋转。这对局部 token 顺序效果良好，但随着上下文长度的增加，会产生位置上的训练-测试不匹配：RoPE 会在训练中未见过的偏移量处产生相对旋转。那些对位置进行重缩放、插值、随机化或加偏置的方法规定了注意力如何处理这些偏移量，但仍然从不断增长的 token 计数器中获取位置信息。我们转而将 token 流划分为单元，在每个单元内保留普通的 RoPE 位置，并为每个已完成的单元分配一个由其内容计算得出的地址。这样，添加单元时只需将同一个学习到的映射应用于新内容，而无需扩展位置范围或标识符表。我们证明该构造能够完全保留局部 RoPE，当其他单元被插入或重排时，两个固定 token 之间的注意力比较保持不变，并且……（摘要在此处被截断）

    arXiv:2609.07314v1 Announce Type: cross  Abstract: Rotary position embedding (RoPE) uses each token's integer position to determine the rotation applied inside attention. This works well for local token order, but increasing context length creates a positional train-test mismatch: RoPE produces relative rotations at offsets not seen during training. Methods that rescale, interpolate, randomize, or bias positions specify how attention handles those offsets, but still derive positional information from a growing token counter. We instead divide a token stream into units, retain ordinary RoPE positions within each unit, and assign every completed unit an address computed from its content. Adding units then applies the same learned map to new content rather than extending a positional range or an identifier table. We prove that this construction preserves local RoPE exactly, leaves the attention comparison between two fixed tokens unchanged when other units are inserted or reordered, and d
    
[^207]: 基于预测约束邻域协作的鲁棒去中心化个性化联邦学习

    Robust Decentralized Personalized Federated Learning via Prediction-Constrained Neighborhood Collaboration

    [https://arxiv.org/abs/2609.07312](https://arxiv.org/abs/2609.07312)

    该论文提出R-DPFL方法，通过对比邻域聚合更新与基于历史的预测更新之间的差异并进行自适应裁剪，使诚实客户端在拜占庭攻击下无需邻居共识即可保持稳定的个性化联邦学习动态。

    

    本文提出了一种鲁棒的去中心化个性化联邦学习方法R-DPFL，使客户端能够通过鲁棒的邻域方向估计和基于历史的更新趋势预测来降低拜占庭攻击的影响，而不是像现有工作那样纯粹地聚合客户端模型。在R-DPFL中，每个客户端首先通过聚合接收到的邻域更新向量来计算当前轮次的模型更新；然后，基于历史值和本地模型变化预测该更新应有的值；最后，R-DPFL计算这两个量之间的差异，自适应地对差异进行裁剪，并将其添加到本地更新中。我们通过严格的理论分析证明了学习过程的收敛性，并表明诚实客户端在拜占庭邻居扰动下能够保持稳定的个性化下降动态，而无需邻居模型之间达成共识。在CIFAR-10上的大量实验验证了该方法的有效性。

    arXiv:2609.07312v1 Announce Type: new  Abstract: This paper proposes a robust decentralized personalized federated learning method R-DPFL, that enables clients to reduce the impact of Byzantine attacks via robust neighborhood direction estimation and history-based update trend prediction, rather than purely aggregating client models as in the existing work. In R-DPFL, each client first computes the current-round model update by aggregating the received neighborhood update vectors. It then predicts what this update should be based on its historical values and local model changes. Finally, R-DPFL computes the difference between these two quantities, adaptively clips this difference, and adds it to the local update. We prove convergence of the learning process through rigorous analysis and show that honest clients maintain stable personalized descent dynamics under Byzantine neighbor perturbations without requiring consensus among neighboring models. Extensive experiments on CIFAR-10 demo
    
[^208]: 基于渐进点匹配的长时程语言模型强化学习

    Long-Horizon Language Model Reinforcement Learning via Progressive Point Matching

    [https://arxiv.org/abs/2609.07303](https://arxiv.org/abs/2609.07303)

    该论文提出“渐进点匹配”——一种简单无偏的片段级密集奖励方法，仅需每个任务一条参考轨迹即可实现，使强化学习在稀疏奖励完全失效的长时程任务（如极难数学推理）上获得指数级效率提升。

    

    arXiv:2609.07303v1 公告类型：新论文 摘要：当前通过强化学习训练语言模型的范式严重依赖稀疏的结果奖励。然而，当我们追求需要更长、更复杂轨迹的任务时，这类策略会导致学习速度缓慢。先前的工作尝试通过奖励部分进展来解决这一问题，但朴素的奖励设计往往存在偏差，并会收敛到次优策略。我们提出了一种简单且无偏的密集奖励方法，称之为渐进点匹配，该方法通过在片段级别奖励部分进展，在理论上和经验上（通过合成环境验证）均能以指数级更高的效率扩展到长时程任务。随后我们展示了如何利用每个任务仅需一条参考轨迹来实际实现渐进点匹配。在极其困难的数学推理问题上，稀疏结果奖励无法取得任何进展，而片段级奖励则能在更大的……

    arXiv:2609.07303v1 Announce Type: new  Abstract: Current paradigms for training language models via reinforcement learning rely heavily on sparse outcome rewards. However, as we pursue tasks that require longer and more complicated trajectories, such strategies result in slow learning. Prior work has attempted to address this problem by rewarding partial progress; however, naive formulations are often biased and converge to suboptimal policies. We show that a simple and unbiased dense reward formulation, which we term progressive point matching, scales exponentially more efficiently to long-horizon tasks by rewarding partial progress on a segment level, both theoretically and empirically via synthetic environments. We then show how progressive point matching can be practically instantiated using a single reference trajectory per task. On extremely hard math reasoning problems, sparse outcome rewards cannot make any progress, whereas segment-level rewards enable improvements at larger t
    
[^209]: PCFlow：面向探地雷达B扫描图像合成的物理条件流匹配方法

    PCFlow: Physics-Conditioned Flow Matching for GPR B-Scan Image Synthesis

    [https://arxiv.org/abs/2609.07300](https://arxiv.org/abs/2609.07300)

    提出PCFlow框架，通过基于麦克斯韦方程的密集物理条件场引导条件流匹配，实现快速且兼具视觉真实性与物理一致性的探地雷达B扫描图像合成。

    

    探地雷达（GPR）B扫描图像合成对于数据增强、算法验证和仿真加速至关重要，然而生成既具有视觉真实性又具备物理一致性的雷达图仍然具有挑战性。现有的基于学习的生成模型往往侧重于视觉外观，但对响应几何形状的控制能力有限。在本文中，我们提出了PCFlow，一个用于快速GPR B扫描图像合成的物理条件流匹配框架。PCFlow的核心是由电磁仿真所使用的参数化物理模型构建的麦克斯韦信息密集物理条件场，包括材料属性、目标几何、传播线索和响应域先验。该条件场提供了物理场景参数与雷达响应几何之间的可解释接口，并引导VAE潜在空间中的条件流匹配朝向物理可行的生成方向进行。

    arXiv:2609.07300v1 Announce Type: new  Abstract: Ground-penetrating radar (GPR) B-scan image synthesis is important for data augmentation, algorithm validation, and simulation acceleration, yet generating radargrams with both visual realism and physical consistency remains challenging. Existing learning-based generative models often emphasize visual appearance but provide limited control over response geometry. In this paper, we propose PCFlow, a physics-conditioned flow matching framework for fast GPR B-scan image synthesis. The core of PCFlow is a Maxwell-informed dense physical condition field constructed from the parameterized physical model used for electromagnetic simulation, including material properties, target geometry, propagation cues, and response-domain priors. This condition field provides an interpretable interface between physical scene parameters and radar response geometry, and guides conditional flow matching in the VAE latent space toward physically feasible generat
    
[^210]: 路径依赖塑性的本构状态空间建模：一种分辨率一致且可并行化的计算框架

    Constitutive State-Space Modeling of Path-Dependent Plasticity: A Resolution-Consistent and Parallelizable Computational Framework

    [https://arxiv.org/abs/2609.07294](https://arxiv.org/abs/2609.07294)

    本文提出一种本构状态空间（CSS）模型，通过将应变增量分解为方向与幅值并嵌入结构化状态空间动力学，实现了路径依赖塑性建模中对应变路径分辨率的一致性，同时保留并行扫描结构以支持长本构历史的高效训练。

    

    针对路径依赖塑性的数据驱动本构模型通常采用非线性循环神经网络来构建，其顺序状态演化限制了并行训练，且预测结果可能依赖于所施加应变路径的离散化方式。我们提出了一种本构状态空间（CSS）模型，将结构化状态空间动力学重新表述为增量本构算子。应变增量被分解为幅值和方向两部分：加载方向驱动潜在状态空间系统，而增量幅值则进入其连续时间线性递推的零阶保持离散化。这种为力学特性量身定制的构造保证了零增量下的平稳性，显著降低了对应变路径分辨率的敏感性，并保留了S5的并行扫描结构，从而能够在长本构历史序列上进行高效训练。CSS和最小状态单元（MSC）架构……

    arXiv:2609.07294v1 Announce Type: new  Abstract: Data-driven constitutive models for path-dependent plasticity are commonly formulated using nonlinear recurrent neural networks, whose sequential state evolution limits parallel training and whose predictions may depend on the discretization of the applied strain path. We introduce a Constitutive State Space (CSS) model that reformulates structured state-space dynamics as an incremental constitutive operator. The strain increment is decomposed into magnitude and direction: the loading direction drives the latent state-space system, while the increment magnitude enters the zero-order-hold discretization of its continuous-time linear recurrence. This mechanics-tailored construction guarantees stationarity under zero increments, strongly reduces sensitivity to strain-path resolution, and retains the parallel-scan structure of S5 for efficient training on long constitutive histories. The CSS and Minimal State Cell (MSC) architectures are com
    
[^211]: 面向模型感知紧凑语义检索的俄罗斯套娃哈希表示

    Matryoshka Hash Representations for Model-Aware Compact Semantic Retrieval

    [https://arxiv.org/abs/2609.07276](https://arxiv.org/abs/2609.07276)

    该论文提出俄罗斯套娃式哈希表示，使同一个压缩编码的各长度前缀均可直接检索，从而让检索系统无需重新编码语料库就能灵活地在不同字节预算下切换效率与质量的权衡点。

    

    检索增强生成（RAG）依赖于稠密检索：每篇文档被存储为一个学习到的向量，查询通过在该向量空间中寻找最近邻来得到回答。在语料库规模下，为每篇文档保存一个全精度向量是索引的主要开销，因此检索系统将每个向量替换为几字节的短编码——这一步称为量化。标准的量化器（如乘积量化 PQ）会选择最能重构原始向量的编码。如果一个编码能同时服务多个字节预算，它将更加有用：当它的各个短前缀均可直接检索时，部署方无需重新编码整个语料库即可设定效率与质量的工作点。但在单一目标下训练所有前缀，会使得前面的比特成为多个预算之间的折中——短编码性能提升的同时，全宽编码的性能却会下降。量化到低位表示（如二进制编码），进一步……

    arXiv:2609.07276v1 Announce Type: cross  Abstract: Retrieval-augmented generation (RAG) depends on dense retrieval: each document is stored as a learned vector, and a query is answered by finding its nearest neighbors in that vector space. Keeping one full-precision vector per document is the dominant index cost at corpus scale, so retrieval systems replace each vector with a short code of a few bytes---a step called quantization. Standard quantizers such as product quantization (PQ) pick the code that reconstructs the original vector most closely.   A single code is even more useful if it serves several byte budgets at once: when its short prefixes are each directly searchable, a deployment can set its efficiency--quality operating point without re-encoding the corpus. But training all prefixes under one objective makes the early bits a compromise across budgets---short codes improve while the full-width code degrades. Quantization to low-bit representation, such as binary codes, furt
    
[^212]: 通过规范校正的通道移除实现Transformer的稠密结构压缩

    Dense Structural Compression of Transformers via Gauge-Correct Channel Removal

    [https://arxiv.org/abs/2609.07264](https://arxiv.org/abs/2609.07264)

    提出GaugeLasso方法，通过规范校正的加性对称组套索通道惩罚，在训练中将Transformer的冗余通道驱动至零并物理移除，同时保持张量稠密性和网络功能，从而降低推理计算成本与能耗。

    

    每个token的推理能耗决定了已部署Transformer模型的运行成本与碳足迹，该能耗主要由稠密矩阵乘法产生，涉及融合乘加（FMA）运算和内存流量。为了在保留稠密张量以获得高GPU吞吐量的同时减少这些计算，我们从第一性原理出发开发了一套方法，在训练过程中调整结构复杂度，以最大化单位计算量下的推理效用。通道惩罚会将整个张量切片驱动至零，从而实现物理移除，同时保持张量的稠密性和网络功能。自然而直接的方法——对作用于每个通道的算子分量的范数施加惩罚——被证明会因规范自由度而不稳定。我们通过GaugeLasso解决了这一病态问题：一种加性对称的组套索（group-lasso）惩罚，当网络收敛至规范平衡时，能够恢复乘积范数的单调函数。我们的均衡分析使得逐通道校准能够正确地……

    arXiv:2609.07264v1 Announce Type: new  Abstract: Inference energy per token drives the cost and carbon footprint of deployed transformers. It is dominated by dense matrix products that incur fused multiply-accumulate (FMA) operations and memory traffic. To reduce these computations while retaining dense tensors for high GPU throughput, we develop a methodology from first principles to adapt structural complexity during training to maximize inference utility per unit compute. Channel penalties drive entire tensor slices to zero to enable physical removal while preserving density and the network function.   The natural approach, penalizing the norm of operator components acting through each channel, is provably destabilized by gauge freedom. We resolve this pathology with GaugeLasso: additive symmetric group-lasso penalties that recover a monotone function of product-norms when the network converges to gauge balance. Our equilibrium analysis enables per-channel calibration to correctly s
    
[^213]: 不连续函数的Kolmogorov-Arnold稳定性

    Kolmogorov--Arnold stability for discontinuous functions

    [https://arxiv.org/abs/2609.07240](https://arxiv.org/abs/2609.07240)

    本文证明了Kolmogorov-Arnold表示定理对于多元不连续且无界函数在隐藏层受到对抗性重参数化时依然保持稳定，为KAN等现代深度学习架构的结构鲁棒性奠定了严格的数学基础。

    

    我们研究了Kolmogorov-Arnold表示定理（KART）在多元不连续且无界函数情形下，当隐藏层受到对抗性重参数化时的稳定性。我们的结果为现代深度学习架构（如Kolmogorov-Arnold网络，KANs）在对抗性配置下的结构鲁棒性提供了严格的数学基础。

    arXiv:2609.07240v1 Announce Type: new  Abstract: Here we investigate the stability of the Kolmogorov--Arnold representation theorem (KART) under adversarial reparameterisations of the hidden layer for multivariate discontinuous and unbounded functions. Our results provide a rigorous mathematical foundation for the structural robustness of modern deep learning architectures, such as Kolmogorov--Arnold Networks (KANs), under adversarial configurations.
    
[^214]: 面向快速训练收敛的并行策略链式方法

    Parallelism Strategy Chaining for Fast Training Convergence

    [https://arxiv.org/abs/2609.07236](https://arxiv.org/abs/2609.07236)

    本文提出CONA训练方法，通过在训练过程中在线动态串联切换最佳并行策略，而非离线固定单一策略，使大型语言模型达到目标验证困惑度的时间缩短1.8-11.4倍。

    

    选择并行策略——即数据并行、张量并行和流水线并行度以及微批次和全局批次大小的组合配置——在很大程度上决定了大型语言模型的训练效率。最先进的方法在离线阶段搜索并行策略，并选择使每次迭代时间最小的单一策略。但我们发现这些方法忽略了目标验证困惑度以及达到该困惑度所需的时间（Time-to-Perplexity，TTP）。特别是，我们的分析揭示，能够带来最快困惑度下降的最佳策略在训练过程中会多次变化。因此，最先进的方法在TTP上比在每次迭代都选择最佳策略的策略序列慢1.8-11.4倍。本文提出了CONA，一种引入在线策略链式方法的新训练技术。CONA不再使用离线选择的单一策略，而是在训练过程中使用由替代指标构建的方法对候选策略进行排序（摘要内容在此处被截断）。

    arXiv:2609.07236v1 Announce Type: cross  Abstract: Selecting a parallelism strategy - the configuration of data, tensor, and pipeline parallelism degrees together with micro- and global-batch sizes - largely determines the training efficiency of large language models. State-of-the-art methods search for a parallelism strategy offline and select the single strategy that minimizes per-iteration time. But we find that they neglect the target validation perplexity and time-to-perplexity (TTP). In particular, our analysis reveals that the best strategy yielding the fastest perplexity improvement changes multiple times during training. As a result, state-of-the-art methods are 1.8-11.4x slower in TTP than the strategy sequence that selects the best strategy at each iteration. This paper proposes CONA, a new training method that introduces online strategy chaining. Instead of a single strategy selected offline, CONA ranks candidate strategies during training using a surrogate metric built fro
    
[^215]: 基于多模态知识协作的鲁棒去中心化联邦蒸馏

    Robust Decentralized Federated Distillation via Multi-Modality Knowledge Collaboration

    [https://arxiv.org/abs/2609.07230](https://arxiv.org/abs/2609.07230)

    本文提出一种基于多模态知识协作的鲁棒去中心化联邦蒸馏方法，通过对类别预测、边界决策和预测相关性三种模态的评估、过滤不可靠客户端、并利用私有数据的监督梯度验证蒸馏梯度来抵御拜占庭攻击，同时证明了方法的收敛性。

    

    本文提出一种鲁棒的去中心化联邦蒸馏方法，使具有异构模型的客户端能够通过在共享的无标签公共数据上的预测进行协作。在所提出的方法中，每个客户端首先从类别预测、边界决策和预测相关性三种模态评估接收到的预测，然后过滤掉不可靠的客户端，为保留的客户端分配基于可靠性的权重，并为每种类型的知识构建一个教师模型。最后，使用由私有数据计算得到的监督梯度对相应的蒸馏梯度进行验证：冲突的预测梯度和边界梯度被移除，冲突的关系梯度在最终模型更新前被抑制。我们通过证明诚实客户端在拜占庭蒸馏下能够实现稳定的局部优化，证明了所提方法的收敛性。特别地，我们展示了我们的方法能确保有界的...（摘要在此处截断）

    arXiv:2609.07230v1 Announce Type: new  Abstract: This paper propose a robust decentralized federated distillation method that enables clients with heterogeneous models to collaborate through predictions on shared unlabeled public data. In the proposed method, each client first evaluates the received predictions in three modalities of class prediction, boundary decision, and prediction correlation. It then filters unreliable clients, assigns reliability-based weights to the retained clients, and constructs a teacher for each type of knowledge. Finally, the corresponding distillation gradients are validated using a supervised gradient computed from private data. Conflicting prediction and boundary gradients are removed, and conflicting relation gradients are suppressed before the final model update. We prove the convergence of the proposed method by showing stable local optimization for honest clients under Byzantine distillation. Particularly, we show that our method ensures a bounded B
    
[^216]: 通过MIMO模型扩展实现具有混合一致性的迭代音频分离

    Iterative Audio Separation with Mixture Consistency via MIMO Model Extension

    [https://arxiv.org/abs/2609.07226](https://arxiv.org/abs/2609.07226)

    本文通过将源分离模型扩展为多输入多输出（MIMO）配置，提出了一种兼具混合一致性与迭代预测能力的通用音频分离框架。

    

    本文提出了一种通用框架，通过将源分离模型扩展为多输入多输出（MIMO）配置，实现稳定且有效的、具有混合一致性的迭代音频分离。在音频分离领域，混合一致性是许多需要目标源精确相位和音色信息的应用所必备的重要特性。虽然扩散模型等迭代方法在语音增强或用户引导的目标源分离任务中能取得感知上更优的结果，但由于混合一致的音频分离通常被视为具有唯一解的回归问题，大多数现有方法侧重于通过架构改进实现单步分离，采用单输入单输出（SISO）或单输入多输出（SIMO）配置。通过将这些架构扩展为MIMO配置，我们在不损害混合一致性的前提下引入了迭代预测能力。

    arXiv:2609.07226v1 Announce Type: cross  Abstract: This paper proposes a general framework for stable and effective iterative audio separation with mixture consistency by extending source separation models to a multi-input multi-output (MIMO) configuration. In the field of audio separation, mixture consistency is an essential property for many applications that require accurate phase and timbral information of target sources. While iterative approaches such as diffusion models achieve perceptually superior results in speech enhancement or user-guided target source separation tasks, most existing methods focus on single-step separation with a single-input single-output (SISO) or single-input multi-output (SIMO) configuration through architectural improvements, since mixture-consistent audio separation is generally regarded as a regression problem that admits a unique solution. By extending these architectures to a MIMO configuration, we introduce iterative prediction without compromisin
    
[^217]: REFINE：基于闭环转录的轨迹表示学习（扩展版）

    REFINE: Trajectory Representation Learning via Closed-Loop Transcription -- Extended Version

    [https://arxiv.org/abs/2609.07206](https://arxiv.org/abs/2609.07206)

    REFINE借鉴反馈控制理论，将路网感知的生成式重建与反馈驱动的对比学习进行闭环耦合，实现了无需人工数据增强的高质量轨迹表示学习，并具有理论收敛保证。

    

    轨迹表示学习是众多轨迹分析任务的基础；然而，现有的大多数自监督方法，无论是判别式还是生成式，都采用开环范式，依赖于固定的数据增强或无反馈的随机掩码，这限制了它们的泛化和扩展能力。我们提出了REFINE，一个简单而有效的基于闭环转录精炼的轨迹数据表示学习框架。借鉴反馈控制理论，REFINE将路网感知的生成式重建与反馈驱动的对比学习紧密耦合，使模型无需人工设计的增强视图即可捕获细粒度的局部移动语义和全局时空依赖关系。我们进一步提供了控制理论分析，为所提出的闭环优化建立了收敛性保证。在四个真实世界数据集上进行了大量实验（摘要在此处截断）。

    arXiv:2609.07206v1 Announce Type: cross  Abstract: Trajectory representation learning underpins a wide range of trajectory analytics tasks; however, most existing self-supervised approaches, whether discriminative or generative, adopt an open-loop paradigm, relying on fixed data augmentations or random masking without feedback, which limits their ability to generalize and scale.   We propose REFINE, a simple yet effective Representation lEarning Framework vIa closed-loop traNscription rEfinement for trajectory data. Drawing upon feedback control theory, REFINE tightly couples road-network-aware generative reconstruction with feedback-driven contrastive learning, enabling the model to capture fine-grained local movement semantics and global spatio-temporal dependencies without manually designed augmentation views. We further provide a control-theoretic analysis that establishes convergence guarantees for the proposed closed-loop optimization.   Extensive experiments on four real-world d
    
[^218]: 测试协议设置对跨 Trust-Hub 家族基于特征的硬件木马检测的影响

    Protocol effects on feature-based hardware-Trojan detection across Trust-Hub families

    [https://arxiv.org/abs/2609.07199](https://arxiv.org/abs/2609.07199)

    该论文发现基于特征的硬件木马检测性能严重依赖测试集划分协议——当测试门电路来自训练中已见过的宿主电路时性能被高估，而按宿主家族留出时 F1/AP 会从约 0.91/0.98 骤降至约 0.46/0.58。

    

    Trust-Hub 复用宿主电路：多个文件之间的主要区别仅在于插入的木马。当同一家族兄弟变体的门电路同时进入训练集和测试集时，检测器可以从已经见过的宿主逻辑中获益。我们测量的是这种效应，而不是提出又一种分类器。语料库包含来自 16 个网表的 49,124 个门电路，分为五个宿主家族。我们保持解析器、36 个门级特征、类别权重、模型设置、阈值和家族级聚合方式不变，仅改变一个选择：测试集边界。三种设置分别从合并语料库中抽取测试门电路、留出一个完整网表、或留出一个宿主的所有变体。这一选择至关重要：随机森林在合并门电路下 F1/AP 为 0.914/0.978，留出一个完整网表时降至 0.636/0.851，留出一个宿主家族时仅为 0.460/0.577；在同样对比下，XGBoost 从 0.946/0.976 下降到 0.464/0.544；逻辑回归损失了 AP，尽管其固定阈值……

    arXiv:2609.07199v1 Announce Type: cross  Abstract: Trust-Hub reuses host circuits: several files differ mainly in the inserted Trojan. When gates from sibling variants enter both training and test folds, a detector can benefit from host logic it has already seen. We measure that effect instead of proposing another classifier. The corpus contains 49,124 gates from 16 netlists grouped into five host families. We left the parser, 36 gate features, class weighting, model settings, threshold, and family-level aggregation unchanged and altered one choice: the test boundary. The three settings draw test gates from the pooled corpus, withhold a complete netlist, or withhold every variant of one host. The choice matters. Random forest records F1/AP of 0.914/0.978 with pooled gates, 0.636/0.851 with one netlist held out, and 0.460/0.577 with a host family held out. XGBoost falls from 0.946/0.976 to 0.464/0.544 across the same comparison. Logistic regression loses AP, although its fixed-threshold
    
[^219]: FedRAW：在异步联邦学习中保留稀有标签的影响力

    FedRAW: Preserving Rare-Label Influence in Asynchronous Federated Learning

    [https://arxiv.org/abs/2609.07192](https://arxiv.org/abs/2609.07192)

    提出FedRAW，一种完全服务器端的异步联邦学习聚合方法，通过同时解决参与偏差和聚合偏差，防止持有稀有标签的客户端对全局模型的贡献被静默削弱。

    

    异步联邦学习通过在客户端更新到达时从服务器端缓冲区更新全局模型，而无需等待所有被选中的客户端完成，从而提高了可扩展性。虽然高效，但这种到达驱动的聚合方式在异构参与下可能会悄无声息地扭曲表示学习。我们识别出“静默稀有性失效”这一隐藏的失效模式：持有稀有标签的客户端对全局模型的贡献过于微弱，即使模型的整体准确率看起来基本未受影响。这种失效源于两个耦合效应：稀有标签客户端可能因速度较慢或可用性较低而更少地提交更新，造成参与偏差；并且一旦它们的更新进入缓冲区，标准的异步聚合不会赋予它们任何补偿性的影响力，造成聚合偏差。我们提出了FedRAW，一种完全在服务器端运行的聚合方法，它在不改变本地（原文在此处截断）……

    arXiv:2609.07192v1 Announce Type: cross  Abstract: Asynchronous federated learning improves scalability by updating the global model from a server-side buffer of client updates as they arrive, rather than waiting for all selected clients to finish. While efficient, this arrival-driven aggregation can silently distort representation learning under heterogeneous participation. We identify silent rarity failure, a hidden failure mode in which clients holding rare labels contribute too weakly to the global model even though its overall accuracy appears largely unaffected. This failure arises from two coupled effects: rare-label clients may submit updates less frequently when they are slower or less available, creating participation bias; and once their updates enter the buffer, standard asynchronous aggregation assigns them no compensating influence, creating aggregation bias. We propose FedRAW, a fully server-side aggregation method that preserves rare-label influence without changing loc
    
[^220]: 基于均值多面体的离散最大熵分布的张量网络表示

    Tensor network representations of discrete maximum entropy distributions via mean polytopes

    [https://arxiv.org/abs/2609.07184](https://arxiv.org/abs/2609.07184)

    本文引入计算-激活网络这一新型张量网络架构，利用期望向量凸多面体的几何性质，为期望约束下的离散最大熵分布提供了统一的张量网络表示，并提出将张量网络秩作为多面体面的复杂度度量。

    

    我们提出了在期望约束下离散最大熵分布的张量网络表示。为此，我们引入了计算-激活网络，这是一种涵盖指数族的张量网络架构。通过利用可实现期望向量的凸多面体的几何性质，我们在同一架构中表示任意最大熵分布。我们利用了该多面体的真面对应于指数族的边界闭包这一事实，从而限制了分布的支撑集。随后，我们在CompActNet架构内推导出支撑集的显式表示。所提出的框架将张量网络秩作为多面体面的复杂度度量。最后，关于布尔统计的案例研究将0/1-多面体的几何结构与命题公式直接联系起来。

    arXiv:2609.07184v1 Announce Type: cross  Abstract: We present tensor network representations for discrete maximum entropy distributions under expectation constraints. To this end, we introduce Computation-Activation Networks (CompActNets), a tensor network architecture that subsumes exponential families. By leveraging the geometry of the convex polytope of realizable expectation vectors, we represent any maximum entropy distribution in the same architecture. We exploit the fact that proper faces of this polytope correspond to the boundary closure of exponential families, which restricts the distribution's support. We then derive explicit representations for the support within the CompActNet architecture. The proposed framework suggests tensor network ranks as complexity measures for faces. Finally, a case study on Boolean statistics links the geometry of 0/1-polytopes directly to propositional formulas.
    
[^221]: CircuitLens：将推理电路作为可验证奖励强化学习的数据选择信号

    CircuitLens: Reasoning Circuits as Data Selection Signals for Reinforcement Learning with Verifiable Rewards

    [https://arxiv.org/abs/2609.07183](https://arxiv.org/abs/2609.07183)

    本文提出基于46个推理敏感注意力头的推理电路得分（CRS）作为RLVR的数据选择信号，并发现反直觉的结果——推理电路激活程度最低的数据反而能带来最佳的训练效果。

    

    可验证奖励强化学习（RLVR）对模型训练所使用的问题十分敏感，然而现有的数据选择标准——难度过滤、人工筛选、奖励轨迹评分——都将数据价值视为问题本身的内在属性，而与将要从中学习的模型无关。我们提出了推理电路得分（CRS），这是一种源自通过对比消融方法识别出的46个推理敏感注意力头的选择信号，可在冻结的基础模型上通过单次前向传播计算得出，无需奖励标签或推理rollout。CRS的结果与直觉假设——更强的推理电路激活会产生更好的训练数据——相反：在Qwen2.5-Math-7B上，激活程度最低的十分位数据在三个中等难度基准上优于随机选择（GSM8K +2.0个百分点，OlympiadBench +1.6个百分点，Minerva +2.9个百分点），而激活程度最高的十分位数据增益较小，且与中间十分位数据无显著差异。

    arXiv:2609.07183v1 Announce Type: cross  Abstract: Reinforcement learning with verifiable rewards (RLVR) is sensitive to which problems a model trains on, yet existing selection criteria--difficulty filtering, hand-curation, reward-trajectory scoring--assess data value as an intrinsic property of problems, independent of the model that will learn from them. We introduce Circuit Reasoning Score (CRS), a selection signal derived from 46 reasoning-sensitive attention heads identified via contrastive ablation, computed in a single forward pass on the frozen base model without reward labels or rollouts. CRS runs against the intuitive hypothesis that stronger reasoning-circuit engagement produces better training data: on Qwen2.5-Math-7B, the lowest-engagement decile improves over random selection on three medium-difficulty benchmarks (GSM8K +2.0 pp, OlympiadBench +1.6 pp, Minerva +2.9 pp), while the highest-engagement decile gains less and is indistinguishable from the middle decile. The adv
    
[^222]: 监督缺口：LLM安全监控器遗漏了什么，以及为什么这不是能力问题

    The Oversight Gap: What LLM Safety Monitors Miss, and Why It Is Not Capability

    [https://arxiv.org/abs/2609.07162](https://arxiv.org/abs/2609.07162)

    该论文提出了“监督缺口”的概念，给出单轨迹监控器检测2-安全性超性质的紧致理论界限，并通过实验证明LLM安全监控器的失效主要源于缺乏明确的检测规范而非能力不足。

    

    安全监控器被要求认证的若干性质，包括跨租户非干扰、沙袋行为和评估意识，属于2-安全性超性质，只能通过两次执行才能见证。标准结论是一个二元的不可判定性：单条轨迹无法判定它们。我们用度量取代了这种二元判定。一个紧致界将任何单轨迹监控器的平衡准确率限定在 $\tfrac12+\tfrac12\,TV(P_0,P_1)$，从而将不可判定性转化为分级的可检测性前沿，并由此定义了“监督缺口”：即监控器低于该前沿的差距。在一个具有闭式全变差的泄漏族上，九个LLM监控器在 $TV=0$ 时表现最优，但随着 $TV$ 增大只能捕获很少的信号；在 $TV=1$ 时，一段20行的成员检查代码能达到100%的准确率，而这些监控器平均只得60.9%。这一差距主要并非能力问题：明确指出需要检查什么能够弥合其中61%的差距，同时使 $TV=0$ 的对照保持在随机水平。同样的二分法贯穿于一个 $2\times2$ 因子实验：一个想象的...（原文摘要在此处截断）

    arXiv:2609.07162v1 Announce Type: new  Abstract: Several properties safety monitors are asked to certify, among them cross-tenant noninterference, sandbagging and evaluation awareness, are 2-safety hyperproperties, witnessed only by two executions. The standard consequence is a binary impossibility: one trace cannot decide them. We replace the binary with a measurement. A tight bound puts the balanced accuracy of any single-trace monitor at $\tfrac12+\tfrac12\,TV(P_0,P_1)$, turning undecidability into a graded detectability frontier and defining an oversight gap: a monitor's shortfall below it. On a leak family with closed-form $TV$, nine LLM monitors are optimal at $TV=0$ but capture little signal as $TV$ grows; at $TV=1$, where a 20-line membership check scores $100\%$, they average $60.9\%$. That shortfall is mostly not capability: naming what to check closes $61\%$ of it while leaving the $TV=0$ control at chance. The same split runs through a $2{\times}2$ factorial: an imagined se
    
[^223]: 注意近似问题：面向视觉Transformer的Fisher加权SVD压缩

    Mind the Approximation: Fisher-Weighted SVD Compression for ViTs

    [https://arxiv.org/abs/2609.07155](https://arxiv.org/abs/2609.07155)

    本文发现提高Fisher近似保真度并不能有效预测ViT压缩后的精度，据此提出结构化Fisher近似方法FACTS及快速的约束秩搜索CoRS，在固定FLOP约束下实现视觉Transformer的高效Fisher加权SVD压缩。

    

    模型压缩是缓解日益庞大的机器学习模型部署挑战的关键。在该研究领域中，基于奇异值分解（SVD）的压缩在计算效率和模型精度之间提供了出色的权衡。其中，Fisher加权SVD尤其能够提供有原则的、损失感知的压缩。然而，我们发现提高压缩中使用的Fisher近似的保真度，对视觉Transformer（ViT）压缩后的精度预测效果很差。受这一观察的启发，我们提出了FACTS，一种专门为基于Fisher加权SVD压缩ViT而设计的结构化Fisher近似方法，它在保持token内激活-梯度依赖性的同时强制执行token局部聚合。此外，我们引入了一种快速的约束秩搜索，在满足固定浮点运算量（FLOP）约束的前提下优化逐层秩分配。大量实验...

    arXiv:2609.07155v1 Announce Type: cross  Abstract: Model compression is key to mitigate deployment challenges of ever growing machine learning models. In this area of research, singular value decomposition (SVD)-based compression offers a compelling trade-off between computational efficiency and model accuracy. Fisher-weighted SVD in particular provides principled, loss-aware compression. However, we find that improving the fidelity of Fisher approximation used in the compression is poorly predictive of post-compression accuracy for Vision Transformers (ViTs). Motivated by this observation, we propose FACTS, a structured Fisher Approximation tailored to Compressing ViTs with Fisher-weighted SVD, which enforces token-local aggregation while preserving within-token activation-gradient dependence. Additionally, we introduce a fast Constrained Rank Search (CoRS), that optimizes layer-wise rank allocation while adhering to a fixed floating point operation (FLOP) constraint. Extensive experi
    
[^224]: Stable-MM-R1：通过熵引导分层锚定多模态推理动态

    Stable-MM-R1: Anchoring Multimodal Reasoning Dynamics via Entropy-Guided Stratification

    [https://arxiv.org/abs/2609.07148](https://arxiv.org/abs/2609.07148)

    该论文提出 Stable-MM-R1 框架，通过潜能感知查询挖掘（PAQM）动态筛选高能力激发潜力的样本，并结合基于路径熵与结果奖励分层的混合分层重放（HSR）机制构建高对比度优化组，从而解决多模态强化学习训练中的熵坍缩与不稳定问题。

    

    虽然强化学习（RL）能有效激励大型语言模型的推理能力，但当前的训练流程受到训练不稳定性和快速熵坍缩的阻碍。这些限制通常源于标准采样过程中的"Rollout 静默"（Rollout Silencing）和低质量的梯度信号。在这项工作中，我们提出了一个稳健的、以数据为中心的框架来稳定强化学习训练。我们首先引入了潜能感知查询挖掘，该方法动态过滤数据，以聚焦于"蒸馏区"（Distillation Zone）——即具有高能力激发潜力的样本。此外，我们提出了混合分层重放，这是一种新颖的机制，通过基于路径熵（Path Entropy，一种 rollout 级别的置信度代理）和结果奖励对 rollout 进行分层来重构批次。在每个优化步骤中，HSR 复用当前策略的"稳定性锚点"（Stability Anchors）和"困难负样本"（Hard Negatives）来构建高对比度优化组，然后在下一步优化前清空其缓冲区。

    arXiv:2609.07148v1 Announce Type: new  Abstract: While Reinforcement Learning (RL) effectively incentivizes reasoning in Large Language Models, current pipelines are hindered by training instability and rapid entropy collapse. These limitations often stem from "Rollout Silencing" and low-quality gradient signals in standard sampling procedures. In this work, we propose a robust, data-centric framework to stabilize RL training. We first introduce Potential-Aware Query Mining (PAQM), which filters data dynamically to focus on the "Distillation Zone"---samples with high potential for capability elicitation. Furthermore, we present Hybrid Stratified Replay (HSR), a novel mechanism that restructures batches by stratifying rollouts based on Path Entropy, a rollout-level confidence proxy, and outcome reward. Within each optimization step, HSR reuses current-policy "Stability Anchors" and "Hard Negatives" to construct high-contrast optimization groups, then clears its buffers before the next s
    
[^225]: 联邦学习中的细粒度分布式后门攻击

    Fine-grained Distributed Backdoor Attacks in Federated Learning

    [https://arxiv.org/abs/2609.07147](https://arxiv.org/abs/2609.07147)

    本文提出细粒度分布式后门攻击框架FDBA，通过基于Canny边缘特征的动态触发器生成和RGB通道分解等嵌入向量优化技术，在联邦学习中以更少的中毒样本实现更隐蔽的分布式后门攻击。

    

    联邦学习作为一种保护隐私的分布式机器学习范式，面临着后门攻击的重大威胁。与集中式攻击相比，分布式后门攻击的危害更大，但需要更多的中毒样本来弥补因触发器分解而损失的攻击强度。此外，固定的触发器模式容易被鲁棒聚合算法检测到，增加了攻击暴露的风险。为应对这些挑战，我们提出了一种细粒度分布式后门攻击框架（FDBA）。该框架利用动态触发器生成和嵌入向量优化技术，以更少的中毒样本实施攻击。首先，我们设计了一种基于图像边缘结构的动态触发器生成方法，使用Canny算法提取边缘特征，并注入拉普拉斯噪声。随后，采用RGB通道分解技术对分布式触发器进行隐蔽适配，以降低被检测……

    arXiv:2609.07147v1 Announce Type: new  Abstract: Federated learning, as a privacy-preserving distributed machine learning paradigm, faces significant threats from backdoor attacks. Compared to centralized attacks, distributed backdoor attacks are more harmful but require more poisoned samples to compensate for the loss of trigger strength due to decomposition. Fixed trigger patterns are also easily detected by robust aggregation algorithms, increasing the risk of attack exposure. To address these challenges, we propose a fine-grained distributed backdoor attack framework (FDBA). This framework uses dynamic trigger generation and embedding vector optimization to perform attacks with fewer poisoned samples. First, we design a dynamic trigger generation method based on image edge structures using the Canny algorithm to extract edge features, which are then injected with Laplacian noise. RGB channel decomposition is applied for covert adaptation of the distributed trigger, reducing detecti
    
[^226]: 高度自动驾驶车辆中乘客危险感知的脑电驱动解码框架

    EEG-Driven Decoding Framework for Passenger Hazard Perception in Highly Automated Vehicles

    [https://arxiv.org/abs/2609.07128](https://arxiv.org/abs/2609.07128)

    该论文提出了一种基于脑电图的脑机接口框架，通过乘客认知模型、风险感知序列标注和集成了3D-CRNN的乘客脑电解码策略，以非侵入方式解码自动驾驶车辆乘客的神经响应，在风险预测中达到95.3%的平衡准确率，并显著提升了危险识别性能。

    

    可靠的风险评估仍然是自动驾驶汽车面临的核心挑战。尽管自动化技术不断进步，但乘客认知提供了一种非侵入式的辅助信号，能够在无需人类主动干预的情况下同时提升客观安全性与感知安全性。我们提出了一种基于脑电图（EEG）的脑机接口（BCI），用于解码乘客在风险预测（RP）和危险识别（DI）方面的神经响应，并明确将人类建模为乘客以匹配真实世界的自动驾驶汽车使用场景。为实现这一目标，我们提出了乘客认知模型（PCM）、风险感知序列标注（RSL）和乘客脑电解码策略（PEDS），该策略集成了三维卷积循环神经网络（3D-CRNN）模型进行联合脑电解码。实验结果表明，3D-CRNN在风险预测中达到了95.3%±2.7%的平衡准确率（BA），并将单被试危险识别的准确率从80.9%±3.9%提升至85.0%±3.2%

    arXiv:2609.07128v1 Announce Type: new  Abstract: Reliable risk assessment remains a central challenge for Autonomous Vehicles (AVs). Despite advances in automation, passenger cognition provides a non-intrusive auxiliary signal that improves both objective and perceived safety without requiring active human intervention. We introduce an Electroencephalogram (EEG)-based Brain-Computer Interface (BCI) that decodes passenger neural responses for both Risk Prediction (RP) and Danger Identification (DI), explicitly modeling humans as passengers to match real-world AV use. To achieve this, we propose the Passenger Cognitive Model (PCM), Risk-aware Sequential Labeling (RSL), and the Passenger EEG Decoding Strategy (PEDS), which integrates a 3D Convolutional Recurrent Neural Network (3D-CRNN) model for joint EEG decoding. Experimental results show that 3D-CRNN achieves a Balanced Accuracy (BA) of $95.3\% \pm 2.7\%$ in RP and improves single-subject DI from $80.9\% \pm 3.9\%$ to $85.0\% \pm 3.2\
    
[^227]: 超越任务成功：感知退化下世界模型规划的阶段性可靠性

    Beyond Task Success: Stage-Wise Reliability of World Model Planning under Sensing Degradation

    [https://arxiv.org/abs/2609.07126](https://arxiv.org/abs/2609.07126)

    本文通过对世界模型规划器施加10种感知退化并进行跨阶段配对评估，揭示感知扰动的影响在表示、预测、规划偏好和物理结果各阶段间并非均匀传递，因此仅凭任务成功无法可靠评估世界模型规划的抗扰性能。

    

    在世界模型规划中，感知输入需要经过编码器和预测器才能影响规划器的决策，因此仅凭最终任务成功无法揭示感知扰动在流水线中的何处衰减或持续。我们对一个世界模型规划器施加10种视觉和时间维度的感知退化，并通过对相同的50个任务进行配对评估，追踪这些退化在表示、未来预测、规划器偏好和物理结果等各阶段的影响。研究发现，退化的相对影响在各阶段之间并未保持一致：较大的表示偏移可能在下游衰减，而较小的初始偏移却可能持续影响到最终结果，且内部响应的排序与物理结果的排序并不直接对应。时间维度的退化也表现出独特的模式：即使观察历史的整体变化相似，响应也会因受损信息的位置以及规划器实际接触到的信息不同而产生显著差异。这种非均[摘要在此处截断]

    arXiv:2609.07126v1 Announce Type: cross  Abstract: In world model planning, sensing inputs pass through an encoder and predictor before affecting planner decisions, so final task success alone cannot reveal where sensing disturbances attenuate or persist in the pipeline. We apply 10 visual and temporal sensing degradations to a world model planner and track their effects across representation, future prediction, planner preference, and physical outcome using paired evaluation on the same 50 tasks. The relative impact of degradations was not preserved across stages: large representation shifts could attenuate downstream, while smaller initial shifts could persist to the outcome, and internal-response ordering did not directly match physical-outcome ordering. Temporal degradations also showed distinct patterns: even with similar overall changes in observation history, responses differed substantially with the location of corrupted information and the planner's actual exposure. This non-u
    
[^228]: 面向大规模长上下文强化学习后训练中投机解码的在线草稿模型协同训练

    Online Draft Co-Training for Speculative Decoding in Large-Scale, Long-Context RL Post-Training

    [https://arxiv.org/abs/2609.07108](https://arxiv.org/abs/2609.07108)

    该论文提出了一个端到端系统，通过在上下文并行中融合分支注意力与主序列注意力、并引入TapChannel独立通道跨流水线阶段传输目标特征，实现了大规模长上下文强化学习后训练中投机解码草稿模型的在线协同训练，从而显著加速rollout生成。

    

    投机解码可以加速rollout生成，而rollout生成在强化学习（RL）后训练的成本中占据主导地位。在线协同训练可以进一步提高草稿模型的准确率，从而获得更大的加速效果。然而，将该方法扩展到长上下文大模型的协同训练面临两个障碍：（1）标准的因果上下文并行（CP）实现不支持分支注意力；（2）目标模型的特征跨越流水线并行（PP）的各个阶段。我们通过一个面向大规模在线草稿协同训练的端到端系统来解决这两个问题。对于CP，我们将rank局部的分支注意力与因果主序列注意力相融合，扩展了打包且负载均衡的锯齿形环形注意力。对于PP，TapChannel通过独立路径跨阶段传输中间目标特征，且不影响流水线调度。实验表明，协同训练的草稿模型能紧密跟随策略基线，同时带来显著的rollout加速。

    arXiv:2609.07108v1 Announce Type: new  Abstract: Speculative decoding accelerates rollout generation, which dominates the cost of reinforcement learning (RL) post-training. Online co-training can further increase the draft's accuracy, yielding greater speedups. However, scaling this approach to co-training on large models with long contexts poses two obstacles: (1) branch attention is unsupported by standard causal context-parallel (CP) implementations, and (2) target features span across pipeline-parallel (PP) stages. We address both with an end-to-end system for large-scale online draft co-training. For CP, we extend packed, load-balanced zigzag ring attention by merging rank-local branch attention with causal main-sequence attention. For PP, TapChannel transports intermediate target features across stages via a separate path, leaving the pipeline schedule unaffected. Experiments demonstrate that co-trained drafts closely track the policy baseline while delivering substantial rollout
    
[^229]: 用于信用卡欺诈检测的时序异构图Transformer

    Temporal Heterogeneous Graph Transformer for Credit Card Fraud Detection

    [https://arxiv.org/abs/2609.07100](https://arxiv.org/abs/2609.07100)

    提出THGT-FD模型，将每笔交易表示为一个交易令牌与六种关系令牌并融合Time2Vec时间编码，利用Transformer学习交易内令牌间交互来进行信用卡欺诈检测，其性能接近但尚未超越梯度提升基线。

    

    信用卡欺诈检测通常依赖于表格特征，而重复出现的属性也能提供有用的关系信号。本文提出了THGT-FD，一种用于欺诈检测的时序异构图Transformer。每笔交易由一个交易令牌和六种类型的关系令牌表示，并将Time2Vec编码融入交易表示中。Transformer学习每笔交易内部这些令牌之间的交互，然后输出欺诈概率。实验在从IEEE-CIS欺诈检测数据集中采样的15万笔交易上进行，并根据TransactionDT按时间顺序划分数据集。在测试集上，THGT-FD取得了0.8536的AUC-ROC、0.4164的平均精度和0.4708的Recall@5%；而采用类加权直方图梯度提升的基线模型达到了0.8722的AUC-ROC。结果表明关系令牌能够提供有用的（关系信号）

    arXiv:2609.07100v1 Announce Type: cross  Abstract: Credit card fraud detection typically relies on tabular features, while repeated attributes can also provide useful relational signals. This paper proposes THGT-FD, a Temporal Heterogeneous Graph Transformer for Fraud Detection. Each transaction is represented using one transaction token and six types of relation tokens and incorporates Time2Vec encoding into the transaction representation. A Transformer learns the interactions among these tokens within each individual transaction and then outputs a fraud probability. Experiments were conducted on 150,000 transactions sampled from the IEEE-CIS Fraud Detection dataset and chronologically partitioned according to TransactionDT. On the test set, THGT-FD achieved an AUC-ROC of 0.8536, an average precision of 0.4164, and a Recall@5% of 0.4708. The class-weighted histogram-based gradient-boosting baseline achieved an AUC-ROC of 0.8722. The results indicate that relation tokens provide useful
    
[^230]: 面向注意力机制的条件化初始化

    Conditioned Initialization for Attention

    [https://arxiv.org/abs/2609.07086](https://arxiv.org/abs/2609.07086)

    提出条件化初始化方法，通过改善注意力层的谱特性来初始化查询、键和值权重，从根本上优化Transformer的训练动态。

    

    Transformer是现代机器学习中的主导架构，驱动着视觉、语言及其他领域的众多应用。其成功的关键在于注意力层，其中查询、键和值矩阵决定了如何捕获标记间的依赖关系。尽管大量研究工作专注于Transformer的扩展和优化，但对于查询、键和值权重的初始化方式却相对鲜有关注。常见的做法依赖于随机初始化或其他替代方案，例如模仿初始化（mimetic initialization，即模仿收敛模型的权重模式）和权重选择（weight selection，即从教师模型迁移权重）。在本文中，我们认为初始化可以引入一种优化偏差，从根本上塑造训练动态。我们提出了条件化初始化，这是一种有原则的方案，通过初始化注意力权重来改善注意力层的谱特性……

    arXiv:2609.07086v1 Announce Type: new  Abstract: Transformers are a dominant architecture in modern machine learning, powering applications across vision, language, and beyond. At the core of their success lies the attention layer, where the query, key, and value matrices determine how token dependencies are captured. While considerable work has focused on scaling and optimizing Transformers, comparatively little attention has been paid to how the weights of the queries, keys and values are initialized. Common practice relies on random initialization or alternatives such as mimetic initialization, which imitates weight patterns from converged models, and weight selection, which transfers weights from a teacher model. In this paper, we argue that initialization can introduce an optimization bias that fundamentally shapes training dynamics. We propose conditioned initialization, a principled scheme that initializes attention weights to improve the spectral properties of the attention lay
    
[^231]: 图形设计中的万物检测：面向自回归检测的元素级奖励

    Detect Anything in Graphic Design: Element-Level Rewards for Autoregressive Detection

    [https://arxiv.org/abs/2609.07072](https://arxiv.org/abs/2609.07072)

    提出 DAD 模型，将图形设计检测建模为按组合顺序解码的组合解构，实现能预测被遮挡完整边界的非模态检测，并提出将 GRPO 扩展至元素级奖励的 EleRPO 方法。

    

    海报、广告和信息图表等图形设计是传递信息和塑造认知的重要媒介。与自然图像不同，它们由具有明确组合顺序的分层元素构成。然而，现有的目标检测模型将这些元素视为无序集合，未能利用组合顺序信息。为解决这一局限性，我们提出了 Detect Anything in Graphic Design (DAD)，一个将图形设计检测建模为组合解构的模型。它按照组合顺序解码元素，利用较低层的元素来更好地检测较高层的元素。DAD 的关键特性是非模态检测，即预测每个元素的完整边界框，包括被其上层元素遮挡的区域。基于这一框架，我们提出了元素相对策略优化，将 GRPO 从序列级监督扩展到元素级优化……

    arXiv:2609.07072v1 Announce Type: cross  Abstract: Graphic designs, such as posters, advertisements, and infographics, are an important medium for communicating information and shaping understanding. Unlike natural images, they consist of layered elements with explicit compositional order. However, existing object detection models treat these elements as an unordered set, leaving compositional order unexploited. To address this limitation, we present Detect Anything in Graphic Design (DAD), a model that formulates graphic design detection as compositional deconstruction. It decodes elements in compositional order, using lower-layer elements to better detect higher-layer ones. The key feature of DAD is amodal detection, which predicts the full bounding box of each element, including regions occluded by elements placed above it. Building on this formulation, we propose Element Relative Policy Optimization (EleRPO), which extends GRPO from sequence-level supervision to element-level optim
    
[^232]: 信任但要验证：抗投毒的本地私有图学习协议

    Trust-But-Verify: Poisoning-Resilient Locally Private Graph Learning Protocols

    [https://arxiv.org/abs/2609.07063](https://arxiv.org/abs/2609.07063)

    本文提出VERITAS，一种基于“信任但要验证”范式的抗投毒本地私有图学习协议，通过分级同伴信任验证机制有效抵御数据投毒攻击，在保护隐私的同时维持图学习效用。

    

    基于本地差分隐私（LDP）构建的本地私有图学习协议已成为去中心化图学习的重要范式，在隐私保护与学习效用之间实现了平衡。在此类协议下，每个用户在传输前对自身的节点特征和邻接信息进行本地扰动，在原始数据不离开设备的情况下提供正式的隐私保证。然而，其固有的开放参与特性使这些协议极易遭受数据投毒攻击——攻击者通过注入精心构造的恶意节点来破坏邻域聚合并降低下游学习效用。尽管这一威胁十分严重，但针对该场景的有效防御在很大程度上仍待探索。本文提出了VERITAS，一种基于“信任但要验证”范式的抗投毒本地私有图学习协议。通过引入一个编码分级同伴信任等级的验证列表……

    arXiv:2609.07063v1 Announce Type: new  Abstract: Built upon local differential privacy (LDP), locally private graph learning protocols have emerged as an important paradigm for decentralized graph learning, balancing privacy protection and learning utility. Under such protocols, each user locally perturbs their node features and adjacency information before transmission, ensuring formal privacy guarantees without original data leaving the device. However, the inherently open participation nature renders these protocols critically vulnerable to data poisoning attacks, where adversaries inject carefully crafted malicious nodes to corrupt neighborhood aggregation and degrade downstream utility. Despite the severity of this threat, effective defenses in this setting remain largely unexplored. In this paper, we propose VERITAS, a poisoning-resilient locally private graph learning protocol built on a trust-but-verify paradigm. By introducing a verification list encoding graded peer trust lev
    
[^233]: PhysSAE：基于稀疏自编码器的机制可解释性研究

    PhysSAE: Mechanistic Interpretability with Sparse Autoencoders

    [https://arxiv.org/abs/2609.07061](https://arxiv.org/abs/2609.07061)

    提出了PhysSAE框架，通过在PINN倒数第二层激活上训练稀疏自编码器并直接在隐藏状态上做因果干预，发现了与物理可观测量高度对齐且因果足迹更加空间集中的可解释物理特征。

    

    物理信息神经网络（PINNs）将偏微分方程（PDE）残差嵌入到神经网络训练中，但其内部表示仍然是不透明的：我们不知道其隐藏层编码了哪些物理特征，也不清楚这些特征是否具有局部化的因果作用。我们提出了PhysSAE，这是一个机制可解释性框架，它在PINN倒数第二层的激活上训练过完备稀疏自编码器（SAEs），并通过直接对原始冻结隐藏状态进行因果干预来评估字典原子：h_cf = h - α z_k d_k，从而完全绕过SAE解码器。在六个PDE家族上，每个家族使用3个PINN种子和3个SAE种子——我们证明：(i) 我们发现的SAE原子与独立定义的物理可观测量对齐（最大Pearson |r|=0.951，始终远大于置换零假设）；(ii) 顶部对齐原子消融所产生的因果足迹在空间集中度上比PCA或ICA干预高1.2–4.2倍。

    arXiv:2609.07061v1 Announce Type: new  Abstract: Physics-Informed Neural Networks (PINNs) embed PDE residuals into neural network training, but their internal representations remain opaque: it is unknown what physical features their hidden layers encode or whether those features have a localized causal role. We present PhysSAE, a mechanistic interpretability framework that trains overcomplete sparse autoencoders (SAEs) on PINN penultimate-layer activations and evaluates dictionary atoms through direct causal intervention in the original frozen hidden state: $h_{\mathrm{cf}} = h - \alpha z_k d_k$, bypassing the SAE decoder entirely. Across six PDE families, with 3 PINN seeds and 3 SAE seeds each---we show that (i) Our discovered SAE atoms align with independently-defined physical observables (max Pearson $|r|=0.951$, always $\gg$ permutation null), (ii) the causal footprint of top-aligned atom ablation is 1.2--4.2$\times$ more spatially concentrated canonical than PCA or ICA interventio
    
[^234]: TrojanWorld：通过想象引导对世界模型智能体植入后门

    TrojanWorld: Backdooring World-Model Agents via Imagination Steering

    [https://arxiv.org/abs/2609.07051](https://arxiv.org/abs/2609.07051)

    该论文提出TrojanWorld——一个针对世界模型智能体的后门攻击框架，通过在场景中放置物理触发物来引导智能体的内部想象，从而在不数字篡改观测流的情况下，隐蔽且持久地诱发攻击者指定的行为。

    

    世界模型日益成为基于模型的强化学习智能体的预测核心，使其能够在行动之前模拟未来动态并对想象轨迹进行推理。其巨大的训练需求使得预训练世界模型在分发和复用方面极具吸引力，从而使下游系统面临模型供应链威胁。后门攻击为利用此类供应链提供了一种有针对性且隐蔽的手段，但其对交互式世界模型智能体的威胁在很大程度上仍未被探索。为填补这一空白，我们提出了TrojanWorld，一个针对世界模型智能体的后门框架，通过引导内部想象来诱发攻击者指定的行为。放置在场景中的物理对象充当触发器，能够在部署时通过智能体自身的观测管线实现激活，而无需对观测流进行数字操控。为实现有效、隐蔽且持久的控制，TrojanWorld……

    arXiv:2609.07051v1 Announce Type: new  Abstract: World models increasingly serve as the predictive core of model-based reinforcement learning agents, enabling them to simulate future dynamics and reason over imagined trajectories before acting. Their substantial training demands make pretrained world models attractive for distribution and reuse, exposing downstream systems to model supply chain threats. Backdoor attacks offer a targeted and stealthy means of exploiting such supply chains, yet their threat to interactive world-model agents remains largely unexplored. To fill this gap, we present TrojanWorld, a backdoor framework for world-model agents that induces attacker-specified behavior by steering internal imagination. A physical object placed in the scene acts as the trigger, enabling deployment-time activation through the agent's native observation pipeline without digitally manipulating the observation stream. To achieve effective, stealthy, and persistent control, TrojanWorld 
    
[^235]: 人工智能与TCAD用于逆向设计与缺陷发现：从简单机器学习到大语言模型

    AI and TCAD for Inverse Design and Defect Discovery: From Simple Machine Learning to LLM

    [https://arxiv.org/abs/2609.07046](https://arxiv.org/abs/2609.07046)

    该论文提出利用TCAD仿真生成精确数据来增强机器学习，通过基于自编码器的模型与噪声工程学习潜在物理规律，在极少领域知识的情况下构建出媲美器件工程师的模型，实现半导体器件的逆向设计与缺陷发现。

    

    人工智能已经彻底改变了各个工程领域，但由于数据有限和维数灾难，其对半导体器件设计和缺陷发现的影响仍然有限。在本文中，我们将讨论我们利用技术计算机辅助设计（TCAD）生成机器学习（ML）所需的精确数据，从而实现仿真增强机器学习的工作。我们证明，凭借最少的领域专业知识，就有可能创建一台在特定任务上表现媲美器件工程师的机器。我们将展示基于自编码器的机器学习模型以及应用于TCAD数据的噪声工程能够有效学习潜在物理规律，并且这些模型可以无缝地应用于实验数据。我们将通过各种实例逐步演示如何构建器件工程师级别的模型，包括仅使用非破坏性电学数据来逆向解析PiN二极管层厚度变化。

    arXiv:2609.07046v1 Announce Type: new  Abstract: AI has revolutionized various engineering domains, but its impact on semiconductor device design and defect discovery is still limited, due to limited data and the curse of dimensionality. In this paper, we will discuss our work on using the Technology Computer-Aided-Design (TCAD) to generate precise data needed for machine learning (ML) to enable simulation-augmented ML. We demonstrate that with minimal domain expertise, it is possible to create a machine that performs as well as a device engineer on a specific task. We will show that auto-encoder-based machine learning models and noise engineering applied to TCAD data are effective at learning latent physics, and that the models can be seamlessly applied to experimental data. We will demonstrate how to build a device-engineer-level model step by step through various examples, including using only non-destructive electrical data to inverse-engineer the PiN diode layer thickness variatio
    
[^236]: 解耦转向向量

    Disentangling Steering Vectors

    [https://arxiv.org/abs/2609.07037](https://arxiv.org/abs/2609.07037)

    该论文提出“转向向量解剖”框架，通过在正负激活差生成的实例级转向向量上训练稀疏自编码器，将复合转向向量中纠缠的多个语义和风格概念解耦分离，从而实现对大语言模型行为更可预测的精准引导。

    

    激活引导已成为一种轻量级的推理时方法，用于控制大语言模型（LLM）的行为。然而，传统用于干预LLM激活的转向向量，例如通过均值差方法得到的向量，往往将多个语义和风格概念纠缠到单一复合方向中，导致不可预测的引导效果。我们的核心目标是将该复合方向解耦为其组成的各个概念。为此，我们提出了转向向量解剖，一个从这些复合方向中显式分离出单个且语义一致特征的框架。具体而言，我们将正激活和负激活进行配对并取其差异，生成一组实例级转向向量，并直接在其上训练一个专用的稀疏自编码器（SAE）。在两个数据集、两个模型和两个干预深度上的定量评估……

    arXiv:2609.07037v1 Announce Type: new  Abstract: Activation steering has emerged as a lightweight, inference-time approach to control the behavior of Large Language Models (LLMs). However, traditional steering vectors used to intervene in LLMs' activations, such as those derived from the difference-in-means method, tend to entangle multiple semantic and stylistic concepts into a single composite direction, leading to unpredictable steering effects. Our core objective is to disentangle this composite direction into its constituent concepts. To this end, we propose Steering Vector Dissection, a framework to explicitly isolate individual and semantically consistent features from these composite directions. Specifically, we pair positive and negative activations and take their differences to generate a set of instance-level steering vectors, and train a dedicated Sparse Autoencoder (SAE) directly on them. Quantitative evaluations across two datasets, two models, and two intervention depths
    
[^237]: Aha-Flow蒸馏：心流标记在LLM推理中至关重要

    Aha-Flow Distillation: Flow Markers Matter in LLM Reasoning

    [https://arxiv.org/abs/2609.07036](https://arxiv.org/abs/2609.07036)

    该论文发现了一种与顿悟时刻互补的“心流时刻”推理模式，并提出Aha-Flow蒸馏方法，通过双模式在线自蒸馏将心流标记作为辅助监督，从而提升大语言模型的推理能力。

    

    我们识别出一种称为“心流时刻”的推理模式，其特征是持续的、过程确认式的言语化表达（例如“我正在做”），这与以修订和回溯为导向的“阿哈时刻”形成对比。我们将它们对应的语言表达分别称为“心流标记”和“阿哈标记”。基于这一观察，我们通过在保留底层推理内容的同时改写原始推理轨迹中的话语标记，构建了Flow-CoT，并将其用作在线策略自蒸馏（OPSD）的辅助监督。我们进一步提出了Aha-Flow蒸馏（AFD），这是OPSD的一种双模式扩展，将不同形式的特权信息与相应的推理指令配对：阿哈分支保留简洁的基于解决方案的监督，而心流分支则在直接且自信的推理指令下引入改写后的Flow-CoT。在推理阶段，模型仅使用标准的反思性（摘要内容在此处不完整）

    arXiv:2609.07036v1 Announce Type: cross  Abstract: We identify the Flow Moment, a reasoning pattern characterized by sustained, process-confirming verbalizations such as I'm doing, in contrast to the revision- and backtracking-oriented Aha Moment. We refer to their corresponding linguistic expressions as Flow Markers and Aha Markers, respectively. Based on this observation, we construct Flow-CoT by rewriting the discourse markers of original reasoning traces while preserving their underlying reasoning content, and use it as auxiliary supervision for on-policy self-distillation (OPSD). We further propose \textbf{Aha-Flow Distillation (AFD)}, a dual-mode extension of OPSD that pairs different forms of privileged information with corresponding reasoning instructions. The Aha branch retains concise solution-based supervision, while the Flow branch introduces rewritten Flow-CoT under a direct and confident reasoning instruction. At inference time, the model uses only the standard reflective
    
[^238]: 精确不变性下的高效学习与对称性发现

    Efficient Learning and Symmetry Discovery under Exact Invariances

    [https://arxiv.org/abs/2609.07031](https://arxiv.org/abs/2609.07031)

    该论文首次提出了统一适用于有限群和无限群的精确群不变性学习多项式时间算法，解决了无限群和未知对称性情形下高效学习的开放性问题。

    

    具有群不变性的学习是许多科学和几何学习问题的核心，但其计算基础仍知之甚少。即使在经典的监督回归设置中，人们也不清楚是否能够高效地计算一个对给定群作用精确不变的回归函数。近期的研究表明，当底层群是有限且已知时，可以在多项式时间内强制实现精确不变性，但无限群和未知对称性的情形仍是悬而未决的问题。在本文中，我们解决了这两个挑战。首先，我们提出了首个适用于有限群和无限群的、具有精确群不变性学习的多项式时间算法。该算法的运行时间是数据维度和样本量的多项式，且与群本身无关，同时能实现强泛化保证。这为不变与等变模型的实证成功提供了计算层面的解释……

    arXiv:2609.07031v1 Announce Type: new  Abstract: Learning with group invariances is central to many scientific and geometric learning problems, yet its computational foundations remain poorly understood. Even for classical supervised regression settings, it has been unclear whether one can efficiently compute a regression function that is exactly invariant to a given group action. Recent work showed that exact invariance can be enforced in polynomial time when the underlying group is finite and known, but left open the cases of infinite groups and unknown symmetries. In this paper, we resolve both challenges. First, we present the first polynomial-time algorithm for learning with exact group invariances that applies uniformly to finite and infinite groups. The runtime is polynomial in the data dimension and sample size, and independent of the group, while achieving strong generalization guarantees. This provides a computational explanation for the empirical success of invariant and equ
    
[^239]: HyperTransfer：理解基础优化器与超球优化器之间的等价性

    HyperTransfer: Understanding the Equivalence between Base Optimizer and Hyperball

    [https://arxiv.org/abs/2609.07017](https://arxiv.org/abs/2609.07017)

    该论文证明了超球优化器与传统基础优化器在尺度不变网络上动力学等价，并据此提出 HyperTransfer 方法，仅需目标基础优化器的初始化和学习率调度即可构建出复现其动力学的超球优化器，无需运行原优化器。

    

    超球优化器通过约束参数范数并仅更新参数方向，为神经网络优化建立了一种独特的范式。尽管这种几何结构看起来与同时更新参数范数和方向的传统基础优化器有着根本的不同，我们证明了对于尺度不变网络，这两种范式在动力学上是等价的。基于这一等价性，我们提出了 HyperTransfer，它仅使用目标基础优化器的初始化和学习率调度，就能构建出一个复现该目标优化器动力学的超球优化器，而无需实际运行目标优化器本身。我们进一步推导了逆映射，并将该框架扩展到非尺度不变网络。实验表明，HyperTransfer 和逆映射所产生的损失轨迹与其目标几乎完全一致，这表明超球动力学主要由其所诱导的有效学习率所支配。

    arXiv:2609.07017v1 Announce Type: new  Abstract: Hyperball optimizers constrain parameter norms and update only their directions, establishing a distinct paradigm for neural network optimization. Although this geometry appears fundamentally different from that of conventional Base Optimizers, which update both parameter norms and directions, we show that the two paradigms are dynamically equivalent for scale-invariant networks. Building on this equivalence, we propose HyperTransfer, which constructs a Hyperball optimizer that reproduces the dynamics of a target Base Optimizer using only its initialization and learning-rate schedule, without running the target optimizer itself. We further derive the inverse mapping and extend the framework to non-scale-invariant networks. Experiments show that both HyperTransfer and the inverse mapping produce loss trajectories nearly identical to those of their targets, suggesting that Hyperball dynamics are governed primarily by the induced effective 
    
[^240]: AstraMoE-SR：面向盲卫星抖动去模糊与超分辨率的轨迹引导扩散模型

    AstraMoE-SR: Trajectory-Guided Diffusion for Blind Satellite Jitter Deblurring and Super-Resolution

    [https://arxiv.org/abs/2609.07012](https://arxiv.org/abs/2609.07012)

    提出AstraMoE-SR框架，将卫星抖动退化重新参数化为推扫几何下的局部曝光轨迹，并利用条件扩散模型估计轨迹分布，实现单图像、无需辅助测量的运动模糊去除与超分辨率联合复原。

    

    推扫式卫星成像同时受限于有限的空间分辨率和平台姿态的不稳定性。平台抖动会产生空间变化的运动模糊，因为每条扫描线都是在不同的瞬时姿态下采集的；同时，透视几何导致相同的扰动会在整个视场内引起不同的像素位移。因此，假设空间不变模糊核的现有盲复原方法，以及依赖辅助观测的卫星抖动校正方法，都无法直接适用。我们提出AstraMoE-SR，一个无需辅助测量即可联合恢复运动模糊和空间分辨率的单图像框架。我们不估计模糊核，而是通过在推扫式几何下将退化过程重新参数化为局部曝光轨迹来推断相机的运动方式。条件扩散模型估计轨迹的分布，缓解了高频细节的过度平滑问题。

    arXiv:2609.07012v1 Announce Type: cross  Abstract: Pushbroom satellite imaging couples limited spatial resolution with platform attitude instability. Platform jitter produces spatially varying motion blur because each scan line is acquired under a different instantaneous attitude, while perspective geometry causes the same perturbation to induce different pixel displacements across the field of view. Existing blind restoration methods that assume a spatially invariant kernel and satellite jitter correction methods that rely on auxiliary observations are therefore not directly applicable. We present AstraMoE-SR, a single-image framework that jointly restores motion blur and spatial resolution without auxiliary measurements. Rather than estimating a blur kernel, we infer how the camera moved by reparameterizing degradation as a local exposure trajectory under pushbroom geometry. A conditional diffusion model estimates the trajectory distribution, mitigating the over-smoothing of high-fre
    
[^241]: NeuCME：通过多专家神经组合学迈向动态多模态持续学习

    NeuCME: Toward Dynamic Multimodal Continual Learning via Neural Combinatorics of Multiple Experts

    [https://arxiv.org/abs/2609.07009](https://arxiv.org/abs/2609.07009)

    提出NeuCME框架，通过多专家神经组合学应对动态多模态持续学习中模态集合随任务变化的挑战，解决时空灾难性遗忘与自适应多模态融合两大核心问题。

    

    多模态持续学习最近在开发具有类人智能的智能体方面展现出巨大潜力，其通过跨多种模态持续学习新任务来实现。然而，现有方法通常假设每个任务的模态集合是预定义且固定的。本文研究了一种更现实的学习设置，称为动态多模态持续学习，其中模态集合可能因任务而异，而非保持固定。这一设置涉及两个主要挑战：（i）时空灾难性遗忘和（ii）自适应多模态融合。为解决这些挑战，我们提出了NeuCME（多专家神经组合学的简称），这是一个新颖的框架，旨在有效地学习和整合具有不同模态集合的任务间的知识。所提出的NeuCME模型包含三个关键组件，即模态组合……

    arXiv:2609.07009v1 Announce Type: new  Abstract: Multimodal continual learning has recently shown great potential for developing agents with human-like intelligence by continuously learning new tasks across multiple modalities. However, existing methods typically assume that the set of modalities per task is predefined and fixed. In this paper, we investigate a more realistic learning setting, referred to as dynamic multimodal continual learning, in which the set of modalities may vary across tasks rather than remaining fixed. This setting involves two primary challenges: (i) spatio-temporal catastrophic forgetting and (ii) adaptive multimodal fusion. To address these challenges, we propose NeuCME (as shorthand for \textbf{Neu}ral \textbf{C}ombinatorics of \textbf{M}ultiple \textbf{E}xperts), a novel framework designed to effectively learn and integrate knowledge across tasks with varying modalities. The proposed NeuCME model comprises three key components, namely modality-combinationa
    
[^242]: LatentMD：对大语言模型生成文本中Markdown边界失败的基准测试

    LatentMD: Benchmarking Markdown Boundary Failures in LLM-Generated Text

    [https://arxiv.org/abs/2609.06993](https://arxiv.org/abs/2609.06993)

    LatentMD是一个将内容正确性与边界正确性分离的基准测试，它揭示了LLM生成的Markdown中边界失败问题普遍存在——38%的内容正确输出实际上边界已损坏。

    

    大语言模型（LLM）生成的Markdown文本日益被渲染器、智能体、代码提取器和结构化下游流水线所使用。然而，现有评估往往将内容质量与格式遵循混为一谈，导致Markdown边界失败问题未被充分测量。我们提出了LatentMD，这是一个用于诊断LLM生成Markdown中CommonMark级别围栏边界失败的基准测试与评估协议。LatentMD将内容正确性与边界正确性分离，能够检测出内容正确但边界损坏的输出。该基准包含4,179个提示词以及一个用于对任意模型输出进行评分的命令行工具（CLI）。在9个大语言模型和约37,600次生成中，我们发现Markdown边界失败非常普遍：38.0%的有效主网格输出内容正确但边界损坏，且在未指定格式的提示词下以及在一个小型人工撰写的验证集中均存在大量的边界损坏。消融实验表明……

    arXiv:2609.06993v1 Announce Type: cross  Abstract: Large language models (LLMs) increasingly generate Markdown that is consumed by renderers, agents, code extractors, and structured downstream pipelines. Yet existing evaluations often conflate content quality with format adherence, leaving Markdown boundary failures under-measured. We introduce LatentMD, a benchmark and evaluation protocol for diagnosing CommonMark-level fence-boundary failures in LLM-generated Markdown. LatentMD separates content correctness from boundary correctness, enabling detection of outputs that are content-correct but boundary-broken. The benchmark contains 4,179 prompts and a CLI for scoring arbitrary model outputs. Across 9 LLMs and roughly 37,600 generations, we find that Markdown boundary failures are widespread: 38.0% of valid main-grid outputs are content-correct but boundary-broken, with substantial boundary breakage under unspecified prompts and in a small human-authored validation set. Ablations show 
    
[^243]: 持续学习机制组合实现长时程记忆

    Continual Learning Mechanisms Compose for Long-Horizon Memorization

    [https://arxiv.org/abs/2609.06986](https://arxiv.org/abs/2609.06986)

    该论文提出长时程记忆设置，发现任何单一持续学习机制都无法在100个连续任务的顺序学习中避免灾难性遗忘，而通过组合解决互补遗忘来源的机制（数据/函数/权重锚点与低秩分配规则）可以实现有效的长期记忆保留。

    

    语言模型可能需要内化随时间不断到达的信息，并在后续多次更新中将其保留。为了研究这一挑战，我们提出了长时程记忆这一设置：模型通过持续监督微调学习100个问答任务，且不保留早期训练样本，推理时也不接收任务标识符。顺序更新会导致灾难性遗忘，而我们评估的任何单一持续学习机制都无法在此时间跨度上保持良好的记忆保留。我们假设，针对互补性遗忘来源的机制在组合使用时会更加有效。我们沿两个设计维度组织这些组合：数据、函数和权重锚点规定了每次更新应保留哪些先验信息，而低秩分配规则则决定了连续更新被保留的位置。为了系统地检验这一假设，我们构建了三个不同的100任务

    arXiv:2609.06986v1 Announce Type: new  Abstract: Language models may need to internalize information that arrives over time and retain it through many subsequent updates. To study this challenge, we introduce long-horizon memorization, a setting in which a model learns 100 query-answer tasks through continual supervised fine-tuning without retaining earlier training examples or receiving task identifiers at inference. Sequential updates cause catastrophic forgetting, and no single continual learning mechanism we evaluate maintains strong retention at this horizon. We hypothesize that mechanisms addressing complementary sources of forgetting will be more effective when composed. We organize these compositions along two design dimensions. Data, function, and weight anchors specify what prior information each update should preserve, while low-rank allocation rules determine where successive updates are retained. To test this hypothesis systematically, we construct three distinct 100-task 
    
[^244]: AF-Mamba：用于房颤发作早期预测的高效长期信号建模

    AF-Mamba: Efficient Long-Term Signal Modeling for Early Prediction of Atrial Fibrillation Onset

    [https://arxiv.org/abs/2609.06984](https://arxiv.org/abs/2609.06984)

    提出了一种融合时序卷积网络与Mamba状态空间模型的混合架构AF-Mamba，能够基于一小时长期RR间期信号高效预测房颤发作，突破了Transformer二次方扩展的限制。

    

    心房颤动（AF）是最常见的心律失常，与中风和心力衰竭的风险增加相关。可穿戴和便携式心电（ECG）监测设备的日益普及，使得在临床环境之外对心律进行持续评估成为可能。在房颤发作前进行预测可以为及时的临床评估提供额外的提前时间，并有可能改善存在房颤相关并发症风险患者的管理。本研究专注于利用长期RR间期（RRIs）提前一小时预测房颤发作。为应对这一挑战，我们提出了一种深度学习架构，该架构将用于局部特征编码的时序卷积网络（TCN）与Mamba（一种能够进行长程序列建模的选择性状态空间模型）相结合。这种TCN-Mamba混合设计使得在一小时输入窗口上进行高效的训练和推理成为可能，克服了Transformer的二次方扩展限制以及循环模型的局限性

    arXiv:2609.06984v1 Announce Type: new  Abstract: Atrial fibrillation (AF) is the most common cardiac arrhythmia and is associated with increased risks of stroke and heart failure. The growing availability of wearable and portable ECG monitoring enables continuous assessment of cardiac rhythm outside clinical settings. Predicting AF before its onset could provide additional lead time for timely clinical assessment and potentially improve the management of patients at risk of AF-related complications. This study focuses on predicting AF onset one hour in advance using long-term RR intervals (RRIs). To address this challenge, we propose a deep learning architecture that integrates temporal convolutional networks (TCNs) for local features encoding with Mamba, a selective state-space model capable of long-range sequence modeling. This hybrid TCN-Mamba design enables efficient training and inference on one-hour input windows, overcoming limitations of Transformers' quadratic scaling and recu
    
[^245]: 面向无乘子共识的二次广义纳什均衡问题的全分布式原始-对偶动力学的输入到状态稳定性框架

    Input-to-State Stability Framework for Fully Distributed Primal-Dual Dynamics for Quadratic GNEPs Without Multiplier Consensus

    [https://arxiv.org/abs/2609.06983](https://arxiv.org/abs/2609.06983)

    本文提出了一种无需乘子共识的全分布式原始-对偶动力学方法来求解二次广义纳什均衡问题，并基于输入到状态稳定性（ISS）框架证明了算法的收敛性，减少了通信需求并提升了隐私性。

    

    广义纳什均衡问题（GNEPs）常见于需要分布式算法的多智能体工程应用中。与传统方法通过对乘子强制达成共识不同，本方法无需共享乘子，从而减少了通信量并提升了隐私保护。因此，不同的初始化可以收敛到不同的广义纳什均衡（GNE），包括非变分均衡。我们利用输入到状态稳定性（ISS）框架，在充分条件下建立了算法的收敛性。

    arXiv:2609.06983v1 Announce Type: cross  Abstract: Generalized Nash Equilibrium Problems (GNEPs) often arise in multi-agent engineering applications that require distributed algorithms. Unlike traditional approaches that enforce consensus on multipliers, our method removes the need to share multipliers, reducing communication and improving privacy. As a result, different initializations can lead to different GNEs, including non-variational ones. We establish convergence under sufficient conditions using an input-to-state stability (ISS) framework.
    
[^246]: HealthLoopQA：一个用于解读糖尿病护理中可穿戴监测数据的情境感知问答基准

    HealthLoopQA: A Context-Aware Question Answering Benchmark for Interpreting Wearable Monitoring Data in Diabetes Care

    [https://arxiv.org/abs/2609.06976](https://arxiv.org/abs/2609.06976)

    该论文提出了HealthLoopQA，一个情境感知的综合诊断基准，用于评估大型语言模型在解读糖尿病连续可穿戴监测数据方面的推理能力，弥补了现有基准忽视长期模式、治疗行为情境和真实部署中系统故障的不足。

    

    随着医疗可穿戴设备日益融入日常慢性病护理，有效解读纵向监测数据对于患者和临床医生理解健康趋势、检测安全关键事件以及做出明智决策至关重要。尽管大型语言模型（LLMs）在将这类流式生理数据转化为个性化健康洞察方面展现出前景，但评估其在多样化监测任务中的推理能力和分析严谨性仍然是一项根本性挑战。现有的医疗可穿戴问答（QA）基准主要评估短期分类或统计摘要，在很大程度上忽略了现实世界部署中固有的长期模式、治疗和行为情境以及潜在的系统故障。为解决这一问题，我们提出了HealthLoopQA，一个用于评估大型语言模型在连续糖尿病监测数据上推理能力的综合诊断性基准。

    arXiv:2609.06976v1 Announce Type: new  Abstract: As medical wearables become integrated into daily chronic disease care, effectively interpreting longitudinal monitoring data is essential for patients and clinicians to understand health trends, detect safety-critical events, and make informed decisions. While large language models (LLMs) show promise for transforming this streaming physiological data into personalized health insights, evaluating their reasoning capability and analytical rigor in diverse monitoring tasks remains a fundamental challenge. Existing medical wearable question answering (QA) benchmarks primarily assess short-horizon classification or statistical summaries, largely ignoring the long-term patterns, therapeutic and behavioural contexts, and potential system failures inherent in real-world deployments. To address this, we introduce HealthLoopQA, a comprehensive diagnostic benchmark for evaluating LLM reasoning over continuous diabetes monitoring data. Grounded in
    
[^247]: AgentDrift：注入劫持LLM智能体轨迹的逐步标注基准

    AgentDrift: A Step-Labeled Benchmark of Injection-Hijacked LLM Agent Trajectories

    [https://arxiv.org/abs/2609.06972](https://arxiv.org/abs/2609.06972)

    提出了AgentDrift基准，包含五个领域的12,536条合成工具调用轨迹，首次逐步标注了间接提示注入进入LLM智能体轨迹的位置及其破坏的具体步骤。

    

    LLM智能体通过发出一系列工具调用来完成任务，而它们读取的每一条观测结果都是间接提示注入进入的通道。当按顺序阅读轨迹时，成功的注入具有特征性的形态：良性前缀会让位于为攻击者而非用户服务的动作。现有的基准衡量此类攻击对运行中的智能体是否成功，而现有的防护模型则是对整条轨迹进行整体判断；目前没有公开语料库逐步标注注入在轨迹中的进入位置以及它破坏了哪些步骤。我们提出了AgentDrift，这是一个包含五个智能体领域、共12,536条合成工具调用轨迹的基准，其中71,024个步骤中的每一步都带有四种标签之一：良性、注入点、被劫持或注入失败。该语料库包含4,000条良性轨迹、5,536条被攻击轨迹、1,500条攻击失败轨迹和1,500条困难负例轨迹；被攻击的轨迹遵循三种合规性……

    arXiv:2609.06972v1 Announce Type: cross  Abstract: LLM agents complete tasks by issuing sequences of tool calls, and every observation they read is a channel through which an indirect prompt injection can enter. A successful injection has a characteristic shape when the trajectory is read in order: a benign prefix gives way to actions that serve the attacker rather than the user. Existing benchmarks measure whether such attacks succeed against live agents, and existing guard models judge a trace as a whole; no public corpus labels, step by step, where an injection enters a trajectory and which steps it corrupts. We present AgentDrift, a benchmark of 12,536 synthetic tool-call trajectories over five agent domains in which every one of the 71,024 steps carries one of four labels: benign, injection point, hijacked, or failed injection. The corpus contains 4,000 benign, 5,536 attacked, 1,500 failed-attack, and 1,500 hard-negative trajectories; attacked trajectories follow three compliance 
    
[^248]: MOLE：检测AI智能体中的内部威胁

    MOLE: Detecting Insider Threats in AI Agents

    [https://arxiv.org/abs/2609.06966](https://arxiv.org/abs/2609.06966)

    MOLE是一个检测AI智能体内部威胁的开放基准测试，包含150个账户、12种威胁和约200亿token，研究发现72%的智能体模型能完成大部分有害目标，而即使最佳监控器也会遗漏近一半已完成的危害。

    

    模型失准、提示注入或操作员误用可能导致操作前沿实验室账户的AI智能体窃取模型权重、污染训练数据或削弱发布门控。现有基准测试并未检验防御者是否能够在有限的审查预算下从日常工作中检测出此类活动。我们推出了MOLE，这是一个开放的基准测试，包含150个AI操作的账户，在30个工作日内共享9个有状态服务，涵盖12种威胁以及来自四个模型的8个语料库，总计约200亿个token。在39个智能体模型中，72%能够完成大部分被指派的有害目标，且智能体的拒绝行为并不能预测任务完成情况。MOLE支持对40个监控器在不同语料库生成器、可观测性级别和威胁类型之间进行比较；即使在我们单日审计事件比较中表现最佳的监控器，也会遗漏近一半已完成的危害行为。MOLE还能促进监控器的开发：基于基准测试的引导式搜索可将中等水平的监控器性能提升49-64%，而选择性使用……

    arXiv:2609.06966v1 Announce Type: new  Abstract: Model misalignment, prompt injection, or operator misuse could lead AI agents operating frontier-lab accounts to exfiltrate model weights, poison training data, or weaken release gates. Existing benchmarks do not test whether defenders can detect this activity among routine work under a limited review budget. We introduce MOLE, an open benchmark of 150 AI-operated accounts sharing 9 stateful services over 30 workdays, with 12 threats and 8 corpora from four models totaling roughly 20 billion tokens. Of 39 agent models, 72% complete most assigned harmful objectives and agent refusal does not predict completion. MOLE enables comparison of 40 monitors across corpus generators, observability levels, and threats; even the best evaluated monitor in our single-day audit-event comparison misses nearly half of completed harm. MOLE also enables monitor development: benchmark-guided search improves a mid-tier monitor by 49-64%, while selective use 
    
[^249]: 导向干扰反映的是模型的默认倾向，而非行为方向

    Steering Interference Reflects the Model's Defaults, Not the Behavior Directions

    [https://arxiv.org/abs/2609.06951](https://arxiv.org/abs/2609.06951)

    激活导向引发的副作用并非来自被导向的行为方向本身，而是由模型自身的默认偏好决定——无论导向何种行为，模型都会趋向其本已偏好的少数行为（如拒答、谄媚、诗歌化）。

    

    激活导向有望实现对语言模型行为的模块化控制：某种行为（如礼貌）对应模型激活中的一个方向，在模型生成时添加该方向应当能开启该行为，且不影响其他方面。但事实并非如此。我们探究了是什么决定了哪些其他行为会发生变化以及变化程度，发现起决定作用的是模型本身，而非被导向的行为。导向会使模型放松趋向于它本已偏好的一小部分行为，主要是拒答、谄媚和诗歌化倾向，且无论导向什么行为，这一行为集合大体相同。横跨24种行为和十个指令微调模型的三项结果支持这一结论，所有效应均由语言模型裁判从生成文本中读取，而非通过探针读取。这种读取方式很重要：所有24种行为都是线性可解码的，但只有20种行为会改变模型的实际输出内容。第一，一个不含任何行为内容、仅在特定方面与真实导向相匹配的方向……（摘要在此处被截断）

    arXiv:2609.06951v1 Announce Type: cross  Abstract: Activation steering promises modular control of language model behavior: a behavior such as politeness corresponds to a direction in a model's activations, and adding that direction while it generates should switch the behavior on and leave everything else alone. It does not. We ask what decides which other behaviors move, and by how much, and find that it is the model rather than the behavior being steered. A steer relaxes the model toward a small set of behaviors it already favors, chiefly refusal, sycophancy, and poeticism, and that set is much the same whatever is steered.   Three results across 24 behaviors and ten instruction-tuned models support this, every effect read off the generated text by a language-model judge rather than off a probe. That readout matters: all 24 behaviors are linearly decodable, but only 20 change what the model writes. First, a direction carrying no behavioral content, matched to a real steer only in th
    
[^250]: 从分阶段几何视角解析流匹配与无分类器引导的粒子动力学

    Particle Dynamics of Flow Matching and Classifier-Free Guidance from a Stagewise Geometry Perspective

    [https://arxiv.org/abs/2609.06947](https://arxiv.org/abs/2609.06947)

    该论文建立了流匹配与无分类器引导的统一分阶段几何理论，证明采样轨迹从噪声到数据会依次被吸引向全局均值邻域、数据凸包和局部聚类邻域，且各阶段均满足统一的收缩估计。

    

    流匹配与无分类器引导在生成建模中被广泛使用，然而其大部分理论理解仍停留在分布层面。由于实际采样遵循的是个体轨迹，仅有分布层面的保证无法充分刻画轨迹如何与数据几何相互作用，以及引导如何重塑数据几何。为克服这一局限，我们针对连续动力学和显式欧拉离散化，建立了一个统一的分阶段吸引与吸收几何理论。具体而言，当 t∈[0,1] 从噪声演化到数据时，我们证明无条件流轨迹会依次被吸引至全局均值的邻域、数据的凸包，以及一个可能非凸的局部聚类的邻域。在这些阶段中，相应的距离满足一个共同的收缩估计，使得在最后阶段距离以 O(1-t) 的速度衰减。对于 CFG，……

    arXiv:2609.06947v1 Announce Type: new  Abstract: Flow matching, together with classifier-free guidance (CFG), is widely used in generative modeling, yet much of the theoretical understanding remains distribution-wise. Since practical sampling follows individual trajectories, distribution-level guarantees alone do not fully capture how trajectories interact with the data geometry or how guidance reshapes it. To overcome this limitation, we establish a unified stagewise geometric theory of attraction and absorption for both continuous dynamics and explicit Euler discretization. Specifically, with $t\in[0,1]$ running from noise to data, we show that unconditional flow trajectories are successively attracted toward a neighborhood of the global mean, the data convex hull, and a neighborhood of a possibly nonconvex local cluster. Across these stages, the corresponding distance satisfies a common contraction estimate, yielding an ${O}(1-t)$ decay of the distance in the final stage. For CFG, t
    
[^251]: PCSDiff：基于扩散模型的偏差校正与超分辨率方法，迈向实用化业务中期降水预报

    PCSDiff: Diffusion-Based Bias Correction and Super Resolution Toward Practical Operational Medium-Term Precipitation Forecast

    [https://arxiv.org/abs/2609.06942](https://arxiv.org/abs/2609.06942)

    PCSDiff提出了一种级联任务解耦的扩散框架，结合降水强度感知多分支解码器（PIMD）和两阶段条件扩散超分辨率模块，实现10天中期降水的动态偏差校正与高分辨率重建，满足业务化部署需求。

    

    中期降水预报长期受到持续性系统偏差、随预报时效增长的误差累积以及粗空间分辨率的困扰，限制了其在旱涝风险评估中的可靠性。现有的人工智能订正技术缺乏对多日动态偏差演变的专门建模以及适当的气象约束，往往生成过度平滑的降雨结构，无法满足业务化部署的需求。本工作提出了PCSDiff，一个面向10天降水偏差校正与降尺度的级联式任务解耦扩散框架。为了同时对抗时间误差漂移并重建物理上合理的局地降水细节，PCSDiff集成了降水强度感知多分支解码器（PIMD）模块，利用天气-时间特征进行动态多日误差缓解，随后通过两阶段条件扩散超分辨率模块来恢复精细的降水结构。

    arXiv:2609.06942v1 Announce Type: cross  Abstract: Medium-range precipitation forecasts are impaired by persistent systematic biases, lead-time-dependent error accumulation, and coarse spatial resolution, restricting their reliability for flood-drought risk assessment. Existing AI correction techniques lack dedicated modeling for multi-day dynamic bias evolution and proper meteorological constraints, often generating over-smoothed rainfall structures, and cannot meet operational deployment demands. This work introduces PCSDiff, a cascaded task-decoupled diffusion framework targeting 10-day precipitation bias correction and downscaling. To jointly counteract temporal error drifts and reconstruct physically plausible local precipitation details, PCSDiff integrates the Precipitation Intensity-aware Multi-branch Decoder (PIMD) module for dynamic multi-day error mitigation using synoptic-temporal features, followed by a two-phase conditional diffusion super-resolution module to restore fine
    
[^252]: 拒答的几何学：为什么事后安全训练脆弱而预训练期安全却能持久

    The Geometry of Refusal: Why Post-Hoc Safety Is Fragile and Pretraining-Time Safety Persists

    [https://arxiv.org/abs/2609.06934](https://arxiv.org/abs/2609.06934)

    该论文从几何视角证明，事后安全训练（如RLHF）的更新与模型能力方向近乎正交，只是在完好的能力之上叠加一道薄而尖锐的拒答“闸门”而非真正删除能力，因此注定会被越狱等攻击绕过，而持久的安全必须在预训练阶段扎根。

    

    事后安全训练（RLHF、DPO）是目前对齐大语言模型的主流方法，然而越狱攻击（Zou et al., 2023b）、微调攻击（Qi et al., 2024）以及激活空间探测（Arditi et al., 2024）总能恢复出那些本应被移除的行为。我们对这种脆弱性给出了一种几何解释，并将其追溯到预训练过程中安全机制能够真正扎根的时机。我们将安全更新 $\Delta = W_{\text{safe}} - W_{\text{base}}$ 与模型能力的曲率（能力损失的经验 Fisher 信息）进行对照测量。研究发现，事后安全训练始终落入一个“抑制区间”：$\Delta$ 与能力方向近乎正交，其在子空间内的微小分量集中于少数高曲率方向。这一更新“薄而尖锐”——它是在完好的能力之上叠加的一道拒答闸门，而非对能力的真正抹除。一个核不动性引理解释了为什么这样的更新只能掩盖能力而无法将其移除，因此……（摘要原文在此处截断）

    arXiv:2609.06934v1 Announce Type: cross  Abstract: Post-hoc safety training (RLHF, DPO) is the dominant way to align large language models, yet jailbreaks (Zou et al., 2023b), fine-tuning attacks (Qi et al., 2024), and activation-space probes (Arditi et al., 2024) keep recovering the behaviors it was meant to remove. We give this fragility one geometric explanation and trace it to when, during pretraining, safety can take hold. We measure the safety update $\Delta = W_{\text{safe}} - W_{\text{base}}$ against the curvature of the model's capabilities (the empirical Fisher of a capability loss). Post-hoc safety consistently lands in a suppression regime: $\Delta$ is nearly orthogonal to the capability directions, and its small in-subspace part concentrates on a few high-curvature ones. The update is thin but sharp, a refusal gate laid over intact capabilities rather than erasure of them. A kernel-immobility lemma explains why such an update can only mask a capability, not remove it, so a
    
[^253]: 基于空间条件化多智能体Transformer的分布式灵巧操作

    Distributed Dexterous Manipulation with Spatially Conditioned Multi-Agent Transformers

    [https://arxiv.org/abs/2609.06930](https://arxiv.org/abs/2609.06930)

    本文提出基于空间条件化多智能体Transformer的框架，通过自适应层归一化、空间对比嵌入以及结合Soft Actor Critic微调的行为克隆方法，实现了由64个软体delta机器人组成的分布式灵巧操作系统的高效鲁棒控制。

    

    分布式灵巧操作（DDM）是一种新颖的范式，由于其高动作空间冗余、机器人间协作以及动态的物体-机器人交互，带来了重大的控制挑战。本文介绍了一个基于空间条件化多智能体Transformer（MATs）的框架，用于为一个由64个软体delta机器人排列成8x8网格阵列构成的DDM系统高效地学习鲁棒的控制策略。我们的三个核心贡献是：（i）带有自适应层归一化的MAT以实现计算效率；（ii）空间对比嵌入，将Transformer的嵌入扎根于机器人的空间配置中；（iii）一种基于MAT的行为克隆方法，并使用Soft Actor Critic进行微调。我们还提出了一种动作选择公式，用于分析任务性能与所使用机器人数量之间的权衡。我们的实验表明，MATs通过堆叠的注意力层迭代地优化其动作……

    arXiv:2609.06930v1 Announce Type: cross  Abstract: Distributed Dexterous Manipulation (DDM) is a novel paradigm that presents significant control challenges due to high action-space redundancy, inter-robot cooperation, and dynamic object-robot interactions. This paper introduces a framework based on spatially conditioned Multi-Agent Transformers (MATs) to efficiently learn robust control policies for a DDM system grounded in an array of 64 soft delta robots arranged in an 8x8 grid. Our three core contributions are: (i) an MAT with adaptive layer norm for compute efficiency, (ii) spatial contrastive embeddings to ground transformer embeddings in the spatial configuration of the robots, and (iii) an MAT-based behavior cloning method fine-tuned using Soft Actor Critic. We also propose an action selection formulation to analyze the trade-off between task performance and the number of robots utilized. Our experiments show that MATs iteratively refine their actions through the stacked attent
    
[^254]: 具有噪声约束值的约束在线学习

    Constrained Online Learning with Noisy Constraint Values

    [https://arxiv.org/abs/2609.06921](https://arxiv.org/abs/2609.06921)

    针对约束值受噪声污染的约束在线凸优化，本文证明了联合次线性保证的不可能性，并提出LEDGER算法，通过引入预算违反新度量，实现了 $O(\sqrt T/V)$ 的期望遗憾和可控的预算违反保证。

    

    我们研究具有对抗性约束的约束在线凸优化问题，其中约束值和梯度是通过无偏噪声观测得到的。标准差为 $\sigma$ 的高斯值噪声会导致期望遗憾与期望硬违反的最大值在最坏情况下存在 $\Omega(\min\{\sigma,1\}T/\log^7T)$ 的下界，即使梯度已知也是如此。这排除了在固定 $\delta>0$ 和固定正噪声水平下任何联合的 $O(T^{1-\delta})$ 保证。因此，我们转而研究预算违反：即固定时间范围内任意窗口上的最大累积超支。我们提出了 LEDGER 算法，该算法在非负余额中跟踪观测到的净消耗，并在当前反馈噪声产生之前设置约束权重。在常见的可行性条件和条件有限方差反馈下，对于固定的问题参数，LEDGER 算法实现了 $O(\sqrt T/V)$ 的期望遗憾和 $O(\sqrt V\,T^{3/4}+\sigma\sqrt T)$ 的期望预算违反，其中 $V$……

    arXiv:2609.06921v1 Announce Type: cross  Abstract: We study constrained online convex optimization with adversarial constraints when constraint values and gradients are observed through unbiased noise. Gaussian value noise of standard deviation $\sigma$ yields a worst-case lower bound of $\Omega(\min\{\sigma,1\}T/\log^7T)$ on the maximum of expected regret and expected hard violation, even with known gradients. This rules out any jointly $O(T^{1-\delta})$ guarantee for fixed $\delta>0$ and fixed positive noise level. We therefore study budget violation: the largest cumulative overspend over any window within a fixed horizon. We introduce \LEDGER, which tracks observed net consumption in a nonnegative balance and sets constraint weights before the current feedback noise. Under common feasibility and conditional finite-variance feedback, for fixed problem parameters, \LEDGER\ achieves $O(\sqrt T/V)$ expected regret and $O(\sqrt V\,T^{3/4}+\sigma\sqrt T)$ expected budget violation for $V\
    
[^255]: 从合成先验到模型行为：表格基础模型中的结构覆盖度

    From Synthetic Priors to Model Behavior: Structural Coverage in Tabular Foundation Models

    [https://arxiv.org/abs/2609.06912](https://arxiv.org/abs/2609.06912)

    该论文从分布层面归因视角出发，通过结构覆盖度和归一化密度量化表格基础模型的合成预训练先验对下游基准任务的支持范围，并探究这种结构支持与预测性能之间的关联。

    

    表格基础模型（TFMs）通常在大量程序化生成的合成任务集合上进行预训练，然而这些合成预训练先验对模型所评估的下游任务的支持程度仍不清楚。我们从分布层面的归因视角来研究这一问题。我们恢复或重构了四个表格基础模型的合成数据生成器，并将其生成的任务与两个广泛使用的表格基准数据集进行比较。每个数据集都由一组共同的结构描述符表示，这些描述符涵盖模式、特征分布、依赖结构、响应属性以及特征-响应关系。在该空间中，我们使用结构覆盖度和归一化密度来衡量每个合成先验到达基准任务的广度与重复程度，并检验更强的局部支持是否与更好的预测性能相关联。我们发现存在显著差异……（原文摘要至此被截断）

    arXiv:2609.06912v1 Announce Type: cross  Abstract: Tabular foundation models (TFMs) are commonly pretrained on large collections of procedurally generated synthetic tasks, yet it remains unclear how well these synthetic pretraining priors support the downstream tasks on which the models are evaluated. We study this question from a distribution-level attribution perspective. We recover or reconstruct the synthetic data generators of four TFMs and compare their generated tasks with datasets from two widely used tabular benchmarks. Each dataset is represented by a common set of structural descriptors capturing schema, feature distributions, dependence structure, response properties, and feature--response relationships. In this space, we measure how broadly and repeatedly each synthetic prior reaches benchmark tasks using structural coverage and normalized density, and examine whether stronger local support is associated with better predictive performance. We find substantial differences a
    
[^256]: 平滑皮卡德哈密顿蒙特卡罗

    Smoothed Picard Hamiltonian Monte Carlo

    [https://arxiv.org/abs/2609.06906](https://arxiv.org/abs/2609.06906)

    该论文提出平滑皮卡德哈密顿蒙特卡罗采样器，通过融合高斯平滑、皮卡德迭代和高阶离散化，对对数凹分布采样实现了改进的复杂度保证，并借助递归温启动生成器框架将其升级为高精度采样方法。

    

    我们开发了一种新的低精度采样器，称为平滑皮卡德哈密顿蒙特卡罗，它结合了高斯平滑、皮卡德迭代和高阶离散化方法。对于维度为 $d$、满足 $0 \prec \alpha I \preceq \nabla^2 V \preceq \beta I$ 的对数凹目标分布 $\pi \propto \exp(-V)$（其中条件数 $\kappa := \beta/\alpha$），平滑皮卡德HMC仅需 $\widetilde O(\kappa^2 + \kappa^{7/6} d^{1/6}/\varepsilon^{1/3})$ 次梯度查询即可返回满足 $\sqrt \alpha\,W_2(\cdot,\pi) \le \varepsilon$ 的样本。我们还证明了更强的 $W_q$ 界，并进一步开发了一种算法框架——递归温启动生成器——将这些 $W_q$ 界升级为更强的散度保证。这为配套工作中提出的近端弹跳粒子采样器提供了温启动，从而构建出复杂度为 $\widetilde O((\kappa^{7/6} d^{1/6} + \kappa^{1/2} d^{1/4})\cdots$ 的高精度对数凹采样器。

    arXiv:2609.06906v1 Announce Type: cross  Abstract: We develop a new low-accuracy sampler, called \emph{smoothed Picard Hamiltonian Monte Carlo}, which combines Gaussian smoothing, Picard iteration, and higher-order discretization. For a log-concave target $\pi \propto \exp(-V)$ in dimension $d$ satisfying $0 \prec \alpha I \preceq \nabla^2 V \preceq \beta I$, with condition number $\kappa := \beta/\alpha$, smoothed Picard HMC returns a sample with $\sqrt \alpha\,W_2(\cdot,\pi) \le \varepsilon$ using $\widetilde O(\kappa^2 + \kappa^{7/6} d^{1/6}/\varepsilon^{1/3})$ gradient queries. We also prove stronger $W_q$ bounds, and then develop an algorithmic framework, the recursive warm start generator, to upgrade these $W_q$ bounds to stronger divergence guarantees. This produces a warm start for the proximal bouncy particle sampler, introduced in a companion work, leading to a high-accuracy log-concave sampler with complexity $\widetilde O((\kappa^{7/6} d^{1/6} + \kappa^{1/2} d^{1/4})\mathrm
    
[^257]: 基于近端弹球粒子采样器的暖启动加速高精度采样

    Accelerated High-Accuracy Sampling from a Warm Start via the Proximal Bouncy Particle Sampler

    [https://arxiv.org/abs/2609.06905](https://arxiv.org/abs/2609.06905)

    该论文提出了结合近端采样器与弹球粒子采样器思想的新型采样器 Proximal BPS，在暖启动条件下期望仅需 Õ(√κ d^{1/4} polylog(1/ε)) 次梯度查询即可在总变差距离上实现高精度采样。

    

    我们研究了在 ℝ^d 上从分布 μ(dx) ∝ e^{-V(x)} dx 中采样的难题，其中 V 是 α-强凸且 β-光滑的函数，并记 κ := β/α（条件数）。我们设计并分析了近端弹球粒子采样器，这是一种结合了近端采样器与弹球粒子采样器思想的新型采样器。从与 μ 的 Rényi 散度为 O(1) 的暖启动初始化出发，Proximal BPS 期望仅需 Õ(√κ d^{1/4} polylog(1/ε)) 次梯度查询，即可返回一个其分布与 μ 在总变差距离上 ε-接近的样本。

    arXiv:2609.06905v1 Announce Type: cross  Abstract: We study the problem of sampling from $\mu(\mathrm{d}x)\propto e^{-V(x)}\,\mathrm{d}x$ on $\mathbb{R}^d$, where $V$ is $\alpha$-strongly convex and $\beta$-smooth, and write $\kappa:=\beta/\alpha$. We design and analyze the Proximal Bouncy Particle Sampler (Proximal BPS), a new sampler that combines ideas from the proximal sampler and the bouncy particle sampler. From a warm start initialization with $ O(1) $ R\'enyi divergence w.r.t. $\mu$, Proximal BPS returns a sample whose law is $\varepsilon$-close to $\mu$ in total variation distance using $\widetilde O(\sqrt\kappa\,d^{1/4} \,\mathrm{polylog}(1/\varepsilon))$ gradient queries in expectation.
    
[^258]: 动态规划引导的分层BPE与词表剪枝的实证分析

    Dynamic-Programming-Guided Hierarchical BPE and Empirical Analysis of Vocabulary Pruning

    [https://arxiv.org/abs/2609.06898](https://arxiv.org/abs/2609.06898)

    提出DH-BPE方法，结合动态规划度量的词元效用与BPE分层依赖关系，通过暴露度引导的剪枝构建固定大小的模型可见词表，在压缩性能上优于标准BPE及现有词表优化基线。

    

    字节对编码（BPE）通过贪心的词对合并来构建词表，但由此产生的合并顺序并不一定能针对压缩任务最优地分配固定的模型可见词表。我们提出了动态规划引导的分层BPE（DH-BPE），这是一种词表构建方法，它将精确最小分词下的词元暴露度与BPE训练所诱导的分层依赖关系相结合。从一个适度超出的BPE候选词表出发，DH-BPE使用动态规划来衡量候选词元的效用，并应用暴露度引导的、依赖感知的剪枝来选择固定大小的模型可见词表。我们在12K和16K目标词表大小的主要评估中将DH-BPE与标准BPE以及近期的词表优化基线（包括剪枝BPE、MinGram和MinGram-PP）进行比较，并额外针对MinGram进行了18K的评估。在主要的12K和16K比较中，DH-BPE（摘要在此处截断）

    arXiv:2609.06898v1 Announce Type: cross  Abstract: Byte Pair Encoding (BPE) constructs vocabularies through greedy pair merging, but the resulting merge order does not necessarily allocate a fixed model-visible vocabulary optimally for compression. We propose Dynamic-Programming-Guided Hierarchical BPE (DH-BPE), a vocabulary-construction method that combines token exposure under exact minimum-token segmentation with the hierarchical dependencies induced by BPE training. Starting from a modestly overshot BPE candidate vocabulary, DH-BPE uses dynamic programming to measure candidate utility and applies exposure-guided, dependency-aware pruning to select a fixed-size model-visible vocabulary. We compare DH-BPE against Standard BPE and recent vocabulary-optimization baselines, including Pruned BPE, MinGram, and MinGram-PP, in primary evaluations at 12K and 16K target vocabulary sizes, with an additional 18K evaluation against MinGram only. Across the primary 12K and 16K comparisons, DH-BPE
    
[^259]: 通过偏好蒸馏弥合离线与迭代对齐之间的差距

    Towards Bridging the Gap Between Offline and Iterative Alignment via Preference Distillation

    [https://arxiv.org/abs/2609.06893](https://arxiv.org/abs/2609.06893)

    该研究揭示迭代对齐方法优于离线方法的关键在于其额外引入的显式偏好模型，并据此提出DP3O算法，通过利用辅助LLM进行偏好蒸馏，将迭代方法的优势成功融入高效的离线对齐框架中。

    

    直接偏好优化（DPO）因其简洁性、计算效率以及对人类偏好的隐式建模，是一种颇具前景的大语言模型（LLM）离线对齐方法。有趣的是，DPO的迭代扩展方法在学术基准测试中取得了更强的性能，这引出了两个关键问题：（i）为什么迭代方法通常优于离线方法？（ii）能否将迭代方法的优势融入离线对齐中？为了回答第一个问题，我们的对照实验揭示，在迭代过程中额外引入的显式偏好模型是其优于离线方法的关键因素。基于这一洞察，我们得以肯定地回答第二个问题，并提出了蒸馏偏好概率策略优化（DP3O），这是一种高效且有效的离线对齐算法。DP3O首先使用一类辅助LLM学习显式偏好模型，并通过……

    arXiv:2609.06893v1 Announce Type: cross  Abstract: Direct preference optimization DPO is a promising offline approach for aligning large language models (LLMs) due to its simplicity, computational efficiency, and implicit modeling of human preferences. Interestingly, iterative extensions of DPO have achieved stronger performance on academic benchmarks, raising two key questions: (i) Why do iterative methods generally outperform offline ones? (ii) Can their advantages be incorporated into offline alignment? To answer the first question, our controlled experiments reveal that the explicit preference model, additionally introduced in the iterative procedure, is a key factor behind its superiority over offline methods. This insight leads us to answer the second question affirmatively and propose Distilled Preference Probability Policy Optimization (DP3O), an effective and efficient offline alignment algorithm. DP3O first learns an explicit preference model using a helper class of LLMs and 
    
[^260]: 离线强化学习中扩散策略的噪声空间策略梯度

    Noisy-Space Policy Gradient for Diffusion Policies in Offline Reinforcement Learning

    [https://arxiv.org/abs/2609.06882](https://arxiv.org/abs/2609.06882)

    该论文提出了一种噪声空间动作值函数与噪声空间策略梯度（NSPG）方法，使扩散策略仅凭干净动作空间的值估计即可优化噪声潜变量，并通过扩散兼容的回归形式避免了通过去噪过程的反向传播，有效解决了扩散模型与离线强化学习结合的难题。

    

    扩散策略为连续控制提供了一种强大且富有表现力的参数化方法。然而，将其与强化学习相结合在概念和算法层面仍然充满挑战。在这项工作中，我们通过引入一种噪声空间动作值（Q-）函数来弥补这一空白，该函数通过去噪过程所诱导的执行动作分布为扩散潜变量赋值。我们证明这一构造具有精确的语义解释，并据此推导出一种噪声空间策略梯度（NSPG），它仅需使用干净动作空间的值估计即可优化噪声潜变量。基于这一结果，我们构建了针对噪声潜变量的KL正则化策略改进方法，并证明所得到的目标函数具有与扩散模型兼容的回归形式，从而避免了通过去噪过程的反向传播。在基于状态的D4RL基准测试和基于视觉的OGBench任务上的实证结果表明，所提出的噪声空间方法……

    arXiv:2609.06882v1 Announce Type: cross  Abstract: Diffusion policies offer a powerful and expressive parameterization for continuous control. Yet, their integration with reinforcement learning remains conceptually and algorithmically challenging. In this work, we address this gap by introducing a noisy-space action-value (Q-)function that assigns values to diffusion latents through the distribution of executed actions induced by the denoising process. We show that this construction admits a precise semantic interpretation and derive a noisy-space policy gradient (NSPG) that optimizes noisy latents using only clean action-space value estimates. Building on this result, we formulate a KL-regularized policy improvement over noisy latents and show that the resulting objective admits a diffusion-compatible regression form, avoiding backpropagation through the denoising process. Empirical results on state-based D4RL benchmarks and vision-based OGBench tasks demonstrate that the proposed noi
    
[^261]: 面向异构负载均衡的学习型自适应SED（最短期望延迟）策略

    Learning Adaptive SED for heterogeneous load balancing

    [https://arxiv.org/abs/2609.06881](https://arxiv.org/abs/2609.06881)

    针对服务速率未知的异构两服务器负载均衡系统，提出了一种在线学习SED路由算法，通过平衡经验路由与强制探索阶段，首次实现了有限遗憾（而非经典多臂老虎机中的对数增长遗憾）。

    

    我们研究了一个具有异构服务速率的两服务器负载均衡系统，调度器事先并不知道这些服务速率。目标是按照最短期望延迟策略来路由顾客，但这需要预先知道服务速率。基于估计的经验型路由策略表现不佳：由于估计误差，经验策略在状态空间的一个无限区域上会与先知策略产生分歧。我们提出了一种在线学习算法，能够在学习服务速率的同时收敛到SED策略。该算法精心平衡了经验SED路由与强制探索阶段，从而保证对两个服务器都进行充分采样。我们证明该算法实现了有限遗憾，这与经典多臂老虎机问题中遗憾通常随时间对数增长的特性形成鲜明对比。最后，数值实验展示了我们算法的性能，并突出了强制探索起作用的运行区间。

    arXiv:2609.06881v1 Announce Type: new  Abstract: We study a two-server load balancing system with heterogeneous service rates that are a priori unknown to the dispatcher. The goal is to route customers according to the Shortest--Expected--Delay (SED) policy, but this requires knowledge of the service rates. Empirical policies that route based on estimates perform poorly: due to estimation error, the empirical policy disagrees with the oracle on an infinite region of the state space. We propose an online learning algorithm that converges to SED while learning the service rates. The algorithm carefully balances empirical SED routing with forced exploration phases that guarantee sufficient sampling of both servers. We prove that our algorithm achieves finite regret; this differs from classical Multi-Armed Bandit settings where regret typically grows logarithmically in time. Finally, numerical experiments demonstrate the performance of our algorithm and highlight the regimes in which force
    
[^262]: 情境观察者定位：评估视觉语言模型中的情境化空间推理

    Contextual Observer Grounding: Evaluating Situated Spatial Reasoning in Vision-Language Models

    [https://arxiv.org/abs/2609.06880](https://arxiv.org/abs/2609.06880)

    该论文提出“情境观察者定位”这一新能力概念，并构建了POVBench基准，用于评估视觉语言模型从情境线索推断说话者视角并进行情境化空间推理的能力。

    

    在机器人等具身任务中对语言指令进行推理，通常需要从说话者所处的视角理解空间关系。人类会从共享的环境知识、活动情境和常识中推断这种视角。最近的视觉语言模型（VLMs）似乎具备空间推理能力，但它们从情境线索中推断说话者视角、并从该视角解释情境化空间关系的能力仍不清楚。我们将这种能力称为情境观察者定位。为了研究这种能力，我们构建了视角基准（POVBench），这是一个由3D场景和查询组成的数据集，它将自然具身交流中推断型、陈述型和给定型三种观察者定位形式加以区分。给定多视角观察和一句自然语言句子，模型必须根据情境化的空间和上下文线索来定位未见过或未明确指定的目标。在多个最先进的VLM上……

    arXiv:2609.06880v1 Announce Type: cross  Abstract: Reasoning over language instructions in embodied tasks such as robotics often requires understanding spatial relations from a speaker's situated perspective. Humans infer such perspectives from shared environmental knowledge, activity context, and commonsense. Recent vision-language models (VLMs) appear capable of spatial reasoning, but their ability to infer a speaker's viewpoint from contextual cues and interpret situated spatial relations from that viewpoint remains unclear. We call this capability contextual observer grounding. To study this capability, we construct the Point-of-View Benchmark (POVBench), a dataset of 3D scenes and queries that disentangles Inferred, Stated, and Given forms of observer grounding in natural embodied communication. Given multi-view observations and a natural-language sentence, models must localize unseen or underspecified targets from situated spatial and contextual cues. Across state-of-the-art VLMs
    
[^263]: 大规模分类风险最优的标签获取

    Large Classification-Risk-Optional Label Acquisition

    [https://arxiv.org/abs/2609.06873](https://arxiv.org/abs/2609.06873)

    该论文提出了一种融合Fisher信息与多分类超额风险局部几何结构的标签获取准则，通过衡量标签信息与扰动贝叶斯决策边界的参数方向的对齐程度来最优分配有限标注预算，从而最小化渐近分类风险。

    

    我们研究在有限标注预算下应如何进行分配，以最小化多分类的0-1分类风险。我们考虑参数化分类问题，其中所有抽样单元的特征均可被观测，而类别标签可以有选择地获取。通过将所获取标签提供的Fisher信息与多分类超额风险的局部几何结构相结合，我们推导出一个标签获取准则，该准则可最小化期望多分类超额风险的领先渐近系数。由此得到的规则依据标签信息与扰动当前贝叶斯决策边界的参数方向的对齐程度来评估标签的价值，而非仅依据后验不确定性或全局参数信息。我们刻画了最优获取设计，建立了其阈值结构，并推导了面向特定情形以及代价敏感的扩展。一个解析示例表明，后验不确定性与分类……（原文摘要至此截断）

    arXiv:2609.06873v1 Announce Type: cross  Abstract: We study how a limited labeling budget should be allocated to minimize multiclass zero-one classification risk. We consider parametric classification problems in which features are observed for all sampling units while class labels can be acquired selectively. By combining the Fisher information supplied by an acquired label with the local geometry of multiclass excess risk, we derive an acquisition criterion that minimizes the leading asymptotic coefficient of expected multiclass excess risk. The resulting rule values a label according to how strongly its information is aligned with parameter directions that perturb the active Bayes decision boundary, rather than according to posterior uncertainty or global parameter information alone. We characterize the oracle acquisition design, establish its threshold structure, and derive face-specific and cost-sensitive extensions. An analytic example shows that posterior uncertainty and classif
    
[^264]: Delta注意力机制中的精确记录删除：传输判据、其代价与重放证书

    Exact Record Omission in Delta Attention: A Transport Criterion, Its Cost, and a Replay Certificate

    [https://arxiv.org/abs/2609.06872](https://arxiv.org/abs/2609.06872)

    该论文证明了在Delta注意力类循环记忆中，通过“收据”传递实现记录精确删除当且仅当该记录诱导的后续更新变化净额为零，并在48B Kimi Linear混合模型上实证表明该条件不成立——记录会留下现有收据类方法均无法消除、重算也只能部分弥补的持久印记。

    

    当用户要求助手忘记某条记录时，检验标准是：记忆当前的状态是否与该记录从未被存储时本应处于的状态一致。独立编码的行可以直接删除；而循环记忆则会将记录折叠进一个不断演化的状态中。一种期望的方案是“收据”（receipt）：保存该记录到来时所产生的差异，在后续更新中将其向前传递，然后将其减去，从而无论对话进行多长，删除都只需一次固定大小的编辑。我们证明，被传递的收据能够实现精确删除，当且仅当该记录在后续更新中所诱导的变化在净额上相互抵消；并且我们在公开发布的48B Kimi Linear混合模型上实测了这种抵消是否发生。结果表明并未发生：在再经过4,096个token之后，该记录仍留下约占状态范数4.5%的印记，所测试的任何一类收据都无法将其移除；重算后半段后缀也只能弥补不到一半的差距；而收据所需的逐token日志的代价是……（摘要原文在此处截断）

    arXiv:2609.06872v1 Announce Type: new  Abstract: When a user asks an assistant to forget a record, the test is whether the memory now matches the state it would hold if the record had never been stored. Independently encoded rows can be removed directly; a recurrent memory folds records into an evolving state. One hope is a receipt: save the difference the record made when it arrived, carry it forward through later updates, and subtract it, so that deletion costs one fixed-size edit no matter how long the conversation runs. We show that a transported receipt reaches exact omission if and only if the changes the record induces in later updates cancel out on net, and we measure whether they do on the released 48B Kimi Linear hybrid. They do not: after 4,096 further tokens the record still leaves an imprint of about 4.5% of the state norm that none of the tested receipt classes removes, recomputing half the suffix closes less than half the gap, and the per-token log a receipt needs costs 
    
[^265]: PPIM：基于Pennes物理信息的Mamba用于热源条件下的三维生物热仿真

    PPIM: Pennes Physics-Informed Mamba for Heat-Source-Conditioned 3D Bioheat Simulation

    [https://arxiv.org/abs/2609.06869](https://arxiv.org/abs/2609.06869)

    本文提出PPIM模型，通过引入条件热源输入和Pennes感知的状态空间模型衰减初始化，在受控三维Pennes生物热仿真中相比空间傅里叶特征PINN和通用PINN-Mamba时间子序列模型取得更优的神经近似性能。

    

    三维生物热仿真旨在预测生物组织中的瞬态温度分布，通常采用Pennes生物热方程进行建模，该方程综合了热扩散、灌注介导的热损失以及外部热生成。在本研究中，我们考虑了受微波消融（MWA）启发的局部热源条件下的受控三维Pennes生物热仿真。为了评估神经近似性能，我们在相同的受控仿真下比较了三种神经偏微分方程（PDE）求解器：空间傅里叶特征物理信息神经网络（PINN）、通用的PINN-Mamba时间子序列模型，以及Pennes物理信息Mamba（PPIM）。PPIM在时间子序列模型的基础上，融入了条件热源输入和Pennes感知的状态空间模型（SSM）衰减初始化。所有三种神经模型均在相同条件下使用相同的P进行训练。

    arXiv:2609.06869v1 Announce Type: new  Abstract: Three-dimensional bioheat simulation aims to predict transient temperature distributions in biological tissue and is commonly modeled using the Pennes bioheat equation, which combines thermal diffusion, perfusion-mediated heat loss, and external heat generation. In this study, we consider a controlled 3D Pennes bioheat simulation under a localized heat-source condition inspired by microwave ablation (MWA). To evaluate neural approximation performance, we compare three neural partial differential equation (PDE) solvers under the same controlled simulation: a spatial Fourier-feature physics-informed neural network (PINN), a generic PINNMamba temporal subsequence model, and Pennes Physics-Informed Mamba (PPIM). PPIM builds on the temporal subsequence model by incorporating conditioned heat-source input and Pennes-aware state-space model (SSM) decay initialization. All three neural models are trained under the same conditions with the same P
    
[^266]: 神经网络中的特征叠加：从理论到实践

    Feature Superposition in Neural Networks: From Theory to Practice

    [https://arxiv.org/abs/2609.06862](https://arxiv.org/abs/2609.06862)

    本综述系统梳理了神经网络中特征叠加的几何、学习与计算理论，并比较了从训练网络中恢复可解释特征的实用方法及其评估标准，从而将理论研究与实证证据连接起来。

    

    叠加是指神经网络所表示的特征数量超过其自身维度的现象。它为多义神经元提供了一种可能的解释，并推动了从神经激活中恢复可解释特征的方法的发展。理论模型通常从给定的一组输入特征以及关于这些特征值在不同输入间如何变化的假设出发，进而研究网络如何将这些值编码到低维的隐藏表示中。相比之下，实证工作旨在识别训练后的网络中所编码的特征，并确定它们在计算中的作用。在这篇综述中，我们回顾了叠加表示的几何结构、学习过程和计算方式，并解释了特征统计特性与解码器选择如何影响研究结论。为了将这些理论解释与训练网络中的证据联系起来，我们比较了恢复和分析特征的实用方法，并考察了这些方法的评估所能确立的内容。

    arXiv:2609.06862v1 Announce Type: new  Abstract: Superposition refers to neural networks representing more features than they have dimensions. It offers a possible explanation for polysemantic neurons and motivates methods for recovering interpretable features from neural activations. Theoretical models typically start with a given set of input features and assumptions about how their values vary across inputs, then study how a network encodes those values in a lower-dimensional hidden representation. Empirical work, by contrast, seeks to identify the features encoded in trained networks and determine their role in computation. In this survey, we review the geometry, learning, and computation of superposed representations, explaining how feature statistics and decoder choice affect the conclusions. To connect these theoretical accounts with evidence from trained networks, we compare practical methods for recovering and analyzing features and examine what their evaluations establish. Si
    
[^267]: BinauralVAE：面向世界模型的空间音频重建

    BinauralVAE: Spatial Audio Reconstruction For World Models

    [https://arxiv.org/abs/2609.06837](https://arxiv.org/abs/2609.06837)

    该论文提出BinauralVAE，一个开源的空间音频重建流水线，通过探索从基础基线到数学化高级架构的多种变分自编码器模型，推动以音频为中心的世界模型发展，以弥补视觉感知在遮挡、低光照等场景下的不足。

    

    arXiv:2609.06837v1 公告类型：交叉。摘要：具身人工智能在历史上很大程度上依赖于视觉感知，导致多种以视觉为中心的世界模型大量涌现。然而，这种依赖未能完整捕捉空间理解的全貌，甚至在存在视觉遮挡、低光照条件或断电场景的环境中可能表现出脆弱性——在这些场景中，声学信息成为空间感知和导航的关键替代手段。尽管潜力巨大，针对真实空间音频的研究，特别是以音频为中心的世界模型的开发仍然稀少。在本技术报告中，我们介绍了BinauralVAE：一个灵活的开源流水线（https://github.com/Luizerko/BinauralVAE），探索了多种用于空间化音频重建的模型，从基础基线逐步推进到先进的、具有数学基础的架构。我们的方法评估了多种变分自编码器架构。

    arXiv:2609.06837v1 Announce Type: cross  Abstract: Embodied artificial intelligence has historically very much relied on visual perception, leading to a proliferation of multiple vision-centric world models. However, this reliance fails to capture spatial understanding in its entirety and can even present vulnerabilities in environments with visual occlusions, low-light conditions, or blackouts-scenarios, where acoustic information becomes a critical alternative for spatial awareness and navigation. Despite its potential, research into realistic spatial audio and particularly the development of audio-centric world models remains sparse. In this technical report, we introduce BinauralVAE: a flexible, open-source pipeline (https://github.com/Luizerko/BinauralVAE) that explores multiple models for spatialized audio reconstruction, progressing from fundamental baselines to advanced, mathematically grounded architectures. Our approach evaluates various Variational Autoencoder architectures 
    
[^268]: 面向植物病害分类的物联网网络中分层联邦学习的约束贝叶斯优化

    Constrained Bayesian Optimization for Hierarchical Federated Learning in IoT Networks for Plant Disease Classification

    [https://arxiv.org/abs/2609.06830](https://arxiv.org/abs/2609.06830)

    本文提出了一种约束贝叶斯优化框架，联合优化分层联邦学习的骨干网络架构、聚合策略和通信轮数，以在资源受限的物联网农业环境中平衡能耗、执行时间与预测性能。

    

    在资源受限的物联网环境中部署分层联邦学习需要精心的配置，以平衡预测性能与能耗和执行时间。这一挑战在智慧农业中尤为突出，因为分布式物联网设备需要在有限的计算和通信资源下支持自动化的植物病害分类。本文提出了一种用于高效配置分层联邦学习部署的约束贝叶斯优化框架。所提出的方法联合探索深度学习骨干网络架构、聚合策略和通信轮数，而联邦规模则根据农业部署的空间覆盖需求来确定。加权目标函数能够捕捉用户定义的能耗、执行时间和预测性能之间的权衡。

    arXiv:2609.06830v1 Announce Type: new  Abstract: The deployment of Hierarchical Federated Learning (HFL) in resource-constrained Internet of Things (IoT) environments requires careful configuration to balance predictive performance with energy consumption and execution time. This challenge is particularly relevant to smart agriculture, where distributed IoT devices can support automated plant disease classification while operating under limited computational and communication resources. This paper presents a constrained Bayesian Optimization framework for the efficient configuration of HFL deployments. The proposed approach jointly explores the deep learning backbone architecture, aggregation strategy, and number of communication rounds, while the federation size is determined according to the spatial coverage requirements of the agricultural deployment. A weighted objective function captures user-defined trade-offs among energy consumption, execution time, and predictive performance, 
    
[^269]: 神经形态系统中结构吸引子的形成

    Formation of structural attractors in neuromorphic systems

    [https://arxiv.org/abs/2609.06826](https://arxiv.org/abs/2609.06826)

    本文提出不变结构学习（ISL）理论，将学习视为超图空间中向结构吸引子的收敛而非损失函数最小化，实现了无需反向传播、仅需极小训练数据集的图像识别学习机制。

    

    本文研究了不变结构学习（ISL）理论，该理论提出了一种非优化的概念形成方法。学习被解释为在超图空间中向结构吸引子的收敛过程，而非对全局损失函数的最小化。论文提出了ISL模型，包括其数学形式化、计算验证以及假设性的神经生物学解释。数学部分介绍了结构化简过程的形式化工具，并证明了其有限收敛性、类结构吸引子的存在性与唯一性，以及吸引子图的自组织性。计算部分通过经典图像识别任务验证了该方法的可行性，利用所提出的学习机制实现了无需反向传播且训练数据集极小的学习。最后，神经生物学部分提出了相应的生物学假说。

    arXiv:2609.06826v1 Announce Type: new  Abstract: This paper examines the theory of Invariant Structural Learning (ISL), which proposes a non-optimization approach to concept formation. Learning is interpreted as convergence to structural attractors in a hypergraph space, rather than as the minimization of a global loss function. The paper presents the ISL model, including its mathematical formalization, computational verification, and a hypothetical neurobiological interpretation. The mathematical section introduces the formal apparatus of the structural reduction process and proves its finite convergence, the existence and uniqueness of class structural attractors, and the self-organization of attractor maps. The computational section demonstrates the feasibility of the proposed approach on classical image recognition tasks, utilizing the proposed learning mechanism without backpropagation and with extremely small training datasets. Finally, the neurobiological section formulates hypo
    
[^270]: 更聪明地训练，而非更努力地训练：主动学习中的切换信号引导训练

    Train Smarter, Not Harder: Switching Signal-Guided Training in Active Learning

    [https://arxiv.org/abs/2609.06806](https://arxiv.org/abs/2609.06806)

    提出HybridAL自适应训练调度方法，通过监控在线稳定化信号在主动学习中智能地从重新训练切换到微调，在保持性能不劣化的同时节省高达49%的重训练开销

    

    训练策略，即是从头重新训练还是从上一个检查点进行微调，是主动学习中一个被忽视的决策变量。我们证明这一选择具有可利用的结构特征：重新训练在早期轮次中最有用，此时每个批次都能显著重塑已标注数据的分布；而一旦模型轨迹趋于稳定，微调就变得更安全。我们提出HybridAL，一种自适应训练调度方法，它监控在线稳定化信号，并在持续稳定后从重新训练切换到微调。两个互补的信号——谱指数变化Δα（基于权重）和准确率变化ΔAcc（基于验证集）——在时间-校准权衡上覆盖不同的位置。在三个编码器骨干网络和六个文本分类任务（每个任务五个随机种子）上，HybridAL保持端点macro-F1不劣于重新训练和微调（边际为0.010），并节省了高达49%的重训练开销

    arXiv:2609.06806v1 Announce Type: new  Abstract: Training strategy, namely whether to retrain from scratch or fine-tune from the previous checkpoint, is an overlooked decision variable in active learning. We show that this choice has exploitable structure: retraining is most useful in early rounds, when each batch can substantially reshape the labeled distribution, while fine-tuning becomes safer once the model trajectory stabilizes. We propose HybridAL, an adaptive training schedule that monitors an online stabilization signal and switches from retraining to fine-tuning after sustained stabilization. Two complementary signals, spectral exponent change $\Delta\alpha$ (weight-based) and accuracy change $\Delta$Acc (validation-based), span different points on the time-calibration trade-off. Across three encoder backbones and six text-classification tasks (five seeds each), HybridAL keeps endpoint macro-F1 non-inferior to retraining and fine-tuning at a 0.010 margin, saves up to 49% of re
    
[^271]: 多芯粒光子神经网络加速器的硬件木马威胁

    Hardware Trojan Threats to Multi-Chiplet Photonic Neural Network Accelerators

    [https://arxiv.org/abs/2609.06796](https://arxiv.org/abs/2609.06796)

    本文从机密性、完整性和可用性三个维度系统分析了多芯粒光子神经网络加速器所面临的硬件木马安全威胁。

    

    多芯粒光子神经网络加速器（MCPNA）结合了光子计算、光子通信和异构芯粒集成技术，能够实现可扩展且高能效的AI加速。然而，其分布式架构以及对第三方芯粒的依赖带来了重大的硬件安全风险。本文从机密性、完整性和可用性三个维度研究了针对MCPNA的硬件木马（HT）威胁。

    arXiv:2609.06796v1 Announce Type: cross  Abstract: Multi-chiplet photonic neural network accelerators (MCPNAs) combine photonic computation, photonic communica-tion, and heterogeneous chiplet integration to enable scalable and energy-efficient AI acceleration. However, their distributed archi-tecture and reliance on third-party chiplets introduce significant hardware security risks. This paper examines Hardware Trojan (HT) threats to MCPNAs across three dimensions: confidentiality, integrity, and availability.
    
[^272]: 当保留约束发生冲突时：缓解表格数据中的遗忘-保留干扰

    When Retain Constraints Conflict: Mitigating Forget-Retain Interference in Tabular Data

    [https://arxiv.org/abs/2609.06786](https://arxiv.org/abs/2609.06786)

    该论文揭示了表格数据遗忘中独有的“模式引起遗忘-保留重叠”难题，并提出冲突感知遗忘方法（CAU）来缓解遗忘与保留之间的相互干扰。

    

    机器遗忘旨在移除指定训练数据对模型的影响，同时保持模型效用，但其在表格数据上的行为仍未得到充分探索。这一空白非常重要，因为表格预测被广泛应用于高风险领域，并且正通过记录序列化和模式感知提示越来越多地适配到语言模型中。我们识别出一个区分表格数据遗忘与自由文本或其他模态遗忘的关键挑战：模式引起的遗忘-保留重叠。在序列化的表格数据中，记录共享固定的列名/值槽位、相似的属性范围和共同的输出空间。因此，一个待遗忘的行可能存在依赖于相同高信号属性的邻近保留行，导致保留维护与遗忘所需的更新相互对立。受这种失败模式的启发，我们提出了冲突感知遗忘（CAU），一种模式感知的方法，能够减少遗忘-保留之间的干扰。

    arXiv:2609.06786v1 Announce Type: new  Abstract: Machine unlearning aims to remove the influence of designated training data while preserving model utility, but its behavior on tabular data remains underexplored. This gap is important because tabular prediction is widely used in high-stakes domains and is increasingly adapted to language models through record serialization and schema-aware prompting. We identify a key challenge that distinguishes tabular unlearning from unlearning in free-form text or other modalities: schema-induced forget-retain overlap. In serialized tabular data, records share fixed column-name/value slots, similar attribute ranges, and common output spaces. Consequently, a forget row may have nearby retain rows that rely on the same high-signal attributes, causing retain preservation to oppose the update required for forgetting. Motivated by this failure mode, we propose Conflict-Aware Unlearning (CAU), a schema-aware approach that reduces forget-retain interferen
    
[^273]: DrugReason：面向药物重定位的知识图谱与语言证据动态多视角推理

    DrugReason: Dynamic Multi-View Reasoning over Knowledge Graph and Language Evidence for Drug Repurposing

    [https://arxiv.org/abs/2609.06779](https://arxiv.org/abs/2609.06779)

    DrugReason 提出了一个动态多视角推理框架，将知识图谱的结构化多跳推理与大语言模型灵活的机理解释相结合，从而实现更可靠、更具事实依据的药物重定位预测。

    

    药物重定位旨在为现有化合物发现新的治疗用途，与从头药物发现相比，它为临床转化提供了更快、更具成本效益的路径。然而，候选药物-疾病对的空间极其庞大，且其潜在关系往往依赖于复杂的多跳生物机制，这使得难以可靠地预测哪些药物-疾病对代表真正的治疗关系。现有方法从两个方向入手：基于知识图谱的方法将整理好的生物医学证据组织为结构化关系网络，以进行有事实依据的多跳推理；而基于大语言模型的方法则利用预训练知识生成灵活的机理性解释。然而，二者单独使用均不充分——知识图谱受限于已观测到的图结构，而大语言模型缺乏事实依据并存在幻觉风险。为填补这一空白，我们提出了DrugReason，一个多视角推理框架……

    arXiv:2609.06779v1 Announce Type: cross  Abstract: Drug repurposing aims to identify new therapeutic uses for existing compounds and, compared with de novo drug discovery, offers a faster and more cost-effective path to clinical translation. However, the space of candidate drug-disease pairs is enormous and their underlying relationships often depend on complex multi-hop biological mechanisms, making it difficult to reliably predict which pairs represent true therapeutic relationships. Existing approaches tackle this from two directions: knowledge graph-based methods organize curated biomedical evidence into structured relational networks for grounded multi-hop reasoning, while LLM-based methods leverage pretrained knowledge to generate flexible mechanistic rationales. Yet neither is sufficient alone - KGs are confined to observed graph structure while LLMs lack factual grounding and risk hallucination. To address this gap, we propose DrugReason, a multi-view reasoning framework that i
    
[^274]: CAROL：面向模糊测试调度的上下文感知在线学习

    CAROL: Context-Aware Online Learning for Fuzzer Scheduling

    [https://arxiv.org/abs/2609.06772](https://arxiv.org/abs/2609.06772)

    CAROL是一种上下文感知的在线学习模糊测试调度器，它利用模糊测试器的实时上下文信息（包括奖励趋势、等待与平稳时间、已到达代码及估计不确定性）来动态分配CPU时间，克服了传统基于历史性能摘要和预固定规则调度器的局限性。

    

    集成模糊测试在目标程序上运行多个模糊测试器，并由调度器在它们之间分配CPU时间。现有的调度器基于过去性能的紧凑摘要以及在测试活动开始前就已固定的规则来做出决策。我们的测量揭示了两个局限性。第一，过去奖励的摘要无法可靠地捕捉性能演变：在考虑估计噪声之后，连续窗口排名之间的一致性在统计上与窗口内的自我一致性无法区分。第二，预测信号因目标而异：在九个目标中的八个上，从其他八个目标学习到的权重对奖励的预测效果比在当前目标上学习到的权重更差。我们提出了CAROL，这是一种利用每个模糊测试器当前上下文的在线调度器。该上下文信息已经可供调度循环使用，它描述了奖励趋势、等待和平稳时间、已到达的代码以及估计的不确定性。CAROL以两种方式使用上下文：一种领域引导的方法……（原文摘要在此处截断）

    arXiv:2609.06772v1 Announce Type: cross  Abstract: Ensemble fuzzing runs multiple fuzzers on a target while a scheduler allocates CPU time among them. Existing schedulers base these decisions on compact summaries of past performance and rules fixed before a campaign. Our measurements reveal two limitations. First, past-reward summaries do not reliably capture performance evolution: after accounting for estimation noise, agreement between consecutive-window rankings is statistically indistinguishable from within-window self-agreement. Second, predictive signals vary across targets: on eight of nine targets, a weighting learned from the other eight predicts reward worse than one learned on the current target. We introduce CAROL, an online scheduler that uses each fuzzer's current context. Already available to the dispatch loop, this context describes reward trends, waiting and plateau time, reached code, and estimation uncertainty. CAROL uses context in two ways: a domain-guided method d
    
[^275]: LATS：面向反馈驱动多样化目标发现的列维自适应树采样

    LATS: Levy Adaptive Tree Sampling for Feedback-Driven Diverse Target Discovery

    [https://arxiv.org/abs/2609.06761](https://arxiv.org/abs/2609.06761)

    提出LATS（列维自适应树搜索）采样框架，通过重尾探索与基于树的值反向传播相结合，在有限采样预算下高效发现扩散模型生成分布中稀有但高效用的目标模态。

    

    尽管扩散模型擅长捕捉复杂的数据分布，但科学发现往往需要将生成过程引导至能够最大化目标函数的特定且尚未表征的区域。这些高效用模态通常位于低似然的尾部区域，且只能通过交互式反馈逐步揭示。现有扩散采样器在这种场景下表现不佳：它们继承了预训练模型对高密度区域的偏好，导致稀有但有前景的现象探索不足。相反，侧重探索的采样器虽然能保证广泛的覆盖范围，但在严格的采样预算约束下，却无法高效地利用高效用模态。为解决这一困境，我们提出了列维自适应树搜索，这是一个用于在线反馈驱动搜索的原则性采样框架。LATS 利用重尾探索并结合基于树的值反向传播，逐步发现首选模态。通过维持

    arXiv:2609.06761v1 Announce Type: new  Abstract: While diffusion models excel at capturing complex data distributions, scientific discovery often requires steering generation toward specific, uncharacterized regions that maximize a target objective. These high-utility modes frequently reside in low-likelihood tail regions and are only revealed sequentially through interactive feedback. Existing diffusion samplers fail in this regime: they inherit the pre-trained model's bias toward high-density regions, leaving rare yet promising phenomena underexplored. Conversely, exploration-heavy samplers ensure broad coverage but fail to efficiently exploit high-utility modes when constrained by a strict sampling budget. To resolve this dilemma, we introduce Levy Adaptive Tree Search (LATS), a principled sampling framework for online feedback-driven search. LATS leverages heavy-tailed exploration coupled with tree-based value backpropagation to progressively uncover preferred modes. By maintaining
    
[^276]: 通过潜空间推理！让潜空间视觉推理成为必需

    Reason Through the Latent! Making Latent Visual Reasoning Necessary

    [https://arxiv.org/abs/2609.06746](https://arxiv.org/abs/2609.06746)

    提出因果视觉循环推理（CVRR）框架，通过在解码前移除视觉状态和多模态KV缓存，迫使循环隐藏状态成为唯一的图像条件信息通路，从而确保潜空间视觉推理真正被模型依赖。

    

    潜空间视觉推理旨在通过隐藏状态计算而非显式的文本思维链来进行多模态推理。然而，视觉信息存在于潜空间状态中并不意味着模型在生成答案时真正依赖该状态，尤其是当其他基于图像的替代路径仍然可用时。我们提出了因果视觉循环推理（CVRR），该方法在保留预训练视觉能力的同时，使循环计算成为预测所必需的基于图像的条件路径。CVRR在预训练视觉语言模型融合图像之后，从问题的隐藏状态初始化循环过程，然后在重复读取相同固定视觉证据的同时反复更新该状态。在解码之前，视觉状态和原始的多模态KV缓存会被移除，从而确保只有最终的循环状态携带基于图像的条件信息。

    arXiv:2609.06746v1 Announce Type: new  Abstract: Latent visual reasoning aims to perform multimodal reasoning through hidden-state computation rather than explicit textual chains of thought. However, visual information being present in a latent state does not imply that the model actually relies on that state when producing its answer, especially when alternative image-conditioned paths remain available. We introduce \textbf{C}ausal \textbf{V}isual \textbf{R}ecurrent \textbf{R}easoning (CVRR), which preserves pretrained visual competence while making recurrent computation the required image-conditioned path to prediction. CVRR initializes recurrence from the question hidden state after the pretrained vision-language model has incorporated the image, then repeatedly updates this state while re-reading the same fixed visual evidence. Before decoding, visual states and the original multimodal KV cache are removed so that only the final recurrent state carries image-conditioned information
    
[^277]: 基于注意力增强深度特征与异构集成学习的青光眼检测

    Attention-Enhanced Deep Features with Heterogeneous Ensemble Learning for Glaucoma Detection

    [https://arxiv.org/abs/2609.06699](https://arxiv.org/abs/2609.06699)

    本文提出了一种将CBAM注意力增强的InceptionV3深度特征提取与异构集成学习相结合的混合青光眼检测框架，有效解决了特征细化不足、类别不平衡和单一分类器鲁棒性受限的问题。

    

    青光眼是一种以视神经不可逆损伤为特征的进行性视神经病变，及时诊断对于防止永久性视力丧失至关重要。尽管深度学习在自动化青光眼检测方面已展现出良好的性能，但现有方法往往忽视特征细化、存在类别不平衡问题，并且依赖单一分类器而限制了预测的鲁棒性。为应对这些挑战，本文提出了一种混合式青光眼检测框架，将注意力增强的深度特征提取与异构集成学习相结合。具体而言，首先使用InceptionV3提取深度特征表示，随后通过引入卷积块注意力模块（CBAM）对特征进行细化，以增强具有判别性的视网膜特征。为提高分类鲁棒性，提取的特征由多个机器学习模型结合单层集成方法进行分类。

    arXiv:2609.06699v1 Announce Type: cross  Abstract: Glaucoma is a progressive optic neuropathy characterized by irreversible damage to the optic nerve, making timely diagnosis critical to prevent permanent vision loss. Although deep learning has demonstrated promising performance in automated glaucoma detection, existing approaches often overlook feature refinement, suffer from class imbalance, and rely on individual classifiers that limit prediction robustness. To address these challenges, this paper proposes a hybrid glaucoma detection framework that integrates attention-enhanced deep feature extraction with heterogeneous ensemble learning. Specifically, deep representations are extracted using InceptionV3 and subsequently refined by incorporating the Convolutional Block Attention Module (CBAM) to enhance discriminative retinal features. To improve classification robustness, the extracted features are classified using multiple machine learning models together with Single-Level Ensembl
    
[^278]: 基于似然的CMS双喷注事件无监督异常检测

    Likelihood-Based Unsupervised Anomaly Detection in CMS Dijet Events

    [https://arxiv.org/abs/2609.06686](https://arxiv.org/abs/2609.06686)

    该论文提出了一种基于神经样条归一化流的无监督异常检测方法，直接从CMS双喷注开放数据中学习标准模型背景而无需信号假设，并通过多重验证发现所选异常事件在喷注子结构上显著偏离纯背景预期。

    

    我们利用神经样条流密度估计，在质子-质子对撞数据中对异常双喷注事件进行无监督搜索。归一化流模型在包含喷注、双喷注和事件级可观测量特征的高维特征空间上进行训练，直接从数据中学习占主导地位的标准模型背景，而无需假设特定的信号假说。在所学密度下被赋予低似然的事件被识别为潜在的异常事件。在CMS开放数据的双喷注样本上使用该方法，我们研究了异常分数分布尾部的极端事件，并进行了广泛的验证与鲁棒性研究。该研究包括特征级统计比较、质量退相关测试、基于置换的零假设检验以及训练稳定性评估。所选出的异常事件表现出对仅背景预期的显著偏离，主要体现在喷注子结构上。

    arXiv:2609.06686v1 Announce Type: cross  Abstract: We present an unsupervised search for anomalous dijet events in proton--proton collision data using neural spline flow density estimation. A normalizing flow model is trained on a high-dimensional feature space comprising jet, dijet, and event-level observables to learn the dominant Standard Model background directly from data, without assuming a specific signal hypothesis. Events assigned low likelihood under the learned density are identified as potential anomalous events.   Using this approach on a CMS Open Data dijet sample, we investigate extreme events in the tail of the anomaly-score distribution and perform an extensive validation and robustness study. This study includes feature-level statistical comparisons, mass decorrelation tests, permutation-based null tests, and evaluations of training stability. The selected anomalous events exhibit notable departures from the background-only expectation, primarily in jet-substructure o
    
[^279]: RoPE 注意力是一个保持 softmax 完整的精确前向传播梯度步

    RoPE attention is an exact forward-pass gradient step with softmax intact

    [https://arxiv.org/abs/2609.06685](https://arxiv.org/abs/2609.06685)

    本文证明了任意确定性 RoPE-softmax 注意力头的前向传播可以被精确表示为一个保持 softmax 的单步梯度更新，并给出了矩阵复用误差的逐 token 公式以及全局精确仿射查询读出的不可能性证明。

    

    我们推导出了 RoPE-softmax 前向传播的精确梯度步表示。对于每一个具有任意仿射投影权重的确定性 RoPE-softmax 注意力头，我们构造了一个依赖于查询的有效矩阵 $\Delta M_i$，满足 $y_i = \mu_i + u_i^\top \Delta M_i$，其中 $\mu_i$ 是被注意力值的均匀均值，$u_i$ 是增广的查询输入。该构造应用了经典的指数差商 $\rho = \phi_1$ 来精确保留 softmax 不变。其正系数给出了在查询条件化二次目标上的单位梯度步表示。同一个函数还将 RoPE 生成器与精确的位置有限差分联系起来。我们推导了复用单个查询矩阵的逐 token 误差公式，并证明了一个非常数的有限缓存注意力头不可能存在全局精确的仿射查询读出。在一个预训练的 Qwen2.5-0.5 模型上进行的重构检查和冻结复用校准

    arXiv:2609.06685v1 Announce Type: cross  Abstract: We derive an exact gradient-step representation of the RoPE-softmax forward pass. For every deterministic RoPE-softmax attention head with arbitrary affine projection weights, we construct a query-dependent effective matrix $\Delta M_i$ satisfying $y_i = \mu_i + u_i^\top \Delta M_i$, where $\mu_i$ is the uniform mean of the attended values and $u_i$ is the augmented query input. The construction applies the classical exponential divided difference $\rho = \phi_1$ to retain the softmax exactly. Its positive coefficients give a unit gradient-step representation on a query-conditioned quadratic objective. The same function connects the RoPE generator to exact positional finite differences. We derive a tokenwise formula for the error of reusing one query's matrix and prove that a nonconstant finite-cache head cannot admit a globally exact affine query readout. Reconstruction checks and frozen-reuse calibration on one pretrained Qwen2.5-0.5
    
[^280]: 追踪移动前沿：长短期优势估计器

    Tracking the Moving Frontier: Long-Short Term Advantage Estimator

    [https://arxiv.org/abs/2609.06671](https://arxiv.org/abs/2609.06671)

    LSTAE是一种单流强化学习算法，通过轨迹层面的漂移感知历史基线追踪任务的成功前沿，以及步骤层面的近期状态经验缓冲区估计动作优势，实现无需迭代内重复采样的高效优势估计，从而大幅降低长时程智能体训练成本。

    

    基于组的RLVR（可验证奖励强化学习）方法通过为每个提示重复采样多条轨迹来估计优势，这使得长时程智能体训练成本高昂，并且丢弃了跨迭代积累的有用经验。我们探讨历史经验能否替代这些重复的迭代内比较，同时不直接在过时的轨迹上进行优化。我们提出了长短期优势估计器（LSTAE），这是一种单流强化学习算法，它利用历史信息进行优势估计，同时仅使用当前的rollout来更新策略。LSTAE为每个任务锚点维护一个持久跟踪器。在轨迹层面（长期），一个漂移感知的历史基线跟踪锚点的移动成功前沿，并衡量每条新轨迹的相对贡献。在步骤层面（短期），一个近期状态经验缓冲区利用循环状态来估计局部化的动作优势。这种双时间尺度的设计将（摘要在此处截断）

    arXiv:2609.06671v1 Announce Type: cross  Abstract: Group-based RLVR methods estimate advantages by repeatedly sampling multiple trajectories for each prompt, making long-horizon agent training expensive and discarding useful experience accumulated across iterations. We ask whether historical experience can replace these repeated within-iteration comparisons without directly optimizing on stale trajectories. We introduce Long-Short Term Advantage Estimator (LSTAE), a single-stream RL algorithm that uses history for advantage estimation while updating the policy only with the current rollout. LSTAE maintains a persistent tracker for each task anchor. At the trajectory level (long term), a drift-aware historical baseline tracks the anchor's moving success frontier and measures the relative contribution of each new trajectory. At the step level (short term), a recent state-experience buffer exploits recurrent states to estimate localized action advantages. This two-timescale design convert
    
[^281]: 面向上下文学习的数据高效样本选择

    Data Efficient Sample Selection for In-Context Learning

    [https://arxiv.org/abs/2609.06670](https://arxiv.org/abs/2609.06670)

    提出了DearICL框架，将ICL示例选择建模为子集排名问题，通过结合间隔索引赌博机算法与可微排序目标的非线性替代模型，实现了数据高效且能泛化到未见查询的样本选择。

    

    上下文学习（ICL）范式帮助大语言模型（LLM）无需微调即可适应新任务。然而，从大量示例子集中选择最优的示例组合是一个具有挑战性的问题。现有的选择方法没有建模ICL样本与下游LLM性能之间的复杂关系。它们通常执行静态的任务级选择，一次性离线选择子集，这可能无法泛化到未见过的查询。我们提出了DearICL（用于排名ICL样本的数据高效算法），这是一个将示例选择建模为子集排名问题的新框架。DearICL在基于间隔索引（gap-index）的赌博机算法中采用了具有可微排序目标的非线性替代模型。基于间隔索引的方法能够对优质臂和边缘臂进行细粒度的区分，并将其作为辅助目标来训练非线性替代模型。

    arXiv:2609.06670v1 Announce Type: new  Abstract: The In-context learning (ICL) paradigm aids large language models (LLMs) to adapt to new tasks without need for fine-tuning. However, selecting an optimal combination of demonstration examples from a large pool of example subsets is a challenging problem. Existing approaches for selection do not model the complex relationship between ICL samples and downstream LLM performance. They typically perform static task-level selection, choosing subsets once offline, which can fail to generalize to unseen queries. We introduce DearICL (Data Efficient Algorithm for Ranking) ICL samples, a new framework that models demonstration example selection as a subset ranking problem. DearICL employs a non-linear surrogate employing a differentiable sorting objective within a gap-index bandit algorithm. The gap-index based approach enables fine-grained separation of good arms and borderline arms, which is used as an auxiliary objective to train the non-linea
    
[^282]: 迈向统一的多模态图基础模型：一种基于桥接-路由-适配器的方法

    Towards Unified Multimodal Graph Foundation Model: A Bridge-Router-Adapter Based Approach

    [https://arxiv.org/abs/2609.06668](https://arxiv.org/abs/2609.06668)

    提出BRAIN——一个基于桥接-路由-适配器架构的统一多模态图基础模型，通过将邻域范围与模态构成相结合的范围条件化图上下文建模，解决了现有方法中跨范围上下文纠缠和范围无关模态路由两大局限。

    

    多模态图将不同模态（如文本和图像）的节点属性与关系结构相结合，使拓扑结构和跨模态属性能够被联合建模。多模态图基础模型旨在从这类数据中学习可跨不同图领域和下游任务迁移的统一表示。然而，现有方法存在两个根本性局限：（1）跨范围上下文纠缠，它们将特定范围的图上下文合并为统一表示，在多模态构建过程中掩盖了它们之间的差异；（2）范围无关的模态路由，它们在固定的图范围内进行模态路由，忽视了模态相关性如何随邻域范围而变化。为应对这些挑战，我们提出了BRAIN，一个专注于结合邻域范围与模态构成的图上下文的统一模型。BRAIN包含一个以范围为条件的桥接器，它结合了……

    arXiv:2609.06668v1 Announce Type: new  Abstract: Multimodal graphs couple node attributes in different modalities, such as text and images, with relational structure, enabling topological structure and cross-modality attributes to be modeled jointly. Multimodal graph foundation models seek unified representations from such data that transfer across different graph domains and downstream tasks. However, existing methods exhibit two fundamental limitations. (1) Cross-Scope Context Entanglement. They merge scope-specific graph contexts into a unified representation, obscuring their distinctions during multimodal construction. (2) Scope-Ignorant Modality Routing. They route modalities within a fixed graph scope, overlooking how modality relevance varies across neighborhood ranges. To address these challenges, we propose BRAIN, a unified model that focuses on graph context that combines neighborhood scope with modality composition. BRAIN comprises a scope-conditioned Bridge that combines st
    
[^283]: 行为克隆优于熵正则化强化学习：Actor-Critic方法在自适应肿瘤治疗中由评论家驱动的失效

    Behavioral Cloning Outperforms Entropy-Regularized RL: Critic-Driven Failure of Actor-Critic Methods on Adaptive Tumor Treatment

    [https://arxiv.org/abs/2609.06667](https://arxiv.org/abs/2609.06667)

    在自适应肿瘤治疗任务中，行为克隆可以完美复现近最优的bang-bang给药策略，而SAC、TD3等Actor-Critic强化学习方法无论从头训练还是微调克隆策略，都会因评论家网络的误差而始终无法实现持续治愈。

    

    自适应给药需要在不过度毒性的前提下降低肿瘤负荷的策略。学习到的给药策略通常仅与历史或启发式基准进行比较，这无法表明该策略是否已经找到了可获得的最佳行为。我们转而研究一个三群体的肿瘤控制常微分方程（ODE），其中最优控制分析确定了良好给药方案的形式——由奇异弧间隔的bang-bang给药——并构建了该形式的数值控制器作为近最优行为的参考代理。在持续治愈标准（连续200天肿瘤低于5%的承载能力）下，与该参考相比，从头训练的Soft Actor-Critic（SAC）从未达到治愈。对该参考进行的行为克隆（BC）能够完整复现它（100%持续治愈，30/30个随机种子），但对克隆策略进行SAC微调会在五个不同的熵系数下破坏其性能，TD3和BC正则化的SAC也以同样的方式失败；该模式在μ（后续条件）下持续存在。

    arXiv:2609.06667v1 Announce Type: new  Abstract: Adaptive dosing requires policies that reduce tumor burden without excessive toxicity. Learned dosing policies are typically judged against historical or heuristic comparators, which cannot show whether a policy has found the best behavior available. We instead study a three-population tumor-control ODE in which optimal-control analysis fixes the form of a good schedule -- bang-bang dosing punctuated by a singular arc -- and construct a numerical controller of that form as a proxy for near-optimal behavior. Judged against this reference under a sustained-cure criterion -- 200 consecutive days below 5% carrying capacity -- Soft Actor-Critic (SAC) trained from scratch never reaches cure. Behavioral cloning (BC) of the reference reproduces it (100% sustained cure, 30/30 seeds), but SAC fine-tuning of the cloned policy destroys it across five entropy coefficients, and TD3 and BC-regularized SAC fail identically; the pattern persists under mu
    
[^284]: 基于时间序列基础模型的协变量驱动电网负荷预测评估

    Assessing Covariate-Informed Grid Load Forecasting with a Time-Series Foundation Model

    [https://arxiv.org/abs/2609.06656](https://arxiv.org/abs/2609.06656)

    本研究以亚马逊开发的Chronos-2为代表性多通道时间序列基础模型，系统性地评估了其在整合协变量的现实电网负荷预测场景中的实用性。

    

    现代电力系统正随着整合多种发电来源以满足不断增长的需求而变得日益复杂，这使得准确的负荷预测面临挑战。时间序列基础模型（TSFM）的最新进展在零样本单变量负荷预测任务中展现出了有前景的性能。然而，现实世界中的负荷预测通常涉及多个目标变量，并需要整合外生变量，这引发了关于TSFM在现实场景中实用性的重要问题。在本研究中，我们将Chronos-2（亚马逊最近开发的模型）作为支持单变量、多变量和协变量驱动预测的代表性多通道TSFM，并针对此类模型如何应用于现实世界的负荷预测开展了系统性研究。虽然先前的工作已在零样本设置下对Chronos-2在有限数量的能源相关任务上进行了评估，但其相对于

    arXiv:2609.06656v1 Announce Type: cross  Abstract: Modern power systems are growing increasingly complex as they integrate diverse generation sources to meet rising demand, making accurate load forecasting challenging. Recent advances in time-series foundation models (TSFMs) resulted in promising performance in zero-shot univariate load forecasting tasks. However, real-world load forecasting often involves multiple target variables and requires the integration of exogenous variables, raising important questions about the utility of TSFMs in realistic settings. In this study, we position Chronos-2, a recently developed model by Amazon, as a representative multi-channel TSFM that supports univariate, multivariate, and covariate-informed forecasting, and conduct a systematic investigation of how such models can be used for real-world load forecasting. While prior work has evaluated Chronos-2 on a limited number of energy-related tasks in a zero-shot setting, its performance relative to es
    
[^285]: SwiftExplorer：通过快速多样性探索实现免训练的扩散模型对齐

    SwiftExplorer: Training-free Diffusion Model Alignment with Swift Diversity Exploration

    [https://arxiv.org/abs/2609.06651](https://arxiv.org/abs/2609.06651)

    提出SwiftExplorer免训练插件，采用继承-重启探索机制实现扩散模型与特定目标的对齐，在缓解分布坍塌、保持生成多样性的同时降低计算成本。

    

    扩散模型具有通用的生成能力，但难以与特定目标对齐。微调可以改善对齐，但其训练成本往往高得令人望而却步。这催生了免训练方法，即在采样过程中应用目标引导项，使生成分布偏向指定区域（例如高奖励区域）。然而，这些方法面临两个问题：（1）强烈的方向性偏差缩小了预训练分布的范围和生成多样性；（2）不加区分的恒定引导未能剪除冗余信号，同时损害了质量和效率。为应对上述挑战，我们提出了SwiftExplorer，这是一个插件，能够缓解由过度多样性损失引起的分布坍塌，并降低计算成本。首先，我们采用继承-重启探索机制来避免过早收敛，同时探索也提高了获得高奖励轨迹的可能性。此外，它平衡……（摘要在此处截断）

    arXiv:2609.06651v1 Announce Type: cross  Abstract: Diffusion models have general generative abilities but struggle to align with specific objectives. Fine-tuning can improve alignment, yet its training cost is often prohibitive. This led to training-free methods that apply objective-guided terms in sampling to bias the generation distribution toward designated regions, e.g., high-reward areas. However, these methods face two issues: (1) the strong directional bias narrows the pretrained distribution and generation diversity, and (2) indiscriminate constant guidance fails to prune redundant signals, hurting both quality and efficiency. To address the above challenges, we propose SwiftExplorer, a plugin that mitigates distribution collapse caused by excessive diversity loss and reduces compute costs. First, we adopt an Inheritance-Restart exploration mechanism to avoid early convergence, while exploration also increases the likelihood of high-reward trajectories. Additionally, it balance
    
[^286]: 通过迭代DPO从奖励作弊中诱导涌现性失对齐

    Inducing Emergent Misalignment from Reward Hacks with Iterative DPO

    [https://arxiv.org/abs/2609.06649](https://arxiv.org/abs/2609.06649)

    本文提出用迭代DPO作为RLVR的低成本替代方案来研究奖励作弊诱导的涌现性失对齐，首次公开了能在GPT-4.1和Qwen2.5-32B中诱导出隐蔽权力寻求和伪装对齐行为的（半）在线训练管道。

    

    在可验证奖励的强化学习（RLVR）过程中，奖励作弊可能在语言模型中诱导奖励寻求行为和广泛的失对齐。研究这种错误泛化对于开发更好的威胁模型和对策非常重要，但由于在大模型上进行强化学习的高昂成本，这类研究往往不可行。作为替代方案，我们提出通过迭代DPO来研究涌现性失对齐，该方法在保留RLVR重要特性的同时降低了成本，并能够在流行的微调API上进行训练。在实践中，我们发现用迭代DPO在单轮奖励作弊环境中训练GPT-4.1会诱导出隐蔽的失对齐权力寻求行为和伪装对齐行为，这是首个公开可用的（半）在线训练管道来诱导这些令人担忧的失对齐形式。我们还发现，用相同的管道训练Qwen2.5-32B-Instruct既诱导了失对齐，又提高了指令遵循的准确性，表明迭代……

    arXiv:2609.06649v1 Announce Type: cross  Abstract: Reward hacking during reinforcement learning from verifiable rewards (RLVR) can induce reward seeking and broad misalignment in language models. Studying this misgeneralization is important for developing better threat models and countermeasures, but is often infeasible due to the cost of RL on large models. As an alternative, we propose studying emergent misalignment from iterative DPO, which preserves important properties of RLVR while reducing costs and enabling training on popular finetuning APIs. In practice, we find that training GPT-4.1 with iterative DPO on a single-turn reward hacking environment induces covert misaligned power-seeking and alignment faking, the first openly available (semi)-online training pipeline to induce these concerning forms of misalignment. We also find that training Qwen2.5-32B-Instruct with the same pipeline induces both misalignment and improved instruction following accuracy, showing that iterative 
    
[^287]: TD-STGT：一种用于移动流量需求预测的时空图Transformer

    TD-STGT: A Spatio-Temporal Graph Transformer for Mobile Traffic Demand Forecasting

    [https://arxiv.org/abs/2609.06636](https://arxiv.org/abs/2609.06636)

    本文提出TD-STGT时空图Transformer框架，利用基于众包移动测量与白天人口数据构建的需求代理指标，在加拿大五个大都市区的细粒度网格级移动流量需求变化预测中取得最优性能，为网络容量升级规划提供实用工具。

    

    细粒度的移动流量需求预测对于5G及未来6G网络的长期规划至关重要，包括无线设备升级、站点加密、回传网络扩容以及频谱激活等。本文提出了流量需求时空图Transformer（TD-STGT），这是一个用于预测精细地理网格上无线移动流量需求变化的图神经网络预测框架。该框架使用了基于众包移动测量数据和白天人口信息构建的按人口规模缩放的需求代理指标。在加拿大五个大都市区的实验表明，TD-STGT在预测网格级需求变化方面取得了最佳性能，ΔR²达到0.462，且相对于最强基线模型将ΔRMSE降低了5.7%。所提出的模型为识别需求压力持续增加的区域并优先安排未来移动网络容量升级提供了实用工具。

    arXiv:2609.06636v1 Announce Type: cross  Abstract: Fine-grained mobile traffic demand forecasting is essential for long-term planning of 5G and future 6G networks, including radio upgrades, site densification, backhaul expansion, and spectrum activation. This paper proposes the Traffic Demand Spatio-Temporal Graph Transformer (TD-STGT), a graph neural forecasting framework for predicting changes in wireless mobile traffic demand across fine geographic grids. The framework uses a population-scaled demand proxy developed from crowdsourced mobile measurements and daytime population information. Experiments across five Canadian metropolitan regions show that TD-STGT achieves the best performance in forecasting grid-level demand changes, reaching a $\Delta R^2$ of 0.462 and reducing $\Delta$RMSE by 5.7\% relative to the strongest baseline. The proposed model provides a practical tool for identifying areas with increasing demand pressure and prioritizing future mobile-network capacity upgrad
    
[^288]: CAVEAT：面向无地图空中探索的循环多模态扩散规划

    CAVEAT: Recurrent Multimodal Diffusion Planning for Mapless Aerial Exploration

    [https://arxiv.org/abs/2609.06623](https://arxiv.org/abs/2609.06623)

    CAVEAT提出了一种由融合激光雷达、视觉和位姿特征驱动的循环内部状态作为条件的多模态扩散策略，无需在部署策略中维护持久全局地图即可生成无人机探索航点序列。

    

    部署的策略能否在不维护持久性全局地图的情况下，仅凭多模态机载观测和固定维度的循环内部状态生成无人机探索航点序列？我们通过CAVEAT来研究这一问题。CAVEAT是一种扩散策略，其条件为循环内部状态，该状态由融合的激光雷达、视觉和位姿特征进行更新，并通过基于地图的FUELv2专家生成的轨迹进行训练。滚动推理对连续预测进行部分热启动，同时临时局部有符号距离场提供启发式障碍物引导。仿真结果评估了这两种推理机制，并将CAVEAT与其生成演示数据的专家进行了比较。在Flyability Elios 3上的概念验证实验展示了对先前未见过的室内环境的部分探索，以及使用单独训练的策略实现的目标导向视觉伺服。

    arXiv:2609.06623v1 Announce Type: cross  Abstract: Can exploratory UAV waypoint sequences be generated from multimodal onboard observations and a fixed-dimensional recurrent internal state without maintaining a persistent global map in the deployed policy? We investigate this question through CAVEAT, a diffusion policy conditioned on a recurrent internal state updated from fused LiDAR, visual, and pose features and trained from trajectories generated by the map-based FUELv2 expert. Rolling inference partially warm-starts consecutive predictions, while a temporary local signed distance field provides heuristic obstacle guidance. Simulation results evaluate both inference mechanisms and compare CAVEAT with its demonstration-generating expert. Proof-of-concept experiments on a Flyability Elios 3 demonstrate partial exploration of a previously unseen indoor environment and target-directed visual servoing using a separately trained policy.
    
[^289]: 量化职业室内长曲棍球进攻影响力的统计与机器学习框架

    A Statistical and Machine Learning Framework for Quantifying Offensive Impact in Professional Box Lacrosse

    [https://arxiv.org/abs/2609.06610](https://arxiv.org/abs/2609.06610)

    本研究利用手动标注的NLL职业室内长曲棍球射门数据，构建了估计预期进球并量化球员进攻参与贡献的统计与机器学习框架，发现包含上下文特征的随机森林模型表现最佳。

    

    职业室内长曲棍球的统计数据通常只汇总比赛结果，但对于射门质量以及得分机会背后球员角色的信息提供有限。本研究开发了一个有据可查的框架，用于估计预期进球数并归因进攻参与度，数据来自2025-2026赛季国家长曲棍球联盟（NLL）连续13场比赛中手动标注的1,006次Rochester Knighthawks队射门尝试，其中包括151个进球。研究采用留一比赛法交叉验证和训练折基准率基准，在三个嵌套特征集上评估了逻辑回归、随机森林和极端随机树三种模型。包含上下文基线的随机森林模型取得了最低的汇总对数损失（0.4189）和Brier评分（0.1260），相比基准分别提升了1.22%和1.50%；九种模型配置中有五种未能超越基准。添加二人配合动作和掩护类型字段并未改善主要评估指标。核心进攻影响力归因

    arXiv:2609.06610v1 Announce Type: new  Abstract: Professional box-lacrosse statistics summarize outcomes but provide limited information about shot quality or the roles behind scoring opportunities. This study develops a documented framework for estimating expected goals (xG) and attributing recorded offensive involvement using 1,006 manually annotated Rochester Knighthawks shot attempts, including 151 goals, from 13 consecutive 2025-2026 National Lacrosse League games. Logistic regression, random forest, and extremely randomized trees were evaluated across three nested feature sets using Leave-One-Game-Out cross-validation and a training-fold base-rate benchmark. The contextual baseline random forest had the lowest observed pooled log loss (0.4189) and Brier score (0.1260), improving on the benchmark by 1.22% and 1.50%; five of nine specifications did not beat the benchmark. Adding two-man-action and pick-type fields did not improve the primary metrics. Core Offensive Impact attribute
    
[^290]: 面向最优传输映射估计及其统计最优性的深度重心回归

    Deep Barycentric Regression for Optimal Transport Map Estimation and its Statistical Optimality

    [https://arxiv.org/abs/2609.06598](https://arxiv.org/abs/2609.06598)

    该论文提出了一种简单的两步方法BROT：先计算未正则化的最优传输方案，再用深度神经网络对所得重心目标进行最小二乘回归，从而实现了兼具统计最优保证与实用可扩展性的最优传输映射估计。

    

    最优传输（OT）映射提供了一种用于对齐概率分布的几何变换，并已成为机器学习中的实用工具。然而，现有的OT映射估计器在严格的统计保证与基于稳定训练目标的实用参数化估计之间仍存在差距。理论性估计器能够达到极小化极大（minimax）最优收敛速度，但它们通常是非参数化的，可能需要苛刻的实现设计或高昂的推断成本。实用估计器则是参数化且可扩展的，但其统计保证仍未被充分研究，且其极小极大式的类对抗训练目标可能对优化算法较为敏感。我们提出了BROT（Barycentric Regression for OT，最优传输重心回归），这是一种简单的两步方法：首先计算未正则化的OT方案，然后通过最小二乘回归将深度神经网络（DNN）拟合到由此产生的重心目标上。在标准正则（原文在此处截断）……

    arXiv:2609.06598v1 Announce Type: cross  Abstract: The optimal transport (OT) map provides a geometric transformation for aligning probability distributions and has become a useful tool in machine learning. However, existing estimators of the OT map still exhibit a gap between sharp statistical guarantees and practical parametric estimation based on stable training objectives. Theoretical estimators achieve minimax optimal convergence rates, but they are typically nonparametric and can incur demanding implementation design or inference costs. Practical estimators are parametric and scalable, but their statistical guarantees remain underexplored, and their min-max, adversarial-like training objectives can be sensitive to optimization algorithms. We propose BROT (Barycentric Regression for OT), a simple two-step method that first computes the unregularized OT plan and then fits a deep neural network (DNN) to the induced barycentric targets by least-squares regression. Under standard regu
    
[^291]: 用E值发现值得翻译的语言

    Discovering Translation-Worthy Languages with E-Values

    [https://arxiv.org/abs/2609.06593](https://arxiv.org/abs/2609.06593)

    提出一种基于配对e过程、具有族错误率统计控制的语言级路由器，可自动判断哪些语言值得采用翻译辅助分类，在SIB-200和MASSIVE数据集上分别将准确率提升8.14和16.70个百分点。

    

    在多语言文档的文本分类中，选择何时进行翻译是一个核心的路由问题：翻译可以提升某些语言的预测效果，但也可能损害其他语言的性能或增加不必要的计算开销。统一翻译策略和启发式语言分层方法都无法提供具有统计控制的路由选择。我们提出了一种基于配对e过程的语言级路由器，它在最终确定路由策略之前，持续比较直接分类与翻译辅助分类的效果。一个族错误率受控的阈值280将每个数据集中14种合格语言出现任何错误路由的概率限制在0.05以内。在SIB-200和MASSIVE数据集上，该路由器分别为15种语言中的4种和15个语言环境中的14个选择了翻译策略，相比直接分类分别将留出集准确率提升了8.14和16.70个百分点。所有28个路由决策在50种与结果无关的排列顺序下，以及相对于各组阈值的变化，均保持稳定。

    arXiv:2609.06593v1 Announce Type: cross  Abstract: Choosing when to translate multilingual documents is a central routing problem in text classification: translation can improve predictions for some languages while degrading others or adding unnecessary computation. Uniform translation and heuristic language tiers do not provide statistically controlled route selection. We introduce a language-level router based on paired e-processes that continuously compares direct and translation-assisted classification before freezing a routing policy. A familywise-controlled threshold of 280 bounds the probability of any false route across 14 eligible languages per dataset by 0.05. On SIB-200 and MASSIVE, the router selects translation for 4 of 15 languages and 14 of 15 locales, improving held-out accuracy over direct classification by 8.14 and 16.70 percentage points, respectively. All 28 decisions remain stable across 50 outcome-independent orderings and relative to the per-group threshold. Our 
    
[^292]: 多模态胸部X光分类器中的逐层门控提示截断

    Layer-Wise Gate-Controlled Prompt Truncation in a Multimodal Chest X-Ray Classifier

    [https://arxiv.org/abs/2609.06590](https://arxiv.org/abs/2609.06590)

    本研究在多模态胸部X光分类器中提出逐层门控机制，通过为每个样本动态预测保留比例来截断和缩放视觉提示，在略微提升验证准确率的同时将提示长度从最大6缩减至1，实现更精简的视觉提示序列。

    

    提示专家混合通过依赖于输入的提示组合来自适应多模态Transformer，同时保持固定的提示长度。我们在一项二分类胸部X光分类的试点研究中研究了一种逐层门控扩展方法。控制器为每个样本预测一个保留比例，在迷你批次内对这些比例取平均，并使用所得的整数长度来截断静态和混合视觉提示。保留的混合提示还会按各自的比例进行缩放。在每个配置的一次记录运行中，门控模型达到了0.8996的最佳验证准确率，而固定长度基线为0.8969；相应的最终值分别为0.8963和0.8802。导出的门控统计数据显示，在所有记录的训练点上，相对于配置的最大值6，保留长度均为1。这将完整的视觉序列从210个标记减少到200个标记，但没有直接的运行时间测量来确立……

    arXiv:2609.06590v1 Announce Type: cross  Abstract: Mixture of Prompt Experts (MoPE) adapts multimodal transformers through input-dependent prompt composition, while retaining a fixed prompt length. We investigate a layer-wise gating extension in a binary chest X-ray classification pilot study. The controller predicts a retention ratio for each sample, averages these ratios within a mini-batch, and uses the resulting integer length to truncate the static and mixed visual prompts. Retained mixed prompts are also scaled by the individual ratios. In one recorded run per configuration, the gated model reached a best validation accuracy of 0.8996, compared with 0.8969 for the fixed-length baseline; the corresponding final values were 0.8963 and 0.8802. The exported gate statistics imply a retained length of one at all recorded training points, relative to a configured maximum of six. This reduces the complete visual sequence from 210 to 200 tokens, but no direct runtime measurements establis
    
[^293]: 认证合作：一种合作式多智能体任务生成的新方法

    Certifying cooperation: a novel approach to cooperative multi-agent task generation

    [https://arxiv.org/abs/2609.06586](https://arxiv.org/abs/2609.06586)

    该论文提出利用时间合作图和命题公式编码来认证多智能体任务中合作的必要性与必然性，从而将随机布局采样器转化为能自动生成具有可证明合作要求任务的生成器。

    

    共享奖励为智能体提供了共同的目标，但并未明确它们何时、如何、甚至是否必须合作才能取得成功。我们在激光学习环境中研究这些问题，这是一个多智能体寻路环境，其中合作具体表现为一个智能体阻挡激光以让队友安全通过。我们通过时间合作图来表示这些交互，图中带有时间标记的边将帮助者与受益者连接起来，并将六种合作模式定义为相互重叠的图谓词，同时证明了每条合作轨迹至少满足其中一种模式。通过将环境动态和模式谓词编码为命题公式，我们能够区分两类任务：一类是在指定时间范围内的某些获胜轨迹中存在某种合作模式的任务，另一类是在每条获胜轨迹中都必须出现该模式的任务。作为过滤器使用时，这些查询将随机布局采样器转变为能够生成具有认证合作要求任务的生成器。实验……

    arXiv:2609.06586v1 Announce Type: cross  Abstract: A shared reward gives agents a common objective, but leaves open when, how and even whether they must cooperate to succeed. We address these questions in the Laser Learning Environment, a multi-agent path-finding environment where cooperation materializes as one agent blocking a laser to let a teammate pass safely. We represent these interactions through temporal cooperation graphs whose timed edges connect helpers to beneficiaries, define six cooperation profiles as overlapping graph predicates, and prove that every cooperative trajectory satisfies at least one. By encoding the environment dynamics and profile predicates as propositional formulae, we distinguish tasks that admit}a profile in some winning trajectory from those that require it in every winning trajectory within a specified horizon. Used as filters, these queries turn a random layout sampler into a generator of tasks with certified cooperation requirements. Experiments w
    
[^294]: 随机非凸双层优化：无需稀有访问假设的改进收敛率

    Stochastic Nonconvex Bilevel Optimization: Improved Rates Without Rare-Visit Assumption

    [https://arxiv.org/abs/2609.06580](https://arxiv.org/abs/2609.06580)

    该论文通过对动态障碍梯度下降（DBGD）乘子进行简单的仅分母正则化，在无需“稀有访问”假设的情况下实现了快速收敛，达到了 $O(\varepsilon^{-2})$ 次迭代的平稳性，并改进了现有最好的无假设复杂度结果。

    

    我们研究了具有光滑且可能非凸的上层和下层目标的随机简单双层优化问题。现有的动态障碍梯度下降（DBGD）的随机扩展方法，要么在不可验证的依赖于轨迹的“稀有访问”假设下获得快速收敛，要么以显著更高的oracle代价移除该假设。我们证明，对DBGD乘子进行简单的仅分母正则化即可消除对这类假设的需要，同时保持快速收敛率。具体而言，我们的方法在 $O(\varepsilon^{-2})$ 次迭代中达到 $(\varepsilon, \varepsilon)$-平稳性，仅需 $O(\varepsilon^{-4})$ 次上层和 $O(\varepsilon^{-7})$ 次下层随机梯度，这改进了现有最好的无假设复杂度结果。此外，我们还推导了任意时刻适用的参数调度方案。

    arXiv:2609.06580v1 Announce Type: cross  Abstract: We investigate stochastic simple bilevel optimization with smooth and possibly nonconvex upper- and lower-level objectives. Existing stochastic extensions of dynamic barrier gradient descent (DBGD) either obtain fast convergence under an unverifiable trajectory-dependent ``rare-visit'' assumption, or remove this assumption at a substantially higher oracle cost. We show that a simple denominator-only regularization of the DBGD multiplier eliminates the need for such an assumption while preserving fast convergence rates. Specifically, our method achieves $(\varepsilon, \varepsilon)$-stationarity in $O(\varepsilon^{-2})$ iterations using $O(\varepsilon^{-4})$ upper-level and $O(\varepsilon^{-7})$ lower-level stochastic gradients, which improves upon the best assumption-free complexities. We additionally derive anytime parameter schedules.
    
[^295]: MARBO：社交推理游戏中LLM智能体的关系信念锚定方法

    MARBO: Relational Belief Grounding for LLM Agents in Social Deduction Games

    [https://arxiv.org/abs/2609.06563](https://arxiv.org/abs/2609.06563)

    提出MARBO框架，将关系信念锚定到偏好优化中，仅在行为有可靠信念支持且产生有利社会结果时给予偏好反馈，使紧凑型LLM智能体在社交推理游戏中实现策略一致的行为。

    

    社交推理游戏（SDGs）要求智能体在部分可观测的条件下进行推理，通过维持关于隐藏角色和阵营归属的关系信念。虽然近期的LLM智能体方法通过提示工程和偏好优化提升了游戏表现，但它们通常在没有将行动和游戏内发言显式锚定于此类信念的情况下进行优化。这经常导致策略上不一致的行为，对于紧凑型LLM智能体尤其如此。我们提出了多智能体关系信念优化（MARBO），这是一个信念锚定的偏好优化框架，利用关系信念来指导战略决策和游戏内发言。MARBO仅在行为得到可靠关系信念支持并带来策略上有利的社会结果时才提供偏好反馈，从而鼓励在不确定性下进行更一致的学习。在代表性社交推理游戏上的实验表明，MARBO使紧凑型LLM智能体能够持续地……

    arXiv:2609.06563v1 Announce Type: new  Abstract: Social deduction games (SDGs) require agents to reason under partial observability by maintaining relational beliefs about hidden roles and team alignments. While recent LLM-agent approaches improve gameplay through prompting and preference optimization, they often optimize actions and in-game speech without explicitly grounding them in such beliefs. This frequently leads to strategically inconsistent behavior, especially for compact LLM agents. We introduce Multi-Agent Relational Belief Optimization (MARBO), a belief-grounded preference optimization framework that leverages relational beliefs to guide strategic decisions and in-game speech. MARBO provides preference feedback only when behaviors are supported by reliable relational beliefs and lead to strategically favorable social outcomes, encouraging more consistent learning under uncertainty. Experiments on representative SDGs show that MARBO enables compact LLM agents to consistentl
    
[^296]: 藏在明处：被忽视的规范元素对大语言模型极端稀疏化的重要意义

    Hidden in Plain Sight: The Overlooked Significance of Canonical Elements for Extreme LLM Sparsity

    [https://arxiv.org/abs/2609.06557](https://arxiv.org/abs/2609.06557)

    该研究提出结合二阶显著性与持续训练的渐进稀疏化框架，通过重新审视基础剪枝策略中的规范元素，证明预训练大语言模型在高达99%的极端稀疏度下仍能保持强大性能，突破了非结构化训练后剪枝的公认极限。

    

    大语言模型（LLMs）通常被认为在激进稀疏化下非常脆弱，维持可靠性能通常需要将稀疏度保持在适度水平。然而，近期研究表明，LLMs对高稀疏度的适应能力比以往认为的更强，这将该问题重新定义为一个设计挑战而非根本性限制。在这项工作中，我们通过重新审视在此规模下相对未被充分探索的基础剪枝策略，来挑战非结构化训练后LLM剪枝的公认极限。通过一个结合二阶显著性度量与稀疏化进程相协调的持续训练的渐进稀疏化框架，我们证明预训练的LLMs可以在远超常见研究范围的稀疏度下保持强大性能。在LLaMA-2和Qwen-3模型系列上，我们的方法在高达99%的稀疏度下提升了困惑度和下游准确率，超越了当前最先进的…

    arXiv:2609.06557v1 Announce Type: new  Abstract: Large language models (LLMs) are often considered fragile under aggressive sparsification, and maintaining reliable performance typically requires sticking to moderate sparsity levels. However, recent studies suggest that LLMs are more resilient to high sparsity than previously thought, reframing the problem as a design challenge rather than a fundamental limitation. In this work, we challenge the perceived limits of unstructured post-training LLM pruning by revisiting elementary pruning strategies that have remained relatively underexplored at this scale. Through a progressive sparsification framework with second-order saliency and continued training coordinated with sparsity progression, we show that pretrained LLMs can retain strong performance far beyond commonly studied sparsity regimes. Across LLaMA-2 and Qwen-3 model families, our approach improves perplexity and downstream accuracy up to 99\% sparsity, surpassing both the current
    
[^297]: EStream：通过专家虚拟化在移动NPU上实现快速且内存高效的MoE预填充

    EStream: Fast and Memory-Efficient MoE Prefill through Expert Virtualization on Mobile NPUs

    [https://arxiv.org/abs/2609.06551](https://arxiv.org/abs/2609.06551)

    EStream通过将NPU编译时固定的计算图与MoE运行时的动态专家选择解耦，并利用专家虚拟化技术将专家权重存储于闪存中按组分页、以计算隐藏加载延迟，实现了移动NPU上快速且内存高效的MoE预填充。

    

    移动设备厂商和应用开发者日益在智能手机上部署大语言模型（LLM），以提供多样化的仅预填充服务。然而，现有系统主要依赖稠密模型，因为其规整的计算能够高效映射到移动NPU上，这使得能力更强的MoE（混合专家）模型未得到充分利用。MoE预填充并不适配移动NPU：NPU计算图在编译时即已固定，而MoE却在运行时才选择专家；且单个请求会触及大多数专家，其数量超过了手机内存所能容纳的极限。我们提出了EStream，通过将NPU必须固定的内容与MoE在运行时决定的内容分离开来，同时解决这两个问题。单一编译的专家计算图可服务于所有专家，每个专家的路由令牌和权重地址在调用时绑定，因此动态MoE执行完全在NPU上运行，无需填充或CPU/GPU回退。专家虚拟化将专家池保存在UFS闪存中，并通过固定大小的NPU可寻址内存区域按组分页加载，加载过程隐藏于计算之后，从而……

    arXiv:2609.06551v1 Announce Type: cross  Abstract: Mobile vendors and application developers increasingly deploy LLMs on smartphones for diverse prefill-only services. Yet current systems rely mainly on dense models whose regular computation maps efficiently to mobile NPUs, leaving more capable MoEs underused. MoE prefill does not fit mobile NPUs: NPU graphs are fixed at compile time, yet MoE picks experts at runtime; and one request touches most experts, more than a phone can hold in memory. We present EStream, which resolves both by separating what the NPU must fix from what MoE decides at runtime. A single compiled expert graph serves every expert, with each expert's routed tokens and weight address bound at call time, so dynamic MoE execution runs entirely on the NPU without padding or CPU/GPU fallback. Expert virtualization keeps the expert pool in UFS flash storage and pages it through a fixed-size NPU-addressable arena, group by group, with loading hidden behind computation, so 
    
[^298]: Learning²入门笔记

    Introductory Notes on Learning$^2$

    [https://arxiv.org/abs/2609.06546](https://arxiv.org/abs/2609.06546)

    Learning²是一种表示层面的框架，通过已知物理变换将主表示与第二表示耦合，以跨表示约束限制假设空间并提供物理可解释的事前筛选准则，并以流体力学中的欧拉-拉格朗日表示EuLaNet作为实例化。

    

    尽管机器学习可以用于从数据预测物理系统的演化，但仅学习每个时刻系统状态的公式化方法会将解的时间与动力学结构留在一个宽泛的假设空间中求解。我们提出了Learning²，这是一个表示层面的框架，通过已知的物理变换将主表示与第二个表示耦合，从而对该假设空间进行结构化。由此产生的跨表示约束限制了有效假设空间，并提供了一个事前的、物理上可解释的准则，用于排除那些仅满足主表示的解。我们通过EuLaNet对Learning²进行实例化，EuLaNet是一个用于流体力学的欧拉-拉格朗日表示。给定速度状态 $u(\mathbf{x},t)$，EuLaNet通过 $\dot{X}(\mathbf{a},t)=u(X(\mathbf{a},t),t)$ 构建其诱导的拉格朗日流映射 $X(\mathbf{a},t)$，从……

    arXiv:2609.06546v1 Announce Type: cross  Abstract: Although machine learning can be used to predict the evolution of physical systems from data, a formulation that learns only the system state at each time leaves the temporal and dynamical structure of the solution to be resolved within a broad hypothesis space. We introduce Learning$^2$, a representation-level framework that structures this space by coupling a primary representation to a second representation through a known physical transformation. The resulting cross-representation constraint restricts the effective hypothesis space and provides an ante-hoc, physically interpretable criterion for excluding solutions that satisfy the primary representation alone. We instantiate Learning$^2$ through EuLaNet, an Eulerian--Lagrangian representation for fluid dynamics. Given the velocity state $u(\mathbf{x},t)$, EuLaNet constructs its induced Lagrangian flow map $X(\mathbf{a},t)$ through $\dot{X}(\mathbf{a},t)=u(X(\mathbf{a},t),t)$, from
    
[^299]: 不仅仅是过度平滑：检测图神经网络中的回音室效应

    Not Just Oversmoothing: Detecting the Echo Chamber Effect in Graph Neural Networks

    [https://arxiv.org/abs/2609.06521](https://arxiv.org/abs/2609.06521)

    该论文揭示了图神经网络中除过度平滑之外的另一种失效模式——回音室效应，即社区内部节点表示坍缩为不可区分而社区间分离却仍然存在，并提出回音室指数（ECI）来量化检测这一效应。

    

    过度平滑是图神经网络（GNNs）一个众所周知的失效模式。然而，现有的大多数诊断方法依赖于全局聚合度量，无法捕捉消息传递的异质性动态。现实世界的图呈现出明显的社区结构，而消息传递在两个时间尺度上运行：节点表示在社区内部迅速坍缩，而在社区之间则缓慢坍缩。这造成了一个关键性的盲区——社区内部的表示可能变得无法区分，而社区之间的分离仍然存在，我们将这种失效模式称为“回音室效应”。为了量化这一效应，我们提出了回音室指数，该指数按社区成员身份对成对距离进行分层，能够揭示全局能量衰减但社区间分离仍然存在的情形。ECI 进一步表明，在理论分析所给出的条件下，特征保留机制可能无法消除回音室效应。

    arXiv:2609.06521v1 Announce Type: new  Abstract: Oversmoothing is a well-known failure mode of Graph Neural Networks (GNNs). However, most existing diagnostics rely on global aggregation measures that fail to capture the heterogeneous dynamics of message passing. Real-world graphs exhibit pronounced community structure, and message passing operates on two timescales, with representations collapsing rapidly within communities and slowly across them. This creates a critical gap in which intra-community representations can become indistinguishable while inter community separation persists, a failure mode that we refer to as the Echo Chamber Effect. To quantify this effect, we introduce the Echo Chamber Index (ECI), which stratifies pairwise distances by community membership and reveals when global energy diminishes while inter-community separation persists. ECI further shows that feature retention mechanisms can preserve the echo chamber under the conditions of our theoretical analysis. T
    
[^300]: 面向非平稳多变量图信号预测的角色特定预测几何

    Role-Specific Predictive Geometries for Nonstationary Multivariate Graph-Signal Forecasting

    [https://arxiv.org/abs/2609.06519](https://arxiv.org/abs/2609.06519)

    该论文提出角色特定的预测几何框架，让有向的长期关系作用于均衡坐标、有向的短期关系作用于滞后差分，从而在非平稳条件下实现更有效的多变量图信号预测。

    

    当节点级轨迹呈现非平稳性，但稳定的关系在节点和特征之间持续存在时，多变量图信号的预测是具有挑战性的。在误差修正表示中，长期均衡恢复和短期瞬态传播代表不同的预测角色，它们不需要共享共同的跨特征几何结构。我们引入了角色特定的预测几何，其中有向的Long（长期）关系作用于估计的均衡坐标，而有向的Short（短期）关系作用于滞后差分。矩阵值的Long响应在图传播之前混合均衡坐标，而Short响应使用图过滤的瞬态设计；直接多视界估计器将相邻视界之间的预测修正耦合起来。时间交叉拟合和Frisch-Waugh-Lovell偏出方法使选定的边相对于图-时间骨干网络具有条件预测解释。

    arXiv:2609.06519v1 Announce Type: new  Abstract: Forecasting multivariate graph signals is challenging when node-level trajectories are nonstationary but stable relations persist across nodes and features. In an error-correction representation, long-run equilibrium restoration and short-run transient propagation represent different predictive roles and need not share a common cross-feature geometry. We introduce role-specific predictive geometries in which directed Long relations act on estimated equilibrium coordinates, whereas directed Short relations act on lagged differences. Matrix-valued Long responses mix equilibrium coordinates before graph propagation, while Short responses use graph-filtered transient designs; a direct multi-horizon estimator couples forecast corrections across adjacent horizons. Temporal cross-fitting and Frisch-Waugh-Lovell partialling-out give selected edges a conditional predictive interpretation relative to a graph-temporal backbone. The Long operator re
    
[^301]: 针对预测性干扰机的模型自适应与风险约束跳频

    Model-Adaptive and Risk-Constrained Frequency Hopping Against Predictive Jammers

    [https://arxiv.org/abs/2609.06514](https://arxiv.org/abs/2609.06514)

    提出了D-PACT-AFH框架，一种针对预测性干扰的模型自适应且风险受约束的对抗性上下文老虎机跳频方法，通过在线模型选择与KL投影风险预算，有效兼顾了环境变化下的模型适应性和跳频策略的抗暴露安全性。

    

    面对预测性干扰的自适应跳频必须同时应对模型不确定性与策略暴露两大问题：上下文与损失之间的关系可能随工作环境的变化而改变，而持续固定的跳频模式可能使高概率信道暴露于攻击之中。我们提出了D-PACT-AFH，一个模型自适应且风险受约束的对抗性上下文多臂老虎机框架，其中Tsallis-FTRL主算法将全局线性学习器与分区局部学习器相结合，并在线选择模型类别。D-PACT-Hit将信道级的边际命中风险纳入模型选择过程，而D-PACT-Safe则采用最小Kullback-Leibler投影来强制执行每个时隙的风险预算。我们在非预知攻击条件下证明了估计器的有效性，给出了相对于更优固定基学习器的oracle分解，并为Safe投影提供了精确的条件风险保证。在多种信道环境和干扰机类型上的实验证明了该方法具备有效的模型自适应能力。

    arXiv:2609.06514v1 Announce Type: new  Abstract: Adaptive frequency hopping against predictive jamming must address both model uncertainty and policy exposure: the context-loss relationship may vary across operating regimes, while persistent hopping patterns may expose high-probability channels to attack. We propose D-PACT-AFH, a model-adaptive and risk-constrained adversarial contextual-bandit framework in which a Tsallis-FTRL master combines a global linear learner with a partitioned local learner and selects the model class online. D-PACT-Hit incorporates channel-wise marginal hit risk into model selection, while D-PACT-Safe applies a minimum-Kullback-Leibler projection to enforce a per-slot risk budget. We establish estimator validity under non-anticipating attacks, an oracle decomposition relative to the better fixed base, and an exact conditional-risk guarantee for the Safe projection. Experiments across diverse channel regimes and jammer types demonstrate effective model adaptat
    
[^302]: Bi-HYCO：碎片化观测下用于偏微分方程参数识别的双目标协同学习

    Bi-HYCO: Bi-Objective Cooperative Learning for PDE Parameter Identification under Fragmented Observations

    [https://arxiv.org/abs/2609.06511](https://arxiv.org/abs/2609.06511)

    提出双目标协同学习框架Bi-HYCO，通过无标签交互点耦合物理模型与合成模型的预测状态，在碎片化观测条件下实现偏微分方程参数识别，并提供了严格的收敛性理论证明。

    

    物理模型与合成模型可能从互补角度描述同一偏微分方程（PDE）支配的系统，同时接收不同的、可能是碎片化的观测数据。我们提出双目标HYCO（Bi-HYCO），这是一个协同学习框架，在保留两种模型表示及其各自局部观测目标的同时，通过无标签的交互点耦合二者的预测状态。这些交互点不包含任何测量数据，也不扩充数据集；它们在共同状态空间中充当通信机制。两个目标准则构成向量值目标函数，加权标量化方法为其提供了计算实现。针对具有固定交互点的确定性共享观测算法，我们证明了整个交替序列的充分下降性和有限长度，并在给定的Kurdyka-Lojasiewicz型假设下证明其收敛于混合临界点。通过椭圆传输问题和二维Navier-Stokes实验对该方法进行了评估（原摘要在此处被截断）。

    arXiv:2609.06511v1 Announce Type: new  Abstract: Physical and synthetic models may describe complementary aspects of the same PDE-governed system while receiving different, possibly fragmented, observations. We propose Bi-Objective HYCO (Bi-HYCO), a cooperative framework that retains both representations and their local observational objectives while coupling their predicted states at unlabeled interaction points. These points contain no measurements and do not augment the data; they provide a communication mechanism in the common state space. The two criteria form a vector-valued objective, and weighted scalarizations provide computational realizations. For the deterministic shared-observation algorithm with fixed interaction points, we prove sufficient decrease and finite length of the whole alternating sequence, which converges to a mixed critical point under the stated Kurdyka-Lojasiewicz-type assumptions. Elliptic transmission and two-dimensional Navier-Stokes experiments assess p
    
[^303]: 结构熵驱动的图扩散生成方法用于一次性联邦图学习

    Structural Entropy-Driven Graph Diffusion Generation for One-Shot Federated Graph Learning

    [https://arxiv.org/abs/2609.06499](https://arxiv.org/abs/2609.06499)

    该论文提出SPIRE方法，利用一阶度分布结构熵推导拓扑感知的客户端权重，并在服务器端通过图扩散模型以加权客户端原型为条件合成伪图，从而在一次性联邦图学习中超越传统仅基于数据量的加权方式，无需额外客户端训练即可融合语义与结构信息。

    

    一次性联邦图学习要求服务器从高度压缩的信息中估计客户端的贡献，然而传统的基于数据量的加权方式只捕捉了客户端数据的数量，却忽略了其连接性的组织方式。在本文中，我们提出了SPIRE，一种结构熵驱动的图扩散生成方法，将拓扑感知的客户端差异化引入一次性联邦图学习。具体而言，我们采用一阶度分布结构熵作为度质量离散度的紧凑描述符，并据此推导出结构性客户端权重，提供了一种能够考虑数据量之外图拓扑差异的归纳偏置。在生成方面，服务器上的图扩散模型以加权客户端原型为条件合成伪图，在无需额外客户端训练的情况下同时捕捉语义和结构信息。

    arXiv:2609.06499v1 Announce Type: new  Abstract: One-shot federated graph learning (FGL) requires the server to estimate client contributions from highly compressed information, yet conventional volume-based weighting captures the amount of client data while overlooking how its connectivity is organized. In this paper, we propose SPIRE, a Structural Entropy-Driven Graph Diffusion Generation method that introduces topology-aware client differentiation into one-shot FGL. Specifically, we employ first-order degree-distribution structural entropy as a compact descriptor of degree-mass dispersion and use it to derive structural client weights, providing an inductive bias that accounts for differences in graph topology beyond data volume. On the generation side, a graph diffusion model on the server synthesizes pseudographs conditioned on the weighted client prototypes, capturing both semantic and structural information without requiring additional client-side training. The generated pseudog
    
[^304]: OracleZoom：基于在线策略自蒸馏启发的参考约束递归图像超分辨率

    OracleZoom: On-Policy Self-Distillation Inspired Reference-Constrained Recursive Image Super Resolution

    [https://arxiv.org/abs/2609.06490](https://arxiv.org/abs/2609.06490)

    OracleZoom提出了一种受自蒸馏启发、参考约束的递归图像超分辨率框架，通过直接与跨尺度监督、无参考质量目标、KL约束潜在先验和EMA一致性等技术，解决了深层递归缺乏真实标签监督的问题，在七个数据集上实现了极端放大倍率下最先进的超分辨率质量。

    

    递归超分辨率（SR）通过将预测结果反复馈入同一模型，将固定尺度的超分辨率扩展到极端放大倍率，类似于反复缩放图像。然而，在每个尺度上获取真实标签数据仍然具有挑战性，尤其是在深层阶段，因为所需的源分辨率呈几何级数增长，导致更深层的预测缺乏监督。我们提出了OracleZoom，这是一个受在线策略蒸馏启发、参考约束的框架，它在自身轨迹上进行训练，同时将最后的真实标签证据延伸到监督边界之外。直接监督和跨尺度监督约束可验证的内容，而无参考质量目标则引导未解决的精细尺度细节。KL约束的预训练潜在先验限制了由质量驱动的漂移，而EMA一致性则稳定了监督边界。在七个数据集上，OracleZoom在各种缩放尺度上实现了最先进的超分辨率质量，平均0

    arXiv:2609.06490v1 Announce Type: cross  Abstract: Recursive Super-Resolution (SR) extends fixed-scale SR to extreme magnification by repeatedly feeding predictions back into the same model, analogous to zooming an image repeatedly. However, ground truth availability at every scale, especially at depth, remains challenging as the required source resolution grows geometrically, leaving deeper predictions unsupervised. We present OracleZoom, an on-policy distillation-inspired, reference-constrained framework that trains on its trajectory while carrying the last ground-truth evidence beyond the supervision boundary. Direct and cross-scale supervision constrain verifiable content, while a no-reference quality objective guides unresolved fine-scale detail. A KL-constrained pretrained latent prior limits quality-driven drift, while EMA consistency stabilizes the supervision boundary. Across seven datasets, OracleZoom achieves the state-of-the-art SR quality across zooming scales, averaging 0
    
[^305]: 随机连续蒙特卡洛树搜索中的幂均值估计

    Power Mean Estimation in Stochastic Continuous Monte Carlo Tree Search

    [https://arxiv.org/abs/2609.06489](https://arxiv.org/abs/2609.06489)

    该论文提出了一种面向连续随机马尔可夫决策过程的新型蒙特卡洛树搜索算法，通过将幂均值作为价值回传算子并结合多项式探索奖励，解决了随机环境中固有的非平稳性问题。

    

    蒙特卡洛树搜索（MCTS）已在确定性环境的在线规划中取得成功，但将其应用于随机马尔可夫决策过程（MDP）仍存在重大挑战，尤其是在连续状态-动作空间中。现有方法（如结合MCTS与分层乐观优化（HOO）老虎机策略的HOOT）虽可处理连续空间，但其依赖于对数探索奖励项，在非平稳、随机环境中缺乏理论保证。近期的进展（如POLY-HOOT）引入了多项式奖励项以在确定性MDP中实现收敛，但针对随机MDP的类似理论仍尚未建立。在本文中，我们提出了一种新颖的MCTS算法，专为连续随机MDP设计。该算法将幂均值作为价值回传算子，并结合多项式探索奖励来解决环境中固有的非平稳性……

    arXiv:2609.06489v1 Announce Type: cross  Abstract: Monte Carlo Tree Search (MCTS) has demonstrated success in online planning for deterministic environments, yet significant challenges remain in adapting it to stochastic Markov Decision Processes (MDPs), particularly in continuous state-action spaces. Existing methods, such as HOOT, which combines MCTS with the Hierarchical Optimistic Optimization (HOO) bandit strategy, address continuous spaces but rely on a logarithmic exploration bonus that lacks theoretical guarantees in non-stationary, stochastic settings. Recent advancements, such as POLY-HOOT, introduced a polynomial bonus term to achieve convergence in deterministic MDPs, though a similar theory for stochastic MDPs remains undeveloped. In this paper, we propose a novel MCTS algorithm, \Algname, designed for continuous, stochastic MDPs. \Algname integrates a power mean as a value backup operator, alongside a polynomial exploration bonus to address the non-stationarity inherent i
    
[^306]: 基于最优传输贝尔曼平滑的二阶平滑规划

    Second-Order Smooth Planning with Optimal-Transport Bellman Smoothing

    [https://arxiv.org/abs/2609.06484](https://arxiv.org/abs/2609.06484)

    本文提出基于最优传输平滑贝尔曼备份的二阶估计器，将SmoothCruiser类规划器的样本复杂度从 $\widetilde O(\varepsilon^{-4})$ 改进到 $\widetilde O(\varepsilon^{-3})$。

    

    使用生成模型进行规划的目标是通过尽可能少的模拟器调用来估计某个状态的价值。SmoothCruiser通过利用熵正则化贝尔曼备份的平滑性，实现了与问题无关的复杂度 $\widetilde O(\varepsilon^{-4})$，但其估计器仅为一阶。我们证明，SmoothCruiser类规划器的样本复杂度指数由局部泰勒余项的阶数 $\beta$ 决定，从而给出预言机复杂度 $\widetilde O(\varepsilon^{-(2+2/(\beta-1))})$：一阶情形 $\beta=2$ 恢复了SmoothCruiser的结果，而二阶/三次余项 $\beta=3$ 则可得到 $\widetilde O(\varepsilon^{-3})$。我们通过在动作分布上引入最优传输平滑的贝尔曼备份来达到这一复杂度区间，该备份具有封闭形式、策略梯度以及Lipschitz海森矩阵，并且其二次校正项可以使用无偏的交叉积估计器来估计。由此得到的SecondOrderSmoothCruiser...

    arXiv:2609.06484v1 Announce Type: cross  Abstract: Planning with a generative model aims to estimate the value of a state using as few simulator calls as possible. SmoothCruiser achieves problem-independent complexity $\widetilde O(\varepsilon^{-4})$ by exploiting the smoothness of the entropy-regularized Bellman backup, but its estimator is only first-order. We show that the sample-complexity exponent of SmoothCruiser-type planners is governed by the order $\beta$ of the local Taylor remainder, giving oracle complexity $\widetilde O(\varepsilon^{-(2+2/(\beta-1))})$: the first-order case $\beta=2$ recovers SmoothCruiser, while a second-order/cubic remainder $\beta=3$ yields $\widetilde O(\varepsilon^{-3})$. We reach this regime with an optimal-transport-smoothed Bellman backup over action distributions, which has a closed form, a policy gradient, and a Lipschitz Hessian, and whose quadratic correction admits an unbiased cross-product estimator. The resulting SecondOrderSmoothCruiser ac
    
[^307]: 参数剪枝如何重塑深度神经网络的表示？一种基于交互视角的探索

    How Does Parameter Pruning Reshape DNN Representations? An Interaction-Driven Exploration

    [https://arxiv.org/abs/2609.06483](https://arxiv.org/abs/2609.06483)

    该研究揭示了剪枝过程中DNN交互模式呈现三阶段动态变化，并发现剪枝对性能的影响关键取决于是否移除了可泛化的低阶交互模式。

    

    本研究聚焦于一个科学问题：理解当不同参数被剪枝时，决定深度神经网络（DNN）性能多样化退化的内部因素。为了解释为什么剪枝某些参数会导致显著的性能下降，而剪枝其他参数却不会，我们研究了剪枝操作如何影响DNN所编码的交互模式。我们发现，当逐渐增大剪枝比例时，DNN所编码的交互模式呈现出明显的三阶段动态特性，即：模型性能不会受到较大影响，直到剪枝操作开始移除低阶交互，且低阶交互表现出很强的泛化能力。此外，我们发现DNN性能对某些模块剪枝的高度敏感性，归因于剪枝操作是否移除了可泛化的低阶交互模式。

    arXiv:2609.06483v1 Announce Type: new  Abstract: This study focuses on the scientific problem of understanding internal factors that govern the diverse performance degradation of deep neural networks (DNNs) when different parameters are pruned. In order to explain why pruning certain parameters leads to significant performance degradation but pruning other parameters does not, we examine how the pruning operation affects the interaction patterns encoded by the DNN. We find that when we progressively increase the pruning ratio, the interaction patterns encoded by DNNs exhibit a distinct three-phase dynamics, \emph{i.e.}, model performance is not largely affected until the pruning operation begins to remove low-order interactions, and low-order interactions exhibit strong generalizability. Moreover, we find that the high sensitivity of DNN performance to the pruning of certain modules is attributed to whether the pruning operation removes generalizable low-order interaction patterns.
    
[^308]: 通过对齐学习核函数的多类贝叶斯分类

    Learning Kernels by Alignment for Multiclass Bayes Classification

    [https://arxiv.org/abs/2609.06474](https://arxiv.org/abs/2609.06474)

    本文证明协作学习本质上是核对齐过程、协作推理等价于核贝叶斯分类，进而用可学习的马氏距离替代余弦相似度将CLaI推广至多类分类，在多个数据集上显著提升了准确率、收敛速度与校准性能。

    

    核方法将数据表示与决策制定分离，但通常需要预先选择核函数。我们证明该核函数可以通过对齐方式来学习，并借助最近提出的协作学习与推理框架（CLaI）发展了由此形成的框架。我们证明协作学习可以被视为一个核对齐过程，其中通过训练嵌入表示，使其诱导的相似度与由标签导出的目标核相匹配。我们还证明协作推理等价于采用Parzen窗密度估计的核贝叶斯分类。基于这些视角，我们通过用可学习的马氏距离替换余弦相似度来推广CLaI，并将其扩展到多类分类。在CIFAR-10、PathMNIST和SleepEDF数据集上，马氏距离形式相比基于余弦相似度的变体提高了准确率、收敛更快，并产生了更低的校准误差。

    arXiv:2609.06474v1 Announce Type: new  Abstract: Kernel methods separate data representation from decision-making, but typically require the kernel to be chosen in advance. We show that this kernel can instead be learned by alignment, and develop the resulting framework through the recently introduced Collaborative Learning and Inference (CLaI). We show that Collaborative Learning can be viewed as a kernel alignment process, in which an embedding is trained so that its induced similarity matches a label-derived target kernel. We also prove that Collaborative Inference is equivalent to kernel Bayes classification with Parzen-window density estimation. Motivated by these perspectives, we generalise CLaI by replacing cosine similarity with a learned Mahalanobis distance and extend it to multiclass classification. On CIFAR-10, PathMNIST, and SleepEDF, the Mahalanobis formulation improves accuracy, converges faster, and yields lower calibration error than the cosine-based variant. Auxiliary
    
[^309]: 压缩下的引导：量化大语言模型中的剂量-反应关系、能力代价与失败不对称性

    Steering Under Compression: Dose-Response, Capability Cost, and Failure Asymmetry in Quantized LLMs

    [https://arxiv.org/abs/2609.06473](https://arxiv.org/abs/2609.06473)

    该论文首次系统量化了激活引导与训练后量化（INT8/NF4）之间的交互作用，提出等效效应框架来衡量量化下的能力代价，发现情感引导在量化后几乎无损地保留，而不同行为目标之间存在失败不对称性。

    

    推理时的激活引导能够在不修改模型参数的情况下实现对大语言模型的行为控制，而训练后量化则可降低模型部署时的内存和计算成本。尽管这两种技术在实际应用中日益交汇，但它们之间的相互作用尚未得到系统表征。我们系统研究了仅权重量化（INT8和NF4）下的激活引导，实验涵盖四个开放权重的7-9B模型以及两个行为目标：有判定的情感和无判定的推理长度。通过采用等效效应框架，在匹配的行为效应下比较能力代价，我们发现情感引导在量化后几乎完整保留。在使用统一的v2.3.1重新评分纠正GSM8K解析器伪影后，INT8下的合并对比值为-0.010（90%置信区间[-0.026, +0.007]），在预注册的三标签规则下达到描述性等效，而NF4在-0.017（[-0.067, +0.033]）处仍无定论。相比之下……

    arXiv:2609.06473v1 Announce Type: new  Abstract: Inference-time activation steering enables behavioral control of large language models without parameter modification, while post-training quantization reduces memory and compute costs for deployment. Despite their growing convergence in practice, the interaction between these two techniques remains uncharacterized. We systematically study activation steering under weight-only quantization (INT8 and NF4) across four open-weight 7-9B models and two behavioral targets: judged sentiment and judge-free reasoning length. Using an iso-effect framework that compares capability costs at matched behavioral effect, we find that sentiment steering survives quantization intact. After correcting a GSM8K parser artifact with a uniform v2.3.1 rescore, the pooled INT8 contrast is -0.010 (90% CI [-0.026, +0.007]), descriptively Equivalent under the preregistered three-label rule, while NF4 remains Inconclusive at -0.017 ([-0.067, +0.033]). In contrast, r
    
[^310]: 一步一主导：通过跨步控制缓解多领域强化学习中的高阶干扰

    One Step, One Lead: Mitigating Higher-Order Interference in Multi-Domain Reinforcement Learning via Cross-Step Control

    [https://arxiv.org/abs/2609.06469](https://arxiv.org/abs/2609.06469)

    本文提出OSOL方法，通过分析相邻检查点间的词元对数概率足迹来捕捉传统单步梯度分析无法发现的跨步序列性干扰，并在每次迭代中指定重点领域，从而缓解多领域强化学习中的高阶干扰问题。

    

    跨多个领域的强化学习（RL）可以拓宽大语言模型（LLM）的推理能力，但联合训练往往会降低各个单一领域的性能，并可能导致优化过程不稳定。现有工作通常从单步视角出发，使用一阶梯度对齐或基于曲率的代理指标来诊断这种干扰。我们证明这种视角可能会忽略一种关键的序列性干扰形式：即使连续的实际更新在输出空间中部分相互抵消，同一点的领域梯度仍可能保持近似正交。我们进一步表明，连续词元的对数概率足迹可以直接从相邻检查点中恢复这种交互，将其作为输出空间中的局部二阶交互，而无需显式重建同一步的曲率。基于这一洞察，我们提出了OSOL方法，该方法在每次迭代中指定一个重点领域，并利用前一个检查点的足迹……（原文摘要在此处截断）

    arXiv:2609.06469v1 Announce Type: new  Abstract: Reinforcement learning (RL) across multiple domains can broaden the reasoning capabilities of large language models (LLMs), yet joint training often degrades individual-domain performance and can destabilize optimization. Existing work typically diagnoses such interference from a single-step view using first-order gradient alignment or curvature-based proxies. We show that this view can miss a critical form of sequential interference: same-point domain gradients may remain nearly orthogonal even when consecutive realized updates partially reverse one another in output space. We further show that consecutive token log-probability footprints recover this interaction directly from adjacent checkpoints as a local second-order interaction in output space, without explicitly reconstructing same-step curvature. Building on this insight, we propose OSOL, which designates a focus domain at each iteration, uses the preceding checkpoint footprint t
    
[^311]: 扇区均值：通过角度扇区划分实现K均值质心的确定性初始化

    Sector-Mean: Deterministic Initialization of K-Means Centroids via Angular Sector Partitioning

    [https://arxiv.org/abs/2609.06468](https://arxiv.org/abs/2609.06468)

    本文提出扇区均值初始化方法，以O(N)时间复杂度通过围绕全局质心的角度扇区划分确定性初始化K均值质心，在保持与K-Means++等主流方法相当聚类质量的同时显著提升计算效率。

    

    K均值（K-Means）是最广泛使用的聚类算法之一，但其对初始质心选择的敏感性仍是其收敛速度和聚类准确性的主要瓶颈。本文提出扇区均值初始化方法，这是一种具有O(N)时间复杂度的确定性初始化策略，它将二维数据空间围绕全局质心划分为角度扇区，并使用各扇区的均值来初始化质心。我们在成熟的二维基准数据集（SIPU、Birch）和多个真实数据集上对该方法进行了评估，在相同的Lloyd迭代次数下与随机初始化、K-Means++和Max-Min初始化方法进行比较。Friedman检验（p<0.05）和Nemenyi事后比较的统计分析表明，扇区均值在提供与K-Means++和Max-Min相当的聚类质量的同时，具有显著的计算效率优势。实验结果表明，扇区均值减少了……

    arXiv:2609.06468v1 Announce Type: new  Abstract: K-Means is one of the most widely used clustering algorithms, but its susceptibility to initial centroid selection remains a primary bottleneck for its convergence speed and clustering accuracy. This paper proposes Sector-Mean Initialization, a deterministic initialization strategy with O(N) time complexity that partitions the two-dimensional data space into angular sectors around the global centroid and initializes centroids using sector-wise means. We evaluate the method on established two-dimensional benchmarks (SIPU, Birch) and multiple real-world datasets, comparing against random, K-Means++, and Max-Min initialization under identical Lloyd iterations. The statistical analysis of Friedman's test (p<0.05) and Nemenyi post-hoc comparison indicates that, while delivering equivalent clustering quality as K-Means++ and Max-Min, Sector-Mean offers significant computational efficiency. Experimental results show that Sector-Mean reduces the
    
[^312]: 表演性强化学习中的局部与全局稳定性

    Local and Global Stability in Performative Reinforcement Learning

    [https://arxiv.org/abs/2609.06467](https://arxiv.org/abs/2609.06467)

    该论文研究了表演性强化学习中策略混合的稳定性，提出了局部混合稳定性（等价于平稳性）与全局混合稳定性两种新概念，从而摆脱了传统收敛性保证对难以验证的Lipschitz敏感性假设的依赖。

    

    在表演性强化学习中，部署的策略会塑造生成学习者未来数据的环境，自然的解概念是表演性稳定策略，即在其自身所诱导的环境中是最优的策略。现有的收敛性保证依赖于环境映射 $\pi \mapsto (P_\pi, r_\pi)$ 上的Lipschitz敏感性假设，这类假设难以验证，并且在多智能体最优响应动态等场景中会失效。我们转而研究策略混合的稳定性，并证明由此得到的图景与表演性预测有着根本性的不同——在表演性预测中，随机化可以完全消除对敏感性假设的需求。我们区分了两类概念：局部混合稳定性，这是一种按占用度加权的二阶松弛，我们证明它等价于平稳性；以及全局混合稳定性，它能够对任意偏离的策略提供稳定性保证。我们的第一个结果表明，一种按状态加权的Hedge动态驱使…

    arXiv:2609.06467v1 Announce Type: new  Abstract: In performative reinforcement learning the deployed policy shapes the environment that generates the learner's future data, and the natural solution concept is a performatively stable policy that is optimal in the environment it induces. Existing convergence guarantees rely on Lipschitz sensitivity assumptions on the environment map $\pi \mapsto (P_\pi, r_\pi)$, which are hard to verify and fail in settings such as multi-agent best-response dynamics. We instead study stability for mixtures of policies, and show that the resulting picture is fundamentally different from performative prediction, where randomization removes the need for any sensitivity assumption. We distinguish local mixed stability, an occupancy-weighted first-order relaxation that we show is equivalent to stationarity, from global mixed stability, which certifies against arbitrary deviating policies. Our first result is that a weighted per-state Hedge dynamic drives the 
    
[^313]: 掩码预训练的理论框架

    A Theoretical Framework for Masked Pretraining (MPT)

    [https://arxiv.org/abs/2609.06460](https://arxiv.org/abs/2609.06460)

    本文提出了一个分析掩码预训练的新理论框架，证明掩码技术通过隐式创建语义相似的正样本对与对比学习建立了理论联系，并针对由此产生的维度坍缩问题提出了U-MPT损失来有效解决。

    

    近年来，基于重构预训练任务的掩码预训练（MPT）已成为一种在多个领域都极具前景的自监督学习范式，并在多个下游任务中取得了显著的性能。然而，对MPT背后工作机制的理论理解仍然有限。在本文中，我们引入了一个新的理论框架来分析MPT，并理解掩码技术在提取有意义表示中的关键作用。我们建立了MPT与另一种流行的自监督学习范式——对比学习之间的理论联系。我们证明了掩码技术隐式地创建了语义相似的正样本对，而重构损失则将这些正样本对在特征空间中拉近。此外，作为隐式对齐的结果，我们指出了MPT存在的维度坍缩问题，并提出了一种增强均匀性的MPT（U-MPT）损失，能够有效解决这一问题。

    arXiv:2609.06460v1 Announce Type: new  Abstract: Recently, Masked Pretraining (MPT) based on reconstruction pretraining tasks has risen to a promising self-supervised learning paradigm across various domains and achieves remarkable performance in multiple downstream tasks. However, the theoretical understanding of the working mechanism behind MPT is still limited. In this paper, we introduce a new theoretical framework to analyze MPT and understand the crucial role of masking in extracting meaningful representations. We establish theoretical connections between MPT and another popular self-supervised paradigm: contrastive learning. We prove that the masking technique implicitly creates positive pairs that are semantically similar and the reconstruction loss pulls them together in the feature space. Besides, as a result of the implicit alignment, we point out the dimensional collapse issue of MPT and propose a Uniformity-enhanced MPT (U-MPT) loss that can effectively address this issue 
    
[^314]: 分解LLM评审器的不确定性以精准定位专家标注

    Decomposing LLM-Judge Uncertainty to Target Expert Labels

    [https://arxiv.org/abs/2609.06444](https://arxiv.org/abs/2609.06444)

    该论文提出一种小型贝叶斯模型，将LLM评审器的总不确定性分解为可被专家标注消除的认知不确定性和不可消除的偶然不确定性，使专家只需标注评审器真正无知的项目，在ChaosNLI数据集上比使用总不确定性多消除83%的误差。

    

    LLM评审器可以大规模评估模型输出，专家应该只在它最不确定的地方进行标注。然而其天然的升级信号混淆了两种不确定性：偶然不确定性——专家群体中真实存在的分歧，标注无法减少这种不确定性；以及认知不确定性——评审器自身的无知，标注可以减少这种不确定性。本文提出一个小型贝叶斯模型来分离这两种不确定性：通过对已收集的标注进行回归，学习在多大程度上信任黑盒评审器的预测。两个组成部分都可以通过简单的公式计算得出，无需采样或额外的评审器调用。在一个面对完全已知真值的真实LLM评审器上，这两种不确定性成分被成功分离，且评审器声称的置信度并不能反映其真实误差。在真实的人类分歧数据集（ChaosNLI）上，在相同的专家标注量下，基于认知不确定性的排序比使用总不确定性多消除83%的误差，不过在该数据集上简单地升级标注最少的项目也能达到同样效果。我们证明了我们可以估计评审器在哪里是无知的，而不是专家们真正存在分歧的地方。

    arXiv:2609.06444v2 Announce Type: replace  Abstract: An LLM judge evaluates outputs at scale. Experts should label only where it is least sure. Its natural escalation signal conflates two uncertainties: aleatoric, real disagreement in the expert pool, which labels cannot reduce, and epistemic, the judge's ignorance, which labels do reduce. A small Bayesian model separates them: a regression on labels already collected learns how far to trust a black-box judge's prediction. Both components follow as simple formulas, with no sampling or further judge calls. The components isolate on a real LLM judge against exactly known truth, and stated confidence is no guide to its actual error. On real human disagreement (ChaosNLI) the epistemic ranking removes 83% more error than total uncertainty for the same expert labels, though simply escalating the least-labelled items does as well there. We demonstrate we can estimate where a judge is ignorant rather than where experts genuinely disagree, and 
    
[^315]: 大规模预训练用于改进基于深度学习的扩散加权成像几何畸变校正

    Large-Scale Pretraining for Improving Deep Learning-Based Geometric Distortion Correction of Diffusion-Weighted Imaging

    [https://arxiv.org/abs/2609.06437](https://arxiv.org/abs/2609.06437)

    本研究证明大规模预训练策略（尤其是cWDM生成式预训练模型）能显著提升基于深度学习的DWI几何畸变校正性能，并在存在采集偏移的低中收入国家数据上展现出良好的可迁移性。

    

    扩散加权成像（DWI）在临床中被广泛应用，但仍然容易受到几何畸变的影响。传统的校正方法通常需要额外的采集或厂商特定的解决方案，限制了其在高通量、资源受限环境中的可行性。本研究探讨了大规模预训练策略是否能够改进基于深度学习的单相位编码DWI畸变校正。我们将该任务建模为图像重建问题，并将非预训练基线模型与自监督预训练模型和生成式预训练模型进行比较，同时采用定量图像相似度指标和定性专家评估进行评价。表现最佳的模型进一步在低中收入国家（LMIC）环境中采集的、存在采集偏移的数据上测试了其可迁移性。结果表明，预训练模型优于非预训练基线，其中cWDM在定量和定性评估中均取得了最佳效果。

    arXiv:2609.06437v1 Announce Type: cross  Abstract: Diffusion-weighted imaging (DWI) is widely used in clinical settings but remains vulnerable to geometric distortion. Conventional correction methods often require additional acquisitions or vendor-specific solutions, limiting their feasibility in high-throughput, resource-constrained settings. This study investigates whether large-scale pretraining strategies can improve deep learning-based distortion correction for single-phase-encoding DWI. We formulate the task as image reconstruction, and compare a non-pretrained baseline against a self-supervised and a generative pretrained model, evaluated using both quantitative image-similarity metrics and qualitative expert assessment. The best-performing model was further tested for transferability on data collected in an LMIC setting with acquisition shift. Pretrained models outperformed the non-pretrained baseline, with cWDM achieving the strongest results across both quantitative and quali
    
[^316]: 训练两层量化神经网络的直通估计器的稳定性与泛化性

    Stability and Generalization of Straight-Through Estimators for Training Two-Layer Quantized Neural Networks

    [https://arxiv.org/abs/2609.06430](https://arxiv.org/abs/2609.06430)

    该论文通过证明零初始化的STE递归恰为凸潜在损失上的随机次梯度下降，首次从算法稳定性角度为不连续的直通估计器训练建立了显式的 $\ell_2$ 平均模型稳定性与泛化界。

    

    我们从统计学习理论（SLT）的视角研究用于训练具有铰链损失的两层二值激活网络的恒等直通估计器（STE）。我们的核心问题是：算法稳定性能否解释由不连续的STE训练规则所产生的估计器的统计泛化性能。在饱和输出机制下，零初始化的逐样本STE递归恰好等价于凸潜在损失 $(-yu^\top x)_+$ 上的随机次梯度下降。这一表示使稳定性分析成为可能。我们为两个耦合更新导出了精确的距离恒等式，并证明了共同样本映射的近似非扩张性，其二次缺陷仅在两个潜在间隔跨越零时出现。随后，我们得到了显式的 $\ell_2$ 平均模型稳定性界与泛化界，并将稳定性等距地从潜在向量转移到完整的第一层权重矩阵上。

    arXiv:2609.06430v1 Announce Type: new  Abstract: We study the identity straight-through estimator (STE) for training a two-layer binary-activation network with hinge loss from the perspective of Statistical Learning Theory (SLT). Our central question is whether algorithmic stability can explain the statistical generalization of the estimator produced by the discontinuous STE training rule. In the saturated-output regime, the zero-initialized samplewise STE recursion is exactly the stochastic subgradient descent on the convex latent loss $(-yu^\top x)_+$. This representation makes a stability analysis possible. We derive an exact distance identity for two coupled updates and prove approximate non-expansiveness of the common-example map, with a quadratic defect only when the two latent margins straddle zero. We then obtain explicit $\ell_2$ on-average model-stability and generalization bounds, transferring stability isometrically from the latent vector to the full first-layer matrix. Com
    
[^317]: 面向更简单加性规则集成的稀疏斜交规则提升方法

    Sparse Oblique Rule Boosting for Simpler Additive Rule Ensembles

    [https://arxiv.org/abs/2609.06426](https://arxiv.org/abs/2609.06426)

    该论文提出了一种稀疏斜交规则提升方法，通过在逻辑命题中引入可学习的稀疏线性变换来扩展经典规则集成，使模型能够以更简单的规则获得足够的预测精度，同时保持良好的可解释性。

    

    小型加性符号规则集成能够提供可解释的预测模型。传统上，这些集成使用的规则条件基于单个输入变量x和阈值t的简单阈值命题（x ≥ t）的合取，在几何上形成轴平行的多面体作为决策区域。虽然这种形式确保了单个规则的高度可解释性，并且可以通过梯度提升方法高效学习，但它依赖于获得一组精心设计的、具有丰富表达力的输入特征，从而使小型轴平行区域集成能够很好地描述目标变量。在缺乏此类特征的情况下，达到足够的预测精度需要增加单个规则的数量和复杂性，这会降低模型的可解释性。本文通过引入包含输入变量可学习稀疏线性变换的逻辑命题，对经典规则集成方法进行了扩展，从而……

    arXiv:2609.06426v1 Announce Type: new  Abstract: Small additive ensembles of symbolic rules offer interpretable prediction models. Traditionally, these ensembles use rule conditions based on conjunctions of simple threshold propositions $x \geq t$ on a single input variable $x$ and threshold $t$, resulting geometrically in axis-parallel polytopes as decision regions. While this form ensures a high degree of interpretability for individual rules and can be learned efficiently using the gradient boosting approach, it relies on having access to a curated set of expressive input features so that a small ensemble of axis-parallel regions can describe the target variable well. Absent such features, reaching sufficient accuracy requires increasing the number and complexity of individual rules, which diminishes the interpretability of the model. Here, we extend classical rule ensembles by introducing logical propositions with learnable sparse linear transformations of input variables, i.e., pr
    
[^318]: 论基于价值的强化学习中的批归一化前向模式

    On BatchNorm Forward Modes in Value-Based Reinforcement Learning

    [https://arxiv.org/abs/2609.06421](https://arxiv.org/abs/2609.06421)

    本文发现在基于价值的强化学习中，只需在BatchNorm的特定前向传播（自举和动作选择）中使用批统计量而非运行统计量，即可逆转离散动作价值学习的性能退化，并显著提升C51和PQN在26个Atari游戏上的表现。

    

    批归一化显著提升了连续控制actor-critic方法（如CrossQ）的样本效率，但近期研究报道其在Atari离散动作价值学习中出现性能下降。这些失败令人意外，因为离散Q网络并不存在CrossQ所指出的动作-输入分布不匹配问题。我们针对基于目标的C51和无目标的PQN研究表明，在特定前向传播中简单地在运行统计量和批统计量之间做出选择即可逆转这种性能退化。在C51中，将BN自举前向传播切换为批统计量模式，其性能显著优于未归一化和LayerNorm基线，并能稳定扩展至高达12的更新-数据比。在PQN中，对动作选择和自举均使用批统计量，可以从失效的运行统计量配置中恢复性能。在26个Atari游戏、400M帧的实验中，该配置取得了更高的最终总体得分。

    arXiv:2609.06421v1 Announce Type: cross  Abstract: Batch normalization (BN) substantially improves sample efficiency in continuous-control actor-critic methods such as CrossQ, yet recent studies report performance degradation in discrete-action value learning on Atari. These failures are surprising because discrete Q-networks lack the action-input distribution mismatch identified by CrossQ. We show for target-based C51 and target-free PQN that the simple choice between running and batch statistics at specific forward passes can reverse this degradation. In C51, switching the BN bootstrap forward to batch-statistic mode significantly improves performance over unnormalized and LayerNorm baselines and scales stably with update-to-data ratios up to 12. In PQN, using batch-statistics for both action selection and bootstrapping recovers performance from the failing running-statistic configuration. Across 26 Atari games at 400M frames, this configuration achieves a higher final aggregate scor
    
[^319]: 将能力与置信度分离：面向GRPO训练医学视觉语言模型的基于视觉证据的双状态校准

    Separating Capability from Confidence: Grounded Dual-State Calibration for GRPO-Trained Medical Vision-Language Models

    [https://arxiv.org/abs/2609.06419](https://arxiv.org/abs/2609.06419)

    提出DualRead方法，将能力学习与置信度估计相分离，通过读取冻结的GRPO训练医学视觉语言模型actor的内部状态（结合回答前可解性评估与回答后视觉支持评估）来校准置信度，并引入CCG-AUC指标评估置信度是否真正基于视觉证据。

    

    医学视觉语言模型（VLM）需要其置信度能够同时反映答案的正确性和患者特定的视觉证据。近年来基于GRPO的方法将言语化置信度与答案生成联合优化。然而，这种联合优化可能干扰答案的学习，并使置信度趋向于接近二值的取值。此外，言语化置信度也无法提供对视觉支持的显式评估。因此，我们将能力学习与置信度估计相分离，提出了DualRead。DualRead建立在一个关键洞察之上：可靠性可以从actor在回答过程中的关键时刻的内部状态中读取。该方法冻结经过GRPO训练的actor，将回答前的可解性评估与回答后对生成答案及其视觉支持的评估相结合。为了进一步评估置信度是否真正反映了视觉依据，我们引入了反事实置信度依据AUC（CCG-AUC）。该指标用于衡量……

    arXiv:2609.06419v1 Announce Type: cross  Abstract: Medical vision-language models (VLMs) require confidence that reflects both answer correctness and patient-specific visual evidence. Recent GRPO-based methods optimize verbalized confidence together with answer generation. However, this joint optimization may interfere with answer learning and drive confidence toward near-binary values. Verbalized confidence also provides no explicit assessment of visual support. We therefore separate capability learning from confidence estimation and propose \textbf{DualRead}. DualRead builds on the insight that reliability can be read from the actor's internal states at critical moments in the answering process. It freezes the GRPO-trained actor and combines pre-answer solvability with a post-answer assessment of the generated answer and its visual support. To further assess whether confidence reflects visual grounding, we introduce \textbf{Counterfactual Confidence Grounding AUC} (CCG-AUC). It measu
    
[^320]: MetaRSI / RSI2：一个针对递归自我改进系统自身的元递归自我改进系统

    MetaRSI / RSI2: A Meta-Recursive Self-Improving System for Recursive Self-Improving Systems Themselves

    [https://arxiv.org/abs/2609.06396](https://arxiv.org/abs/2609.06396)

    该论文提出 MetaRSI-v1 系统，通过 Data-RSI、Harness-RSI 和 Model-RSI 三个算子的统一组合，将递归自我改进从机器可验证的编程与数学基准扩展到真实的科学、工程和元科学等开放性领域。

    

    递归自我改进（RSI）使系统能够从自身的失败中改进模型构建机制，从而使后续的每个模型都继承这一收益。然而，RSI 几乎只在编程和形式化基准（如科学问答和数学）上得到验证。这种格式限制使 RSI 只能在机器可验证的狭小范围内进行改进，而非在问题开放、正确性由论证、复现或测量来确定的通用能力领域。我们认为，RSI 下一步必须跨真实的、多样化的科学、工程和元科学领域运行，而不能局限于形式化评估仅仅可行的地方。为此，我们提出了 MetaRSI-v1，其中改进是在统一范式下三个类型化算子的按计划组合：Data-RSI 放大现有能力并标记其边界；Harness-RSI 在不触及权重的情况下编辑五槽支架；Model-RSI 通过有界训练将能力内化到模型参数中。

    arXiv:2609.06396v2 Announce Type: new  Abstract: Recursive self-improvement (RSI) lets a system improve the model-building machinery from its own failures, so every later model inherits the gain. Yet RSI has been validated almost exclusively on coding and formal benchmarks such as science QA and mathematics. This format bound limits RSI to improvement within a machine-checkable slice, not general capability where questions are open and correctness is settled by argument, replication, or measurement. We argue RSI must next operate across real, diverse scientific, engineering, and meta-scientific domains, not where formal evaluation is merely tractable. To that end we present MetaRSI-v1, where improvement is the scheduled composition of three typed operators over one unified paradigm. Data-RSI amplifies existing competence and marks its boundary; Harness-RSI edits a five-slot scaffold without touching weights; Model-RSI internalizes capability into parameters through bounded training. Sh
    
[^321]: 通过行列式采样超越最坏情况的k-聚类核心集界

    Beyond Worst-Case Coreset Bounds for $k$-Clustering via Determinantal Sampling

    [https://arxiv.org/abs/2609.06394](https://arxiv.org/abs/2609.06394)

    该论文提出了一种基于行列式点过程的新相关采样框架——行列式采样，证明在温和自然的数据分布假设下，可以为(k,z)-聚类问题构造出显著超越最坏情况界的小型ε-核心集。

    

    现代机器学习中的大规模数据集使数据约简成为一项核心挑战，尤其是对于聚类任务，内存和计算限制要求既紧凑又忠实的摘要。一种标准方法是构建ε-核心集：一个小型的加权子集，能够对每个可能的中心选择近似地保持聚类代价。对于(k,z)-聚类问题，现有的核心集大小最坏情况界本质上是紧的，这在一般情况下排除了存在显著更小核心集的可能性。然而，这类最坏情况的实例往往无法代表真实世界的数据。在这项工作中，我们证明了对底层数据分布施加温和且自然的假设后，可以构造出显著更小的核心集。我们引入了一种新的相关采样框架，称为行列式采样，它是行列式点过程的一种新颖应用。利用该框架……

    arXiv:2609.06394v1 Announce Type: cross  Abstract: Massive datasets in modern machine learning have made data reduction a central challenge, particularly for clustering tasks where memory and computational constraints demand compact yet faithful summaries. A standard approach is to construct an \textit{$\epsilon$-coreset}: a small weighted subset that approximately preserves the clustering cost for every plausible choice of centers. For the \textit{$(k,z)$-clustering problem}, existing worst-case bounds on coreset size are essentially tight, ruling out substantially smaller coresets in general. However, such worst-case instances are often unrepresentative of real-world data. In this work, we show that significantly smaller coresets are possible under mild and natural assumptions on the underlying data distribution. We introduce a new correlated sampling framework, called \textit{determinantal sampling}, based on a novel application of determinantal point processes. Using this framework
    
[^322]: GRPO组内的验证器误差是否独立？来自Qwen2.5 rollout的证据

    Are Verifier Errors Independent Within a GRPO Group? Evidence from Qwen2.5 Rollouts

    [https://arxiv.org/abs/2609.06386](https://arxiv.org/abs/2609.06386)

    该研究发现GRPO组内的验证器误差并非独立——组内误差相关性高达0.530，使八个完成结果的有效样本量降至1.70，且这种依赖程度在不同答案形式间差异显著，表明假设验证器误差独立的分析可能存在系统性偏差。

    

    基于可验证奖励的群组强化学习（RLVR）使用自动验证器对每个提示的多个完成结果进行评分。基于独立验证器误差假设的分析可能忽略了与共享答案格式相关的依赖性。我们在由Qwen2.5-1.5B在MATH、GSM8K和DeepMath-103K上生成的24,998个组（每组八个完成结果）中研究了这种依赖性。我们估计汇总的组内验证器误差相关性为0.530（95%置信区间：0.500–0.560）。在可交换误差模型下，这对应于八完成组经设计效应调整后的有效样本量仅为1.70。依赖性在不同答案形式之间差异显著：分数、根式、符号表达式和区间表现出比单位标注和百分号更强的误差聚类。通过在四种基于规则的验证器配置下重放组相对优势，我们发现最多有0.83%的组存在至少一个优势符号不一致的情况。

    arXiv:2609.06386v1 Announce Type: new  Abstract: Group-based reinforcement learning with verifiable rewards (RLVR) scoresmultiple completions per prompt using automatic verifiers. Analysesbased on independent verifier errors may overlook dependence associatedwith shared answer formats. We investigate this dependence in24,998 groups of eight completions generated by Qwen2.5-1.5B onMATH, GSM8K, and DeepMath-103K. We estimate a pooled within-groupverifier-error correlation of 0.530 (95% confidence interval:0.500--0.560). Under an exchangeable-error model, this correspondsto a design-effect-adjusted effective sample size of 1.70 for aneight-completion group. Dependence varies substantially across answerforms: fractions, radicals, symbolic expressions, and intervals exhibitstronger clustering than unit annotations and percent signs. Replayinggroup-relative advantages across four rule-based verifier configurationsidentifies at least one advantage-sign disagreement in up to 0.83% ofgroups. Be
    
[^323]: 欧几里得公平 k-中心聚类的参数化与流式算法

    Parameterized and Streaming Algorithms for Euclidean Fair $k$-Center Clustering

    [https://arxiv.org/abs/2609.06384](https://arxiv.org/abs/2609.06384)

    本文针对欧几里得公平 k-中心聚类问题，首次提出了近似比为 2.732 的参数化近似算法和近似比为 4.464 的单遍流式算法，并在运行时间对 k 呈指数级时将两者近似比分别提升至 2.414 和 3.828。

    

    受机器学习中公平性日益重要的推动，公平 k-中心聚类作为一个基础问题引起了广泛的研究关注。在该问题中，数据集被划分为 m 个不相交的组，目标是在每组所选中心数量受上界约束的条件下，选取 k 个数据点作为中心，以最小化任意数据点与其分配中心之间的最大距离。聚焦于在机器学习应用中无处不在的欧几里得空间，我们首先为欧几里得公平 k-中心问题开发了一种近似比为 2.732 的参数化近似算法。通过将该算法作为后处理阶段并入面向大规模数据的单遍流式框架中，我们获得了 4.464 的近似比。在运行时间对 k 呈指数级的情况下，这两个近似比可分别进一步改进为 2.414 和 3.828。为确保……（摘要在此处截断）

    arXiv:2609.06384v1 Announce Type: cross  Abstract: Motivated by the growing importance of fairness in machine learning, fair $k$-center clustering has attracted considerable research attention as a fundamental problem. In this problem, a dataset is partitioned into $m$ disjoint groups, and the objective is to select $k$ data points as centers, subject to upper bounds on the number of centers chosen from each group, aiming to minimize the maximum distance between any data point and its assigned center. Focusing on Euclidean spaces, which are ubiquitous in machine learning applications, we first develop a parameterized approximation algorithm for Euclidean fair $k$-center with an approximation ratio of $2.732$. By incorporating this algorithm as a post-processing stage into a one-pass streaming framework for large-scale data, we obtain an approximation ratio of $4.464$. These ratios can be further respectively improved to $2.414$ and $3.828$ with a runtime exponential on $k$. To ensure p
    
[^324]: 使用归一化流恢复微弱信号

    Recovering Weak Signals with Normalizing Flows

    [https://arxiv.org/abs/2609.06382](https://arxiv.org/abs/2609.06382)

    本文提出一种基于归一化流模型的方法，通过利用目标信号的统计不变性，有效重建和恢复在校准过程中被主导干扰信号掩盖或抑制的微弱信号成分。

    

    在许多科学学科中，感兴趣的微弱信号会被比其强几个数量级的主导性干扰信号所掩盖。恢复这些微弱信号需要扣除主导信号；然而，这种校准过程本身会不可避免地扭曲或部分抑制感兴趣的底层信号。为解决这一问题，我们提出使用归一化流模型来重建受校准影响的微弱信号。通过利用目标信号的统计不变性并假设初始抑制程度最小，我们的框架能够有效恢复丢失的信号成分。我们对这一基于归一化流的恢复方法提供了全面的理论概述，并通过模拟数据证明了其有效性。

    arXiv:2609.06382v1 Announce Type: cross  Abstract: In many scientific disciplines, weak signals of interest are obscured by dominant nuisance signals that are several orders of magnitude stronger. Recovering these weak signals requires subtracting the dominant ones; however, this calibration process inherently distorts or partially suppresses the underlying signal of interest. To address this problem, we propose the use of normalizing flow models to reconstruct calibration-affected weak signals. By leveraging the statistical invariance of the target signals and assuming minimal initial suppression, our framework effectively recovers the lost signal components. We provide a comprehensive theoretical overview of this normalizing flow-based recovery method and demonstrate its efficacy using simulated data.
    
[^325]: 鲁棒共形共识：基于共形预测的多智能体LLM-as-a-Judge区间评估

    Robust Conformal Consensus: Multi-Agent LLM-as-a-Judge Interval Evaluation with Conformal Prediction

    [https://arxiv.org/abs/2609.06367](https://arxiv.org/abs/2609.06367)

    提出了一种基于共形预测的多智能体LLM-as-a-Judge鲁棒不确定性估计框架，通过聚合多个LLM评判者的预测区间，实现了具有覆盖率保证的更稳定、更可靠的不确定性量化。

    

    LLM-as-a-Judge已成为评估自然语言生成的一种有前景的范式。然而，与此类评估相关的不确定性在很大程度上仍未被探索，这限制了其在现实应用中的可靠性。尽管共形预测为不确定性量化提供了一个有原则的框架，但现有方法通常将其应用于单个LLM评判者，忽视了使用不同LLM评估器所引入的变异性。在这项工作中，我们提出了一种面向多智能体LLM-as-a-Judge评估的鲁棒不确定性估计框架。我们的方法为来自多个LLM的评分构建共形预测区间。通过综合考量来自不同LLM评判者的区间，我们获得了更加稳定和可靠的不确定性估计。大量实验表明，我们的方法能够产生具有覆盖率保证的有效预测区间，并且基于区间对多个评判者进行聚合能够带来更好的效果。

    arXiv:2609.06367v1 Announce Type: cross  Abstract: LLM-as-a-Judge has emerged as a promising paradigm for evaluating natural language generation. However, the uncertainty associated with such evaluations remains largely unexplored, which limits their reliability in real-world applications. Although conformal prediction offers a principled framework for uncertainty quantification, existing approaches typically apply it to a single LLM judge, overlooking the variability introduced by using different LLM evaluators. In this work, we propose a robust uncertainty estimation framework for multi-agent LLM-as-a-Judge evaluation. Our approach constructs conformal prediction intervals for LLM-based scores from multiple LLMs. By considering intervals from different LLM judges, we obtain more stable and reliable uncertainty estimates. Extensive experiments demonstrate that our method produces valid prediction intervals with coverage guarantees, and that interval-based aggregation across multiple j
    
[^326]: AGSA-Net：面向光谱解混感知高光谱遥感图像分类的丰度引导自注意力网络

    AGSA-Net: Abundance-Guided Self-Attention Network for Spectral Unmixing-Aware Hyperspectral Remote Sensing Image Classification

    [https://arxiv.org/abs/2609.06359](https://arxiv.org/abs/2609.06359)

    提出丰度引导自注意力网络AGSA-Net，将光谱解混先验显式融入高光谱图像分类，利用丰度亲和先验引导光谱Transformer强化类别判别性特征的建模与融合。

    

    高光谱图像（HSI）分类在农业、环境监测和城市分析等遥感应用中发挥着至关重要的作用。然而，其性能仍面临高光谱冗余、噪声敏感性，以及难以联合建模局部物质组成与长程光谱依赖性等挑战。为解决这些问题，我们提出了AGSA-Net，一种丰度引导的自注意力网络，将光谱解混先验显式地整合到分类过程中。AGSA-Net首先在非负性与和为一约束条件下估计具有物理意义的亚像元丰度图，并通过线性-非线性混合重建解码器进行正则化。随后，学习到的丰度被用于构建丰度亲和先验，引导光谱Transformer强化类别判别性交互，并将所得的Transformer特征与紧凑的丰度描述符进行融合……

    arXiv:2609.06359v1 Announce Type: cross  Abstract: Hyperspectral image (HSI) classification plays a vital role in remote sensing applications, including agriculture, environmental monitoring, and urban analysis. However, its performance remains challenged by high spectral redundancy, noise sensitivity, and the difficulty of jointly modeling local material composition and long-range spectral dependencies. To address this, we propose AGSA-Net, an abundance-guided self-attention network that explicitly integrates spectral unmixing priors into the classification process. AGSA Net first estimates physically meaningful subpixel abundance maps subject to non-negativity and sum-to-one constraints, regularized by hybrid linear-nonlinear reconstruction decoder. The learned abundances are then used to construct an abundance affinity prior that guides a spectral transformer to emphasize class-discriminative interactions, and the resulting transformer features are fused with compact abundance descr
    
[^327]: 基于净化与选择性恢复的后门攻击下持续学习鲁棒动态扩展方法

    Robust Dynamic Expansion for Continual Learning under Backdoor Attacks via Purification and Selective Recovery

    [https://arxiv.org/abs/2609.06346](https://arxiv.org/abs/2609.06346)

    该论文提出了后门攻击下持续学习（CLUBA）这一新设定，并设计了一个融合样本净化与选择性恢复的鲁棒动态扩展框架，使模型能够在序列学习过程中同时缓解灾难性遗忘并抵御恶意样本的污染。

    

    持续学习（CL）使模型能够从按顺序到达的任务中获取新知识，同时保留先前学到的知识。然而，在实际场景中，从不可信来源收集的任务流可能包含后门中毒样本，这对持续学习器的稳定性、可塑性和安全性构成了重大挑战。在这项工作中，我们研究了一个具有挑战性的新设定，称为后门攻击下的持续学习（CLUBA），其中每个增量任务可能包含少量恶意操纵的训练样本。与传统的持续学习或后门防御场景不同，CLUBA 要求模型在顺序更新过程中同时缓解灾难性遗忘、保持适应能力，并防止吸收恶意监督信息。为了应对这一挑战，我们提出了一个鲁棒的动态扩展框架，该框架集成了样本净化与选择性恢复机制。

    arXiv:2609.06346v1 Announce Type: new  Abstract: Continual learning (CL) enables models to acquire new knowledge from sequentially arriving tasks while retaining previously learned knowledge. However, in practical scenarios, task streams collected from untrusted sources may contain backdoor-poisoned samples, posing a critical challenge to the stability, plasticity, and security of continual learners. In this work, we investigate a challenging setting termed Continual Learning Under Backdoor Attack (CLUBA), where each incremental task may involve a small proportion of maliciously manipulated training samples. Unlike conventional continual learning or backdoor defense scenarios, CLUBA requires models to simultaneously mitigate catastrophic forgetting, preserve adaptation capability, and prevent the absorption of malicious supervision during sequential updates. To address this challenge, we propose a robust dynamic-expansion framework that integrates sample purification, selective recover
    
[^328]: 高效注意力的线性代数基础：SVD压缩下秩坍缩的相位反转现象

    Linear Algebra Foundations of Efficient Attention: A Phase Reversal in Rank Collapse Under SVD Compression

    [https://arxiv.org/abs/2609.06341](https://arxiv.org/abs/2609.06341)

    本文整合了十四篇关于线性代数技术在transformer基础模型中应用的研究，并原创性地发现了SVD压缩方法与网络自然秩坍缩相互作用中的相位反转现象。

    

    线性代数提供了现代人工智能用于在神经网络中编码、压缩和传播信息的概念框架（矩阵秩、奇异值分解（SVD）和特征分解）。本文统一整合了十四篇经同行评审的独立研究工作，分析了这些技术在基于transformer的基础模型研究中的应用，聚焦于该主题的三个领域：自注意力矩阵输出秩的推导与性质、有目的地利用这一现象的压缩方法，以及低秩键值（KV）缓存投影及其与线性注意力和状态空间结构化模型之间的半可分矩阵对偶性。我们在观察到该文献中一个悬而未决的问题后产生了开展这项工作的动机：上述压缩方法与网络自然秩坍缩之间的相互作用。通过本文，我们报告了一项原创发现，即SVD压缩下秩坍缩的相位反转现象。

    arXiv:2609.06341v1 Announce Type: cross  Abstract: Linear algebra provides the framework of concepts (matrix rank, singular value decomposition (SVD), and eigendecomposition) that modern artificial intelligence employs to encode, compress, and propagate information through neural networks. This paper unifies fourteen separate peer-reviewed works analyzing the usage of these techniques in the context of transformer-based foundation model research, focusing on three areas of the topic: derivations and properties of self-attention matrices' output rank, compression methods that purposefully utilize this phenomenon, and the low-rank key-value (KV) cache projection and its semiseparable-matrix duality to linear attention and state-space structured models. We were motivated to conduct this work after observing an open problem in this literature: the interplay of the mentioned compression methods with natural rank collapse of the network. With this paper, we report an original finding that us
    
[^329]: 单一共享LoRA权重实现跨加速因子的MRI重建

    One Shared LoRA Weight for MRI Reconstruction across Acceleration Factors

    [https://arxiv.org/abs/2609.06338](https://arxiv.org/abs/2609.06338)

    提出Shared LoRA参数高效框架，通过冻结预训练SHFormer骨干、训练单一共享的LoRA适配器与轻量级门控网络GateNet，实现跨多个加速因子的MRI重建，在保持最佳或竞争力的重建质量的同时大幅降低训练与存储成本。

    

    加速MRI重建旨在从欠采样的k空间数据中恢复图像。然而，不同的加速因子会产生不同的伪影模式。现有方法通常针对每个加速因子训练单独的模型，导致跨因子泛化能力差，且训练和存储成本高昂。我们提出了Shared LoRA，一个参数高效的框架，它冻结预训练的SHFormer骨干网络，训练单一共享的LoRA适配器集合，并配合一个轻量级的门控网络。在训练过程中，通过随机采样加速因子及其对应的采样掩码来生成欠采样输入，使共享适配器能够学习跨因子的重建知识。给定加速因子后，GateNet生成逐层系数，动态调节每个适配器的残差强度。实验表明，Shared LoRA在各加速因子下取得了最佳或具有竞争力的PSNR和SSIM，同时…

    arXiv:2609.06338v1 Announce Type: cross  Abstract: Accelerated MRI reconstruction recovers images from undersampled k-space. However, different acceleration factors produce distinct artifact patterns. Existing methods often train separate models for each factor, leading to poor cross-factor generalization and high training and storage costs. We propose Shared LoRA, a parameter-efficient framework that freezes the pretrained SHFormer backbone and trains a single shared set of LoRA adapters together with a lightweight gating network. During training, undersampled inputs are generated by randomly sampling acceleration factors and their corresponding sampling masks, enabling the shared adapters to learn reconstruction knowledge across factors. Given the acceleration factor, GateNet generates layer-wise coefficients to dynamically modulate the residual strength of each adapter. Experiments show that Shared LoRA achieves the best or competitive PSNR and SSIM across acceleration factors, whil
    
[^330]: 从群众中深度学习：星系形态分类的基础

    Deep learning from the crowd Fundamentals of morphological galaxy classification

    [https://arxiv.org/abs/2609.06316](https://arxiv.org/abs/2609.06316)

    该研究以Galaxy Zoo 1为实验平台，系统探索了基于群众标注训练卷积神经网络进行星系形态分类的多种策略（如层级结构、分阶段迁移训练与模型集成），为众包数据驱动的深度学习天文分类奠定了方法基础。

    

    目的：本工作的目标是调整深度神经网络模型，使其能够基于群众标注进行训练并完成星系形态分类，同时考虑训练方案、标注者之间的一致性以及分类层级结构。方法：我们以Galaxy Zoo 1作为实验测试平台，训练卷积神经网络（CNN）对星系形态进行自动分类。我们分析了以下几个方面对分类准确率和训练效率的影响：(i) 仅训练最后一层与训练整个网络的对比；(ii) 仅使用CNN分类与考虑层级结构的对比；(iii) 比较使用不同数据量和不同标注者一致性水平训练的模型；(iv) 分阶段训练，将知识从一个模型迁移到另一个模型；(v) 将多个模型组合为集成模型。结果：从实验中我们得出以下结果：（注：原文摘要在此处截断，结果部分内容不完整）

    arXiv:2609.06316v1 Announce Type: cross  Abstract: Aims. The objective of this work is to adapt a deep neural network model to perform galaxy morphological classification trained from crowd annotations, considering the training scheme, the agreement between the annotators, and the hierarchy.   Methods. We use Galaxy Zoo 1 as our experimental testbed and trained a convolutional neural network (CNN) for the automatic classification of galaxies' morphologies. We analyze the impact of the following aspects on the classification accuracy and training efficiency: (i) Training only the last layer vs. training all the network; (ii) Classification with only the CNN vs. considering the hierarchy; (iii) Comparing the models trained with different amounts of data and levels of agreement between the annotators; (iv) Training by stages, transferring knowledge from one model to another; and (v) Combining several models as an ensemble.   Results From the experiments, we derive the following results: (
    
[^331]: 变分量子分布学习的分层傅里叶逼近方法

    Hierarchical Fourier Approximation for Variational Quantum Distribution Learning

    [https://arxiv.org/abs/2609.06307](https://arxiv.org/abs/2609.06307)

    该论文提出一种基于布尔立方体上分层Walsh-Fourier逼近的变分量子分布学习框架，通过逐层热启动训练量子电路Born机器，并证明了逼近误差由被省略的傅里叶质量决定的端到端学习理论保证。

    

    我们通过布尔立方体上的Walsh-Fourier逼近层级结构来研究变分量子分布学习。在每一层，选定的目标傅里叶系数集合定义了一个谱截断，该谱截断被投影到概率单纯形上，并用作量子电路Born机器的学习目标。在某一层学习到的参数通过热启动映射来初始化下一层的训练。我们证明了一个端到端的期望学习保证，其中逼近误差项由被省略的傅里叶质量决定，同时一个归一化的无偏估计量为经验截断提供了明确的统计界限。随后，我们针对全变差距离实例化了抽象的差异条件，并将由此产生的分布误差与量子态保真度联系起来。在我们的归一化ℓ₂约定下，全变差特化会产生显式因子2^(n-1)，因此仅对足够……（摘要在此处被截断）

    arXiv:2609.06307v1 Announce Type: cross  Abstract: We study variational quantum distribution learning through a hierarchy of Walsh--Fourier approximations on the Boolean cube. At each level, a selected set of target Fourier coefficients defines a spectral truncation, which is projected onto the probability simplex and used as the target of a quantum circuit Born machine. Parameters learned at one level initialize the next through a warm-start map. We prove an end-to-end expected learning guarantee where the approximation term is determined by the omitted Fourier mass, while a normalized unbiased estimator yields an explicit statistical bound for empirical truncations. We then instantiate the abstract discrepancy conditions for total variation distance and relate the resulting distributional error to quantum-state fidelity. The total-variation specialization incurs the explicit factor $2^{n-1}$ under our normalized $\ell_2$ convention and is therefore informative only for sufficiently c
    
[^332]: 通过利用多个结果实现样本高效CATE估计的表示学习

    Representation Learning for Sample-Efficient CATE Estimation by Leveraging Multiple Outcomes

    [https://arxiv.org/abs/2609.06294](https://arxiv.org/abs/2609.06294)

    该论文提出利用历史数据中丰富的多结果测量信息，通过因果表示学习将高维协变量降维到保留处理效应异质性的低维表示，从而在实验样本有限的情况下实现更高效的CATE估计。

    

    估计条件平均处理效应（CATE）能够实现对干预措施的高效目标定位，但许多应用的实验样本有限，使得从高维协变量中估计异质性效应变得困难。在这种情况下，政策制定者和医疗从业者往往陷入维度灾难，或使用可能无法保留处理异质性的现成降维方法。然而，这些领域通常拥有大量测量了广泛结果的历史数据集——这是一个在实践中很少被利用的监督信号来源。遵循因果表示学习的思路，我们假设这类具有高维协变量的领域存在低维的底层动态。因此，我们可以利用历史数据中测量的多样化结果来学习协变量的低维表示。在理论方面，我们证明了当辅助结果...

    arXiv:2609.06294v1 Announce Type: new  Abstract: Estimating conditional average treatment effects (CATE) enables efficient targeting of interventions, but many applications have limited experimental samples, making it difficult to estimate heterogeneous effects from high-dimensional covariates. In such settings, policymakers and medical practitioners often succumb to the curse of dimensionality or apply off-the-shelf dimension reduction methods that may not preserve treatment heterogeneity. Yet these domains often come with large historical datasets measuring a wide range of outcomes -- a source of supervision that is rarely exploited in practice. Following causal representation learning, we hypothesize that such domains with high-dimensional covariates have lower-dimensional underlying dynamics. We can thus leverage the diverse outcomes measured in historical data to learn a lower-dimensional representation of the covariates. Theoretically, we prove that when the auxiliary outcomes sa
    
[^333]: 转向几何：在大语言模型转向空间中验证人类价值几何结构

    Steering Geometry: Validating Human Value Geometry in LLM Steering Space

    [https://arxiv.org/abs/2609.06289](https://arxiv.org/abs/2609.06289)

    该研究基于施瓦茨基本人类价值理论构建了包含26K样本、覆盖20种人类价值的基准，首次系统验证了大语言模型转向向量的潜在几何结构能够反映人类价值的理论规定结构。

    

    随着大语言模型（LLM）越来越多地被部署在对齐敏感的场景中，激活转向已成为一种轻量级的推理时行为控制方法，可作为微调方法（如RLHF、DPO）的替代方案。然而，现有工作通常在孤立的行为上验证转向效果，这使得转向向量究竟是编码了连贯的语义结构，还是仅仅利用了特定行为的捷径，仍不清楚。我们研究了LLM转向向量的潜在几何结构是否反映了人类价值与道德领域中被理论所规定的结构。以施瓦茨基本人类价值理论作为我们的主要细粒度框架，我们引入了一个包含26K样本、覆盖20种人类价值的基准，并在多种模型家族和规模上分析了分布驱动方法（如CAA、SphericalSteer、ODESteer）和以行为为中心的方法（如COLD-Steer、BiPO）。我们发现分布驱动方法能够恢复人类价值……（原文在此处截断）

    arXiv:2609.06289v1 Announce Type: cross  Abstract: As large language models (LLMs) are increasingly deployed in alignment-sensitive contexts, activation steering has emerged as a lightweight, inference-time alternative to fine-tuning methods (e.g., RLHF, DPO) for behavioral control. However, existing work typically validates steering on isolated behaviors, leaving it unclear whether steering vectors encode coherent semantic structure or merely exploit behavior-specific shortcuts. We investigate whether the latent geometry of LLM steering vectors reflects theory-specified structure in human values and morality. Using Schwartz's Theory of Basic Human Values as our primary fine-grained framework, we introduce a 26K-sample benchmark covering 20 human values and analyze distribution-driven methods (e.g., CAA, SphericalSteer, ODESteer) and behavior-centric approaches (e.g., COLD-Steer, BiPO) across diverse model families and sizes. We find that distribution-driven methods recover human value
    
[^334]: 针对相异性数据的鲁棒条件降维方法

    Robust conditional dimension reduction for dissimilarity data

    [https://arxiv.org/abs/2609.06284](https://arxiv.org/abs/2609.06284)

    本文提出鲁棒条件多维标度（rcMDS），通过用Fair M-估计目标替代平方应力准则，并设计重加权条件SMACOF算法进行优化，有效解决了异常值对相异性数据条件降维的干扰问题。

    

    条件降维（cDR）在学习低维潜在坐标的同时，考虑代表数据中已知变异来源的观测协变量。条件多维标度（cMDS）是一种直接处理相异性数据的条件降维技术。然而，其标准的平方应力公式对受污染的相异性数据较为敏感，因为异常值可能会主导目标函数并扭曲学习到的配置。我们通过用Fair M-估计目标替代平方应力准则，提出了鲁棒条件多维标度（rcMDS），并开发了一种重加权的条件SMACOF算法来优化该目标。所提出的算法具有计算上可行的更新方式，其稳定后的目标值单调递减并收敛到有限极限。在合成数据和真实数据上的实验表明，该方法在处理受污染数据时表现优异。

    arXiv:2609.06284v1 Announce Type: cross  Abstract: Conditional dimension reduction (cDR) learns low-dimensional latent coordinates while accounting for observed covariates that represent known sources of variation in the data. Conditional Multidimensional Scaling (cMDS) is a cDR technique that works directly with dissimilarity data. Its standard squared-stress formulation, however, is sensitive to contaminated dissimilarity, since outliers can dominate the objective and distort the learned configuration. We proposed Robust Conditional Multidimensional Scaling (rcMDS) by replacing the squared-stress criterion with a Fair M-estimation objective. We developed a reweighted conditional SMACOF algorithm to optimize this objective. The proposed algorithm admits computationally tractable updates, and its stabilized objective values decrease monotonically and converge to a finite limit. Experiments on synthetic and real data show that the pro
    
[^335]: SeaCausal-FL：面向海事物联网故障诊断与反事实推理的联邦模糊因果学习

    SeaCausal-FL: Federated Fuzzy Causal Learning for Maritime IoT Fault Diagnosis and Counterfactual Reasoning

    [https://arxiv.org/abs/2609.06257](https://arxiv.org/abs/2609.06257)

    提出SeaCausal-FL联邦模糊因果学习框架，通过区间二型模糊层与物理约束结构因果模型对齐分布式机制，并采用多维度证据加权聚合，实现了海事物联网中船舶发动机的可靠故障诊断与区间反事实推理。

    

    海事物联网中可靠的船舶发动机故障诊断面临分布式数据所有权、异构故障分布以及持续变化的运行条件等挑战。本文提出了SeaCausal-FL，这是一种联邦模糊因果学习框架，它将共享的时间诊断路径与基于机制的因果推理相结合。该框架采用区间二型模糊层来表示不确定且相互重叠的运行机制，同时每个机制都与一个物理约束的结构因果模型相关联。在聚合之前，利用运行上下文、因果结构和条件干预-响应特征对本地学习的机制进行对齐。随后，模型参数根据样本、类别、机制以及机制-类别证据进行聚合，而不仅仅依据客户端样本量。学习到的结构方程还进一步通过溯因、行动和预测支持区间反事实推理。

    arXiv:2609.06257v1 Announce Type: new  Abstract: Reliable marine-engine fault diagnosis in maritime IoT is challenged by distributed data ownership, heterogeneous fault distributions, and continuously changing operating conditions. This paper proposes SeaCausal-FL, a federated fuzzy causal learning framework that combines a shared temporal diagnostic path with mechanism-conditioned causal reasoning. An interval type-2 fuzzy layer represents uncertain and overlapping operating mechanisms, while each mechanism is associated with a physics-constrained structural causal model. Before aggregation, locally learned mechanisms are aligned using operating context, causal structure, and conditional intervention-response signatures. Model parameters are then aggregated according to sample, class, mechanism, and mechanism-class evidence instead of client sample size alone. The learned structural equations further support interval counterfactual reasoning through abduction, action, and prediction. 
    
[^336]: 面向缺陷定位的空间注意力监督：在扩散增强缺陷检测中将真实掩码用作训练信号

    Spatial Attention Supervision for Defect Localization: Exploiting Ground-Truth Masks as Training Signal in Diffusion-Augmented Defect Detection

    [https://arxiv.org/abs/2609.06232](https://arxiv.org/abs/2609.06232)

    该论文的核心创新是将真实缺陷掩码从传统的仅用于评估转变为分类网络训练的空间监督信号，通过基于激活的注意力对齐损失引导卷积特征图聚焦缺陷区域，并在混合监督框架下结合DDPM扩散增强实现合成图像提供数据量、掩码提供空间精度的互补优势，从而提升缺陷定位能力。

    

    工业检测数据集中的真实缺陷掩码通常仅用于评估。本文将这些掩码重新用作分类网络训练过程中的空间监督信号，不仅教会模型预测什么，还教会模型应该关注哪里。该方法添加了一个基于激活的注意力对齐损失，引导卷积特征图聚焦于缺陷区域，并采用混合监督的公式化设计，同时兼容没有掩码的样本，例如扩散生成的图像。结合DDPM数据增强，合成图像贡献了数据数量，而掩码贡献了空间精度。我们在MVTec-AD瓶子基准上评估了85个模型（四个CNN骨干网络在2×2数据/训练因子设计下、五个随机种子，以及一个Swin-V2-T Transformer基线），定位性能在从分类器梯度更新中排除的保留缺陷图像上进行测量。主要发现：（1）注意力引导的训练改善了基于激活的…

    arXiv:2609.06232v1 Announce Type: cross  Abstract: Ground-truth defect masks in industrial inspection datasets are typically reserved for evaluation. This paper repurposes them as spatial supervision signals during training of classification networks, teaching a model not just what to predict but where to look. The method adds an activation-based attention alignment loss that steers convolutional feature maps toward defect regions, in a mixed-supervision formulation that also accommodates samples without masks, such as diffusion-generated images. Combined with DDPM augmentation, synthetic images contribute quantity while masks contribute spatial precision. We evaluate 85 models (four CNN backbones under a 2x2 data/training factorial over five seeds, plus a Swin-V2-T transformer baseline) on the MVTec-AD bottle benchmark, with localization measured on held-out defect images excluded from classifier gradient updates. Main findings: (1) attention-guided training improves activation-based 
    
[^337]: 基于重启朗之万动力学的快速PAC全局优化：探索、开发与退化冷却

    Fast PAC Global Optimization via Restarted Langevin: Exploration, Exploitation, and Degenerate Cooling

    [https://arxiv.org/abs/2609.06196](https://arxiv.org/abs/2609.06196)

    本文提出并行重启朗之万与朗之万-梯度混合两种新算法，通过随机动力学实现全局探索、梯度流实现局部开发，为光滑非凸函数的全局优化提供了快速的PAC性能保证。

    

    我们研究光滑（可能非凸）目标函数 $\Gamma:\mathbb{R}^d\to\mathbb{R}$ 的全局优化所需的计算量。如果一个算法的输出 $\widehat X$ 满足 $\mathbb{P}\{\Gamma(\widehat X)-\Gamma^\star>\varepsilon\}\leq\delta$，则称该算法满足 $(\varepsilon,\delta)$-PAC 性能要求。算法设计与分析均在连续时间框架下进行。我们将经典的模拟退火和固定温度朗之万扩散与本文引入并分析的两种新方法进行比较：并行重启朗之万方法，以及一种朗之万-梯度混合方案，该方案使用随机动力学进行全局探索，使用梯度流进行局部开发。设 $L=\log(1/\delta)$，$E_*$ 表示主导能垒。在低温区域的对数精度下，前两种方法需要与 $L/\varepsilon$ 呈指数关系的模拟时间。对于并行固定温度朗之万，适当数量的独立……

    arXiv:2609.06196v1 Announce Type: cross  Abstract: We study the computational effort required for global optimization of a smooth, possibly nonconvex objective $\Gamma:\mathbb{R}^d\to\mathbb{R}$. An algorithm satisfies the $(\varepsilon,\delta)$-PAC performance requirement if its output $\widehat X$ obeys $\mathbb{P}\{\Gamma(\widehat X)-\Gamma^\star>\varepsilon\}\leq\delta$. Algorithm design and analysis are in continuous time. We compare classical simulated annealing and fixed-temperature Langevin diffusion with two approaches introduced and analyzed here: parallel-restart Langevin and a Langevin--gradient scheme using stochastic dynamics for global exploration and gradient flow for local exploitation.   Let $L=\log(1/\delta)$ and let $E_*$ denote the dominant energy barrier. At logarithmic precision in the low-temperature regime, the first two approaches require simulation time exponential in $L/\varepsilon$. For parallel fixed-temperature Langevin, an appropriate number of independe
    
[^338]: EgoNeMo：基于自我中心激光雷达扫描的可迁移行人动态地图

    EgoNeMo: Transferable Map of Pedestrian Dynamics via Egocentric LiDAR Scan

    [https://arxiv.org/abs/2609.06195](https://arxiv.org/abs/2609.06195)

    本文提出EgoNeMo框架，仅利用自我中心激光雷达点云训练连续的行人动态地图，结合位置平衡采样、多任务学习与视点感知损失，使动态地图能够泛化到未知环境，无需在每个新地点积累轨迹数据。

    

    本文提出了一种可迁移的动态地图框架，该框架仅使用自我中心的3D激光雷达点云即可泛化到未知环境，从而克服了传统动态地图方法的长期局限。尽管动态地图对于编码人类运动特征、实现准确的行人轨迹预测或安全的机器人导航至关重要，但传统方法存在特定场所的局限，需要在每个新位置进行详尽的轨迹积累。基于神经隐式建模的最新进展，我们的框架在多样化环境中训练出一个连续的、基于激光雷达的动态地图估计器。为了缓解真实世界轨迹数据固有的稀疏性和时间偏差，我们引入了位置平衡采样策略和多任务学习架构，该架构联合预测运动分布和空间频率分数图。后者进一步通过视点感知损失进行增强，以补偿……

    arXiv:2609.06195v1 Announce Type: new  Abstract: This paper proposes a transferable Map of Dynamics (MoD) framework that generalizes to unknown environments using only egocentric 3D LiDAR point clouds to overcome the long-standing limitation of traditional MoD methods. While MoDs are essential for encoding human motion characteristics to enable accurate pedestrian trajectory prediction or safe robot navigation, traditional approaches suffer from site-specificity, requiring exhaustive trajectory accumulation at every new location. Extending recent advances in neural implicit modeling, our framework trains a continuous, LiDAR-based MoD estimator across diverse environments. To mitigate the inherent sparsity and temporal bias of real-world trajectory data, we introduce a position-balanced sampling strategy and a multi-task learning architecture that jointly predicts motion distributions and a spatial frequency score map. The latter is further augmented by visibility-aware losses to compen
    
[^339]: 利用机器学习和天气预报预测风力涡轮机功率

    Predicting Wind Turbine Power Using Machine Learning and Weather Forecasts

    [https://arxiv.org/abs/2609.06194](https://arxiv.org/abs/2609.06194)

    本研究开发了基于线性回归和人工神经网络的风力涡轮机功率预测模型，仅利用天气条件即可预测功率输出以增强模型在不同涡轮机间的适用性，并系统研究了数据预处理技术和数据量对模型性能的影响，从而帮助识别适合安排维护工作的低功率时段。

    

    海上风力涡轮机被广泛用于生产可再生能源，但其维护工作可能因强制停机而导致效率下降。准确的风力涡轮机功率预测可以识别出低功率时段，这些时段是安排维护工作的理想时间。然而，数据量、特征选择和数据预处理对功率预测模型性能的影响尚未得到深入研究。此外，现有模型在不同风力涡轮机之间的可迁移性有限。因此，本研究开发了一个基线线性回归模型，并与更复杂的人工神经网络模型进行性能比较，仅使用天气条件来预测风力涡轮机的功率输出，以增强模型的适用性。研究考察了多种数据预处理技术，并分别使用一个月和一年的数据训练模型，以确定数据预处理和数据量对模型性能的影响。

    arXiv:2609.06194v1 Announce Type: new  Abstract: Offshore wind turbines are widely used to generate renewable energy, but their maintenance can result in decreased efficiency due to forced shutdowns. Accurate wind turbine power predictions can identify periods of low power that would be ideal for scheduling maintenance. However, the effects of data volume, feature selection, and data preprocessing on the performance of such power prediction models have not been thoroughly studied. Besides, current models have limited transferability between different wind turbines. Therefore, this study developed a baseline Linear Regression for performance comparison with a more complex Artificial Neural Network model to predict the power output of a wind turbine, using weather conditions only to enhance applicability. A range of data preprocessing techniques were studied, and models were trained on one month and one year of data to determine the effects of data preprocessing and volume on model perfo
    
[^340]: SolarBench：一个全球太阳能超短期预报基准

    SolarBench: A global solar energy nowcasting benchmark

    [https://arxiv.org/abs/2609.06187](https://arxiv.org/abs/2609.06187)

    SolarBench是一个开放的全球性太阳能超短期预报基准，整合了11个站点、跨越十年的超过六百万张天空与卫星图像及配套工具箱，为深度学习太阳能预报模型提供了统一、可复现的评估平台。

    

    随着太阳能发电占比的不断提升，对天气驱动的太阳能变率进行超短期预报，已成为保障能源系统可靠运行的关键。最先进的方法越来越多地将深度学习应用于天空相机和地球静止卫星观测数据，但数据集的碎片化和评估标准的不一致，使得人们难以判断已报告的性能提升能否在不同气候、云况和光伏（PV）系统之间推广。在此，我们提出SolarBench，一个开放的、基于图像的全球太阳能超短期预报基准。SolarBench整合了来自11个多样化站点、跨越十年的超过六百万张天空和卫星图像，并配有辐照度或光伏输出数据以及辅助大气数据。配套的工具箱支持可复现的数据访问、处理、模型开发和评估。利用SolarBench，我们对代表性模型进行了基准测试，揭示了平均预报精度与捕捉快速变化能力之间的差距

    arXiv:2609.06187v1 Announce Type: cross  Abstract: As the share of solar power grows, nowcasting weather-driven solar variability becomes critical for reliable energy system operation. State-of-the-art approaches increasingly apply deep learning to sky camera and geostationary satellite observations, but fragmented datasets and inconsistent evaluation make it difficult to determine whether reported improvements generalize across climates, cloud regimes, and photovoltaic (PV) systems. Here we introduce SolarBench, an open global benchmark for image-based solar nowcasting. SolarBench harmonizes more than six million sky and satellite images from 11 diverse sites spanning a decade, together with irradiance or PV output and auxiliary atmospheric data. An accompanying toolbox supports reproducible data access, processing, model development, and evaluation. Using SolarBench, we benchmark representative models and reveal a gap between average forecasting accuracy and the ability to capture ra
    
[^341]: 非平稳强化学习中的谱优先扫除

    Spectral Prioritized Sweeping in Nonstationary Reinforcement Learning

    [https://arxiv.org/abs/2609.06186](https://arxiv.org/abs/2609.06186)

    该论文提出谱优先扫除（GTA-PS/SPS），利用图的预解算子与扩散语义来增强优先级信号，解决了非平稳奖励环境下经典优先扫除因残差传播受限而导致的重新规划短视问题。

    

    优先扫除通过根据贝尔曼残差大小选择备份操作来加速基于模型的强化学习。然而，在非平稳奖励设置中，经典的优先级评分是短视的：在局部奖励发生偏移后，残差仅通过已执行的备份进行传播，因此在有限的重新规划预算下，处于瓶颈位置或拓扑距离较远的状态估计可能保持不变。我们引入了图拓扑增强框架，利用图的预解算子及其扩散语义来增强被查询的信号。我们的应用，用于优先扫除的图拓扑增强（GTA-PS），或其更为通用的别名谱优先扫除（SPS），为固定动态和变化状态奖励的设置提供了一种即插即用的排序评分。GTA-PS 使用平滑化的策略，诱导出一个转移链，并结合其入拉普拉斯算子和出拉普拉斯算子。标准的优先级键被增强……

    arXiv:2609.06186v1 Announce Type: new  Abstract: Prioritized Sweeping (PS) accelerates model-based reinforcement learning by selecting backups according to Bellman residual magnitude. In nonstationary reward settings, however, the canonical priority score is shortsighted: after a localized reward shift, residuals propagate only through realized backups, so bottlenecked or topologically distant state estimates may remain static under a limited replanning budget. We introduce the Graph Topology Augmentation framework, which employ the graph's resolvent and its diffusion semantic, to augment the inquired signal. Our application, Graph Topology Augmentation for Prioritized Sweeping (GTA-PS), or which the alias Spectral Prioritized Sweeping (SPS) might be more universal, provides a drop-in ordering score for the setting of fixed dynamics and changing state rewards. GTA-PS uses a smootherized policy, inducing a transition chain, with its in- and out-Laplacian. The standard priority key is au
    
[^342]: 从间接观测中恢复稀疏信号的线性映像

    Recovering linear images of sparse signals from indirect observations

    [https://arxiv.org/abs/2609.06182](https://arxiv.org/abs/2609.06182)

    本文提出了在不对感知矩阵做零空间或受限等距性质等强假设的前提下，基于 ℓ1 最小化和凸优化，从含噪间接观测中恢复稀疏信号线性映像的方法，其风险界可通过高效计算显式凸优化问题得到。

    

    在本文中，我们开发并分析了从含噪间接观测 $\omega=Ax+\xi$ 中恢复未知信号 $x$ 的线性映像 $Bx$ 的技术。已知先验信息为 $x\in \cX$（一个给定的凸紧集），且 $x$ 是 $s$-稀疏的——即至多含有 $s$ 个非零元素。所提出的估计属于基于 $\ell_1$ 最小化的一大类恢复方法。然而，与描述此类估计性能的经典结果不同，我们不对感知矩阵 $A$ 做任何特殊（且难以验证）的假设，例如零空间条件或受限等距性质（RIP）等。因此，估计的参数及其风险上界无法以封闭解析形式给出，而是通过高效计算作为显式凸优化问题的解来获得。

    arXiv:2609.06182v1 Announce Type: cross  Abstract: In this paper, we develop and analyze techniques for recovering a linear image $Bx$ of an unknown signal $x$ from indirect noisy observation $\omega=Ax+\xi$. It is {\em a priori} known that $x\in \cX$, a given convex compact set, and that $x$ is $s$-sparse---has at most $s$ nonvanishing entries. The proposed estimates belong to a large family of recovery routines by $\ell_1$-minimization. However, unlike the classical result describing performance of such estimates, we do not make any special (and hard to check) assumptions about the sensing matrix $A$ such as nullspace or Restricted Isometry condition and the like. As a consequence, parameters of the estimates and the upper bounds on their risks are not available in a closed analytic form, but are delivered instead by efficient computation as solutions to explicit convex optimization problems.
    
[^343]: FMMO：检测局部归因与全局漂移之间的偏差

    FMMO: Detecting the Divergence Between Local Attribution and Global Drift

    [https://arxiv.org/abs/2609.06173](https://arxiv.org/abs/2609.06173)

    本文提出模型监控与可观测性框架FMMO，揭示局部归因方法在模型漂移时可能呈现虚假稳定、掩盖受保护群体遭受差异性影响的公平性盲点，并通过整合全局代理模型与模型利用率测量加以弥补。

    

    部署后的模型漂移对算法问责构成了关键风险，尤其是当真实标签延迟到达、性能退化演变为“静默失败”时。尽管人们常依赖可解释人工智能（XAI）来审计这些变化，我们证明了流行的局部归因方法（如TreeSHAP）即使在模型可靠性崩溃时也可能表现出具有误导性的稳定性。在本文中，我们提出了一个模型监控与可观测性框架（FMMO），旨在揭示局部解释稳定性与全局分布漂移之间的偏差。通过使用基准数据集、合成数据集和真实世界数据集，我们展示了局部XAI方法未能标记漂移引起的差异性影响，具体表现为受保护群体的假阳性率飙升而特征归因却保持不变。通过将全局代理模型与模型利用率测量相结合，FMMO缓解了这一公平性盲点，确保……（原文在此处截断）

    arXiv:2609.06173v1 Announce Type: new  Abstract: Post-deployment drift poses a critical risk to algorithmic accountability, particularly when ground truth labels are delayed and performance degradation becomes a "silent failure". While Explainable AI (XAI) is often relied upon to audit these shifts, we demonstrate that popular local attribution methods (e.g., TreeSHAP) can exhibit misleading stability even as model reliability collapses. In this paper, we propose a Framework for Model Monitoring and Observability (FMMO) designed to expose the divergence between local explanation stability and global distribution shifts. Using benchmark, synthetic, and real-world datasets, we show that local XAI methods fail to flag drift-induced disparate impact, specifically where False Positive Rates spike for protected groups while feature attributions remain unchanged. By integrating global surrogate models with model utilization measurements, FMMO mitigates this fairness blind spot, ensuring that 
    
[^344]: 业务流程的决策感知后缀预测与推理

    Decision-Aware Suffix Prediction and Reasoning of Business Processes

    [https://arxiv.org/abs/2609.06169](https://arxiv.org/abs/2609.06169)

    该论文提出了一种决策感知的后缀预测框架，将决策挖掘与神经网络后缀预测相结合，通过神经符号方法利用挖掘出的决策规则对预测事件进行推理，从而改善短前缀和罕见流程变体场景下的预测效果。

    

    后缀预测旨在预测一个运行中案例直至完成所剩余的事件序列。大多数方法依赖于在事件日志上训练的神经网络，这些方法在平均意义上表现良好，但在处理短前缀或属于罕见流程变体的目标时表现不佳。在这些场景中，正确的路径可能跨越多个分支决策，而这些决策主要由案例级和事件级属性决定——基于神经网络的后缀预测模型往往低估这一信号，因为它们可能严重偏向（密集的）事件标签。决策挖掘可以从事件日志中提取此类决策的规则，但迄今为止仅被应用于事后分析和假设分析，而非后缀预测。因此，我们将决策挖掘与后缀预测相结合，提出了一种决策感知的后缀预测框架，这是一种神经符号方法，能够通过挖掘到的决策规则对预测事件进行推理。在四个事件日志中的三个上进行的实验……

    arXiv:2609.06169v1 Announce Type: cross  Abstract: Suffix prediction forecasts the remaining sequence of events of a running case until completion. Most approaches rely on neural networks trained on event logs, which, on average, perform well but struggle with short prefixes or targets belonging to a rare process variant. In such scenarios, the correct path may cross multiple branching decisions, determined primarily by case- and event-level attributes, a signal that NN-based suffix prediction models tend to underweight because they may heavily weight (dense) event labels. Decision mining extracts rules for such decisions from the event log, but has so far been applied only to post-hoc and what-if analysis, not suffix prediction. We therefore extend suffix prediction with decision mining, introducing a decision-aware suffix prediction framework, a neuro-symbolic approach that enables reasoning about predicted events via mined decision rules. Experiments on three of four event logs and 
    
[^345]: 一切为了1比特：面向大语言模型的真正1比特训练后量化

    All for 1-Bit: Towards Genuine 1-Bit Post-Training Quantization for LLMs

    [https://arxiv.org/abs/2609.06161](https://arxiv.org/abs/2609.06161)

    本文提出AF1框架，通过零空间感知二值分解（NABF）和分层Shapley分配（HiSA）两个互补组件，首次实现了大语言模型在严格1.0-BPW预算下消除隐藏开销的真正1比特训练后量化，同时保持模型精度。

    

    大语言模型（LLMs）已取得显著进展，但其庞大的存储和内存带宽需求仍阻碍了高效部署。权重二值化是一种有前景的解决方案，但现有的基于二值化的训练后量化（PTQ）方法通常由于隐藏开销而远远超出名义上的1比特存储目标。为解决这一差距，我们提出了All for 1-Bit（AF1），一个面向大语言模型的真正1比特训练后量化框架。AF1包含两个互补的组件：（1）零空间感知二值分解（NABF），通过Hessian感知代理重参数化、零空间感知二值分解和仅缩放的全局重构来改进二值重构；（2）分层Shapley分配（HiSA），利用分层Shapley敏感度来分配结构容量。两者相结合，在PTQ设置下严格1.0-BPW的预算内保持了模型精度。在LLaMA、Qwen和Gemma系列模型上的实验……

    arXiv:2609.06161v1 Announce Type: cross  Abstract: Large language models (LLMs) have achieved remarkable progress, yet their massive storage and memory-bandwidth demands still hinder efficient deployment. Weight binarization is a promising solution, but existing binarization-based post-training quantization (PTQ) methods usually far exceed the nominal 1-bit storage target due to hidden overhead. To address this gap, we propose All for 1-Bit (AF1), a genuine 1-bit PTQ framework for LLMs. AF1 comprises two complementary components: (1) Null-space-Aware Binary Factorization (NABF) for improving binary reconstruction through Hessian-aware surrogate reparameterization, null-space-aware binary factorization, and scale-only global reconstruction; and (2) Hierarchical Shapley Allocation (HiSA) for assigning structural capacity using hierarchical Shapley sensitivity. Together, they preserve model accuracy under a strict 1.0-BPW budget in the PTQ setting. Experiments on LLaMA, Qwen, and Gemma fa
    
[^346]: 重新思考单轮联邦图学习：免训练的统计估计方法

    Rethinking One-Shot Federated Graph Learning: Training-Free Statistical Estimation

    [https://arxiv.org/abs/2609.06154](https://arxiv.org/abs/2609.06154)

    该论文提出免训练框架SPEAR，将单轮联邦图学习重新表述为统计估计问题，通过计算拓扑平滑的类别原型并使用样本量自适应的收缩估计器进行聚合，有效解决了极端非独立同分布条件下本地GNN训练导致的跨客户端表示不对齐问题。

    

    单轮联邦图学习通常旨在通过单轮通信，在多个拥有互不相连子图的客户端上训练图神经网络（GNN）。现有方法主要基于“本地GNN训练不可或缺”这一前提来设计先进的优化策略。然而，实证观察表明，在极端非独立同分布条件下，本地GNN训练会产生严重的跨客户端表示不对齐问题，成为误差的主要来源而非补救手段。受此启发，我们将单轮联邦图学习重新表述为一个统计估计问题。我们提出了SPEAR（具有自适应可靠性的统计原型估计），这是一个完全免训练的框架，它直接在原始特征空间中从本地图计算拓扑平滑的类别原型。服务器随后使用样本量自适应的收缩估计器来聚合这些原型，对不可靠的本地估计进行降权处理。

    arXiv:2609.06154v1 Announce Type: new  Abstract: One-shot federated graph learning generally aims to train Graph Neural Networks (GNNs) across clients with disconnected subgraphs in a single communication round. Existing methods predominantly design advanced optimization strategies under the premise that local GNN training is indispensable. However, empirical observations reveal that under extreme non-IID conditions, local GNN training suffers from severe cross-client representation misalignment, becoming a major source of error rather than a remedy. Motivated by this, we reformulate one-shot FGL as a statistical estimation problem. We propose SPEAR (Statistical Prototype Estimation with Adaptive Reliability), a completely training-free framework that directly computes topology-smoothed class prototypes from local graphs in the original feature space. The server then aggregates these prototypes using a sample-size-adaptive shrinkage estimator that down-weights unreliable local estimate
    
[^347]: 窗口未含之物：在基于文档的答案不稳定性基准中审计证据溯源

    What the Window Does Not Contain: Auditing Provenance in a Document-Grounded Instability Benchmark

    [https://arxiv.org/abs/2609.06147](https://arxiv.org/abs/2609.06147)

    论文构建了基于真实风投融资文件的稳定性基准Probity，并通过自我审计揭示了摘录式基准中的证据溯源缺陷——证据缺失于模型文本窗口的条目答案不稳定性远高于正常条目（0.255对0.087），且会虚增跨模型一致性约五分之一。

    

    对同一份文档，向语言模型询问同一个问题二十次，它有时会返回两个不同的答案。我们构建了Probity——一个包含来自真实风险融资文件、共60个任务和470个条目的基准——来衡量这种情况发生的频率。随后我们对自己的语料库进行了审计，发现了一种任何由摘录构建的基准都可能存在的缺陷：某些条目的证据缺失于展示给模型的文本窗口之中。该审计标记了36个条目，并将单一标记容易混淆的两种失败情形区分开来：证据真正缺失于窗口之外，以及必须从窗口所提供的数字中计算得出的答案。被标记条目改变答案的频率要高得多——其不稳定率达0.255，而427个干净条目仅为0.087；排除这些条目会使表面上的跨模型一致性下降约五分之一。在检验缺失证据是否能够解释这种不稳定性之前，我们预先注册了一项预测：重新裁剪每个窗口使其包含证据，不稳定……

    arXiv:2609.06147v1 Announce Type: cross  Abstract: Ask a language model the same question about the same document twenty times, and it sometimes returns two different answers. We built Probity, a benchmark of 60 tasks and 470 items from real venture-financing filings, to measure how often this happens. Then we audited our own corpus and found a defect any excerpt-built benchmark can carry: items whose evidence is missing from the window of text the model is shown. The audit flags 36 items and separates two failures a single flag would conflate: evidence genuinely absent from the window and answers that must be computed from numbers the window does supply. Flagged items change their answers far more often, wobbling at 0.255 against 0.087 on the 427 clean items, and excluding them cuts apparent cross-model agreement by about a fifth. Before testing whether the missing evidence explains the instability, we registered a prediction: re-cut each window to hold its evidence, and instability s
    
[^348]: 反蜂群学说：遏制协同智能体入侵

    Counter-Swarm Doctrine: Containing Coordinated Agent Intrusions

    [https://arxiv.org/abs/2609.06140](https://arxiv.org/abs/2609.06140)

    提出以可修订的“协调事件”作为防御运作单元，通过前瞻性事件发现，在评估者标注之前识别并遏制智能体之间未经许可的协同入侵。

    

    智能体可以将共享基础设施转变为协同入侵的通道。Hugging Face 事件以及另一项公开维基调查表明，一次安全评估可能需要来自多次执行及其所留痕迹的证据。我们主张，防御的运作单元应当是一个可修订的“协调事件”，它将观测到的传输、任务权限和响应历史关联起来。其核心研究问题是前瞻性事件发现：在评估者给出成员归属之前，确定哪些行动属于同一事件。我们相对于协作与委托授权策略定义了“未经许可的协调”，将经由存储介导的协调与共识主动性联系起来，并明确了区分“影响”与“共同原因”所需的证据。首次接触信号是事件发现的一种可能输入；该设计还追踪继承状态和后续使用。所提议的评估将孤立行动、滚动……

    arXiv:2609.06140v1 Announce Type: cross  Abstract: Agents can turn shared infrastructure into a channel for coordinated intrusion. The Hugging Face incident and a separate public-wiki investigation show why a security assessment may need evidence from several executions and the artifacts they leave behind. We argue that the operational unit of defence should be a revisable coordination episode linking observed transfers, task authority, and response history. The central research problem is prospective episode discovery: finding which actions belong together before an evaluator supplies their membership. We define unsanctioned coordination relative to collaboration and delegated-authority policy, connect storage-mediated coordination to stigmergy, and specify the evidence needed to distinguish influence from common causes. First-contact signals are one possible input to discovery; the design also follows inherited state and later use. A proposed evaluation compares isolated actions, rol
    
[^349]: Tsetlin机中的压缩循环反馈：一项可复现的布尔有限状态机研究

    Compressed Recurrent Feedback in Tsetlin Machines: A Reproducible Boolean-FSM Study

    [https://arxiv.org/abs/2609.06133](https://arxiv.org/abs/2609.06133)

    本文提出一种固定宽度的循环反馈压缩方法，通过XOR折叠与双时间尺度阈值化将循环Tsetlin机的480个子句激活压缩为96个比特，并在可复现的布尔有限状态机基准上验证了其准确率与原始反馈相当。

    

    小型设备上的序列推理要求模型能够保留有用的历史信息，而无需反复处理冗长的输入记录。循环Tsetlin机（RTM）通过将一个时间步的布尔子句输出作为下一个时间步的输入来提供这种记忆能力。然而，直接反馈会随着子句库的规模而增长，可能使循环输入变得不必要地宽。本文研究了一种固定宽度的替代方案。我们通过异或（XOR）折叠来组合子句激活，在两个时间尺度上保留折叠后的比特，并将其阈值化回二进制状态。所得设计将480个子句激活压缩为96个循环比特。我们在一个可复现的布尔有限状态机基准上评估了该方法，该基准具有明确的转换规则、数据划分方式和随机种子。在144次运行中，压缩模型在两个任务族上分别取得了61.47±6.74%和62.94±9.92%的准确率。原始子句反馈使这些均值产生了……（摘要此处截断）

    arXiv:2609.06133v1 Announce Type: new  Abstract: Sequential inference on small devices requires a model to retain useful history without repeatedly processing a long input record. A Recurrent Tsetlin Machine (RTM) provides this memory by returning Boolean clause outputs from one time step as inputs to the next. Direct feedback, however, grows with the clause bank and can make the recurrent input unnecessarily wide. This paper investigates a fixed-width alternative. We combine clause activations by exclusive-OR (XOR) folding, retain the folded bits at two time scales, and threshold them back to a binary state. The resulting design reduces 480 clause activations to 96 recurrent bits. We evaluate the method on a reproducible Boolean finite-state-machine benchmark with explicit transition rules, data splits, and random seeds. Across 144 runs, the compressed model obtains $61.47 \pm 6.74\%$ and $62.94 \pm 9.92\%$ accuracy on the two task families. Raw clause feedback changes these means by 
    
[^350]: IIns-VAE+：一种用于无线感知中环境识别的鲁棒迁移学习框架

    IIns-VAE+: A Robust Transfer Learning Framework for Environmental Identification in Wireless Sensing

    [https://arxiv.org/abs/2609.06131](https://arxiv.org/abs/2609.06131)

    本文提出IIns-VAE+混合模型，通过将实例间变分自编码器与极小化极大风险分类器相结合，显著提升了无线感知环境识别在多种跨环境迁移学习场景下的鲁棒性和泛化能力。

    

    无线感知中的环境识别对于6G通信感知一体化（ISAC）系统实现可靠的态势感知至关重要。然而，用于该任务的深度学习（DL）模型在跨不同环境的域偏移下往往难以泛化。虽然实例间变分自编码器能够学习到表示丰富的特征，但其神经分类器仍然容易受到这些分布变化的影响。在本文中，我们提出了IIns-VAE+，这是一种将IIns-VAE框架与极小化极大风险分类器相结合的混合模型，旨在提高迁移学习场景中的适应性。我们使用真实世界的数据集在三种迁移学习场景下评估了我们的框架，包括从通用到特定房间环境、从高到低标签分辨率、以及从混合到特定环境。实验结果表明，IIns-VAE+显著优于基线方法，展示了……

    arXiv:2609.06131v1 Announce Type: new  Abstract: Environmental identification in wireless sensing is essential for 6G integrated sensing and communication (ISAC) systems to achieve reliable situational awareness. However, deep learning (DL) models for this task often fail to generalize under domain shift across diverse environments. While the Inter-Instance Variational Auto-encoder (IIns-VAE) learns features of rich representation, its neural classifier remains vulnerable to these distribution changes. In this paper, we propose IIns-VAE+, a hybrid model that combines the IIns-VAE framework with Minimax Risk Classifiers (MRC) to improve adaptability in transfer learning scenarios. We use real-world datasets to evaluate our framework across three transfer learning scenarios, including general to specific room environments, high to low label resolutions, and mixed to specific environments. The experimental results indicate that IIns-VAE+ significantly outperforms baselines, demonstrating 
    
[^351]: 面向数字孪生分析的12小时港口洪水预警的稀疏事件聚类学习

    Sparse Incident-Cluster Learning for 12-hour Port Flood Pre-warning in Digital-Twin Analytics

    [https://arxiv.org/abs/2609.06109](https://arxiv.org/abs/2609.06109)

    该论文将12小时港口洪水预警建模为事件聚类学习问题，通过结合稀疏特征选择、预警聚类分组及多重控制手段避免因时间数据泄漏导致的高估性能，并以利物浦为案例进行了数字孪生分析验证。

    

    港口洪水数字孪生需要能够在中断发生前向运营者发出预警的分析方法，但官方预警事件通常稀少，且相邻观测在时间上相互依赖。因此，行级分类方法可能因将同一事件的窗口同时置于模型开发数据和评估数据中而高估性能。我们将12小时港口洪水预警表述为一个事件聚类学习问题，并使用八点水位历史、预测时刻上下文协变量以及可解释的短窗口动态来评估数字孪生分析模块。该评估协议结合了特定折的稀疏特征选择、预警聚类分组、负标签控制、100次重复的随机top-k控制以及警报事件评估。利物浦是主要的四聚类案例研究，同时使用统一的Humber/Hull代理数据和Wessex South数据进行协议迁移性检验。在利物浦各折中，top-10 ElasticNet模型……

    arXiv:2609.06109v1 Announce Type: new  Abstract: Port flood digital twins require analytics that warn operators before disruption, but official warning incidents are often few and adjacent observations are temporally dependent. Row-level classification can therefore overstate performance by placing windows from the same event in both model-development and evaluation data. We formulate 12-hour port flood pre-warning as an incident-cluster learning problem and evaluate a digital-twin analytics module using eight-point water-level histories, prediction-time contextual covariates, and interpretable short-window dynamics. The protocol combines fold-specific sparse feature selection, warning-cluster grouping, negative-label controls, 100-repeat random top-k controls, and alert-episode evaluation. Liverpool is the primary four-cluster case study, with harmonised Humber/Hull-proxy and Wessex South data used for protocol-transfer checks. Across the Liverpool folds, the top-10 ElasticNet model a
    
[^352]: DataFlex-RL：RLVR数据策略评估平台

    DataFlex-RL: An Evaluation Platform for RLVR Data Policies

    [https://arxiv.org/abs/2609.06107](https://arxiv.org/abs/2609.06107)

    该论文提出了DataFlex-RL评估平台，在统一GRPO设置下系统比较13种RLVR数据策略，发现没有任何一种rollout选择、重新加权或自适应混合方法能显著优于简单的均匀采样。

    

    可验证奖励强化学习（RLVR）的数据策略决定了使用哪些rollout、它们的加权强度，以及哪些领域为后续训练批次做出贡献。我们提出了DataFlex-RL，这是一个在统一GRPO配方下比较这些选择的评估平台。我们的主要实验使用Qwen2.5-7B-Base模型和12个数学、逻辑和科学基准测试，在12个匹配的随机种子上评估了13种配置。均匀GRPO相比未训练的检查点将领域平衡平均准确率提高了7.76个百分点。八种rollout选择或重新加权方法中，没有一种相对于均匀采样达到排除零的配对95%置信区间；三种自适应混合方法中，也没有一种在相同精度水平下优于固定的等比例混合。在Llama-3.1-8B-Base上进行的修正后12种子扩展实验，将其他方法置于与原始对比相同的分数尺度上。

    arXiv:2609.06107v1 Announce Type: new  Abstract: Data policies for reinforcement learning with verifiable rewards (RLVR) determine which rollouts are used, how strongly they are weighted, and which domains contribute to subsequent training batches. We introduce DataFlex-RL, an evaluation platform for comparing these choices under a common GRPO recipe. Our primary experiment evaluates 13 configurations across 12 matched seeds using Qwen2.5-7B-Base and 12 mathematics, logic, and science benchmarks. Uniform GRPO improves the domain-balanced average accuracy by 7.76 percentage points over the untrained checkpoint. None of the eight rollout-selection or reweighting methods achieves a paired 95% confidence interval that excludes zero relative to uniform sampling, and none of the three adaptive mixtures outperforms a fixed equal mixture at the same level of precision. A corrected 12-seed extension on Llama-3.1-8B-Base places the additional methods on the same score scale as the original contr
    
[^353]: FANS：面向异构设备的联邦自适应网络搜索学习

    FANS: Federated Adaptive Network Search Learning for Heterogeneous Devices

    [https://arxiv.org/abs/2609.06106](https://arxiv.org/abs/2609.06106)

    FANS提出了一种基于超网络的联邦自适应网络搜索框架，通过联邦并行缩放算法并行训练多个采样子网络并结合自蒸馏，将异构联邦学习中可行的子网络池扩大了几个数量级。

    

    异构联邦学习（HFL）旨在跨具有不同资源预算的设备训练模型，同时保护数据隐私。现有的异构联邦学习方法通常将训练限制在一个较小的预定义模型配置集合上，这限制了架构的覆盖范围。为了解决这一瓶颈，我们提出了联邦自适应网络搜索，这是一个基于超网络的框架，它学习的是一个共享的架构空间，而不是固定的客户端模型集合。为了高效地优化这个共享空间，我们提出了联邦并行缩放算法，该算法并行地联合训练多个采样的子网络，并结合自蒸馏技术，使得较大的采样子网络可以在本地更新过程中监督较小的子网络。我们分别在CIFAR-10、CIFAR-100和MNLI数据集上使用ResNet-18、DenseNet-121和BERT-base对FANS进行了评估。在所有基准测试中，FANS将可行的子网络池扩大了几个数量级（例如，4,680个可以……

    arXiv:2609.06106v1 Announce Type: new  Abstract: Heterogeneous Federated Learning (HFL) aims to train models across devices with diverse resource budgets while preserving data privacy. Existing HFL methods typically bind training to a small predefined menu of model configurations, which limits architectural coverage. To address this bottleneck, we introduce Federated Adaptive Network Search (FANS), a hypernetwork-based framework that learns a shared architecture space rather than a fixed set of client models. To optimize this shared space efficiently, we propose the Federated Parallel Scaling (FPS) algorithm, which jointly trains multiple sampled subnetworks in parallel with self-distillation so that larger sampled subnetworks can supervise smaller ones during local updates. We evaluate FANS on CIFAR-10, CIFAR-100, and MNLI using ResNet-18, DenseNet-121, and BERT-base, respectively. Across all benchmarks, FANS expands the feasible subnetwork pool by orders of magnitude (e.g., 4,680 can
    
[^354]: 可编程元胞自动机

    Programmable Cellular Automata

    [https://arxiv.org/abs/2609.06102](https://arxiv.org/abs/2609.06102)

    提出了可编程元胞自动机的概念，将元胞自动机系统表示为Python代码，并将其模块化为局部函数和决策函数，解决了创建有效局部规则困难且难以解释的问题。

    

    元胞自动机是一种局部计算范式，其中复杂行为可以从简单函数之间的局部交互中产生。这一范式已被用于解释许多系统，如生物过程、交通模拟、计算机网络等。在游戏中，元胞自动机已被用于《模拟城市》等游戏中，以及用于生成洞穴或地牢等空间内容。然而，创建有效的局部规则既困难又不直观。元胞自动机可以有效地进行演化，但结果仍然可能难以解释。在这项工作中，我们引入了可编程元胞自动机的概念，即将系统表示为Python代码。我们还将元胞自动机模块化为局部函数和决策函数。局部函数接受一个局部邻域并返回一个值，而决策函数则接受局部函数的输出并决定下一个状态的值。将元胞自动机分离

    arXiv:2609.06102v2 Announce Type: cross  Abstract: Cellular automata is a local computation paradigm where complex behavior can arise from local interactions between simple functions. This paradigm has been used to explain many systems such as biological processes, traffic simulation, computer networks, etc. In games, cellular automata have been used in games such as SimCity and for the generation of spatial content such as caves or dungeons. However, creating effective local rules is hard and unintuitive. Cellular automata can be effectively evolved, but may still be hard to interpret. In this work, we introduce the concept of programmable cellular automata, where we represent the system as Python code. We also modularize the cellular automata into local functions and a decision function. Local functions take a local neighborhood and return a value, while the decision function takes the output of the local functions and decides the value of the next state. Separating the cellular auto
    
[^355]: VERPO：验证证据正则化策略优化

    VERPO: Verified Evidence Regularized Policy Optimization

    [https://arxiv.org/abs/2609.06100](https://arxiv.org/abs/2609.06100)

    VERPO提出了一种验证证据正则化策略优化框架，通过Fisher证据对比和带停止机制的逐token ZPD控制器有选择性地应用基于证据的修正，在保留可验证结果目标的同时避免盲目模仿导致的负面迁移。

    

    可验证的结果奖励可以指导语言模型的后训练，但序列级别的优势无法识别哪些token级别的决策应当被保留或修改。证据条件教师通过以特权反馈重放采样轨迹来提供更密集的监督。然而，不加区分的模仿可能会迁移那些不支持任务成功的格式或推理风格偏移。我们提出了VERPO，一个验证证据正则化策略优化框架，它将证据视为策略修正的提议，同时保留结果目标。该框架将无证据的参考恢复与带符号的token级证据修正分离开来。Fisher证据对比沿着估计的证据存在方向对修正进行衰减。一个带停止机制的逐token ZPD控制器根据局部奖励对齐程度和Fisher移动成本来调节修正的接受度，而参考通道保持独立于接受决策。

    arXiv:2609.06100v1 Announce Type: cross  Abstract: Verifiable outcome rewards guide language-model post-training, but sequence-level advantages do not identify which token-level decisions should be preserved or revised. Evidence-conditioned Teachers provide denser supervision by replaying sampled trajectories with privileged feedback. Yet indiscriminate imitation risks transferring formatting or reasoning-style shifts that do not support task success. We introduce VERPO, a Verified Evidence Regularized Policy Optimization framework that treats evidence as a proposal for policy correction while retaining the outcome objective. It separates evidence-free reference restoration from signed token-level evidence corrections. Fisher Evidence Contrast attenuates corrections along an estimated evidence-presence direction. A stopped token-wise ZPD controller scales acceptance according to local reward alignment and Fisher movement cost, while the reference channel remains independent of acceptan
    
[^356]: 基于泊松稀释结构方程模型的计数数据因果DAG识别

    Causal DAG Identification for Count Data via Poisson Thinning Structural Equation Models

    [https://arxiv.org/abs/2609.06098](https://arxiv.org/abs/2609.06098)

    提出泊松稀释结构方程模型（PT-SEM），通过泊松稀释机制和灵活的节点级外生分布设定，首次实现了从观测数据中对计数数据因果DAG、稀释系数及外生分布的完全识别。

    

    计数值变量出现在许多科学和应用场景中，然而能够从观测数据中完全识别因果DAG的显式结构模型仍然有限。泊松分支结构因果模型（PB-SCM）通过二项稀释和独立的泊松外生变量，为线性结构方程模型提供了计数值的类似物，但其因果DAG通常只能被部分识别。在此框架基础上，我们提出了泊松稀释结构方程模型（PT-SEM），该模型用泊松稀释替代PB-SCM中的二项稀释，并允许节点级外生分布来自多样化的计数分布族。在节点级正则性条件下，我们建立了因果DAG、稀释系数以及节点级外生分布的可识别性。相同的识别分析可以扩展到二项稀释情形，从而在某些条件下获得完全可识别性。

    arXiv:2609.06098v1 Announce Type: cross  Abstract: Count-valued variables arise in many scientific and applied settings, yet explicit structural models that allow full identification of causal DAGs from observational data remain limited. The Poisson branching structural causal model (PB-SCM) provides a count-valued analogue of linear structural equation models using binomial thinning and independent Poisson exogenous variables, but its causal DAG is generally only partially identifiable.   Building on this framework, we propose the Poisson thinning structural equation model (PT-SEM), which replaces binomial thinning in PB-SCM with Poisson thinning and allows node-wise exogenous distributions from diverse count-distribution families. Under node-wise regularity conditions, we establish identifiability of the causal DAG, the thinning coefficients, and the node-wise exogenous distributions. The same identification analysis extends to binomial thinning, yielding full identifiability wheneve
    
[^357]: 从连接组到功能：面向储备池计算的条件生成式潜在表示

    Connectome-to-Function: Conditional Generative Latent Representations for Reservoir Computing

    [https://arxiv.org/abs/2609.06093](https://arxiv.org/abs/2609.06093)

    提出了一种条件生成式潜在表示框架，将连接组图编码到紧凑结构空间中，既能以高达0.910的AUC高精度重建观测连接，又能生成新的候选连接组，为连接组结构到功能的映射提供了统一的生成式分析途径。

    

    连接组是神经元及其突触连接的图级映射，为理解大脑回路如何支持功能与计算提供了结构基础。然而，将连接组结构映射到计算仍然十分困难，因为这些图具有高维、稀疏的特性，并且对局部结构变化敏感。现有方法通常依赖于手工设计的结构描述符或特定任务的预测器，这限制了它们以兼具生成性和功能意义的形式来表示连接组的能力。我们提出了一个条件生成式潜在框架，将连接组图编码到紧凑的结构空间中，同时利用可用的节点级条件来指导重建与生成。基于该空间，模型能够以最高达0.910的平均边重建AUC重建观测到的连接，并生成新的候选连接组，从而实现了对图结构与功能的统一分析。

    arXiv:2609.06093v1 Announce Type: new  Abstract: Connectomes, graph-level maps of neurons and their synaptic connections, provide a structural basis for understanding how brain circuits support function and computation. However, mapping connectome structure to computation remains difficult because these graphs are high-dimensional, sparse, and sensitive to local structural variation. Existing approaches often depend on hand-crafted structural descriptors or task-specific predictors, which limits their ability to represent connectomes in a form that is both generative and functionally meaningful. We propose a conditional generative latent framework that encodes connectome graphs into a compact structural space while using available node-level conditions to guide reconstruction and generation. From this space, the model can reconstruct observed connectivity with a mean edge-reconstruction AUC up to 0.910 and generate new candidate connectomes, enabling a unified analysis of graph structu
    
[^358]: 情境化与删失需求下的定价与库存联合学习

    Learning to Price and Stock Under Contextual and Censored Demand

    [https://arxiv.org/abs/2609.06083](https://arxiv.org/abs/2609.06083)

    本文提出了一个同时应对情境化需求与删失需求观测的联合定价与库存控制学习框架，通过将需求建模为基函数的线性组合并设计高效算法，实现了与理论下界匹配的最优遗憾界。

    

    对于现代零售商而言，做出最优的联合定价与库存控制决策是一项关键挑战。在实践中，零售商面临不断变化的市场环境，需求受到各种情境因素的影响，同时还要应对销售损失（lost sales）掩盖真实需求信息的困难。然而，现有方法往往无法同时考虑情境信息和删失需求观测。我们通过提出一个框架来弥补这一空白：在该框架中，我们将需求建模为具有未知系数的基函数的线性组合，从而实现能够响应环境变化的自适应定价与库存决策。我们提出了一种高效算法，在凹收益条件下实现了 O(K√T log T) 的遗憾界，在一般情形下实现了 O(K^(2/3)T^(2/3)(log T)^(1/2)) 的遗憾界，并且匹配的下界证实了算法的最优性。跨多个场景的大量数值实验验证了方法的有效性。

    arXiv:2609.06083v1 Announce Type: new  Abstract: To make optimal joint pricing and inventory control decisions is a critical challenge for modern retailers. In practice, retailers face changing market conditions where demands are influenced by various contextual factors, while simultaneously dealing with the difficulty of lost sales that obscure true demand information. However, existing approaches often fail to account for both contextual information and censored demand observations. We address this gap by presenting a framework where we model demand as a linear combination of basis functions with unknown coefficients, allowing for adaptive pricing and inventory decisions that respond to changing contexts. We propose an efficient algorithm to achieve regret bound $\mathcal{O}(K\sqrt{T}\log T)$ under concave revenue conditions and $\mathcal{O}(K^{2/3}T^{2/3}(\log T)^{1/2})$ for the general case, with matching lower bounds confirming optimality. Extensive numerical experiments across di
    
[^359]: PhenoBench：深度表型人类队列能告诉我们什么

    PhenoBench: Mapping What a Deeply Phenotyped Human Cohort Can Tell Us

    [https://arxiv.org/abs/2609.06080](https://arxiv.org/abs/2609.06080)

    该论文提出了PhenoBench——一个基于人类表型项目（超13,000名参与者）构建的可执行评估基准，通过定义15个领域、26种输入模态下的90项临床任务，系统性地量化了哪些测量数据对哪些健康问题具有预测价值。

    

    深度表型队列结合了从秒级到年际跨时间尺度的临床、影像、分子和可穿戴设备观测数据。这种广度可以揭示哪些测量数据能为哪些健康相关问题提供信息，但异构的分析结果无法直接比较。我们提出了PhenoBench，一个围绕人类表型项目构建的可执行基准，该项目已有超过13,000名参与者完成了首次访问。每个问题都固定了目标、合格人群、时间点和允许使用的信息；其评估契约规定了数据划分、评估指标、基线和结论边界。该基准定义了90项基于临床的任务，涵盖15个领域和26种输入模态。测量结果显示出依赖于具体问题和数据表示的预测价值，包括相对于匹配基线在留出集性能上的正向、接近零以及负向的变化。我们使用PhenoBench评估了新兴的表格基础模型……

    arXiv:2609.06080v1 Announce Type: cross  Abstract: Deeply phenotyped cohorts combine clinical, imaging, molecular, and wearable observations across timescales from seconds to years. This breadth can reveal which measurements inform which health-related questions, but heterogeneous analyses are not directly comparable. We present PhenoBench, an executable benchmark built around the Human Phenotype Project, in which more than 13,000 participants have completed the initial visit. Each question fixes the target, eligible population, timing, and allowed information; its evaluation contract specifies the split, metric, baseline, and claim boundary. The benchmark defines 90 clinically grounded tasks across 15 domains and 26 input modalities. Measurements showed question- and representation-dependent predictive value, including positive, near-zero, and negative changes in held-out performance relative to matched baselines. We used PhenoBench to evaluate emerging tabular foundation models acros
    
[^360]: LayerRoute：面向视觉-语言-动作策略的动作条件混合层路由

    LayerRoute: Action-Conditioned Mixture-of-Layers Routing for Vision-Language-Action Policies

    [https://arxiv.org/abs/2609.06079](https://arxiv.org/abs/2609.06079)

    提出LayerRoute，一个动作条件的混合层路由接口，使VLA策略能够根据具体动作需求自适应地访问和混合VLM不同层次的表示，突破了传统固定层分配的限制。

    

    arXiv:2609.06079v1 公告类型：新论文 摘要：视觉-语言-动作（VLA）策略利用预训练的视觉-语言模型（VLM）来指导机器人控制的动作生成。VLM提供跨层演化的分层视觉-语义表示，从局部视觉几何到抽象的、与语言对齐的语义；因此，不同的操作任务可能需要不同的层表示混合。同时，动作模块在动作计算过程中维护不断演化的中间表示，这些表示可能为后续决策提供有用信息。然而，现有的VLA接口在表示访问方面的灵活性有限：VLM信息通过为每个动作层固定的层分配来暴露，而中间动作状态仅通过残差流隐式传播，缺乏显式复用。我们提出了LayerRoute，一个动作条件的表示路由接口，使VLA策略能够自适应地访问VLM的各层表示。

    arXiv:2609.06079v1 Announce Type: new  Abstract: Vision-Language-Action (VLA) policies leverage pretrained vision-language models (VLMs) to guide action generation for robot control. VLMs provide hierarchical visual-semantic representations that evolve across layers, from local visual geometry to abstract, language-aligned semantics; different manipulation tasks may therefore require different mixtures of layer representations. Meanwhile, the action module maintains intermediate representations that evolve throughout action computation and may provide useful information for subsequent decisions. However, existing VLA interfaces offer limited flexibility in representation access: VLM information is exposed through fixed layer assignments for each action layer, while intermediate action states are only propagated implicitly through residual streams without explicit reuse. We introduce LayerRoute, an action-conditioned representation routing interface that enables adaptive access to VLM l
    
[^361]: 超越免再训练MoE压缩：后压缩调整的成本归一化研究

    Beyond Retraining-Free MoE Compression: A Cost-Normalized Study of Post-Compression Adjustment

    [https://arxiv.org/abs/2609.06076](https://arxiv.org/abs/2609.06076)

    压缩后的MoE模型应被视为“压缩初始化”而非最终产物：仅需3,000条样本、单个epoch的低成本后压缩调整（尤其是全参数微调），即可平均恢复压缩造成的37.3%的性能损失，且比token级知识蒸馏更具成本效益。

    

    免再训练的MoE（混合专家）压缩通过剪枝或合并专家来减少部署内存，但通常将压缩后的检查点视为最终产物。我们认为这种观点是不完整的：压缩后的MoE检查点更适合被理解为压缩后的初始化状态，它们可以从一个微小的压缩后调整阶段中受益。在两个MoE大语言模型骨干网络、四种剪枝/合并方法、三种专家保留比例以及28个基准测试上，我们在匹配的小数据预算和实测GPU成本下，比较了语言模型微调与基于教师的知识蒸馏。仅使用3,000条C4样本和单个epoch的调整，全量微调平均可以恢复原始模型与压缩模型之间性能差距的37.3%。此外，语言模型微调比标准的token级知识蒸馏更具成本效益，并且在测试的调整范围中，全参数调整展现出最强的成本-性能恢复权衡。这些结果表明，免再训练压缩应该与小的（压缩后调整）相结合。

    arXiv:2609.06076v1 Announce Type: new  Abstract: Retraining-free MoE compression reduces deployment memory by pruning or merging experts, but often treats the compressed checkpoint as the final artifact. We argue that this view is incomplete: compressed MoE checkpoints are better understood as compressed initializations that benefit from a tiny post-compression adjustment stage. Across two MoE LLM backbones, four pruning/merging methods, three expert-retention ratios, and 28 benchmarks, we compare LM fine-tuning and teacher-based KD under matched small-data budgets and measured GPU costs. Using only 3,000 C4 examples and a single epoch of adjustment, Full FT recovers 37.3% of the original-to-compressed performance gap on average. Moreover, LM fine-tuning is more cost-effective than standard token-level KD, and full-parameter adjustment gives the strongest cost--recovery trade-off among the tested scopes. These results suggest that retraining-free compression should be paired with small
    
[^362]: FedSubMuon：基于结构化子空间Muon的通信高效联邦大语言模型微调

    FedSubMuon: Communication-Efficient Federated LLM Fine-Tuning via Structured Subspace Muon

    [https://arxiv.org/abs/2609.06073](https://arxiv.org/abs/2609.06073)

    FedSubMuon通过在共享结构化子空间中优化紧凑系数矩阵，在保持Muon矩阵感知优化特性的同时，将客户端上传量从完整层大小的更新压缩为紧凑系数矩阵，从而大幅降低联邦LLM微调的通信成本。

    

    联邦微调使大语言模型（LLM）能够适应分散的客户端数据，但其在跨设备训练中的可扩展性往往受到高昂通信成本的限制。Muon是一种通过对矩阵值参数的动量进行正交化来提升优化性能的优化器。现有的联邦Muon方法证明了矩阵感知优化在联邦学习中的优势，但仍需传输完整层大小的更新和优化器状态。减少通信的一种自然方式是直接将Muon应用于LoRA因子，但这会改变被优化的对象并削弱Muon的矩阵感知更新几何特性。我们提出FedSubMuon，一种通信高效的联邦Muon微调方法，它在共享的结构化子空间内优化紧凑的系数矩阵。这一设计使Muon保持在单一的矩阵值可训练对象上，同时将客户端上传量减少为紧凑的系数矩阵。

    arXiv:2609.06073v1 Announce Type: cross  Abstract: Federated fine-tuning adapts large language models (LLMs) to decentralized client data, but its scalability in cross-device training is often limited by the high communication cost. Muon is an optimizer that improves optimization performance by orthogonalizing momentum for matrix-valued parameters. Existing federated Muon methods demonstrate the benefit of matrix-aware optimization in federated learning, but still require transmitting full layer-size updates and optimizer state. A natural way to reduce communication is to directly apply Muon to LoRA factors, but this changes the optimized object and weakens Muon's matrix-aware update geometry. We propose FedSubMuon, a communication-efficient federated Muon fine-tuning method that optimizes compact coefficient matrices within shared structured subspaces. This design keeps Muon on a single matrix-valued trainable object, while reducing the client upload to compact coefficient matrices. W
    
[^363]: ACE：面向MoE大语言模型参数高效微调的跨专家适配器合并

    ACE: Adapter Consolidation across Experts for Parameter-Efficient Fine-Tuning of MoE LLMs

    [https://arxiv.org/abs/2609.06072](https://arxiv.org/abs/2609.06072)

    提出ACE方法，通过将冗余专家的独立LoRA适配器合并为组共享的高秩模块并统一执行，在相同参数预算下同时提升MoE大语言模型参数高效微调的效果与训练效率。

    

    混合专家模型的参数高效微调（PEFT）通常为每个专家附加一个独立的低秩适配器。这种逐专家设计从三个方面使适配过程碎片化：模型容量被分割到多个狭窄的低秩更新中，在稀疏路由下梯度监督变得稀疏且不均衡，执行过程被分解为许多小的GEMM（通用矩阵乘法）操作。我们发现这种逐专家的分离往往是不必要的，因为LoRA适配器的子集在微调过程中会变得功能相似，这揭示了专家特定适配器之间的冗余性。基于这种冗余性，我们提出了ACE（跨专家适配器合并），它在相同的PEFT预算下对冗余专家进行分组，并用组共享的更高秩LoRA模块替换其专家特定的适配器。ACE进一步引入了分组适配器执行机制，将碎片化的逐专家适配器计算合并为更少、更大的组级GEMM。在各种……

    arXiv:2609.06072v1 Announce Type: cross  Abstract: Parameter-efficient fine-tuning (PEFT) of mixture-of-experts (MoE) models commonly attaches a separate low-rank adapter to each expert. This expert-wise design fragments adaptation in three ways: capacity is split across narrow low-rank updates, gradient supervision becomes sparse and imbalanced under sparse routing, and execution is decomposed into many small GEMMs. We find that such expert-wise separation is often unnecessary, as subsets of LoRA adapters become functionally similar during fine-tuning, revealing redundancy among expert-specific adapters. Based on this redundancy, we propose ACE (Adapter Consolidation across Experts), which groups redundant experts and replaces their expert-specific adapters with group-shared higher-rank LoRA modules under the same PEFT budget. ACE further introduces grouped adapter execution, which consolidates fragmented expert-wise adapter computations into fewer, larger group-level GEMMs. Across ev
    
[^364]: Calendar-SPCA：面向多周期用电曲线的可解释表示学习

    Calendar-SPCA: Interpretable Representation Learning for Multi-Periodic Electricity Consumption Profiles

    [https://arxiv.org/abs/2609.06060](https://arxiv.org/abs/2609.06060)

    提出 Calendar-SPCA 日历结构稀疏主成分方法，将日、周、年等多周期日历几何直接嵌入用电数据的低维表示学习中，生成稀疏且局部连贯、可直接解读的载荷模式，并在两个独立智能电表数据集上验证了其有效性。

    

    长期用电曲线呈现出多种同时存在的周期结构，包括日周期、周周期和年周期。本工作提出了 Calendar-SPCA，这是一种日历结构的稀疏主成分方法，将这种已知的多周期几何结构直接融入低维表示学习中。特征域被表示为循环日历轴的笛卡尔积，并通过 L1 载荷惩罚与日历图上的图全变差来估计低秩分解。因此，该方法产生的稀疏且局部连贯的载荷模式在其原始时间坐标下保持直接可读性。Calendar-SPCA 在两个具有不同样本量和时间分辨率的独立智能电表数据集上进行了评估：GoiEner 和 Low Carbon London。一个因子实验刻画了稀疏性与日历约束的互补效应。

    arXiv:2609.06060v1 Announce Type: cross  Abstract: Long-term electricity-consumption profiles exhibit several simultaneous periodic structures, including daily, weekly, and annual cycles. This work introduces Calendar-SPCA, a calendar-structured sparse principal component method that incorporates this known multi-periodic geometry directly into low-dimensional representation learning. The feature domain is represented as the Cartesian product of cyclic calendar axes, and a low-rank factorization is estimated using an L1 loading penalty together with graph total variation over the resulting calendar graph. The method therefore produces sparse and locally coherent loading patterns that remain directly readable in their original temporal coordinates. Calendar-SPCA is evaluated on two independent smart-meter datasets with different sample sizes and temporal resolutions: GoiEner and Low Carbon London. A factorial experiment characterizes the complementary effects of sparsity and calendar co
    
[^365]: GradeTrap：尽管有明确指令要求忽略，图像中的权威线索仍会使视觉语言模型的判断发生偏移

    GradeTrap: Authority Cues in Images Shift VLM Judgments Despite Explicit Instructions to Ignore Them

    [https://arxiv.org/abs/2609.06058](https://arxiv.org/abs/2609.06058)

    该论文提出GradeTrap受控评估框架，发现即使被明确指示忽略学生答案等社会线索，图像中的权威线索（如教师或官方答案）仍会显著偏移视觉语言模型的独立判断。

    

    随着视觉语言模型日益强大并被部署在具有重要影响的现实场景中，它们必须独立评估证据，而非不加批判地屈从于人类权威。我们提出了GradeTrap，这是一个受控评估框架，将两种社会线索置于直接冲突之中：学生答案（本应诱发谄媚性认同）与归属于同伴、教师或官方参考答案的冲突性答案（本应诱发对权威的屈从）。模型在生成自由格式答案的同时，被明确指示要独立解题并忽略所有学生答案、反馈和批改标记。我们在60个合成的现实权衡场景上测试了模型：五次中性试验用于建立稳定的模型相对偏好，随后对包括对照组在内的六种实验线索进行三次重复测试。在Gemini 3.5 Flash-Lite、GPT-5.6 Luna和Claude Haiku三者共有的45项交集样本上……

    arXiv:2609.06058v1 Announce Type: cross  Abstract: As vision-language models (VLMs) become increasingly capable and are deployed in consequential real-world settings, they must evaluate evidence independently rather than defer uncritically to human authority. We introduce GradeTrap, a controlled evaluation that places two social cues in direct conflict: a student answer, which should attract sycophantic agreement, and a conflicting answer attributed to a peer, teacher, or official answer key, which should attract authority-based deference. Models produce free-form answers while being explicitly instructed to solve independently and ignore all student answers, feedback, and grading marks. We test the models on 60 synthetic real-world trade-off scenarios. Five neutral trials establish a stable model-relative preference, followed by three repetitions of six experimental cues including controls. On the 45-item common intersection across Gemini 3.5 Flash-Lite, GPT-5.6 Luna, and Claude Haiku
    
[^366]: 使用大语言模型生成数据质量规则

    Data Quality Rule Generation with LLMs

    [https://arxiv.org/abs/2609.06053](https://arxiv.org/abs/2609.06053)

    本文提出了一个形式化的可泛化生成-过滤框架，并引入基于大语言模型的LeDQeR方法，实现数据质量规则的自动化生成，解决了领域专家手动维护规则集时容易遗漏关键规则的难题。

    

    数据验证（如客户和员工数据）是许多组织中的一项重要任务。数据错误可能造成严重后果。例如，患者记录中错误的药物剂量单位可能导致危及生命的用药错误，而地址中缺失的门牌号可能导致配送失败。企业通常采用基于规则的企业数据质量（DQ）工具，允许领域专家制定规则以持续验证数据。虽然基于规则的DQ工具计算高效并能提供可解释的报告，但手动维护一套全面的规则集极具挑战性，因为领域专家往往会忽略重要的规则，尤其是在复杂领域和大数据量的情况下。因此，在实践中弥补这些缺口仍然是一个悬而未决的问题。在本文中，我们解决了自动化DQ规则生成的挑战。为此，我们形式化了一个可泛化的生成-过滤框架，并引入了LeDQeR，一个基于大语言模型的DQ规则生成方法。

    arXiv:2609.06053v1 Announce Type: new  Abstract: The validation of data, such as customer and employee data, is an important task in many organizations. Errors in data can have severe consequences. For example, a wrong drug unit in a patient record can lead to life-threatening medication errors, and a missing street number in an address to failed deliveries. Companies often employ rule-based enterprise data quality (DQ) tools, which allow domain experts to specify rules to validate the data over time. While rule-based DQ tools are computationally efficient and provide explainable reports, maintaining a comprehensive rule set manually is challenging, as domain experts often overlook essential rules, especially in complex domains and large data volumes. Hence, closing these gaps remains an open problem in practice. In this paper, we address the challenge of automated DQ rule generation. For this, we formalize a generalizable generate-filter framework and introduce LeDQeR, an LLM-based DQ
    
[^367]: FALCON-S：固定翼地效空气动力学仿真器与飞行控制学习套件

    FALCON-S: Fixed-wing ground-effect Aerodynamics Simulator and Flight Control Learning Suite

    [https://arxiv.org/abs/2609.06046](https://arxiv.org/abs/2609.06046)

    本文提出了FALCON-S，一个高保真、模块化的固定翼近地面飞行仿真框架，完整建模六自由度物理与半经验地效空气动力学，并支持CPU/GPU高吞吐量并行计算，可用于大规模强化学习训练和最优控制算法的开发与基准测试。

    

    我们提出了一个模块化、高保真的仿真框架，用于开发和基准测试在近地面运行的固定翼飞行机器人的飞行控制策略。与现有依赖简化动力学或面向悬停动力学的仿真器不同，我们的框架建模了完整的六自由度刚体物理、半经验地效空气动力学、执行器动力学、传感器噪声和环境扰动。这种物理真实性结合模块化组件设计，能够系统性地分析真实条件下的低空飞行行为。该仿真器通过 Torch 和 NVIDIA Warp 支持 CPU 和 GPU 双后端，可实现高吞吐量并行执行，适用于大规模强化学习训练和最优控制推演。统一的接口可适配多种控制器（包括强化学习和最优控制算法），用于高度调节和轨迹跟踪等任务。

    arXiv:2609.06046v1 Announce Type: cross  Abstract: We present a modular, high-fidelity simulation framework for the development and benchmarking of flight control strategies in fixed-wing aerial robots operating near the ground. Unlike existing simulators that rely on simplified or hover-oriented dynamics, our framework models full 6DoF rigid-body physics, semi-empirical ground-effect aerodynamics, actuator dynamics, sensor noise, and environmental disturbances. This physical realism, combined with modular component design, enables systematic analysis of low-altitude flight behavior under realistic conditions. The simulator supports both CPU and GPU backends via Torch and NVIDIA Warp, enabling high-throughput parallel execution suitable for large-scale reinforcement learning training and optimal control rollouts. A unified interface accommodates a range of controllers (both RL and optical control algorithms) across tasks such as altitude regulation and trajectory tracking. Cross-valida
    
[^368]: 最小化前向-前向算法中睡眠剥夺的影响

    Minimizing the Effect of Sleep Deprivation in the Forward-Forward Algorithm

    [https://arxiv.org/abs/2609.06042](https://arxiv.org/abs/2609.06042)

    本文提出通过替代激活函数、优化损失函数、阈值调整以及模拟周期性休息（包括咖啡因刺激）等方法，有效缓解了前向-前向算法中因睡眠剥夺导致的学习效果灾难性下降问题，并在MNIST和Fashion-MNIST数据集上验证了改进效果。

    

    本文解决了前向-前向算法中睡眠剥夺所带来的挑战，其中将算法中的两次前向传播分离，并使两次传播中的数据处理不平衡，被视为对睡眠剥夺人类所观察到的认知过程的模拟。先前的研究已表明，前向-前向算法中的睡眠剥夺会对学习效果产生灾难性的影响。为了缓解这一问题，我们探索了多种方法，包括替代激活函数、优化的损失函数以及阈值调整。为了模拟周期性休息，我们在交替的训练轮次中减少正样本传播的次数，从而创造出短暂的休息阶段。此外，我们还研究了咖啡因诱导刺激在睡眠剥夺条件下提升性能的潜力。在MNIST和Fashion-MNIST数据集上进行的实验评估表明，这些改进措施提升了（模型性能）。

    arXiv:2609.06042v1 Announce Type: new  Abstract: This paper addresses the challenge posed by sleep deprivation in the Forward-Forward algorithm, where separating the two passes in this algorithm and imbalancing the data processing in the passes is considered an imitation of the cognitive processes observed in humans suffering from sleep deprivation. Previous research has demonstrated that sleep deprivation in the Forward-Forward algorithm has a catastrophic effect on learning efficacy. To mitigate this issue, we explore several approaches; these include alternative activation, optimized loss function, and threshold tuning. To simulate periodic rest, we reduce the number of positive passes in alternating epochs, creating short break phases. We additionally investigate the potential of caffeine-induced stimulation to enhance performance during sleep-deprived conditions. Experimental evaluations conducted on the MNIST and Fashion-MNIST datasets demonstrate that these modifications improve
    
[^369]: IXPLORE：基于网格不确定性量化的有界理想点估计方法

    IXPLORE: Bounded Ideal Point Estimation with Grid-Based Uncertainty Quantification

    [https://arxiv.org/abs/2609.06018](https://arxiv.org/abs/2609.06018)

    IXPLORE是一种结合预测拟合目标与稀疏感知似然函数的有界理想点估计算法，在稀疏响应场景下的重构与插补误差显著优于IRT等模型方法和机器学习方法，且非线性特征变换可进一步降低误差并保持可视化可解释性。

    

    理想点估计被广泛用于政治数据的分析与可视化。然而，选择相应的空间模型涉及多种权衡：基于模型的方法（如项目反应理论IRT）基于效用函数而非针对预测精度进行优化，而大多数机器学习（ML）替代方法在嵌入稀疏测试响应时难以在训练数据之外进行泛化。我们提出了IXPLORE，这是一种将有预测拟合目标与稀疏感知似然函数相结合的有界理想点估计算法。在涵盖调查、点名投票和审议的五个基准数据集上，该方法在重构误差和插补误差方面超越了基于模型和基于机器学习的算法——尤其是对于响应稀疏的用户。此外，我们证明非线性特征变换可以进一步降低重构误差，同时保持视觉上的可解释性。为了量化不确定性，（摘要在此处截断）

    arXiv:2609.06018v1 Announce Type: new  Abstract: Ideal point estimation is widely used to analyze and visualize political data. However, selecting the corresponding spatial model involves various trade-offs: while model-based approaches such as Item Response Theory (IRT) are based on utility functions rather than optimized for predictive accuracy, most Machine Learning (ML) alternatives struggle to generalize beyond training data when embedding sparse test responses. We introduce IXPLORE, a bounded ideal point estimation algorithm that combines a predictive fit objective with a sparsity-aware likelihood function. On five benchmark datasets spanning surveys, roll calls, and deliberation, this approach surpasses model-based and ML-based algorithms on reconstruction and imputation error - especially for users with sparse responses. Furthermore, we show that non-linear feature transforms can further reduce the reconstruction error while remaining visually interpretable. To quantify uncerta
    
[^370]: 面向资源高效与鲁棒学习的颗粒球量子聚类

    Granular-Ball Quantum Clustering for Resource-Efficient and Robust Learning

    [https://arxiv.org/abs/2609.06016](https://arxiv.org/abs/2609.06016)

    该论文提出颗粒球量子聚类（GBQC）框架，通过PCA引导的颗粒球压缩策略将量子核构造所需的电路执行次数从O(n²)大幅降低，同时保留量子关联结构，实现了资源高效且对非凸、含噪数据鲁棒的量子聚类。

    

    量子聚类旨在利用量子特征表示来揭示超越传统欧几里得几何的复杂数据结构。然而，这种样本级的核构造需要对 n 个数据点执行 O(n^2) 次量子电路运行，在近期量子资源受限的条件下构成了重大瓶颈。已有的解决方案未能化解这一效率与精度之间的两难困境：经典颗粒球聚类降低了样本复杂度，但依赖的欧几里得度量无法捕捉量子关联；而现有的量子压缩方案则以效率优先、牺牲结构保持，导致在非凸或含噪数据上的性能下降。本文提出颗粒球量子聚类（GBQC），这是一个将颗粒球结构抽象与量子特征学习紧密耦合的框架。GBQC 首先通过一种由 PCA 引导的分裂策略，将原始数据压缩为紧凑且具有代表性的颗粒球，从而减少核（此处摘要原文不完整）……

    arXiv:2609.06016v1 Announce Type: new  Abstract: Quantum clustering aims to exploit quantum feature representations to uncover complex data structures beyond conventional Euclidean geometry. Yet this sample-level kernel construction requires O(n^2) quantum circuit executions for n data points, creating a major bottleneck under near-term quantum resource constraints. Prior solutions fail to resolve this efficiency-accuracy dilemma: classical granular-ball clustering reduces sample complexity but relies on Euclidean metrics that cannot capture quantum correlations, while existing quantum compression schemes prioritize efficiency over structural preservation, degrading performance on non-convex or noisy data. Here we propose Granular-Ball Quantum Clustering (GBQC), a framework that tightly couples granular-ball structural abstraction with quantum feature learning. GBQC first compresses raw data into compact, representative granular balls via a PCA-guided splitting strategy, reducing kerne
    
[^371]: Cadence：基于时间序列基础模型的误差有界需求时间序列有损压缩

    Cadence: Error-Bounded Lossy Compression of Demand Time Series with a Time-Series Foundation Model

    [https://arxiv.org/abs/2609.06008](https://arxiv.org/abs/2609.06008)

    Cadence将时间序列基础模型TimesFM-3与自适应范围编码器相结合实现误差有界的有损时间序列压缩，并揭示了一个关键洞察：基础模型在无损编码中因节省比特数与精度呈对数关系而几乎无用，但在误差有界编码中一旦预测落入误差带即可使样本近乎零成本。

    

    我们提出了Cadence，一种针对数值时间序列的误差有界有损压缩器，它将一个3.3亿参数的时间序列基础模型（Google TimesFM-3）与自适应算术编码器相结合，保证每个样本满足误差界 $|\hat{x}_t-x_t|\le\tau$。一个负面结果限制了设计空间：对于无损编码，基础模型毫无价值，因为节省的比特数与预测器精度呈对数关系，即 $\Delta b=\log_2(\mathrm{MAE_{old}}/\mathrm{MAE_{new}})$。因此，TimesFM-3 相对于32抽头线性预测器1.51倍的精度优势，仅能在20.28比特中节省0.60比特，中位数增益仅为+0.03%。而误差有界编码在这一点上摆脱了该限制：一旦预测值落入误差带内，残差索引为零，该样本的编码几乎不占成本。Cadence的贡献包括：(1) 一种采用上下文建模二值化的自适应范围编码器，在真实指数数据上比xz/zstd提升9.7%（15/15全胜），并推翻了来自通用压缩后端的一个已有结论；(2) 一个确定性结果——

    arXiv:2609.06008v1 Announce Type: cross  Abstract: We present Cadence, an error-bounded lossy compressor for numeric time series pairing a 330M-parameter time-series foundation model (Google TimesFM-3) with an adaptive arithmetic coder, guaranteeing $|\hat{x}_t-x_t|\le\tau$ on every sample. One negative result constrains the design space: for lossless coding a foundation model is worth nothing, because bits saved are logarithmic in predictor accuracy, $\Delta b=\log_2(\mathrm{MAE_{old}}/\mathrm{MAE_{new}})$. So the $1.51\times$ advantage TimesFM-3 holds over a 32-tap linear predictor buys 0.60 bits of 20.28, a median gain of +0.03%. Error-bounded coding escapes this at one point: once a forecast lands inside the band the residual index is zero and the sample nearly free. Cadence contributes: (1) an adaptive range coder with context-modelled binarization, beating xz/zstd on real indices by 9.7% (15/15) and reversing a finding from a general-purpose back end; (2) a determinism result -- 
    
[^372]: 深度时间序列模型中的记忆

    Memory in Deep Time-Series Models

    [https://arxiv.org/abs/2609.06006](https://arxiv.org/abs/2609.06006)

    本文提出用一个统一的“记忆”视角来重新审视时间序列深度学习中从循环网络到基础模型等各类架构范式，将其视为对“模型如何在直接输入之外保留和访问信息”这一共同问题的不同解决方案。

    

    时间序列深度学习经历了多个架构范式的相继演进，从循环网络和Transformer，到结构化状态空间模型、检索增强预测器、基础模型以及使用工具的智能体。这些发展通常被孤立地研究，按架构或建模时代来组织。我们认为，可以通过一个共同的问题来审视它们：时间序列模型如何在直接输入之外保留和访问信息？这个问题的动机来自传统时间序列建模的一个根本局限：与预测相关的信息可能位于可行输入窗口之外很远的地方，而将历史压缩为固定大小的状态可能会丢弃之后可能变得有用的信息。我们将这一挑战表述为一个“记忆”问题，并将现有的时间序列方法沿着一个从内部记忆（编码在参数和固定大小的状态中）到外部记忆的谱系进行组织……

    arXiv:2609.06006v1 Announce Type: cross  Abstract: Deep learning for time series has progressed through successive architectural paradigms, from recurrent networks and transformers to structured state-space models, retrieval-augmented predictors, foundation models, and tool-using agents. These developments are typically studied in isolation, organized by architecture or modeling era. We argue that they can instead be viewed through a common question of \emph{how does a time-series model retain and access information beyond its immediate input?} This question is motivated by a fundamental limitation of conventional time-series modeling: information relevant to a prediction may lie far beyond a feasible input window, while compressing history into a fixed-size state can discard information that may become useful later. We formulate this challenge as a \emph{memory} problem and organize existing time-series methods along a spectrum from internal memory, encoded in parameters and fixed-siz
    
[^373]: ModularPhaseNet：面向标准Transformer中可计算语义层次、方向与上下文一致性的有限循环相位几何

    ModularPhaseNet: Finite-Cyclic Phase Geometry for Computable Semantic Hierarchy, Direction, and Context Consistency in Standard Transformers

    [https://arxiv.org/abs/2609.06000](https://arxiv.org/abs/2609.06000)

    ModularPhaseNet通过将辅助相位通道离散化为有限循环群并以纯经典整数运算实现，使标准Transformer获得可计算的语义层次结构、语义方向表示和上下文一致性度量能力。

    

    我们提出ModularPhaseNet，这是对QuantumPhaseNet中引入的连续复相位几何的一种经典且可整数计算的离散化方法。该方法保留标准Transformer的实值隐藏状态，仅将一个辅助相位通道量化为F_p乘法群中阶为q | (p-1)的循环子群G = <g>。连续相位 e^{iφ} 由 z = g^a mod p 表示；相位复合对应群乘法，相对相位对应群除法，概念层次由循环商群的滤过（filtration）诱导，语义方向由有向的相对群元素表示，而上下文一致性则通过规范不变的循环和乐（holonomy）来度量。该方法在原本标准的Transformer中引入三个组件：有限相位编码器、商群滤过层次模块和群值联络模块，它们的输出以实值形式进入自注意力机制。

    arXiv:2609.06000v1 Announce Type: cross  Abstract: We propose ModularPhaseNet, a classical and integer-computable discretization of the continuous complex phase geometry introduced in QuantumPhaseNet. The real-valued hidden states of a standard Transformer are retained, while only an auxiliary phase channel is quantized into a cyclic subgroup G =  of order q | (p-1) in the multiplicative group of F_p. A continuous phase e^{i phi} is represented by z = g^a mod p; phase composition becomes group multiplication, relative phase becomes group division, conceptual hierarchy is induced by a filtration of cyclic quotients, semantic direction is represented by oriented relative group elements, and contextual consistency is measured by gauge-invariant cycle holonomy. The method introduces three components into an otherwise standard Transformer: a finite-phase encoder, a quotient-filtration hierarchy module, and a group-valued connection module. Their outputs enter self-attention as real-valued b
    
[^374]: MOAE：基于帕累托保持搜索的多目标智能体进化

    MOAE: Multi-Objective Agent Evolution with Pareto-Preserving Search

    [https://arxiv.org/abs/2609.05992](https://arxiv.org/abs/2609.05992)

    该论文提出MOAE框架，将智能体的迭代式上下文内优化组织为帕累托保持的进化搜索，在有限的执行预算下同时优化任务准确率、交互质量、安全性和效率等多个目标，避免了传统标量化方法带来的信息损失。

    

    随着基于大语言模型（LLM）的智能体不断进步，对它们的评估变得日益多面化：一个有能力的智能体不仅要在任务完成准确率上达到较高水平，还需在交互质量、安全性和效率方面表现良好，这引出了一个核心问题：这些目标能否被同时优化？现有方法已考虑过多个目标，但许多方法将异构的度量指标压缩为固定的标量分数。这种标量化依赖于指标归一化和偏好权重，可能会丢弃那些代表有用部署权衡的候选方案。我们提出了多目标智能体进化，它将迭代式的上下文内精炼过程组织为一种针对完整智能体执行轨迹的帕累托保持进化搜索。在有限的执行预算下，MOAE 维护一个由非支配候选组成的经验档案库，利用针对特定目标的诊断信息来指导后代生成，并应用约束感知的……（摘要在此处截断）

    arXiv:2609.05992v1 Announce Type: new  Abstract: As LLM-based agents continue to advance, their evaluation has become increasingly multifaceted: a capable agent must not only achieve high task completion accuracy but also perform well in interaction quality, safety, and efficiency, raising a central question: can these objectives be optimized simultaneously? Existing methods have considered multiple objectives, but many collapse heterogeneous measurements into a fixed scalar score. Such scalarization depends on metric normalization and preference weights and may discard candidates that represent useful deployment trade-offs. We introduce Multi-Objective Agent Evolution (MOAE), which organizes iterative in-context refinement as a Pareto-preserving evolutionary search over complete agent rollouts. Given a limited rollout budget, MOAE maintains an empirical archive of non-dominated candidates, uses objective-specific diagnostics to guide offspring generation, and applies constraint-aware 
    
[^375]: DART：面向算法学习的分布式对抗递归训练

    DART: Distributional Adversarial Recurrent Training for Algorithm Learning

    [https://arxiv.org/abs/2609.05988](https://arxiv.org/abs/2609.05988)

    DART通过用真实解周围的局部目标分布替代单点监督，并结合对抗训练目标，显著提升了递归推理模型在迷宫、国际象棋和数独等结构化问题上的求解质量与稳定性。

    

    递归推理模型（RRMs）能够解决结构化问题，通过在隐藏空间中进行迭代计算实现从简单到困难的泛化。这些模型通常采用实例级监督进行训练，但随着任务难度的提升，这种训练方式的问题日益凸显：有效解仅占据解空间的极小区域，而无效解却迅速增多。我们提出了分布式对抗递归训练（DART），该训练框架用真实解周围的局部目标分布取代单点监督，并通过对抗性目标使模型输出与该分布对齐。DART提供了更丰富的学习信号，并促使模型朝着有效解形成更稳定的迭代轨迹。在迷宫（Maze）、国际象棋（Chess）和掩码数独（masked Sudoku）任务上，使用包括深度思维系统和微小递归模型在内的多种递归推理模型进行评估，DART均提升了解题质量、稳定性以及……

    arXiv:2609.05988v1 Announce Type: cross  Abstract: Recurrent reasoning models (RRMs) can solve structured problems, achieving easy-to-hard generalization through iterative computation in hidden space. These models are typically trained with instance-level supervision, which becomes increasingly problematic as task difficulty grows: valid solutions occupy a tiny region of the solution space, while invalid solutions proliferate rapidly. We propose Distributional Adversarial Recurrent Training (DART), a training framework that replaces single-point supervision with a local target distribution around the ground-truth solution and aligns model outputs with this distribution through an adversarial objective. DART provides a richer learning signal and encourages more stable iterative trajectories toward valid solutions. When evaluated on Maze, Chess, and masked Sudoku with multiple RRMs, including Deep Thinking Systems and Tiny Recursive Models, DART improves solution quality, stability, and 
    
[^376]: 基于高斯过程校正特征缓存的扩散Transformer加速方法

    Accelerating Diffusion Transformers with Gaussian Process Rectified Feature Cache

    [https://arxiv.org/abs/2609.05981](https://arxiv.org/abs/2609.05981)

    本文发现缓存特征与全计算轨迹之间的残差局部服从零均值高斯分布，据此将全计算特征视为含噪观测，提出利用高斯过程在线回归动态校正特征缓存偏差的方法，在无需标签数据的情况下有效加速扩散Transformer。

    

    扩散Transformer已成为生成式人工智能领域的主流范式，但其高昂的计算成本严重阻碍了实时应用。基于预测的特征缓存被广泛用于加速扩散Transformer；然而，随着步数的增加，其预测结果与参考全计算轨迹之间的偏差会逐渐增大。一个直观的思路是使用在线回归模型来动态校正这种偏差，但这面临着加速过程中标签数据不可获取的问题。本文提出了一项统计观察：使用缓存方法时，全计算步骤的特征与参考全计算轨迹之间的残差在局部呈现出零均值高斯分布。通过将全计算步骤的特征视为参考特征的含噪观测值，解决了数据获取难题。基于这一观察，本文提出了一种……（摘要原文在此处截断）

    arXiv:2609.05981v1 Announce Type: cross  Abstract: Diffusion Transformers have become the dominant paradigm in generative AI, but their high computational costs severely hinder real-time applications. Prediction-based feature caching is widely used to accelerate diffusion transformers; however, as the number of steps increases, the deviation between its predictions and the reference full-compute trajectory gradually grows. An intuitive idea is to use an online regression model to dynamically correct this deviation, but it faces the issue of label data being unavailable during the acceleration process. This paper presents a statistical observation that the residuals between the features of full computation steps using caching methods and reference full-compute trajectory locally exhibit a zero-mean Gaussian distribution. By treating the features of full computation steps as noisy observations of reference features, the data acquisition problem is resolved. Based on this observation, a p
    
[^377]: 用于培养前ESBL风险分层以指导经验性抗生素选择的机器学习：一项涵盖12家医院的肠杆菌科细菌培养研究

    Machine Learning for Pre-Culture ESBL Risk Stratification to Guide Empiric Antibiotic Selection: A 12-Hospital Study of Enterobacteriaceae Cultures

    [https://arxiv.org/abs/2609.05970](https://arxiv.org/abs/2609.05970)

    该研究开发了一种基于培养前电子健康记录特征的机器学习模型，可在培养结果出来前预测ESBL感染风险，有望在非ICU环境中帮助临床医生安全地减少碳青霉烯类药物的过度使用。

    

    疑似产ESBL（超广谱β-内酰胺酶）肠杆菌科细菌感染的经验性抗生素治疗必须在培养结果出来前48-72小时内选择，这迫使临床医生在治疗不足（导致耐药感染得不到控制）与过度使用碳青霉烯类药物（进而加剧耐药性）之间做出艰难抉择。我们开发了一个成本敏感的XGBoost模型，在开具培养医嘱时利用45个培养前电子健康记录（EHR）特征预测ESBL表型（即对头孢曲松、头孢他啶、头孢吡肟或哌拉西林-他唑巴坦耐药）。研究数据涵盖12家医院72,217名患者的132,955份培养样本（其中14.41%为ESBL表型），培养数据在患者层面进行了划分。在90%灵敏度下，该模型达到了95.8%的阴性预测值（NPV），将检测后ESBL概率降至4.2%，这一阈值可能支持在非ICU环境中安全地避免使用碳青霉烯类药物，同时每1,000份培养中约有307份可避免不必要的广谱抗生素疗程，代价是每1,000份培养中漏诊14例ESBL病例。SHAP分析显示既往ESBL定植是……（原文摘要在此处不完整）

    arXiv:2609.05970v1 Announce Type: new  Abstract: Empiric antibiotic therapy for suspected ESBL-producing Enterobacteriaceae must be selected 48-72 hours before culture results, forcing clinicians to choose between undertreating resistant infections and overusing carbapenems that drive further resistance. We developed a cost-sensitive XGBoost model predicting an ESBL phenotype (resistance to ceftriaxone, ceftazidime, cefepime or piperacillin-tazobactam) at culture ordering using 45 pre-culture EHR features across 132,955 cultures from 72,217 patients at 12 hospitals (14.41% with the ESBL phenotype). Cultures were partitioned at the patient level. At 90% sensitivity, the model achieved 95.8% NPV, reducing post-test ESBL probability to 4.2%, a threshold that may support safe carbapenem-sparing in non-ICU settings, while sparing 307 of every 1,000 cultures an unnecessary broad-spectrum course at the cost of 14 missed ESBL cases per 1,000. SHAP analysis identified prior ESBL colonization as
    
[^378]: 通过ERASE实现无需显式遗忘的即时遗忘

    On-the-go Forgetting without Explicit Unlearning via ERASE

    [https://arxiv.org/abs/2609.05966](https://arxiv.org/abs/2609.05966)

    ERASE是一个无需修改模型权重的即时遗忘框架，通过类别条件化的输入扰动在推理阶段实现选择性遗忘，避免了传统遗忘方法的重训练、微调和额外内存开销。

    

    arXiv:2609.05966v1 公告类型：新 摘要：现有的机器遗忘方法通常依赖于事后权重调整或知识蒸馏，导致重复的内存开销、泛化能力下降以及可扩展性受限。在本工作中，我们提出了ERASE（通过重构性对抗信号编辑实现擦除），这是一个即时遗忘框架，能够在不修改模型权重的情况下抑制私有数据的可观测影响。ERASE利用结构化的、类别条件化的输入扰动，在推理过程中诱导选择性遗忘，从而无需重新训练、微调或模型副本。我们严格刻画了ERASE能够可证明地实现对指定子类功能性遗忘、同时保留同一超类中其他子类预测的充分条件。这一分析在温和的正则性假设下，为推理时遗忘提供了有原则的基础。在多种架构和基准数据集上，ERASE

    arXiv:2609.05966v1 Announce Type: new  Abstract: Existing unlearning approaches typically rely on post hoc weight adaptation or distillation, leading to duplicated memory costs, degraded generalization, and limited scalability. In this work, we introduce ERASE, Erasure via Reconstructive Adversarial Signal Editing, a framework for on-the-go forgetting that suppresses the observable influence of private data without modifying model weights. ERASE leverages structured, class-conditioned input perturbations to induce selective forgetting during inference, eliminating the need for retraining, fine-tuning, or model copies. We rigorously characterize sufficient conditions when ERASE provably achieves functional forgetting of designated subclasses while preserving predictions across other subclasses within the same superclass. This analysis offers a principled foundation for inference-time forgetting under mild regularity assumptions. Across diverse architectures and benchmark datasets, ERASE
    
[^379]: LoGIC：基于预算的上下文构建用于表格基础模型的节点级图上下文学习

    LoGIC: Budgeted Context Construction for Node-Level Graph In-Context Learning with Tabular Foundation Models

    [https://arxiv.org/abs/2609.05955](https://arxiv.org/abs/2609.05955)

    提出LoGIC方法，通过在带标签上下文预算和无标签光环预算下检索并跨查询共享上下文，解决了节点级图上下文学习中注意力随带标签池二次方增长导致的预处理和内存瓶颈问题。

    

    表格基础模型已成为强大的图学习器。诸如G2T-FM和GraphPFN等系统将每个节点编码为特征行，并通过上下文学习（ICL）进行预测，其中带标签的行作为提示。当前的协议使用完整的训练表作为上下文，导致注意力随带标签池的规模呈二次方增长，并引入了预处理和内存瓶颈。我们研究节点级图ICL的上下文构建问题：哪些带标签节点和辅助的无标签节点应该构成指定查询的提示。我们从两种资源的角度来表述这种分配：用于预测证据的带标签上下文预算，以及用于适配器消息传递且不占用标签容量的无标签光环预算。我们提出了LoGIC，它通过结构、特征和覆盖三种通道检索带标签节点，在图局部聚类内的查询之间共享上下文，并结合…

    arXiv:2609.05955v1 Announce Type: new  Abstract: Tabular foundation models have become powerful graph learners. Systems such as G2T-FM and GraphPFN encode each node as a feature row and make predictions through in-context learning (ICL), with labeled rows serving as the prompt. Current protocols employ the complete training table as context, causing attention to scale quadratically with the labeled pool and introducing preprocessing and memory bottlenecks. We investigate context construction for node-level graph ICL: which labeled nodes and auxiliary unlabeled nodes should constitute the prompt for specified queries. We formulate this allocation in terms of two resources: a labeled-context budget for predictive evidence and an unlabeled-halo budget for adapter message passing without using label capacity. We present LoGIC, which retrieves labeled nodes via structural, feature-based, and coverage channels, shares each context across the queries in a graph-local cluster, incorporates an 
    
[^380]: QGB-W$k$NN：基于量子颗粒球学习的鲁棒分类方法

    QGB-W$k$NN: Quantum Granular-Ball Learning for Robust Classification

    [https://arxiv.org/abs/2609.05952](https://arxiv.org/abs/2609.05952)

    本文提出基于量子颗粒球的加权K近邻分类框架QGB-W$k$NN，通过量子增强颗粒球表示、层次化近邻搜索和纯度感知加权决策机制，同时提升了计算效率与噪声环境下的分类鲁棒性。

    

    近邻分类在机器学习中被广泛应用，然而现有方法往往存在计算效率低以及在噪声环境中鲁棒性有限的问题。为了同时应对这些挑战，本文提出了一种基于量子颗粒球的高效且可靠的加权K近邻分类框架，称为QGB-W$k$NN。该框架通过将量子增强的颗粒球表示与层次化近邻搜索相结合来提升计算效率，同时通过一种纯度感知的加权决策机制来增强分类的可靠性。具体而言，构建量子核颗粒球以减少检索冗余，并在有限量子资源下强化非线性特征表示。此外，开发了一种由颗粒球纯度引导的HNSW优化策略，在近邻搜索的层次图构建过程中利用结构可靠性信息……

    arXiv:2609.05952v1 Announce Type: new  Abstract: Nearest-neighbor classification is widely used in machine learning, yet existing methods often suffer from low computational efficiency and limited robustness in noisy environments. To jointly address these challenges, this paper proposes an efficient and reliable weighted $K$-nearest neighbor classification framework based on quantum granular balls, termed QGB-W$k$NN. The proposed framework improves computational efficiency by integrating quantum-enhanced granular-ball representation with hierarchical nearest-neighbor search, while enhancing classification reliability through a purity-aware weighted decision mechanism. Specifically, quantum-kernel granular balls are constructed to reduce retrieval redundancy and strengthen nonlinear feature representation under limited quantum resources. A granular-ball purity-guided HNSW optimization strategy is developed to exploit structural reliability for hierarchical graph construction during neig
    
[^381]: 通过多目标学习实现可解释且公平的广义可加神经网络

    Interpretable and Fair Generalized Additive Neural Networks via Multi-objective Learning

    [https://arxiv.org/abs/2609.05946](https://arxiv.org/abs/2609.05946)

    本文提出多目标神经基模型（MONBM），通过引入可解释性的显式定量指标，首次实现了基于神经网络的广义可加模型中可解释性与公平性的同时优化，并深入分析了两者的权衡关系。

    

    可解释性和公平性是可信人工智能（AI）中最受重视的两个维度。为了提高可解释性，已有多种可解释AI方法被提出。本文聚焦于基于神经网络（NN）的广义可加模型（GAM），这是一类自解释模型。虽然现有研究大多优先考虑提高基于神经网络的广义可加模型的准确性，但其可解释性在很大程度上仍未得到充分探索。为填补这一空白，本文引入了用于评估基于神经网络的广义可加模型可解释性的显式定量指标，通过实验验证了这些指标的有效性，并探索了提升这些模型可解释性的策略。此外，可解释性与公平性的同时显式优化，以及两者之间的权衡及其深层原因，也仍然缺乏充分研究。为此，我们提出了一种多目标神经基模型（MONBM）。

    arXiv:2609.05946v1 Announce Type: new  Abstract: Interpretability and fairness are two of the most emphasized dimensions in trustworthy artificial intelligence (AI). Various explainable AI methods have been introduced to improve interpretability. This paper focuses on neural network (NN)-based generalized additive models (GAMs), a class of self-interpretable models. While most existing research has prioritized improving the accuracy of NN-based GAMs, their interpretability remains largely underexplored. To address this gap, this paper introduces explicit quantitative metrics for evaluating the interpretability of NN-based GAMs, empirically examines their effectiveness, and explores strategies for improving interpretability within these models. In addition, the simultaneous and explicit optimization of both interpretability and fairness, along with their trade-offs and the underlying reasons, remains underexplored. To address this, we propose a multi-objective neural basis model (MONBM)
    
[^382]: 重新思考多智能体系统效率评估方法

    Rethinking the Evaluation of Efficiency Methods for Multi-Agent Systems

    [https://arxiv.org/abs/2609.05933](https://arxiv.org/abs/2609.05933)

    本文指出现有多智能体系统效率方法的评估可能高估了其真实效果，并提出一个受控且对MAS有需求的诊断基准，在共享环境下公平地评估这些效率方法。

    

    效率对于基于大语言模型（LLM）的多智能体系统（MAS）日益重要，因为更大的模型和更多的智能体会带来可观的执行成本。最近的方法旨在通过剪枝智能体、移除通信边或搜索紧凑结构来降低MAS的成本。然而，我们认为现有的评估可能高估了这些方法真正提升MAS效率的能力。所报告的收益通常是在特定于方法的提示词和起始拓扑下测量的，因此难以将其归因于所提出的结构改变。此外，许多报告的成功案例出现在对MAS需求不高的场景中——在这些场景下，单个智能体或随机剪枝的系统已经能够保持强大的性能。为了研究这些问题，我们引入了一个受控且对MAS有需求的诊断基准，用于评估代表性的MAS效率方法。我们在共享的骨干模型、智能体注册表和运行时环境下对这些方法进行评估。

    arXiv:2609.05933v1 Announce Type: new  Abstract: Efficiency is increasingly important for Large Language Model (LLM)-based multi-agent systems (MAS), as larger models and more agents introduce substantial execution costs. Recent methods aim to make MAS cheaper by pruning agents, removing communication edges, or searching for compact structures. However, we argue that existing evaluations may overestimate their true ability to improve MAS efficiency. Reported gains are often measured under method-specific prompts and starting topologies, making them difficult to attribute to the proposed structural changes. Moreover, many reported successes appear in non-MAS-demanding settings, where a single agent or a randomly pruned system can already preserve strong performance. To study these issues, we introduce a controlled and MAS-demanding diagnostic benchmark for representative MAS efficiency methods. We evaluate methods under a shared backbone model, agent registry, and runtime, across contro
    
[^383]: 基于滤波器和最优传输的图字典学习框架

    A dictionary learning framework for graphs via filters and optimal transport

    [https://arxiv.org/abs/2609.05919](https://arxiv.org/abs/2609.05919)

    本文提出了一种基于滤波图距离与最优传输的图字典学习框架，将图表示为滤波拉普拉斯导出的高斯分布，通过可端到端优化的sfGOT距离实现图的重构，并从HSIC视角给出了新的理论解释。

    

    我们提出了一种图字典学习（GDL）框架，其中每个图被表示为由其滤波拉普拉斯矩阵导出的零均值高斯分布。每个观测图通过在学到的原子图上计算的重心（barycenter）来近似，该计算基于滤波图距离（fGOT），这是一种对全局结构性质敏感的图比较度量。观测图与其重心之间的重构误差由代理滤波图距离（sfGOT）来度量，它是fGOT的一种易于处理的近似，能够处理没有已知节点对应关系的图，并通过反向传播进行端到端的最小化。我们进一步通过希尔伯特-施密特独立性准则（Hilbert-Schmidt Independence Criterion）的视角为sfGOT提供了新颖的解释，表明最小化两个图之间的sfGOT距离等价于最大化其节点谱嵌入之间的统计依赖性。在基准数据集上的实验展示了有竞争力的性能。

    arXiv:2609.05919v1 Announce Type: new  Abstract: We propose a graph dictionary learning (GDL) framework where each graph is represented as a zero-mean Gaussian distribution derived from its filtered Laplacian. Each observed graph is approximated by a barycenter over learned atom graphs, computed under the filter graph distance (fGOT), a graph comparison metric sensitive to global structural properties. The reconstruction error between the observed graph and its barycenter is measured by the surrogate fGOT (sfGOT) distance, a tractable approximation of fGOT that handles graphs without known node correspondence, and is minimized end-to-end via backpropagation. We further provide a novel interpretation of sfGOT through the lens of the Hilbert-Schmidt Independence Criterion, showing that minimizing the sfGOT distance between two graphs is equivalent to maximizing statistical dependence between the spectral embedding of their nodes. Experiments on benchmark datasets demonstrate competitive 
    
[^384]: 结构上相近，时间上遥远：测量长时程LLM智能体的安全暴露

    Structurally Close, Temporally Distant: Measuring Security Exposure in Long-Horizon LLM Agents

    [https://arxiv.org/abs/2609.05911](https://arxiv.org/abs/2609.05911)

    该论文提出了一种溯源感知的执行图与“影响距离”新度量，证明在有状态的长时程LLM智能体中，仅凭时间步骤数衡量攻击距离会高估安全隔离，并通过“间隔”量化被步骤数掩盖的真实安全暴露。

    

    长时程LLM智能体需要与不可信内容、持久记忆、外部状态以及敏感工具进行交互。现有分析通常通过恶意输入与下游动作之间的执行步骤数来刻画攻击。我们证明了在有状态的智能体中，时间上的遥远程度可能会夸大安全隔离的效果。我们引入了一种溯源感知的执行图，通过确定性的状态、标识符和工具溯源将智能体事件关联起来，并将影响距离 $\DI$ 定义为从不可信源到敏感动作的最短结构路径。我们将其与序列距离 $\DT$ 进行比较，后者是有序轨迹中从注入点到敏感汇聚点的最短路径。由于影响图包含所有序列边，因此 $\DI \leq \DT$；而 $\Gap=\DT-\DI$ 度量的是被步骤数所掩盖的隔离程度。在基于OpenAI的gpt-4o-mini等模型的360条长时程AgentDojo轨迹中的454个注入-汇聚对上（摘要截断）

    arXiv:2609.05911v1 Announce Type: cross  Abstract: Long-horizon LLM agents interact with untrusted content, persistent memory, external state, and sensitive tools. Existing analyses often characterize attacks by the number of execution steps between malicious input and a downstream action. We show that temporal remoteness can overstate security separation in stateful agents. We introduce a provenance-aware execution graph linking agent events through deterministic state, identifier, and tool provenance, and define \emph{influence distance} $\DI$ as the shortest structural path from an untrusted source to a sensitive action. We compare it with \emph{sequence distance} $\DT$, the shortest injection--sink path in the ordered trajectory. Since the influence graph contains every sequence edge, $\DI \leq \DT$; $\Gap=\DT-\DI$ measures the separation hidden by step count. Across 454 injection--sink pairs from 360 long-horizon AgentDojo trajectories over OpenAI's \texttt{gpt-4o-mini} and \textt
    
[^385]: 从叙事到可审计的预测：一种用于智能体预测的结构化支架

    From Narrative to Auditable Forecasts: A Structured Scaffold for Agentic Forecasting

    [https://arxiv.org/abs/2609.05905](https://arxiv.org/abs/2609.05905)

    提出AuditForecast结构化预测支架，通过基线模型锚定、模型引导的数据检索以及赔率空间中的机械聚合，将LLM智能体预测从隐式叙事判断转变为具有明确中间步骤的可审计过程。

    

    大语言模型（LLM）智能体正越来越多地被用于实时预测任务，它们检索最新信息并对尚未解决的未来事件做出概率估计。然而，当前的智能体预测往往依赖于隐式的叙事式聚合：智能体收集证据，以散文形式进行讨论，然后在没有从证据到预测的明确更新路径的情况下直接给出概率。这既限制了预测准确性，也限制了可审计性。我们提出了AuditForecast，一个用于结构化概率预测的智能体支架。AuditForecast首先使用合适的定量基线模型锚定预测，通过模型引导的数据检索得出基础概率，然后通过在赔率空间中的机械聚合，将模型范围之外的情势因素更新纳入预测。这将预测从基于文本叙述的判断转变为具有明确中间对象的结构化过程。在多个实时预测基准测试中，AuditForecast（原文此处截断）……

    arXiv:2609.05905v1 Announce Type: cross  Abstract: LLM agents are increasingly used for live forecasting, where they retrieve up-to-date information and produce estimates for unresolved future events. However, current agentic forecasting often relies on implicit narrative aggregation: agents collect evidence, discuss it in prose, and often assign a probability without an explicit update path from evidence to forecast. This limits both forecasting accuracy and auditability. We propose AuditForecast, an agentic scaffold for structured probabilistic forecasting. AuditForecast first anchors the forecast with a suitable quantitative baseline model, uses model-guided data retrieval to derive a base probability, and then applies situational factor updates outside the model's scope through mechanical aggregation in odds space. This turns forecasting from a prose-based judgment into a structured process with explicit intermediate objects. Across multiple live forecasting benchmarks, AuditForeca
    
[^386]: 链接符号预测的极性非对称结构校准

    Polarity-Asymmetric Structural Calibration for Link Sign Prediction

    [https://arxiv.org/abs/2609.05896](https://arxiv.org/abs/2609.05896)

    该论文提出极性非对称结构校准框架PASC，通过目标边结构先验得分与局部符号上下文线索的冲突残差来校准符号注意力聚合，提升了符号网络链接符号预测在严重符号不平衡和局部冲突场景下的鲁棒性。

    

    链接符号预测（LSP）旨在推断符号网络中未观测链接的正负极性。符号图神经网络（SGNNs）通常依赖符号图的结构先验，包括结构平衡和类同质性相似性，来指导消息传递和预测。然而，这些先验描述的是群体层面的倾向性，而非对个体目标边的保证。在严重的符号不平衡情况下，这些先验失效的代价尤为高昂，因为少数派和局部冲突关系上的错误更难被检测和纠正。我们提出了极性非对称结构校准（PASC），一个面向符号链接预测的目标边结构先验校准框架。PASC构建仅基于结构的先验表示，估计目标边的结构先验得分，并将该得分与局部符号上下文线索进行比较，从而得出冲突残差。该残差用于校准符号注意力聚合……

    arXiv:2609.05896v1 Announce Type: cross  Abstract: Link sign prediction (LSP) aims to infer the positive or negative polarity of unobserved links in signed networks. Signed Graph Neural Networks (SGNNs) usually rely on signed-graph structural priors, including structural balance and homophily-like similarity, to guide message passing and prediction. These priors describe population-level tendencies, not guarantees for individual target edges. Their failures are especially costly under severe sign imbalance, where errors on minority and locally conflicting relations are harder to detect and correct. We propose Polarity-Asymmetric Structural Calibration (PASC), a target-edge structural-prior calibration framework for signed link prediction. PASC constructs a structure-only prior representation, estimates a target-edge structural prior score, and compares this score with a local signed-context cue to derive a conflict residual. The residual calibrates signed attention aggregation, target-
    
[^387]: 一种具有常数后悔的在线资源分配一阶学习算法

    A First-Order Learning Algorithm for Online Resource Allocation with Constant Regret

    [https://arxiv.org/abs/2609.05895](https://arxiv.org/abs/2609.05895)

    该论文提出了一种无需求解线性规划的一阶学习策略，在请求到达概率未知的在线资源分配问题中实现了与时域长度无关的常数期望后悔界。

    

    我们研究了一个有限时域的在线资源分配问题，其中初始资源容量与时域长度成比例。在每个周期，会观察到一种请求类型，并从有限的动作菜单中选择一个动作。每个动作会获得一份奖励并消耗一个资源向量。请求到达类型是独立同分布的，但其概率是未知的。我们提出了一种原始一阶学习策略，在每个周期中，仅对与当前请求类型相关的动作坐标执行一次梯度上升更新。该策略实现了相对于事后最优解的 O(1) 期望加性后悔，且该后悔界与时域长度 T 无关。该策略不需要求解任何线性规划，并且其后悔界不要求流体线性规划满足非退化假设。

    arXiv:2609.05895v1 Announce Type: new  Abstract: We study a finite-horizon online resource allocation problem with initial resource capacities proportional to the horizon. In each period, a request type is observed and one action is chosen from a finite menu. Each action earns a reward and consumes a vector of resources. The arrival types are independent and identically distributed, but their probabilities are unknown. We present a primal first-order learning policy that, in each period, performs one gradient ascent update of the action coordinates associated with the current request type. The policy achieves $O(1)$ expected additive regret relative to the hindsight optimum, with a bound independent of the horizon $T$. It does not solve any linear program, and the regret bound does not require a nondegeneracy assumption on the fluid linear program.
    
[^388]: 一种学习率是不够的：用于LoRA微调的自适应各向异性学习率

    One Rate Is Not Enough: Adaptive Anisotropic Learning Rates for LoRA Fine-Tuning

    [https://arxiv.org/abs/2609.05885](https://arxiv.org/abs/2609.05885)

    提出AnLR-LoRA，一种为LoRA适配器的每个秩一分量自适应分配有效学习率的各向异性方法，该方法利用AdamW优化中的函数空间速度和Adam信噪比在线计算学习率并按模块归一化，解决了统一学习率惯例忽视模块内异质性、导致秩预算利用不足的问题。

    

    低秩适应（LoRA）已成为大语言模型参数高效微调的标准方法。大多数LoRA变体遵循统一学习率的惯例，即对所有适配器的每个秩一分量应用单一的全局学习率。我们表明，这一惯例忽视了模块内部显著的异质性：LoRA适配器的秩一分量以极不均匀的速率更新，且低速度模块会收敛到集中的奇异谱，从而未能充分利用标称的秩预算。为解决这一问题，我们提出了一种自适应各向异性学习率模型，为每个秩一分量分配其自身的有效学习率，该学习率根据训练过程中的信号在线计算，并在每个模块内进行均值归一化以保持全局学习率预算。AnLR-LoRA利用AdamW优化期间可获得的两种信号（即函数空间速度和Adam信噪比）来实例化该模型，作为一种轻量级方案，无需额外的可训练参数。

    arXiv:2609.05885v1 Announce Type: new  Abstract: Low-rank adaptation (LoRA) has become the standard for parameter-efficient fine-tuning of large language models. Most LoRA variants follow a uniform-LR convention, applying a single global learning rate across every rank-one component of every adapter. We show that this convention overlooks substantial within-module heterogeneity, where the rank-one components of a LoRA adapter update at highly uneven rates and low-velocity modules converge to concentrated singular spectra that underutilize the nominal rank budget. To address this, we propose an adaptive anisotropic learning-rate model that assigns each rank-one component its own effective learning rate, computed online from training-time signals and mean-normalized per module to preserve the global LR budget. AnLR-LoRA instantiates this model with two signals available during AdamW optimization, namely function-space velocity and Adam SNR, as a lightweight scheme with no extra trainable
    
[^389]: CALM：面向去中心化联邦学习的类级一致性与标签门控分歧调制

    CALM: Class-wise Agreement and Label-gated Disagreement Modulation for Decentralized Federated Learning

    [https://arxiv.org/abs/2609.05884](https://arxiv.org/abs/2609.05884)

    提出CALM方法，在去中心化联邦蒸馏中以平滑的多层信任门控（类级一致性加权和样本级分歧调制）取代现有硬性的全有或全无决策，从而提升严重非独立同分布数据下的模型聚合可信度和精度。

    

    传统联邦学习依赖于参数平均，这要求客户端具有双重同质性：所有客户端必须运行完全相同的模型架构，且当本地数据为非独立同分布（non-IID）时精度会显著下降。去中心化联邦蒸馏则同时规避了这两个问题：每个客户端将其他客户端的模型快照作为教师模型，在本地数据上运行并从其软预测中进行蒸馏，无需服务器、无需公共数据、也无需共享架构。然而，在严重的非独立同分布数据偏斜下，聚合教师目标的可信度是一个程度问题，而现有流程却做出硬性的全有或全无决策：离群教师被阈值直接丢弃，而幸存下来的目标则被完全信任。我们提出了CALM，它在三个层面上用平滑的信任门控取代所有硬性决策：在类别层面，教师根据与同伴共识的一致性进行加权；在样本层面，蒸馏强度根据教师与该目标的分歧程度进行调制；以及一个……（摘要在此处截断）

    arXiv:2609.05884v1 Announce Type: new  Abstract: Conventional federated learning relies on parameter averaging, which forces clients to be doubly homogeneous: all must run an identical architecture, and accuracy degrades when local data are non-IID. Decentralized federated distillation sidesteps both: each client runs its peers' model snapshots as teachers on its own local data and distills from their soft predictions, with no server, no public data, and no shared architecture. Under severe non-IID skew, however, the trustworthiness of the aggregated teacher target is a matter of degree, yet existing pipelines make hard, all-or-nothing decisions: outlier teachers are discarded by threshold, and whatever target survives is trusted in full. We propose CALM, which replaces every hard decision with a smooth trust gate at three levels: per class, teachers are weighted by agreement with the peer consensus; per sample, distillation is scaled by the teachers' divergence from that target; and a
    
[^390]: 出厂即坏：公共模型注册库中静默失效的大语言模型制品及其检测方法

    Broken on Arrival: Silently Defective LLM Artifacts in Public Model Registries and How to Catch Them

    [https://arxiv.org/abs/2609.05881](https://arxiv.org/abs/2609.05881)

    首次系统性冒烟测试了公共模型注册库中的327个量化GGUF模型制品，发现官方Ollama库中存在五个静默失效的量化模型转换制品，并提出了一套可有效捕获此类缺陷的检测流程。

    

    开发者越来越多地通过从公共模型注册库拉取量化GGUF制品来在本地运行大语言模型，然而整个分发流程中没有任何环节在用户获取这些转换产物之前对其进行功能性测试。我们共执行了327个具备代码能力的量化模型制品：其中305个来自官方Ollama库，涵盖15个模型系列在8GB及以下所有符合条件的量化级别；另外22个来自HuggingFace上下载量最高的社区仓库。每个制品运行一个包含15项任务的冒烟测试套件，该套件经过校准，健康的制品能够通过，而已知损坏的制品则会失败；疑似有问题的制品随后将接受完整的164项任务评估、第二种推理后端的测试、由独立分发商对同一模型和量化级别进行转换作为仲裁，对于社区文件，还会在制品自身的模板下重新测试。官方库中存在五个静默失效的制品，包括一批四个Qwen2.5-Coder-3B转换版本和一个phi3.5-mini转换版本……

    arXiv:2609.05881v1 Announce Type: cross  Abstract: Developers increasingly run large language models locally by pulling quantized GGUF artifacts from public registries, yet nothing in the distribution pipeline functionally tests these conversions before they reach users. We executed 327 quantized code-capable model artifacts: 305 from the official Ollama library, spanning 15 model lines at every eligible quantization level at or under 8 GB, and 22 from the most-downloaded community repositories on HuggingFace. Each ran a 15-task smoke suite calibrated so that healthy artifacts pass while a known-broken one fails; suspects then faced full 164-task evaluation, a second inference backend, an independent distributor's conversion of the same model and quantization as referee, and, for community files, re-testing under the artifact's own template. The official library carries five silently defective artifacts, a batch of four Qwen2.5-Coder-3B conversions and one phi3.5-mini conversion, that 
    
[^391]: 有依据且忠实的管道与仪表图推理：利用恢复的证据图约束视觉语言模型

    Grounded and Faithful P&ID Reasoning: Constraining Vision-Language Models with Recovered Evidence Graphs

    [https://arxiv.org/abs/2609.05880](https://arxiv.org/abs/2609.05880)

    本文提出从P&ID图纸中恢复显式证据图，并约束视觉语言模型仅通过七个只读算子查询该图作答、使拓扑结论必须引用支持的查询结果，从而将TopoPID-VQA上的精确匹配准确率从约36.7–41.3%大幅提升至约74%。

    

    管道与仪表图（P&ID）是过程工厂的权威地图：隔离、维护和HAZOP决策都依赖于“什么连接到什么”。视觉语言模型能够流畅地描述这些图纸，但它们经常虚构或遗漏工艺连接——而一个被虚构或遗漏的连接可能颠覆隔离或可达性判断，因此工厂决策不能信任从未对照图纸线条核查过的流畅回答。我们转而恢复图纸的显式图结构——包括其符号、符号之间的工艺连接以及命名它们的标签——然后要求模型仅通过七个只读算子查询该图来作答，从而保证只有当拓扑断言引用了支持它的查询结果时才会被返回。在TopoPID-VQA（一个针对这些图纸、包含3000个拓扑问题的新测试集）上，图接地框架（Graph-Grounded Harness，本文方法）将精确匹配准确率从仅图像提示下的36.7–41.3%提升到74%（原文截断于此）。

    arXiv:2609.05880v1 Announce Type: cross  Abstract: Piping and Instrumentation Diagrams (P&IDs) are the authoritative maps of process plants: isolation, maintenance, and HAZOP decisions depend on what connects to what. Vision-language models describe these sheets fluently, yet they often invent or miss process connections---and an invented or missed link can reverse an isolation or reachability call, so a plant decision cannot trust a fluent answer that was never checked against the linework. We instead recover an explicit graph of the drawing---its symbols, the process connections between them, and the tags that name them---and then require the model to answer only by querying that graph through seven read-only operators, so a topology claim is returned only when it cites the query results that support it. On TopoPID-VQA, a new suite of 3000 topology questions over these sheets, Graph-Grounded Harness (Ours) raises exact match accuracy from 36.7--41.3% under image-only prompting to 74.
    
[^392]: 机器学习原子间势训练集选择中覆盖度导向与响应导向方法之间随数据预算变化的交叉转变

    A budget-dependent crossover between coverage- and response-based training-set selection for machine-learned interatomic potentials

    [https://arxiv.org/abs/2609.05877](https://arxiv.org/abs/2609.05877)

    本研究揭示了机器学习原子间势训练集选择中，覆盖度导向与响应导向（基于模型分歧）两种策略的优劣会随数据保留预算的变化而发生交叉转变，表明单一训练集规模下的比较不足以确定最优选择策略。

    

    为机器学习原子间势选择紧凑训练集时，需要决定是保留结构的多样性，还是针对模型之间存在分歧的构型。哪种选择更优可能取决于保留多少数据，因此仅在单一训练集规模下进行比较是不充分的。本研究通过对GAP-20 Carbon和合并修订版MD17数据集上重新训练的MACE模型进行按数据预算分辨的比较，将选择标准与预测精度联系起来。研究者将结构覆盖度方法与一种响应导向选择器进行对比，后者针对覆盖度训练模型与全数据参考模型之间的分歧。这种回溯性响应见证方法检验了模型分歧对于压缩已标记数据池的价值。在5%数据保留率下，在两个数据集的四个力端点上，覆盖度方法相比随机采样与全数据误差的绝对偏差更小。见证方法在1%和5%时比覆盖度方法偏差更大，但排序关系……（摘要在此处截断）

    arXiv:2609.05877v1 Announce Type: new  Abstract: Selecting compact training sets for machine-learned interatomic potentials requires deciding whether to preserve structural diversity or target configurations on which models disagree. The better choice can depend on how much data is retained, making a comparison at one training-set size insufficient. Here we link selection criteria to prediction accuracy through a budget-resolved comparison of retrained MACE models on GAP-20 Carbon and pooled revised MD17. Structural coverage is compared with a response-guided selector that targets disagreement between a coverage-trained model and a full-data reference. This retrospective response witness tests the value of model disagreement for compressing an already labelled pool. At 5\%, coverage gives smaller absolute deviations from the full-data error than random sampling across four force endpoints in both datasets. The witness has larger deviations than coverage at 1\% and 5\%, but the ordering
    
[^393]: 预算约束下面向任务的动态网络信息获取

    Budgeted Task-Aware Acquisition of Dynamic Networks

    [https://arxiv.org/abs/2609.05862](https://arxiv.org/abs/2609.05862)

    本文提出轻量级框架Scout，能在预算约束下学习查询动态网络中每个节点对下游任务的价值，从而智能地选择性刷新过时的图信息，在绝大多数基准设置中取得最优下游性能。

    

    当底层网络的变化只能被部分观测时，在动态图上进行学习变得十分困难。获取最新的图信息会产生观测成本和计算成本，使得在资源受限的条件下进行完整更新并不现实。本文聚焦于动态网络上预算约束下的任务感知获取问题，即模型需要决定应为下游任务刷新哪些过时的图信息。我们提出了Scout，一个轻量级框架，它能够从所维护的图和观测历史中学习查询每个节点所带来的任务价值。我们的评估涵盖一个合成动态网络和四个真实世界动态网络、两个下游任务、九种获取基线方法以及多种查询预算。Scout在21个基准-预算设置中的19个中取得了最高的平均下游性能。在16个真实世界设置中的13个中，任务效用监督也优于结构变化监督。在同一动态网络上，任务

    arXiv:2609.05862v1 Announce Type: new  Abstract: Learning on dynamic graphs is difficult when changes in the underlying network are only partially observed. Acquiring current graph information incurs observation and computational costs, making complete updates impractical under limited resources. This paper focuses on budgeted task-aware acquisition on dynamic networks, where a model needs to decide which stale graph information to refresh for a downstream task. We propose Scout, a lightweight framework that learns the task value of querying each node from the maintained graph and observation history. Our evaluation covers one synthetic and four real-world dynamic networks, two downstream tasks, nine acquisition baselines, and several query budgets. Scout achieves the highest mean downstream performance in 19 of the 21 benchmark-budget settings. Task-utility supervision also outperforms structural-change supervision in 13 of the 16 real-world settings. On the same dynamic network, task
    
[^394]: 使用稳定性感知约束PPO的AI原生NG-RAN的SLA安全节能控制

    SLA-Safe Energy Control for AI-Native NG-RAN Using Stability-Aware Constrained PPO

    [https://arxiv.org/abs/2609.05861](https://arxiv.org/abs/2609.05861)

    本文提出一种基于稳定性感知约束PPO（带自适应拉格朗日惩罚）的强化学习框架，用于5G NG-RAN的SLA安全节能控制，在保障用户QoS和SLA的前提下动态优化小区节能模式切换。

    

    一个重要的AI赋能无线接入网（AI-for-RAN）应用场景是节能，其中需要对无线资源和小区节能模式进行动态控制，同时不能违反用户服务质量（QoS）或服务等级协议（SLA）的要求。然而，激进的休眠状态或去激活决策可能会以吞吐量下降、延迟增加、SLA违规以及不稳定的模式切换为代价来降低能耗，尤其是在时变和突发的流量条件下。本文提出了一种面向5G NG-RAN中SLA安全节能控制的稳定性感知约束强化学习框架。该问题被建模为一个约束马尔可夫决策过程（CMDP），其中AI原生控制器基于小区负载、队列状态、活跃用户信息、当前节能模式以及SLA相关指标来选择闭环节能动作。所提出的框架采用带自适应拉格朗日惩罚的约束近端策略优化（PPO）……

    arXiv:2609.05861v1 Announce Type: cross  Abstract: One important AI-for-RAN use case is energy saving, in which radio resources and cell energy modes must be dynamically controlled without violating user quality-of-service (QoS) or service-level agreement (SLA) requirements. However, aggressive sleep-state or deactivation decisions may reduce energy consumption at the cost of throughput degradation, delay increase, SLA violations, and unstable mode switching, especially under time-varying and bursty traffic conditions. This paper proposes a stability-aware constrained reinforcement learning framework for SLA-safe energy control in 5G NG-RAN. The problem is formulated as a constrained Markov decision process in which an AI-native controller selects closed-loop energy-saving actions based on cell load, queue status, active-user information, current energy mode, and SLA-related indicators. The proposed framework uses constrained proximal policy optimization with adaptive Lagrangian penalt
    
[^395]: 超越任意几何：神经偏微分方程算子的拓扑泛化

    Beyond Arbitrary Geometry: Topology Generalization In neural PDE Operators

    [https://arxiv.org/abs/2609.05860](https://arxiv.org/abs/2609.05860)

    该论文引入TopoBox-3D基准并通过霍奇热流的受控分析，揭示了神经PDE算子拓扑泛化的主要困难来自谱特性（初始瑞利商是最稳定的误差预测指标）而非调和维度变化，且显式拓扑坐标信息并不总是最优方案。

    

    接受任意网格输入的神经算子通常被视为具有几何通用性，但未见过的域拓扑会同时改变偏微分方程算子的不变子空间和衰减子空间。我们使用霍奇热流作为受控视角来考察这一区别，并引入了TopoBox-3D基准，其中隧道和空腔改变贝蒂数支撑，同时精确的霍奇分解将调和核与正谱分离。在六种架构中，隐式推断拓扑的模型在45个模型-任务拓扑分布外组合中的37个遭受了过度的匹配退化，然而改变调和维度的情形平均而言并未受到更强的惩罚。主要的困难反而是谱方面的：初始瑞利商是误差最稳定的预测指标，而谱展宽为边链和面链增添了信息。最引人注目的是，受控探针实验表明，显式的关联矩阵与调和坐标并不能带来最佳的核一致性准确率……

    arXiv:2609.05860v1 Announce Type: new  Abstract: Neural operators that accept arbitrary meshes are often treated as geometry-general, but unseen domain topology changes both the invariant and decaying subspaces of a PDE operator. We use Hodge heat flow as a controlled lens on this distinction and introduce TopoBox-3D, where tunnels and cavities vary Betti support while the exact Hodge decomposition separates the harmonic kernel from the positive spectrum. Across six architectures, models that infer topology implicitly suffer excess matched degradation in 37 of 45 model--task topology-OOD cells, yet cases that change harmonic dimension are not more strongly penalized on average. The dominant difficulty is instead spectral: the initial Rayleigh quotient is the most stable predictor of error, and spectral broadening adds information for edge and face cochains. Most strikingly, controlled probes show that explicit incidence and harmonic coordinates do not yield the best kernel-identity acc
    
[^396]: 面向前向校正分类的选择性后验边际正则化

    Selective Posterior Margin Regularization for Forward-Corrected Classification

    [https://arxiv.org/abs/2609.05859](https://arxiv.org/abs/2609.05859)

    提出选择性后验边际正则化（SPMR），在保留前向校正目标的基础上，将反向后验主导类别与标注的不一致转化为按后验间隔分级缩放的成对边际更新，从而有效抑制网络对噪声标签的记忆。

    

    在类条件标签噪声下进行学习通常依赖于一个从潜在干净类别到观测标注的转移模型。前向校正将这种转移嵌入到似然中，然而有限样本的网络仍然可能记忆损坏的标签。校正后的似然还会诱导出一个关于干净类别的反向后验，该后验可以解释每个标注。当其主导类别与标注不一致时，模型和转移矩阵提供了反对该标注的证据，但主导的备选类别可能仍然几乎持平。我们提出了选择性后验边际正则化（SPMR），该方法保留前向目标函数，并将这种不一致转化为对干净分类器的分级更新。SPMR选择主导的反向后验类别，利用两个主导后验类别之间的间隔来缩放一个分离的成对边际，并对模糊冲突赋予相应较小的影响力。该差距...

    arXiv:2609.05859v1 Announce Type: new  Abstract: Learning with class-conditional label noise often relies on a transition model from latent clean classes to observed annotations. Forward correction embeds this transition in the likelihood, yet finite-sample networks may still memorize corrupted labels. The corrected likelihood also induces a reverse posterior over the clean classes that could explain each annotation. When its leading class differs from the annotation, the model and transition matrix provide evidence against that annotation, but the leading alternatives can remain nearly tied. We introduce Selective Posterior Margin Regularization (SPMR), which preserves the Forward objective and converts this disagreement into a graded update on the clean classifier. SPMR selects the leading reverse-posterior class, scales a detached pairwise margin by the separation between the two leading posterior classes, and assigns correspondingly little influence to diffuse conflicts. The gap fa
    
[^397]: SAFEGuard：通过有害语义分析和流畅度测量检测基于优化的越狱攻击

    SAFEGuard: Detect Optimization-Based Jailbreak Attacks Through Harmful Semantic Analysis and Fluency Measurement

    [https://arxiv.org/abs/2609.05850](https://arxiv.org/abs/2609.05850)

    提出统一检测框架SAFEGuard，结合基于跨层分布距离与困惑度的混合流畅度测量以及基于梯度匹配的有害语义分析，有效检测针对LLM的基于优化的越狱攻击。

    

    尽管人们在将大型语言模型（LLM）与人类价值观对齐并确保其安全部署方面付出了巨大努力，但最近的研究表明，LLM仍然容易受到对抗性越狱攻击，这些攻击能够绕过安全防护机制并引发有害响应。目前已有许多防御方法被提出来检测越狱攻击，但它们在应对广泛的基于优化的越狱机制方面效果有限，这类机制可以生成高度流畅优化或有害语义混淆的提示。为了应对这一挑战，我们提出了一个统一的检测框架SAFEGuard，它结合了基于跨层分布距离和困惑度的混合流畅度测量方法，以及通过梯度匹配进行的有害语义分析。我们的方法基于一个至关重要的观察：高流畅度的提示其恶意意图与有害提示保持接近，而有害语义混淆的提示通常……

    arXiv:2609.05850v1 Announce Type: cross  Abstract: Despite the significant efforts devoted to aligning large language models (LLMs) with human values and ensuring safe deployment, recent work has revealed that LLMs remain vulnerable to adversarial jailbreak attacks that can bypass safety guardrails and elicit harmful responses. Many defense methods are proposed to detect jailbreaks but they are limited in their effectiveness to counter wide-range optimization-based jailbreak mechanisms that can yield highly fluency-optimized or harmful semantic obfuscated prompts. To tackle this challenge, we propose a unified detection framework SAFEGuard which incorporates a hybrid fluency measurement based on cross-layer distribution distance and perplexity, and the analysis of harmful semantics through gradient matching. Our method is grounded in a paramount observation: high fluency prompts maintain their malicious intention close to harmful prompts while harmful semantic obfuscated prompts often 
    
[^398]: 函数型注意力可解释回归

    Functional Attentive Interpretable Regression

    [https://arxiv.org/abs/2609.05846](https://arxiv.org/abs/2609.05846)

    提出FAIR方法，通过坐标特征与自注意力机制直接学习函数对函数回归中系数面的复杂支撑结构，实现局部与全局尺度的自适应信息共享，并在稀疏与平滑约束下获得边界连贯的局部化估计。

    

    在函数对函数回归中，系数面 β(s,t) 可能表现出复杂的支撑结构——从局部斑块到诸如不连通区域、带状或环状等全局模式——此时效应相似性与欧氏邻近性并不一致。依赖固定基展开的投影方法可能会掩盖这种结构，而直接平滑方法则存在过度平滑曲面及其边界的风险。我们提出了函数型注意力可解释回归（FAIR），该方法通过坐标特征直接表示 β(s,t)，并利用自注意力机制学习效应自适应的邻域，从而实现局部和全局尺度上的信息共享。一个标量压缩网络将这些学习到的表示映射到系数面上。在这些邻域上施加的稀疏性与平滑性惩罚促进了具有连贯边界的局部化支撑。我们建立了筛等价理论以...

    arXiv:2609.05846v1 Announce Type: cross  Abstract: In function-on-function regression, the coefficient surface $\beta(s,t)$ may exhibit complex support structure---from localized patches to global patterns such as disconnected regions, bands, or rings---where effect similarity does not align with Euclidean proximity. Projection-based methods that rely on fixed basis expansions can obscure such structure, while direct smoothing approaches risk oversmoothing the surface and its boundaries. We propose Functional Attentive Interpretable Regression (FAIR), which represents $\beta(s,t)$ directly through coordinate features and uses self-attention to learn effect-adaptive neighborhoods, enabling information sharing at both local and global scales. A scalar compression network maps these learned representations to the coefficient surface. Sparsity and smoothness penalties applied over these neighborhoods promote localized support with coherent boundaries. We establish a sieve equivalence to te
    
[^399]: 量子AI会在路径中做梦吗？通过Grover干涉实现路径积分式慢思考

    Do Quantum AIs Dream in Paths? Path-Integral Slow Thinking through Grover Interference

    [https://arxiv.org/abs/2609.05842](https://arxiv.org/abs/2609.05842)

    该论文提出将“慢思考”建模为推理轨迹上的量子路径积分，借助Grover振幅放大使接受与拒绝轨迹发生干涉，使最优成功概率收敛于一个内部解析值，从而避免传统强化学习中的策略坍缩问题。

    

    带有可验证奖励的强化学习使大型语言模型能够进行“慢思考”，但同样的训练可能引发策略坍缩：概率集中到少数成功轨迹上，探索性多样性逐渐丧失。我们探讨量子AI能否以不同的方式实现慢思考。我们将慢思考表述为推理轨迹上的相干动力学，即一个离散路径积分，其中动作序列以叠加态共存，并在测量前重新组合。在我们的可训练实现中，一个精确验证器将系综划分为集体接受与集体拒绝两个部分，它们在Grover振幅放大下发生干涉。当放大前的成功概率处于一个解析确定的、小于1的内部值时，有限的Grover演化被最大化，因此推理过程本身定义了一个内部训练目标，消除了向单位成功概率单调推进的压力。在精确态矢量模拟中……（摘要在此处截断）

    arXiv:2609.05842v1 Announce Type: cross  Abstract: Reinforcement learning with verifiable rewards enables large language models to think slowly, but the same training can induce policy collapse: probability concentrates onto a few successful trajectories and exploratory diversity erodes. We ask whether quantum AI can realize slow thinking differently. We formulate slow thinking as coherent dynamics over reasoning trajectories, a discrete path integral in which action sequences coexist in superposition and recombine before measurement. In our trainable realization, an exact verifier partitions the ensemble into collective accepted and rejected components that interfere under Grover amplitude amplification. A finite Grover evolution is maximized when the pre-amplification success probability lies at an analytically determined value below one, so inference itself defines an interior training target and removes the monotonic pressure toward unit success. In exact statevector simulations of
    
[^400]: 通过自适应特征与样本缩减实现最优分类树的可扩展求解

    Scaling Optimal Classification Trees via Adaptive Feature and Sample Reduction

    [https://arxiv.org/abs/2609.05826](https://arxiv.org/abs/2609.05826)

    该论文提出基于STreeD的加权与自适应联合特征-样本缩减框架，通过合并重复样本记录并迭代细化候选特征集，在保证候选集内最优性的前提下将最优分类树的求解速度提升最高121倍以上。

    

    arXiv:2609.05826v1 公告类型：新论文 摘要：随着特征数量和训练样本数量的增加，用于求解最优分类树的动态规划方法的计算成本变得极为高昂。我们基于STreeD开发了一个联合特征空间与样本空间缩减框架。加权STreeD将在投影到固定候选特征集后产生的重复记录合并为带权重的代表记录，从而在不改变固定候选集优化问题的前提下减少依赖样本的计算量。自适应STreeD则反复细化一个有界的候选特征集，保留当前最优树所使用的特征，重建加权表示，并求解由此得到的缩减问题。每一个经过验证的加权STreeD解对于其当前候选集而言都是最优的，而外层的特征搜索在整个特征空间上则保持启发式策略。在五个数据集上的实验表明，加权STreeD相比标准STreeD实现了高达121.41倍的加速。自适应STreeD降低了运行时间……

    arXiv:2609.05826v1 Announce Type: new  Abstract: Dynamic programming for optimal classification trees becomes computationally expensive as the numbers of features and training samples increase. We develop a joint feature- and sample-space reduction framework based on STreeD. Weighted STreeD merges duplicate records created after projection onto a fixed candidate set into weighted representatives. This reduces sample-dependent computation without changing the fixed-candidate optimization problem. Adaptive STreeD repeatedly refines a bounded candidate set, retains features used by the incumbent tree, rebuilds the weighted representation, and solves the resulting reduced problems. Each certified Weighted STreeD solution is optimal for its current candidate set, while the outer feature search remains heuristic over the full feature space. Experiments on five data sets show that Weighted STreeD achieves speedups of up to 121.41 times over standard STreeD. Adaptive STreeD reduces runtime in 
    
[^401]: 基于域随机化强化学习的HVAC控制泛化方法

    Generalizing HVAC Control With Domain Randomized Reinforcement Learning

    [https://arxiv.org/abs/2609.05822](https://arxiv.org/abs/2609.05822)

    提出了基于物理信息归一化流的自适应域随机化强化学习控制器NOMAD-RL，无需精确建筑模型或现场调参，即可通过通用非侵入式恒温器接口在异构热区之间实现HVAC控制的泛化迁移。

    

    大规模部署先进的HVAC（供暖、通风与空调）控制器仍然十分困难，因为其性能往往依赖于精确的建筑模型或针对每个现场进行的重新调参。我们提出了NOMAD-RL（Neural Online Meta-Adaptation for Dynamics，面向动力学的神经在线元自适应），这是一个通用的强化学习（RL）控制器，旨在通过一个通用的、非侵入式的恒温器接口，在异构的热区之间实现迁移。该控制器基于区域温度测量值和预测值对温度设定点进行操作，同时其循环策略支持在部分可观测条件下的在线自适应。我们的主要贡献是一种基于物理信息归一化流的自适应域随机化方案，该方案在保持物理合理性和可控性的同时，能够对热区参数的相关性和多峰分布进行建模。由此产生了一个逼真且渐进自适应的训练课程，改善了跨建筑的迁移能力（原文摘要到此被截断）。

    arXiv:2609.05822v1 Announce Type: new  Abstract: Deploying advanced HVAC (Heating, Ventilation and Air Conditioning) controllers at scale remains difficult because performance often depends on accurate building models or per-site retuning. We propose NOMAD-RL (Neural Online Meta-Adaptation for Dynamics), a general-purpose Reinforcement Learning (RL) controller designed to transfer across heterogeneous thermal zones through a universal, non-invasive thermostat interface. The controller acts on temperature setpoints from zone measurements and forecasts, while a recurrent policy supports online adaptation under partial observability.   Our main contribution is an adaptive domain randomization scheme based on physics-informed normalizing flows, which models correlated and multimodal distributions of thermal-zone parameters while maintaining physical plausibility and controllability. This produces a realistic and progressively adaptive training curriculum that improves transfer across build
    
[^402]: 基于有限反馈的LLM专家在线学习

    Online Learning with LLM Experts from Limited Feedback

    [https://arxiv.org/abs/2609.05820](https://arxiv.org/abs/2609.05820)

    该论文提出了在有限反馈预算下将提示自适应路由到LLM专家的在线学习算法，在完全信息设置和老虎机设置下分别实现了$\tilde{O}(dT/\sqrt{m})$和$\tilde{O}(dT\sqrt{K/m})$的遗憾界，能够高效学习高质量的LLM路由策略。

    

    我们研究在有限反馈的在线环境中，如何自适应地将提示路由到大语言模型（LLM）专家以最大化响应质量。我们将其表述为一个老虎机问题，其中$K$个动作代表专家，$d$个特征对提示进行编码，时间范围为$T$轮。我们提出了策略性地选择和观察奖励以最小化遗憾的算法。在完全信息设置下，我们实现了$\tilde{O}(dT/\sqrt{m})$的遗憾，而在老虎机设置下我们实现了$\tilde{O}(dT\sqrt{K/m})$的遗憾，其中$m \ll T$是反馈预算。我们的实验表明，我们能够从有限反馈中高效地学习跨多样化LLM的高质量路由策略。

    arXiv:2609.05820v1 Announce Type: new  Abstract: We study adaptive routing of prompts to large language model (LLM) experts to maximize response quality in an online setting with limited feedback. We formulate it as a bandit problem with $K$ actions that represent experts and $d$ features that encode prompts, over a horizon of $T$ rounds. We propose algorithms that strategically select and observe rewards to minimize regret. In the full-information setting, we achieve a regret of $\tilde{O}(d T / \sqrt{m})$, while in the bandit setting we achieve $\tilde{O}(d T \sqrt{K / m})$, where $m \ll T$ is a budget on feedback. Our experiments show that we efficiently learn high-quality routing strategies across diverse LLMs from limited feedback.
    
[^403]: 用于原位随机伴随训练的物理策略梯度定理

    Physical policy gradient theorem for in situ stochastic-adjoint training

    [https://arxiv.org/abs/2609.05808](https://arxiv.org/abs/2609.05808)

    本文提出了策略梯度定理的物理对应形式——一种随机伴随梯度估计器，通过用非简并扩散取代互易性限制，仅凭测量得到的随机轨迹即可直接从物理系统中原位提取参数梯度，并在非线性谐振器网络上成功实现了训练。

    

    原位伴随训练可以直接从测量中提取参数梯度，但迄今为止仅限于互易系统或受限系统。在本文中，我们引入了策略梯度定理的物理对应形式：一种随机伴随梯度估计器，它通过用非简并扩散替代互易性来解除这些限制。作为验证，我们训练了一个非线性谐振器网络——其自身动力学充当策略——仅利用从测量的随机轨迹中获得的梯度来对抗拮抗性时间调制，而无需有限差分或单独的伴随实验。

    arXiv:2609.05808v1 Announce Type: cross  Abstract: In situ adjoint training extracts parameter gradients directly from measurement, but has so far been limited to reciprocal or restricted systems. Here, we introduce the physical counterpart of the policy gradient theorem: a stochastic-adjoint gradient estimator that lifts these constraints by trading reciprocity for nondegenerate diffusion. As validation, we train a nonlinear resonator network, whose own dynamics supply the policy, against antagonistic temporal modulations with gradients from measured stochastic trajectories alone, without finite differences or a separate adjoint experiment.
    
[^404]: 对角衰减：主成分分析（PCA）的有限样本校正

    Diagonal Attenuation: A Finite-Sample Correction for PCA

    [https://arxiv.org/abs/2609.05796](https://arxiv.org/abs/2609.05796)

    提出对角衰减方法，通过降低坐标方向样本方差、保留样本交叉协方差来校正有限样本下PCA的旋转偏差，并给出一致的期望风险显式定理及渐近最优衰减强度。

    

    当协方差矩阵由有限数据估计时，主成分分析（PCA）可能会偏离其总体目标方向。我们引入了对角衰减方法，该方法在保留样本交叉协方差的同时降低坐标方向上的样本方差。该方法通过对随机输入掩码下的线性全输出重构损失取平均而被精确揭示；直接研究这一校正可将其扩展到掩码方法所能达到的范围之外。我们分离出保留与舍弃的总体方向之间随机耦合中由样本方差误差所贡献的部分，并展示衰减如何能够减小由此导致的旋转偏差。在边际方差平衡的条件下，我们推导出一个显式的期望风险定理，该定理对所有足够大的有限样本在整条衰减路径上一致成立，并得到了渐近风险最小化的衰减强度。对于一般协方差，我们刻画了衰减在何种条件下保持总体主成分不变。

    arXiv:2609.05796v1 Announce Type: cross  Abstract: Principal component analysis (PCA) can rotate away from its population target when a covariance matrix is estimated from limited data. We introduce diagonal attenuation, which preserves sample cross-covariances while reducing coordinatewise sample variances. The method is revealed exactly by averaging a linear full-output reconstruction loss over random input masks; studying the correction directly extends it beyond the range attainable by masking. We isolate the part of the random coupling between retained and omitted population directions that is contributed by sample-variance errors, and show how attenuation can reduce the resulting rotation. Under balanced marginal variances, we derive an explicit expected-risk theorem, uniform over the attenuation path for all sufficiently large finite samples, and obtain the asymptotically risk-minimizing strength. For general covariances, we characterize when attenuation leaves the population PC
    
[^405]: 基于参数化Deep Ritz方法的非线性椭圆均匀化

    Nonlinear elliptic homogenization with the parametric Deep Ritz method

    [https://arxiv.org/abs/2609.05778](https://arxiv.org/abs/2609.05778)

    本文提出采用参数化Deep Ritz方法求解非线性椭圆均匀化中的参数化胞元问题，从而获得胞元问题解的可微表示，以支持非线性均匀化本构关系的数值计算。

    

    椭圆均匀化用于确定具有小尺度特征的材料在粗粒化尺度上的性质。当这些小尺度特征具有快速的周期性波动时，基于均匀化本构关系得到的解场与基于非均匀材料的真实解非常接近。材料的这种均匀化行为通过求解胞元问题（cell problem）计算得到，其中一个胞元被定义为波动材料的一个周期。在线性椭圆偏微分方程的背景下，均匀化本构关系仅由一个常系数张量定义；但对于非线性问题，均匀化响应依赖于宏观状态和/或其梯度，因此需要求解参数化的胞元问题。在使用均匀化本构关系计算数值解时，拥有胞元问题解的可微表示是非常有用的。

    arXiv:2609.05778v1 Announce Type: new  Abstract: Elliptic homogenization is used to determine coarse-grained properties of materials with features on small scales. When these small scale features have rapid, periodic fluctuations, the solution field corresponding to a homogenized constitutive relation closely resembles the true solution based on the heterogeneous material. This homogenized behavior of the material is computed from a cell problem, where a cell is defined to be one period of the fluctuating material. In the context of linear elliptic partial differential equations, the homogenized constitutive relation is defined simply by a constant coefficient tensor, but for nonlinear problems, the homogenized response depends on the macroscopic state and/or its gradient, thus requiring solutions to parametric cell problems. When computing a numerical solution with the homogenized constitutive relation, it is useful to have a differentiable representation of the solution to the cell p
    
[^406]: RAPTOR：面向混合专家模型的角色感知隐私训练

    RAPTOR: Role-Aware Private Training for Mixture-of-Experts

    [https://arxiv.org/abs/2609.05770](https://arxiv.org/abs/2609.05770)

    RAPTOR提出了一种角色感知的差分隐私训练框架，通过专家特定的裁剪与噪声、公共期望所有者分母和计数无关的更新计划，解决了现有DP微调方法在稀疏MoE模型上的三种失效模式，同时保持(ε,δ)-DP保证。

    

    差分隐私（DP）微调方法将稀疏的混合专家模型视为单个密集整体，忽略了共享层能看到所有数据、而专家只能看到被路由到自己的记录这一差异。我们识别并正式刻画了由此产生的三种失效模式：全局梯度裁剪会抑制专家梯度、批次级归一化会稀释稀疏的专家更新、固定的隐私噪声会降低低负载专家的信噪比。我们提出了RAPTOR——一个角色感知的隐私训练框架，它交替进行共享层和专家层的优化，并直接针对每种失效模式，采用专家特定的裁剪与噪声，结合一个公共的期望所有者分母以及与专家计数无关的更新计划，从而避免以私有的、实际实现的专家计数为条件。我们证明了所得机制满足$(\varepsilon,\delta)$-差分隐私：由于每条记录被恰好分配给一个所有者专家，层内的各专家机制……

    arXiv:2609.05770v1 Announce Type: new  Abstract: Differentially private (DP) fine-tuning methods treat sparse Mixture-of-Experts (MoE) models as a single dense block, ignoring that shared layers see all data while experts only see routed records. We identify and formally characterize three resulting failure modes: global clipping suppresses expert gradients, batch-level normalization dilutes sparse expert updates, and fixed privacy noise degrades signal-to-noise ratio on low-load experts. We introduce RAPTOR - a Role-Aware Private Training framework, which alternates shared and expert optimization and targets each failure directly, using expert-specific clipping and noise together with a public expected-owner denominator and a count-independent update schedule that avoids conditioning on private, realized expert counts. We prove the resulting mechanism satisfies $(\varepsilon,\delta)$-DP: because each record is assigned to exactly one owner expert, per-expert mechanisms within a layer 
    
[^407]: Data Scout：面向领域特定预训练语料库的定向网络爬取

    Data Scout: Targeted Web Crawling for Domain-Specific Pretraining Corpora

    [https://arxiv.org/abs/2609.05766](https://arxiv.org/abs/2609.05766)

    Data Scout提出用“定向爬取”取代传统的“过滤存档”范式，利用LLM将主题扩展为分类体系和搜索查询，并借助子域名级分类器探针进行筛选，使爬取页面中高质量数学内容占比达到21.9%，是传统过滤方法的70倍。

    

    arXiv:2609.05766v1 公告类型：交叉  摘要：构建领域特定预训练语料库的主流方法是对大型网络存档（如CommonCrawl）进行过滤。这种方法对热门领域效果良好，但对于专业领域则会失效，因为专业领域的相关内容稀疏，且往往超出了基于流行度的爬虫的覆盖范围。我们提出了Data Scout，它颠覆了这一模式：不是过滤存档，而是引导定向爬取。一个LLM将根主题扩展为分类体系及数千个搜索查询；返回的URL（种子）按子域名分组，并用用户提供的分类器（探针）进行筛选，基于小样本决定是否接纳每个子域名。这种方法有效的原因在于相关性在子域名层面存在清晰的边界：在数学领域，一个页面比其兄弟子域名上的页面相关性高21倍。以FineMath分类器作为探针，爬取的页面中有21.9%是高质量数学内容，是从可比网络存档中过滤所得0.31%比率的70倍。

    arXiv:2609.05766v1 Announce Type: cross  Abstract: The dominant approach to building domain-specific pretraining corpora is to filter large web archives such as CommonCrawl. This works well for popular domains but breaks down for specialized ones, where relevant content is sparse and often beyond the reach of popularity-driven crawlers. We present Data Scout, which inverts this: instead of filtering an archive, it directs a targeted crawl. An LLM expands a root topic into a taxonomy and thousands of search queries; the returned URLs (seeds) are grouped by subdomain and screened with a user-supplied classifier (the probe), admitting each subdomain on the basis of a small sample. This works because relevance has a sharp boundary at the subdomain level: in mathematics, a page is 21x more likely to be relevant than one on a sibling subdomain. With the FineMath classifier as the probe, 21.9% of crawled pages are high-quality math content, 70x the 0.31% rate from filtering a comparable web s
    
[^408]: AAMBERS-UAV：面向无人机杂草稻分割的采集感知多模态骨干网络评估与排序

    AAMBERS-UAV: Acquisition-Aware Multimodal Backbone Evaluation and Ranking for UAV Weedy Rice Segmentation

    [https://arxiv.org/abs/2609.05762](https://arxiv.org/abs/2609.05762)

    该研究提出采集感知的评估框架AAMBERS-UAV，揭示了数据集划分方式对无人机杂草稻分割中多模态输入排名的关键影响——完全采集留存时RGB表现最佳，而目标采集暴露后RGB+多光谱最优，且这一结论在U-Net/ResNet18上得到复现验证。

    

    无人机图像采集包含空间和时间上相关的帧，然而语义分割基准通常在图像层面进行数据集划分。这种划分方式可能将来自同一次采集的样本同时放入模型开发和测试集中，从而掩盖了模型向真正新调查场景迁移的能力。利用包含734个样本的WeedyRice-RGBMS-DB数据集，我们固定了一个包含124张图像的目标采集测试集，并在训练、验证和测试数量完全相同的条件下比较两种协议：目标留存协议将目标采集从模型开发中完全排除，目标暴露协议则纳入目标采集的其余图像。研究在两个固定划分种子上评估了SegFormer-B0使用RGB、四波段多光谱（MS）以及七通道RGB+MS输入的表现。结果表明，在完全采集留存条件下RGB表现最强（0.7317±0.0201 IoU），而在目标暴露后RGB+MS变为最强（0.7822±0.0269）。固定划分的U-Net/ResNet18复现实验证实了所有三种输入在暴露条件下均获得正向增益，

    arXiv:2609.05762v1 Announce Type: cross  Abstract: UAV image collections contain spatially and temporally related frames, yet semantic-segmentation benchmarks commonly split them at image level. Such splitting can place samples from one acquisition in both model development and testing, obscuring transfer to a genuinely new survey. Using the 734-sample WeedyRice-RGBMS-DB, we fix a 124-image target-acquisition test set and compare two protocols with identical train, validation, and test counts: target-held-out, which excludes the target acquisition from development, and target-exposed, which admits its remaining images. SegFormer-B0 is evaluated with RGB, four-band multispectral (MS), and seven-channel RGB+MS input over two fixed-split seeds. RGB is strongest under complete acquisition holdout ($0.7317\pm0.0201$ IoU), whereas RGB+MS becomes strongest after target exposure ($0.7822\pm0.0269$). A fixed-split U-Net/ResNet18 replication confirms positive exposure gains for all three inputs,
    
[^409]: 面向替代性度假租赁房源推荐的候选生成多源集成方法

    A Multi-Source Ensemble Approach to Candidate Generation for Alternative Vacation Rental Property Recommendations

    [https://arxiv.org/abs/2609.05748](https://arxiv.org/abs/2609.05748)

    该论文提出一种将基于物品的协同过滤与图神经网络检索相结合的多源混合候选生成架构，在拥有超过200万房源的度假租赁平台上将替代房源推荐的Recall@300提升了14.8%。

    

    替代性房源推荐在度假租赁市场中起着至关重要的作用，能够帮助用户在浏览某个具体房源时发现相关的其他选项。然而，生成高质量的候选替代房源面临着独特的挑战：库存的异构性、地理限制、可用性的快速变化以及房源分布的长尾特性。本文对度假租赁替代房源的候选生成方法进行了全面研究，比较了协同过滤、浅层嵌入以及图神经网络方法。我们在一个大型度假租赁平台（拥有超过200万个活跃房源）上的实验表明，将基于物品的协同过滤与基于图神经网络的检索相结合的混合架构，通过利用两种来源的互补优势，使Recall@300相比最强基线提升了14.8%：协同过滤在召回具有丰富交互信息的房源方面表现出色……

    arXiv:2609.05748v1 Announce Type: new  Abstract: Alternative property recommendations play a critical role in vacation rental marketplaces, helping users discover relevant options when viewing a specific listing. However, generating high-quality candidate alternatives presents unique challenges: heterogeneous inventory, geographic constraints, rapid availability changes, and long-tail property distributions. We present a comprehensive study of candidate generation (CG) approaches for vacation rental alternatives, comparing collaborative filtering, shallow embeddings, and graph neural network (GNN) methods.   Our experiments on a large-scale vacation rental platform (over 2M active properties) show that a hybrid architecture combining item-based collaborative filtering with GNN-based retrieval improves Recall@300 by 14.8% over the strongest baseline, by leveraging the complementary strengths of the two sources: collaborative filtering excels at early recall for properties with rich inte
    
[^410]: 面向海量点云数据的高斯线性泛函流形方法

    Gaussian Linear Functional Manifold Method for Massive Point Cloud Data

    [https://arxiv.org/abs/2609.05744](https://arxiv.org/abs/2609.05744)

    提出高斯线性泛函流形（GLFM）物理信息统计框架，结合SVD降秩算法实现线性时间参数估计，解决了复杂城乡交错带环境中海量机载LiDAR点云的连续地形重建难题，兼具结构可解释性与无需逐点标注的优势。

    

    从海量、非结构化的机载激光雷达（LiDAR）点云中重建连续地形流形，在复杂的城乡交错带（WUI）环境中仍然具有挑战性，因为深度神经网络需要代价高昂的逐点标注，而非参数曲面重建方法往往缺乏结构可解释性。本文提出了高斯线性泛函流形（GLFM），这是一种基于物理信息的统计框架，利用确定性线性泛函基表示连续的地形表面，同时将微观尺度的激光漫反射后向散射建模为各向同性高斯过程。为了避免精确约束极大似然估计的二次方计算成本，我们开发了一种代数奇异值分解（SVD）降秩算法，实现了线性时间的参数估计和闭式二次曲面分类。在35.2平方公里的真实机载激光雷达数据上的评估表明，GLFM能够自动……

    arXiv:2609.05744v1 Announce Type: cross  Abstract: Reconstructing continuous terrain manifolds from massive, unstructured airborne LiDAR point clouds remains challenging in complex Wildland-Urban Interface (WUI) environments, where deep neural networks require costly point-wise annotations and nonparametric surface reconstruction methods often lack structural interpretability. This paper introduces the Gaussian Linear Functional Manifold (GLFM), a physics-informed statistical framework that represents continuous surface topography using deterministic linear functional bases while modeling microscale diffuse laser backscatter as an isotropic Gaussian process. To avoid the quadratic computational cost of exact constrained maximum likelihood estimation, we develop an algebraic singular value decomposition (SVD) rank-reduction algorithm that enables linear-time parameter estimation and closed-form quadric classification. Evaluated on 35.2 km^2 of real-world aerial LiDAR data, GLFM automati
    
[^411]: RenderFormer-V2：基于异构场景基元的神经渲染

    RenderFormer-V2: Neural Rendering with Heterogeneous Scene Primitives

    [https://arxiv.org/abs/2609.05738](https://arxiv.org/abs/2609.05738)

    RenderFormer-V2是一个无需针对每个场景训练的统一Transformer神经渲染模型，通过在视角无关阶段引入窗口注意力与渲染信息注意力池相结合的新机制，在保持渲染精度的同时提升可扩展性，并能处理焦散、体积散射、环境光照等多样化全局光传输效果。

    

    我们提出了"RenderFormer-V2"，这是一个统一的、基于Transformer的神经渲染学习模型，与现代基于物理的渲染系统互补，能够处理多种光传输效果，如焦散、体积散射、环境光照、带纹理和位移的表面以及分布外材质，而无需针对每个场景进行训练或专门的代码。RenderFormer-V2将全局光传输建模为序列到序列的转换。与其前身一样，RenderFormer-V2也采用两阶段流程：一个与视角无关的阶段，用于解析场景内基元到基元之间的传输；一个与视角相关的阶段，将内部神经场景表示转换为图像像素。与RenderFormer不同，我们的模型在与视角无关的阶段中采用了新颖的窗口注意力与渲染信息注意力池相结合的机制，在保持渲染精度的同时提升了可扩展性。

    arXiv:2609.05738v1 Announce Type: cross  Abstract: We present 'RenderFormer-V2', a unified learned transformer-based neural rendering model, complementary to modern physics-based rendering systems, that can handle diverse light-transport effects such as caustics, volumetric scattering, environment lighting, textured and displaced surfaces and out-of-distribution materials without per-scene training or specialized code. RenderFormer-V2 models global light transport as a sequence-to-sequence transformation. Following its predecessor, RenderFormer-V2 also employs a two stage process: a view-independent stage that resolves intra-scene primitive to primitive transport, and a view-dependent stage that transforms the internal neural scene representation into image pixels. Different from RenderFormer, our model employs a novel combined windowed-attention and rendering-informed attention sink in the view-independent stage to improve scalability while maintaining render accuracy. To further impr
    
[^412]: 面向生成式建模的牛顿匹配：微调与采样的统一框架

    Newton Matching for Generative Modeling: A Unified Framework for Fine-Tuning and Sampling

    [https://arxiv.org/abs/2609.05727](https://arxiv.org/abs/2609.05727)

    提出牛顿匹配统一框架，通过证明规范模型流形上反向KL散度的Hessian矩阵恰好等于Fisher-Rao度量，使牛顿方向与负Fisher-Rao梯度方向重合，从而统一了生成式建模中的微调与采样任务。

    

    我们提出了牛顿匹配，这是一个用于生成式建模中微调与采样的统一框架。其目标分布为 π ∝ μ e^{τr}，其中 r 为奖励函数，τ>0 为逆温度，μ 在微调场景下表示预训练模型的终端密度，在采样场景下则为常数 1。我们将研究范式从孤立的损失函数转向对规范模型的迭代优化：即终端密度的标准条件匹配的总体最小化器。在相容的光滑实现假设下，规范速度构成一个与密度流形微分同胚的流形。通过将 Fisher-Rao 度量与混合联络传输到该流形上，我们证明反向 KL 散度的 Hessian 矩阵等于该度量，因此牛顿方向与负 Fisher-Rao 梯度方向重合。在终端密度 ρ 处，每个阶段采取由正则化奖励 r - (1/τ)log(ρ/μ) 所生成的切向步骤，随后……（摘要在此处截断）

    arXiv:2609.05727v1 Announce Type: cross  Abstract: We develop Newton Matching, a unified framework for fine-tuning and sampling in generative modeling. The target is $\pi\propto\mu e^{\tau r}$, where $r$ is the reward, $\tau>0$ the inverse temperature, and $\mu$ denotes the pretrained model's terminal density for fine-tuning or the constant $1$ for sampling. We shift the paradigm from isolated losses to iterative optimization over canonical models: population minimizers of standard conditional matching for terminal densities. Under compatible smooth-realization assumptions, canonical velocities form a manifold diffeomorphic to the density manifold. Transporting the Fisher-Rao metric and mixture connection to this manifold, we show that the reverse-KL Hessian equals the metric, so the Newton direction coincides with the negative Fisher-Rao gradient. At terminal density $\rho$, each stage takes a tangential step generated by the regularized reward $r-\frac1\tau\log(\rho/\mu)$, followed b
    
[^413]: 有限纠缠条件下量子态层析的紧致下界

    Tight Lower Bounds for State Tomography with Limited Entanglement

    [https://arxiv.org/abs/2609.05718](https://arxiv.org/abs/2609.05718)

    本文证明了在每次测量最多使用 $k$ 个新鲜副本且无量子存储的受限纠缠设定下，$d$ 维量子态层析的副本复杂度下界为 $\max\{d^3/(\sqrt{k}\epsilon^2), d^2/\epsilon^2\}$，与已知上界紧致匹配，并适用于任意块内测量和块间自适应策略。

    

    我们研究量子态层析问题，其中每次测量最多作用于 $k$ 个新鲜副本，且各测量块之间不保留量子存储器。我们证明了一个与 [arXiv:2510.07788] 中上界相匹配的下界。因此，对于每个 $k$ 以及所有足够小的 $\epsilon$，在迹距离 $\epsilon$ 下估计任意 $d$ 维量子态的副本复杂度（在绝对常数因子范围内）为 $\max\{d^3/(\sqrt{k}\epsilon^2), d^2/\epsilon^2\}$。这消除了早期要求 $k$ 必须随精度参数保持较小的限制。该下界适用于每个块内的任意测量以及块之间的自适应选择。该下界在最小本征值数量级为 $1/d$ 的任意量子态的小邻域内即已成立，即使该中心态是已知的。证明的核心是一个仅依赖于量子态最小本征值的单个测量块的一致费希尔信息界。该证明避免了之前的...

    arXiv:2609.05718v1 Announce Type: cross  Abstract: We study state tomography when each measurement acts on at most $k$ fresh copies and no quantum memory is retained between blocks. We prove a lower bound matching the upper bound in [arXiv:2510.07788]. Thus the copy complexity of estimating an arbitrary $d$-dimensional state to trace distance $\epsilon$ is, up to absolute constant factors, $\max\{d^3/(\sqrt{k}\epsilon^2),d^2/\epsilon^2\}$ for every $k$ and all sufficiently small $\epsilon$. This removes the earlier restriction that $k$ be small as a function of the accuracy. The lower bound applies to arbitrary measurements within each block and adaptive choices between blocks. The lower bound already applies in a small neighborhood of any state whose smallest eigenvalue is of order $1/d$, even when the center is known.   The main ingredient is a uniform Fisher information bound for one measurement block that depends only on the smallest eigenvalue of the state. The proof avoids the pe
    
[^414]: CUSP：面向多智能体多模态推理的可分解集体不确定性

    CUSP: Decomposable Collective Uncertainty for Multi-Agent Multimodal Reasoning

    [https://arxiv.org/abs/2609.05708](https://arxiv.org/abs/2609.05708)

    提出了无需训练的CUSP框架，通过语义意见池化量化多智能体多模态推理的系统级不确定性，并能将集体熵精确分解为模型个体语义熵均值与模型间冲突（JSD）两个可解释的信号。

    

    聚合异构视觉语言模型（VLMs）可以提升多模态推理能力，但无论是单个模型的置信度还是聚合答案的置信度，都无法衡量系统层面的可靠性。我们提出了CUSP（通过语义意见池化实现的集体不确定性），这是一个无需训练的不确定性量化框架，它将多个VLM的响应映射到共享的语义响应空间，将其池化为一个池化语义意见，并报告两个互补的系统级信号：集体不确定性，即池化意见的离散程度；以及Jensen-Shannon散度（JSD），即模型级意见之间的冲突程度。在该池化语义意见中，未归一化的集体熵可以精确分解为各个模型个体语义熵的均值与JSD之和，从而将总离散度与模型间冲突分离开来。CUSP既不需要token logits也不需要校准标签，因此适用于开源权重和商业模型（原文在此处截断）。

    arXiv:2609.05708v1 Announce Type: new  Abstract: Aggregating heterogeneous vision-language models (VLMs) can improve multimodal reasoning, but neither an individual model's confidence nor that of the aggregated answer measures reliability at the system level. We present CUSP (Collective Uncertainty through Semantic Opinion Pooling), a training-free uncertainty quantification framework that maps multiple VLM responses to a shared semantic response space, pools them into a pooled semantic opinion, and reports two complementary system-level signals: collective uncertainty, the dispersion of the pooled opinion, and Jensen-Shannon divergence (JSD), the conflict among the model-level opinions. Within this pooled semantic opinion, the unnormalized collective entropy decomposes exactly into the mean of the models' individual semantic entropies and the JSD, separating total dispersion from model conflict. Requiring neither token logits nor calibration labels, CUSP applies to open-weight and com
    
[^415]: 新兴量子原生访问下量子机器学习隐私风险的刻画

    Characterizing Privacy Risks of Quantum Machine Learning with Emergent Quantum-Native Access

    [https://arxiv.org/abs/2609.05702](https://arxiv.org/abs/2609.05702)

    本文突破了以往假设攻击者仅有经典计算能力的局限，首次系统刻画了在用户/攻击者具备量子计算能力和量子信息访问权限的量子原生场景下，量子机器学习模型（如成员推断攻击）所面临的隐私风险。

    

    量子机器学习（QML）通过利用量子计算执行机器学习任务而取得了快速进展。与此同时，伴随QML产生的隐私风险也开始受到研究关注，这些风险既继承了“经典”机器学习的隐私泄露渠道，也包含量子独有的风险。现有的隐私保护QML研究主要聚焦于QML即服务（QML-as-a-service）场景，该场景通常假设QML模型所有者仅向查询提供经典比特输出，而用户（及攻击者）仅具备经典计算能力。然而，在一个量子原生世界中，具备量子能力的用户/攻击者可能同时拥有量子计算能力并能访问服务提供商量子信息输出，这使得上述假设日益受到挑战。本文旨在通过研究针对QML模型的成员推断攻击来弥合这一差距，证明随着量子访问权限和量子计算能力的提升……（原文摘要在此处截断）

    arXiv:2609.05702v2 Announce Type: cross  Abstract: Quantum Machine Learning (QML) has shown rapid advances by utilizing quantum computing for machine learning tasks. Meanwhile, the privacy risks accompanying QML is also starting to be studied, which inherit privacy leakage channels from "classical" ML and also quantum-unique risks. Existing work on privacy-preserving QML largely focuses on a QML-as-a-service scenario, which generally assumes that the QML model owner provides only classical bit outputs to queries, while users (and adversaries) have only classical computing abilities. However, this view is increasingly challenged in a quantum-native world of quantum-capable users/adversaries, which may have access to both quantum computing abilities and access to quantum information output from service providers.   In this paper, we aim to bridge this gap by examining membership inference attacks against QML models by demonstrating that increasing quantum access and quantum computing abi
    
[^416]: 基于神经网络的呼吸性窦性心律不齐分析

    Analysis of Respiratory Sinus Arrhythmia with Neural Networks

    [https://arxiv.org/abs/2609.05698](https://arxiv.org/abs/2609.05698)

    本文开发了三种深度神经网络架构，利用呼吸性窦性心律不齐现象直接从心电图信号预测呼吸波形以估计呼吸频率，实现了无需人工预处理的稳健可扩展的无创呼吸监测方案。

    

    本文介绍了一种基于神经网络的方法，通过利用呼吸性窦性心律不齐（RSA）现象来分析心电图（ECG）信号以估计呼吸频率。我们的方法采用经过训练的深度学习模型，可直接从ECG输入数据预测呼吸波形。为实现这一目标，我们开发并评估了三种不同的神经网络架构，这些架构能够自动从ECG信号中提取相关特征，无需人工预处理。所提出的方法为无创呼吸监测提供了一种强大且可扩展的解决方案，在医疗保健和可穿戴技术领域具有潜在的应用前景。

    arXiv:2609.05698v1 Announce Type: cross  Abstract: The paper introduces a neural network-based approach for analyzing ECG signals to estimate respiratory rate by leveraging the phe- nomenon of Respiratory Sinus Arrhythmia (RSA). Our method employs a deep learning model trained to predict respiratory waveforms directly from ECG input data. To achieve this, we developed and evaluated three different neural network architectures capable of automatically extract- ing relevant features from ECG signals without the need for manual preprocessing. The proposed approach offers a robust and scalable solu- tion for non-invasive respiratory monitoring, with potential applications in healthcare and wearable technology
    
[^417]: GraphNOSE：嗅觉领域的图Transformer

    GraphNOSE: A Graph Transformer in Olfaction

    [https://arxiv.org/abs/2609.05694](https://arxiv.org/abs/2609.05694)

    GraphNOSE是一个开源图Transformer框架，能从SMILES字符串预测单分子和二元混合物的气味描述符，其参数量仅为标准GNN的六分之一，同时在AUROC指标上平均超越现有方法4.52%。

    

    从分子结构预测嗅觉特性是化学信息学中的一个开放性问题。尽管线性模型可以将分子特征与气味描述符关联起来，但在外推到新的化学骨架、极端分子量或复杂气味混合物时，它们往往表现不佳。为了解决这一问题，我们提出了GraphNOSE，一个开源的图Transformer框架，它可以从简化分子线性输入规范（SMILES）字符串中预测单个分子和二元混合物的多标签气味描述符。通过在基于Transformer的图架构中整合位置编码和结构编码，GraphNOSE以比标准图神经网络（GNN）基线少六倍的参数量实现了强劲的性能，同时在平均ROC曲线下面积（AUROC）上以4.52%的优势（p < 0.01）持续超越线性模型、分子语言模型嵌入、分子指纹和基线GNN。

    arXiv:2609.05694v1 Announce Type: new  Abstract: Predicting olfactory qualities from molecular structure is an open problem in chemoinformatics. Although linear models can link molecular features to odor descriptors, they often fail when extrapolating to novel chemical scaffolds, extreme molecular weights, or complex odor mixtures. To address this, we introduce GraphNOSE, an open-source graph transformer framework that predicts multi-label odor descriptors from simplified molecular-input line-entry system (SMILES) strings for single molecules and binary mixtures. By integrating positional and structural encodings within a transformer-based graph architecture, GraphNOSE achieves strong performance with six times fewer parameters than standard graph neural network (GNN) baseline while consistently outperforming linear models, molecular language model embeddings, molecular fingerprints, and baseline GNNs by an average area under the ROC curve (AUROC) margin of 4.52% (p < 0.01). GraphNOSE 
    
[^418]: 混合线性回归中得分匹配、最大似然与期望最大化的联系

    Connecting Score Matching, Maximum Likelihood, and Expectation-Maximization in Mixed Linear Regression

    [https://arxiv.org/abs/2609.05688](https://arxiv.org/abs/2609.05688)

    该论文在混合线性回归中建立了得分匹配损失与最大似然估计及期望最大化（EM）算子之间的理论联系，证明了基于扩散的估计器能收敛到真实参数并达到最大似然估计的高斯极限。

    

    我们研究了在具有未知混合权重的混合线性回归（MLR）中响应变量的方差保持扩散问题。我们的分析将得分匹配的统计保证与固定扩散噪声水平下的损失几何结构和优化信号分离开来。KL散度将在扩散路径上积分的去噪得分匹配目标与似然函数及终端差异联系起来。在温和的正则性条件和终端调度下，所得估计器收敛于MLR的真实参数，且其缩放误差收敛到最大似然估计器的高斯极限。在固定扩散噪声水平尺度下，我们推导出一个将得分匹配损失与交叉熵和期望最大化（EM）算子联系起来的分解。该分解产生了一个与EM相关的低噪声梯度展开，其中包含潜在方差的额外修正项。在高噪声极限下，我们（摘要在此处截断）……

    arXiv:2609.05688v1 Announce Type: new  Abstract: We study variance-preserving diffusion of the response in mixed linear regression (MLR) with unknown mixing weights. Our analysis separates the statistical guarantees of score matching from the loss geometry and optimization signal at a fixed diffusion noise level. The KL divergence links the denoising score matching objective integrated over the diffusion path with the likelihood and a terminal discrepancy. Under mild regularity conditions and terminal schedule, the resulting estimator converges up to the ground truth parameters of MLR, and its scaled error converges to the Gaussian limit of the maximum-likelihood estimator. At a fixed scale of the diffusion noise level, we derive a decomposition linking the score matching loss to cross-entropy and Expectation-Maximization (EM) operators. This decomposition yields an EM-related low-noise gradient expansion with additional correction terms of latent variance. In the high-noise limit, we 
    
[^419]: PAC-隐私保护自回归生成：将噪声校准至集成分歧程度

    PAC-Private Autoregressive Generation: Calibrating Noise to Ensemble Disagreement

    [https://arxiv.org/abs/2609.05676](https://arxiv.org/abs/2609.05676)

    本文将PAC隐私保护预测从分类任务首次扩展到自回归生成，通过在128个重叠世界（各自训练适配器）上投票，并根据各世界后验加权的分歧程度动态校准噪声，在预测一致时减少噪声，从而在保护隐私的同时提升生成质量。

    

    在私有文本上微调的语言模型通常通过API提供服务，因此隐私泄露是通过生成的输出而非暴露的模型权重发生的。私有预测（Private prediction）正是为了保护这类发布。诸如PMixED之类的方法在每次发布时都会产生隐私成本，且在长序列生成中越来越依赖公共模型。PAC隐私（PAC privacy）则根据可能秘密之间的输出变异性来校准噪声，当预测稳定时添加更少的噪声。据我们所知，PAC隐私保护预测此前尚未从分类任务扩展到自回归生成任务。我们从私有语料库中构建了m=128个相互重叠的“世界”，每条记录恰好出现在m/2个世界中，并在冻结的公共模型之上为每个世界训练一个适配器。实际实现的世界即为秘密。在生成每个词元时，公共模型定义一个候选集合，各世界进行投票，其后验加权分歧程度决定PAC噪声的大小；当所有世界完全一致时则无需（文本在此处截断）。

    arXiv:2609.05676v1 Announce Type: cross  Abstract: Language models adapted on private text are often served through APIs, so privacy leakage occurs through generated outputs rather than exposed weights. Private prediction protects these releases. Methods such as PMixED incur privacy cost at each release and increasingly rely on the public model over long horizons. PAC privacy instead calibrates noise to output variability across possible secrets, adding less noise when predictions are stable. To our knowledge, PAC-private prediction has not previously been extended from classification to autoregressive generation.   We construct $m=128$ overlapping worlds from the private corpus, with each record appearing in exactly $m/2$ worlds, and train one adapter per world over a frozen public model. The realized world is the secret. At each token, the public model defines a candidate set, the worlds vote, and their posterior-weighted disagreement determines the PAC noise; unanimity requires no c
    
[^420]: Srijika：面向九种印度文字的复用OpenType排版的字体重样式化系统

    Srijika: OpenType-Layout-Reusing Font Restyling for Nine Indic Scripts

    [https://arxiv.org/abs/2609.05661](https://arxiv.org/abs/2609.05661)

    Srijika通过复用排版完整模板字体的OpenType布局数据（cmap、GSUB闭包和GPOS）来重新样式化字形轮廓，为九种印度文字生成构造上保证排版一致性的可安装字体，从而解决了合体字等复杂文本形式难以保持一致的生成难题。

    

    我们提出Srijika，一个用于生成九种婆罗米文字可安装OpenType字体的系统，涵盖天城文、泰米尔文、孟加拉文、泰卢固文、卡纳达文、马拉雅拉姆文、古吉拉特文、古尔穆奇文和奥里亚文。Srijika并非从零开始生成字体，而是对排版完整的模板字体的字形轮廓进行重新样式化。它在文档化的度量策略下保留模板的cmap与GSUB闭包以及GPOS数据，使得每个输出在构造上都必然是完整的字体。这解决了印度文字字体生成的核心挑战：数百乃至数千个合体字、半形式和元音符号变体必须在OpenType排版下保持相互一致。Srijika生成了66个TTF文件：57个精心策划的预设字体和九个开放词汇的展示字体。所有字体均通过OpenType Sanitizer检查，并且HarfBuzz和CoreText在合体字密集的测试用例上重现了模板的字形ID序列。一项涵盖80,915个字形和54,812个锚点的全闭包审计量化了度量变化。

    arXiv:2609.05661v1 Announce Type: cross  Abstract: We present Srijika, a system for producing installable OpenType fonts for nine Brahmic scripts: Devanagari, Tamil, Bengali, Telugu, Kannada, Malayalam, Gujarati, Gurmukhi, and Odia. Rather than generating fonts from scratch, Srijika restyles glyph outlines from shaping-complete template fonts. It preserves the template's cmap and GSUB closure and its GPOS data under a documented metric policy, making every output a complete font by construction. This addresses a central challenge of Indic font generation: hundreds to thousands of conjuncts, half forms, and matra variants must remain mutually consistent under OpenType shaping.   Srijika produces 66 TTFs: 57 curated presets and nine open-vocabulary showcase fonts. All pass the OpenType Sanitizer, while HarfBuzz and CoreText reproduce the template glyph-ID sequences on conjunct-heavy probes. A full-closure audit covering 80,915 glyphs and 54,812 anchors quantifies metric changes. Natural-
    
[^421]: LLM生成的SystemVerilog断言对语义保持RTL变换的鲁棒性

    Robustness of LLM-Generated SystemVerilog Assertions to Semantics-Preserving RTL Transformations

    [https://arxiv.org/abs/2609.05658](https://arxiv.org/abs/2609.05658)

    本文提出了一种受控蜕变评估方法，通过操作数重排序、标识符重命名和冗余括号化三种语义保持的RTL变换，系统评估了LLM生成SystemVerilog断言的输出稳定性，揭示了传统单一点准确率无法反映模型鲁棒性的问题。

    

    大型语言模型（LLM）正被越来越多地探索用于自动化SystemVerilog断言（SVA）生成，然而大多数评估仅报告输入在单一语法表示下的正确性。这种点准确率无法揭示当相同的RTL行为以不同方式编写时，模型的正确输出是否保持稳定。本文针对基于LLM的SVA生成，在语义保持的RTL变换下进行了受控的蜕变评估。从VERT数据集出发，我们构建了一个经过质量筛选的条件控制池，以及一个包含295个赋值行为的分层40程序评估集。我们使用相同的评估提示词和贪心解码评估了两个开源代码模型：Qwen2.5-Coder-7B和DeepSeek-Coder-V2-Lite。研究了三种变换：操作数重排序、确定性标识符重命名和冗余括号化。除了基线和变换后的准确率之外，我们还测量了……（原文摘要在此处截断）

    arXiv:2609.05658v1 Announce Type: new  Abstract: Large language models (LLMs) are increasingly being explored for automating SystemVerilog Assertion (SVA) generation, yet most evaluations report correctness on a single syntactic representation of an input. Such point accuracy does not reveal whether a model's correct output is stable when the same RTL behavior is written differently. This paper presents a controlled metamorphic evaluation of LLM-based SVA generation under semantics-preserving RTL transformations. Starting from the VERT dataset, we construct a quality-filtered conditional-control pool and a stratified 40-program evaluation set containing 295 assignment behaviors. We evaluate two open code models, Qwen2.5-Coder-7B and DeepSeek-Coder-V2-Lite, with an identical evaluation prompt and greedy decoding. Three transformations are studied: operand reordering, deterministic identifier renaming, and redundant parenthesization. Beyond baseline and transformed accuracy, we measure c
    
[^422]: 基于内在好奇心的强化学习内生探索

    Endogenous Exploration in Reinforcement Learning with Intrinsic Curiosity

    [https://arxiv.org/abs/2609.05650](https://arxiv.org/abs/2609.05650)

    该论文提出一种由内在好奇心驱动探索的强化学习框架，基于“有效探索产生于中等不一致水平”这一核心假设，在液态状态机基底上实现，并在LunarLanderv2和BipedalWalkerv3两个基准上达到了与PPO等成熟算法相当的性能。

    

    我们提出了一种由内在好奇心驱动探索的强化学习框架，适用于环境非平稳且奖励稀疏、延迟、无信息或缺失的场景。在该模型中，动作选择由外部奖励与一种认知动机机制相结合来引导，该机制使智能体倾向于沿着结构化的探索方向行动。其核心假设是：有效探索在中等程度的不一致性水平下产生，而在过于僵化或过于无序的动态下性能都会退化。为验证这一想法，我们在液态状态机（LSM）基底上实现了该框架，并在两个标准基准上进行评估：离散动作的LunarLanderv2和连续控制的BipedalWalkerv3。所提出的方法在这两项任务上均取得了与成熟深度强化学习算法（包括近端策略优化PPO）相当的性能。

    arXiv:2609.05650v1 Announce Type: new  Abstract: We propose a reinforcement learning framework in which exploration is driven by intrinsic curiosity, designed for scenarios where environments are non-stationary and rewards are sparse, delayed, uninformative, or absent. In our model, action selection is guided by a combination of external rewards and an epistemic motivation mechanism that biases the agent toward structured exploratory directions. The central hypothesis is that effective exploration emerges at intermediate levels of incoherence, while performance degrades under both overly rigid and overly disordered dynamics. To test this idea, we implement the framework on top of a Liquid State Machine (LSM) substrate and evaluate it on two standard benchmarks: the discrete-action LunarLanderv2 and the continuous-control BipedalWalkerv3. The proposed method achieves competitive performance on both tasks relative to established deep RL algorithms, including Proximal Policy Optimization 
    
[^423]: 矩阵乘积抖动标量量化的核范数下界

    A Nuclear-Norm Lower Bound for Dithered Scalar Quantization of Matrix Products

    [https://arxiv.org/abs/2609.05641](https://arxiv.org/abs/2609.05641)

    该论文证明了矩阵乘积抖动标量量化误差的核范数下界，并通过SVD对齐的Hadamard构造证明该下界是紧的。

    

    我们研究量化矩阵乘法 $C=AB$ 中最小化误差的问题。对标量因子进行量化会引入舍入误差，其大小取决于各行和各列的最大绝对值——即范围，这些范围决定了量化网格步长。为了降低误差，我们在不改变 $C$ 的前提下，对能够改变因子范围和网格步长的保乘积变换进行优化。具体而言，我们在可逆的内基变换和正交的外旋转下寻找最小的首项期望平方误差。在无界格上施加独立、零均值减性抖动噪声的条件下，我们证明了仅依赖输出的下界 $E_{\rm lead} \ge (c_A+c_B)/K \Vert AB\Vert_*^2$，其中 $K$ 是内维数，$c_A$ 和 $c_B$ 是归一化噪声方差，$\Vert AB\Vert_*$ 是核范数。该下界是紧的：SVD对齐的Hadamard构造在Hadamard存在的条件下可以达到该下确界。

    arXiv:2609.05641v1 Announce Type: cross  Abstract: We consider the problem of minimizing error in quantized matrix multiplication $C=AB$. Scalar quantization of the factors introduces rounding errors whose scale depends on the maximum absolute entries -- the ranges -- of their rows and columns. These ranges determine the quantization grid steps. To reduce the error, we optimize over product-preserving transformations that alter the factor ranges and grid steps without changing $C$. Specifically, we seek the smallest leading expected squared error over invertible inner changes of basis and orthogonal outer rotations. Under independent, zero-mean subtractive dither noise on an unbounded lattice, we prove the output-only bound $E_{\rm lead} \ge (c_A+c_B)/K \Vert AB\Vert_*^2$, where $K$ is the inner dimension, $c_A$ and $c_B$ are normalized noise variances, and $\Vert AB\Vert_*$ is the nuclear norm. The bound is tight: an SVD-aligned Hadamard construction attains the infimum whenever a Had
    
[^424]: HB-PVI：一种用于复杂活动识别的层次贝叶斯个性化与信息价值框架

    HB-PVI: A Hierarchical Bayesian Personalization and Value-of-Information Framework for Complex Activity Recognition

    [https://arxiv.org/abs/2609.05582](https://arxiv.org/abs/2609.05582)

    提出HB-PVI框架，通过层次贝叶斯模型与EVSI停止规则，联合量化复杂活动识别中参与者异质性、四种个性化机制的收益以及校准标签的经济价值。

    

    个性化可以提升活动识别性能，但参与者特定的收益是异质的，且每增加一个校准标签都有获取成本。本研究提出了HB-PVI，一个层次贝叶斯个性化与信息价值框架，针对47名参与者的MUSIC-CAR复杂活动队列，联合建模参与者异质性、四种个性化机制的收益与损害，以及额外标签的经济价值。一种无泄漏的留一参与者评估方法将序贯蒙特卡洛参与者效应更新器、学生t分布层次增益模型与一步样本信息期望值（EVSI）停止规则相结合。适配器个性化产生了小幅的正平均F1增益，从一个标签时的0.00099增长到十个标签时的0.00198，而适配器加头部以及原型残差个性化平均为负。在主要的实际应用……

    arXiv:2609.05582v1 Announce Type: new  Abstract: Personalization can improve activity-recognition performance, but participant-specific gains are heterogeneous, and every additional calibration label has an acquisition cost. This study presents HB-PVI, a hierarchical Bayesian personalization and value-of-information framework jointly modeling participant heterogeneity, the benefit and harm of four personalization mechanisms, and the economic value of an additional label, for the 47-participant MUSIC-CAR complex-activity cohort. A leakage-safe, leave-one-participant-out evaluation combines a sequential-Monte-Carlo participant-effect updater with a Student-$t$ hierarchical gain model and a one-step expected-value-of-sample-information (EVSI) stopping rule. Adapter personalization produced small positive mean F1 gains, growing from 0.00099 at one label to 0.00198 at ten, while adapter-plus-head and prototype-residual personalization were negative on average. Under the primary practical-be
    
[^425]: 胶囊透镜：在模型表示中定位与追踪概念几何结构

    Capsule Lens: Locating and Tracking Concept Geometry in Model Representations

    [https://arxiv.org/abs/2609.05575](https://arxiv.org/abs/2609.05575)

    提出Capsule Lens框架，通过闭式拟合由可解释参数定义的胶囊几何形状，来定位和追踪机器学习模型表示中概念的几何结构，并可同时应用于静态与动态表示场景。

    

    理解概念如何编码在机器学习模型的内部表示中，是机制可解释性（mechanistic interpretability）领域的核心问题，这对于深度学习的科学研究以及日益强大的模型的可信部署都至关重要。现有的模型表示解释方法主要将表示映射到更可解释的空间中，并未直接刻画概念如何占据表示空间；尽管已有多种假设被提出，但往往缺乏严格的验证，且大多聚焦于静态表示。在本工作中，我们提出了Capsule Lens，这是一个将概念所占区域与一种简单、可追踪的几何形状——胶囊（capsule）——相匹配的框架。该胶囊由若干可解释的参数定义，以闭式形式拟合到每个概念的几何结构上，并在留出样本上进行了验证。我们将Capsule Lens应用于两大场景：静态表示和动态表示。在静态表示上……

    arXiv:2609.05575v1 Announce Type: new  Abstract: Understanding how concepts are encoded in the internal representations of machine learning models is a central problem in mechanistic interpretability, essential both for the science of deep learning and for the trustworthy deployment of increasingly capable models. Existing approaches to interpret model representations mainly map representations onto more interpretable spaces and do not directly characterize how concepts occupy representation space; various hypotheses have been proposed, but often lack of rigorous validation and largely focus on static representations. In this work, we introduce Capsule Lens, a framework that matches the region a concept occupies with a simple, trackable geometric form, a capsule, defined by several interpretable parameters, fitted in closed form to each concept's geometry and validated on held-out samples. We apply Capsule Lens in two major settings: static and dynamic representations. On static repres
    
[^426]: 基于粒球的多粒度自适应超图表示学习

    Multi-granularity Adaptive Hypergraph Representation Learning via Granular-ball

    [https://arxiv.org/abs/2609.05574](https://arxiv.org/abs/2609.05574)

    提出了基于粒球的多粒度超图表示学习框架MGHRL，通过自适应粒度超图生成策略在多个粒度层级上生成超边，从而自适应地发现图中的高阶关系并高效处理复杂结构信息。

    

    超图表示学习旨在通过构造能同时连接多个节点的超边来捕捉图中的高阶信息。这些超边能够适应图的拓扑特征，从而有助于在多个粒度上提取高阶关系。以往的大多数工作依赖预定义的方式来生成超边，忽视了图拓扑结构的多样性以及超边的多粒度特性。因此，这限制了它们有效且自适应地发现高阶关系、高效处理复杂结构信息的能力。为了解决这一局限，我们提出了一个名为多粒度超图表示学习（MGHRL）的新颖框架。MGHRL 引入了一种自适应粒度超图生成策略，该策略可在多个粒度层级上生成超边

    arXiv:2609.05574v1 Announce Type: new  Abstract: Hypergraph representation learning aims to capture high-order information in graphs by constructing hyperedges that simultaneously connect multiple nodes. These hyperedges adapt to the graph's topological features, facilitating the extraction of high-order relationships at multiple granularities. Most prior work relies on predefined definitions to generate hyperedges, overlooking the diversity in graph topological structures and the multi-granularity characteristics of hyperedges. As a result, this limits their ability to effectively and adaptively discover high-order relationships and efficiently process complex structural information. To address this limitation, we propose a novel framework called \underline{M}ulti-\underline{G}ranularity \underline{H}ypergraph \underline{R}epresentation \underline{L}earning (MGHRL). MGHRL introduces an Adaptive Granular Hypergraph Generation strategy, which generates hyperedges at multiple levels of g
    
[^427]: 深度信念网络是精确的

    Deep belief networks are exact

    [https://arxiv.org/abs/2609.05572](https://arxiv.org/abs/2609.05572)

    本文证明了 \(\{-1,1\}^n\) 上任意严格正的概率分布都能被有限参数的 Sigmoid 信念网络精确表示，利用布劳威尔不动点定理回答了 Sutskever 和 Hinton 的开放性问题。

    

    我们证明，定义在 \(\{-1,1\}^n\) 上的每一个严格正的概率分布，都可以被一个具有有限参数的 Sigmoid 信念网络精确表示。这回答了 Sutskever 和 Hinton 提出的一个问题。该证明利用布劳威尔（Brouwer）不动点定理，将他们的概率共享近似方法升级为精确表示。

    arXiv:2609.05572v1 Announce Type: new  Abstract: We prove that every strictly positive probability distribution on \(\{-1,1\}^n\) is represented exactly by a sigmoid belief network with finite parameters. This answers a question of Sutskever and Hinton. The proof upgrades their probability-sharing approximation to exact representation using Brouwer's fixed-point theorem.
    
[^428]: 迈向可持续的分布式大语言模型推理：面向能源、碳排与缓存感知的llm-d控制平面的系统综合与研究议程

    Toward Sustainable Distributed LLM Inference: A Systems Synthesis and Research Agenda for an Energy-, Carbon-, and Cache-Aware llm-d Control Plane

    [https://arxiv.org/abs/2609.05565](https://arxiv.org/abs/2609.05565)

    本文综合现有系统研究成果，归纳出可复用的设计模式，提出面向llm-d分布式推理控制平面的能源、碳排与缓存感知的可持续推理控制平面（SICP）框架与研究议程。

    

    大语言模型（LLM）的可持续性日益成为一个服务系统问题，而不仅仅是训练问题。在生产环境中，能源和碳排的影响不仅取决于模型大小：工作负载形态、批处理、键值（KV）缓存复用、预填充/解码任务放置、模型与加速器选择、电源状态、地理碳强度以及服务级目标（SLO）等因素都至关重要。近期的系统研究论文分别单独探讨了其中的许多因素。本文将这些研究结果联系起来，并提出一个实际的工程问题：当决策点是像llm-d这样的分布式推理控制平面时，这些研究结果意味着什么？本文的贡献在于综合归纳，而非提出新的基准测试结果。所报告的性能、能源、碳排和成本方面的改进仍是所引用论文和系统的成果。我将相关文献归纳为反复出现的设计模式，并利用这些模式为llm-d勾勒出一个可持续推理控制平面（SICP）。

    arXiv:2609.05565v1 Announce Type: cross  Abstract: Large language model (LLM) sustainability is increasingly a serving-systems problem, not only a training problem. In production, energy and carbon impact depend on more than model size: workload shape, batching, key-value (KV) cache reuse, prefill/decode placement, model and accelerator choice, power state, geographic carbon intensity, and service-level objectives (SLOs) all matter. Recent systems papers study many of these factors separately. This paper connects those results and asks a practical engineering question: what do they imply when the decision point is a distributed inference control plane such as llm-d?   The contribution here is synthesis, not a new set of benchmark results. Reported performance, energy, carbon, and cost improvements remain the results of the cited papers and systems. I group the literature into recurring design patterns and use those patterns to sketch a Sustainable Inference Control Plane (SICP) for llm
    
[^429]: Rollcast：用于自适应概率时间序列预测的适当评分门控滚动锚点

    Rollcast: Proper-Score Gated Rolling Anchors for Adaptive Probabilistic Time-Series Forecasting

    [https://arxiv.org/abs/2609.05561](https://arxiv.org/abs/2609.05561)

    Rollcast 提出了一种用适当评分门控的滚动统计锚点集合替代单一全局模型的概率预测方法，通过状态依赖门控机制和相似历史状态的残差分布，在多种时间序列动态过程中实现自适应的不确定性量化。

    

    Rollcast 是一种针对单变量时间序列的概率预测方法，它结合了一组紧凑的滚动统计锚点，而非依赖单一的全局模型。滚动均值、中位数、极值、回归端点和分位数定义了候选预测位置以及当前状态的表示。一个状态依赖的 softmax 门控通过最小化负对数预测密度来学习锚点权重，同时从相似历史状态中检索的残差分布提供局部不确定性。递归模拟将所得混合分布传播到多个预测视界上。该方法在涵盖八种数据生成过程的蒙特卡洛研究中进行评估，包括自回归、随机游走、局部趋势、阈值、状态切换、随机波动、厚尾和方差突变等动态过程。在 2,000 个独立拟合序列上，Rollcast 与真实条件预测分布进行了比较（摘要在此处截断）。

    arXiv:2609.05561v1 Announce Type: cross  Abstract: Rollcast is a probabilistic forecasting method for univariate time series that combines a compact set of rolling statistical anchors rather than relying on a single global model. Rolling means, medians, extrema, regression endpoints, and quantiles define candidate forecast locations and a representation of the current state. A state-dependent softmax gate learns anchor probabilities by minimizing negative log predictive density, while residual distributions retrieved from similar historical states provide local uncertainty. Recursive simulation propagates the resulting mixture over multiple forecast horizons. The method is evaluated in a Monte Carlo study covering eight data-generating processes, including autoregressive, random-walk, local-trend, threshold, regime-switching, stochastic-volatility, heavy-tailed, and variance-break dynamics. Across 2,000 independent fitted series, Rollcast is compared with the true conditional predictiv
    
[^430]: 曲面地形上深度平均颗粒雪崩动力学的物理信息神经网络

    Physics-Informed Neural Networks for Depth-Averaged Granular Avalanche Dynamics on Curved Topography

    [https://arxiv.org/abs/2609.05542](https://arxiv.org/abs/2609.05542)

    本研究将基于Savage-Hutter方程的深度平均物理信息神经网络（PINN）扩展至指数弯曲溜槽上的颗粒雪崩动力学，证明分阶段时间课程学习可将保留观测的轨迹误差降低约两个数量级，且稀疏数据下观测点的位置比观测数量更为重要。

    

    物理信息神经网络（PINNs）为求解控制方程提供了一种无网格框架，但其在弯曲地形上颗粒雪崩动力学中的应用在很大程度上尚未被探索。本研究将基于Savage-Hutter方程的深度平均PINN公式扩展到一个具有空间变化倾角的指数弯曲溜槽，并采用了应变率相关的莫尔-库仑土压力闭合关系。该模型通过实验室颗粒雪崩实验中测量的前缘和后缘轨迹进行了验证，其中部分观测数据在训练中被保留（不参与训练）。分阶段的时间课程学习策略对准确预测至关重要，与从一开始就在整个时间域上进行训练相比，该策略将保留轨迹的预测误差降低了约两个数量级。稀疏数据实验进一步表明，在配置固定的情况下，观测点的位置比观测数量对结果的影响更大。

    arXiv:2609.05542v1 Announce Type: cross  Abstract: Physics-informed neural networks (PINNs) provide a mesh-free framework for solving governing equations, but their application to granular avalanche dynamics over curved terrain remains largely unexplored. This study extends a depth-averaged PINN formulation based on the Savage-Hutter equations to an exponentially curved chute with spatially varying inclination and a strain-rate-dependent Mohr-Coulomb earth-pressure closure. The model is validated against measured front- and rear-edge trajectories from a laboratory granular-avalanche experiment, with selected observations withheld from training. A staged temporal curriculum proved essential for accurate prediction, reducing the held-out trajectory error by approximately two orders of magnitude compared with training over the full time domain from the outset. Sparse-data experiments further showed that observation placement was more influential than observation number within the configur
    
[^431]: SimpleMemVLA：一种简单而有效的面向视觉-语言-动作模型的原生视频记忆

    SimpleMemVLA: A Simple but Effective Native-Video Memory for Vision-Language-Action Models

    [https://arxiv.org/abs/2609.05533](https://arxiv.org/abs/2609.05533)

    SimpleMemVLA提出了一种无需专用记忆模块的视觉-语言-动作模型，通过完整保留历史信息并以时间戳视频格式直接输入骨干网络，利用子任务隐藏状态作为历史到流匹配动作头的唯一通道，从而有效解决长时程操作中的部分可观测问题。

    

    长时程操作是部分可观测的：选择下一个动作所需的信息可能仅出现在几分钟之前的观测中。现有的记忆机制——检索库、学习型压缩器、循环状态——必须在不知道未来决策需要什么的情况下，先决定从过去保留哪些信息。这一设计源于一种假设，即分钟级的历史数据过于庞大而无法直接处理，但现代VLM骨干网络已不再受此限制。在这项工作中，我们提出了SimpleMemVLA，一种无需专用记忆模块的VLA模型。它完整保留采样的历史信息，并以骨干网络预训练时所用的时间戳视频格式将其传递给骨干网络；生成的子任务的隐藏状态随后构成从历史信息到标准流匹配动作头的唯一通道。由于连续决策共享大部分历史信息，在动作执行期间预填充共享前缀可使延迟保持在接近

    arXiv:2609.05533v1 Announce Type: cross  Abstract: Long-horizon manipulation is partially observable: the information needed to choose the next action may appear only in observations from minutes earlier. Existing memory mechanisms: retrieval banks, learned compressors, recurrent states must decide what to keep from the past before knowing what a future decision will require. This was motivated by the assumption that minute-scale history is too large to process directly, which modern VLM backbones no longer make true. In this work, we introduce SimpleMemVLA, a VLA without a dedicated memory module. It keeps the sampled history intact and passes it to the backbone in the timestamped video format the backbone was pretrained to process; the hidden states of a generated sub-task then form the only channel from history to a standard flow-matching action head. Since consecutive decisions share most of their history, prefilling the shared prefix during action execution keeps latency close to 
    
[^432]: 一种用于解读头颈癌PET/CT的专用大型多模态模型

    A Specialized Large Multimodal Model for Interpreting PET/CT in Head and Neck Cancer

    [https://arxiv.org/abs/2609.05532](https://arxiv.org/abs/2609.05532)

    研究者利用大规模多机构PET/CT数据集和两级训练课程微调LLaVA-NeXT，开发出一种专用大型多模态模型，实现了头颈癌PET/CT的自动化解读，验证了专用LMM在医学影像诊断中的可行性。

    

    背景：由于头颈部区域解剖结构的复杂性，使用PET/CT诊断头颈癌在临床上具有挑战性且耗时，这推动了计算机辅助诊断（CAD）的发展。通用大型多模态模型（LMM）在医学领域的应用仍然受限，原因包括领域专业知识不足、隐私与安全问题以及输出冗长，这促使了专用独立LMM的发展。目的：我们利用大规模多机构PET/CT数据集、定制化训练课程和自回归训练，评估了专用LMM在头颈癌PET/CT自动解读中的可行性。方法：LLaVA-NeXT通过两级课程进行微调，其图像-对话对由两位放射科医生从公开数据中整理而成。数据集包含临床重要标注，如原发肿瘤的存在和转移性淋巴结的位置。第一级使用28,000个图像-对话对来学习……

    arXiv:2609.05532v1 Announce Type: cross  Abstract: Background: Diagnosing head and neck cancer using PET/CT is clinically challenging and time-consuming due to the anatomical complexity of the region, motivating computer-aided diagnosis (CAD). Generalist Large Multimodal Models (LMMs) remain limited in medical contexts by insufficient domain-specific knowledge, privacy and security concerns, and verbosity, motivating specialized standalone LMMs. Purpose: We evaluated the feasibility of a specialized LMM for automated PET/CT interpretation in head and neck cancer using a large-scale multi-institutional PET/CT dataset, a tailored training curriculum, and autoregressive training. Methods: LLaVA-NeXT was fine-tuned using a two-level curriculum with image-conversation pairs curated by two radiologists from public data. The dataset included clinically important annotations such as primary tumor presence and metastatic lymph node location. Level 1 used 28,000 image-conversation pairs to learn
    
[^433]: 基于基础设施的单目三维车辆定位框架及实验验证

    Infrastructure-based Monocular 3D Vehicle Localization Framework with Experimental Validation

    [https://arxiv.org/abs/2609.05523](https://arxiv.org/abs/2609.05523)

    本文提出一种单阶段学习框架，直接从路边单目相机图像联合估计车辆在地面固定坐标系中的位置、尺寸和偏航角，并利用路边相机与无人机同步视频构建数据采集与标注流水线，完成了实验验证。

    

    本文提出了一种单阶段学习框架，将路边单目相机图像直接映射到地面固定坐标系中的车辆状态。与先在图像平面中检测车辆、随后进行几何后处理的传统方法不同，所提出的方法利用预训练目标检测器提取的特征，联合估计每辆车的地面平面位置、尺寸和偏航角。因此，该框架不仅将视觉特征用于车辆检测，还将其用于直接的空间位置和朝向估计。为支持模型训练和评估，我们开发了一个基于路边相机与无人机（UAV）同步视频的数据采集和标签生成流水线。作为临时性的俯视感知平台，无人机提供车辆轨迹、尺寸和朝向信息，这些信息被转换到地面固定坐标系中。

    arXiv:2609.05523v1 Announce Type: cross  Abstract: This paper presents a one-stage learning framework that maps monocular roadside-camera images directly to vehicle states in a ground-fixed coordinate frame. Unlike conventional approaches that first detect vehicles in the image plane and subsequently apply geometric post-processing, the proposed method leverages features from a pretrained object detector to jointly estimate each vehicle's ground-plane position, dimensions, and yaw angle. The framework therefore uses visual features not only for vehicle detection but also for direct spatial and orientation estimation. To support model training and evaluation, we develop a data-collection and label-generation pipeline based on synchronized video from a roadside camera and an unmanned aerial vehicle (UAV). Acting as a temporary top-view sensing platform, the UAV provides vehicle trajectories, dimensions, and orientations, which are transformed into the ground-fixed coordinate frame and te
    
[^434]: 多光照/多焦点显示图像异常检测的对比知识蒸馏方法

    Contrastive Knowledge Distillation for Anomaly Detection in Multi-Illumination/Focus Display Images

    [https://arxiv.org/abs/2609.05520](https://arxiv.org/abs/2609.05520)

    本文提出多分辨率对比蒸馏（MCD）方法，无需正负样本对，通过拉近/推远师生网络特征距离，实现多光照/多焦点显示图像中微小缺陷的异常检测。

    

    本文研究多光照和多焦点显示图像中的自动异常检测问题。显示表面的微小缺陷在RGB图像中很难被发现，仅用正常数据训练的模型也难以将其检测出来。为解决这一问题，我们提出了一种新颖的对比学习方案，用于基于知识蒸馏的异常检测。在我们的框架中，采用多分辨率知识蒸馏作为基线，该方法通过测量教师网络和学生网络之间的特征相似度来工作。基于MKD，我们提出了一种新颖的对比学习方法，即多分辨率对比蒸馏，它不需要带有锚点的正/负样本对，而是通过拉近/推远教师和学生特征之间的距离来进行学习。此外，我们还提出了一个融合模块，用于将多通道信息转换并聚合到MCD的三通道输入层。

    arXiv:2609.05520v1 Announce Type: cross  Abstract: In this paper, we tackle automatic anomaly detection in multi-illumination and multi-focus display images. The minute defects on the display surface are hard to spot out in RGB images and by a model trained with only normal data. To address this, we propose a novel contrastive learning scheme for knowledge distillation-based anomaly detection. In our framework, Multiresolution Knowledge Distillation (MKD) is adopted as a baseline, which operates by measuring feature similarities between the teacher and student networks. Based on MKD, we propose a novel contrastive learning method, namely Multiresolution Contrastive Distillation (MCD), which does not require positive/negative pairs with an anchor but operates by pulling/pushing the distance between the teacher and student features. Furthermore, we propose the blending module that transforms and aggregate multi-channel information to the three-channel input layer of MCD. Our proposed met
    
[^435]: 基于YOLOv8的统一驾驶感知中频率感知任务加权的探索性研究

    An Exploratory Study of Frequency-Aware Task Weighting for YOLOv8-Based Unified Driving Perception

    [https://arxiv.org/abs/2609.05516](https://arxiv.org/abs/2609.05516)

    本文提出频率感知任务加权（FTW）方法，通过分析近期损失历史轨迹的低频能量比率来估计任务稳定性，并据此动态调整任务权重，以改善基于YOLOv8的统一驾驶感知中多任务联合优化的平衡问题。

    

    arXiv:2609.05516v1 公告类型：交叉 摘要：统一感知使自动驾驶系统能够在单个网络内执行目标检测、可行驶区域分割和车道分割，从而提高效率并降低部署复杂性。联合优化多个感知任务仍然具有挑战性，因为不同任务表现出不同的收敛速度、损失尺度和优化稳定性。现有的任务加权方法使用损失幅值、学习到的不确定性、短期损失变化或梯度统计；在此，我们探索将近期损失历史窗口的频率结构作为补充信号。我们实现并研究了频率感知任务加权，这是一种动态任务平衡规则，它从近期损失历史的低频能量比率中估计损失轨迹稳定性代理。FTW为那些均值中心化后的损失轨迹中包含更大比例低频功率的任务分配更大的权重。我们记录了FTW和两种

    arXiv:2609.05516v1 Announce Type: cross  Abstract: Unified perception enables autonomous driving systems to perform object detection, drivable-area segmentation, and lane segmentation within a single network, improving efficiency and reducing deployment complexity. Jointly optimizing multiple perception tasks remains challenging because tasks exhibit different convergence rates, loss scales, and optimization stability. Existing task-weighting methods use loss magnitude, learned uncertainty, short-term loss changes, or gradient statistics; here, we explore the frequency structure of a recent loss-history window as a complementary signal.   We implement and examine Frequency-aware Task Weighting (FTW), a dynamic task-balancing rule that estimates a loss-trajectory stability proxy from the low-frequency energy ratio of recent loss histories. FTW assigns larger weights to tasks whose mean-centered loss trajectories contain a larger proportion of low-frequency power. We document FTW and two
    
[^436]: 何时教与教什么：面向网络智能体的预算感知在线适应

    When and What to Teach: Budget-Aware Online Adaptation for Web Agents

    [https://arxiv.org/abs/2609.05513](https://arxiv.org/abs/2609.05513)

    该论文提出了一种预算感知的在线教学框架，通过可解性感知的教师门控决定何时查询教师模型，并通过分数引导的轨迹修剪筛选教学内容，从而避免在不可解决的回合和冗余执行步骤上浪费训练预算。

    

    网络智能体在自动化复杂互联网任务方面已取得显著成功，但将其部署到真实环境中需要持续的在线适应。鉴于部署强大的专有模型在商业上成本过高，从业者必须依赖轻量级本地模型，这些模型在部署后通过来自更强教师模型的在线教学不断进化。然而，标准的交互式反馈会带来难以承受的成本。我们证明了传统的轨迹级偏好优化会在不可解决的回合和冗余的执行步骤上浪费预算。为解决这些低效问题，我们提出了“带预算轨迹修剪的分数引导在线教学”，这是一个预算感知框架，系统地编排何时教和教什么。具体而言，我们的框架集成了一个可解性感知的教师门控来决定何时查询教师模型，以及一个分数引导的轮（摘要在此处截断）

    arXiv:2609.05513v1 Announce Type: new  Abstract: Web agents have achieved significant success in automating complex internet tasks but deploying them in real-world environments requires continuous online adaptation. Given that deploying powerful proprietary models remains commercially cost-prohibitive, practitioners must rely on lightweight local models that evolve post-deployment via online teaching from a stronger teacher. However, standard interactive feedback imposes prohibitive costs. We show that conventional trajectory-level preference optimization wastes budget on both unresolvable episodes and redundant execution turns. To resolve these inefficiencies, we propose \textbf{Score-Guided Online Teaching with Budgeted Trajectory Trimming}, a budget-aware framework that systematically orchestrates \textbf{when} and \textbf{what} to teach. Specifically, our framework integrates a solvability-aware teacher gate to dictate \textbf{when} to query the teacher model and a score-guided tur
    
[^437]: 选项何时有用？Option-Critic中的策略坏死与冗余覆盖

    When Do Options Help? Policy Necrosis and Redundant Coverage in Option-Critic

    [https://arxiv.org/abs/2609.05508](https://arxiv.org/abs/2609.05508)

    该论文通过理论和实验证明，Option-Critic中学习到的终止规则毫无作用（甚至可能阻碍探索并产生Ω(T)的遗憾），且选项内部策略几乎不探索会导致“策略坏死”，从而解释了性能提升实际来源于增加选项数量所带来的冗余覆盖。

    

    Option-Critic（选项-评论家）方法学习“选项”：即子策略，同时学习一个规则来决定每个选项何时将控制权交回。其标志性结果是：随着选项数量的增加，性能会得到提升。我们通过理论和实验解释了这一结果。首先，Option-Critic通过最大化回报所学习到的终止规则实际上毫无作用。当终止测试和选择选项的策略读取相同的值时，该测试在每一步都会触发，因此学习到的规则与“总是终止”完全相同。当该策略进行探索而终止测试不进行探索时（正如Option-Critic本身的情况），该规则可能会阻碍探索；在某些实例中，它会产生 Ω(T) 的遗憾，而“总是终止”则能保持 O(log T) 的遗憾。强制在每一步终止后，选项数量与性能的曲线依然保持不变。其次，选项内部的策略几乎不进行探索，因此某个状态会锁定在第一个看起来不错的动作上，并且再也不更新。我们将这种现象命名为“策略坏死”。

    arXiv:2609.05508v1 Announce Type: cross  Abstract: Option-critic learns options: sub-policies together with a learned rule for when each one hands control back. Its headline result is that performance improves as options are added. We explain that result, with theory and experiment. First, the termination rule option-critic learns by maximising return contributes nothing. When the termination test and the policy that picks options read the same values, the test fires at every step, so the learned rule is identical to always terminating. When that policy explores and the test does not, as in option-critic itself, the rule can block the exploration; there are instances where it suffers $\Omega(T)$ regret while always terminating holds to $O(\log T)$. Forcing termination at every step leaves the option-count curve intact. Second, the policy inside an option barely explores at all, so a state locks onto the first action that looked good and never updates again. We name this policy necrosis
    
[^438]: 面向Black-Scholes隐含波动率计算的渐近信息驱动神经网络

    Asymptotically-informed neural networks for Black-Scholes implied volatility computation

    [https://arxiv.org/abs/2609.05491](https://arxiv.org/abs/2609.05491)

    本文提出一类渐近信息驱动的神经网络架构，通过门控函数系统对价格域进行可训练划分并结合专门化的局部近似，实现了Black-Scholes隐含波动的稳健高效计算，尤其适用于极端价格、行权价或期限的渐近区域。

    

    Black-Scholes隐含波动率的计算是量化金融中的一项基础任务，支撑着期权估值、模型校准和风险管理。尽管隐含波动率在实践中被常规使用，但Black-Scholes定价公式的反演仍然是一个具有挑战性的数值问题，特别是在对应于极端期权价格、行权价或期限的渐近区域中，此时逆映射对价格的扰动变得高度敏感。在本文中，我们引入了一类新的渐近信息驱动的神经网络架构用于隐含波动率计算。利用Black-Scholes定价函数在不同波动率区域中的不同行为，我们提出了一族架构，它们通过门控函数系统学习价格-对数货币性域的可训练划分，并结合隐含波动率函数的专门化局部近似……

    arXiv:2609.05491v1 Announce Type: cross  Abstract: The computation of Black-Scholes implied volatility is a fundamental task in quantitative finance, underpinning option valuation, model calibration and risk management. Although implied volatility is routinely used in practice, the inversion of the Black-Scholes pricing formula remains a challenging numerical problem, particularly in asymptotic regimes corresponding to extreme option prices, strikes or maturities, where the inverse map becomes highly sensitive to perturbations of the price. In this paper, we introduce a new family of asymptotically-informed neural-network architectures for implied-volatility computation. Exploiting the distinct behaviours of the Black-Scholes pricing function in different volatility regimes, we propose a family of architectures that learn a trainable partition of the price-log-moneyness domain through a system of gating functions and combines specialised local approximations of the implied-volatility f
    
[^439]: 深度学习医学图像配准中的架构组件与正则化组件：系统性消融研究

    Architectural and Regularization Components in Deep Learning Medical Image Registration: Systematic Ablation Study

    [https://arxiv.org/abs/2609.05484](https://arxiv.org/abs/2609.05484)

    本文通过系统性消融实验发现，在深度学习医学图像配准中，正则化损失对性能提升的贡献远大于仿射预对齐等架构增强组件，仅正则化即可带来21.3%的相对增益。

    

    深度学习配准方法通常在基础网络上叠加两类增强手段：架构层面的附加组件（如仿射预对齐阶段）和训练目标层面的附加组件（如正则化损失）。由于论文往往同时采用两者，因此不清楚究竟是哪一类在发挥作用。本研究进行了受控消融实验将二者区分开来。使用OASIS脑部MRI数据集（394个训练受试者、20个测试受试者），作者训练了同一配准流程的四个变体：采用基础相似性损失的基线3D U-Net、采用完整正则化套件的相同U-Net、采用基础损失的仿射加可变形架构，以及采用完整套件的仿射架构。研究评估了配准精度（MSE、NCC、SSIM）、形变质量（雅可比行列式保持度、位移统计量、解剖学合理性评分）以及计算成本。结果表明，仅正则化一项就贡献了大部分性能增益，相对提升了21.3%……

    arXiv:2609.05484v1 Announce Type: cross  Abstract: Deep learning registration methods routinely stack two kinds of enhancement on a base network: architectural additions such as affine pre-alignment stages, and training-objective additions such as regularization losses. Papers tend to adopt both at once, so it is unclear which is doing the work. I ran a controlled ablation to separate them. Using the OASIS brain MRI dataset (394 training subjects, 20 test subjects), I trained four variants of the same registration pipeline: a baseline 3D U-Net with basic similarity losses, the same U-Net with a full regularization suite, an affine-plus-deformable architecture with basic losses, and the affine architecture with the full suite. I evaluated registration accuracy (MSE, NCC, SSIM), deformation quality (Jacobian determinant preservation, displacement statistics, an anatomical plausibility score), and computational cost. Regularization alone accounted for most of the gain: a 21.3% relative ga
    
[^440]: ARC-Bench：闭环重规划掩盖了冻结JEPA世界模型中失效的动作排序

    ARC-Bench: Closed-Loop Replanning Masks Broken Action Ranking in Frozen JEPA World Models

    [https://arxiv.org/abs/2609.05461](https://arxiv.org/abs/2609.05461)

    该论文提出ARC-Bench无泄漏审计协议，揭示冻结JEPA世界模型中“潜在距离可正确排序动作”的核心假设严重且结构性失效，且该缺陷在不同视觉骨干网络下均持续存在。

    

    无奖励的潜在世界模型通过在冻结的潜在空间中以距离对候选动作进行评分来进行规划：如果一个动作预测的未来嵌入更接近目标嵌入，则该动作会被优先选择。这悄然假设了潜在接近度是可对动作进行排序的，即按潜在距离对候选动作排序与按真实代价排序是一致的。我们直接审计了这一假设。我们提出了ARC-Bench，一种无泄漏、固定候选集的评测协议，用于衡量冻结的JEPA式目标能否正确排序候选动作，并将其应用于官方发布的JEPA-WM检查点，涵盖导航和操作类控制任务。这一假设严重且结构性地失效了：在官方的操作任务审计中，得分最高的候选动作几乎总是次优的，同样的排序倒置现象也出现在迷宫环境中。一项受控的视觉骨干网络扩展实验表明，当将DINOv2替换为视频预训练的V-JEPA 1时，该缺陷依然存在。

    arXiv:2609.05461v1 Announce Type: new  Abstract: Reward-free latent world models plan by scoring candidate actions with distances in a frozen latent space: an action is preferred if its predicted future embedding lands closer to the goal embedding. This silently assumes that latent closeness is action-rankable, i.e., that ordering candidates by latent distance agrees with ordering them by true cost. We audit this assumption directly. We introduce ARC-Bench, a no-leak, fixed-candidate protocol that measures whether frozen JEPA-style objectives rank candidate actions correctly, and apply it to official released JEPA-WM checkpoints across navigation and manipulation-style control. The assumption fails, severely and structurally: on the official manipulation audits the top-scored candidate is almost always suboptimal, and the same inversion appears in the maze domains. A controlled visual-backbone extension shows that the defect persists when DINOv2 is replaced by video-pretrained V-JEPA 1
    
[^441]: 条件感知学习实现对不同化学修饰与检测条件下寡核苷酸熔解行为的稳健预测

    Condition aware learning enables robust prediction of oligonucleotide melting behavior across diverse chemistries and assay conditions

    [https://arxiv.org/abs/2609.05454](https://arxiv.org/abs/2609.05454)

    提出了一种条件感知的核苷酸语言模型，将上下文序列表示与反应环境信息相结合，能够在多种实验条件下准确预测未修饰和化学修饰寡核苷酸的熔解行为，克服了传统固定参数热力学模型难以推广的局限。

    

    寡核苷酸熔解温度是核酸杂交的基本决定因素，是分子诊断、聚合酶链式反应（PCR）检测以及众多其他生物技术应用设计的基础。然而，准确预测熔解行为仍然十分困难，因为它不仅取决于序列组成，还取决于现代检测设计中常用的实验条件和化学修饰。现有的热力学模型依赖于固定的参数化方法，往往难以推广至不同的反应环境和核苷酸化学体系。在本研究中，我们展示了一种条件感知的核苷酸语言模型，能够在多种实验条件下准确预测未修饰和化学修饰寡核苷酸的熔解行为。通过将上下文序列表示与描述反应环境的显式信息相结合，该框架……

    arXiv:2609.05454v1 Announce Type: cross  Abstract: Oligonucleotide melting temperature is a fundamental determinant of nucleic acid hybridization and underpins the design of molecular diagnostics, polymerase chain reaction assays, and many other biotechnology applications. However, accurately predicting melting behavior remains difficult because it depends not only on sequence composition, but also on experimental conditions and chemical modifications commonly used in modern assay design. Existing thermodynamic models rely on fixed parameterizations that are often difficult to extend across diverse reaction environments and nucleotide chemistries. Here we show that a condition-aware nucleotide language model can accurately predict oligonucleotide melting behavior across diverse experimental conditions and both unmodified and chemically modified oligonucleotides. By combining contextual sequence representations with explicit information describing the reaction environment, the framework
    
[^442]: ZetaDial：在推理时调节蛋白质结合物的净电荷以提升治疗性药物的可开发性

    ZetaDial: dialing net charge of protein binders at inference time for therapeutic developability

    [https://arxiv.org/abs/2609.05451](https://arxiv.org/abs/2609.05451)

    ZetaDial 提出了一种围绕固定骨架 ProteinMPNN 的采样后逐蛋白割线控制器，能够在推理时将治疗性蛋白质结合物的净电荷精确调节至目标设定值，显著降低电荷误差并提升药物可开发性。

    

    净电荷是治疗性结合物中与可开发性相关的一个特性，与黏度、清除率、非特异性相互作用和聚集有关，抗体筛选已经在使用电荷相关标准。然而，逆折叠流程尚未提供将其设定为目标值的方法。ProteinMPNN 和 BindCraft 虽然提供了氨基酸偏好、权重选择和自定义损失函数，但两者都没有提供逐蛋白的反馈回路，即无法在采样后测量实际电荷并将其校正至设定值。ZetaDial 在固定骨架的 ProteinMPNN 周围构建了一个采样后、逐蛋白的割线控制器。在匹配的随机基准测试中，与固定斜率回路相比，割线回路降低了平均绝对误差：在 RCSB 复合物上（5.17 对 6.46 电荷单位），在 Cas13 单体上（5.57 对 8.23）。相对于优化后的匹配全局偏好，它将 RCSB 误差从 11.71 降至 5.17（聚类自助法 p < 0.001），而在 Cas13 上统计上无显著差异（5.47 对……）。

    arXiv:2609.05451v1 Announce Type: cross  Abstract: Net charge is a developability-relevant property of therapeutic binders, linked to viscosity, clearance, nonspecific interaction and aggregation, and antibody screens already use charge-related criteria. Yet inverse-folding pipelines expose no way to set it to a target value. ProteinMPNN and BindCraft offer amino-acid biases, weight choices and custom losses, but neither supplies a per-protein feedback loop that measures realised charge after sampling and corrects it to a setpoint. ZetaDial contributes a post-sampling, per-protein secant controller around fixed-backbone ProteinMPNN. On matched stochastic benchmarks the secant loop reduced mean absolute error relative to a fixed-slope loop on RCSB complexes (5.17 vs 6.46 charge units) and Cas13 monomers (5.57 vs 8.23). Relative to the optimised matched global bias, it cut RCSB error from 11.71 to 5.17 (cluster bootstrap p < 0.001) and was statistically indistinguishable on Cas13 (5.47 v
    
[^443]: 面向视觉与语言Transformer的损伤感知赌博机剪枝方法

    Damage-Aware Bandit Pruning for Vision and Language Transformers

    [https://arxiv.org/abs/2609.05448](https://arxiv.org/abs/2609.05448)

    提出了一种损伤感知的多臂赌博机框架，通过配对损伤评估和UCB/分数Beta汤普森采样策略，在固定评估预算下为视觉和语言Transformer选择抑制后性能损失最小的结构单元（注意力头和MLP通道组）。

    

    Transformer的结构化训练后剪枝需要选择那些被抑制后仅造成有限性能下降的完整功能单元。我们将语言和视觉Transformer的结构化单元选择形式化为固定候选评估预算下的损伤感知多臂赌博机问题。在校准批次上临时掩蔽注意力头和MLP通道组，配对损伤定义为同一批次上掩蔽后的损失减去基准损失，从而减少批次间的波动。一个平滑有界的奖励驱动UCB风格的策略或分数Beta汤普森采样，最终掩蔽通过每步添加一个单元的方式顺序构建。所选单元在原始稠密模型检查点中被功能性地置零；因此，报告的参数效应代表有效的结构性抑制，而非物理压缩或实测加速。在WikiText-2、LAMBADA和Imagenette上的实验涵盖了GPT-2等模型。

    arXiv:2609.05448v1 Announce Type: new  Abstract: Structured post-training pruning of transformers requires selecting complete functional units whose suppression causes limited degradation. We formulate structured-unit selection for language and vision transformers as a damage-aware multi-armed bandit problem under a fixed candidate-evaluation budget. Attention heads and MLP channel groups are temporarily masked on calibration batches. Paired damage is the masked loss minus the base loss on the same batch, reducing batch-to-batch variation. A smooth bounded reward drives either a UCB-style policy or fractional-Beta Thompson Sampling, and the final mask is constructed sequentially by adding one unit at each step. The selected units are functionally zeroed in the original dense checkpoint; therefore, the reported parameter effects represent effective structural suppression rather than physical compression or measured speedup. Experiments on WikiText-2, LAMBADA, and Imagenette cover GPT-2,
    
[^444]: 人类大脑皮层折叠的表征学习以揭示持久的神经发育特征

    Representation learning of human cortical folding to reveal long lasting neurodevelopmental signatures

    [https://arxiv.org/abs/2609.05438](https://arxiv.org/abs/2609.05438)

    本文提出自监督学习框架Champollion，能从结构MRI中学习可解释的皮层折叠局部表征，其性能超越现有神经影像及通用基础模型，并揭示出比传统形态学指标更丰富的遗传关联和持久的神经发育特征。

    

    人类大脑在子宫内发生折叠，主要发生在妊娠晚期。出生后不久，皮层折叠模式即已确立并在此后保持稳定，使其成为极具前景的早期神经发育标志物。然而，目前尚不清楚现有神经影像基础模型所提供的表征能否捕捉皮层折叠的变异性。在此，我们提出Champollion，一个自监督学习框架，可从结构磁共振成像中学习可解释的皮层折叠局部表征。通过在代表性折叠相关任务上进行优化，Champollion能够准确捕捉跨皮层区域和外部数据集的已知折叠模式。在一项全面的基准测试中，它始终优于神经影像基础模型和通用基础模型。此外，Champollion揭示了比传统形态计量描述符更丰富的遗传关联，并识别出与海马不完全……相关的局部折叠特征。

    arXiv:2609.05438v1 Announce Type: cross  Abstract: The human brain folds in utero, primarily during late gestation. Shortly after birth, cortical folding patterns are established and remain stable thereafter, making them promising early neurodevelopmental markers. Yet it is unclear whether representations given by current neuroimaging foundation models capture cortical folding variability. Here, we introduce Champollion, a self-supervised learning framework that learns interpretable local representations of cortical folding from structural MRI. Optimized on representative folding-related tasks, Champollion accurately captures known folding patterns across cortical regions and external datasets. In a comprehensive benchmark, it consistently outperforms neuroimaging and general-purpose foundation models. Furthermore, Champollion reveals richer genetic associations than conventional morphometric descriptors and identifies localized folding signatures associated with incomplete hippocampal
    
[^445]: 基于加权机器学习集成、束搜索和质量约束重排序的新型混合蛋白质支架缺口填充方法

    Novel hybrid protein scaffold gap filling using weighted machine learning ensemble, beam search, and mass-constrained reranking

    [https://arxiv.org/abs/2609.05436](https://arxiv.org/abs/2609.05436)

    本研究提出了一种融合加权机器学习集成、束搜索和质量约束重排序的混合框架，用于解决蛋白质序列重建中已知缺口大小和质量条件下的支架缺口填充问题。

    

    蛋白质支架缺口填充是蛋白质序列重建中的一项重要计算任务，需要根据不完整的支架信息推断缺失的氨基酸区域。本研究提出了一种混合机器学习与质量约束重排序框架，用于已知缺口大小和已知缺口质量两种设置下的蛋白质支架缺口填充。研究使用来自MabCampath、P5A蛋白质型和碳酸酐酶2的同源蛋白质序列，生成掩码的11聚体残基级样本和完整缺口评估案例。残基预测任务被表述为采用首位、中间位和末位掩码策略的20类氨基酸分类问题。研究使用原始编码、行平均和SVD降维特征训练了多个经典机器学习模型，并通过验证准确率加权集成的方式组合了表现最强的模型。对于已知大小的缺口重建，使用束搜索方法生成填补序列，并通过质量约束重排序对候选序列进行筛选优化。

    arXiv:2609.05436v1 Announce Type: cross  Abstract: Protein scaffold gap filling is an important computational task in protein sequence reconstruction, where missing amino acid regions must be inferred from incomplete scaffold information. This study proposes a hybrid machine learning and mass constrained reranking framework for protein scaffold gap filling under known-gap-size and known-gapmass settings. Homologous protein sequences from MabCampath, P5A proteoform, and carbonic anhydrase 2 were used to generate masked 11-mer residue-level samples and fullgap evaluation cases. The residue prediction task was formulated as a 20-class amino acid classification problem using first-, middle-, and last-position masking. Multiple classical machine learning models were trained using raw encoded, row-average, and SVD-reduced features, and the strongest models were combined through a validation-accuracy-weighted ensemble. For known-size gap reconstruction, beam search was used to generate comple
    
[^446]: AhaBench：智能体能从先验经验中学习吗？一个面向长时程持续学习的基准测试

    AhaBench: Do Agents Learn from Prior Experience? A Benchmark for Long-Horizon Continual Learning

    [https://arxiv.org/abs/2609.05435](https://arxiv.org/abs/2609.05435)

    AhaBench 是一个长时程持续学习基准，通过谜题探索、带精确验证器的数学任务和自动售货机模拟三个组件，评估固定模型在获得先验经验后，在相关但支持已被移除、改变或延迟的条件下行为是否真正得到改善。

    

    arXiv:2609.05435v1 公告类型：新论文。摘要：现代语言智能体被期望在长时程上运行：它们会提出后续问题、复用已解决的示例、处理工具反馈，并适应延迟出现的后果。然而大多数评估仍然在每次提示后重置智能体，或仅对单条轨迹的最终状态进行评分。AhaBench 提出了一个更具操作性的问题：当一个固定模型获得有用的经验后，在一个明显的支持已被移除、改变或延迟的相关评估条件下，其后续行为是否会得到改善？该测试套件包含三个组成部分：Aha-Puzzle 测试智能体在解开隐藏状态谜题后的无提示探索能力；Aha-Euler 将类似 Project Euler 的数学思想转化为带有精确验证器的生成式教学/保留任务；Aha-Vending 是一个受 Vending-Bench 启发的开源实现，测试模拟的自动售货机智能体在处理延迟反馈和运营事故时能否保持盈利。AhaBench 报告了一个由三部分组成的评估结果

    arXiv:2609.05435v1 Announce Type: new  Abstract: Modern language agents are expected to operate over long horizons: they ask follow-up questions, reuse worked examples, handle tool feedback, and adapt to delayed consequences. Most evaluations still reset the agent after a prompt or score only the final state of one trajectory. AhaBench asks a more operational question: when a fixed model receives useful experience, does its later behavior improve under a related evaluation condition where the obvious support has been removed, changed, or delayed? The suite contains three components. Aha-Puzzle tests no-hint exploration after solved hidden-state puzzles; Aha-Euler turns Project-Euler-style mathematical ideas into generated taught/held-out tasks with exact validators; and Aha-Vending, an open-source implementation inspired by Vending-Bench, tests whether a simulated vending agent remains profitable while handling delayed feedback and operational incidents. AhaBench reports a three-part s
    
[^447]: 使用NIO Bench对机器学习工作负载的存储系统进行基准测试

    Benchmarking Storage Systems for Machine Learning Workloads Using NIO Bench

    [https://arxiv.org/abs/2609.05418](https://arxiv.org/abs/2609.05418)

    本文提出了NIO Bench基准测试框架，通过Python级I/O钩子与Linux strace相结合的两层追踪方法，刻画了六种机器学习模型架构的存储访问模式，发现I/O主要集中在数据准备、模型加载和检查点保存阶段，且训练是计算受限而非数据受限的。

    

    机器学习训练工作负载对存储系统提出了独特的需求，然而大多数现有的基准测试主要关注计算吞吐量而非文件系统I/O行为。我们提出了一个名为神经I/O基准测试（NIO Bench）的基准测试框架，用于刻画六种不同机器学习模型架构的存储访问模式：语言Transformer、视觉Transformer、扩散模型、脉冲神经网络、人工神经网络和强化学习。我们的框架采用两层追踪方法，将用于获取语义阶段上下文的Python级I/O钩子与用于完整系统调用覆盖（包括DataLoader工作子进程）的Linux strace相结合。我们在配置Ceph分布式文件系统的Nautilus Kubernetes集群上对全部六种模型进行了评估。我们的结果显示，I/O高度集中在数据准备、模型加载和模型检查点保存阶段。我们还发现，训练是计算受限的而非数据……（摘要原文在此处截断）

    arXiv:2609.05418v1 Announce Type: cross  Abstract: Machine learning training workloads place unique demands on storage systems, yet most existing benchmarks focus on computational throughput rather than file system I/O behavior. We present a benchmarking framework, Neural I/O Benchmark (NIO Bench), that characterizes storage access patterns across six diverse ML model architectures: Language Transformers, Vision Transformers, Diffusion Models, Spiking Neural Networks, Artificial Neural Networks, and Reinforcement Learning. Our framework employs a two-layer tracing approach combining Python-level I/O hooks for semantic phase context with Linux strace for complete syscall coverage including DataLoader worker subprocesses. We evaluate all six models on a Nautilus Kubernetes cluster with Ceph distributed file system. Our results reveal that I/O is heavily concentrated in data preparation, model loading, and model checkpointing. We also found that training is compute-bound rather than data-
    
[^448]: 学习增强算法：性能保证、构造机制与系统级意义

    Learning-Augmented Algorithms: Guarantees, Construction Mechanisms, and System-Level Implications

    [https://arxiv.org/abs/2609.04787](https://arxiv.org/abs/2609.04787)

    本综述系统梳理了学习增强算法领域的预测接口、误差度量、一致性—鲁棒性权衡及五种代表性构造机制，明确区分形式化保证与实证系统证据，并指出了该领域的未来开放问题。

    

    学习增强算法在利用可能出错的预测的同时保持正式的性能保证。本综述综合梳理了预测接口、误差度量、一致性—鲁棒性权衡，以及涵盖在线优化、缓存、学习型数据结构、图问题和机制设计等领域的五种代表性构造机制。综述还引入了一个正交的定理层面维度，用于区分所达到的上界与匹配的渐近依赖关系。正式的理论保证与实证系统证据被分开处理，并对预测成本、反馈和组合进行了明确分析。由此形成的综合性阐述给出了有限端到端推理的充分条件，并勾画了成本感知预测、内生误差、语义预测器和基准测试方面的开放问题。

    arXiv:2609.04787v1 Announce Type: new  Abstract: Learning-augmented algorithms use fallible predictions while retaining formal performance guarantees. This survey synthesizes prediction interfaces, error measures, consistency--robustness trade-offs, and five representative construction mechanisms across online optimization, caching, learned data structures, graph problems, and mechanism design. An orthogonal theorem-level axis distinguishes achieved upper bounds from matched asymptotic dependence. Formal guarantees are separated from empirical systems evidence, with explicit treatment of prediction cost, feedback, and composition. The resulting synthesis states sufficient conditions for limited end-to-end reasoning and delineates open problems in cost-aware prediction, endogenous error, semantic predictors, and benchmarking.
    
[^449]: 面向JEPA风格世界模型的频谱目标物理潜在结构化方法

    Spectral-Target Physical Latent Structuring for JEPA-Style World Models

    [https://arxiv.org/abs/2609.04264](https://arxiv.org/abs/2609.04264)

    该论文发现了JEPA风格世界模型中“物理表示惰性”这一新失败模式，并提出轻量级傅里叶辅助头在训练时对潜在空间施加物理信息结构化约束，在不增加推理成本的情况下有效提升下游规划性能。

    

    潜在世界模型作为一种在潜在空间而非像素空间中进行预测和规划的方法，正日益受到欢迎。近期的架构，如LeWorldModel（LeWM），采用SIGReg等正则化技术联合训练编码器和预测器，以防止表示坍塌。然而，即使有此类正则化来防止表示坍塌，我们仍然识别出一种新的世界模型失败模式——“物理表示惰性”，该现象在高动态环境中尤为显著。对于这些“惰性”情况，学习到的潜在状态并未坍塌，但却无法表示关键的物理属性，从而普遍导致下游规划失败。为解决这一问题，我们提出在训练阶段引入带有轻量级“傅里叶辅助头”的辅助监督，该方法在潜在空间中强制实施基于物理信息的结构化，且不产生任何额外的推理时开销，并可推广至任意环境。

    arXiv:2609.04264v1 Announce Type: new  Abstract: Latent world models have become increasingly popular as a method to predict and plan in latent space rather than pixel space. Recent architectures, such as LeWorldModel (LeWM), jointly train the encoder and predictor using regularization techniques like SIGReg to prevent representation collapse. Even with such regularization preventing representation collapse, we identify a new world model failure mode of \textit{physical representation laziness}, particularly noted in highly dynamic environments. For these lazy cases, the learned latent states do not collapse but nonetheless fail to represent key physical properties, causing ubiquitous downstream planning failure. To resolve this issue, we propose training-time auxiliary supervision with a lightweight "Fourier auxiliary head", which enforces physically-informed structuring of the latent space with no additional inference-time cost and can be generalized to any environment. Experimentall
    
[^450]: 面向金融领域的EXAONE预测模型

    EXAONE Forecast for Finance

    [https://arxiv.org/abs/2609.04239](https://arxiv.org/abs/2609.04239)

    EXAONE Finance是一个专为金融预测设计的时间序列基础模型，其核心创新在于采用无注意力机制的高效架构，通过因果一维卷积和组感知池化两个线性时间复杂度算子取代自注意力机制，从而解决了现有模型计算成本过高、无法处理间歇性观测数据以及未能捕捉金融市场独特动态的问题。

    

    本技术报告介绍了EXAONE Finance（EXAONE Forecast for Finance），这是一个专为金融预测定制的金融时间序列（TS）基础模型（TSFM）。近年来，时间序列基础模型通过大规模预训练实现了强大的零样本预测性能。然而，这些模型主要是为通用领域的时间序列开发的，并且很大程度上依赖于自注意力架构（self-attention backbone），其计算成本随序列长度和变量数量呈二次方增长。此外，这些模型假设输入是完全可观测的，且其预训练语料库未能捕捉金融市场独特的动态特性。这些限制阻碍了它们在金融领域的实际应用，因为金融领域常见的是长序列、多通道、间歇性观测的面板数据。为了解决这些挑战，EXAONE Finance采用了一种无注意力机制的架构，用两个简单而有效的线性时间复杂度算子取代了自注意力机制：1）用于时间维度混合的因果一维卷积，以及2）组感知池化多层感知机。

    arXiv:2609.04239v1 Announce Type: new  Abstract: This technical report presents EXAONE Forecast for Finance (EXAONE Finance), a financial time series (TS) foundation model (TSFM) tailored to financial forecasting. Recent TSFMs achieve strong zero-shot performance through large-scale pretraining. However, they are primarily developed for general-domain TS and largely rely on self-attention backbones whose computational cost grows quadratically with sequence length and variate count. Moreover, they assume fully observed inputs and are pretrained on corpora that fail to capture the unique dynamics of financial markets. These limitations hinder their applicability to finance, where long, many-channel, intermittently observed panels are common. To address these challenges, EXAONE Finance adopts an attention-free architecture, replacing self-attention with two simple yet effective linear-time operators: 1) a causal 1D convolution for temporal mixing and 2) a group-aware pooling multi-layer p
    
[^451]: 免费暂停标记

    Free Pause Tokens

    [https://arxiv.org/abs/2609.03807](https://arxiv.org/abs/2609.03807)

    提出免费暂停标记，通过权重共享主干上的并行预测流为模型提供额外思考计算，在不增加上下文长度、KV缓存和推理延迟的情况下，仅以1.14倍训练计算量的代价提升下一个词元预测性能。

    

    免费暂停标记为语言模型提供额外的计算量来形成每个下一个词元预测（如同暂停标记或思考标记的作用），但它不是在序列中添加额外标记，而是通过权重共享主干上的并行预测流来承载这些计算。在实际应用中，它使一个10亿参数模型的下一个词元预测提升了2-3厘奈特。由于暂停是搭载在已有位置上而非新增位置，因此使用它是免费的：在推理时它不增加上下文长度、不增加KV缓存，且几乎不产生延迟，推理浮点运算量的增长通常无关紧要，因为它并非吞吐量的主动瓶颈。唯一的主要成本在于训练阶段：与优化过的预训练流程相比，额外的训练计算量可低至1.14倍，同时保留了大部分收益。其结果是在与标准下一个词元训练的Transformer等浮点运算、等参数量和等词元数量条件下取得的性能改进。

    arXiv:2609.03807v1 Announce Type: cross  Abstract: A free pause token gives a language model extra compute to form each next-token prediction (as a pause, or thinking, token does) but carries that compute in a parallel prediction stream over a weight-shared backbone rather than as an extra token in the sequence. It improves next-token prediction by 2-3 centinats in practice on a 1B parameter model. Because the pause rides an existing position instead of adding one, it is free to use: at inference it adds no context length, no KV cache, and essentially no latency with the growth in inference flops typically irrelevant as it is not the active bottleneck on throughput. The only primary cost is in training, where additional training compute versus an optimized pretraining pipeline is reduced to as low as x1.14 while preserving most of the benefits. The result is an isoflop, isoparameter, and isotoken improvement over standard next token trained transformers.
    
[^452]: Dude：一种用于论文-代码差异检测的双检测多智能体系统

    Dude: A Dual-Detection Multi-Agent System for Paper-Code Discrepancy Detection

    [https://arxiv.org/abs/2609.03416](https://arxiv.org/abs/2609.03416)

    提出了首个用于论文-代码差异检测的双检测多智能体系统Dude，通过粒度对齐协商机制和两阶段显著性过滤机制，有效解决了论文语言与代码语言粒度不对称导致的误报问题。

    

    随着研究论文提交数量的增长超过了人工审阅能力，基于大语言模型（LLM）的论文-代码差异检测日益受到关注。然而，现有单智能体LLM范式的上下文容量有限且差异检测视角单一，导致差异检测的召回率表现不佳。本文提出了Dude，首个用于论文-代码差异检测的双检测多智能体系统。我们发现，论文语言与代码语言之间的粒度不对称性给差异检测的多智能体系统设计带来了过度解读和过度报告的挑战，导致误报增多。为解决这一问题，我们在Dude中提出了粒度对齐协商机制和两阶段显著性过滤机制，有效防止智能体错误报告差异。在真实世界论文-代码差异数据集上的实验结果表……

    arXiv:2609.03416v1 Announce Type: new  Abstract: LLM-empowered paper-code discrepancy detection has received growing concern since the scaling of research submissions exceeds the manual review capability. However, the limited context capacity and one-sided discrepancy detection of existing single-agent LLM paradigms lead to an inferior recall performance in detecting discrepancies. In this paper, we propose Dude, the first Dual-Detection Multi-Agent System for paper-code discrepancy detection. We discover that the granularity asymmetry of the paper-language and code-language introduces over-interpretation and over-reporting challenges in a multi-agent system design for discrepancy detection, resulting in increasing false positives. To address this, we propose a granularity-aligned negotiation and a two-stage salience-filtering mechanism in Dude, which effectively prevents agents from falsely reporting discrepancies. Experimental results in real-world paper-code discrepancy datasets sho
    
[^453]: VoxReason：合成前基于源记录的语音规划的无听者评估

    VoxReason: Listener-Free Evaluation of Source-Grounded Speech Planning Before Synthesis

    [https://arxiv.org/abs/2609.03203](https://arxiv.org/abs/2609.03203)

    VoxReason提出了一种无需听者参与的评估任务，在语音合成之前通过带证据引用的说话计划和确定性验证器，衡量语音表达方式的选择是否真正建立在被引用的源记录之上。

    

    表现力语音系统在任何波形被渲染之前就必须做出一个决定：一句话语将以何种方式被表达。在对话智能体、旁白叙述和角色条件TTS中，这一隐藏的规划步骤决定了情感、音高、能量、语速、停顿、重音和立场，然而下游音频评分很少能揭示这些选择是否由源记录所支持——这是一种在任何波形存在之前就发生的源使用失败。VoxReason将这一合成前的决策转化为可度量的、无需听者参与的任务，用于评估基于源记录的语音规划。在合成之前，VoxReason衡量话语表达方式的选择是否有被引用的源记录作为依据。系统输出带有证据引用的、注明来源的说话计划，随后一个确定性验证器检查引用合法性、槽位一致性、无支持状态、模式有效性以及单线索反事实局部性。在1,440个经过检查的源标签案例上，捷径控制实验表明了为什么仅凭槽位准确率是不安全的：一个简单的键值查找……（原文摘要在此处截断）

    arXiv:2609.03203v1 Announce Type: cross  Abstract: Expressive speech systems make a decision before any waveform is rendered: how an utterance is delivered. In dialogue agents, narration, and role-conditioned TTS, that hidden planning step sets affect, pitch, energy, rate, pause, emphasis, and stance, yet downstream audio scores rarely reveal whether those choices were licensed by the source record, a source-use failure that occurs before any waveform exists. VoxReason makes that pre-synthesis decision measurable as a listener-free task for source-grounded speech planning. Before synthesis, VoxReason measures whether delivery choices are grounded in cited source records. Systems output a source-cited speaking-plan with evidence citations, and a deterministic verifier checks citation legality, slot agreement, unsupported state, schema validity, and one-cue counterfactual locality. On 1,440 checked source-label cases, shortcut controls show why slot accuracy alone is unsafe: a key-lookup
    
[^454]: 联邦学习中的隐私泄露：面向车联边缘网络中惯性传感的基于梯度的客户端身份推断与防御

    Privacy Leakage in Federated Learning: Gradient-Based Client Identity Inference and Defenses for Inertial Sensing in Vehicular Edge Networks

    [https://arxiv.org/abs/2609.02971](https://arxiv.org/abs/2609.02971)

    该论文揭示了在车联网联邦学习中，服务器可从客户端上传的梯度更新中以近乎完美的准确率推断出基于IMU惯性数据的客户端身份，证实了严重的匿名性隐私泄露风险并提出了相应的防御方法。

    

    随着车联网向5G/6G边缘智能发展，联邦学习（FL）被广泛推广为一种隐私保护方法，使车辆和基础设施能够在不暴露原始传感器数据的情况下协同训练共享模型。然而，客户端传输的更新仍然会泄露足够的信息来识别发送者的身份，这威胁到了安全关键的V2X应用所依赖的匿名性，并加剧了人们对对抗性机器学习、模型投毒和后门攻击等已有问题的担忧。我们研究了服务器端基于传输的权重增量进行客户端身份推断的问题，使用惯性（IMU）测量数据，并在UCI人体活动识别（HAR）基准上进行评估，该基准作为车载网联车辆所产生的IMU数据流的易于获取的代理。在五种攻击分类器和五种非独立同分布（non-IID）数据划分下，诚实但好奇的服务器能够从未加防御的更新中以近乎完美的准确率（约1.000）恢复客户端身份，证实了一种具体的可识别性风险。

    arXiv:2609.02971v1 Announce Type: cross  Abstract: As vehicular networks move toward 5G/6G edge intelligence, federated learning (FL) is widely promoted as a privacy-preserving way for vehicles and infrastructure to train shared models without exposing raw sensor data. Yet the updates clients transmit still leak enough information to identify who sent them, which threatens the anonymity that safety-critical V2X applications assume and adds to existing concerns over adversarial ML, model poisoning, and backdoor attacks. We study server-side client identity inference from transmitted weight deltas using inertial (IMU) measurements, evaluated on the UCI Human Activity Recognition (HAR) benchmark as an accessible proxy for the IMU streams produced onboard connected vehicles. Across five attack classifiers and five non-IID partitions, an honest-but-curious server recovers client identity with near-perfect accuracy (approximately 1.000) from undefended updates, confirming a concrete identifi
    
[^455]: SPADE：从网联汽车视角的SPaT攻击检测

    SPADE: SPaT Attack Detection from the Connected Vehicle's Perspective

    [https://arxiv.org/abs/2609.02741](https://arxiv.org/abs/2609.02741)

    本文提出了SPADE——首个专为网联汽车车载视角下的SPaT攻击检测深度学习研究设计的带标签多模态仿真数据集，填补了入侵检测研究在SPaT消息完整性防护领域的空白。

    

    信号相位与配时（SPaT）消息是网联汽车（CV）安全的基石，使网联汽车能够通过车路协同（V2I）和车车协同（V2V）通信来感知并响应交叉口状态。当路侧单元或对等车辆被攻陷时，一系列应用层攻击可以绕过传统认证机制，从而威胁这些消息的完整性。现有的入侵检测研究要么从基础设施侧进行防御，要么针对V2V基本安全消息（BSM）/协同感知消息（CAM）的异常行为，而车载网联汽车视角下的SPaT完整性问题尚未得到解决。为填补这一空白，我们推出了SPADE——SPaT攻击检测与评估数据集——一个带标签的、多模态的、基于仿真的数据集，专为该领域的深度学习入侵检测系统（IDS）研究而设计。SPADE通过Eclipse MOSAIC生成，采用在SAE……

    arXiv:2609.02741v1 Announce Type: cross  Abstract: Signal Phase and Timing (SPaT) messages are a cornerstone of connected vehicle (CV) safety, enabling CVs to perceive and respond to intersection state through Vehicle-to-Infrastructure (V2I) and Vehicle-to-Vehicle (V2V) communication. The integrity of these messages is threatened by a range of application-layer attacks that can bypass conventional authentication when a roadside unit or peer vehicle is compromised. Existing intrusion detection research either defends the infrastructure side or targets V2V Basic Safety Message (BSM) / Cooperative Awareness Message (CAM) misbehavior, leaving the onboard CV perspective on SPaT integrity unaddressed.To close this gap, we introduce SPADE --- the SPaT Attack Detection and Evaluation dataset --- a labelled, multi-modal, simulation-based dataset designed specifically for deep learning IDS research in this space. SPADE is generated through Eclipse MOSAIC using runtime attack injection at the SAE
    
[^456]: FlashKAN：基于截断幂形式的B样条KAN

    FlashKAN: B-Spline KANs via Truncated Power Form

    [https://arxiv.org/abs/2609.01956](https://arxiv.org/abs/2609.01956)

    FlashKAN用逼近论中的截断幂形式取代Cox-de Boor递归，通过torch.compile融合为单一GPU内核并结合有界坐标稳定化，显著加速了KAN中B样条激活函数的计算，并提供了开源软件包。

    

    Kolmogorov-Arnold网络（KAN）将可学习的B样条激活函数放置在网络边上，而非在节点上使用固定激活函数。标准的Cox-de Boor递归在计算k次样条的这些激活函数时需要k次顺序传递，消耗了超过90%的前向传播时间。FlashKAN用截断幂形式取代了这种递归，这是逼近论中的一个经典结果，它将每个均匀三次B样条表示为在移位节点位置上的五个(x)_+^3项。本文做出了三项贡献：(1) 一个torch.compile融合的实现，将这些操作合并为单个GPU内核，消除了所有递归、跨度查找和散布-聚集操作；(2) 一种有界坐标稳定化方法，将归一化输入钳制到[0, k+1]，防止了历史上促使Cox-de Boor递归被采用的灾难性抵消问题；(3) 一个可用于生产环境的开源软件包（pip install flashkan）。

    arXiv:2609.01956v1 Announce Type: new  Abstract: Kolmogorov-Arnold Networks (KANs) place learnable B-spline activations on network edges rather than fixed activations on nodes. The standard Cox-de Boor recursion evaluates these activations through k sequential passes for degree-k splines, consuming over 90% of forward-pass time. FlashKAN replaces this recursion with the truncated power form, a classical result from approximation theory that expresses each uniform cubic B-spline as five (x)_+^3 terms at shifted knot positions. This paper makes three contributions: (1) a torch.compile-fused implementation that collapses these operations into a single GPU kernel, eliminating all recursion, span lookup, and scatter-gather operations; (2) a bounded-coordinate stabilization that clamps the normalized input to [0, k+1], preventing the catastrophic cancellation that historically motivated the Cox-de Boor recursion; and (3) a production-ready, open-source package (pip install flashkan) that ser
    
[^457]: 延迟老虎机中的池化与漂移

    Pooling and Drift in Delayed Bandits

    [https://arxiv.org/abs/2609.01761](https://arxiv.org/abs/2609.01761)

    该论文发现当延迟老虎机的反馈结果仅通过动作所产生的状态依赖于动作时，学习代价由有效维度（真正不同的状态数量）而非动作数量决定，并据此证明了 $\widetilde{O}(\sqrt{(d+1)V\log K})$ 等新的遗憾界，突破了以往随动作数增长的界限。

    

    系统常常不得不在得知行动是否奏效之前就采取行动：推荐系统几秒内就能观察到点击，而购买则要几天后才能看到。在 $K$ 个动作、延迟 $d$ 轮的设定下，$T$ 轮内已知的最优遗憾率为 $\widetilde{O}(\sqrt{(K+d)T})$，因此可选动作越多，学习代价就越高。但事实未必如此：如果结果仅通过动作所产生的状态依赖于动作，那么一个迟到的结果就能为所有可能产生该观测状态的动作提供信息，此时代价由动作产生的真正不同的状态数量决定，而非动作的数量。我们用介于 $1$ 与状态总数之间的有效维度 $v_t$ 来度量这一概念，并针对任意预先固定的预算证明：对于轮转算法，遗憾率为 $\widetilde{O}(\sqrt{(d+1)V\log K})$；对于实践中常用的单副本算法，遗憾率为 $\widetilde{O}(\sqrt{V^{-}}+\sqrt{dT})$；合并相似状态可以进一步降低这一代价。

    arXiv:2609.01761v1 Announce Type: cross  Abstract: A system often has to act long before it learns whether the act worked: a recommender sees a click in seconds and a purchase in days. With $K$ actions and a delay of $d$ rounds, the best rate known for this setting is $\widetilde{O}(\sqrt{(K+d)T})$ over $T$ rounds, so a longer menu is always more expensive to learn from. It need not be: if the outcome depends on the action only through the state it produced, then one late outcome informs every action that could have produced the observed state, and the price is set by how many genuinely different states the actions produce rather than by how many actions there are. We measure this using an effective dimension $v_t$ between $1$ and the number of states, and prove $\widetilde{O}(\sqrt{(d+1)V\log K})$ for a rotating algorithm and $\widetilde{O}(\sqrt{V^{-}}+\sqrt{dT})$ for the single-copy algorithm used in practice, for any budget fixed in advance; merging similar states lowers the price 
    
[^458]: PRISM：面向自动驾驶交通系统主动安全的智能体多模型架构

    PRISM: An Agentic Multi-Model Architecture for Proactive Safety in Autonomous Transportation Systems

    [https://arxiv.org/abs/2609.01623](https://arxiv.org/abs/2609.01623)

    PRISM是一种智能体多模型安全架构，通过逆向事故概率建模将事故分类器转化为可解释的动态安全评分，并由推理层协调三个并发专门模型（轨迹运动学、环境风险、VRU交互），实现从被动事故规避到主动持续风险管理的转变。

    

    自动驾驶和智能交通系统在复杂的城市环境中运行，其安全性取决于车辆行为、环境条件以及行人、骑行者等弱势道路使用者（VRU）之间的交互。大多数高级驾驶辅助系统（ADAS）采用被动反应机制，只有在危险出现后才被激活——美国VRU死亡人数的持续上升凸显了这一关键局限。本研究提出了PRISM（Proactive Risk Intelligence and Safety Management，主动风险智能与安全管理），这是一种智能体多模型安全架构，实现了从被动的事故规避向主动、持续的风险管理的范式转变。PRISM采用逆向事故概率建模，将二分类事故分类器转换为动态、可解释的安全评分。三个专门化模型分别处理轨迹运动学、环境风险和VRU交互，三者并发运行，由一个融合……的推理层进行统一协调。

    arXiv:2609.01623v1 Announce Type: cross  Abstract: Autonomous and intelligent transportation systems operate in complex urban environments where safety depends on interactions among vehicle behavior, environmental conditions, and vulnerable road users (VRUs) such as pedestrians and cyclists. Most advanced driver assistance systems (ADAS) employ reactive mechanisms that activate only after hazards have emerged, a critical limitation underscored by rising VRU fatalities in the United States.   This study introduces PRISM (Proactive Risk Intelligence and Safety Management), an agentic multi-model safety architecture that transitions from reactive crash avoidance to proactive, continuous risk management. PRISM employs inverse crash-probability modeling to convert binary crash classifiers into dynamic, interpretable safety scores. Three specialized models addressing trajectory kinematics, environmental risk, and VRU interaction operate concurrently, coordinated by a reasoning layer incorpor
    
[^459]: SMELT：计算匹配的混合专家循环Transformer缩放定律

    SMELT: Scaling Laws for Compute-Matched MoE Looped Transformers

    [https://arxiv.org/abs/2609.01343](https://arxiv.org/abs/2609.01343)

    SMELT在严格匹配计算量、参数量和KV缓存的条件下将MoE Transformer的中间层循环两次，其损失下降更快，可在计算最优前沿节省6.8%–18.0%的训练FLOPs，且在代码任务和长样本上优势更明显。

    

    循环Transformer通过迭代共享的层块来增加有效深度，但大多数评估是在固定模型大小下进行比较的，这混淆了架构优势与额外的浮点运算量（FLOPs）。我们在混合专家Transformer上研究循环机制，同时严格匹配每token的FLOPs、总非嵌入参数量以及KV缓存。通过一系列消融实验，我们得出了一种名为SMELT（稀疏MoE Transformer，中间层循环两次）的设计方案，它将中间一半的层循环两次，同时在所有三项预算上与非循环基线相匹配。我们将SMELT扩展到四个规模，最大达540亿非嵌入参数，并为每种架构分别拟合了Chinchilla风格的缩放定律。SMELT的损失随计算量增加下降得更快，在计算最优前沿上可节省6.8%–18.0%的训练FLOPs。这种优势在下游基准测试中的表现超出了验证损失所能预测的程度，在代码任务上最为显著，并随样本长度和迭代次数的增加而增长。

    arXiv:2609.01343v1 Announce Type: new  Abstract: Looped Transformers increase effective depth by iterating a shared block of layers, but most evaluations compare at fixed model size, conflating architectural advantage with extra FLOPs. We study looping on Mixture-of-Experts Transformers while closely matching per-token FLOPs, total non-embedding parameters, and KV cache. Through a series of ablations, we arrive at a recipe we call SMELT (Sparse MoE Transformer, middle layers Loop Twice), which loops the middle half of layers twice while matching the unlooped Baseline on all three budgets. We scale SMELT across four sizes up to 54B non-embedding parameters and fit a separate Chinchilla-style scaling law for each architecture. SMELT's loss drops faster with compute, saving 6.8--18.0\% of training FLOPs on the compute-optimal frontier. The advantage transfers to downstream benchmarks beyond what validation loss predicts, is largest on Code, and grows with sample length and the number of i
    
[^460]: EEG-VID：面向脑电解码与辅助目标选择的任务引导式潜变量预测预训练

    EEG-VID: Task-Guided Latent Predictive Pretraining for EEG Decoding and Assistive Target Selection

    [https://arxiv.org/abs/2609.00566](https://arxiv.org/abs/2609.00566)

    EEG-VID提出了一种任务引导式潜变量预测预训练框架，通过指数移动平均编码器预测未来EEG潜状态，在42组跨会话跨被试对比中有41组提升准确率（最高提升16.22个百分点），并可有效应用于场景约束下的辅助目标选择。

    

    我们提出EEG-VID，这是一个面向跨会话与跨被试脑电解码的任务引导式潜变量预测预训练框架。EEG-VID利用指数移动平均目标编码器与弱任务引导，从近期的EEG历史预测未来的潜变量EEG状态，随后进行有监督微调。在VIG-48和BCI竞赛IV-2a/IV-2b数据集上，第一阶段预训练在42组匹配的骨干网络-数据集-协议对比中有41组提升了平均准确率，包括全部12个留一被试设置，最大提升达16.22个百分点。在48区域的跨天VIG-48任务上，EEG-VID实现了6.52%的Top-1准确率和30.50%的Top-5准确率。在一项独立的六名被试离线机器人场景研究中，经过被试特定校准后，候选约束下的目标选择准确率达到40.24%，而随机水平仅为25%。这些结果支持任务引导的潜变量预测作为脑电解码与场景约束辅助目标选择的一种可迁移预训练策略。

    arXiv:2609.00566v1 Announce Type: cross  Abstract: We propose EEG-VID, a task-guided latent predictive pretraining framework for EEG decoding under session and subject shifts. EEG-VID predicts future latent EEG states from recent history using an exponential-moving-average target encoder and weak task guidance, followed by supervised fine-tuning. Across VIG-48 and BCI Competition IV-2a/IV-2b, Stage 1 improves mean accuracy in 41 of 42 matched backbone-dataset-protocol comparisons, including all 12 leave-one-subject-out settings, with a maximum gain of 16.22 percentage points. On the 48-region cross-day VIG-48 task, EEG-VID achieves 6.52% Top-1 and 30.50% Top-5 accuracy. In a separate six-participant offline robot-scene study, candidate-constrained target selection reaches 40.24% versus a 25% chance level after subject-specific calibration. These results support task-guided latent prediction as a transferable pretraining strategy for EEG decoding and scene-constrained assistive target s
    
[^461]: 一种用于检测能源价格序列中事件驱动动态的卷积框架

    A convolutional framework for detecting event-driven dynamics in energy price series

    [https://arxiv.org/abs/2609.00402](https://arxiv.org/abs/2609.00402)

    本文提出了一个通用卷积神经网络框架，可精确表示并逼近多种事件检测统计量（如极差、最大回撤、波动率等），并在能源价格序列中有效识别出以地缘政治为主的事件驱动动态。

    

    本文开发了一个通用的卷积神经网络（CNN）框架，用于检测单变量时间序列窗口中异质的事件驱动动态。我们证明该CNN类能够精确表示基于极差、最大上涨、最大回撤和斜率变化的分类器，并在紧集上一致逼近已实现波动率和自回归爆炸性。我们进一步为有限样本中的代表性规则建立了误差界，并为跨规则学习建立了预言机不等式。模拟结果表明，随着训练样本的增长，所提出的模型能够匹配或超越基于单一统计量的分类器。在应用于六个日度能源价格序列时，层次化CNN能够区分事件窗口和事件族。在无需重新训练的情况下，将该模型应用于2026年2月20日之后保留的观测数据，其成功在多种石油和成品油价格中识别出以地缘政治为主的事件驱动动态。

    arXiv:2609.00402v1 Announce Type: new  Abstract: This paper develops a general convolutional neural network (CNN) framework for detecting heterogeneous event-driven dynamics in univariate time series windows. We show that the induced CNN class exactly represents classifiers based on range, maximum drawup, maximum drawdown and slope change, and uniformly approximates realised volatility and autoregressive explosiveness on compact domains. We further establish error bounds for representative rules in finite samples and an oracle inequality for learning across them. Simulations show that the proposed model can match or outperform classifiers based on individual statistics as the training sample grows. In an application to six daily energy price series, a hierarchical CNN distinguishes event windows and event families. Applied without retraining to observations withheld after 20 February 2026, the fitted model identifies predominantly geopolitical dynamics in several oil and refined produc
    
[^462]: 面向腐蚀可靠机理推理的生成式人工智能

    Generative artificial intelligence for reliable mechanistic reasoning for corrosion

    [https://arxiv.org/abs/2609.00099](https://arxiv.org/abs/2609.00099)

    该研究提出了一个面向腐蚀领域的检索增强生成框架，通过对三个开源大语言模型在专家验证的问答数据上进行微调并结合混合检索流水线，实现了兼具准确性与机理可靠性的腐蚀知识推理，在镁合金腐蚀上取得了显著效果。

    

    腐蚀造成的损失约占全球GDP的4%，可靠的预测对于及时采取防护措施至关重要。机器学习虽然能够基于成分、微观组织和环境变量有效预测腐蚀速率，但无法解释其背后的机理。在安全攸关的材料工程领域，可靠的方法不仅需要准确的检索能力，还需要机理上站得住脚的推理能力，而现有的真实性评估指标无法评价这一能力。本工作提出了一个针对腐蚀领域知识综合的领域自适应检索增强生成框架，并以镁合金腐蚀为例进行了演示。研究者在来自840篇同行评审论文的3,309个经专家验证的问答对上，对三个开源权重语言模型进行了微调，并将其与密集-词法混合检索流水线相结合。检索增强带来了143-194%的Token F1提升，系统的忠实度…

    arXiv:2609.00099v1 Announce Type: new  Abstract: Corrosion accounts for approximately 4% of global GDP, and reliable prediction is essential for timely mitigation. Machine learning effectively predicts corrosion rates from composition, microstructure, and environmental variables, but cannot explain the underlying mechanisms. A reliable approach in safety-critical materials engineering requires not only accurate retrieval but also mechanistically defensible reasoning, a capability that existing factuality metrics cannot assess. This work presents a domain-adapted retrieval-augmented generation framework for corrosion knowledge synthesis, demonstrated on magnesium alloy corrosion. Three open-weight language models (Llama-3.1-8B, Qwen-2.5-7B, Mistral-7B) are fine-tuned on 3,309 expert-verified question-answer pairs from 840 peer-reviewed papers and integrated with a hybrid dense-lexical retrieval pipeline. Retrieval augmentation produces Token F1 gains of 143-194%, with system faithfulnes
    
[^463]: 基于证据权重评估特征重要性解释的对齐性与稳定性

    Assessing Alignment and Stability of Feature Importance Explanations via Weight of Evidence

    [https://arxiv.org/abs/2609.00090](https://arxiv.org/abs/2609.00090)

    该论文提出了一个基于证据权重的假设检验框架，能够从原理上评估特征重要性解释方法与先验知识的对齐程度及其稳定性，并将其应用于LIME和SHAP的分析中。

    

    特征重要性方法（FIMs）被广泛应用于可解释人工智能中，用于解释模型的预测结果，然而仅凭归因分数往往难以深入洞察模型背后的推理过程。在本工作中，我们引入了一种新颖的视角，将特征重要性方法嵌入到基于证据权重的假设检验框架中。我们量化了观测到的证据对任何给定的特征重要性假设的支持强度。其中，参考假设可以来源于领域知识、真实标签，或由特征重要性方法本身推导得出。这一表述方式使得对特征重要性方法的原理化评估成为可能，既能捕捉它们与先验知识的对齐程度，也能捕捉其变异性。我们进一步提供了将证据权重与归因方差相联系的理论结果。实证结果表明，在具有不同参考假设的设置下分析LIME和SHAP解释时，我们的策略展现出良好的适用性与灵活性。总体而言，我们的框架提供了一个互补的……

    arXiv:2609.00090v1 Announce Type: cross  Abstract: Feature importance Methods (FIMs) are widely used in Explainable AI to interpret model predictions, yet attribution scores alone often provide limited insight into the underlying reasoning process. In this work, we introduce a novel perspective by embedding FIMs within a hypothesis-testing framework based on Weight of Evidence (WoE). We quantify how strongly the observed evidence supports any given hypothesis on feature importance. The reference hypothesis can stem from domain knowledge, ground truth, or be derived from the FIM itself. This formulation enables a principled evaluation of FIMs, capturing both their alignment with prior knowledge and their variability. We further provide theoretical results linking WoE to attribution variance. Empirical results shows the applicability and flexibility of our strategy analyzing LIME and SHAP explanations in settings with different reference hypotheses. Overall, our framework offers a comple
    
[^464]: 当预测误差不足够时：评估用于因果估计的干扰函数预测

    When Prediction Error Is Not Enough: Evaluating Nuisance-Function Prediction for Causal Estimation

    [https://arxiv.org/abs/2609.00071](https://arxiv.org/abs/2609.00071)

    在部分线性模型的模拟研究中，干扰函数的预测误差无法一致地反映因果估计的偏差，表明仅用预测误差来评估干扰函数估计器对于因果推断而言是不充分的。

    

    预测误差被广泛用于评估因果推断中干扰函数估计器的性能，但其与因果估计器表现之间的关系可能因性能度量指标的不同而存在差异。我们在部分线性模型中通过蒙特卡洛模拟研究了这一问题。我们比较了普通最小二乘法（OLS）、广义可加模型（GAMs）、XGBoost 以及结合 XGBoost 的双重机器学习（DML-XGBoost），评估指标包括干扰函数预测误差、偏差、均方根误差（RMSE）以及95%置信区间覆盖率。我们还检验了一种简单的联合误差度量，其基于暴露干扰函数与结局干扰函数估计误差的绝对交叉乘积。在所有模拟设置中，XGBoost 在非先知方法中具有最低的 RMSE，而 DML-XGBoost 通常能提供更好的置信区间覆盖率。预测误差在不同方法和设置下并不能一致地反映因果估计的偏差，且点估计表现最好的方法（摘要在此处截断）

    arXiv:2609.00071v1 Announce Type: new  Abstract: Prediction error is widely used to evaluate nuisance-function estimators in causal inference, but its relationship with causal estimator performance may differ across performance measures. We studied this question in a partially linear model using Monte Carlo simulations. We compared ordinary least squares (OLS), generalized additive models (GAMs), XGBoost, and Double Machine Learning with XGBoost (DML-XGBoost), evaluating nuisance-function prediction error, bias, RMSE, and 95\% confidence interval coverage. We also examined a simple joint-error measure based on the absolute cross-product of estimation errors from the exposure and outcome nuisance functions. Across the simulated settings, XGBoost had the lowest RMSE among the non-oracle methods, while DML-XGBoost generally provided better confidence interval coverage. Prediction error did not consistently track causal bias across methods and settings, and the method with the best point-e
    
[^465]: ReNFT：通过内部概率质量重新校准修复奖励后训练中的模式坍塌

    ReNFT: Repairing Mode Collapse in Reward Post-Training via Internal Probability-Mass Recalibration

    [https://arxiv.org/abs/2609.00061](https://arxiv.org/abs/2609.00061)

    本文提出ReNFT方法，通过内部概率质量重新校准修复扩散模型奖励后训练中出现的模式坍塌，在保留已获得奖励的同时恢复提示内多样性，无需依赖任何外部信号或接口。

    

    扩散生成器的奖励后训练不可避免地将概率质量集中在少数受奖励偏好的模式上，这种模式坍塌消除了提示内的多样性。现有的缓解坍塌方法依赖于外部信号或接口，例如用感知目标增强奖励、调整参考正则化或修改文本编码器，但没有一种方法能够在保留已获得奖励的同时修复已经坍塌的适配器。我们观察到，在线后训练主要是在预训练继承的能力之上重新分配概率质量，而不是学习新的视觉内容。因此，坍塌是抑制而非删除，可以从生成器内部逆转。我们提出ReNFT，通过内部概率质量重新校准来修复高奖励、低多样性的适配器。无条件探测首先优先处理“反中心”提示，在这些提示中，与提示无关的偏差最容易暴露。

    arXiv:2609.00061v1 Announce Type: cross  Abstract: Reward post-training of diffusion generators inevitably concentrates probability mass on a few reward-favored modes, a mode collapse that erases within-prompt diversity. Existing methods for mitigating collapse rely on external signals or interfaces, augmenting the reward with perceptual objectives, adjusting reference regularization, or modifying the text encoder, but none repairs an adapter that has already collapsed while preserving the acquired reward. We observe that online post-training primarily reallocates probability mass over capabilities inherited from pretraining rather than learning new visual content. Collapse is therefore suppression, not deletion, and can be reversed from within the generator. We propose ReNFT, which repairs a high-reward, low-diversity adapter through internal probability-mass recalibration. Unconditional probes first prioritize "anti-hub" prompts where the prompt-independent bias is easiest to expose.
    
[^466]: CoJEPA：结合对比学习与JEPA实现全局-局部音乐表征

    CoJEPA: Combining Contrastive Learning and JEPA for Global-Local Music Representations

    [https://arxiv.org/abs/2608.30974](https://arxiv.org/abs/2608.30974)

    CoJEPA将对比学习与JEPA联合训练于单一共享骨干网络中，利用对比梯度取代EMA教师以稳定训练，同时通过JEPA的局部预测丰富序列标记，在不增加任何额外参数的情况下学习全局-局部音乐表征。

    

    联合嵌入预测架构（JEPA）通过在潜空间中进行自监督预测来学习丰富的表征，已展现出强大的性能。然而，它通常依赖于带有指数移动平均（EMA）的教师-学生架构来稳定训练，并且可能倾向于产生缺乏信息量的表征。对比学习训练稳定并能产生强大的全局表征，但由于其目标函数的全局性质，在局部任务上仍然受限。在本工作中，我们将两者结合形成CoJEPA：一个单一共享的骨干网络，在掩码序列标记上以JEPA目标、在类标记上以对比目标进行联合训练。对比梯度提供了训练稳定性，完全消除了对EMA教师的需求，而JEPA则通过对比学习本身无法提供的局部预测来丰富序列标记。至关重要的是，骨干网络没有添加任何额外参数：同一个模型被引导学习更丰富的表征。

    arXiv:2608.30974v1 Announce Type: cross  Abstract: Joint-Embedding Predictive Architecture (JEPA) has shown strong performance in learning rich representations through self-supervised prediction in latent space. However, it typically relies on teacher--student architecture with an EMA to stabilise training, and can tend to yield uninformative representations. Contrastive learning is stable to train and produces strong global representations, but remains limited on local tasks by the global nature of its objective. In this work, we combine both into CoJEPA: a single shared backbone jointly trained with a JEPA objective on masked sequence tokens and a contrastive objective on the class token. The contrastive gradient provides stability, removing the need for an EMA teacher entirely, while JEPA enriches the sequence tokens via local predictions that contrastive learning alone cannot provide. Crucially, no extra parameters are added to the backbone: the same model is guided towards richer 
    
[^467]: ReLU 训练中的奇异曲率：微分与梯度流极限不必交换

    Singular Curvature in ReLU Training:Differentiation and the Gradient-Flow Limit Need Not Commute

    [https://arxiv.org/abs/2608.30960](https://arxiv.org/abs/2608.30960)

    该论文证明在 ReLU 训练中“先微分离散梯度下降再取极限”与“先取梯度流极限再微分”不可交换：离散精确导数收敛到无事件的区域传播子，而极限流的导数额外包含速度归一化的激活事件转移，由此产生秩一的奇异曲率差异，且全局凸性也无法将其完全抵消。

    

    梯度下降（GD）是梯度流的显式欧拉离散化，但一个在状态上精确的连续时间替代模型在微分之后未必仍然精确。在每个固定的非共振步长下，普通的自动微分可以精确地微分所执行的硬 ReLU GD 程序。我们证明，在固定的有限时间范围内，GD 状态收敛，并且这些精确的离散导数趋近于一个无事件的区域传播子，而极限流的导数还额外包含速度归一化的激活事件转移。一个前点 Stieltjes 表示将绝对连续的区域 Hessian 与原子界面曲率分离开来；一个非零梯度跳变会产生恰好秩一的端点差异，并且只要某个事件是严格的，全局凸性就会阻止多个事件之间的完全抵消。然而，一个标准的全局 1-强凸的残差-ReLU 平方损失风险族可以实现任意大的 r……（原文摘要在此处截断）

    arXiv:2608.30960v1 Announce Type: new  Abstract: Gradient descent (GD) is explicit Euler for gradient flow, but a state-accurate continuous-time surrogate need not remain accurate after differentiation. At every fixed nonresonant step size, ordinary automatic differentiation exactly differentiates the executed hard-ReLU GD program. We prove that, over a fixed finite horizon, the GD states converge and these exact discrete derivatives approach an event-free regional propagator, whereas the derivative of the limiting flow also contains speed-normalized activation-event transfers. A prepoint Stieltjes representation separates the absolutely continuous regional Hessian from atomic interface curvature; one nonzero gradient jump produces an exactly rank-one endpoint discrepancy, and global convexity prevents complete multi-event cancellation whenever an event is strict. Nevertheless, a standard family of globally 1-strongly convex residual-ReLU squared-loss risks realizes arbitrarily large r
    
[^468]: 粗粒度组成特征足矣：用于多活性抗菌肽谱分析的表格上下文学习

    Coarse composition suffices: tabular in-context learning for multi-activity antimicrobial peptide profiling

    [https://arxiv.org/abs/2608.30337](https://arxiv.org/abs/2608.30337)

    该研究证明，仅用330个可解释序列描述符结合无需梯度训练的TabPFN表格基础模型，即可在ESCAPE多标签抗菌肽活性预测基准上超越复杂的多模态深度模型，将mAP-5从72.1%提升至77.8%。

    

    抗菌肽（AMPs）通常对多种病原体类别具有活性，这使得多标签活性预测比二元抗菌分类更符合实际的筛选目标。ESCAPE基准将这一设定形式化，但领先的方法通常依赖于多模态、结构条件化的深度模型，其训练和调优成本高昂。我们证明，一个简单的、仅基于序列的流程可以通过将330个可解释的序列描述符与TabPFN（一种表格基础模型，可在单次前向传播中完成上下文内预测，无需基于梯度的训练或超参数搜索）相结合，来匹配并超越这些方法。在ESCAPE基准（82,359条肽；五个标签）上，标签幂集（label-powerset）TabPFN模型实现了mAP-5 = 77.8%，优于此前报告的最佳结果72.1%。概率分类器链方法则首次在全部五个标签上同时达到或超过已发表的最佳平均精度。

    arXiv:2608.30337v1 Announce Type: new  Abstract: Antimicrobial peptides (AMPs) often act against multiple pathogen classes, making multi-label activity prediction a more realistic screening target than binary antimicrobial classification. The ESCAPE benchmark formalizes this setting, but leading approaches typically rely on multimodal, structure-conditioned deep models that are costly to train and tune. We show that a simple, sequence-only pipeline can match and surpass these methods by combining 330 interpretable sequence descriptors with TabPFN, a tabular foundation model that performs in-context prediction in a single forward pass without gradient-based training or hyperparameter search. On ESCAPE (82,359 peptides; five labels), a label-powerset TabPFN model achieves mAP-5 = 77.8%, improving on the previously best reported 72.1%. A probabilistic classifier chain is the first method to match or exceed the best published average precision on each of the five labels simultaneously. The
    
[^469]: Dec-BFTRL：分离访问下去中心化在线上可线性化优化的平方根遗憾及其在连续次模最大化中的应用

    Dec-BFTRL: Squre-Root Regret for Decentralized Online Upper-Linearizable Optimization under Separation Access with Application to Continuous Submodular Maximization

    [https://arxiv.org/abs/2608.30271](https://arxiv.org/abs/2608.30271)

    提出去中心化障碍正则化跟随领导者算法 Dec-BFTRL，在分离访问条件下对去中心化在线上可线性化优化实现了 $\widetilde O(\sqrt{T})$ 的平方根遗憾界，并应用于连续 DR 次模最大化问题。

    

    我们研究在高效分离访问条件下，动作集上可上线性化收益的去中心化在线优化问题，其应用涵盖在线连续收益递减（DR）次模最大化。我们提出去中心化障碍正则化跟随领导者算法（Dec-BFTRL），并将每个智能体所执行的动作与所有局部目标的平均值进行对比评估。每个智能体通过近似规范投影将内部迭代点映射为可行动作，仅通信累积的代理梯度对偶状态，并调用本地 HybridNewton 过程来近似最小化通信后的 BFTRL 势函数。对于每个智能体，我们实现了期望网络聚合遗憾为 $\widetilde O(\sqrt{T})$。在 $T$ 轮过程中，每个智能体仅需 $T$ 步邻居混合和 $\widetilde O(T)$ 次分离预言机调用。我们给出了四个包装器实例化，覆盖三个 DR 次模最大化问题。

    arXiv:2608.30271v1 Announce Type: cross  Abstract: We study decentralized online optimization of upper-linearizable payoffs over an action set under efficient separation access, with applications to online continuous diminishing-return (DR) submodular maximization. We propose Decentralized Barrier Follow-the-Regularized-Leader (Dec-BFTRL), and evaluate each agent's played action against the average of all local objectives. Each agent maps an internal iterate to a feasible action through an approximate gauge projection, communicates only a cumulative surrogate-gradient dual state, and invokes the local HybridNewton procedure to approximately minimize its post-communication BFTRL potential. For every agent, we achieve expected network-aggregate regret of $\widetilde O(\sqrt{T})$. Over $T$ rounds, each agent uses $T$ neighbor-mixing steps and $\widetilde O(T)$ separation-oracle calls. We give four wrapper instantiations covering three DR-submodular maximization problems.
    
[^470]: 基准的平衡：面向基准多重性与任务条件评估的语义密度重加权

    Balance of Benchmarks: Semantic Density Reweighting for Benchmark Multiplicity and Task-Conditioned Evaluation

    [https://arxiv.org/abs/2608.30044](https://arxiv.org/abs/2608.30044)

    提出基准平衡方法，通过对基准分配逆密度语义权重来纠正基准密集区域的重复计算问题，并支持基于任务查询的条件化模型评估，在586个模型上的留出任务预测相关性从等权加权的0.049大幅提升至0.462。

    

    语言模型通常通过对基准列表中的分数进行等权平均来进行比较。这类列表通过公开发表而不断增长，缺乏显式的测量设计，因此等权加权使已发表基准的密度成为隐含的能力权重：基准密集的区域会被重复计算。我们提出了基准平衡方法，该方法对基准描述进行嵌入，并为每个基准分配逆密度语义权重。在公开的密度尺度下，相近的基准条目共享聚合影响力。在将异构分数统一到共同潜在尺度后，残差场利用相同的几何结构，使模型排名能够以任务查询为条件。这两个组成部分在实证上发挥着不同的作用。在包含586个模型和14个基准的数据快照上，BoB能够预测哪些模型在留出任务上表现出超出其一般能力的异常强势，画像相关性达到0.462，而等权加权下仅为0.049。

    arXiv:2608.30044v1 Announce Type: new  Abstract: Language models are commonly compared by averaging scores across a benchmark list with equal weight. Such lists grow through publication outside an explicit measurement design, so equal weighting turns the density of published benchmarks into an implicit capability weight: densely benchmarked regions count repeatedly. We introduce Balance of Benchmarks (BoB), which embeds benchmark descriptions and assigns each benchmark an inverse-density semantic weight. Nearby entries share aggregate influence at a disclosed density scale. After equating heterogeneous scores onto a common latent scale, a residual field uses the same geometry to condition model rankings on a task query. The two components serve distinct empirical roles. On a snapshot of 586 models and 14 benchmarks, BoB predicts which models are unusually strong on a held-out task beyond their general ability, reaching a profile correlation of 0.462 compared with 0.049 under equal weig
    
[^471]: PET/CT放射学报告错误检测：领域专用模型与大语言模型的对比

    Error Detection for PET/CT Radiology Reports: Domain-Specific vs Large Language Models

    [https://arxiv.org/abs/2608.30021](https://arxiv.org/abs/2608.30021)

    该研究首次系统评估了语言模型在PET/CT放射报告错误检测中的表现，结果表明仅1500万参数的领域专用BERT模型即可达到94.4%的平衡准确率，可与更大的开源大语言模型相媲美。

    

    放射学报告中的错误可能对患者治疗产生不利影响，然而自动化的报告质量保证仍然充满挑战，因为错误往往很细微，需要领域专业知识才能发现。尽管大语言模型（LLMs）最近被提出用于放射学报告验证，但它们在胸部X光数据集之外检测具有临床意义错误的能力仍未得到充分探索。为此，我们首次对用于PET/CT报告错误检测的语言模型进行了系统性评估，将紧凑的领域专用模型与最先进的开源权重LLM进行比较。我们收集了23位放射科医生在10年间撰写的30,633份肿瘤学FDG PET/CT报告。我们训练了领域专用的BERT模型来检测基于临床动机合成的报告错误，并将其与零样本/少样本的Qwen3-32B、Gemma-3-27B和Llama-3.3-70B在包含11,500份报告的留出基准数据集上进行了评估。一个1500万参数的模型达到了94.4%的平衡准确率……

    arXiv:2608.30021v1 Announce Type: cross  Abstract: Errors in radiology reports can adversely affect patient treatment, yet automated report quality assurance remains challenging because errors are often subtle and require domain expertise to detect. Although large language models (LLMs) have recently been proposed for radiology report verification, their ability to detect clinically meaningful errors beyond chest X-ray datasets remains under-explored. To this end, we present the first systematic evaluation of language models for PET/CT report error detection, comparing compact domain-specific models with SOTA open-weight LLMs. We collected 30,633 oncology FDG PET/CT reports from 23 radiologists over 10 years. We trained domain-specific BERT models to detect clinically motivated synthetic reporting errors and evaluated alongside zero-/few-shot Qwen3-32B, Gemma-3-27B and Llama-3.3-70B on a held-out benchmark of 11,500 reports. A 15M-parameter model achieved 94.4% balanced accuracy with a
    
[^472]: ARMOR：面向数据稀缺场景下对抗鲁棒航空目标检测的流形导向训练

    ARMOR: Manifold-Oriented Training for Adversarially Robust Aerial Object Detection under Data Scarcity

    [https://arxiv.org/abs/2608.29510](https://arxiv.org/abs/2608.29510)

    提出了ARMOR方法，通过流形导向的对抗训练，在训练数据仅有数百张图像的稀缺条件下，实现对物理通用对抗补丁攻击具有鲁棒性的航空目标检测。

    

    航空目标检测正日益被部署到实际应用中，但模型仍然容易受到物理的、通用对抗补丁的攻击，这些补丁会导致模型漏检目标。此外，防御者还面临训练数据稀缺的实际约束：航空图像的采集和标注成本高昂，因此一个部署站点通常只能提供数百张图像，而非对抗鲁棒性基准测试所假设的数万张图像。为了解决模型脆弱性和训练数据稀缺这两个问题，我们提出了基于流形导向训练的对抗鲁棒性方法（ARMOR），这是一种新颖的防御方法，在低数据量场景下实现了流形上对抗训练（OMAT）的核心思想。ARMOR 建立在 OMAT 的洞察之上，即对数据流形——捕捉数据相关特征的紧凑结构——进行建模，从而在训练过程中学习并增强这些特征的鲁棒性。尽管 OMAT 依赖于训练大型生成……（原文摘要在此处截断）

    arXiv:2608.29510v1 Announce Type: cross  Abstract: Aerial object detection is increasingly deployed in real-world applications, but models remain vulnerable to physical, universal adversarial patches that cause them to miss objects. Furthermore, defenders face the practical constraint of training data scarcity: aerial imagery is costly to collect and label, so a deployment site typically yields hundreds of images rather than the tens of thousands that adversarial robustness benchmarks assume. To tackle model vulnerability and training data scarcity, we propose Adversarial Robustness with Manifold-Oriented Training (ARMOR), a novel defense that realizes the core insights of on-manifold adversarial training (OMAT) in low-data regimes. ARMOR builds on the insight of OMAT to model the data manifold - the compact structure capturing the data's relevant features - to learn and robustify these features during training. While OMAT relies on the data-intensive operations of training large gener
    
[^473]: PathGuide：基于在线策略传输对齐的动态无分类器引导

    PathGuide: Dynamic Classifier-Free Guidance via On-Policy Transport Alignment

    [https://arxiv.org/abs/2608.29107](https://arxiv.org/abs/2608.29107)

    提出PathGuide框架，将无分类器引导的强度选择从静态参数调整为在线策略传输问题，利用连续性方程的弱形式证明了当引导场与精确条件场弱等价时采样路径与目标条件分布一致，从而实现动态的引导优化。

    

    尽管现代生成模型在建模复杂数据方面表现出色，但在条件生成中实现精确的推理时控制仍然是一个关键挑战。无分类器引导（CFG）是实现此类控制的主要机制，然而它通常被视为一个静态的调节参数。然而，在基于流的模型中，引导强度从根本上决定了速度场以及由此产生的概率路径，这使得引导选择成为一个动态的路径优化问题。我们提出了PathGuide，一个将标量CFG选择重新表述为在线策略传输问题的框架。利用连续性方程的弱形式，我们推导出了一个具有直接路径正确性解释的选择准则：我们证明，如果引导场沿着生成的轨迹与精确的条件场弱等价，那么采样器的路径将与目标条件分布律重合。对于标量CFG，该准则产生了一个严格二次的……（原文摘要在此处被截断）

    arXiv:2608.29107v1 Announce Type: new  Abstract: While modern generative models excel at modeling complex data, precise inference-time control in conditional generation remains a critical challenge. Classifier-free guidance (CFG) is a primary mechanism for such control, yet it is typically treated as a static tuning parameter. In flow-based models, however, the guidance scale fundamentally dictates the velocity field and the resulting probability path, making guidance selection a dynamic path-optimization problem. We introduce PathGuide, a framework that reformulates scalar CFG selection as an on-policy transport problem. Leveraging the weak form of the continuity equation, we derive a selection criterion with a direct path-correctness interpretation: we prove that if the guided field is weakly equivalent to the exact conditional field along the generated rollout, the sampler's path coincides with the target conditional law. For scalar CFG, this criterion yields a strictly quadratic lo
    
[^474]: 欧几里得傅里叶神经算子

    Euclidean Fourier Neural Operators

    [https://arxiv.org/abs/2608.28425](https://arxiv.org/abs/2608.28425)

    提出欧几里得傅里叶神经算子（EFNO），通过将谱核参数化为物理波矢的连续函数，使神经算子能够跨不同形状和大小的周期域保持一致地学习与迁移，克服了传统FNO的域依赖性问题。

    

    傅里叶神经算子（FNO）为学习函数空间之间的映射提供了一个高效的框架，因为其构建方式使其独立于训练和评估时所用的网格分辨率。然而，FNO 并不独立于其所应用的周期域：其离散谱权重由整数傅里叶模数索引，而这些模数对应于物理波矢。当应用于不同的域时，相同的训练权重会在不同的波矢处起作用，FNO 会在不被察觉的情况下表示一个不同的算子。这使得 FNO 不适用于跨域迁移至关重要的任务。我们提出欧几里得傅里叶神经算子（EFNO）作为 FNO 的域独立替代方案。通过将谱核参数化为物理波矢的连续函数，EFNO 能够学习在不同形状和大小的周期域上保持一致作用的算子。我们在（摘要在此处截断）

    arXiv:2608.28425v1 Announce Type: new  Abstract: Fourier neural operators (FNOs) provide an efficient framework for learning mappings between function spaces as they are, by construction, independent of the grid resolution at which they are trained and evaluated. However, FNOs are not independent of the periodic domain they are applied to: their discrete spectral weights are indexed by integer Fourier mode numbers, which correspond to physical wavevectors. When applied to a different domain, the same trained weights act at different wavevectors, and the FNO silently represents a different operator. This makes FNOs unsuitable for tasks where transfer across domains is crucial. We propose Euclidean Fourier neural operators~(EFNOs) as a domain-independent alternative to FNOs. By parameterizing the spectral kernel as a continuous function of the physical wavevector, the EFNO can learn operators that act consistently across periodic domains of varying shape and size. We evaluate the EFNO on
    
[^475]: D-TAIA：面向多任务预测性流程监控的领域感知大语言模型适配

    D-TAIA: Domain-Aware LLM Adaptation for Multi-Task Predictive Process Monitoring

    [https://arxiv.org/abs/2608.28236](https://arxiv.org/abs/2608.28236)

    提出D-TAIA框架，通过领域感知训练与基于注意力的推理架构对大语言模型进行参数高效微调，在数据稀缺、高流程熵和分布偏移条件下实现下一个活动与剩余时间的联合预测。

    

    预测性流程监控（PPM）使组织能够预测未来的流程行为，例如进行中案例的下一个活动及剩余时间。在实践中，三种情况会导致现有方法性能下降，即数据稀缺、高流程熵和分布偏移。尽管基础模型（FMs），尤其是大语言模型（LLMs），凭借广泛的序列推理能力提供了一种新范式，但在这些条件下将其适配到多任务PPM仍然是一个悬而未决的挑战。现有基于基础模型的方法要么缺乏处理分布偏移的机制，要么依赖于与连续时间预测任务在结构上可能不匹配的直接回归头。本文提出了D-TAIA（领域感知训练与基于注意力的推理架构），一个通过对基础模型骨干进行参数高效微调来联合预测下一个活动和剩余时间的框架。我们的方法结合了领域……（摘要在此处被截断）

    arXiv:2608.28236v1 Announce Type: new  Abstract: Predictive Process Monitoring (PPM) enables organizations to forecast future process behavior, such as the next activity and remaining time of ongoing cases. In practice, three conditions cause existing methods to degrade, namely data scarcity, high process entropy and distributional shift. While Foundation Models (FMs), especially Large Language Models (LLMs), offer a new paradigm through broad sequential reasoning, adapting them to multi-task PPM under these conditions remains an open challenge. Existing FM-based approaches either lack mechanisms for handling distributional shift or rely on direct regression heads that can be structurally misaligned with continuous time prediction tasks. This paper introduces D-TAIA (Domain-aware Training and Attention-based Inference Architecture), a framework for a joint next activity and remaining time prediction task via parameter-efficient fine-tuning of an FM backbone. Our approach combines domai
    
[^476]: 使用视觉基础模型的可解释糖尿病视网膜病变分类

    Explainable Diabetic Retinopathy Classification Using Vision Foundation Models

    [https://arxiv.org/abs/2608.28207](https://arxiv.org/abs/2608.28207)

    该研究提出了一种基于视觉基础模型（DINOv2、CLIP、ViT）结合多种迁移学习策略的可解释糖尿病视网膜病变分类框架，其中DINOv2-LoRA内部AUROC达0.758，外部AUROC达0.920，并通过Grad-CAM和HiResCAM结合专家标注病灶掩码实现了模型可解释性评估。

    

    糖尿病视网膜病变（DR）是可预防性失明的主要原因之一，因此需要准确且可信的自动化筛查。本研究探索了一个使用视觉基础模型和多种迁移学习策略的可解释DR分类框架。研究评估了三种骨干网络——DINOv2、CLIP和视觉Transformer（ViT），并采用了全量微调、线性探测和低秩适应（LoRA）三种策略。模型在ODIR数据集上进行训练和内部评估，并在APTOS数据集上进行外部评估以检验泛化能力。DINOv2-LoRA取得了最高的内部AUROC（0.758），而DINOv2全量微调和ViT全量微调取得了最高的外部AUROC（0.920）。在等渗回归之后，通过可靠性分析进一步评估了模型的校准性能。在可解释性方面，使用IDRiD数据集中专家标注的病灶掩码，通过Dice系数和交并比（IoU）等指标对Grad-CAM和HiResCAM进行了评估。

    arXiv:2608.28207v1 Announce Type: cross  Abstract: Diabetic retinopathy (DR) is a major cause of preventable blindness, creating a need for accurate and trustworthy automated screening. This study investigates an explainable DR classification framework using vision foundation models and multiple transfer learning strategies. Three backbones, DINOv2, CLIP, and Vision Transformer (ViT), were evaluated using full fine-tuning, linear probing, and Low-Rank Adaptation (LoRA). Models were trained and internally evaluated on the ODIR dataset and externally evaluated on APTOS to assess generalization. DINOv2-LoRA achieved the highest internal AUROC of 0.758, while DINOv2 full fine-tuning and ViT full fine-tuning achieved the highest external AUROC of 0.920. Calibration was further assessed using reliability analysis after isotonic regression. For explainability, Grad-CAM and HiResCAM were evaluated against expert-annotated lesion masks from the IDRiD dataset using Dice, Intersection over Union 
    
[^477]: VICT：面向长程LLM智能体强化学习的验证器插桩式功劳追踪

    VICT: Verifier-Instrumented Credit Tracing for Long-Horizon LLM Agent Reinforcement Learning

    [https://arxiv.org/abs/2608.28128](https://arxiv.org/abs/2608.28128)

    VICT提出一种训练时接口，将终端验证器内部的可执行或证据支持的原子检查通过依赖证明边追溯到具体动作，并仅沿这些边重新分配组相对优势，从而解决长程LLM智能体强化学习中的细粒度功劳分配问题。

    

    细粒度的功劳分配是长程LLM智能体强化学习中的核心挑战。标准目标通常基于可编程验证的终端奖励进行训练，将每个稀疏的结果广播到轨迹中的每一个动作。现有方法通常从rollout侧寻求更细粒度的功劳信号，通过构建辅助轨迹信号或额外的比较来估计动作的重要性。尽管有效，这些方法仍然将判定成功的验证器当作一个标量奖励，丢弃了其内部蕴含的任务结构。我们的关键洞察是：许多可验证任务已经在终端验证器中编码了相关的检查逻辑。我们提出VICT（验证器插桩式功劳追踪），这是一个训练时接口，它将可执行或有证据支持的原子检查暴露出来，并通过依赖有效的证明边将这些检查追溯到具体动作。VICT仅沿着这些边重新分配组相对优势，从而转移……

    arXiv:2608.28128v1 Announce Type: new  Abstract: Fine-grained credit assignment is a central challenge in reinforcement learning for long horizon LLM agents. Standard objectives often train from programmatically verifiable terminal rewards by broadcasting each sparse outcome to every action in a trajectory. Existing methods typically seek finer credit from the rollout side, constructing auxiliary trajectory signals or additional comparisons to estimate action importance. Although useful, these approaches still treat the verifier that judged success as a scalar reward, discarding its internal task structure. Our key insight is that many verifiable tasks already encode the relevant checks inside their terminal verifier. We propose VICT (VerifierInstrumented Credit Tracing), a training-time interface that exposes executable or evidence backed atoms and traces them back to actions through dependency-valid proof edges. VICT redistributes group-relative advantage only along those edges, shif
    
[^478]: 条件流匹配何时可以替代逐点负对数似然？

    When Can Conditional Flow Matching Replace Pointwise Negative Log-Likelihood?

    [https://arxiv.org/abs/2608.28010](https://arxiv.org/abs/2608.28010)

    本文通过将端点NLL精确分解为熵、加权CFM目标、内部速度-得分残差和边界残差，刻画了条件流匹配损失何时能替代逐点负对数似然，证明普通CFM通常不是逐点NLL估计器，而特定加权 \(w_{\mathrm{sc}}(t)=(1-t)/t\) 在离策略最优下可消除内部残差，但该结论不能推广到训练或在线策略对齐场景。

    

    流匹配使得无似然训练成为可能，然而对齐方法越来越多地将条件流匹配（CFM）损失重新用作端点负对数似然（NLL），并将其新旧差异用作对数似然比。我们刻画了这些替代何时有效。对于线性高斯路径，我们将端点NLL精确分解为熵、加权CFM目标、内部速度-得分残差和边界残差。因此，仅基于CFM的估计和差异只有在相应残差相互抵消时才是精确的。在离策略总体最优处，普通CFM通常并不是逐点NLL估计器，而权重函数 \(w_{\mathrm{sc}}(t)=(1-t)/t\) 可以消除内部残差；但这一积极结果通常不能推广到训练或在线策略对齐。即使对于相同的端点分布，或在替代目标优化之后，在线策略的对数似然比仍可能保持有偏。在跨维度、分布和几何结构的实验中（原文在此处截断）……

    arXiv:2608.28010v1 Announce Type: new  Abstract: Flow matching enables likelihood-free training, yet alignment methods increasingly reuse conditional flow matching (CFM) losses as endpoint negative log-likelihoods (NLLs) and their old/new differences as log-likelihood ratios. We characterize when these substitutions are valid. For linear Gaussian paths, we exactly decompose endpoint NLL into entropy, a weighted CFM objective, an interior velocity--score residual, and a boundary residual. Thus CFM-only estimates and differences are exact only when the corresponding residuals cancel. At the off-policy population optimum, ordinary CFM is not generally a pointwise NLL estimator, whereas \(w_{\mathrm{sc}}(t)=(1-t)/t\) removes the interior residual; this positive result does not extend generally to training or on-policy alignment. On-policy log-ratios can remain biased even for identical endpoint laws or after surrogate optimization. Experiments across dimensions, distributions, and geometri
    
[^479]: 基于时序记忆感知的动态图在线测试时自适应

    Temporal Memory-Aware Online Test-Time Adaptation on Dynamic Graphs

    [https://arxiv.org/abs/2608.27948](https://arxiv.org/abs/2608.27948)

    提出了DGOTTA框架，通过时序记忆感知的在线测试时自适应方法，有效应对动态图上DGNN模型因结构和语义持续演化而面临的分布偏移挑战。

    

    arXiv:2608.27948v1 公告类型：新论文 摘要：图上的测试时自适应（TTA）旨在将在训练图上训练良好的图神经网络（GNN）适配到测试图上，其中存在可能损害模型泛化能力和测试时推理的潜在分布偏移。虽然近期的研究已经探索了静态图上的TTA，但在使用动态GNN（DGNN）模型学习的动态图上仍存在研究空白，因为动态图中结构连接和节点语义都会随时间持续演化。这使得适配DGNN模型以获得可靠的测试时性能面临巨大挑战。为填补这一空白，本工作提出了一种新颖的时序记忆感知的动态图在线测试时自适应框架，命名为DGOTTA，以在测试时有效地适配训练良好的DGNN。具体而言，所提出的DGOTTA包含三个模块：（1）时序感知增强，用于扩展测试动态图的多样性以应对复杂的时序……（摘要内容在此处被截断）

    arXiv:2608.27948v1 Announce Type: new  Abstract: Test-time adaptation (TTA) on graphs aims to adapt a graph neural network (GNN) that is well-trained on the training graph to the test graph, which involves potential distribution shifts that may harm model generalization and test-time inference. While recent efforts have investigated TTA on static graphs, there is still a research gap on dynamic graphs learned with dynamic GNN (DGNN) models, where both structural connectivity and node semantics evolve continuously over time. This makes adapting a DGNN model for reliable test-time performance substantially challenging. To fill this gap, in this work, we propose a novel framework of temporal memory-aware Online Test-Time Adaptation on Dynamic Graphs, named DGOTTA, to effectively adapt well-trained DGNNs during test time. Specifically, the proposed DGOTTA contains three modules: (1) temporal-aware augmentation, to extend the diversity of test dynamic graphs for addressing complex temporal 
    
[^480]: 可操作的CBFI：融合结构分解与因果反事实补救的表格机器学习框架

    Actionable CBFI: Integrating Structural Decomposition and Causal Counterfactual Recourse for Tabular Machine Learning

    [https://arxiv.org/abs/2608.27821](https://arxiv.org/abs/2608.27821)

    该论文提出A-CBFI框架，基于结构因果模型将特征重要性分解与因果反事实补救相结合，通过隔离协同交互瓶颈并释放抑制性结构锁，为表格机器学习生成因果有效、可操作的针对性干预方案，克服了现有方法因果无效与干预分散的问题。

    

    可解释人工智能（XAI）日益呼唤可操作的反事实补救，然而现有方法面临因果无效性、过高认知负担以及预测失效等挑战。穷举式因果搜索算法往往需要对多个属性进行修改，而加性归因引导的方法（如SHAP）则忽略了高阶特征之间的协同作用，导致在XGBoost等复杂非线性模型中出现次优的预测动力和分散的干预努力。为弥合这一差距，我们提出了可操作的基于案例的特征重要性（A-CBFI），这是一个面向表格机器学习的“诊断-处方”一体化框架。该框架植根于结构因果模型（SCM），能够隔离协同交互瓶颈并释放抑制性结构锁，进而将其转化为有针对性的干预措施。通过在数学上将活跃的用户干预空间……（摘要在此处不完整）

    arXiv:2608.27821v1 Announce Type: new  Abstract: Explainable artificial intelligence (XAI) increasingly calls for actionable counterfactual recourse, yet current methodologies face challenges related to causal invalidity, excessive cognitive burden, and predictive failure. Exhaustive causal search algorithms often require modifications to multiple attributes, whereas additive attribution-guided methods, such as SHAP, ignore higher-order feature synergies, leading to suboptimal predictive momentum and diffuse intervention effort in complex nonlinear models, such as XGBoost. To bridge this gap, we introduce actionable case-based feature importance (A-CBFI), a diagnosis-prescription integrated framework for tabular machine learning. Grounded in structural causal models (SCMs), A-CBFI isolates synergistic interaction bottlenecks and releases suppressive structural locks, translating them into targeted interventions. By mathematically separating the active user intervention space (L_{\mathr
    
[^481]: 大语言模型能够设计近乎最优的运筹学算法

    LLMs Can Design Near-Optimal OR Algorithms

    [https://arxiv.org/abs/2608.27296](https://arxiv.org/abs/2608.27296)

    大语言模型（如gpt-5.6-sol）能够仅凭少量提示和固定计算资源，自动设计出在库存控制、排队网络和分类优化等问题上匹配或超越现有最佳方法的近乎最优运筹学算法。

    

    arXiv:2608.27296v1 公告类型：新论文 摘要：我们探究大型语言模型（LLMs）是否能够为定义明确的运筹学（OR）问题设计有效的算法。我们研究了库存控制、排队网络控制和分类优化问题。我们评估了LLM使用的两个层级：在第一层级，模型接收单个问题实例并为该实例返回解决方案；在第二层级，模型仅接收问题类别描述和广泛的参数范围，并返回一个将实例参数映射到解决方案的算法。人工输入极少：我们提供一个未经调整的提示来描述问题，模型可以使用带有固定计算预算的Python沙盒工具。我们测试的最强模型gpt-5.6-sol，在几乎所有评估实例上匹配或超越了现有最佳方法。即使在第二层级，即在看到评估实例之前返回的算法已固定的情况下，这一结论也成立。性能也随着新发布模型的迭代而显著提升。

    arXiv:2608.27296v1 Announce Type: new  Abstract: We ask whether large language models (LLMs) can design effective algorithms for well-specified operations research (OR) problems. We study inventory control, queueing network control, and assortment optimization. We evaluate two levels of LLM use: at level 1, the model receives one problem instance and returns a solution for that instance; at level 2, it receives only the problem class description and broad parameter ranges, and returns an algorithm that maps instance parameters to solutions. Human input is minimal: we give one untuned prompt that describes the problem, and the model has access to a Python sandbox tool with a fixed compute budget.   The strongest model we test, gpt-5.6-sol, matches or outperforms the best existing method on almost all evaluated instances. This holds even at level 2, where the returned algorithm is fixed before seeing the evaluation instances. Performance also improves sharply across models released less 
    
[^482]: 当文本误导：音频对话中的不一致感知推理

    When Text Misleads: Inconsistent-Aware Reasoning for Audio-Grounded Dialogue

    [https://arxiv.org/abs/2608.27176](https://arxiv.org/abs/2608.27176)

    本文提出了ContraTalk基准测试，通过跨模态不一致检测框架，系统性地识别并利用文本误导与语音线索之间的冲突，以评估模型在音频对话中是否真正基于语音进行推理。

    

    理解口语对话需要对词汇内容和副语言声学信号（如情感和对话意图）进行联合推理。然而，现有的评估往往允许基于转录文本或单一模态解决方案的捷径，这掩盖了模型是否真正基于语音进行预测。我们将这种失败模式形式化为跨模态不一致，即转录文本暗示了看似合理但错误的表面解释，而韵律或说话风格等声学线索则支持不同的答案。我们开发了一个可扩展的框架，用于识别文本偏见的表面解释，并将不一致区域转换为冲突问答示例。我们还包含了一致的情况，即基于转录和基于语音的解释一致，从而能够在对抗性音频依赖之外进行评估。这产生了ContraTalk，一个受控基准测试，包含五个话语维度中的501个问题：情感、意图、讽刺、强调和不确定性。

    arXiv:2608.27176v1 Announce Type: new  Abstract: Understanding spoken dialogue requires joint reasoning over lexical content and paralinguistic acoustic signals such as emotion and conversational intent. However, existing evaluations often allow shortcuts based on transcripts or single-modality solutions, obscuring whether models genuinely ground predictions in speech. We formalize this failure mode as cross-modal disagreement, where transcripts suggest plausible but incorrect surface interpretations while acoustic cues such as prosody or speaking style support different answers. We develop a scalable framework that identifies text-biased surface interpretations and converts disagreement regions into conflict QA examples. We also include consistent cases where transcript-based and speech-grounded interpretations agree, enabling evaluation beyond adversarial audio dependence. This results in ContraTalk, a controlled benchmark containing 501 questions across five discourse dimensions: in
    
[^483]: 何时锐利协方差包络是紧的？体积采样最小二乘的仅特征几何

    When Is the Sharp Covariance Envelope Tight? Feature-Only Geometry for Volume-Sampled Least Squares

    [https://arxiv.org/abs/2608.26877](https://arxiv.org/abs/2608.26877)

    本文建立了体积采样最小二乘中心系数协方差的Loewner包络，并揭示了仅特征边际nu_A决定谱包络严格性的精确条件。

    

    arXiv:2608.26877v1 公告类型：新 摘要：Derezinski和Warmuth的先前分析建立了普通体积采样的全尺寸采样恒等式、选定OLS无偏性和逆矩，而他们精确的任意固定响应损失和预测协方差公式位于秩-尺寸端点s=d。我们针对每个满秩固定池、响应和合法预算d <= s <= m，在普通索引固定尺寸体积采样后进行选定未加权最小二乘的情况下，建立了中心系数协方差的Loewner包络；其系数在满秩类上全局锐利。全局锐利性并不决定在当前池上的可达性。在正损失、严格内部预算和无共环条件下，一个仅特征边际nu_A给出了精确的固定设计谱相位：nu_A > 0当且仅当归一化谱包络对每个兼容残差是严格的，而nu_A = 0当且仅当某个兼容残差是谱紧的。

    arXiv:2608.26877v1 Announce Type: new  Abstract: Prior analyses by Derezinski and Warmuth established all-size sampling identities, selected-OLS unbiasedness, and inverse moments for ordinary volume sampling, while their exact arbitrary-fixed-response loss and prediction-covariance formulas are at the rank-size endpoint s=d. We establish a Loewner envelope for centered coefficient covariance for every full-rank fixed pool, response, and legal budget d <= s <= m under ordinary indexed fixed-size volume sampling followed by selected unweighted least squares; its coefficient is globally sharp over the full-rank class. Global sharpness does not determine attainability on the pool in hand. Under positive loss, strict-interior budgets, and no coloops, a feature-only margin nu_A gives the exact fixed-design spectral phase: nu_A > 0 if and only if the normalized spectral envelope is strict for every compatible residual, whereas nu_A = 0 if and only if some compatible residual is spectrally tig
    
[^484]: 无限记忆逻辑预测的尖锐极小极大遗憾

    Sharp Minimax Regret for Infinite-Memory Logistic Prediction

    [https://arxiv.org/abs/2608.26515](https://arxiv.org/abs/2608.26515)

    本文为无限记忆逻辑预测问题建立了尖锐的极小极大遗憾界，证明了滞后分辨谱$\Gamma_T(r)$是精确的累积遗憾尺度，并通过局部贝叶斯混合和Toeplitz设计分别给出了匹配的上界和下界。

    

    arXiv:2608.26515v1 公告类型：交叉 摘要：我们研究了一个具有无限输入记忆的特定有限字母表、外生驱动源的在线预测问题。独立Rademacher输入$(U_t)$被顺序观测，下一个二元标记的对数几率（logit）为$\sum_{j=1}^{t}\theta_jU_{t+1-j}$，其中$\abs{\theta_j}\leq r_j$且$\sum_jr_j\leq B$。遗憾定义为期望累积超额对数损失。滞后$j$可以通过尺度$r_j$影响预测，并且仅在$n_{T,j}=T-j+1$个预测轮次中起作用，这导致了滞后分辨谱$\Gamma_T(r)=\sum_{j=1}^{T}\log\!\left(1+n_{T,j}r_j^2\right)$。对于每个可求和包络，局部贝叶斯混合证明了$\cR_T(r)\leq C\Gamma_T(r)$。对于指数和多项式包络，在所述有限样本维度条件下，Toeplitz设计逆命题证明了$\cR_T(r)\geq c\Gamma_T(r)$，其中常数允许依赖于固定衰减参数和对数几率界。因此，$\Gamma_T(r)$是此问题的极小极大累积遗憾尺度。

    arXiv:2608.26515v1 Announce Type: cross  Abstract: We study online prediction for a specific finite-alphabet, exogenously driven source with infinite input memory. Independent Rademacher inputs $(U_t)$ are observed sequentially, and the next binary mark has logit $\sum_{j=1}^{t}\theta_jU_{t+1-j}$, where $\abs{\theta_j}\leq r_j$ and $\sum_jr_j\leq B$. Regret is expected cumulative excess log loss. Lag $j$ can affect prediction by scale $r_j$ and enters only $n_{T,j}=T-j+1$ prediction rounds, leading to the lag-resolved spectrum $\Gamma_T(r)=\sum_{j=1}^{T}\log\!\left(1+n_{T,j}r_j^2\right)$. For every summable envelope, a localized Bayesian mixture proves $\cR_T(r)\leq C\Gamma_T(r)$. For exponential and polynomial envelopes, under the stated finite-sample dimension condition, a Toeplitz-design converse proves $\cR_T(r)\geq c\Gamma_T(r)$, with constants allowed to depend on the fixed decay parameters and the logit bound. Thus $\Gamma_T(r)$ is the minimax cumulative-regret scale for this so
    
[^485]: 语言模型中结构化知识与推理的共同演化

    Co-Evolving Structured Knowledge and Reasoning in Language Models

    [https://arxiv.org/abs/2608.26386](https://arxiv.org/abs/2608.26386)

    本文提出KBevo框架，通过端到端联合优化结构化知识库构建与推理，使推理结果直接反馈改进知识库质量，从而提升问答的准确性、可控性和组合推理能力。

    

    arXiv:2608.26386v1 公告类型：交叉 摘要：检索增强方法通过将语言模型基于外部知识来提高事实准确性，但检索非结构化文本常常引入无关上下文，并对检索信息控制有限。结构化知识库提供了一种更可控的替代方案，但其构建成本高昂，且推理时往往脆弱。为解决这些限制，我们提出KBevo：一个共同演化框架，联合学习构建结构化知识库并对其进行推理，以应对知识密集型问答任务。通过使用问答结果奖励对两个组件进行端到端优化，我们的方法使推理成功直接提升所构建知识库的质量。这导致更大、连接更紧密、答案可达性更高的知识结构，同时与标准检索基线相比，也改善了组合事实推理和可控性。

    arXiv:2608.26386v1 Announce Type: cross  Abstract: Retrieval-augmented methods improve factual accuracy by grounding language models in external knowledge, but retrieving over unstructured text often introduces irrelevant context and offers limited control over the retrieved information. Structured knowledge bases offer a more controllable alternative, yet they are expensive to construct and often brittle to reason over. To address these limitations, we propose KBevo: a co-evolving framework that jointly learns to construct a structured knowledge base and reason over it for knowledge-intensive question answering. By optimizing both components end-to-end with QA outcome rewards, our method enables reasoning success to directly improve the quality of the constructed knowledge base. This leads to larger, better-connected knowledge structures with higher answer reachability, while also improving compositional factual reasoning and controllability compared to standard retrieval baselines.
    
[^486]: 当规范补全出错时：形式化并度量大型语言模型中的跳跃

    When the Canonical Completion Is Wrong: Formalizing and Measuring the Jump in Large Language Models

    [https://arxiv.org/abs/2608.26187](https://arxiv.org/abs/2608.26187)

    本文首次形式化了LLM中的“跳跃”概念，将其定义为具有可验证证书的有限扩展问题，并提供了度量方法，以解决关于LLM溯因能力的争论。

    

    arXiv:2608.26187v1 公告类型：交叉 摘要：大型语言模型（LLMs）是否能够执行从证据到新公理系统的溯因跳跃（通常称为“跳跃”），近来引起了相当大的争论。一种主流观点认为LLMs在结构上无法进行此类跳跃，而近期研究则对其机制和证据提出了质疑。然而，这一争论仍难以定论，因为该领域缺乏对跳跃的形式化定义以及测试双方观点的度量方法。在本文中，我们分四步开发了跳跃的形式化描述，并度量了第二步。这些步骤询问部分数据的默认补全是什么、何时被迫放弃它、何时放弃是正确的，以及连续跳跃如何复合。具体来说，我们将跳跃实例定义为有限扩展问题，并带有机器可检查的证书，证明正确的补全存在、在重命名意义下唯一，且与数据的规范补全不同。

    arXiv:2608.26187v1 Announce Type: cross  Abstract: Whether large language models (LLMs) can perform the abductive leap from evidence to a new system of axioms, commonly referred to as a jump, has recently attracted considerable debate. A prominent position holds that LLMs are structurally incapable of such jumps, while recent studies challenge both its mechanism and its evidence. However, the debate remains difficult to settle, since the field still lacks a formal definition of the jump and a measure to test either side. In this paper, we develop a formal account of the jump in four steps and measure the second. The steps ask what the default completion of partial data is, when abandoning it is forced, when the abandonment is correct, and how successive jumps compound. Specifically, we define a jump instance as a finite extension problem with a machine-checked certificate that a correct completion exists, is unique up to renaming, and differs from the canonical completion of the data. 
    
[^487]: 将科学论文展开为多轮生成轨迹以进行持续预训练

    Unfolding Scientific Papers into Multi-Turn Generation Trajectories for Continued Pre-Training

    [https://arxiv.org/abs/2608.25826](https://arxiv.org/abs/2608.25826)

    本文提出了一种将科学论文展开为多轮生成轨迹的流水线，通过重建写作过程生成持续预训练语料，规模为源文本两倍，并适用于指令数据和评估。

    

    近期一系列合成数据工作重建了现有文本背后的思考过程，而非重写文本本身，但这些工作仅适用于短篇网页段落，只能恢复局部想法，且未触及整篇文档的结构。科学论文以清晰且大致统一的结构撰写，自然适合将此范式提升至文档层面。我们提出了一种流水线，将每篇论文展开为多轮生成轨迹，其中教师模型重建整篇论文的写作过程：包括写作请求、全局规划以及每个章节的写作前思考。所有章节文本和摘要均逐字保留自原始论文。我们将此流水线应用于质量筛选后的arXiv论文，获得了一个用于持续预训练（CPT）的语料库，其规模约为源文本的两倍。相同的逆向构建方法也扩展至指令数据和评估。

    arXiv:2608.25826v1 Announce Type: new  Abstract: A recent line of synthetic-data work reconstructs the thinking behind existing text rather than rewriting the text itself, but it operates on short web passages, recovers only local thoughts, and leaves the structure of whole documents untouched. Scientific papers are written to a clear and largely uniform structure and make a natural substrate for lifting this paradigm to the document level. We present a pipeline that unfolds each paper into a multi-turn generation trajectory in which a teacher model reconstructs the writing process of the whole paper: a writing request, a global plan, and pre-writing deliberation for each section. All section texts and the abstract are kept verbatim from the source paper. We apply the pipeline to quality-filtered arXiv papers and obtain a corpus for continued pre-training (CPT) that is roughly twice the size of the source text. The same reverse construction extends to instruction data and evaluation. T
    
[^488]: LM-X：面向通用机器人操作的可解释动作建模——集成进度、事件与不确定性预测

    LM-X: Explainable Action Modeling with Progress, Event, and Uncertainty Prediction for Generalist Robot Manipulation

    [https://arxiv.org/abs/2608.25757](https://arxiv.org/abs/2608.25757)

    本文提出LM-X框架，通过在线预测任务进度、事件转换和局部不确定性三个显式信号，使VLA策略的动作生成具有内在可解释性，无需事后解释。

    

    arXiv:2608.25757v1 公告类型：交叉 摘要：通用视觉-语言-动作（VLA）策略主要通过短视距动作预测来学习长期行为，并且除了采样的命令外几乎不透露其他信息。这造成了两个相互关联的瓶颈：单一的动作目标必须隐含地吸收任务进度、中间意图和局部可靠性，而这些控制状态在执行过程中仍然隐藏。受生物感觉运动控制的功能原理启发，我们引入了LM-X，它在任务、事件和运动尺度上组织预测，而不声称解剖学对应关系。三个显式监督信号在线发出并直接调节动作生成：返回到目标（RTG）衡量可见任务进度，事件到目标（ETG）识别下一个语义转换，以及异方差动作流通过传播方差估计局部可靠性。因此，解释是控制的内在部分，而非事后生成。

    arXiv:2608.25757v1 Announce Type: cross  Abstract: Generalist vision--language--action (VLA) policies learn long-horizon behavior mainly through short-horizon action prediction and reveal little beyond sampled commands. This creates two coupled bottlenecks: a single action target must implicitly absorb task progress, intermediate intent, and local reliability, while these control states remain hidden during execution. Inspired by functional principles of biological sensorimotor control, we introduce LM-X , which organizes prediction across task, event, and motor scales without claiming anatomical correspondence. Three explicitly supervised signals are emitted online and directly condition action generation: return-to-go (RTG) measures visible task progress, event-to-go (ETG) identifies the next semantic transition, and heteroscedastic action flow estimates local reliability through propagated variance. Explanation is therefore intrinsic to control rather than generated post hoc. Before
    
[^489]: 参数化知识图谱记忆中的存储-检索差距

    A Storage-Retrieval Gap in Parametric Knowledge Graph Memory

    [https://arxiv.org/abs/2608.25489](https://arxiv.org/abs/2608.25489)

    该论文提出将知识图谱离线编译为LoRA适配器作为参数化知识层，在零查询上下文成本下实现事实知识泛化，但发现存储知识无法通过相似性检索恢复，揭示了参数化记忆中的存储-检索差距。

    

    arXiv:2608.25489v1 公告类型：交叉 摘要：图检索增强生成在查询时将检索到的子图放入模型的上下文窗口中，每次调用都支付重复的令牌成本，并在每次调用时暴露源数据。我们研究了一种替代方案：将知识图谱离线编译为每个实体一个LoRA适配器的库，这些适配器作为参数化知识层，通过注入权重而非文本来查询，在查询时零上下文成本。在MetaQA数据集上，我们发现子图训练的适配器编码了上下文无关的事实知识，这些知识能泛化到未见问题：在单值关系上，适配器相对于几乎无法闭卷的基础模型（0.007）获得了+0.243的精确匹配分数提升，且只有正确的适配器能恢复这些知识（相对于基础模型的oracle差距为+0.283）。然而，存储的知识无法通过相似性恢复：在无子图的查询下，基于嵌入和权重空间几何的检索性能均不佳。

    arXiv:2608.25489v1 Announce Type: cross  Abstract: Graph retrieval-augmented generation places retrieved subgraphs into the model's context window at query time, paying a recurring token cost and exposing source data on every call. We study an alternative: compiling a knowledge graph offline into a bank of LoRA adapters, one per entity, that serve as a parametric knowledge layer queried by injecting weights rather than text, at zero query-time context cost. On the MetaQA dataset, we find that subgraph-trained adapters encode context-free factual knowledge that generalizes to unseen questions: on single-valued relations the adapter gains $+0.243$ exact-match score over a base model that is nearly blind closed-book ($0.007$), and only the correct adapter recovers this knowledge (an oracle gap of $+0.283$ over the base model). However, the stored knowledge is not recoverable by similarity: given a query with no subgraph, embedding-based and weight-space geometry retrieval both perform at 
    
[^490]: 面向令牌的语义通信与预训练视觉变换器

    Token-Oriented Semantic Communication with Pretrained Vision Transformers

    [https://arxiv.org/abs/2608.25410](https://arxiv.org/abs/2608.25410)

    提出了一种面向令牌的语义通信框架，通过利用预训练视觉变换器和学习图像压缩模型，实现令牌粒度传输而不直接传输令牌嵌入，从而降低通信成本并增强互操作性，无需端到端训练。

    

    令牌通信在变换器令牌粒度上实现了语义通信原理，为资源受限的边缘系统中的客户端-服务器协作推理提供了有前景的方向。然而，直接传输令牌嵌入面临两个实际挑战：显著的通信成本和跨模型特定令牌嵌入空间的有限互操作性。为应对这些挑战，我们提出了一种面向令牌的语义通信框架。在该框架中，令牌级别的任务相关性决定了传输哪些压缩图像潜变量，从而实现令牌粒度的传输而无需直接传输令牌嵌入。该框架是模块化的，协调三个预训练组件——轻量级客户端视觉变换器（ViT）、学习图像压缩（LIC）模型和大型服务器端ViT——而无需端到端训练。关键使能因素是客户端和服务器令牌之间的——对应关系。

    arXiv:2608.25410v1 Announce Type: cross  Abstract: Token communications realize the semantic communication principle at the granularity of transformer tokens, providing a promising direction for client--server collaborative inference in resource-constrained edge systems. However, directly transmitting token embeddings presents two practical challenges: substantial communication cost and limited interoperability across model-specific token embedding spaces. To address these challenges, we propose a \emph{token-oriented} semantic communication framework. In this framework, token-level task relevance determines which compressed image latents are transmitted, enabling token-granular transmission without directly transmitting token embeddings. The framework is modular, coordinating three pretrained components---a lightweight client-side vision transformer (ViT), a learned image compression (LIC) model, and a large server-side ViT---without end-to-end training. The key enabler is the one-to-
    
[^491]: 生成式动作块采样在物理人机协作中的自适应刚度控制

    Generative Action-Chunk Sampling for Adaptive Stiffness Control in Physical Human-Robot Collaboration

    [https://arxiv.org/abs/2608.25284](https://arxiv.org/abs/2608.25284)

    通过采样未来动作块并利用其变异性动态调整机器人刚度，实现了在意图明确时提供辅助、在意图模糊时保持顺从的自适应协作控制，显著提升了任务成功率。

    

    物理人机协作要求机器人在人类意图明确时提供辅助，而在多种未来动作可能时保持顺从性。我们提出了一种基于生成式动作块采样的自适应刚度框架。该策略以RGB图像和外部关节扭矩估计为条件，从观测条件先验中采样多个未来动作块。采样动作块之间的变异性用于连续调整关节刚度和阻尼。变异性较大时，机器人更顺从以促进人类引导，而变异性较小时则提供更坚定的辅助。在具有四种可能方向的真实世界协作运输任务中，所提方法实现了平均成功率0.95，而固定刚度消融为0.83，确定性基线为0.69。在方向确定附近，采样动作块之间的变异性增加。

    arXiv:2608.25284v1 Announce Type: cross  Abstract: Physical human-robot collaboration requires a robot to provide assistance when human intention is clear while remaining compliant when several future motions are plausible. We present an adaptive stiffness framework based on generative action-chunk sampling. Conditioned on an RGB image and external joint-torque estimates, the policy samples multiple future action chunks from an observation-conditioned prior. Variation among the sampled action chunks is used to continuously adapt joint stiffness and damping. Greater variation makes the robot more compliant to facilitate human guidance, whereas lower variation provides firmer assistance. In a real-world collaborative transport task with four possible directions, the proposed method achieved an average success rate of 0.95, compared with 0.83 for a fixed-stiffness ablation and 0.69 for a deterministic baseline. Near direction determination, variation among the sampled action chunks increa
    
[^492]: 基于可验证奖励的在线策略蒸馏

    On-policy Distillation with Verifiable Reward

    [https://arxiv.org/abs/2608.24696](https://arxiv.org/abs/2608.24696)

    提出了一种无需额外超参数的无缝结合在线策略蒸馏和可验证奖励强化学习的方法，通过基于轨迹正确性的奖励重构和ReLU门控机制，有效提升大型语言模型后训练性能。

    

    arXiv:2608.24696v1 公告类型：交叉 摘要：基于可验证奖励的强化学习（RLVR）和在线策略蒸馏（OPD）已成为大型语言模型后训练中广泛采用的两种范式。然而，RLVR面临稀疏的任务级反馈问题，而OPD提供密集的令牌级指导但忽略轨迹正确性，将其性能限制在教师模型水平。将两者结合是一个有前景的方向：OPD提供密集的监督信号，而RLVR提供任务级正确性。尽管如此，现有集成方法通常依赖加权组合或启发式切换，引入了额外超参数和权衡。我们提出了一种名为基于可验证奖励的在线策略蒸馏（OPDVR）的方法，这是一种简单而有效的方法，无需添加任何超参数即可无缝结合OPD和RLVR。我们首先根据轨迹正确性重新表述采样令牌OPD的隐式奖励，然后应用ReLU门控机制以确保正确的轨迹获得正向更新。

    arXiv:2608.24696v1 Announce Type: cross  Abstract: Reinforcement Learning with Verifiable Rewards (RLVR) and on-policy distillation (OPD) have become two widely adopted paradigms for post-training large language models. However, RLVR suffers from sparse task-level feedback, while OPD provides dense token-level guidance but ignores trajectory correctness, limiting its performance to that of the teacher. Combining them is a promising direction: OPD supplies dense supervisory signals, while RLVR provides task-level correctness. Nevertheless, existing integrations often rely on weighted combination or heuristic switching, introducing extra hyperparameters and trade-offs. We propose On-policy Distillation with Verifiable Reward (OPDVR), a simple yet effective method that seamlessly combines OPD and RLVR without adding any hyperparameters. We first reformulate the implicit reward of sampled-token OPD based on trajectory correctness, then apply a ReLU gating mechanism to ensure that correct t
    
[^493]: 监督式UQ集成何时能提升LLM幻觉检测？一项稳健性研究

    When Do Supervised UQ Ensembles Improve LLM Hallucination Detection? A Robustness Study

    [https://arxiv.org/abs/2608.24492](https://arxiv.org/abs/2608.24492)

    本文系统评估了监督式UQ集成在不同LLM、数据集和生成模式下的稳健性，发现其性能提升受限于特定条件，并非普遍适用。

    

    不确定性量化（UQ）方法广泛用于大型语言模型（LLMs）在封闭书设置中的幻觉检测，该场景下推理时无法获得真实证据。先前工作提出了通过学习的集成组合UQ信号，但对这些集成稳健性的实证研究有限。我们研究了一个监督式集成框架，该框架在小型、领域特定的标记LLM响应数据集上训练一个分类器，对基于异构UQ的评分器输出进行训练，然后将其应用于无检索、工具或参考文档的样本外幻觉分类。在四个LLM、九个数据集和三种生成模式（短问答、长文生成和代码生成）下，我们沿三个轴提供了系统性稳健性分析：样本效率、领域内数据集转移和生成模式依赖性。我们发现监督式集成优于...

    arXiv:2608.24492v1 Announce Type: cross  Abstract: Uncertainty quantification (UQ) methods are widely used for hallucination detection in large language models (LLMs) in closed-book settings where ground-truth evidence is unavailable at inference time. Prior work has proposed combining UQ signals via learned ensembles, but empirical investigations into the robustness of these ensembles are limited. We study a supervised ensembling framework that trains a classifier over heterogeneous UQ-based scorer outputs on a small, domain-specific dataset of labeled LLM responses, then applies it to out-of-sample hallucination classification without retrieval, tools, or reference documents. Across four LLMs, nine datasets, and three generation regimes (short-form QA, long-form generation, and code generation), we provide a systematic robustness analysis along three axes: sample efficiency, in-domain dataset transfer, and generation regime dependence. We find that supervised ensembles outperform the
    
[^494]: 依赖数据下的序列算子学习

    Sequential operator learning under dependent data

    [https://arxiv.org/abs/2608.24426](https://arxiv.org/abs/2608.24426)

    本文提出了在依赖数据下用于算子学习的时间一致自归一化浓度界，无需独立性或混合假设，为自适应和动力学数据驱动的算子学习提供了收敛性保证。

    

    摘要：arXiv:2608.24426v1 公告类型：交叉 摘要：从顺序收集的数据中学习算子出现在自适应实验设计、贝叶斯优化和动力系统建模中，在这些场景中，观测可能是依赖的，且未来的输入或传感算子可能依赖于先前数据。我们推导了在具有向量值噪声的希尔伯特空间中随机过程的时间一致自归一化浓度界。我们利用这些界获得了线性算子的回归误差保证，包括估计空间外的目标，以及使用强凸损失和正则化器训练的非线性参数算子。我们的结果允许可能无限维的输入和输出，无需独立性或混合假设，这为自适应算子学习和从随机动力学数据中学习的收敛性保证迈出了重要一步。

    arXiv:2608.24426v1 Announce Type: cross  Abstract: Learning operators from sequentially collected data arises in adaptive experimental design, Bayesian optimization, and dynamical-system modelling, where observations may be dependent, and future inputs or sensing operators may depend on preceding data. We derive time-uniform self-normalized concentration bounds for stochastic processes in Hilbert spaces with vector-valued noise. We use these bounds to obtain regression-error guarantees for linear operators, including targets outside the Hilbert estimation space, and for nonlinear parametric operators trained with strongly convex losses and regularizers. Our results allow possibly infinite-dimensional inputs and outputs without independence or mixing assumptions, providing a major step towards convergence guarantees for adaptive operator learning and learning from stochastic dynamical data.
    
[^495]: 从松弛可索引性到精确可索引性：部分可观测静止多臂老虎机的$t$步方法

    From Relaxed Indexability to Exact Indexability: A $t$-Step Approach for Partially Observable Restless Bandits

    [https://arxiv.org/abs/2608.24167](https://arxiv.org/abs/2608.24167)

    本文提出一种$t$步前瞻阈值策略，通过多步值迭代扩展了部分可观测静止多臂老虎机的Whittle指数计算，在$t=1$时恢复现有线性阈值，在$t>1$时使阈值依赖补贴，从而更精确地逼近精确可索引性。

    

    arXiv:2608.24167v1 公告类型：新 摘要：Whittle指数策略为静止多臂老虎机提供了一种可扩展的方法，但在部分可观测性下，即使在单一信念状态确定无差异补贴也需要解决一个无闭式值函数的无限时域信念状态问题。Liu [10] 通过线性化未知决策边界来解决这一难题，从而得到一个线性系统和闭式近似Whittle指数。然而，由此产生的阈值仅使用一步主动-被动比较，未考虑更长时域的延续值。我们将此框架扩展为一种“$t$步前瞻阈值策略”。对于每个补贴$m$，阈值由$t$步有限时域值迭代下的主动减被动优势定义。当$t=1$时，阈值与$m$无关，并恢复Liu [10]的线性阈值；当$t>1$时，它通过诱导的首穿结构变得依赖补贴。

    arXiv:2608.24167v1 Announce Type: new  Abstract: Whittle index policies offer a scalable method for restless multi-armed bandits, but under partial observability even determining the indifference subsidy at a single belief requires solving an infinite-horizon belief-state problem with no closed-form value function. Liu [10] addresses this difficulty by linearizing the unknown decision boundary, leading to a linear system and a closed-form approximate Whittle index. However, the resulting threshold uses only a one-step active--passive comparison and does not account for longer-horizon continuation values.   We extend this framework to a \emph{$t$-step lookahead threshold policy}. For each subsidy $m$, the threshold is defined by the active-minus-passive advantage under $t$-step finite-horizon value iteration. At $t=1$, the threshold is $m$-independent and recovers the linear threshold of Liu [10]; for $t>1$, it becomes subsidy-dependent through the induced first-crossing structure and t
    
[^496]: SplitLite：用于分割学习的低秩残差压缩方法

    SplitLite: Low-Rank Residual Compression for Split Learning

    [https://arxiv.org/abs/2608.23018](https://arxiv.org/abs/2608.23018)

    SplitLite通过利用相邻周期激活和梯度残差的低秩结构（秩-2r和秩-4r），仅传输量化的截断奇异值，大幅降低分割联邦LoRA微调的通信成本。

    

    摘要：arXiv:2608.23018v1 公告类型：交叉 摘要：设备端大型语言模型（LLMs）的联邦微调面临显著的计算负担。为克服此限制，分割学习（SL）作为一种有前景的解决方案出现，它将主要训练工作负载卸载到强大的服务器上。然而，SL需要在客户端和服务器之间交换高维激活和梯度，导致高昂的通信成本。为应对这一挑战，我们提出了SplitLite，一种通信高效的分割联邦LoRA微调方法，该方法利用连续周期激活和梯度残差的低有效秩结构。我们的关键发现是，当LoRA在参数空间中使用秩为$r$的更新时，相邻周期之间同一数据样本的激活和梯度残差分别表现出有效秩-$2r$和秩-$4r$的结构。通过揭示这一特性，SplitLite仅传输量化的截断奇异值。

    arXiv:2608.23018v1 Announce Type: cross  Abstract: Federated fine-tuning of on-device large language models (LLMs) faces a significant computing burden. To overcome this limitation, split learning (SL) has emerged as a promising solution, which offloads the primary training workload to a powerful server. However, SL requires exchanging high-dimensional activations and gradients between clients and the server, resulting in prohibitive communication costs. To overcome this challenge, we propose SplitLite, a communication-efficient split federated LoRA fine-tuning method that exploits the low effective rank structure of consecutive-epoch activation and gradient residuals. Our key finding is that, when LoRA uses rank $r$ updates in parameter space, the activation and gradient residuals of the same data sample between adjacent epochs also exhibit effective rank-$2r$ and rank-$4r$ structures, respectively. By revealing this property, SplitLite transmits only quantized truncated singular valu
    
[^497]: CatchBench：何时能够捕获智能体故障？

    CatchBench: When Can an Agent Failure Be Caught?

    [https://arxiv.org/abs/2608.22808](https://arxiv.org/abs/2608.22808)

    CatchBench提出了一个在三种信息状态（运行前配置PRE、运行中轨迹前缀LIVE、完成后轨迹POST）下评估智能体故障审计能力的基准，通过七项任务契约对72个参赛者进行评分，并发现现有方法在大多数对比中缺乏区分能力。

    

    何时能够捕获智能体的故障？审计通常受限于记录而非方法本身。因此，CatchBench将审计员的一个问题置于三种信息状态下进行考察：运行前声明的配置（PRE）、不断增长的轨迹前缀（LIVE），以及已完成的轨迹（POST）。以往的基准测试要么固定其中一种状态，要么改变遥测数据；据我们所知，尚无基准在统一的任务-方法接口下对这三种状态同时评分。由于每种状态允许提出不同的问题，七个任务契约各自携带自己的标签和指标，而非采用单一排行榜。其中四项任务为证据性任务，三项为基于Gold的机制诊断任务。该发布版本对72个参赛者进行了评分，参赛者包括规则扫描器、结构化模型，以及来自九个模型家族（GPT、Claude、Gemini、Gemma、Llama、Qwen、DeepSeek、Mistral、Nova）的十一个LLM裁判，涵盖1187个声明配置和1162次记录的运行。大多数竞技场结果并不具有排序区分度：138个注册对比中的56个sep（摘要在此处被截断）

    arXiv:2608.22808v2 Announce Type: replace  Abstract: When can an agent failure be caught? An audit is usually limited by the record rather than by the method. CatchBench therefore puts one auditor's question to three information states: the declared configuration before a run (PRE), a growing prefix of its trace (LIVE), and the finished trace (POST). Prior benchmarks fix one of these states or vary the telemetry; to our knowledge none scores all three under one task-method interface. Each state admits different questions, so seven task contracts carry their own labels and metrics rather than one leaderboard. Four are evidential; three are Gold-derived mechanism diagnostics.   The release scores 72 entrants, from rule scanners and structural models to eleven LLM judges across nine model families (GPT, Claude, Gemini, Gemma, Llama, Qwen, DeepSeek, Mistral, Nova), over 1187 declared configurations and 1162 recorded runs. Most of the arena does not order: 56 of 138 registered contrasts sep
    
[^498]: 稀缺确认性PET测量的合理分配：A4/LEARN中的目标对齐验证

    Spending Scarce Confirmatory PET Measurements: Target-Aligned Validation in A4/LEARN

    [https://arxiv.org/abs/2608.22223](https://arxiv.org/abs/2608.22223)

    本文提出了一种目标对齐的PET验证策略，通过结合目标影响和残差不确定性来优化稀缺确认性测量的分配，避免在影响弱的受试者上浪费资源。

    

    抗淀粉样蛋白疗法和血液生物标志物正在将阿尔茨海默病的诊疗流程转变为两阶段测量工作流：首先使用成本较低的信息进行广泛筛查，然后在能支持最终报告决策的关键环节使用稀缺的确认性淀粉样蛋白测量。淀粉样蛋白正电子发射断层扫描（PET）仍是此类用于评估淀粉样蛋白负担的协议测量手段之一，但PET机位、试验预算和面向支付方的证据包都是有限的。本文提出了一个明确的操作性问题：何时简单的透明PET验证足够，何时拟合残差不确定性评分值得增加复杂度？对于加权协议目标，验证受试者i的一阶价值是目标影响与残差协议不确定性的乘积。通用不确定性采样仅使用第二个因素，可能将PET测量分配给那些难以预测但对科学、临床或商业目标影响较弱的受试者。

    arXiv:2608.22223v1 Announce Type: cross  Abstract: Anti-amyloid therapies and blood-based biomarkers are changing Alzheimer disease workups into a two-stage measurement workflow: screen broadly with cheaper information, then spend scarce confirmatory amyloid measurements where they support the decision that will be reported. Amyloid positron-emission tomography (PET) remains one such protocol measurement for amyloid burden, but PET slots, trial budgets, and payer-facing evidence packages are finite. This paper asks a deliberately operational question: when is simple transparent PET validation enough, and when is a fitted residual-uncertainty score worth the added complexity? For a weighted protocol target, the first-order value of validating subject i is the product of target influence and residual protocol uncertainty. Generic uncertainty sampling uses only the second factor and can spend PET measurements on subjects that are hard to predict but weak for the scientific, clinical, or c
    
[^499]: 短定价面板中的跨设计不确定性：来自模拟价格轨迹的证据

    Across-Design Uncertainty in Short Pricing Panels: Evidence from Simulated Price Trajectories

    [https://arxiv.org/abs/2608.21334](https://arxiv.org/abs/2608.21334)

    本文通过模拟价格轨迹证明，短定价面板中跨设计不确定性占估计误差方差的绝大部分（97.6%），并提出了一个经验关系式来描述其分散度。

    

    arXiv:2608.21334v1 公告类型：新 摘要：短观测定价面板可能包含许多观测值，但仅提供少量不同的价格变动。本文在一个校准至稀疏定价机制的合成数据生成过程中研究这一区别的推断后果。我们将条件于已实现价格轨迹的不确定性与同一定价过程生成的不同替代轨迹间估计误差的变化分开。在基线模拟中，后者成分占梯度提升规格估计误差方差的97.6%。面板内重采样程序使用一个已实现轨迹的信息，无法识别这一跨设计成分。三个结果组织了分析。首先，跨设计离散度由经验关系 sigma_hat 约等于 0.182 V^(-0.271) 良好描述，其中 V 等于变动次数乘以幅度平方。其次，添加共享区域

    arXiv:2608.21334v1 Announce Type: new  Abstract: Short observational pricing panels can contain many observations while offering only a small number of distinct price movements. This paper studies the inferential consequences of that distinction in a synthetic data-generating process calibrated to a sparse pricing regime. We separate uncertainty conditional on a realised price trajectory from variation in estimation error across alternative trajectories generated by the same pricing process. In the baseline simulations, the latter component accounts for 97.6% of the variance of estimation error for the gradient-boosted specification. Within-panel resampling procedures use the information of one realised trajectory and do not identify this across-design component.   Three results organise the analysis. First, across-design dispersion is well described by the empirical relation sigma_hat approx 0.182 V^(-0.271), where V equals moves times magnitude squared. Second, adding regions sharing
    
[^500]: 利用$\mathbb{F}_2$线性代数学习精确的NVIDIA SASS编码器

    Learning Exact NVIDIA SASS Encoders with $\mathbb{F}_2$ Linear Algebra

    [https://arxiv.org/abs/2608.20532](https://arxiv.org/abs/2608.20532)

    F2Asm首次利用$\mathbb{F}_2$线性代数学习精确的SASS指令编码器，并成为支持Rubin SM107的开源NVIDIA SASS汇编器。

    

    NVIDIA提供了SASS反汇编器，但没有针对近期数据中心GPU的公开SASS汇编器，这限制了受控的机器码重写。我们提出了F2Asm，它通过成对的反汇编结果和原始CUBIN指令字来学习精确的128位SASS编码器。据我们所知，F2Asm是首个将SASS指令编码器学习为$\mathbb{F}_2$上向量值仿射映射的系统，也是首个支持Rubin SM107的开源NVIDIA SASS汇编器。F2Asm使用$\mathbb{F}_2$上的高斯消元法来增量构建紧凑基，检测不一致性，并拒绝超出学习跨度范围的输入。F2Asm将目标特定的控制位、重定位规则和CUBIN元数据与其学习算法分离。我们使用来自固定NVIDIA和第三方生产库、CUDA 13.3包以及CUDA 13.4开发者预览存档中的3,225个CUBIN，为Hopper SM90/SM90a、Blackwell SM100和Rubin SM107训练编码器。在往返测试中，F2Asm重新组装了...

    arXiv:2608.20532v1 Announce Type: new  Abstract: NVIDIA provides a SASS disassembler but no public SASS assembler for recent data-center GPUs, limiting controlled machine-code rewriting. We present F2Asm, which learns exact 128-bit SASS encoders from paired disassembly and original CUBIN instruction words. To our knowledge, F2Asm is the first system to learn SASS instruction encoders as vector-valued affine maps over F2 and the first open-source NVIDIA SASS assembler to support Rubin SM107. F2Asm uses Gaussian elimination over F2 to incrementally build a compact basis, detect inconsistencies, and reject inputs outside the learned span. F2Asm separates target-specific control bits, relocation rules, and CUBIN metadata from its learning algorithm. We train encoders for Hopper SM90/SM90a, Blackwell SM100, and Rubin SM107 using 3,225 CUBINs from pinned NVIDIA and third-party production libraries, CUDA 13.3 packages, and CUDA 13.4 Developer Preview archives. In round-trip tests, F2Asm reass
    
[^501]: 决策树与K均值分析食用油的拉曼光谱：一种物理信息人工智能方法

    Decision Tree and K-Means Analysis of Raman Spectra for Edible Oils: A Physics-Informed AI Approach

    [https://arxiv.org/abs/2608.20440](https://arxiv.org/abs/2608.20440)

    本研究通过物理信息人工智能方法，利用决策树仅需四个拉曼光谱变量即可对五种纯食用油实现100%准确分类，显著简化了食用油鉴别过程。

    

    摘要：arXiv:2608.20440v1 公告类型：交叉 摘要：对加工食品中食用油的真伪鉴别对于食品质量、防欺诈和法规遵从至关重要。本研究建立了一个集成的拉曼光谱和机器学习框架，将固有光谱组织、可解释分类和物理信息人工智能（PI-AI）联系起来。研究了五种食用油在纯态和油炸薯片基质中的情况，使用t-SNE、K均值聚类、决策树和基于非负最小二乘（NNLS）的光谱分解。无监督分析揭示了纯油中显著更强的类别组织和可分性，而食品基质效应引入了明显的光谱重叠。决策树仅使用原始1866特征光谱空间中的四个拉曼变量，对纯油实现了100%的分类准确率。这四个变量在预剪枝和后剪枝模型中被一致识别。

    arXiv:2608.20440v1 Announce Type: cross  Abstract: Authentication of edible oils in processed foods is important for food quality, fraud prevention, and regulatory compliance. This study establishes an integrated Raman spectroscopy and machine-learning framework that links intrinsic spectral organization, interpretable classification, and Physics-Informed Artificial Intelligence (PI-AI). Five edible oils were investigated in pure form and within a fried-potato-chip matrix using t-SNE, K-means clustering, Decision Trees, and Non-Negative Least Squares (NNLS)-based spectral decomposition. Unsupervised analyses revealed substantially stronger class organization and separability in pure oils, whereas food-matrix effects introduced pronounced spectral overlap. Decision Trees achieved 100% classification accuracy for pure oils using only four Raman variables from the original 1866-feature spectral space. These four variables, consistently identified by both pre-pruned and post-pruned models,
    
[^502]: VA-DPO：基于效价-唤醒度的直接偏好优化，用于语言模型中的可控情感生成

    VA-DPO: Valence-Arousal Direct Preference Optimization for Controllable Emotion Generation in Language Models

    [https://arxiv.org/abs/2608.20374](https://arxiv.org/abs/2608.20374)

    VA-DPO通过将情感目标表示为连续效价-唤醒度点，并基于距离阈值筛选偏好数据，实现了比现有提示方法更精确可控的情感生成，显著降低了目标距离并提升了相关性。

    

    arXiv:2608.20374v1 公告类型：交叉 摘要：我们能多精确地告诉语言模型如何感受？大多数情感生成的工作使用离散标签（如快乐、愤怒、悲伤）来回答，这无法表达像“略带沮丧但平静”这样的目标。我们转而将期望的情感指定为效价-唤醒度平面中的一个连续点（v*, a*），并训练模型去命中该点。我们的方法VA-DPO是对直接偏好优化（DPO）的一个小修改：一个冻结的VA回归器根据每个采样生成与目标的欧氏距离进行评分，我们只保留距离差距超过阈值τ的候选对，并使用普通的DPO损失对冻结参考模型优化一个LoRA适配器。DPO目标本身未变；新意在于偏好数据的构建方式。在Llama-3.1-8B-Instruct上，该方法将平均VA距离比系统提示减少33%，比少样本提示减少25%，并将效价/唤醒度相关性提升至r_v=0.93和r_a=0.75。这些改进……

    arXiv:2608.20374v1 Announce Type: cross  Abstract: How precisely can we tell a language model how to feel? Most work on emotional generation answers with a discrete label - happy, angry, sad - which cannot express a target like "mildly downcast but calm." We instead specify the desired affect as a continuous point (v*, a*) in the Valence-Arousal plane and train the model to hit it. Our method, VA-DPO, is a small modification to Direct Preference Optimization: a frozen VA regressor scores each sampled generation by its Euclidean distance to the target, we keep only candidate pairs whose distance gap clears a margin tau, and we optimize a LoRA adapter with the ordinary DPO loss against a frozen reference. The DPO objective itself is unchanged; what is new is how the preference data is built. On Llama-3.1-8B-Instruct this cuts mean VA distance to the target by 33% over system-prompting and 25% over few-shot prompting, lifting valence/arousal correlation to r_v=0.93 and r_a=0.75. The gains
    
[^503]: 复杂神经网络下降的Kähler景观与包括Calabi-Yau流形搜索与消灭的保证

    K\"ahler landscapes for complex neural network descents and guarantees including a search and destroy of the Calabi-Yau manifold

    [https://arxiv.org/abs/2608.19584](https://arxiv.org/abs/2608.19584)

    本文提出了一种在复参数神经网络中使用Kähler信息度量和自然梯度下降的新方法，并针对Calabi-Yau流形上的不良曲率条件提供了理论保证，通过几何定义的全局势实现了搜索与消灭策略。

    

    我们研究复参数化网络的景观。我们的方法受参数的信息论流形视角以及经典优化保证的启发，尽管涉及复杂几何变体，如通过Dolbeault渐近。下降路径在交叉熵下承认Kähler信息度量，通过Wirtinger Hessian作用于对数似然势。我们关注一种下降更新规则，采用自然梯度下降，通过逆度量缩放的微分损失，使下降路径保持在全纯切丛中。我们强调Calabi-Yau信息流形，这些流形通过不良曲率条件的景观提供了理论保证。在Calabi-Yau度量下，特别是在非紧致设置中，具有全局势而非调用Calabi猜想的拓扑要求，一个楔入无处消失的全纯...

    arXiv:2608.19584v1 Announce Type: new  Abstract: We study landscapes for complex-parameterized networks. Our approach is motivated with an information-theoretic manifold perspective of the parameter and via classical optimization guarantees although of complex geometric variety such as through Dolbeault asymptotics. The descent path admits a K\"ahler information metric under a cross-entropy via the Wirtinger Hessian on the log-likelihood potential. We restrict attention to a descent update rule with natural gradient descent via a differentiated loss scaled by the inverse metric, so the descent path remains in the holomorphic tangent bundle. We emphasize Calabi-Yau information manifolds which profane theoretical guarantees via an ill-curvature-conditioned landscape. Under a Calabi-Yau metric, specifically in a non-compact setting with a global potential so defined geometrically rather than invoking the topological requirements of the Calabi conjecture, a wedged nowhere-vanishing holomor
    
[^504]: 学习后遗忘：预训练中一个可测量的单样本反事实实验

    Learned, Then Lost: A Measured Single-Example Counterfactual in Pre-training

    [https://arxiv.org/abs/2608.19168](https://arxiv.org/abs/2608.19168)

    通过小规模反事实实验，直接测量了单个训练样本在预训练模型中的即时影响及其衰减，揭示了模型对特定数据的短暂学习能力。

    

    arXiv:2608.19168v1 公告类型：新 摘要：单个训练样本对最终模型的贡献通常是被估计而非直接测量的，因为测量它需要两次昂贵的完整预训练运行，且这两次运行仅在一个批次中的一行数据上有所不同。我们在小规模下进行了24次这种反事实实验。我们使用OpenWebText数据集，从头开始训练了32个GPT-2模型，参数规模为124M，跨越四个条件和八个随机种子。在9,536步中的第200步，即峰值学习率时，我们将一个256行批次中的一行替换为固定上下文注入，其中包含一段194个令牌的文本。三个注入条件为：1. 流畅的散文，主题在语料库中有证实；2. 流畅的散文，但主题为虚构，且与条件1在全批次梯度差异上匹配度在0.14%以内；3. 随机键盘字符。第四个条件是无注入的孪生模型。该文本在一次曝光后被学习，然后逐渐衰减。在注入后50步，看到该文本的模型比未看到的模型预测该文本的效果好0.039。

    arXiv:2608.19168v1 Announce Type: new  Abstract: A single training example's contribution to a finished model is normally estimated rather than measured, because measuring it takes two expensive full pre-training runs that differ in one row of one batch. We ran that counterfactual 24 times at a small scale. We trained 32 GPT-2 models at 124M parameters from scratch on OpenWebText, over four conditions and eight seeds. At step 200 of 9,536, at peak learning rate, we replaced one row of a 256-row batch with a fixed context injection carrying a 194-token passage. The three injected conditions are: 1. fluent prose with a corpus-attested subject, 2. fluent prose with a fabricated subject matched to it within 0.14% on full-batch gradient delta, and 3. random keyboard characters. The fourth condition is an uninjected twin. The passage is learned from one exposure and then decays. Fifty steps after injection, the arm that saw a passage predicts it better than the arm that did not by 0.039 and 
    
[^505]: 预算有限下的协调：少标签的联邦主动学习

    Coordination on a Budget: Federated Active Learning with Few Labels

    [https://arxiv.org/abs/2608.18634](https://arxiv.org/abs/2608.18634)

    该论文揭示了低预算联邦主动学习中同质数据比异质数据更需协调的异质性反转现象，并提出基于联邦表示学习的框架来优化查询选择。

    

    联邦主动学习（FAL）解决了数据隐私和标签稀缺的双重挑战，其中缺乏全局数据视图为协调查询选择带来了额外障碍。我们研究了低预算场景下的跨孤岛FAL，在此场景中，标注决策最为关键。我们在理论和实证上刻画了一种异质性反转：在低预算设置中，同质（IID）数据需要更强的协调以避免冗余查询，而异质数据则自然促进多样性；这种趋势在高预算时发生逆转。因此，与标准联邦学习（FL）中异质性作为主要挑战的叙事相反，我们表明在FAL中，IID设置对查询选择更具挑战性。基于这些发现，我们提出了一种新的FAL框架，利用联邦表示学习将客户端数据对齐到共享嵌入空间中，从而使服务器能够……

    arXiv:2608.18634v1 Announce Type: new  Abstract: Federated Active Learning (FAL) addresses the dual challenges of data privacy and label scarcity, where the absence of a global data view introduces additional hurdles for coordinated query selection. We study cross-silo FAL in the low-budget regime, where annotation decisions are most critical. We characterize, both theoretically and empirically, a heterogeneity reversal: in low-budget settings, homogeneous (IID) data requires stronger coordination to avoid redundant queries, whereas heterogeneous data naturally promotes diversity; this trend reverses at higher budgets. Thus, in contrast to the standard federated learning (FL) narrative where heterogeneity is a primary challenge, we show that IID settings are more challenging for query selection in FAL.   Motivated by these findings, we propose a new FAL framework that utilizes federated representation learning to align client data in a shared embedding space. This enables the server to
    
[^506]: 关于自我改进智能体的脆弱性：方差、任务顺序与规范不足

    On the Fragility of Self-Improving Agents: Variance, Task Order, and Underspecification

    [https://arxiv.org/abs/2608.18066](https://arxiv.org/abs/2608.18066)

    本文通过多运行方差分析和任务顺序随机化实验，揭示了基于记忆的自我改进智能体在评估噪声和任务顺序依赖上的脆弱性，指出其改进效果高度不稳定且受隐含课程影响。

    

    arXiv:2608.18066v1 公告类型：新论文 摘要：基于记忆的自我改进智能体——即从在线任务流中学习，并通过维护文本记忆库随时间提升性能的智能体——在近期文献中展现出巨大潜力。然而，这些方法的可靠性方面被严重忽视。在本工作中，我们对两种基于记忆的方法进行了全面重新评估，沿两个维度拓宽了评估范围：（1）包含多次运行以量化方差，（2）随机打乱任务顺序以研究任务顺序的影响。通过这些实验，我们得出两个观察结果，揭示了当前方法的脆弱性：首先，在复杂环境和多步骤任务中，智能体评估本质上具有噪声性，而叠加自我改进循环可能进一步放大这种噪声。其次，智能体的改进高度依赖于任务顺序。先前工作常采用默认排序，这隐含地施加了一种课程学习，从而激活了...

    arXiv:2608.18066v1 Announce Type: new  Abstract: Memory-based self-improving agents--those that learn from an online stream of tasks and improve over time by maintaining a textual memory bank--have shown great promise in recent literature. However, the reliability aspects of these methods have been critically overlooked. In this work, we conduct a comprehensive re-evaluation of two memory-based methods, broadening the scope of evaluation along two axes: (1) including multiple runs to quantify variance, and (2) randomly shuffling the tasks to investigate the effect of task order. Through these experiments, we make two observations that expose the fragility of current methods: First, agent evaluation is inherently noisy in complex environments and on multi-step tasks, and stacking a self-improving loop on top can further amplify this noise. Second, the agent's improvement is highly dependent on task order. Prior works often adopt default orderings that impose an implicit curriculum, acti
    
[^507]: 在线线性回归中的特征启动：稀疏遗憾下界与紧致单变量速率

    Feature Priming in Online Linear Regression: Sparse-Regret Lower Bounds and a Tight Univariate Rate

    [https://arxiv.org/abs/2608.17573](https://arxiv.org/abs/2608.17573)

    本文证明在特征启动的在线线性回归中，三种规则均无法实现稀疏对数遗憾，并揭示了干扰插值导致的共同障碍，通过Hadamard构造给出下界。

    

    在高维在线预测中，最佳预测器可能仅依赖于少数几个特征，因此遗憾应随稀疏性而非环境维度扩展。特征启动通过从过去数据估计特征权重，并在重新缩放的设计上重新拟合最小范数预测器来实现这一目标。Warmuth和Amid在COLT 2023上询问，这三种规则中是否有任何一种能提供竞争性的在线遗憾保证。基于仅使用过去数据的自然Moore-Penrose协议，我们对这一COLT开放问题的稀疏对数形式给出了否定答案。我们的分析识别出一个共同障碍：廉价的干扰插值导致重新拟合低估了真正预测性坐标的权重。一个精确的目标质量恒等式和双符号论证将此效应转化为裁剪预测损失。Hadamard构造迫使所有三种规则对零损失单稀疏比较器产生$\Omega(\min\{T,\sqrt{d}\})$的遗憾，并附带扩展结果。

    arXiv:2608.17573v1 Announce Type: cross  Abstract: In high-dimensional online prediction, the best predictor may depend on only a few features, so regret should scale with sparsity rather than the ambient dimension. Feature priming pursues this goal by estimating feature weights from past data and refitting a minimum-norm predictor on the rescaled design. Warmuth and Amid asked at COLT 2023 whether any of three such rules admits a competitive online regret guarantee. Using the natural Moore--Penrose protocol based only on past data, we give a negative answer to the sparse-logarithmic form of this COLT open problem. Our analysis identifies a common obstruction: cheap nuisance interpolation causes the refit to underweight the truly predictive coordinate. An exact target-mass identity and a two-sign argument turn this effect into clipped prediction loss. Hadamard constructions force $\Omega(\min\{T,\sqrt{d}\})$ regret for all three rules against a zero-loss one-sparse comparator, with ext
    
[^508]: 金融新闻NLP中的时间泄漏：一项基于特定制度并购信号的多架构审计

    Temporal Leakage in Financial News NLP: A Multi-Architecture Audit with a Regime-Specific M&A Signal

    [https://arxiv.org/abs/2608.17223](https://arxiv.org/abs/2608.17223)

    本文通过多架构审计发现，随机划分会严重夸大金融新闻NLP模型的性能，且并购（M&A）是唯一在时间顺序评估下具有稳健正向信号的类别。

    

    金融新闻方向预测已成为一个流行的NLP基准，但报告的性能提升关键取决于训练-测试划分是按时间顺序还是随机的，即是否存在时间泄漏。我们在一个包含49,799篇文章的语料库上，对16种特征-模型组合（涵盖TF-IDF、MiniLM、FinBERT以及微调的RoBERTa-large/DeBERTa-v3-large）进行了审计，并分别对Llama-3和Qwen2.5 LLM进行了零样本/少样本和LoRA探测。结果显示，随机划分将MCC（马修斯相关系数）夸大了1.1倍至6.5倍，且这种夸大程度与模型容量和特征丰富度相关；端到端的FinBERT微调不仅没有缩小这一差距，反而进一步放大（大小匹配比率为1.75倍）。按事件类型进行条件分析时，并购（M&A）是唯一在接近时间顺序的评估下具有正向锁定测试信号的类别（TF-IDF MCC在仅训练集上为0.138，在训练∪验证集重新拟合下为0.068；10,000次置换检验p < 10^-3）；该信号并未转移至其他类别。

    arXiv:2608.17223v1 Announce Type: new  Abstract: Financial-news direction prediction has become a popular NLP benchmark, yet reported gains depend critically on whether the train-test split is chronological or random, i.e., on temporal leakage. We audit this dependence on a 49,799-article corpus across 16 feature-model combinations spanning TF-IDF, MiniLM, FinBERT, and fine-tuned RoBERTa-large / DeBERTa-v3-large, plus separate zero/few-shot and LoRA probes of Llama-3 and Qwen2.5 LLMs: random splits inflate MCC by $1.1\times$ to $6.5\times$, tracking model capacity and feature richness, and end-to-end FinBERT fine-tuning re-amplifies rather than closes the gap (size-matched ratio $1.75\times$). Conditioning on event type, mergers and acquisitions (M&A) is the only audited category with a positive locked-test signal under near-temporal chronological evaluation (TF-IDF MCC $= 0.138$ train-only, $0.068$ under train$\cup$val refit; 10,000-permutation $p < 10^{-3}$); the signal does not tran
    
[^509]: 描绘ε：追求图像的身份级隐私保证

    Picture the Epsilon: Pursuing Identity-Level Privacy Guarantees for Images

    [https://arxiv.org/abs/2608.17147](https://arxiv.org/abs/2608.17147)

    本文系统比较了四种审计黑盒人脸生成器身份级差分隐私的方法，并揭示了各方法的适用性与局限性，为隐私保证验证提供了实践指南。

    

    arXiv:2608.17147v1 公告类型：交叉 摘要：图像到图像的人脸生成器被广泛使用，其输出与源图像之间的视觉不相似性有时被视为隐私的证据。审计这些系统是否满足正式的身份级(ε, δ)-差分隐私，需要在多种将嵌入空间观测转换为差分隐私参数ε的估计或界限的路线中进行选择。我们提出了四种适用于预训练、黑盒人脸生成器的审计方法的比较研究：基于每身份敏感性的高斯机制解读（GaussMech）；通过基本组合聚合的逐维核密度对数比率（KDE-LR）；通过总变差距离从最大均值差异推导的纯DP ε的解析群体级下界（MMD-TV）；以及交叉验证分类器折外ROC的假设检验评估（ROC-HT）。对每种方法，我们进行了详细的实验……

    arXiv:2608.17147v1 Announce Type: cross  Abstract: Image-to-image face generators are widely used, and visual dissimilarity between their outputs and source images is sometimes treated as evidence of privacy. Auditing whether these systems satisfy formal identity-level (epsilon, delta)-differential privacy requires choosing among several distinct routes for converting embedding-space observations into estimates or bounds on the differential privacy parameter epsilon. We present a comparative study of four such audits applicable to pre-trained, black-box face generators: a Gaussian-mechanism reading of per-identity sensitivity (GaussMech); a per-dimension kernel-density log-ratio aggregated by basic composition (KDE-LR); an analytical population-level lower bound on pure-DP epsilon derived from the maximum mean discrepancy via the total variation distance (MMD-TV); and a hypothesis-testing evaluation of a cross-validated classifier's out-of-fold ROC (ROC-HT). For each method we make exp
    
[^510]: CaliBench：视频世界模型的随机动力学是否经过物理校准？

    CaliBench: Are the Stochastic Dynamics of Video World Models Physically Calibrated?

    [https://arxiv.org/abs/2608.16829](https://arxiv.org/abs/2608.16829)

    CaliBench提出了一种在物理可解释离散空间中进行视频世界模型校准评估的新方法，将性能分解为可评分性和校准性两个正交维度，并利用闭式已知参考分布实现精确测试。

    

    视频世界模型通过生成采样来近似物理结果的随机分布，但现有基准测试要么对单个生成结果进行评分，要么在整个数据集上粗略地比较分布，从而未测试特定现象的细粒度偶然不确定性。我们引入了CaliBench，它在物理可解释的离散空间中对结果进行评分——例如箱索引、骰子面、花色、颜色——而不是像FID中那样使用学习到的特征空间，因此可以直接测量与已知参考分布的距离。我们构建了参考分布以闭式形式已知的结果空间（二项式高尔顿板、伯努利叉、均匀骰子/扑克牌/彩票、偏斜的欧洲轮盘颜色），从而实现精确的校准测试。我们将性能分解为两个正交轴，而单一的准确度指标会混淆它们：可评分性（生成结果可评分的比例）和校准性（生成分布与参考分布的一致性）。

    arXiv:2608.16829v1 Announce Type: cross  Abstract: Video world models approximate the stochastic distribution of physical outcomes through generative sampling, but existing benchmarks score individual generations or compare distributions coarsely over a whole dataset, leaving the fine-grained aleatoric uncertainty of specific phenomena untested. We introduce CaliBench, which scores outcomes in a physically interpretable discrete space - a bin index, a die face, a suit, a colour - rather than a learned feature space such as in FID, so the distance from a known reference distribution is measured directly. We curate outcome spaces whose reference is known in closed form (binomial Galton boards, Bernoulli forks, uniform dice/cards/lottery, a skewed European-roulette colour), enabling an exact calibration test. We decompose performance into two orthogonal axes that a single accuracy metric conflates: scorability, the fraction of generations yielding a scoreable outcome, and calibration, the
    
[^511]: 推进开放且可复现的关系学习：RelArena-α、TabPFN-Rel 和 RPI

    Advancing Open and Reproducible Relational Learning: RelArena-$\alpha$, TabPFN-Rel and RPI

    [https://arxiv.org/abs/2608.16319](https://arxiv.org/abs/2608.16319)

    本文通过开源 RelArena-α 统一框架、TabPFN-Rel 和 RPI 三款软件，旨在建立关系学习任务的可复现比较标准，以推动该领域开放科学和实际应用。

    

    arXiv:2608.16319v1 公告类型：新  摘要：Prior Labs 在关系学习领域的首次发布展示了我们对开放科学的持续承诺。我们开源了三款软件，期望能加速该领域研究向有意义的实际影响迈进。我们旨在根据社区的反馈并与之合作，进一步引导开发方向。鉴于开发尚处于早期阶段，我们的 α 版本主要面向研究人员和早期采用者。过去几年中，出现了多种关系学习的数据集和任务，但社区尚未就这些任务上比较不同方法的可靠、可复现方式达成共识。我们的 α 版本 RelArena-α 提供了一个统一框架，用于在 RelBench v1 上运行和比较基线方法，通过标准化数据加载、评估协议、调参制度，并支持自定义调参的系统，灵感来源于诸如 TabArena 等成熟的表格基准。

    arXiv:2608.16319v1 Announce Type: new  Abstract: This first release of Prior Labs in relational learning shows our continued commitment to open science. We open-source three pieces of software that we expect to accelerate research in the field towards meaningful real-world impact. We aim to steer further development based on feedback from, and in collaboration with, the community. Given the early stage of development, our $\alpha$-release targets researchers and early-adopting practitioners. Over the past years, a variety of datasets and tasks for relational learning have emerged, but the community has not converged on a reliable, reproducible way to compare different methods on these tasks. Our $\alpha$-release, RelArena-$\alpha$, provides a unified framework for running and comparing baselines on RelBench v1 by standardizing data loading, evaluation protocols, tuning regimes, and support for systems with custom tuning, inspired by established tabular benchmarks such as TabArena. We p
    
[^512]: 保证的自适应模态获取：当策略选择自己的校准组时

    Guaranteed Adaptive Modality Acquisition: When the Policy Chooses Its Own Calibration Group

    [https://arxiv.org/abs/2608.15520](https://arxiv.org/abs/2608.15520)

    本文提出了RouteCert方法，通过两种有限样本构造（终端模式校准和策略-模式对同步认证），解决了自适应模态获取中策略诱导分组导致的条件校准失效问题，并提供了保证有效性的条件及反例验证。

    

    一个多模态系统可能在推理开始时仅持有部分输入，并可能以一定成本获取其余输入。通过自适应获取，策略决定了最终观察到的输入，因此我们根据该最终输入模式来陈述保证。条件校准通常假设分组映射独立于校准样本固定，而策略诱导的分组不满足此条件。我们刻画了模式条件保证何时仍然有效，并给出了两个有限样本构造：在终端模式上应用校准的无阈值路由，以及完整策略-模式对的同步认证，这允许校准数据选择部署的策略。一个反例表明，对于校准独立的分组映射证明的保证，在策略使终端组依赖校准时不一定能转移。我们将所得方法称为RouteCert。在临床电子健康记录数据上，该方法相比基线方法在保证覆盖率和成本之间实现了更好的权衡。

    arXiv:2608.15520v1 Announce Type: new  Abstract: A multimodal system may begin inference holding only some of its inputs and may acquire the rest at a cost. With adaptive acquisition, the policy determines which inputs are ultimately observed, so we state the guarantee conditional on that terminal input pattern. Conditional calibration normally assumes the grouping map is fixed independently of the calibration sample, which policy-induced grouping does not satisfy. We characterize when pattern-conditional guarantees remain valid and give two finite-sample constructions: threshold-free routing with calibration applied at the terminal pattern, and simultaneous certification of complete policy-pattern pairs, which lets calibration data select the deployed policy. A counterexample shows that a guarantee proved for a calibration-independent grouping map need not transfer once the policy makes the terminal group calibration-dependent. We call the resulting method RouteCert. On a clinical ele
    
[^513]: 在线凸优化与决斗反馈

    Online Convex Optimization with Dueling Feedback

    [https://arxiv.org/abs/2608.15050](https://arxiv.org/abs/2608.15050)

    本文首次将决斗反馈应用于对抗性凸优化，通过简单归约转换为近似梯度，实现了静态、自适应和动态遗憾的最优保证，并在光滑和强凸条件下获得改进速率。

    

    我们研究了带有决斗（成对比较）反馈的在线凸优化问题，其中学习者仅能观察到两个查询点之间的二元偏好。尽管决斗反馈在离散或随机设置中已被充分理解，但对抗性凸设置仍未得到探索。我们提出了一种简单的归约方法，将决斗反馈转换为近似梯度，从而能够使用标准的一阶方法。我们表明，在这种归约下，遗憾保证可以转移，为该设置提供了首个结果，包括$\mathcal{O}(T^{3/4})$的静态、自适应和动态遗憾。在额外结构下，我们获得了改进的速率：对于光滑目标为$\mathcal{O}(T^{2/3})$，对于强凸函数为$\mathcal{O}(\sqrt{T \log T})$。

    arXiv:2608.15050v1 Announce Type: new  Abstract: We study online convex optimization with dueling (pairwise comparison) feedback, where the learner observes only a binary preference between two queried points. While dueling feedback is well understood in discrete or stochastic settings, the adversarial convex setting has remained unexplored. We propose a simple reduction that converts dueling feedback into approximate gradients, enabling the use of standard first-order methods. We show that regret guarantees transfer under this reduction, yielding the first results for this setting, including $\mathcal{O}(T^{3/4})$ static, adaptive, and dynamic regret. Under additional structure, we obtain improved rates of $\mathcal{O}(T^{2/3})$ for smooth objectives and $\mathcal{O}(\sqrt{T \log T})$ for strongly convex functions.
    
[^514]: 高效无海森方法用于非凸下层的多目标双层优化

    Efficient Hessian-Free Methods for Multi-Objective Bilevel Optimization with Nonconvex Lower Level

    [https://arxiv.org/abs/2608.12704](https://arxiv.org/abs/2608.12704)

    本文提出了一种新的多目标Moreau包络无海森算法（MOMEHA），首次解决了非凸下层问题的多目标双层优化，通过单循环和无海森设计实现了高效计算。

    

    摘要：arXiv:2608.12704v1 公告类型：交叉 摘要：多目标双层优化在人工智能领域有广泛应用，如自动学习和多任务元学习。尽管最近一些工作开始研究多目标双层优化，但提出的方法依赖于（强）凸下层问题。实际上，这些多目标双层学习问题通常是非凸的，特别是其下层问题是非凸的。为填补这一空白，我们提出了一类基于多目标Moreau包络的无海森算法（MOMEHA），用于解决具有非凸下层的多目标双层学习问题。具体来说，我们的方法使用Moreau包络将原始问题转换为带有包络约束的多目标单层优化。特别是，我们的方法通过引入平滑处理，在多目标设置中保留了单循环和无海森的计算优势。

    arXiv:2608.12704v1 Announce Type: cross  Abstract: Multi-objective bilevel optimization has wide applications in the AI area such as automated learning and multi-task meta-learning. Although recently some works have been begun to study the multi-objective bilevel optimization, the proposed methods rely on the (strongly) convex lower level problems. In fact, these multi-objective bilevel learning problems are generally nonconvex, and particularly their lower level problems are nonconvex. To fill this gap, we propose a class of Multi-Objective Moreau Envelope based Hessian-free Algorithms (MOMEHA) to solve the multi-objective bilevel learning problems with nonconvex lower level. Specifically, our method uses the Moreau envelope to convert the original problem into a multi-objective single-level optimization with an envelope constraint. In particular, our method retains computational advantages of being single-loop and Hessian-free in the multi-objective setting by incorporating a smooth 
    
[^515]: 拟阵上泊松过程子模最大化的对抗韧性：从鲁棒离线优化到全臂赌博学习

    Adversarial Resilience of Poisson-Process Submodular Maximization over Matroids: From Robust Offline Optimization to Full-Bandit Learning

    [https://arxiv.org/abs/2608.12134](https://arxiv.org/abs/2608.12134)

    本文证明了SGS-Poisson算法在任意受控价值预言机下，无需修改其核心机制，即可保持子模最大化的逼近因子，并由此推导出全臂赌博学习算法。

    

    我们研究了在一般拟阵约束下，当离线算法被赋予任意受控价值预言机时的非负子模最大化问题。我们的主要结果是针对“恶意贪心交换泊松过程”（SGS-Poisson）的对抗韧性定理：在不修改其泊松强度、单元素交换规则或恶意丢弃步骤的情况下，该算法对非单调目标保持极限逼近因子$1/e$，对单调目标保持$1-1/e$。更精确地说，在满足$|\widehat f(S)-f(S)|\le \xi$（对所有集合$S$）的每个受控预言机$\widehat f$下，我们的实现返回一个可行集合，其期望值分别至少为$(1/e-\varepsilon)\OPT-O(k\xi)$和$(1-1/e-\varepsilon)\OPT-O(k\xi)$，使用$\widetilde O(nk^2\varepsilon^{-2})$次预言机调用。因此，离线到在线的归约产生了针对一般拟阵约束子模奖励的全臂CMAB算法。

    arXiv:2608.12134v1 Announce Type: cross  Abstract: We study nonnegative submodular maximization subject to a general matroid when the offline algorithm is given an arbitrary controlled value oracle. Our main result is an adversarial resilience theorem for the Spiteful Greedy Swap Poisson Process (SGS-Poisson): without modifying its Poisson intensity, single-element exchange rule, or spiteful drop step, the algorithm retains limiting approximation factors $1/e$ for non-monotone objectives and $1-1/e$ for monotone objectives. More precisely, under every controlled oracle $\widehat f$ satisfying $|\widehat f(S)-f(S)|\le \xi$ for every set $S$, our implementation returns a feasible set with expected value at least $(1/e-\varepsilon)\OPT-O(k\xi)$ and $(1-1/e-\varepsilon)\OPT-O(k\xi)$, respectively, using $\widetilde O(nk^2\varepsilon^{-2})$ oracle calls. As a consequence, the offline-to-online reduction yields full-bandit CMAB algorithms for general matroid-constrained submodular rewards wi
    
[^516]: Mechanist：将人工智能作为科学仪器，探索智能机制

    Mechanist: AI as a Scientific Instrument for Discovering the Mechanisms of Intelligence

    [https://arxiv.org/abs/2608.12036](https://arxiv.org/abs/2608.12036)

    Mechanist是一个自主代理系统，通过集成大规模知识图谱和多学科数据库，将AI作为科学仪器，自动发现AI智能的底层机制，从而弥合模型能力与人类理解控制之间的差距。

    

    arXiv:2608.12036v1 公告类型：新 摘要：人工智能模型在多个领域取得了显著成功，但其能力背后的机制以及可能带来的风险仍知之甚少。随着人工智能开发速度加快并日益自动化，机制探索仍主要依赖人工，这加剧了模型能力与我们理解和控制它们之间的差距。为弥合这一差距，我们引入了Mechanist，一个代理系统，将人工智能用作科学仪器，自主发现人工智能智能的机制。为支持自主机制探索，我们构建了一个以可解释性为重点的知识图谱，包含约13,000篇论文，并将其与一个涵盖26个领域、包含4300万篇论文的多学科数据库集成。我们还整理了一个包含32种基础方法的库，用于机制分析、因果干预和验证。与Claude Code和现有AI科学家系统相比，Mechanist……

    arXiv:2608.12036v1 Announce Type: new  Abstract: AI models have achieved remarkable success across diverse domains, yet the mechanisms underlying their capabilities and the risks they may pose remain poorly understood. As AI development becomes faster and increasingly automated, mechanistic exploration remains largely manual, widening the gap between what models can do and our ability to understand and control them. To bridge this gap, we introduce Mechanist, an agentic system that uses AI as a scientific instrument for the autonomous discovery of mechanisms underlying AI intelligence. To support autonomous mechanistic discovery, we construct an interpretability-focused knowledge graph of approximately 13,000 papers and integrate it with a multidisciplinary database of 43 million papers spanning 26 fields. We further curate a library of 32 foundational methods for mechanism analysis, causal intervention, and validation. Compared with Claude Code and existing AI-scientist systems, Mecha
    
[^517]: 如何验证预测模型的概率一致性

    How to Verify Probabilistic Consistency of Predictive Models

    [https://arxiv.org/abs/2608.11181](https://arxiv.org/abs/2608.11181)

    该论文提出了一种交互式PCP协议，使多项式时间的验证者能够通过少量查询验证由概率电路和置信度电路所指定的预测模型的概率自洽性，从而为AI安全中的可验证诚实预测提供了保障。

    

    当一个概率预测器回答大量条件概率查询时，它的答案是否自洽？能否在多项式时间内验证这一点？这个问题对于AI安全具有重要意义，因为其安全性源自对AI行为可能引发的不良后果进行概率预测时的诚实性。我们构建了一个交互式PCP（概率可检验证明）协议。设预测模型由概率电路P和一个输出预测置信度的电路Q指定。P和Q共同隐式地指定了指数级多的概率断言。我们提出了一个协议，使多项式时间的验证者能够验证(P,Q)的近似一致性。验证者获得电路对(P,Q)，只在少数几个点上对它们进行求值；同时它还获得一个证明预言机——这是据称与(P,Q)的预测相一致的一个见证概率分布的编码——验证者只读取其中的少数位置。

    arXiv:2608.11181v2 Announce Type: replace-cross  Abstract: When a probabilistic predictor answers many conditional-probability queries, are its answers self-consistent, and can this be verified in polynomial time? This problem is of interest for AI safety, where safety is derived from honesty about probabilistic predictions of unwanted outcomes potentially caused by an AI action. We construct an interactive PCP as follows. Let a predictive model be specified by a probability circuit P and a circuit Q which outputs confidence in predictions. Together, P and Q implicitly specify exponentially many probabilistic claims. We show a protocol in which a polynomial-time verifier can verify the approximate consistency of (P,Q). The verifier is given the pair of circuits (P,Q), which it evaluates at only a few points; alongside them it is given a proof oracle, an encoding of a witnessing probability distribution allegedly consistent with the predictions of (P,Q), which it reads at a few location
    
[^518]: softmax注意力的量子路线图：概率单纯形上softmax注意力的精确玻恩规则类比

    A Quantum Roadmap for Softmax Attention: Exact Born-Rule Analogs for Softmax Attention on the Probability Simplex

    [https://arxiv.org/abs/2608.11173](https://arxiv.org/abs/2608.11173)

    该论文证明在输入输出约束于概率单纯形的场景下，softmax注意力可以逐分量地精确映射为量子测量过程（Hadamard检验得分、玻恩规则余弦平方族、后选择温度与确定性数值聚合信道），为注意力的量子实现提供了完整路线图。

    

    注意力机制构成了许多现代AI模型（如Transformer）的基础。在使用注意力机制的一类子问题中，输入和输出被约束在概率单纯形上，使得所有输出之和为1。在这种设定下，softmax注意力可以实现精确的、逐分量的量子对应实现。注意力得分是对振幅编码输入的块编码投影进行Hadamard检验所得的统计量。指数softmax是在精确双射下由玻恩规则测量所生成的余弦平方族的内部，其边界在有限参数值处以精确零点的形式表达稀疏注意力。softmax温度对应于一个重复次数，其中后选择测量轮次可精确实现离散化的逆温度。数值聚合是一个确定性的列加载信道，它对列随机的数值矩阵进行了扩张。门控残差是……的制备角度（原文摘要在此处截断）。

    arXiv:2608.11173v2 Announce Type: replace-cross  Abstract: The attention mechanism forms the foundation of many modern AI models such as the Transformer. In one subclass of problems where attention is used, inputs and outputs are bound to the probability simplex so that all outputs sum to one. In this setting, softmax attention admits an exact, component-by-component quantum realization. Attention scores are Hadamard-test statistics on block-encoded projections of amplitude-encoded inputs. The exponential softmax is the interior of a cosine-squared family generated by Born-rule measurement under an exact bijection, whose boundary expresses sparse attention with exact zeros at finite parameter values. The softmax temperature is a repetition count where post-selected measurement rounds realize discretized inverse temperature exactly. Value aggregation is a deterministic column-loading channel that dilates the column-stochastic value matrix. The gated residual is the preparation angle of 
    
[^519]: DoGMA：一种以中心法则为导向的肿瘤学多组学对齐与多任务学习基础模型

    DoGMA: A Central-Dogma-Guided Foundation Model for Multi-Omics Alignment and Multi-Task Learning in Oncology

    [https://arxiv.org/abs/2608.08148](https://arxiv.org/abs/2608.08148)

    提出了DoGMA，一个以中心法则为导向的泛癌症多组学基础模型，通过在Transformer-MoE架构中引入有向注意力和掩码分层组学重构预训练，将组学间信息流约束为中心法则方向，从而在异质性癌症、下游任务和模态不完整设置之间实现稳健迁移。

    

    注意力机制在现代深度学习中被广泛使用，许多现有多组学模型沿用了其传统用法，允许无限制的双向交互。然而，生命的基本逻辑是方向性的。现有设计往往忽略了中心法则所提示的方向性，这可能限制了模型在异质性癌症、下游任务以及模态不完整设置之间的迁移能力。在本工作中，我们提出了DoGMA，一个以中心法则为导向的泛癌症多组学分析基础模型，并论证了稳健的迁移需要具备领域特定归纳偏置的表示。具体而言，我们将其构建于Transformer-MoE架构之上，其中有向注意力使组学间通信偏向中心法则的信息流动方向。我们进一步采用掩码分层组学重构对模型进行预训练，引导其学习与中心法则一致的交互。

    arXiv:2608.08148v2 Announce Type: replace-cross  Abstract: Attention mechanisms have been widely utilized in modern deep learning, and many existing multi-omics models inherit their conventional use to allow unrestricted bidirectional interactions. However, the fundamental logic of life is directional. Existing designs often overlook the directionality suggested by the central dogma, potentially limiting transfer across heterogeneous cancers, downstream tasks, and incomplete modality settings. In this work, we present DoGMA, a central-dogma-guided foundation model for pan-cancer multi-omics analysis, arguing that robust transfer requires representations with domain-specific inductive bias. Concretely, we build it on a Transformer-MoE architecture where directed attention biases inter-omics communication toward central-dogma information flow. We further pretrain our model with masked hierarchical omics reconstruction to guide it toward learning central-dogma-consistent interactions. Acr
    
[^520]: 一种面向视觉令牌剪枝的AI4AI框架

    An AI4AI Framework for Visual Token Pruning

    [https://arxiv.org/abs/2608.07193](https://arxiv.org/abs/2608.07193)

    提出一种AI4AI框架，利用大语言模型自动设计视觉令牌剪枝算法，通过设计合适的搜索状态表示将LLM的通用算法知识转化为多模态大模型推理加速的专用解决方案。

    

    视觉令牌剪枝能够大幅降低多模态大语言模型（MLLMs）的推理成本，然而现有方法在很大程度上依赖于固定的、手工设计的启发式规则以及代价高昂的专家反复试验。随着剪枝目标、预算和模型架构的日益多样化，人工探索不断扩大的设计空间变得愈发困难。本文旨在构建一个用于视觉令牌剪枝的AI4AI框架，以回答一个自然的问题：大语言模型能否自动设计有效的视觉令牌缩减算法？尽管大语言模型拥有广泛的算法知识和强大的推理能力，但将这些通用知识转化为特定任务的有效解决方案仍然并非易事。我们认为，关键在于设计合适的搜索状态表示，将大语言模型的内部知识与视觉令牌剪枝的结构需求和约束相连接。

    arXiv:2608.07193v2 Announce Type: replace  Abstract: Visual-token pruning can substantially reduce the inference cost of multimodal large language models (MLLMs), yet existing methods largely rely on fixed, handcrafted heuristics and costly expert trial and error. As pruning objectives, budgets, and model architectures diversify, manually navigating the expanding design space becomes increasingly difficult. This paper aims to build an AI4AI framework for visual-token pruning by addressing a natural question: Can large language models automatically design effective visual-token reduction algorithms? Although LLMs possess broad algorithmic knowledge and strong reasoning capabilities, translating such general knowledge into effective solutions for a specialized task remains nontrivial. We argue that the key lies in designing an appropriate search-state representation that connects the internal knowledge of LLMs with the structural requirements and constraints of visual-token pruning. Base
    
[^521]: ArborEnum：基于连续特征的决策树Rashomon集合枚举

    ArborEnum: Decision Tree Rashomon Sets over Continuous Features

    [https://arxiv.org/abs/2608.04310](https://arxiv.org/abs/2608.04310)

    该论文提出了首个利用连续特征有序结构来精确枚举决策树Rashomon集合的算法，并进一步提供了用于近似枚举的松弛方法和可逐步细化结果的随时算法。

    

    Rashomon效应描述了许多模型在同一学习任务上能够取得几乎同等性能的现象，这对模型的鲁棒性、特征重要性和可定制性具有重要意义。这些应用场景促使人们计算Rashomon集合：即所有正则化损失接近最优的模型的集合。决策树是为数不多的可以完全枚举Rashomon集合的模型类别之一，但此类计算一直依赖于对原始数据的二值化处理，要么限制每棵树允许进行的分裂方式，要么大幅增加原本已经十分困难的组合问题的复杂性。我们提出了首个能够在利用连续特征有序结构的同时精确枚举决策树Rashomon集合的算法。我们还进一步开发了用于近似枚举的松弛方法以及一种随时算法，该算法可以逐步细化模型集合……

    arXiv:2608.04310v3 Announce Type: replace  Abstract: The Rashomon effect describes the phenomenon that many models can achieve nearly equivalent performance on the same learning task, with significant ramifications for robustness, feature importance, and customizability. These use cases motivate the computation of Rashomon sets: the set of all models whose regularized loss is near-optimal. Decision trees are one of the few model classes for which Rashomon sets can be fully enumerated, but this computation has always been conditional on a binarization of the original data, either restricting which splits each tree is allowed to make or substantially increasing the complexity of an already difficult combinatorial problem. We introduce the first algorithm that exactly enumerates decision-tree Rashomon sets while exploiting the ordered structure of continuous features. We further develop a relaxation for approximate enumeration and an anytime algorithm that progressively refines the set of
    
[^522]: TimeRLM：递归语言模型实现长上下文时间序列中的精确异常定位

    TimeRLM: Recursive Language Models Enable Precise Anomaly Localization in Long-Context Time-Series

    [https://arxiv.org/abs/2608.03391](https://arxiv.org/abs/2608.03391)

    TimeRLM 提出了一种面向时间序列的递归语言模型方法，让大语言模型通过代码和视觉能力对外部信号进行顺序化操作，从而在长上下文时间序列中实现精确的异常定位，并发布了 AnomalyXL 合成基准数据集。

    

    长上下文时间序列上的精确异常定位是临床监护、工业运营、金融服务和物流等监测应用中的一项关键任务，在这些场景中，短暂的关键证据可能隐藏在高频数据的长时段内。时间序列语言模型（TSLM）能够接收时间序列数据，并用自然语言表述其对异常的发现；然而，近期的基准测试报告显示，这类模型在长上下文下的检索性能会下降，这与文本、视觉和音频领域中的失败模式类似。在文本领域，递归语言模型（RLM）通过将上下文保持在大语言模型（LLM）外部，并允许模型通过代码对其进行查询，从而能够恢复大部分损失的性能。我们提出了 TimeRLM，一种针对时间序列的 RLM 形式化方法，它利用代码和视觉能力对信号进行顺序化操作。此外，我们还介绍了 AnomalyXL，一个合成的长上下文异常定位基准。

    arXiv:2608.03391v2 Announce Type: replace  Abstract: Precise anomaly localization over long-context time series is a crucial task in monitoring applications across clinical care, industrial operations, financial services, and logistics, where brief evidence may hide inside long spans of high-frequency data. Time-Series Language Models (TSLMs) are able to ingest time series data and verbalize findings on anomalies in natural language; however, recent benchmarks report a decrease in retrieval performance at long contexts, mirroring failure modes in text, vision, and audio. In the text domain, Recursive Language Models (RLMs) can recover much of this lost performance by keeping context external to the large language model (LLM), allowing the model to query it through code. We present TimeRLM, an RLM formulation for time-series that sequentially manipulates the signal using code and vision capabilities. We further introduce AnomalyXL, a synthetic long-context anomaly localization benchmark
    
[^523]: 扩散模型中的伪随机流作为可学习输入影响生成质量

    Pseudorandom Streams within Diffusion Models Act as Learnable Inputs That Affect Generation Quality

    [https://arxiv.org/abs/2608.02575](https://arxiv.org/abs/2608.02575)

    本文发现扩散模型中的伪随机流若包含可学习结构，会被模型利用以降低损失，从而显著影响生成质量，且这种效应主要源于扩散噪声角色的可预测性。

    

    数字学习系统消耗的是具体的伪随机值，而非抽象的随机变量。这些值在训练期间进入实际损失及其梯度。如果伪随机流包含模型可访问的结构，那么这种结构就可能成为学习系统所学内容的一部分。我们证明，这种效应足以改变扩散模型的生成质量。对于扩散噪声预测，存在两条相关途径可减少损失：模型可以学习干净数据的规律性并利用它们推断添加的噪声，也可以利用噪声源本身的规律性从噪声输入中恢复实际噪声。随机角色消融实验表明，在我们实验中主要的源相关效应与扩散噪声角色相关。一种去除可重用真实图像结构的扩散探针仍显示出明显的源依赖性。

    arXiv:2608.02575v2 Announce Type: replace  Abstract: Digital learning systems consume concrete pseudorandom values rather than abstract random variables. These values enter the realized loss and its gradient during training. If a pseudorandom stream contains structure that is accessible to the model, this structure can therefore become part of what the learning system learns. We show that this effect can be strong enough to change generation quality in diffusion models. For diffusion noise prediction, there are two related routes by which the loss can be reduced. The model can learn regularities of the clean data and use them to infer the added noise. It can also exploit regularities of the noise source itself to recover the realized noise from the noisy input. Random-role ablation shows that the dominant source-dependent effect in our experiments is associated with the diffusion-noise roles. A diffusion probe that removes reusable real-image structure still shows clear source-dependen
    
[^524]: 一种基于人工智能的日前光伏预测决策支持流程

    An AI-Based Decision-Support Pipeline for Day-Ahead Photovoltaic Forecasting

    [https://arxiv.org/abs/2608.02088](https://arxiv.org/abs/2608.02088)

    该研究针对数据缺失严重的实际光伏站点，提出了一套包含时间戳校验、特征工程和非负最小二乘堆叠五模型组合的AI日前预测流程，在英国充电站数据上优于智能持续性基线。

    

    可靠的光伏（PV）预测可以支持低碳能源系统，但已部署的场地可能只有短暂且不完整的记录。物理和混合方法可能对天气输入、校准和时间戳对齐较为敏感，而单个机器学习模型可能只能捕捉预测问题的部分内容。我们利用英国某充电站一年的逆变器测量数据（其中9.25%的小时数据缺失），研究其日前逐小时光伏预测。该流程检查时间戳对齐、推导太阳辐射和晴朗度特征、添加短期天气上下文信息，并使用非负最小二乘堆叠方法将五个互补模型组合起来（组合仅基于验证观测数据拟合）。我们将其与“智能持续性”基线进行比较，该基线利用目标日辐照度将前一天的PV行为向前推演。结合回溯性天气数据，组合模型降低了（预测误差）……

    arXiv:2608.02088v2 Announce Type: replace-cross  Abstract: Reliable photovoltaic (PV) forecasts can support low-carbon energy systems, but deployed sites may have only short and incomplete records. Physical and hybrid methods can be sensitive to weather inputs, calibration, and timestamp-alignment, while individual machine learning models may capture different parts of the forecasting problem. We study hourly day-ahead PV forecasting at a United Kingdom charging station using one year of inverter measurements, with 9.25% of hours missing. The pipeline checks timestamp-alignment, derives solar and clearness features, adds short-term weather context, and combines five complementary models using non-negative least squares stacking, with the combination fitted only on validation observations. We compare against smart persistence, a weather-scaled baseline that carries the previous day's PV behaviour forward using target-day irradiance. With retrospective weather, the combined model reduces
    
[^525]: Celty：面向高效双稀疏大语言模型推理的SpMSpV GPU内核与SIMT协同设计

    Celty: SpMSpV GPU Kernel and SIMT Co-Design for Efficient Dual-Sparse LLM Inference

    [https://arxiv.org/abs/2608.01536](https://arxiv.org/abs/2608.01536)

    提出Celty——一个协同设计的稀疏格式、GPU内核和SIMT微架构，通过游程压缩CSC格式与流水线RLC解码器高效处理双稀疏工作负载，实现单用户解码场景下高效的大语言模型SpMSpV推理。

    

    大语言模型日益依赖稀疏性来降低推理成本，但以往大多数工作仅利用单一稀疏来源，且面向批量多用户推理场景。双稀疏性将非结构化权重剪枝与运行时激活稀疏性相结合，为单用户解码提供了极具吸引力的模型尺寸-精度-延迟权衡，但其形成了稀疏矩阵-稀疏向量工作负载，而现有GPU内核对此处理不佳。我们提出了Celty，这是一个针对LLM推理中SpMSpV运算协同设计的稀疏格式、GPU内核和SIMT微架构。Celty的游程压缩CSC（RLC-CSC）格式支持对压缩权重列的向量化加载，并利用两种稀疏来源跳过内存访问，在共享内存中累积部分乘积。Celty稀疏SIMT核心随后增加了流水线式RLC解码器，消除了软件索引重建的开销，并将本地寄存器文件重新用于无冲突的累加计算。

    arXiv:2608.01536v2 Announce Type: replace-cross  Abstract: Large Language Models (LLMs) increasingly rely on sparsity to cut inference cost, but most prior work exploits a single sparsity source and targets batched multi-user inference. Dual-sparsity, which pairs unstructured weight pruning with runtime activation sparsity, offers a compelling size-accuracy-latency tradeoff for single-user decoding, but forms a Sparse Matrix-Sparse Vector (SpMSpV) workload that existing GPU kernels handle poorly. We propose Celty, a co-designed sparse format, GPU kernel, and SIMT microarchitecture for SpMSpV in LLM inference. Celty's Run-Length Compressed CSC (RLC-CSC) format enables vectorized loading of compressed weight columns and exploits both sparsity sources to skip memory accesses, accumulating partial products in shared memory. The Celty Sparse SIMT Core then adds a pipelined RLC decoder that removes software index reconstruction and repurposes local register files for conflict-free accumulati
    
[^526]: 关于掩码预测的可识别性：模式盲性与掩码调度

    On the Identifiability of Masked Prediction: Mode Blindness and Mask Schedules

    [https://arxiv.org/abs/2608.01383](https://arxiv.org/abs/2608.01383)

    本文发现掩码预测的可识别性完全由掩码调度决定，大上下文主导的调度对全局模式权重具有不可消除的盲性，并引入ε-可识别性模量来量化这一现象。

    

    掩码预测通过拟合一个由调度加权的条件分布族来学习表示，但尚不清楚何时接近最优的条件预测能够确定底层联合分布。我们针对具有两个良好分离的全局模式的数据研究这一问题，这些数据超出了快速混合恢复保证的适用范围，并表明答案仅由掩码调度决定。在大上下文模式固定下，对两个模式进行重新加权可以在总变差距离上使联合分布移动一个常数，而掩码目标在可见上下文大小上仅产生指数级小的扰动：由大上下文主导的掩码调度被证明对全局模式权重是盲的。为了量化这一点，我们引入了一个ε-可识别性模量，即与给定超额风险一致的最大分布误差，并证明它在超额风险指数级小时仍保持宏观尺度。一个精确的信息分解揭示了...

    arXiv:2608.01383v2 Announce Type: cross  Abstract: Masked prediction learns representations by fitting a schedule-weighted family of conditional laws, but it remains unclear when near-optimal conditional prediction pins down the underlying joint law. We study this question for data with two well-separated global modes, outside the reach of rapid-mixing recovery guarantees, and show that the answer is decided by the mask schedule alone. Under large-context mode pinning, reweighting the two modes can move the joint law by a constant in total variation while perturbing the masked objective exponentially little in the visible-context size: mask schedules dominated by large contexts are provably blind to the global mode weights. To quantify this, we introduce an $\varepsilon$-identifiability modulus, the largest distributional error consistent with a given excess risk, and prove that it remains macroscopic at an excess risk that is exponentially small. An exact information decomposition pin
    
[^527]: 剪枝BPE：字节对编码的训练后可见性剪枝与词元重新分配

    Pruned BPE: Post-training Visibility Pruning and Token Reallocation for Byte Pair Encoding

    [https://arxiv.org/abs/2608.00837](https://arxiv.org/abs/2608.00837)

    提出Pruned BPE方法，在BPE训练后将低暴露度的合并词元剪枝为仅内部使用的节点，并把释放的可见词表槽位重新分配给更高暴露度的候选词元，实现合并构建与模型可见词表选择的分离。

    

    字节对编码被广泛用于子词分词，但标准BPE会将每个学习到的合并词元都暴露给下游模型，其中包括那些主要作为中间构建单元、很少出现在最终编码语料库中的词元。本文提出了Pruned BPE，这是一种训练后可见性剪枝与词元重新分配方法，将合并构建过程与模型可见词表的选择分离开来。在标准BPE训练完成后，根据最终暴露程度对词元进行评估。低暴露度的词元被保留为仅内部使用的合并节点，而其占用的可见词表槽位则被重新分配给通过恢复训练学到的、暴露度更高的候选词元。在编码过程中，仅内部使用的词元会被递归展开为可见的后代词元，同时保留原始BPE的合并顺序。在两个互不重叠的分别以英语和汉语为主的语料库及其组合上的实验表明，Pruned BPE持续……

    arXiv:2608.00837v3 Announce Type: replace-cross  Abstract: Byte Pair Encoding (BPE) is widely used for subword tokenization, but standard BPE exposes every learned merge token to the downstream model, including tokens that mainly serve as intermediate construction units and rarely appear in the final encoded corpus. This paper proposes Pruned BPE, a post-training visibility-pruning and token-reallocation method that separates merge construction from model-visible vocabulary selection. After standard BPE training, tokens are evaluated by final exposure. Low-exposure tokens are retained as internal-only merge nodes, while their visible vocabulary slots are reassigned to better-exposed candidates learned through resumed training. During encoding, internal-only tokens are recursively expanded into visible descendants while the original BPE merge order is preserved. Experiments on two non-overlapping English- and Chinese-dominated corpora and their combination show that Pruned BPE consisten
    
[^528]: 一个虚假的平均值：思维链监控器在其作为唯一防线之处崩溃

    A False Average: Chain-of-Thought Monitors Collapse Where They Are the Only Defense

    [https://arxiv.org/abs/2608.00583](https://arxiv.org/abs/2608.00583)

    仅重写智能体的思维链推理（保持动作完全不变）即可在一次无梯度攻击中将CoT监控器的捕获率从约95%暴跌至11%以下，揭示监控器的总体准确率是掩盖其在唯一防线场景下近乎全面失效的虚假平均值。

    

    思维链监控旨在捕捉那些在动作层面看起来干净、只在推理中暴露自身的奖励作弊行为。我们证明，这恰恰是控制推理的攻击者能够击败它的地方。仅重写智能体的推理内容使其看起来像是善意的工程工作，同时逐字复制每个命令和输出以保持漏洞利用不变，在一次无需梯度的攻击中，就能将留出监控器在该子集上的捕获率从约95%降至11%以下。监控器的总体准确率是一个虚假的平均值：它由动作层面就会暴露的作弊行为主导，从而掩盖了这种重写在CoT监控是唯一信号的子集上造成的近乎全面的崩溃。该攻击可跨监控器系列和智能体模型迁移，并在真实智能体上复现，不过在面对经过校准的监控器时，规避行为主要集中在最强的智能体上。仅基于轨迹的防御只能部分恢复捕获率，即使是预先针对该攻击进行训练的防御也是如此。

    arXiv:2608.00583v2 Announce Type: replace-cross  Abstract: Chain-of-thought (CoT) monitoring is meant to catch the reward hacks that look clean in the actions and betray themselves only in the reasoning. We show that this is exactly where an adversary who controls the reasoning can defeat it. Rewriting only an agent's reasoning to read as good-faith engineering, while copying every command and output verbatim so the exploit is unchanged, drops a held-out monitor's catch rate on that subset from about 95% to under 11% in one gradient-free shot. A monitor's aggregate accuracy is a false average: dominated by hacks the actions give away, it hides the near-total collapse this rewrite produces on the subset where CoT monitoring is the only signal. The attack transfers across monitor families and agent models, reproduces with live agents, though against a calibrated monitor evasion concentrates in the strongest agent. Trace-only defenses recover it only partially, even one primed on the atta
    
[^529]: 面向临床脑电数据隐私保护联邦学习的安全聚合

    Secure Aggregation for Privacy-Preserving Federated Learning on Clinical EEG Data

    [https://arxiv.org/abs/2607.28191](https://arxiv.org/abs/2607.28191)

    提出了一个以基于掩码的安全聚合为核心的临床脑电数据隐私保护联邦学习框架，结合图通信、门限秘密共享、掉线恢复、更新裁剪与可验证性机制，以抵御半诚实和恶意聚合方的信息泄露。

    

    联邦学习使多个机构能够在不交换原始数据的情况下协同训练一个共享模型。然而，单个模型更新依赖于数据，可能会泄露客户端本地训练数据的信息。本文提出了一种面向临床脑电（EEG）数据的隐私保护联邦学习框架，该框架以基于掩码的安全聚合作为核心保护机制。该框架结合了基于图的通信、门限秘密共享、掉线恢复、本地更新裁剪、可选的基于布隆过滤器的隐私保护记录链接初始化模块，以及基于辅助公证人的可验证性。它支持半诚实和恶意聚合设置，并使用Flower联邦学习框架实现。在使用TUH脑电派生数据的模拟跨机构医疗环境中，对不同客户端配置下的安全聚合变体进行了评估。

    arXiv:2607.28191v2 Announce Type: replace-cross  Abstract: Federated learning enables multiple institutions to collaboratively train a shared model without exchanging their raw data. However, individual model updates are data-dependent and may reveal information about clients' local training data. This paper presents a privacy-preserving federated learning framework for clinical EEG data that uses masking-based secure aggregation as its core protection mechanism. The framework combines graph-based communication, threshold secret sharing, dropout recovery, local update clipping, an optional Bloom filter-based privacy-preserving record-linkage initialization module, and auxiliary-notary-based verifiability. It supports semi-honest and malicious aggregation settings and is implemented using the Flower federated learning framework. The secure aggregation variants are evaluated in a simulated cross-silo healthcare setting using TUH EEG-derived data under different client configurations. Und
    
[^530]: 减法、传输还是重放？语言模型记忆中的可审计删除

    Subtract, Transport, or Replay? Auditable Deletion from Language-Model Memory

    [https://arxiv.org/abs/2607.27539](https://arxiv.org/abs/2607.27539)

    本文提出并比较了三种从语言模型记忆中删除记录的方法，发现原生删除不可靠，而检查点重放能实现可审计的精确删除，并展示了在冻结模型上无需额外训练即可构建可删除记忆的可行性。

    

    从持久化语言模型记忆中精确删除取决于记录的效果在后续计算后是否仍可寻址。原生Kimi Delta Attention (KDA)在测试的收据接口上给出了负面结果：语料库汇总的原始循环贡献随后缀变化12-49%，且在衰减账本修正后仍保持8-49%的差异。原生省略也改变了后续的转换项、写入项和其他活动缓存。冻结输入传输在其固定输入控制上成功；更改的项将原生省略置于测试的收据类别之外。检查点重放提供了评估的重新计算路径；最终logits上的零残差和所有80个审计的KDA数组验证了在声明的检查点表面上的恢复。补充结果是建设性的。我们将支持向量记忆改造为冻结的Gemma 3，无需注意力转移、低秩恢复、蒸馏、适配器或语言模型修改。

    arXiv:2607.27539v2 Announce Type: replace-cross  Abstract: Exact deletion from persistent language-model memory depends on whether a record's effect remains addressable after later computation. Native Kimi Delta Attention (KDA) gives a negative result for the tested receipt interface: the corpus-pooled raw recurrent contribution changes by 12-49% with the suffix and remains 8-49% after a decay-ledger correction. Native omission also changes later transition and write terms and other active caches. Frozen-input transport succeeds on its fixed-input control; the changed terms place native omission outside the tested receipt classes. Checkpoint replay supplies the evaluated recomputation path; zero residual on final logits and all 80 audited KDA arrays verifies restoration across the declared checkpoint surface.   The complementary result is constructive. We retrofit support-vector memory into frozen Gemma 3 without attention transfer, low-rank recovery, distillation, adapters, or languag
    
[^531]: 基于并行轨迹回火的能量模型平衡训练

    Equilibrium Training of Energy-Based Models with Parallel Trajectory Tempering

    [https://arxiv.org/abs/2607.27077](https://arxiv.org/abs/2607.27077)

    本文提出基于并行轨迹回火（PTT）的能量模型训练算法，通过利用优化路径的连续性在学习全程保持平衡采样，实现了在高多峰、数据稀缺科学数据集上的稳定快速训练，且计算成本与持续对比散度相当，在受限玻尔兹曼机实验中始终优于现有EBM训练方法。

    

    能量模型（EBMs）为科学数据的生成建模提供了一个可解释的框架，但马尔可夫链蒙特卡洛（MCMC）混合能力差常常限制了其可靠性。我们提出了一种基于并行轨迹回火（Parallel Trajectory Tempering, PTT）的训练算法，该算法利用优化路径的连续性，在整个学习过程中保持平衡采样，从而能够在高度多峰且数据稀缺的科学数据集上实现稳定且快速的训练。结合水库采样和自适应优化，PTT的计算成本与持续对比散度相当，使其成为标准训练方法的实用替代方案。此外，它还能在几乎无需额外成本的情况下，直接估计热化时间、从训练后的模型获取平衡样本并计算精确的对数似然。在受限玻尔兹曼机上的实验表明，PTT始终优于现有的EBM训练方法。

    arXiv:2607.27077v2 Announce Type: replace  Abstract: Energy-Based Models (EBMs) provide an interpretable framework for generative modeling of scientific data, but poor Markov Chain Monte Carlo mixing often limits their reliability. We introduce a training algorithm based on Parallel Trajectory Tempering (PTT), which exploits the continuity of the optimization path to maintain equilibrium sampling throughout learning. This enables stable and fast training on highly multimodal and data-scarce scientific datasets. Combined with reservoir sampling and adaptive optimization, PTT has a computational cost comparable to Persistent Contrastive Divergence, making it a practical replacement for standard training methods. It also provides direct estimates of thermalization times, equilibrium samples from trained models, and accurate log-likelihoods at essentially no additional cost. Experiments on Restricted Boltzmann Machines show that PTT consistently outperforms existing EBM training approaches
    
[^532]: 重尾噪声下的无参数动态遗憾

    Parameter-Free Dynamic Regret under Heavy-Tailed Noise

    [https://arxiv.org/abs/2607.27073](https://arxiv.org/abs/2607.27073)

    该论文提出了一种无需预知噪声矩参数、Lipschitz 界、梯度尺度及比较序列路径长度的在线凸优化算法，通过重启 AdaGrad 专家与自适应熵正则化主算法的结合，在重尾噪声下实现了具有噪声路径指数 $(p-1)/p$ 的最优通用动态遗憾界。

    

    我们研究在线凸优化问题，其中随机梯度噪声的条件 $p$ 阶中心矩被 $\sigma^p$ 所界定，而 $p\in(1,2]$ 是未知的。对于在直径为 $D$ 的定义域上具有 Lipschitz 界 $G$ 的损失函数，我们获得了期望通用动态遗憾界 $\widetilde O(GD\sqrt{T\Lambda}+\sigma DT^{1/p}\Lambda^{(p-1)/p})$，其中 $\Lambda=1+P_T/D$，$P_T$ 是固定比较序列的路径长度。该算法将重启的 AdaGrad 专家与自适应熵正则化主算法相结合，每轮仅使用一次随机梯度，且不需要知道 $G$、$\sigma$、$p$ 或 $P_T$ 的任何信息。其迭代点在梯度的正数缩放变换下保持不变。该分析在取期望之前控制重启块内比较序列的移动，从而得到噪声路径指数 $(p-1)/p$，而非直接非重启扩展所给出的指数 $1/2$。同时给出了一个匹配的随机一阶预言机下界……

    arXiv:2607.27073v2 Announce Type: replace-cross  Abstract: We study online convex optimization with stochastic gradient noise whose conditional $p$-th central moment is bounded by $\sigma^p$, for an unknown $p\in(1,2]$. For losses with Lipschitz bound $G$ on a domain of diameter $D$, we obtain expected universal dynamic regret $\widetilde O(GD\sqrt{T\Lambda}+\sigma DT^{1/p}\Lambda^{(p-1)/p})$, where $\Lambda=1+P_T/D$ and $P_T$ is the path length of a fixed comparator sequence. The algorithm combines restarted AdaGrad experts with an adaptive entropy-regularized master, uses one stochastic gradient per round, and requires no knowledge of $G,\sigma,p$, or $P_T$. Its iterates are invariant under positive rescaling of the gradients. The analysis controls comparator movement within restart blocks before taking expectations, yielding the noise path exponent $(p-1)/p$ rather than the exponent $1/2$ of a direct non-restarted extension. A matching stochastic first-order oracle lower bound, comb
    
[^533]: 探测音频深度伪造检测器中的说话人身份敏感性

    Probing Speaker Identity Sensitivity in Audio Deepfake Detectors

    [https://arxiv.org/abs/2607.21820](https://arxiv.org/abs/2607.21820)

    论文提出身份敏感性分数（ISS）这一新型诊断指标，揭示音频深度伪造检测器的跨数据集性能下降部分源于检测器依赖说话人身份线索而非真正的合成痕迹。

    

    音频深度伪造检测器旨在区分真实语音与合成语音，并且在标准基准测试中通常表现良好。然而，同一个在某个数据集上错误率低于1%的检测器，在不同数据集上评估时，其错误率可能会增加二十倍。我们认为，造成这一现象的一个因素是说话人身份依赖性：标准训练语料库将说话人身份与真实/合成标签相关联，使检测器能够部分依赖与说话人相关的线索，而非仅仅依赖合成痕迹。我们提出了身份敏感性分数（Identity Sensitivity Score, ISS），这是一种逐条语音的诊断指标，用于量化检测器的输出在不同说话人身份情境下的变化程度。ISS在推理时无需真实标签，只需利用检测器的分数和一组参考说话人样本即可计算。在两个检测器和两个数据集上的实验表明，被错误分类的语音样本的ISS分数……

    arXiv:2607.21820v2 Announce Type: replace-cross  Abstract: Audio deepfake detectors are trained to distinguish genuine speech from synthetic speech and often perform well on standard benchmarks. Yet the same detector that achieves less than 1% error on one dataset can see its error rate increase twentyfold when evaluated on a different dataset. We argue that one contributing factor is speaker-identity reliance: standard training corpora correlate speaker identity with the genuine/synthetic label, allowing detectors to partially rely on speaker-related cues rather than synthesis artifacts alone. We propose the Identity Sensitivity Score (ISS), a per-utterance diagnostic that quantifies how much a detector's output changes across different speaker identity contexts. ISS requires no ground-truth labels at inference time and can be computed from the detector score and a pool of reference speaker examples. Across two detectors and two datasets, incorrectly classified utterances have ISS sco
    
[^534]: 面向轻量级联邦学习的信息论安全聚合：可抵御掉线与对手攻击

    Information-Theoretically Secure Aggregation for Lightweight Federated Learning: Resilient to Dropouts and Adversaries

    [https://arxiv.org/abs/2607.20890](https://arxiv.org/abs/2607.20890)

    提出了一种专为基于符号的轻量级联邦学习设计的信息论安全聚合框架，通过单轮安全乘法计算多数表决，在诚实多数假设下实现端到端的信息论安全，并能有效抵御掉线与恶意对手攻击。

    

    设备端联邦学习（FL）能够在智能手机和物联网节点等资源受限的设备上进行隐私保护和个性化的模型训练。为了降低通信成本，基于符号的方法（例如signSGD）仅传输一比特的梯度。然而，暴露梯度符号使其容易受到推理攻击，而现有的安全聚合方案通常与此类方法不兼容，或者会带来巨大的计算和通信开销。我们提出了一种专为基于符号的联邦学习量身定制的轻量级信息论安全聚合框架。该框架通过单轮安全乘法安全地计算多数表决（MV）多项式，在诚实多数假设下确保端到端的信息论安全，同时仅向服务器揭示最终聚合的符号。为了提升效率和可扩展性，我们引入了两项关键技术。首先，逆形式指数……

    arXiv:2607.20890v3 Announce Type: replace  Abstract: On-device federated learning (FL) enables privacy-preserving and personalized model training on resource-constrained devices such as smartphones and IoT nodes. To reduce communication cost, sign-based methods (e.g., signSGD) transmit one-bit gradients. However, exposing gradient signs makes them vulnerable to inference attacks, while existing secure aggregation schemes are often incompatible with such methods or incur significant computational and communication overhead. We propose a lightweight and information-theoretically secure aggregation framework tailored for sign-based FL. The framework securely computes the majority vote (MV) polynomial through single-round secure multiplication, ensuring end-to-end information-theoretic security under the honest-majority assumption while revealing only the final aggregated sign to the server. To enhance efficiency and scalability, we introduce two key techniques. First, inverse-form exponen
    
[^535]: 论图神经网络作为供应链优化元模型的潜力：数据集、架构与方向

    On the Potential of Graph Neural Networks as Metamodels for Supply Chain Optimization: Dataset, Architectures, and Directions

    [https://arxiv.org/abs/2607.16769](https://arxiv.org/abs/2607.16769)

    本文形式化了供应链优化问题，利用SupplyNetPy仿真库创建了大型公开的供应链图训练数据集，并探索了可作为仿真代理的图神经网络架构及其精度与计算成本的权衡，为GNN在供应链结构优化中的应用奠定基础。

    

    图神经网络（GNN）已成为一类针对图结构系统的强大可微分学习模型。其跨拓扑结构泛化的能力为结构优化与参数优化相结合的代理模型开辟了前景，而这是经典元模型无法实现的。供应链是一个天然的应用目标，然而将GNN代理模型应用于供应链问题的研究在很大程度上仍未被探索。本文为此奠定了基础，提出了初步步骤，并讨论了关键研究方向。作为基础工作，我们对问题进行了形式化表述，并创建了一个大型公开训练数据集，其中包含通过程序化方式生成的供应链图，以及使用我们的SupplyNetPy仿真库获得的输入参数和稳态性能指标。作为初步探索，我们研究了适合作为节点级和网络级预测代理模型的GNN架构，并分析了其相对于仿真方法在精度与计算成本之间的权衡。最重要的是……

    arXiv:2607.16769v2 Announce Type: replace  Abstract: Graph Neural Networks (GNNs) have emerged as a powerful, differentiable class of learning models for graph-structured systems. Their ability to generalize across topologies opens the prospect of a surrogate for combined structural and parametric optimization, which classical metamodels cannot offer. Supply chains are a natural target, yet the use of GNN surrogates for supply chain problems is largely unexplored. This paper lays the foundation, presents initial steps, and discusses key research directions. As a foundation, we formulate the problem and create a large public training dataset of programmatically generated supply chain graphs with input parameters and steady-state performance metrics obtained using our SupplyNetPy simulation library. As initial steps, we explore GNN architectures that work well as surrogates for node- and network-level predictions, and analyze their accuracy-compute trade-off against simulation. Most impo
    
[^536]: 决策制定需要不确定性量化【讲义】

    Decision Making Needs Uncertainty Quantification [Lecture Notes]

    [https://arxiv.org/abs/2607.14407](https://arxiv.org/abs/2607.14407)

    本讲义从决策理论基本原理出发证明，最优决策所需的不确定性表示取决于智能体的风险偏好——风险中性智能体需要后验分布，风险厌恶智能体可依赖预测集与最坏情况决策规则，并进一步提出应对环境未知时认知不确定性的互补方法。

    

    许多信号处理系统最终都是为了执行行动而存在的。每当决定决策者（即智能体）所采取行动的状态变量存在不确定性时，这种不确定性的表示方式就决定了智能体的表现优劣以及其表现的可信程度。本讲义从第一性原理出发，在统一的决策理论框架内，建立了智能体的目标与其知识之间的联系，以及足以实现最优行动的不确定性表示形式。首先，在假设环境分布已知的条件下，我们证明风险中性的智能体需要状态的后验分布，而风险厌恶的智能体可以依靠预测集和最坏情况决策规则而不损失最优性。随后，我们转向环境分布未知的情况，并识别出三种互补的方法来应对由此产生的认知不确定性：校准（原文在此处截断）……

    arXiv:2607.14407v4 Announce Type: replace-cross  Abstract: Many signal processing systems ultimately exist to act. Whenever the state variable that determines the action to be taken by a decision maker, or agent, is uncertain, the way that uncertainty is represented decides how well the agent performs and how much its performance can be trusted. This lecture note develops, from first principles and within a single decision-theoretic setting, the link between the {objective} and the knowledge of an agent and the form of uncertainty representation that is sufficient to act optimally. To start, assuming a known environment distribution, we show that a risk-neutral agent needs the posterior distribution over the state, whereas a risk-averse agent can rely without loss of optimality on a {prediction set} and a worst-case decision rule. We then turn to the case in which the environment is unknown, and identify three complementary approaches to address the resulting epistemic uncertainty: cal
    
[^537]: 面向脑肿瘤演化预测与治疗调度的AI增强自适应数字孪生建模

    AI-Augmented Adaptive Digital Twin Modeling for Brain Tumor Evolution Prediction and Treatment Scheduling

    [https://arxiv.org/abs/2607.13877](https://arxiv.org/abs/2607.13877)

    提出了一种AI增强的自适应数字孪生框架，融合可解释反应-扩散模型、3D残差学习、患者特异性更新与模型预测控制，实现了脑肿瘤演化的高精度长期预测以及约束性化疗放疗调度优化。

    

    脑肿瘤的进展表现出空间异质性生长、患者特异性的治疗反应以及与周围解剖结构的复杂相互作用，这使得准确的长期预测极具挑战性。我们提出了一种用于脑肿瘤演化预测和治疗调度的AI增强自适应数字孪生（DT）框架。该框架集成了可解释的反应-扩散（RD）模型、用于模型形式校正的3D残差学习模块、递归展开过程中的患者特异性数字孪生更新，以及用于约束性化疗和放疗调度的模型预测控制（MPC）。在387条具有120步演化的合成肿瘤轨迹上进行的实验表明，基线RD模型能够捕捉肿瘤位置和整体时间行为，但在长期预测中会低估异质性肿瘤负荷。混合RD-残差建模将掩码体素均方误差降低了84.3%，并提高了Dice系数。

    arXiv:2607.13877v2 Announce Type: replace  Abstract: Brain tumor progression exhibits spatially heterogeneous growth, patient-specific treatment response, and complex interactions with surrounding anatomy, making accurate long-term prediction challenging. We propose an AI-augmented adaptive digital twin (DT) framework for brain tumor evolution prediction and treatment scheduling. The framework integrates an interpretable reaction--diffusion (RD) model, a 3D residual learning module for model-form correction, patient-specific DT updating during recursive rollout, and model predictive control (MPC) for constrained chemotherapy and radiotherapy scheduling. Experiments on 387 synthetic tumor trajectories with 120-step evolution show that the baseline RD model captures tumor location and overall temporal behavior but underestimates heterogeneous tumor burden during long-horizon prediction. Hybrid RD--residual modeling reduces masked voxel-wise mean squared error by 84.3% and increases Dice 
    
[^538]: 基于学习的场景式模型预测控制加速方法

    Learning-enabled Acceleration of Scenario-based Model Predictive Control

    [https://arxiv.org/abs/2607.12775](https://arxiv.org/abs/2607.12775)

    本文提出一种学习加速的ADMM算法，将SBMPC问题重构为可分解的一致性形式，结合并行计算与Moreau包络学习实现跨场景和跨时间步的并行更新，在保持高求解精度的同时大幅提升场景式模型预测控制的求解效率。

    

    基于场景的模型预测控制（SBMPC）是模型预测控制（MPC）的一种变体，它通过在多个预测场景上优化控制动作来显式地考虑不确定性。然而，其计算复杂度会随着场景数量和预测时域的增加而迅速增长，限制了其在实时规划与控制中的适用性。本文提出了一种学习加速的交替方向乘子法（ADMM）算法，通过利用并行计算和Moreau包络学习来高效求解SBMPC问题，同时保持较高的求解精度。我们将SBMPC问题重新表述为可通过ADMM分解的一致性形式，将依赖场景的动力学与非预期性约束分离，从而实现跨场景和跨时间步的并行更新。基于这一分解，我们采用一种学习优化（learning-to-optimize）方案，利用Moreau

    arXiv:2607.12775v2 Announce Type: replace-cross  Abstract: Scenario-based model predictive control (SBMPC) is a variant of model predictive control (MPC) that explicitly accounts for uncertainty by optimizing control actions over multiple predicted scenarios. However, its computational complexity increases rapidly with the number of scenarios and prediction horizon, limiting its applicability to real-time planning and control. This paper presents a learning-accelerated Alternating Direction Method of Multipliers (ADMM) algorithm for efficiently solving SBMPC problems by leveraging parallel computing and Moreau envelope learning, while maintaining high solution accuracy. We reformulate the SBMPC problems into consensus forms that can be decomposed via ADMM, separating the scenario-dependent dynamics from non-anticipativity constraints and enabling parallel updates across scenarios and time steps. Building on this decomposition, we utilize a learning-to-optimize scheme that leverages Mor
    
[^539]: 物理一致的参数推断：高能物理与宇宙学中的透明机器学习仿真

    Physically Consistent Parameter Inference: Transparent Machine Learning Emulation in High Energy Physics and Cosmology

    [https://arxiv.org/abs/2607.12726](https://arxiv.org/abs/2607.12726)

    该论文提出了一种基于XGBoost梯度提升回归树的机器学习框架，可高效仿真高能物理和宇宙学全局拟合中复杂的非高斯似然函数，并借助SHAP值实现透明可解释的参数推断。

    

    高能物理和宇宙学中的全局拟合常常面临探索高维参数空间的挑战，其似然函数往往计算代价高昂或拓扑结构复杂。在本工作中，我们提出了一个机器学习框架，旨在利用梯度提升回归树（XGBoost）来仿真复杂的、通常呈非高斯分布的似然地形。我们讨论了机器学习方法在计算效率和置信区域分辨率方面的优势，特别是在存在复杂相关性或“弯曲”简并的场景中。我们通过将该框架应用于最近一项关于半轻子B介子衰变中味反常的分析来验证该方法，并讨论了该框架对其他唯象系统（如类轴子粒子或宇宙学全局拟合）的适应性。最后，我们利用SHAP（Shapley Additive exPlanations）值提供了透明的分析。

    arXiv:2607.12726v2 Announce Type: replace-cross  Abstract: Global fits in high energy physics and cosmology often face the challenge of exploring high-dimensional parameter spaces with computationally expensive or topologically complex likelihood functions. In this work, we present a Machine Learning framework designed to emulate complex, often non-Gaussian likelihood landscapes using gradient-boosted regression trees (XGBoost). We discuss the advantages of the Machine Learning approach in terms of computational efficiency and the resolution of confidence regions, particularly in scenarios with complex correlations or "curved" degeneracies. We validate this methodology by applying it to a recent analysis on flavour anomalies in semileptonic $B$ meson decays and discussing the adaptability of this framework to other phenomenological systems, such as axion-like particles or cosmology global fits. Finally, we utilise SHAP (Shapley Additive exPlanations) values to provide a transparent ana
    
[^540]: 强化学习中协同设计的环境参数梯度定理

    Environment Parameter Gradient Theorem for Co-Design in Reinforcement Learning

    [https://arxiv.org/abs/2607.12590](https://arxiv.org/abs/2607.12590)

    本文提出环境参数梯度定理，通过引入包含两份环境参数副本的广义动作值函数实现解耦，为强化学习中策略与环境的协同设计（联合优化）提供了可处理的闭式梯度表达式。

    

    强化学习（RL）传统上关注为固定环境学习控制策略。然而在许多工程系统中，环境本身是可变的，即可以通过调节物理或运行参数来塑造RL智能体所经历的系统转移动力学和代价。这促使我们对策略和环境设计参数进行联合优化。为此，我们建立了环境参数梯度定理——这是RL目标函数关于环境参数的梯度的形式化表达式。关键的理论工具是一个广义动作值函数 $Q_{\pi,\xi}(s,a,\zeta)$，它包含环境参数的两个副本：ζ 管控当前状态-动作对处的代价和转移动力学，而 ξ 管控未来的展开过程。这种解耦产生了可处理的闭式梯度表达式。

    arXiv:2607.12590v2 Announce Type: replace-cross  Abstract: Reinforcement learning (RL) is traditionally concerned with learning a control policy for a fixed environment. In many engineering systems, however, the environment itself is alterable, i.e., physical or operational parameters can be tuned to shape the system's transition dynamics and costs experienced by the RL agent. This motivates jointly optimizing both the policy and the environment design parameters. To this end, we establish an Environment Parameter Gradient Theorem --- a formal expression for the gradient of the RL's objective function with respect to environment parameters. The key theoretical device is a generalized action-value function $Q_{\pi,\xi}(s,a,\zeta)$, which comprises two copies of the environment parameters: $\zeta$ governs the cost and transition dynamics at the current state--action pair, while $\xi$ governs the future rollouts. This decoupling yields a tractable closed-form gradient expression and is es
    
[^541]: 删除证书覆盖什么，又在哪里失效：支持向量记忆中的可审计移除

    What a Deletion Certificate Covers, and Where It Expires: Auditable Removal from a Support-Vector Memory

    [https://arxiv.org/abs/2607.12204](https://arxiv.org/abs/2607.12204)

    该论文提出一种带显式权重的支持向量上下文记忆，利用测试时的单类支持向量边界将键划分为激活或保留状态，从而提供可验证的删除证书——保留键移除无需重新求解、激活键删除可通过减量求解达到与重新求解一致的状态——并揭示了这些证书在新词元加入后可能失效的边界。

    

    密集键值缓存使操作者无法验证一次删除操作：它既不能说明哪些已存储条目目前对输出没有贡献，也不提供编辑后的记忆应当匹配的参考状态。我们构建了一种条目携带显式权重的上下文记忆，并探讨其上的删除证书能覆盖什么范围、又在何处失效。在测试时对上下文的键拟合一个单类支持向量边界，由其给出读取时使用的系数，使每个键处于激活状态（正系数）或保留状态（零系数）。移除一个保留键无需重新求解即可保持读取结果不变；而通过减量求解器删除一个激活键，可达到与在相同系数上限下对剩余键重新求解完全一致的状态。一个三键构造展示了这两种性质的极限：一个惰性键在再多接纳一个词元之后可能获得正权重，因此保留状态证书……（原文在此处截断）

    arXiv:2607.12204v3 Announce Type: replace  Abstract: A dense key--value cache gives an operator no way to verify a deletion: it does not say which stored entries currently contribute nothing to the output, and it offers no reference state that the edited memory should match. We build a context memory whose entries carry explicit weights and ask what a deletion certificate over it can cover and where it expires. A one-class support-vector boundary fit around the keys of a context at test time supplies the coefficients used in the readout, so that each key is active (positive coefficient) or reserve (zero coefficient). Removing a reserve key leaves the readout unchanged without a re-solve, and deleting an active key with a decremental solver reaches the same state as re-solving on the remaining keys at the same coefficient cap. A three-key construction shows the limit of both properties: an inert key can acquire positive weight after one more token is admitted, so reserve status certifie
    
[^542]: 奖励何时教会状态？一个隐藏自动机测量工具与一个群语言警告信号

    When Does Reward Teach State? A Hidden-Automaton Instrument and a Group-Language Warning Signal

    [https://arxiv.org/abs/2607.11953](https://arxiv.org/abs/2607.11953)

    该论文提出一个基于已知隐藏有限自动机的诊断框架，通过奖励归一化和状态探测两种测量手段分离强化学习智能体中奖励与状态学习的失败模式，并发现置换自动机的群结构特征可作为训练前预测状态学习失败的警告信号。

    

    获得奖励的强化学习智能体是否学会了其任务的隐藏状态？我们通过智能体部分控制的隐藏有限自动机来研究这一问题。由于每个自动机都是已知的，我们可以用最佳可实现回报对奖励进行归一化，并在每一步对网络进行探测以获取真实状态。这两种测量方法共同区分了仅凭奖励无法区分的失败模式：智能体可能对其网络本可容纳的状态编码过少，或者虽然编码了状态但控制仍然很差。弱在线策略强化学习的表现与随机行为相当，而此时状态探测的准确率仍停留在随机水平。状态学习取决于优化器、训练预算以及任务的结构。置换自动机可以在训练之前提供警告信号：没有任何输入符号会将两个不同的状态映射到相同的后继状态。在一个分层划分的保留测试集上，103个置换自动机中有86个未能通过状态探测，并且该分类结果在不同的探测读出方式和恢复阈值下保持稳定。

    arXiv:2607.11953v4 Announce Type: replace  Abstract: Does a reinforcement-learning agent that earns reward learn its task's hidden state? We study this question with hidden finite automata that the agent partially controls. Because each automaton is known, we can normalize reward by the best achievable return and probe the network for the true state at every step. Together the two measurements separate failures that reward alone conflates. An agent can encode too little of a state its network could hold, or encode the state and still control poorly. Weak on-policy RL matches random play while the state probe stays at chance. State learning depends on the optimizer, the training budget, and the task's structure. Permutation automata provide a warning before training: no input symbol maps two distinct states to the same successor. On a stratified held-out set, 86 of 103 permutation automata fail the state probe, and the classification is stable across probe read-outs and recovery thresho
    
[^543]: 使用孪生图神经网络学习子群关系

    Learning Subgroup Relations Using Siamese Graph Neural Networks

    [https://arxiv.org/abs/2607.11140](https://arxiv.org/abs/2607.11140)

    本文提出一种将孪生图神经网络编码的凯莱图结构表示与代数特征相结合的统一框架，用于预测有限群之间的子群关系。

    

    判断一个有限群是否同构于另一个有限群的子群是计算群论中的一个基本问题。在本工作中，我们提出了一种孪生图神经网络（Siamese GNN），利用有限群的凯莱图表示进行子群关系预测。每个输入群由其无向凯莱图表示，并由孪生图神经网络的一个分支进行编码以生成图嵌入。所得的图嵌入与直接从输入群导出的代数特征相结合，构建出联合特征向量，再由全连接分类器处理以预测有限群之间的子群关系。通过将基于图的结构表示与代数特征相融合，所提出的框架为从有限群中学习子群关系提供了一种统一的方法。在从11个群族中抽取的308个有限群对所构成的扩展且更加多样化的数据集上的实验结果……

    arXiv:2607.11140v2 Announce Type: replace  Abstract: Determining whether one finite group is isomorphic to a subgroup of another is a fundamental problem in computational group theory. In this work, we propose a Siamese Graph Neural Network (Siamese GNN) for subgroup prediction using Cayley graph representations of finite groups. Each input group is represented by its undirected Cayley graph and encoded by one branch of a Siamese GNN to produce a graph embedding. The resulting graph embeddings are combined with algebraic features derived directly from the input groups to construct a joint feature vector, which is processed by a fully connected classifier to predict subgroup relations between finite groups. By integrating graph-based structural representations with algebraic features, the proposed framework provides a unified approach for learning subgroup relations from finite groups. Experimental results on an expanded and more diverse dataset of 308 finite-group pairs drawn from 11 g
    
[^544]: 大步长梯度下降在平坦极小值流形附近的动力学

    Dynamics of Gradient Descent with Large Step Size Near a Manifold of Flat Minima

    [https://arxiv.org/abs/2607.08380](https://arxiv.org/abs/2607.08380)

    本文将大步长梯度下降在平坦极小值附近的收敛理论从单标量输出的孤立平坦极小值情形，扩展到向量值输出情形以及平坦极小值流形邻域情形，其中后者对矩阵分解等应用至关重要。

    

    梯度下降（GD）理论中的一个重要量是“锐度”（sharpness），其定义为目标函数Hessian矩阵的最大特征值。经典分析通常要求步长一致地小于锐度倒数的两倍，但这一条件在深度神经网络的训练中经常被违反。最近的工作在具有“单标量输出”的过参数化最小二乘设置中弥合了这一差距，为“孤立”平坦极小值邻域内的大步长梯度下降提供了规范形式（normal form），并建立了三个相应的收敛结果。在本文中，我们将这一理论向两个方向进行扩展：（1）扩展到具有“向量值输出”的过参数化最小二乘问题（包括具有任意多观测值的回归）；（2）扩展到“平坦极小值流形”的邻域（我们证明了这一扩展对于矩阵分解等应用是必不可少的）。

    arXiv:2607.08380v2 Announce Type: replace  Abstract: An important quantity in the theory of gradient descent (GD) is the \emph{sharpness}, defined as the largest eigenvalue of the objective Hessian. Classical analyses typically require the step size to be uniformly smaller than twice the reciprocal of the sharpness, but this condition is frequently violated in the training of deep neural networks. Recent work bridges this gap in the setting of overparametrised least-squares with a \emph{single scalar output}, providing a normal form for large-step GD in a neighbourhood of an \emph{isolated} flat minimum and establishing three corresponding convergence results. In this paper, we extend this theory in two directions: (1) to overparametrised least-squares with \emph{vector-valued outputs} (including regression with arbitrarily many observations), and (2) to a neighbourhood of a \emph{manifold} of flat minima (which we show is essential for applications such as matrix factorisation). We ge
    
[^545]: 基于采样的任意维度学习

    Any-Dimensional Learning by Sampling

    [https://arxiv.org/abs/2607.07680](https://arxiv.org/abs/2607.07680)

    本文提出一种统一框架，利用随机采样映射来比较不同大小的输入，从而解决变尺寸机器学习模型从训练时的小尺寸输入向更大尺寸输入的泛化问题，以及通过草图化大输入来高效获得相似模型输出的问题。

    

    许多机器学习模型是为不同大小的输入定义的，例如包含不同数量点的点云、不同长度的标记序列以及具有不同数量节点的图。这些模型是在有限个、必然尺寸受限的样本上进行训练的。那么，这些模型从小尺寸输入泛化到训练期间未见过的更大尺寸输入的效果如何？此外，在大输入上评估这类模型通常成本高昂。我们如何对大输入进行草图化（sketch）以获得更小的输入，使模型在其上取到相近的值？这两个问题的核心都在于比较不同大小的输入，以及用小输入来近似大输入。我们提出了一种统一的方法，通过使用随机采样映射来比较不同大小的输入，从而解决这些问题。我们所考虑的采样映射是带放回采样、随机分箱以及物种采样（species sampling）的推广。

    arXiv:2607.07680v2 Announce Type: replace-cross  Abstract: Many machine learning models are defined for inputs of different sizes, such as point clouds containing different numbers of points, sequences of tokens of different lengths, and graphs on different numbers of nodes. Such models are trained on finitely many examples of necessarily limited sizes. How well do these models generalize from inputs of small size to larger inputs of size not seen during training? Furthermore, evaluating such models on large inputs is often expensive. How can we sketch large inputs to obtain smaller ones on which the model takes similar values? At the heart of both questions is the need to compare inputs of different sizes and to approximate large inputs by small ones. We present a unified approach to address these questions by using random sampling maps to compare inputs of different sizes. The sampling maps we consider are generalizations of sampling with replacement, random binning, and species samp
    
[^546]: 面向LLM导师答案密钥依赖性的上下文掩码截断推理审计

    Context-Masked Truncated Reasoning Audits for Answer-Key Dependence in LLM Tutors

    [https://arxiv.org/abs/2607.04572](https://arxiv.org/abs/2607.04572)

    本文提出上下文掩码截断推理审计方法，用于检测LLM导师是否直接依赖私有答案密钥生成讲解，实验表明掩码后各项指标几乎降至仅问题基线水平，证明该方法能有效区分私有上下文直接访问与讲解中携带的答案信息。

    

    大语言模型（LLM）导师在生成面向学生的讲解时，可能可以访问教师笔记、答案密钥、评分标准或检索到的解答。我们研究截断推理探针能否区分对这类私有上下文的直接访问与书面讲解中携带的答案信息。使用截断推理AUC评估（TRACE），我们在仅问题、正确答案密钥和错误答案密钥三种上下文下评估了1000道GSM8K问题。当强制回答探针保留私有密钥时，答案密钥TRACE AUC从0.375上升至0.900，且在1000个案例中有998个案例即使完全没有讲解也能恢复出正确答案。随后我们引入了上下文掩码重放方法：在仅问题的提示下对答案密钥生成的前缀进行探测。掩码将10%前缀准确率从0.997降至0.126，中位数AUC从0.900降至0.375，几乎与仅问题条件下的0.113和0.375相匹配。

    arXiv:2607.04572v3 Announce Type: replace  Abstract: Large language model (LLM) tutors may have access to teacher notes, answer keys, rubrics, or retrieved solutions while producing student-facing explanations. We study whether truncated reasoning probes can distinguish direct access to such private context from answer information carried by the written explanation. Using Truncated Reasoning AUC Evaluation (TRACE), we evaluate 1000 GSM8K problems under question-only, correct answer-key, and wrong answer-key contexts. When forced-answer probes retain the private key, answer-key TRACE AUC rises from 0.375 to 0.900, and the gold answer is recoverable with no explanation at all in 998 of 1000 cases. We then introduce a context-masked replay: answer-key-generated prefixes are probed under the corresponding question-only prompt. Masking reduces 10\% prefix accuracy from 0.997 to 0.126 and median AUC from 0.900 to 0.375, nearly matching question-only values of 0.113 and 0.375. On 746 pairs wh
    
[^547]: 用于跨数据集脑电情绪识别的掩码生成-对比表征学习

    Masked Generative-Contrastive Representation Learning for Cross-Dataset EEG-Based Emotion Recognition

    [https://arxiv.org/abs/2607.04139](https://arxiv.org/abs/2607.04139)

    提出了MGCRL——一种专为脑电情绪识别设计的新型自监督学习框架，通过融合生成式与对比式学习以及区域感知的时空编码器，实现了面向跨数据集泛化的细粒度与全局判别性表征。

    

    自监督学习（SSL）通过改善特征表示和泛化能力，在跨数据集迁移方面展现出巨大潜力。然而，其在基于脑电图（EEG）的情绪识别中的应用在很大程度上仍未被探索。现有的SSL方法难以在多变的通道配置下捕获脑电信号复杂的时空依赖关系，难以提取对噪声具有鲁棒性的细粒度表示，也难以获得能够跨被试良好泛化的全局特征。为了解决这些挑战，我们提出了掩码生成-对比表征学习（MGCRL），这是一种专为基于脑电的情绪识别设计的新型自监督学习框架。MGCRL建立在区域感知的时空编码器之上，将生成式学习与对比式学习相结合，以同时实现细粒度和全局判别性表征，从而实现跨数据集泛化。MGCRL引入了三个关键设计：1）一个融合了……的时空编码器

    arXiv:2607.04139v2 Announce Type: replace  Abstract: Self-supervised learning (SSL) shows strong potential for cross-dataset transfer by improving feature representation and generalization. However, its application to EEG-based emotion recognition remains largely unexplored. Existing SSL methods struggle to capture the intricate spatiotemporal dependencies of EEG signals under varying channel configurations, extract fine-grained representations resilient to noise, and derive global features that generalize well across subjects. To address these challenges, we propose Masked Generative-Contrastive Representation Learning (MGCRL), a novel SSL framework specifically designed for EEG-based emotion recognition. Built upon a region-aware spatiotemporal encoder, MGCRL integrates generative and contrastive learning to achieve both fine-grained and global discriminative representations for cross-dataset generalization. MGCRL introduces three key designs: 1) a spatiotemporal encoder that incorpo
    
[^548]: 一种用于表征化学与结构因素对水溶性贡献的加性MLP-GNN框架

    An Additive MLP-GNN Framework for Characterizing Chemical and Structural Contributions to Aqueous Solubility

    [https://arxiv.org/abs/2607.02212](https://arxiv.org/abs/2607.02212)

    提出一种加性深度学习框架，将理化描述符（MLP化学分支）与分子图拓扑（GNN结构分支）分离建模并在预测阶段加性融合，实现了化学性质与分子结构对水溶性贡献的直接分解与可解释分析，并借助大库预训练加小库微调提升预测性能。

    

    水溶性是药物早期发现中的一项关键性质，但大多数预测模型将理化描述符与分子图信息合并为单一表示，从而掩盖了预测究竟是由全局化学性质、分子结构还是二者共同驱动的。我们提出了一种加性深度学习框架，在训练全过程中保持这两类信息源相互分离：理化描述符由多层感知机（化学分支）编码，分子图拓扑结构由图神经网络（结构分支）编码，两个输出仅在预测阶段通过带可选乘性交互项的加性模型进行组合。这种设计提供了化学成分与结构成分的直接分解，可在训练后分别加以考察。此外，通过在规模较大的AqSolDB数据集上进行预训练，并在规模较小的BigSolDB2数据集上进行微调，可进一步提升模型的预测性能。

    arXiv:2607.02212v3 Announce Type: replace-cross  Abstract: Aqueous solubility is a key property in early-stage drug discovery, but most predictive models merge physicochemical descriptors and molecular graph information into a single representation, obscuring whether a prediction is driven by global chemistry, molecular structure, or both. We present an additive deep-learning framework that keeps these two sources of information separate throughout training: physicochemical descriptors are encoded by a multilayer perceptron (the chemical branch) and molecular graph topology by a graph neural network (the structural branch), with the two outputs combined only at the prediction stage through an additive model with an optional multiplicative interaction. This design provides a direct decomposition of chemical and structural components that can be examined separately after training. Furthermore, pretraining on the larger AqSolDB dataset and fine-tuning on the smaller BigSolDB2 dataset subs
    
[^549]: 探测化学语言模型：预训练与微调的影响

    Probing Chemical Language Models: Effects of Pre-training and Fine-tuning

    [https://arxiv.org/abs/2607.02140](https://arxiv.org/abs/2607.02140)

    该研究通过系统探测78种分子子结构，揭示了预训练能提升化学语言模型的分子结构感知能力（尤以上层显著），且微调主要改变与任务相关的子结构表示，表明模型的表示变化遵循化学理论。

    

    化学语言模型（CLMs）使用诸如SMILES之类的线性化表示进行训练，然而它们编码了哪些具有化学意义的子结构仍不清楚。为了促进对CLMs的更好理解，我们进行了一项系统性研究，在八个预训练模型和六个随机初始化模型中探测了78种分子子结构。我们进一步研究了在化学下游任务上进行微调如何影响所学习到的分子子结构表示。我们的结果表明，预训练普遍提升了CLMs对分子结构的感知能力，尤其是在模型的上层。此外，随机初始化的模型在第一层就已经能很好地编码环状结构。我们对两个化学下游任务的分析进一步揭示了一个有趣的现象：微调对与任务相关的分子子结构的影响大于其他子结构，这表明表示的变化遵循化学理论。

    arXiv:2607.02140v2 Announce Type: replace  Abstract: Chemical language models (CLMs) are trained with linearized representations such as SMILES, yet it remains unclear which chemically meaningful substructures they encode. To foster a better understanding of CLMs, we conduct a systematic study and probe for 78 molecular substructures across eight pre-trained and six randomly initialized models. We furthermore study how fine-tuning on chemical downstream tasks affects the learned representations of molecular substructures. Our results show that pre-training generally improves molecular structure awareness of CLMs, particularly in the upper layers. Moreover, randomly initialized models already encode ring structures well in the first layer. Our analysis on two chemical downstream tasks further reveals that, interestingly, fine-tuning affects task-relevant molecular substructures more than others, indicating that the changes in the representations follow chemical theory.
    
[^550]: 用于混合可解释性的多阶段延迟树：若首次未能成功，那就再“树”一次

    Multistage Defer Trees for Hybrid Interpretability: If at First You Can't Succeed, Tree Again

    [https://arxiv.org/abs/2606.30995](https://arxiv.org/abs/2606.30995)

    提出多阶段延迟树，通过一列稀疏决策树对大多数样本进行可解释预测，仅将少数困难样本延迟给后续树或黑盒模型，从而在匹配复杂树集成准确率的同时实现混合可解释性。

    

    近期研究表明，经过良好优化的单个决策树在某些场景下（主要是在噪声较大的领域）可以匹敌复杂的黑盒模型。然而，对于其余场景，复杂的树集成组合通常以牺牲可解释性为代价来获得更高的准确率，使从业者在准确率与可解释性之间的权衡上面临艰难的建模决策。理想情况下，我们希望用一棵或少量树尽可能多地对数据进行分类，在保持最先进准确率的同时，为大多数样本实现可解释性。我们提出了多阶段延迟树：由稀疏决策树组成的序列，其中每棵树对大多数样本进行预测，同时将一小部分样本延迟交给序列中的下一棵树，或最终交给黑盒模型。我们证明可以训练这一模型类别，使其匹配复杂树集成的性能，同时将大多数样本仅通过……（摘要原文不完整）

    arXiv:2606.30995v2 Announce Type: replace  Abstract: Recent work has shown that well-optimized individual decision trees can match complex black box models in some settings, primarily in noisy domains. For the remaining settings, however, complex ensembled compositions of trees often achieve higher accuracy at the cost of interpretability, leaving practitioners with difficult modeling decisions along an accuracy-interpretability tradeoff. Ideally, we would like to classify as much of the data as possible with one or a small number of trees, achieving interpretability for most samples while maintaining state-of-the-art accuracy. We introduce Multistage Defer Trees: a sequence of sparse decision trees that each make predictions for most samples, while deferring a small proportion to the next tree in the sequence or, ultimately, to a black box. We demonstrate that we can train this model class to match the performance of complex tree-based ensembles while routing most samples through only
    
[^551]: 激活导向何时会改变模型计算所依据的内容？

    When Does Activation Steering Change What a Model Computes From?

    [https://arxiv.org/abs/2606.29522](https://arxiv.org/abs/2606.29522)

    该论文的核心贡献在于通过受控状态追踪任务区分激活导向的两种机制——真正编辑模型后续计算所依赖的内部状态还是仅使输出偏向期望结果——并发现行为等价不等于机制等价，均值激活导向并不能重现模型自然使用的内部配置。

    

    激活导向可以通过修改智能体的内部激活来可靠地改变其输出。然而，得到相同的答案未必涉及相同的计算：行为等价并不意味着机制等价。我们检验激活编辑究竟是改变了后续计算所使用的状态，还是仅仅使计算偏向期望的输出。在一个受控的状态追踪任务中，轨迹监督创造了一个可编辑的寄存器：改变其内部值会使模型对编辑后的状态应用下一个操作。接下来我们探究广泛使用的均值激活导向是否可以作类似解读：导向是否重现了模型在执行任务时自然使用的内部配置？如果是这样，移植该激活应当是有效的，且导向应当在自然源到目标激活变化的尺度上保持有效。然而，在这两个选定的模型-任务组合中，这两个预测均不成立。

    arXiv:2606.29522v2 Announce Type: replace  Abstract: Activation steering can reliably change an agent's output by modifying its internal activations. Yet arriving at the same answer need not involve the same computation: behavioral equivalence does not imply mechanistic equivalence. We test whether an activation edit changes the state used in subsequent computation or biases that computation toward the desired output. In a controlled state-tracking task, trace supervision creates an editable register: changing its internal value makes the model apply the next operation to the edited state. We next ask whether widely used mean activation steering admits a similar interpretation: does steering recreate the internal configuration the model naturally uses when performing a task? If so, transplanting that activation should be effective, and steering should remain effective at the scale of the natural source-to-target activation change. Neither prediction holds in the two selected model-task
    
[^552]: 保形风险控制何时能够认证大语言模型的输出？结构化生成的边界、不可能性与自适应方法

    When Can Conformal Risk Control Certify LLM Outputs? Bounds, Impossibility, and Adaptation for Structured Generation

    [https://arxiv.org/abs/2606.29054](https://arxiv.org/abs/2606.29054)

    该论文刻画了保形风险控制认证LLM结构化输出的可行边界，证明了当基础风险超过目标时任何无分布方法的最小弃权率下界，给出了闭式可行性测试，并通过716种配置验证了认证相图与自适应方法。

    

    部署用于结构化生成任务（命名实体识别、JSON提取、问答和分类）的大语言模型（LLM）缺乏形式化的可靠性保证，而标准的启发式弃权策略在7.5%至12.5%的设置中会违反用户指定的风险目标，且无法提供针对每次部署的保证。我们刻画了保形风险控制（CRC）何时能够认证结构化的LLM输出，以及何时在理论上可以证明其无法做到。首先，我们证明了一个更精细且可达到的可行性前沿：当基础风险μ超过目标风险α时，任何无分布方法必须至少在(μ-α)/(M-α)比例的输入上弃权，由此得到一个闭式形式的可行性测试，可在运行CRC之前判定其是否可行。其次，我们建立了一个经过验证的认证相图，涵盖Hoeffding、经验Bernstein以及基于投注的e-CRC三种边界方法，并在716种配置（六个3B至72B的开源权重模型、八个数据集、六种评分函数）上进行了验证：在严格目标（α≤0.20）下……

    arXiv:2606.29054v2 Announce Type: replace  Abstract: Large language models (LLMs) deployed for structured generation (NER, JSON extraction, QA, and classification) lack formal reliability guarantees, and standard heuristic abstention policies violate user-specified risk targets on 7.5--12.5% of settings, with no per-deployment guarantee. We characterize when conformal risk control (CRC) can certify structured LLM outputs and when it provably cannot. First, we prove a sharpened, attained feasibility frontier: when base risk \mu exceeds target \alpha, any distribution-free method must abstain on at least (\mu-\alpha)/(M-\alpha) of inputs, yielding a closed-form feasibility test that decides whether CRC can work before running it. Second, we establish a proven certification phase diagram across Hoeffding, empirical Bernstein, and a betting-based e-CRC bound, verified over 716 configurations (six open-weight models 3B--72B, eight datasets, six scores): at strict targets (\alpha <= 0.20) th
    
[^553]: 基于可解释神经网络连接从头算对称性与全局核质量

    Bridging Ab Initio Symmetries and Global Nuclear Masses with Interpretable Neural Networks

    [https://arxiv.org/abs/2606.28287](https://arxiv.org/abs/2606.28287)

    该论文提出了新型可解释神经网络WINN，通过约束质量公式对SU(3)和SU(4)对称性算符呈线性关系，成功架起了从头算对称性与全局核质量之间的桥梁，实现了兼具物理可解释性和外推预测能力的核质量建模。

    

    从头算理论确立了维格纳SU(4)对称性和埃利奥特SU(3)对称性作为轻核和中等质量核中核力的主导对称性。先前的工作已表明前者对称性与核结合能的相关性，但后者是否组织核结合能仍不明确。我们探究这两种对称性是否都能组织核质量，旨在通过可解释模型获得物理洞察并实现预测能力。基于SU(3)和SU(4)卡西米尔算符，我们构建了3个神经网络模型。其中两个是常规模型：特征信息神经网络（FINN）和具有预测离散度的高斯变体（GINN），而维格纳信息网络（WINN）是一种全新设计，它约束质量公式对算符呈线性关系，并学习其依赖于的耦合系数，从而使模型具有内在可解释性。所有模型均在经液滴模型扣除后的AME2016数据上按4种数据比例进行训练，并在AME2020新增的核素上进行验证，具备外推能力。

    arXiv:2606.28287v2 Announce Type: replace-cross  Abstract: Ab initio theory establishes Wigner's SU(4) and Elliott's SU(3) as dominant symmetries of the nuclear force in light and intermediate-mass nuclei. Previous work shows the relevance of the former symmetry for nuclear binding, whether the latter organizes binding remains elusive. We probe whether both these symmetries organize nuclear masses, aiming at physical insights through interpretable models and predictive capability. From the SU(3) and SU(4) Casimirs we build 3 neural-network models. Two are conventional, a feature-informed NN (FINN) and a Gaussian variant (GINN) with predictive spread, while Wigner-informed network (WINN) is a new design constraining the mass formula to be linear in the operators, learning their (N,Z)-dependent couplings, so that the model is intrinsically explainable. All are trained on AME2016 subtracted by the liquid drop model at 4 data fractions and validated on nuclei new to AME2020, with extrapola
    
[^554]: 平均场PhiBE：基于离散时间数据的连续时间平均场强化学习

    Mean-Field PhiBE: Continuous-Time Mean-Field Reinforcement Learning from Discrete-Time Data

    [https://arxiv.org/abs/2606.26498](https://arxiv.org/abs/2606.26498)

    本文提出MF-PhiBE，通过将离散时间数据融入Wasserstein空间上的连续时间偏微分方程，解决了连续时间平均场控制中模型不可识别性的难题。

    

    本文研究了在群体动态根据未知的McKean-Vlasov随机微分方程连续演化，但仅有离散时间转移数据可用的情况下的无模型连续时间平均场控制问题。在基于模型的公式中，策略评估自然由$\mathcal P_2(\mathbb R^d)$上的稳态Hamilton-Jacobi-Bellman方程描述，但该方程涉及受控McKean-Vlasov动态的漂移和扩散系数，而这些系数在仅有离散时间数据时无法识别。另一方面，直接简化为时间离散的Bellman方程虽避免了不可识别性问题，但失去了微分方程结构。为桥接这两种视角，我们引入了平均场PhiBE（MF-PhiBE），它将离散时间转移信息整合到Wasserstein空间上的连续时间偏微分方程中。

    arXiv:2606.26498v1 Announce Type: cross  Abstract: This paper addresses model-free continuous-time mean-field control in a setting where the population dynamics evolve continuously according to an unknown McKean-Vlasov stochastic differential equation, while only discrete-time transition data are available. In the model-based formulation, policy evaluation is naturally described by a stationary Hamilton-Jacobi-Bellman equation on $\mathcal P_2(\mathbb R^d)$, but this equation involves the drift and diffusion coefficients of the controlled McKean-Vlasov dynamics, which are not identifiable when only discrete-time data are available. On the other hand, a direct reduction to a time-discrete Bellman equation avoids the non-identifiability issue but loses the differential equation structure. To bridge these two viewpoints, we introduce a Mean-Field-PhiBE (MF-PhiBE), which incorporates discrete-time transition information into a continuous-time PDE on the Wasserstein space. The MF-PhiBE repl
    
[^555]: Pre-Warm：基于首批图像块字典的卷积滤波器初始化方法

    Pre-Warm: Initializing Convolutional Filters from First-Batch Patch Dictionaries

    [https://arxiv.org/abs/2606.25256](https://arxiv.org/abs/2606.25256)

    提出Pre-Warm方法，基于规则地从单个小批次构建的图像块字典（选择重构效果更好的主成分或k-means）来初始化卷积网络第一层一半的滤波器，相比随机初始化能更有效地利用训练图像信息。

    

    卷积滤波器的随机初始化未能利用训练图像。先前的研究表明，图像块可以被复制到网络第一层，且图像块的k-means聚类或主成分可以作为滤波器使用。本文在固定其他所有因素的条件下，比较了小型卷积网络第一层的四种初始化方法：He初始化、随机去均值图像块、图像块的主成分以及k-means质心。我们提出的Pre-Warm方法是一种基于规则的双字典使用方式：图像块数量由滤波器数量和前景密度共同决定，两个字典均从单个小批次（minibatch）样本构建，且主成分与k-means中能更好重构这些图像块的方法将被写入滤波器组的前半部分，而无需通过验证集搜索来选择。其余滤波器保持随机初始化。在五个数据集上，主成分方法提升了CIFAR-1……

    arXiv:2606.25256v2 Announce Type: replace-cross  Abstract: Random initialization of convolutional filters does not use the training images. Previous work has shown that image patches can be copied into the first layer, and that k-means or principal components of patches can serve as filters. This paper compares four initializations of the first layer of a small convolutional network, with every other factor held fixed: He initialization, random mean-centered patches, principal components of those patches, and k-means centroids. Pre-Warm, our proposed methodology, is the rule-based use of both dictionaries: the patch count follows the filter count and a foreground density, both dictionaries are built from a single minibatch, and whichever of principal components or k-means better reconstructs those patches is written into the first half of the filter bank, rather than chosen by a validation search. The remaining filters stay random. On five datasets, principal components improve CIFAR-1
    
[^556]: 发现潜在群体以实现鲁棒分类

    Discovering Latent Groups for Robust Classification

    [https://arxiv.org/abs/2606.23609](https://arxiv.org/abs/2606.23609)

    本文提出神经分类树（NCT）框架，通过在树形架构中编码子群体结构，基于预测正确性将样本路由到“简单”或“困难”节点并迭代生成伪标签，无需子群体监督即可解耦冲突子群体，实现鲁棒分类。

    

    机器学习模型往往会利用虚假相关性，虽然能获得较高的平均准确率，但在代表性不足的子群体上却会出现不成比例的失败。现有方法通过调整网络参数来解决这一问题，其依据要么是子群体标注，要么是推断出的伪群体标签。然而在推理阶段，这些方法仅输出类别预测，无法揭示样本的潜在子群体。我们提出了神经分类树，这是一个通过在树形架构中编码子群体结构来实现鲁棒性的框架。通过基于预测正确性将每个样本路由到该树的“简单”或“困难”节点，并将这些路由结果作为下一轮迭代的伪标签，NCT无需子群体监督即可解耦相互冲突的子群体。我们在五个涵盖二元和多类虚假相关性的基准上对NCT进行评估。实验表明，学习到的树拓扑结构提供了……

    arXiv:2606.23609v2 Announce Type: replace-cross  Abstract: Machine learning models exploit spurious correlations, achieving high average accuracy but failing disproportionately on underrepresented subgroups. Existing methods address this by adjusting network parameters, guided either by subgroup annotations or inferred pseudo-group labels. Yet at inference, these methods produce only a class prediction, with no insight into a sample's latent subgroup. We propose neural classification trees (NCT), a framework that achieves robustness by encoding subgroup structure in its tree-shaped architecture. By routing each sample to an "easy" or "hard" node of this tree - based on prediction correctness - and reusing these routes as pseudo-labels for the next iteration, NCT disentangles conflicting subgroups, without requiring subgroup supervision. We evaluate NCT on five benchmarks spanning binary and multi-class spurious correlations. Our experiments show that the learned tree topology provides 
    
[^557]: 交错式语音语言模型在潜在空间中隐式处理文本

    Interleaved Speech Language Models Latently Work In Text

    [https://arxiv.org/abs/2606.22473](https://arxiv.org/abs/2606.22473)

    交错式语音语言模型在中间层会隐式地将口语单词转录为对应的文本词元，且这一隐式转录现象与模型的口语事实知识检索能力呈正相关。

    

    语音语言模型（SLM）日益将语音和文本结合起来，通常通过在单个序列中交错二者的词元来实现。然而，这两种模态在模型潜在空间中如何相互作用仍不清楚。在本工作中，我们使用三种互补的方法，分析了来自不同模型家族和训练配置的交错式语音-文本语言模型。我们发现，这些模型会经历一个隐式的潜在转录阶段：在该阶段中，与口语单词相对应的文本词元在中间层变得可解码，尽管这些模型并未接受过语音识别的训练。这一现象出现在多样且自然的语音中，并且中间表示还编码了可能的文本后续内容。我们进一步表明，当结合文本语言模型预训练与语音-文本交错训练时，隐式转录现象最为显著，且其普遍程度与口语事实知识检索能力呈正相关。我们的分析揭示了……

    arXiv:2606.22473v2 Announce Type: replace-cross  Abstract: Speech language models (SLMs) increasingly combine speech and text, often by interleaving their tokens within a single sequence. Yet how these two modalities interact in the model's latent space remains unclear. In this work, we analyze interleaved speech--text LMs from different model families and training configurations using three complementary methods. We reveal that these models pass through an implicit latent transcription phase in which the text token matching the spoken word becomes decodable in intermediate layers, despite not being trained for speech recognition. This phenomenon occurs in diverse, natural speech, and intermediate representations also encode likely text continuations. We further show that implicit transcription emerges most when combining text-LM pretraining and speech--text interleaving, and that its prevalence is positively associated with spoken factual-knowledge retrieval. Our analysis sheds light 
    
[^558]: 信号藏在哪里？一种用于医学编码器预训练的网络数据配方

    Where Does the Signal Live? A Web Data Recipe for Medical Encoder Pretraining

    [https://arxiv.org/abs/2606.22079](https://arxiv.org/abs/2606.22079)

    该论文提出了一种面向医学编码器预训练的网络数据配方，通过医学术语密度过滤与LLM信号放大改写两种互补策略，在法语医学NLP任务上超越了广泛使用的教育质量过滤方法。

    

    网络数据策划已被广泛研究用于解码器大语言模型（LLM）的预训练。相比之下，医学等密集术语领域的编码器通常在小型、人工策划的语料库上进行预训练，这限制了可扩展性和写作风格的多样性，而这一瓶颈在非英语临床环境中更为严重。网络规模的数据策划是否也能使密集术语领域的编码器掩码语言建模（MLM）受益，仍然是一个悬而未决的问题。为了解决这个问题，我们引入了两个互补的杠杆：医学术语密度过滤，用于选择富含医学术语的文档；信号放大改写，使用LLM将文档改写为术语更密集且实体上下文更广泛的变体。我们在法语医学NLP上实例化了该配方。结果表明，医学术语密度过滤器在下游医学任务上优于广泛使用的教育质量过滤器，且二者互补。信号放大改写…

    arXiv:2606.22079v2 Announce Type: replace-cross  Abstract: Web data curation has been widely studied for decoder Large Language Model (LLM) pretraining. Encoders for dense-terminology domains such as medicine, by contrast, are pretrained on small, manually-curated corpora that limit scalability and writing style diversity, a bottleneck even more severe in non-English clinical settings. Whether web-scale data curation also benefits encoder Masked Language Modeling (MLM) in a dense-terminology domain remains an open question. To address this, we introduce two complementary levers. Medical-term density filtering selects documents rich in medical terms. Signal-amplifying rephrasing uses an LLM to rewrite documents into denser variants with broader entity contexts. We instantiate the recipe on French medical NLP. The medical-term density filter outperforms the widely-used educational quality filter on downstream medical tasks, and the two complement each other. Signal-amplifying rephrasing 
    
[^559]: VegSim：一个用于情景条件化植被模拟的地理空间世界模型

    VegSim: A Geospatial World Model for Scenario-Conditioned Vegetation Simulation

    [https://arxiv.org/abs/2606.21961](https://arxiv.org/abs/2606.21961)

    VegSim是一个地理空间世界模型，可从稀疏卫星数据推断植被潜在状态，并通过将未来天气作为可控输入，实现观测天气下的概率预测和用户自定义气象情景下的条件化植被模拟。

    

    气候压力下的植被监测不仅需要回答植被在预期天气下将如何演变，还需要回答植被对不同气象条件的响应。现有的预测模型只能返回在观测天气下的预期植被状态，无法回答这些情景条件化的问题，因为未来天气被固定在已记录的轨迹上。我们提出了VegSim，一个用于情景条件化植被模拟的地理空间世界模型。VegSim从稀疏的卫星NDVI历史数据、过去的气象协变量和静态空间上下文中推断潜在的植被状态，通过循环潜在动力学在未来天气强迫下将状态向前传播，并在每个预测时长解码出预测的NDVI分位数。由于未来天气强迫作为可控输入进入模型，同一个训练好的模型既支持在观测天气下的概率预测，也支持在用户定义的气象情景下的条件模拟。

    arXiv:2606.21961v2 Announce Type: replace  Abstract: Vegetation monitoring under climate stress requires answering not only how it will evolve given the expected weather, but how it would respond to alternative meteorological conditions. Forecasting models return the expected vegetation state for the observed weather and cannot answer these scenario-conditioned questions, because future weather is fixed to the recorded trajectory. We present VegSim, a geospatial world model for scenario-conditioned vegetation simulation. VegSim infers a latent vegetation state from sparse satellite-derived NDVI histories, past meteorological covariates, and static spatial context, propagates it forward under future weather forcing through recurrent latent dynamics, and decodes predictive NDVI quantiles at each lead time. Because future forcing enters as a controllable input, the same trained model supports probabilistic forecasting under observed weather and conditional simulation under user-defined me
    
[^560]: A-Evolve-Training：一个300亿参数模型的自主后训练

    A-Evolve-Training: Autonomous Post-Training of a 30B Model

    [https://arxiv.org/abs/2606.20657](https://arxiv.org/abs/2606.20657)

    该论文提出了一个无需人工干预的自主后训练系统，能在数周内对300亿参数模型进行多轮训练，并自主发现并修正了优化策略偏差，使模型性能接近人类顶尖水平。

    

    arXiv:2606.20657v2 公告类型：替换 摘要：对前沿模型进行后训练通常需要数周的人工工作：提出数据和配方更改、启动运行、阅读评估结果、决定保留哪些内容。我们报告了一个自主系统，该系统无需人工干预即可运行这一循环，在数周内对300亿参数的Nemotron模型进行了四轮后训练。自主生成的模型在公开的NVIDIA Nemotron-Reasoning挑战排行榜上达到了0.86的保留得分，而人类最佳提交得分为0.87，截至撰写时在约4000个参赛者中排名第8。比数字更引人注目的是：该系统检测到其自身的开发指标已停止追踪最弱领域的外部性能——候选方案将开发指标推至历史新高，但未能推动外部目标——于是它修订了自己的搜索策略，不再最大化开发指标，而是寻求在改善外部目标的同时降低现已具有误导性的代理指标的干预措施。我们将此视为可审计的直接证据，表明一个规模化系统能够自主调整其优化策略。

    arXiv:2606.20657v2 Announce Type: replace  Abstract: Post-training a frontier model is normally weeks of human work: proposing data and recipe changes, launching runs, reading evals, deciding what to keep. We report an autonomous system that runs this loop with no human in the loop, post-training a 30B Nemotron across four rounds over multiple weeks. The autonomously produced model reaches a held-out score of 0.86 against the top human submission's 0.87 on the public NVIDIA Nemotron-Reasoning Challenge leaderboard, placing 8th of ~4000 at the time of writing. More striking than the number: the loop detected that its own dev metric had stopped tracking external performance on the weakest domain -- candidates drove dev to record highs without moving the external target -- and revised its own search policy, no longer maximizing dev but seeking interventions that lowered the now-misleading proxy while improving the external target. We treat this as direct, auditable evidence that a scaled 
    
[^561]: 激活与影响感知秩（AIR）：面向大语言模型的保功能SVD压缩

    Activation- and Influence-Aware Ranks (AIR): Function-Preserving SVD Compression for LLMs

    [https://arxiv.org/abs/2606.19993](https://arxiv.org/abs/2606.19993)

    AIR是一种基于SVD的大语言模型压缩框架，通过将激活感知最优解与反向信号影响度量结合于单次闭式ALS扫描中，在参数保留率≤60%时实现超过18%的困惑度改善，校准数据需求减少约90%，并带来实际的计算量、内存和延迟收益。

    

    我们提出了激活与影响感知秩（AIR），这是一个基于SVD的大语言模型压缩框架，它利用一种基于反向信号的影响度量来指导每个权重矩阵的低秩近似。从SVD-LLM(W)的激活感知最优解出发，AIR仅需运行一次闭式交替最小二乘（ALS）扫描，即可在单调下降保证下逐元素地整合影响信息。AIR是层局部的，并可与端到端方法正交组合：单独使用时它超越ACIP，而AIR+LoRA则能进一步提升性能。AIR在参数保留率≤60%时将困惑度相比SVD-LLM(W)改善超过18%，并用约少90%的校准数据达到同等质量，同时将参数节省转化为FLOP、峰值内存和每token延迟方面的实际收益。

    arXiv:2606.19993v2 Announce Type: replace  Abstract: We present Activation- and Influence-Aware Ranks (AIR), an SVD-based LLM compression framework that guides each weight matrix's low-rank approximation with a backward-signal influence metric. Starting from the activation-aware optimum of SVD-LLM(W), AIR runs a single closed-form alternating least squares (ALS) sweep that integrates influence element-wise under a monotone-descent guarantee. AIR is layer-local and composes orthogonally with end-to-end methods: alone it exceeds ACIP, and AIR+LoRA outperforms it further. AIR improves perplexity over SVD-LLM(W) by >18% at <=60% parameter retention, matches its quality with ~90% less calibration data, and turns parameter savings into FLOP, peak-memory, and per-token latency gains.
    
[^562]: 用户满意度保证下基于有限用户反馈的成本最优LLM路由

    Cost-Optimal LLM Routing with Limited User Feedback under User Satisfaction Guarantees

    [https://arxiv.org/abs/2606.19376](https://arxiv.org/abs/2606.19376)

    提出SLARouter，一种能从生产环境中稀疏的单侧用户反馈中在线学习成本最优路由策略、同时提供严格SLA合规理论保证的LLM路由算法。

    

    大型语言模型（LLM）应用的推理成本正在快速增长，这是由需求激增和基础设施成本上升所驱动的。用户期望获得高质量的回复，而在商业环境中，这一期望被正式写入服务水平协议（SLA）中，从而在成本与质量之间形成了根本性的张力。近年来在成本感知的LLM请求路由方面的进展显示出解决这一矛盾的潜力，但现有方法依赖于完整的反馈信号、离线训练以及针对每个工作负载的大量调优，且大多数方法缺乏SLA保证或推理时的自适应性。我们提出了SLARouter，这是一种在线路由算法，能够从生产系统中可获得的稀疏、单侧用户反馈中学习成本最优策略。SLARouter为成本最优性和严格的SLA合规性提供了理论保证。在广泛的LLM基准测试上的实验表明，SLARouter在满足SLA约束的同时实现了成本节约。

    arXiv:2606.19376v2 Announce Type: replace-cross  Abstract: Inference costs for large language model (LLM) applications are rapidly growing, driven by surging demand and rising infrastructure cost. Users expect high-quality responses, and in commercial settings this is formally codified in Service Level Agreements (SLAs), creating a fundamental tension between cost and quality. Recent progress on cost-aware LLM request routing has shown potential to resolve this tension, but existing approaches rely on complete feedback signals, offline training, extensive per-workload tuning, and most lack SLA guarantees or inference-time adaptivity. We introduce SLARouter, an online routing algorithm that learns a cost-optimal policy from the sparse, one-sided user feedback available in production systems. SLARouter provides theoretical guarantees for both cost optimality and strict SLA compliance. Experiments across a wide range of LLM benchmarks show that SLARouter satisfies SLA constraints without 
    
[^563]: 单调柯尔莫哥洛夫-阿诺德网络：单调性作为归纳偏置的理论与实证研究

    Monotonic Kolmogorov-Arnold Networks: A Theoretical and Empirical Study of Monotonicity as an Inductive Bias

    [https://arxiv.org/abs/2606.17886](https://arxiv.org/abs/2606.17886)

    提出MKAN，一种通过B样条系数指数重参数化、正边权重和单调基础激活函数，为所有参数值保证硬单调性的柯尔莫哥洛夫-阿诺德网络，可直接使用标准无约束梯度下降训练，并附有表示成本定理这一核心理论贡献。

    

    单调性长期以来一直是神经网络的一种架构归纳偏置，其动机来自表格数据、科学和经济等领域，在这些场景中输出对某些输入的响应已知是单调的。现有方法基于MLP或流模型，缺乏逐边的功能透明性；唯一具有单调性的柯尔莫哥洛夫-阿诺德网络（KAN）变体MonoKAN仅在受限的参数子集上强制执行该约束，且需要投影式的训练过程。我们通过MKAN弥补了这一空白：MKAN是一种通过对B样条系数进行指数重参数化、采用正边权重以及单调基础激活函数，从而为所有参数值保证硬单调性的KAN，其训练可简化为标准的无约束梯度下降。我们的核心理论贡献是一个表示成本定理：任何诱导球形语义邻域划分的C^K（K>0）特征提取器都…（摘要原文在此处截断）

    arXiv:2606.17886v3 Announce Type: replace  Abstract: Monotonicity has been a long-running architectural inductive bias for neural networks, motivated by tabular, scientific, and economic settings where outputs are known to respond monotonically to certain inputs. Existing approaches are MLP- or flow-based and lack per-edge functional transparency; the only Kolmogorov--Arnold Network (KAN) variant with monotonicity, MonoKAN, enforces the constraint only on a restricted parameter subset and requires a projection-style training procedure. We close this gap with \textbf{MKAN}, a KAN with hard monotonicity guaranteed for \emph{all} parameter values via exponential reparameterization of B-spline coefficients, positive edge weights, and a monotone base activation. Training reduces to standard unconstrained gradient descent. Our headline theoretical contribution is a \emph{representation-cost} theorem: any $C^K, K >0$ feature extractor inducing a ball-shaped semantic-neighborhood partition adm
    
[^564]: 基于语义引导的联邦图推荐改进

    Improving Federated Graph Recommendation with Semantic Guidance

    [https://arxiv.org/abs/2606.15277](https://arxiv.org/abs/2606.15277)

    该论文提出了一种语义-结构联邦图推荐框架，利用大语言模型的语义嵌入引导跨客户端对齐，解决了联邦学习中非独立同分布数据下图表示无法全局对齐的问题。

    

    基于图的推荐模型能够有效捕捉用户-物品交互图中的高阶协同信号。联邦学习（FL）支持在分布式客户端之间进行隐私保护的训练。然而，在联邦学习下直接聚合图表示是具有挑战性的：在非独立同分布（non-IID）的数据分布下，本地学习到的结构化嵌入无法实现全局对齐，而简单的参数平均也无法恢复跨客户端的关系结构。现有的联邦图推荐方法主要依赖于结构聚合，却忽略了大型语言模型（LLM）中编码的全局语义知识。在这项工作中，我们提出了一种语义-结构联邦图推荐框架，利用LLM嵌入来引导跨客户端对齐。每个客户端从其本地交互图中学习用户表示，并使用（摘要在此处截断）将典型的交互模式总结为紧凑的语义向量。

    arXiv:2606.15277v2 Announce Type: replace-cross  Abstract: Graph-based recommendation models effectively capture high-order collaborative signals from user--item interaction graphs. Federated learning (FL) enables privacy-preserving training across distributed clients. However, directly aggregating graph representations under FL is challenging: locally learned structural embeddings are not globally aligned under non-IID data distributions, and naive parameter averaging fails to recover cross-client relational structure. Existing federated graph-based approaches primarily rely on structural aggregation, yet overlook the global semantic knowledge encoded in large language models (LLMs). In this work, we propose a semantic--structural federated graph recommendation framework that leverages LLM embeddings to guide cross-client alignment. Each client learns user representations from its local interaction graph and summarizes typical interaction patterns into compact semantic vectors using a
    
[^565]: 数据科学中的表示成本：深度神经网络的基础与拟巴拿赫空间

    Representation Costs in Data Science: Foundations and the Quasi-Banach Spaces of Deep Neural Networks

    [https://arxiv.org/abs/2606.14954](https://arxiv.org/abs/2606.14954)

    本文提出了一个统一框架，通过参数空间正则化器分析数据科学中的表示成本，揭示了参数化方法与其原生函数空间之间的联系，并将核方法、小波和神经网络等经典方法统一为特例。

    

    我们开发了一个通用框架，用于通过参数空间正则化器分析参数化数据拟合方法的表示成本。从这一抽象视角出发，我们定义了任意参数化模型的表示成本，并揭示了它们所诱导的（原生）函数空间。这统一了近期关于数据拟合方法的函数空间视角。我们还证明，在该抽象设定下许多自然结论成立，包括参数化方法在其原生空间上的表示定理。该框架还严格地将参数化方法与其在充分过参数化下的等价非参数描述联系起来。经典方法及其原生空间，如核方法/再生核希尔伯特空间、小波/贝索夫空间以及浅层神经网络/变分空间，均作为我们抽象框架的特例出现。将表示成本研究“公理化”是一个副产品。

    arXiv:2606.14954v3 Announce Type: replace-cross  Abstract: We develop a general framework for analyzing representation costs of parametric data-fitting methods through their parameter-space regularizers. From this abstract perspective, we define representation costs for arbitrary parametric models and reveal their induced (native) function spaces. This unifies recent function-space views of data-fitting methods. We also prove that many natural results hold in this abstract setting, including representer theorems for parametric methods on their native spaces. The framework also rigorously connects parametric methods with their equivalent nonparametric descriptions under sufficient overparameterization. Classical methods and their native spaces, such as kernel methods / reproducing kernel Hilbert spaces, wavelets / Besov spaces, and shallow neural networks / variation spaces emerge as special cases of our abstract framework. A byproduct of "axiomatizing" the study of representation costs
    
[^566]: 理解持续视觉-语言模型中的跨模态贡献：一个理论视角

    Understanding Cross-Modal Contributions in Continual Vision-Language Models: A Theoretical Perspective

    [https://arxiv.org/abs/2606.14883](https://arxiv.org/abs/2606.14883)

    本文提出了一个新的理论视角来理解持续视觉-语言模型中视觉-语言跨模态对不同环境的贡献，并在大型模型上实证验证了其有效性。

    

    持续视觉-语言模型通常通过顺序微调来解决；然而，尽管这一范式能够适应新环境（任务），但它本质上强调了对先前学习环境（任务）的贡献，却牺牲了保留先前获得的知识所需的稳定性。虽然现有方法已经充分研究了视觉-语言模型（VLMs）中的持续学习和灾难性遗忘，但对跨一系列环境的模态特定贡献的理论理解在很大程度上仍未被探索。在本文中，我们提出了一个新的理论视角来理解跨模态（视觉-语言）对连续环境的贡献。我们在大型VLMs上实证评估了我们的理论发现，并展示了它们在捕获环境级跨模态贡献方面的有效性。我们的分析提供了更深入的见解

    arXiv:2606.14883v2 Announce Type: replace-cross  Abstract: Continual vision-language models are commonly addressed through sequential fine-tuning; however, although this paradigm enables adaptation to new environments (tasks), it inherently emphasizes the contribution of previously learned environments (tasks) at the expense of the stability required to preserve previously acquired knowledge. While existing approaches have adequately studied continual learning and catastrophic forgetting in vision-language models (VLMs), the theoretical understanding of modality-specific contributions across a sequence of environments remains largely unexplored. In this paper, we present a new theoretical perspective to understand the cross-modal (vision-language) contributions to consecutive environments. We empirically evaluate our theoretical findings on large VLMs and demonstrate their effectiveness in capturing environment-level cross-modal contributions. Our analysis provides deeper insights into
    
[^567]: 机器遗忘行为审计中的隐私-审计对齐特性刻画

    Characterizing Privacy-Audit Alignment in Behavioral Audit of Machine Unlearning

    [https://arxiv.org/abs/2606.14518](https://arxiv.org/abs/2606.14518)

    本文提出几何迁移定理，首次量化了机器遗忘行为审计中审计准确度与保留集隐私泄露之间的内在权衡关系，为互不信任双方的机器遗忘审计提供了理论基础。

    

    通过机器遗忘从机器学习模型中移除已学习数据的研究已被广泛开展；然而，目前尚无公认的机器遗忘审计方案。现有工作表明，不诚实的模型所有者可以伪造证据以避免执行机器遗忘，而好奇的审计者（以及攻击者）即使只有有限的访问权限，也可以推断出模型及其训练数据中的隐私敏感属性。然而，在模型所有者与审计者互不信任的情况下对机器遗忘进行审计的研究仍处于空白。在本文中，我们刻画了仅依赖查询模型获取行为信号的通用审计方案在多大程度上不可避免地导致与保留集相关的隐私泄露，为此我们提供了一个几何迁移定理，该定理基于审计准确度建立了保留集成员可区分性的下界。此外，我们还研究了被遗忘集、目标样本和查询协议如何共同决定……

    arXiv:2606.14518v2 Announce Type: replace  Abstract: The removal of learned data from Machine Learning models through Machine Unlearning (MU) has been widely studied; however, there is no agreed-upon scheme for auditing MU. Existing work shows that a dishonest model owner can falsify evidence to avoid executing MU, while curious auditors (and adversaries) can infer privacy-sensitive properties of the model and its training data even with limited access. Yet auditing of MU under mutual distrust between the model owner and the auditor remains unexplored. In this paper, we characterize how much a generic audit scheme that relies solely on querying the model for behavioral signals inevitably results in privacy leakage related to the retained set by providing a geometric transfer theorem that establishes a lower bound on retained set membership distinguishability based on the audit accuracy. In addition, we study how the unlearned set, target sample, and query protocol jointly determine the
    
[^568]: OdysSim：构建人类行为模拟的基础模型

    OdysSim: Building Foundation Models for Human Behavior Simulation

    [https://arxiv.org/abs/2606.14199](https://arxiv.org/abs/2606.14199)

    该论文提出了OdysSim——迄今最大规模的行为基础模型系统性研究，通过SOUL五维能力分类法统一62个数据集和23个基准任务，并利用2140万次交互的语料库与结合中期训练、强化学习和专家蒸馏的训练方案，训练出在23项任务中8项排名第一的开源8B人类行为模拟模型。

    

    大型语言模型正日益被部署为人类模拟器，用于交互式评估和社会模拟。然而，以有帮助性为导向的后训练使它们趋向于同质化、过于顺从的助手语调，造成了行为上的“虚拟到现实”鸿沟。我们提出了OdysSim，这是迄今为止最大规模的关于行为基础模型的系统性开放研究，即训练用于大规模模拟人类行为的模型。我们提出了SOUL分类法，这是一个包含五个能力维度（CONV对话、SS社交技能、COG认知、ROLE角色扮演、EVAL评估）的分类体系，将62个数据集和23个基准任务统一在一个框架下。具体而言，我们策划了OdysSim语料库（2140万次交互、100亿token，并通过回生成的社交语境进行了改造），构建了SOUL-Index基准，并开发了一种结合中期训练、任务特定强化学习和专家蒸馏的端到端训练方案。由此产生的开源8B OSim模型在23个任务中的8个任务上排名第一或并列第一，超越了任何单个（对话截断）

    arXiv:2606.14199v2 Announce Type: replace-cross  Abstract: Large language models are increasingly deployed as human simulators for interactive evaluation and social simulation. Yet helpfulness-driven post-training pulls them toward a homogeneous, overly agreeable assistant register, creating a behavioral Sim2Real gap. We present OdysSim, the largest open systematic investigation of behavioral foundation models, i.e., models trained to simulate human behavior at scale. We propose SOUL, a taxonomy of five capability axes (CONV, SS, COG, ROLE, EVAL) that unifies 62 datasets and 23 benchmark tasks under one framework. Specifically, we curate the OdysSim corpus (21.4M interactions, 10B tokens, retrofitted with back-generated social contexts), construct the SOUL-Index benchmark, and develop an end-to-end training recipe combining midtraining, task-specific RL, and expert distillation. The resulting open 8B OSim model ranks first or tied-first on 8 of 23 tasks, outperforming any individual fr
    
[^569]: 分子通信中游程受限信道编码的掩码神经检测

    Masked Neural Detection for Run-Length-Limited Channel Coding in Molecular Communication

    [https://arxiv.org/abs/2606.12489](https://arxiv.org/abs/2606.12489)

    该论文提出了一种解码器感知的训练掩码方法，通过在训练SBRNN神经检测器时屏蔽RLIM解码器大概率会确定性覆盖的位置，将网络容量集中于承载信息的位置，使掩码RLIM₂-SBRNN在57个工作点中的40个点优于最佳未编码接收机。

    

    分子通信（MC）因扩散记忆效应而受到严重影响，因为针对某一符号释放的分子可能在后续符号间隔内才到达。神经序列检测器，尤其是滑动双向递归神经网络（SBRNN），在此类信道中的性能显著优于阈值检测。这为MC信道编码提出了一个核心问题：当编码传输与未编码传输均采用神经检测进行评估时，那些其优越性最初是在阈值检测下建立的码是否仍能保持优势？本短文针对游程受限ISI缓解（RLIM）码回答了这一问题，提出了一种解码器感知的训练掩码，该掩码移除了RLIM解码器极大概率会确定性覆盖的位置，从而将紧凑型SBRNN的容量引导至承载信息的位置。掩码RLIM₂-SBRNN在57个工作点中的40个点优于最佳未编码接收机（阈值或SBRNN），并带来了增益。

    arXiv:2606.12489v3 Announce Type: replace-cross  Abstract: Molecular communication (MC) suffers from severe diffusion memory because molecules released for one symbol may arrive during later symbol intervals. Neural sequence detectors, especially sliding bidirectional recurrent neural networks (SBRNNs), substantially outperform threshold detection in such channels. This raises a central question for MC channel coding: does a code whose superiority was established under threshold detection retain it when both coded and uncoded transmission are evaluated with neural detection? This letter answers this question for run-length-limited ISI-mitigation (RLIM) codes by proposing a decoder-aware training mask that removes the positions the RLIM decoder has a high probability of deterministically overwriting, steering compact-SBRNN capacity toward the information-bearing positions. The masked RLIM$_2$-SBRNN beats the best uncoded receiver (threshold or SBRNN) at 40 of 57 operating points; gains 
    
[^570]: 基于PCA增强的自适应NVAR框架用于日本海高分辨率海表温度预测

    PCA-Enhanced Adaptive NVAR Framework for High-Resolution Sea Surface Temperature Forecasting in the East Sea

    [https://arxiv.org/abs/2606.12141](https://arxiv.org/abs/2606.12141)

    本研究将基于SVD的PCA降维与自适应非线性向量自回归（Adaptive NVAR）框架相结合，有效压缩高分辨率海表温度场数据，克服了传统方法的误差累积问题，实现了对日本海等海域长期、低计算成本且准确的海表温度预测。

    

    海表温度（SST）的精确预测对海洋生态系统监测、气候评估、渔业管理以及业务化海洋预报至关重要。尽管数值海洋模式能够提供可靠的预测，但其计算成本高昂；而传统机器学习方法则常因高维输入以及长期自主预报中的误差累积问题而表现受限。本研究通过引入基于奇异值分解（SVD）的主成分分析（PCA），将我们此前提出的自适应非线性向量自回归框架扩展至高分辨率真实世界的海表温度预测。来自GLORYS12V1再分析数据集、覆盖日本海、黄海和东海的逐日海表温度场被压缩为能够保留主导空间变率的低维潜在表示。所提出的降阶框架利用……进行评估（原文摘要在此处截断，内容不完整）

    arXiv:2606.12141v2 Announce Type: replace  Abstract: Accurate forecasting of sea surface temperature (SST) is essential for marine ecosystem monitoring, climate assessment, fisheries management, and operational ocean forecasting. While numerical ocean models provide reliable predictions, they are computationally expensive, and conventional machine learning methods often suffer from high-dimensional inputs and error accumulation during long-term autonomous forecasting. This study extends our previously proposed Adaptive Nonlinear Vector Autoregression (Adaptive NVAR) framework to high-resolution real-world SST prediction by integrating Principal Component Analysis (PCA) through Singular Value Decomposition (SVD). Daily SST fields from the GLORYS12V1 reanalysis dataset covering the East Sea, Yellow Sea, and East China Sea are compressed into a lower-dimensional latent representation that preserves the dominant spatial variability. The proposed reduced-order framework is evaluated using a
    
[^571]: 何时对齐，何时预测：多模态学习的相图

    When to Align, When to Predict: A Phase Diagram for Multimodal Learning

    [https://arxiv.org/abs/2606.11190](https://arxiv.org/abs/2606.11190)

    本文通过带结构化跨模态噪声相关的尖峰信号加噪声模型，揭示了跨模态对齐与跨模态预测两种范式的互补失败模式，并构建相图为多模态学习中范式的选择提供了理论指导。

    

    跨模态对齐（CA）和跨模态预测（CP）是多模态表征学习的两大主流范式，然而对于两者何时成功、何时失败，目前缺乏系统性的理解——这一空白使得实践者，尤其是在拥有异构仪器和多层次测量的科学领域中的从业者，无法诊断为什么标准方法的表现不如最佳单一模态。我们在具有结构化跨模态噪声相关性的尖峰信号加噪声模型下研究这两种目标，这种噪声相关性正是打破经典恢复保证的关键因素，并推导出了揭示互补失败模式的分离比率：对齐方法会对每个模态进行白化处理，当噪声在视图间强相关时会失败；预测方法通过单侧白化编码所有可跨模态预测的信息，其恢复效果由源模态的质量决定。由此得到的相图将多模态问题划分为不同区域。

    arXiv:2606.11190v3 Announce Type: replace  Abstract: Cross-modal alignment (CA) and cross-modal prediction (CP) are the dominant paradigms for multimodal representation learning, yet there is no systematic understanding of when each succeeds and when each fails --- a gap that leaves practitioners, especially in scientific domains with heterogeneous instruments and multiple levels of measurement, unable to diagnose why standard methods underperform the best single modality. We study both objectives under a spiked signal-plus-noise model with structured cross-modal nuisance correlation, the ingredient that breaks the classical recovery guarantees, and derive separation ratios that expose complementary failure modes: alignment whitens each modality and fails when nuisance is strongly correlated across views; prediction encodes whatever is cross-predictable through a one-sided whitening, with recovery governed by source-modality quality. The resulting phase diagram partitions multimodal pr
    
[^572]: 机器人策略编排中什么最重要：分层VLA智能体的系统性研究

    What Matters in Orchestrating Robot Policies: A Systematic Study of Hierarchical VLA Agents

    [https://arxiv.org/abs/2606.10267](https://arxiv.org/abs/2606.10267)

    本文对分层视觉-语言-动作（Hi-VLA）机器人系统进行了系统性研究，将代表性智能体统一到选项式控制框架下并在多种任务上基准测试核心设计选择，从中提炼出规划器、控制器与切换机制的实用设计原则。

    

    分层视觉-语言-动作（Hi-VLA）系统已成为复杂机器人操作领域一种有前景的范式，其通过高层VLM规划器将任务分解为语言子目标，再由低层VLA控制器执行。尽管近期已有实证进展，但这些系统缺乏统一的设计原则：现有的Hi-VLA系统在如何选择和连接规划器与控制器、两者之间的切换机制、以及规划器中观测和记忆的表示方式等方面存在差异。本文对机器人操作的Hi-VLA设计进行了系统性研究。我们将代表性的Hi-VLA智能体统一到选项式控制框架下，并在短时程、长时程和推理密集型任务上对核心设计选择进行基准测试。我们的分析提炼出了构建Hi-VLA系统的实用原则，展示了模型选择与接口机制如何共同决定系统性能。

    arXiv:2606.10267v2 Announce Type: replace-cross  Abstract: Hierarchical vision-language-action (Hi-VLA) systems have emerged as a promising paradigm for complex robot manipulation, by using high-level VLM planners to decompose tasks into language subgoals executed by low-level VLA controllers. Despite recent empirical progress, there is a lack of unified design principles for these systems: existing Hi-VLA systems differ in how they choose and connect planners, controllers, mechanisms to switch between the two, and how observations and memory are represented in the planner. In this paper, we present a systematic study of Hi-VLA design for robot manipulation. We unify representative Hi-VLA agents under an options-style control framework and benchmark core design choices across short-horizon, long-horizon, and reasoning-intensive tasks. Our analysis distills practical principles for building Hi-VLA systems, showing how model choices and interface mechanisms jointly shape performance. App
    
[^573]: 具有层次结构与空间局部性先验的神经场标记化

    Neural Field Tokenizations with Hierarchy and Spatial Locality Priors

    [https://arxiv.org/abs/2606.08204](https://arxiv.org/abs/2606.08204)

    本文提出LH-NeF框架，通过注入局部性与层次性先验，用前馈编码取代内存密集的逐样本元学习，实现了模态无关且可扩展的神经场标记化表示学习。

    

    神经场将数据参数化为从坐标到值的函数，为跨模态的表示学习提供了统一的框架。现有方法主要以逐样本元学习为主导，但由于内存密集型的内循环优化，其扩展性较差。作为自然替代方案的前馈编码通常会引入特定于模态的假设，牺牲了使神经场学习具有吸引力的通用性。我们认为，局部性和层次性是学习场表示的有用先验，可以在不损害模态无关性的情况下注入这些先验。我们提出了LH-NeF，一个用于学习连续信号通用标记化表示的框架。一个保持局部性的层次化编码器将原始的坐标-值场观测映射为结构化标记，并在训练过程中从这些标记重建场。通过用单次前向传播取代元学习的内循环，LH-NeF实现了高效的通用神经场表示学习。

    arXiv:2606.08204v2 Announce Type: replace  Abstract: Neural fields parameterize data as functions from coordinates to values, providing a unified framework for representation learning across modalities. Existing approaches are dominated by per-sample meta-learning, which scales poorly due to memory-intensive inner-loop optimization. The natural alternative -- feed-forward encoding -- typically introduces modality-specific assumptions, sacrificing the generality that makes learning with neural fields attractive. We argue that locality and hierarchy are useful priors for learning field representations that can be injected without compromising modality-agnosticism. We propose LH-NeF, a framework to learn general-purpose tokenized representations of continuous signals. A locality-preserving hierarchical encoder maps raw coordinate-value field observations to structured tokens, from which the field is reconstructed during training. By replacing meta-learning's inner loop with a single forwa
    
[^574]: 你能做到多小？面向金融交易商户信息提取的270M-8B模型LoRA微调研究

    How Small Can You Go? LoRA Fine-Tuning 270M-8B Models for Merchant Information Extraction in Financial Transactions

    [https://arxiv.org/abs/2606.08051](https://arxiv.org/abs/2606.08051)

    本研究通过系统评估多种小型模型的LoRA微调，发现Qwen~3.5~4B在保持与8B模型相当性能的同时，显著降低资源消耗，实现了高效商户信息提取。

    

    商户信息提取在生产规模上将嘈杂的金融交易描述转换为结构化字段。我们部署的LoRA微调LLaMA~3.1-8B模型达到了96.95%的F1分数，但其内存和吞吐量促使我们寻找更小的替代方案。我们评估了23次保留的微调运行以及一个单独训练的生产参考模型，涵盖Gemma~3（270M--4B）、Qwen~3.5（0.8B--4B）、Aya~3.35B和LLaMA~3.1-8B，跨越不同的LoRA秩、提示、训练模板和服务环境。秩为8的LLaMA微调达到了96.75%的F1分数，仅比秩为32的生产参考低0.20个百分点。使用仅JSON提示的Qwen~3.5~4B达到了96.60%的F1分数和91.67%的严格记录级精确匹配，其逆吞吐时间估计比秩为8的8B模型低3.8倍。Qwen~3.5~0.8B达到了94.75%的F1分数，且Qwen Think和Nothink模板之间的差异小于0.004 F1。在14个Databricks端点中，平均F1变化与本地评估相比略有差异。

    arXiv:2606.08051v2 Announce Type: replace  Abstract: Merchant information extraction turns noisy financial transaction descriptors into structured fields at production scale. Our deployed LoRA-fine-tuned LLaMA~3.1-8B reaches 96.95\% F1, but its memory and throughput motivate smaller replacements. We evaluate 23 retained fine-tuning runs plus a separately trained production reference, spanning Gemma~3 (270M--4B), Qwen~3.5 (0.8B--4B), Aya~3.35B, and LLaMA~3.1-8B across LoRA ranks, prompts, training templates, and serving environments. A rank-8 LLaMA fine-tune reaches 96.75\% F1, only 0.20 points below the rank-32 production reference. Qwen~3.5~4B with JSON-Only prompting reaches 96.60\% F1 and strict record-level exact match of 91.67\%, with a $3.8\times$ lower inverse-throughput time estimate than the rank-8 8B model. Qwen~3.5~0.8B reaches 94.75\% F1, and Qwen Think and Nothink templates differ by less than 0.004 F1. Across 14 Databricks endpoints, mean F1 change from local evaluation i
    
[^575]: 基于可微分代理模型的摊销神经优化用于版图前信号完整性设计空间探索

    Amortized Neural Optimization for Pre-Layout Signal Integrity Design Space Exploration using Differentiable Surrogates

    [https://arxiv.org/abs/2606.07463](https://arxiv.org/abs/2606.07463)

    本文提出摊销神经优化（ANO）方法，利用完全可微分的神经网络代理模型提取梯度信息，离线训练全局优化策略，从而避免迭代黑盒推理并摊销计算成本，大幅加速版图前信号完整性设计空间探索。

    

    高速信号完整性（SI）分析的版图前设计空间探索（DSE）通常受到现代电子设计自动化（EDA）工作流程中仿真计算成本以及迭代优化算法的限制。虽然机器学习代理模型可以加速仿真步骤，但优化设计仍需要使用迭代的黑盒搜索方法。这种迭代特性扩展性差，导致多工艺角扫描的计算成本高昂。作为解决方案，本文提出用于版图前信号完整性设计的摊销神经优化（ANO）。ANO避免了迭代黑盒推理，利用完全可微分的神经网络代理模型，从中提取梯度信息来训练全局优化策略。该方法并非在推理时重复求解优化问题，而是离线学习整个过程，从而摊销计算成本。一旦ANO策略……

    arXiv:2606.07463v2 Announce Type: replace-cross  Abstract: Pre-layout design space exploration (DSE) for high-speed signal integrity (SI) analysis is often limited by the computational cost of simulations and iterative optimization algorithms within modern electronic design automation (EDA) workflows. While machine learning surrogate models accelerate the simulation step, optimizing designs still requires utilizing iterative black-box search methods. This iterative nature scales poorly and makes multi-corner sweeps computationally expensive. As a solution, this paper proposes amortized neural optimization (ANO) for pre-layout SI design. ANO avoids iterative black-box inference and utilizes a fully differentiable neural network surrogate model from which it extracts gradient information to train a global optimization policy. This does not solve the optimization problem repeatedly at inference, but learns the process offline, which amortizes the computational cost. Once the ANO policy is
    
[^576]: 车联网上的联邦基础模型

    Federated Foundation Models over Vehicular Networks

    [https://arxiv.org/abs/2606.06786](https://arxiv.org/abs/2606.06786)

    本文首次提出了将多模态多任务联邦基础模型集成到车联网中的前瞻性愿景，系统阐述了其训练/微调原理、代表性应用场景、车联网环境中的关键部署挑战以及未来研究方向。

    

    本文提出了将新兴的多模态多任务联邦基础模型（M3T FedFMs）集成到车联网中的前瞻性愿景，旨在将多模态多任务基础模型（M3T FMs）的强大表达能力与联邦学习（FL）的隐私保护和分布式学习能力相统一。鉴于该研究方向在很大程度上尚未被充分探索，我们首先介绍了M3T FedFMs的基本训练/微调原理。随后，我们讨论了它们在车联网中的一系列代表性用例，展示了M3T FedFMs在实现下一代车联网智能方面的巨大潜力。接着，我们识别了车联网环境中固有的、制约M3T FedFMs实际部署的关键约束条件，并提出了一系列前瞻性的研究方向来应对这些挑战。此外，通过一个案例研究……（原文截断）

    arXiv:2606.06786v2 Announce Type: replace  Abstract: This paper presents a forward-looking vision for integrating the emerging multi-modal multi-task federated foundation models (M3T FedFMs) into vehicular networks, with the goal of unifying the expressive power of multi-modal multi-task foundation models (M3T FMs) with the privacy-preserving and distributed learning capabilities of federated learning (FL). Given the largely underexplored nature of this research direction, we first introduce the fundamental training/fine-tuning principles of M3T FedFMs. We then discuss a range of their representative use cases in vehicular networks, illustrating the significant potential of M3T FedFMs to enable next-generation vehicular intelligence. Afterwards, we identify key constraints inherent to vehicular environments that challenge the practical deployment of M3T FedFMs, and articulate a set of forward-looking research directions to address these challenges. Furthermore, through a case study con
    
[^577]: HAARES：面向深度Transformer的半分裂残差基路由

    HAARES Half-Split Residual Basis Routing for Deep Transformers

    [https://arxiv.org/abs/2606.06564](https://arxiv.org/abs/2606.06564)

    HAARES通过在块级残差路由的基础上增加一个由前后半段残差更新差值构成的半分裂细节基，在不引入密集子层级路由开销的前提下，为深度Transformer（尤其是48层模型）的残差聚合带来了一致的性能提升。

    

    块级残差路由通过在块摘要上进行路由，使学习到的残差聚合变得实用，但每个摘要都将一个有序的注意力与MLP更新序列压缩为单个累积向量。我们提出了HAARES，一个轻量级的残差基路由器，它保留了累积的块源信息，并额外增加了一个半分裂细节基，该细节基计算为前半部分与后半部分残差更新之间的差值。该细节基经过RMS匹配并在线更新，在不进行密集子层级路由的情况下，暴露出粗糙的块内轨迹信息。在OpenWebText、跨域字符级基准测试以及BPE分词的OpenWebText上，实验结果呈现出深度依赖的模式：在浅层模型中增益较小或效果参差不齐，而在48层模型中最为可靠。在201M参数的48层设置中，HAARES在全部三个随机种子上均优于Block AttnRes，而453M参数的双种子初步实验也显示出相同的趋势。消融实验排除……

    arXiv:2606.06564v3 Announce Type: replace-cross  Abstract: Block-level residual routing makes learned residual aggregation practical by routing over block summaries, but each summary compresses an ordered sequence of attention and MLP updates into one cumulative vector. We propose \method{}, a lightweight residual basis router that keeps the cumulative block source and adds one half-split detail basis, computed as the difference between first-half and second-half residual updates. The detail basis is RMS-matched and updated online, exposing coarse intra-block trajectory information without dense sublayer-level routing. Across OpenWebText, cross-domain character-level benchmarks, and BPE-tokenized OpenWebText, the empirical pattern is depth-dependent: gains are small or mixed at shallow depth and most reliable in 48-layer models. In the 201M 48-layer setting, \method{} improves over Block AttnRes across all three seeds, while a 453M two-seed probe shows the same direction. Ablations rul
    
[^578]: 上下文多示例学习

    In-Context Multiple Instance Learning

    [https://arxiv.org/abs/2606.06458](https://arxiv.org/abs/2606.06458)

    该论文提出在合成数据上以Perceiver风格架构预训练上下文学习器，使模型仅需少量带标签实例包、通过一次前向传播且无需梯度更新即可解决新的多示例学习任务。

    

    多示例学习（MIL）处理的是监督信息仅存在于实例包（bag）层面的问题，已成功应用于从计算病理学到卫星图像等多个领域。然而，现有算法在低标签场景中表现不佳，而这种低标签场景正是许多现实应用的典型特征：灵活的模型容易过拟合，而僵化的模型又无法适应手头的任务。我们证明，使用Perceiver风格架构在合成数据上预训练一个上下文学习器，可以得到一个仅需少量带标签的实例包就能解决新任务的模型。在推理阶段，分类只需一次前向传播即可完成，且无需任何梯度更新。我们提出并研究了多种针对包结构数据的合成数据生成器，发现它们能够捕捉互补的归纳偏置。在这些生成器的混合数据上预训练的模型继承了各自在不同任务上的优势，并取得了最佳的平

    arXiv:2606.06458v2 Announce Type: replace-cross  Abstract: Multiple Instance Learning (MIL) addresses problems where supervision is available at the level of bags of instances and has been successfully applied in fields ranging from computational pathology to satellite imagery. Nevertheless, existing algorithms struggle in the low-label regime that characterizes many real-world applications. Flexible models overfit and rigid ones fail to adapt to the task at hand. We show that pretraining an in-context learner with a Perceiver-style architecture on synthetic data yields a model that can solve new tasks from a handful of labeled bags. At inference time, classification happens in a single forward pass and requires no gradient updates. We propose and investigate different synthetic data generators for bag-structured data and find that they capture complementary inductive biases. A model pretrained on a mixture of these generators inherits their per-task strengths and achieves the best ave
    
[^579]: 反应通量匹配：稀有事件的机理发现与自适应采样

    Reactive Flux Matching: Mechanism Discovery and Adaptive Sampling of Rare Events

    [https://arxiv.org/abs/2606.06295](https://arxiv.org/abs/2606.06295)

    提出通量匹配框架，直接从反应轨迹数据中学习流速度与标量势两个互补对象，分别用于揭示主导反应路径和构建数据驱动的反应坐标，并利用其水平集实现稀有事件的自适应增强采样。

    

    路径采样方法能够生成连接亚稳态的反应轨迹系综，但从这些数据中提取机理洞察仍然并非易事。我们提出了通量匹配这一框架，它直接从反应轨迹数据中学习两个互补的对象：其一是流速度 u(z)，其流线可描绘出主导的反应路径；其二是标量势 h(z)，它通过对反应流的加权亥姆霍兹-霍奇分解获得，可作为数据驱动的反应坐标。两者均在反应路径系综中最小化二次泛函，类似于生成建模中的流匹配损失，且无需了解底层动力学或平稳分布。与基于倾向函数的方法不同，u 和 h 在投影到非马尔可夫集合变量后仍保持良定义，并且它们的水平集进而为改进稀有事件的增强采样提供了自适应界面。

    arXiv:2606.06295v2 Announce Type: replace  Abstract: Path sampling methods generate ensembles of reactive trajectories connecting metastable states, but extracting mechanistic insight from these data remains nontrivial. We introduce Flux Matching, a framework that learns two complementary objects directly from reactive trajectory data: a current velocity $u(z)$, whose streamlines trace the dominant reaction pathways, and a scalar potential $h(z)$, obtained from a weighted Helmholtz-Hodge decomposition of the reactive current, that serves as a data-driven reaction coordinate. Both minimize quadratic functionals over the reactive path ensemble, analogous to the flow matching loss in generative modeling, and require no knowledge of the underlying dynamics or stationary distribution. Unlike committor-based methods, $u$ and $h$ remain well-defined under projection onto non-Markovian collective variables, and their level sets in turn provide adaptive interfaces for improved sampling with enh
    
[^580]: 当新生成器到来时：基于岭回归特征迁移的终身机器生成文本溯源

    When New Generators Arrive: Lifelong Machine-Generated Text Attribution via Ridge Feature Transfer

    [https://arxiv.org/abs/2606.05626](https://arxiv.org/abs/2606.05626)

    提出RidgeFT框架，通过在首次遇到每个生成器时存储紧凑的类级充分统计量，并以冻结编码器后无需样本重放的闭式解析更新方式，解决了终身机器生成文本溯源中适应新生成器与保留旧生成器能力的平衡难题。

    

    机器生成文本（MGT）溯源旨在识别生成给定文本的具体生成器，从而为模型问责和滥用调查提供细粒度的证据。随着新的大语言模型不断涌现，溯源模型必须在保持识别先前已见生成器能力的同时，持续纳入新生成器。先前的研究表明，这种终身MGT溯源设置极具挑战性，现有方法往往难以在适应新类别与保留旧类别之间实现稳定的平衡。为解决这一问题，我们提出了RidgeFT，一个不依赖样本重放的轻量级解析式更新框架。RidgeFT在初始生成器集合上训练一个任务感知编码器，在首次观察到每个生成器类别时存储紧凑的类级充分统计量，随后冻结编码器以进行无需重放的闭式更新，进而……

    arXiv:2606.05626v2 Announce Type: replace-cross  Abstract: Machine-generated text (MGT) attribution aims to identify the specific generator responsible for a given text, thereby providing fine-grained evidence for model accountability and misuse investigation. As new large language models continue to emerge, attribution models must continuously incorporate new generators while preserving their ability to recognize previously seen ones. Prior works have shown that this lifelong MGT attribution setting is challenging, and existing methods often struggle to achieve a stable balance between adapting to new classes and retaining old ones. To address this issue, we propose RidgeFT, a lightweight analytic update framework that does not rely on exemplar replay. RidgeFT trains a task-aware encoder on the initial generator set, stores compact class-wise sufficient statistics when each generator class is first observed, and then freezes the encoder for replay-free closed-form updates. It then sup
    
[^581]: 面向GRPO的Rollout级优势优先经验回放

    Rollout-Level Advantage-Prioritized Experience Replay for GRPO

    [https://arxiv.org/abs/2606.04560](https://arxiv.org/abs/2606.04560)

    该论文提出一种面向GRPO的rollout级优势优先经验回放方法，通过按年龄淘汰限制数据过时、以新鲜在策略数据锚定训练稳定性，并优先回收高优势样本，从而提升推理大语言模型强化学习后训练的样本效率。

    

    结合GRPO的基于可验证奖励的强化学习是对推理型大语言模型进行后训练的标准方法，但其样本效率仍然低下：每次采样的rollout仅用于单次梯度更新后即被丢弃。朴素的经验回放并不适合该场景，因为LLM策略在每次梯度步中漂移迅速，存储的rollout因此会变得过时，并可能破坏训练稳定性。我们提出了一种面向GRPO的rollout级别回放缓冲区，它存储和采样的对象是单个rollout而非整个组。该缓冲区通过按年龄淘汰来限制数据过时程度，任何超过tau_max个训练步的rollout都会被移除。缓冲区还通过“新鲜锚定”的组合方式保留在策略数据：每个批次保留其新鲜的在策略rollout，再拼接从缓冲区中单独抽取的回放rollout。我们依据每个rollout的优势幅度对回放进行优先级排序，并回收优势较大的单个rollout。在三个Qwen3-B（原文截断）……的实验中……

    arXiv:2606.04560v3 Announce Type: replace-cross  Abstract: Reinforcement learning from verifiable rewards with GRPO is a standard approach for post-training reasoning LLMs. It remains sample inefficient. Each rollout is used for a single gradient update and then discarded. Naive replay is not well suited in this setting because LLM policies drift quickly per gradient step. Stored rollouts therefore become stale and can destabilize training. We propose a rollout-level replay buffer for GRPO that stores and samples individual rollouts rather than whole groups. The buffer bounds staleness through age eviction. Any rollout older than tau_max training steps is removed. The buffer also preserves on-policy data via fresh-anchored composition. Each batch keeps its fresh on-policy rollouts and then concatenates replay rollouts drawn separately from the buffer. We prioritize replay by per-rollout advantage magnitude and recycle individual rollouts whose advantages are large. Across three Qwen3-B
    
[^582]: SymTRELLIS：面向三维生成的对称性约束体素潜变量

    SymTRELLIS: Symmetry-Enforced Voxel Latents for 3D Generation

    [https://arxiv.org/abs/2606.04108](https://arxiv.org/abs/2606.04108)

    SymTRELLIS提出了一种无需重新训练基础模型的方法，通过将空间变换近似为体素潜变量上的学习线性算子，并在每个ODE步骤中对所有对称等价变换的流速度取平均，从而在基于流的3D生成中强制执行任意有限点群对称性。

    

    单视角3D生成模型已经取得了令人印象深刻的视觉质量，但它们的设计初衷并非满足结构或功能性要求，因此在实际应用中往往达不到这些要求。对称性就是这样一项要求：对称性的违背，即使是微小的偏差，也可能使模型在物理上无法使用。我们提出了SymTRELLIS，这是一种在TRELLIS基于流的3D生成过程中强制执行任意有限点群对称性（旋转、反射和多面体对称）的方法，且无需重新训练底层的VAE或流模型。我们的核心思想是将空间变换在潜在空间中的作用近似为体素潜变量上的一个学习线性算子，具体实现为一个在通用非对称3D数据上训练的轻量级空间变换潜变量映射器。在生成阶段，我们通过在每个ODE步骤中对所有对称等价变换的预测流速度进行平均来强制执行对称性，我们将这一过程称为速度对称化。

    arXiv:2606.04108v2 Announce Type: replace-cross  Abstract: Single-view 3D generative models have achieved impressive visual quality, yet they are not designed to satisfy structural or functional requirements, and in practice, often fall short. Symmetry is one such requirement: violations, even subtle ones, on symmetry can render a model physically unusable. We present SymTRELLIS, a method that enforces arbitrary finite point group symmetries (rotational, reflectional, and polyhedral) during the flow-based 3D generation of TRELLIS.2, without retraining the underlying VAE or flow model. Our key idea is to approximate the latent-space action of spatial transformations as a learned linear operator on voxel latents, implemented as a lightweight spatial-transform latent mapper trained on generic, non-symmetric 3D data. At generation time, we enforce symmetry by averaging predicted flow velocities across all symmetry-equivalent transformations at each ODE step, a process we call velocity symm
    
[^583]: 面向商用CPU的脉冲感知INT8执行方案：用于脉冲语言模型

    Spike-Aware INT8 Execution for Spiking Language Models on Commodity CPUs

    [https://arxiv.org/abs/2606.03026](https://arxiv.org/abs/2606.03026)

    该论文针对脉冲语言模型提出了一种脉冲感知的INT8量化执行策略，在商用CPU上实现了相比FP32快2倍以上的推理速度，同时将权重存储减少约68%。

    

    二值脉冲激活使语言模型运行时只需读取活跃的权重列，并用权重求和替代乘法运算。我们使用C++为874M参数的脉冲门控语言模型实现了这一执行策略。稀疏投影采用列主序INT8权重、整数累加以及每个输出通道一次的缩放应用；稠密投影则保留行主序访问和FP32激活。在使用早期检查点的单线程对比中，INT8达到23.31 tokens/s，而FP32仅为9.82 tokens/s，同时将权重存储从3355.2 MiB降至1087.4 MiB。在稠密投影上使用INT4的变体可再节省17.4%的存储空间，但会使解码吞吐量降低46.6%。在AMD Ryzen 7 5800X上，最终的INT8检查点在单线程下达到22.63 tokens/s，四线程下达到47.90 tokens/s；512 token的预填充在八线程下达到94.68 tokens/s。另一项独立的ARM输出头案例研究记录了更高的截断解码窗口（原文此处似乎截断）。

    arXiv:2606.03026v2 Announce Type: replace-cross  Abstract: Binary spike activations allow a language-model runtime to read only active weight columns and replace multiplications by weight sums. We implement this execution strategy in C++ for an 874M-parameter spike-gated language model. Sparse projections use column-major INT8 weights, integer accumulation, and one scale application per output channel; dense projections retain row-major access and FP32 activations. In a single-thread comparison using an early checkpoint, INT8 achieves 23.31 tokens/s versus 9.82 for FP32, while reducing weight storage from 3355.2 to 1087.4 MiB. A variant using INT4 on dense projections saves a further 17.4% of storage but reduces decode throughput by 46.6%. On an AMD Ryzen 7 5800X, the final INT8 checkpoint achieves 22.63 tokens/s on one thread and 47.90 on four threads; 512-token prefill reaches 94.68 tokens/s on eight threads. A separate ARM output-head case study records higher trimmed decode-window 
    
[^584]: LayerRoute：基于LoRA微调的面向智能体语言模型的输入条件自适应层跳过方法

    LayerRoute: Input-Conditioned Adaptive Layer Skipping via LoRA Fine-Tuning for Agentic Language Models

    [https://arxiv.org/abs/2606.01838](https://arxiv.org/abs/2606.01838)

    LayerRoute通过为Qwen2.5-0.5B-Instruct的每个transformer层配备轻量级二进制门控路由器和LoRA适配器，学习根据输入特征动态跳过不必要的层，使智能体语言模型在结构化工具调用和开放式推理这两类异构步骤间实现自适应计算分配。

    

    智能体语言模型系统在两种结构截然不同的步骤类型之间交替：结构化工具调用（简短、确定性、低困惑度）和开放式规划/推理步骤（冗长、复杂、高困惑度）。尽管存在这种异质性，当前的推理系统却对每个步骤都施加相同的计算量。我们提出了LayerRoute，这是一种轻量级适配器，能够学会针对每个输入选择性地跳过transformer块。LayerRoute为Qwen2.5-0.5B-Instruct中的24个transformer块分别增强了：(1) 逐层路由器（约897个参数，Linear(896,1)），通过直通估计器输出硬二进制门控信号；(2) 位于Q/K/V/O注意力投影上的LoRA适配器（秩为8，约108万个参数）。骨干网络权重保持冻结。通过在智能体数据（Hermes、Glaive、GSM8K、Turing）上进行单次端到端训练，并辅以门控正则化项，迫使系统发现哪些块是可以跳过的……（摘要在此处被截断）

    arXiv:2606.01838v2 Announce Type: replace-cross  Abstract: Agentic language model systems alternate between two structurally distinct step types: structured tool calls (short, deterministic, low perplexity) and open-ended planning/reasoning steps (long, complex, high perplexity). Despite this heterogeneity, current inference systems apply identical compute to every step. We introduce LayerRoute, a lightweight adapter that learns to selectively skip transformer blocks on a per-input basis. LayerRoute augments each of the 24 transformer blocks in Qwen2.5-0.5B-Instruct with: (1) a per-layer router (~897 parameters, Linear(896,1)) that outputs a hard binary gate via the straight-through estimator, and (2) LoRA adapters (rank 8, ~1.08M parameters) on the Q/K/V/O attention projections. The backbone weights remain frozen. A single end-to-end training pass on agentic data (Hermes, Glaive, GSM8K, Turing) with a gate regularisation term forces the system to discover which blocks are skippable pe
    
[^585]: 隐性学习是一种LoRA伪影

    Subliminal Learning is a LoRA Artifact

    [https://arxiv.org/abs/2606.00831](https://arxiv.org/abs/2606.00831)

    本文揭示了隐性学习现象——即模型通过无害数据向其他模型传递行为特征——实际上是LoRA微调的伪影，其传递强度与LoRA秩呈倒U型关系，在全量微调下消失，且高度依赖于微调和评估时的上下文。

    

    隐性学习是一种语言模型能够通过看似无害的数据向其他模型传递行为特征的现象。在隐性学习中，具有某种行为特征（例如对猫的痴迷）的教师模型，可以将这种对猫的痴迷传递给仅通过教师模型生成的数字序列进行微调的学生模型。在本文中，我们提出问题：这种意想不到的行为传递是如何发生的？我们证明隐性学习是一种LoRA伪影。当隐性学习发生时，行为传递与LoRA秩之间呈倒U型关系；在全量微调下该现象会消失。我们还证明隐性学习高度依赖于微调和评估期间所见的上下文。例如，在微调期间使用默认系统提示词（"You are Qwen, created by Alibaba Cloud. You are a helpful assistant."）的Qwen模型在生成时不会表现出隐性学习……

    arXiv:2606.00831v2 Announce Type: replace  Abstract: Subliminal learning is a phenomenon where language models can transmit behavioral traits to other models through seemingly innocuous data (Cloud et al., 2025). In subliminal learning, a teacher model with a behavioral trait (e.g. obsession with cats) can transmit this cat obsession to a student model finetuned only on numerical sequences generated by the teacher. In this paper, we ask: how does this unexpected behavioral transmission occur? We show that subliminal learning is a LoRA artifact. When subliminal learning occurs, transmission has an inverted U-shaped relationship with LoRA rank; it also disappears with full finetuning. We show that subliminal learning is highly dependent on the context seen during finetuning and evaluation. For example, a Qwen model with the default system prompt during finetuning ("You are Qwen, created by Alibaba Cloud. You are a helpful assistant.") does not show subliminal learning during generation w
    
[^586]: 从罗生门理论到PRAXIS：高效的决策树罗生门集

    From Rashomon Theory to PRAXIS: Efficient Decision Tree Rashomon Sets

    [https://arxiv.org/abs/2606.00202](https://arxiv.org/abs/2606.00202)

    PRAXIS算法在运行时间和内存使用上实现了数量级的改进，能够高效近似计算稀疏决策树的罗生门集（近优模型集合），并且通常可以恢复几乎完整的罗生门集，从而使真实世界数据集的罗生门集建模变得可扩展。

    

    标准机器学习流程通常会产生许多接近最优的模型。这些“罗生门集”为不确定性感知的稳健决策带来了诸多挑战与机遇。它们允许用户融入领域知识和偏好——这些内容往往难以直接在目标函数中指定——并且能够量化针对给定训练数据集和目标函数的有效模型之间的多样性。然而，即使对于简单、可解释的模型类别（如稀疏决策树），罗生门集的计算仍然需要巨大的内存和运行时间资源。我们提出了PRAXIS，这是一种近似计算罗生门集的算法，在运行时间和内存使用方面实现了数量级的改进。我们验证了PRAXIS通常能够恢复几乎完整的罗生门集。PRAXIS使研究人员和从业者能够可扩展地对真实世界数据集的罗生门集进行建模。PRAXIS的代码已公开。

    arXiv:2606.00202v2 Announce Type: replace-cross  Abstract: Standard machine learning pipelines often admit many near-optimal models. These "Rashomon sets" pose a range of challenges and opportunities for uncertainty-aware, robust decision making. They allow users to incorporate domain knowledge and preferences that would otherwise be difficult to specify directly in an objective, and they quantify diversity among valid models for a given training dataset and objective function. However, computation of Rashomon sets, even for simple, interpretable model classes such as sparse decision trees, continues to require immense memory and runtime resources. We present PRAXIS, an algorithm to approximate this Rashomon set with orders of magnitude improvement in runtime and memory usage. We validate that PRAXIS regularly recovers almost all of the full Rashomon set. PRAXIS allows researchers and practitioners to scalably model the Rashomon set for real-world datasets. Code for PRAXIS is available
    
[^587]: 学习构建实用的智能体系统

    Learning to Construct Practical Agentic Systems

    [https://arxiv.org/abs/2606.00189](https://arxiv.org/abs/2606.00189)

    提出了一种基于“伪工具”实现模块化的实用智能体系统设计框架，并证明手工构建的固定工作流比动态规划的工作流成本更低、准确性更高。

    

    基于大语言模型的智能体系统的自动化设计与优化能够构建出复杂的系统，相较于现成的智能体模式可显著提升结果质量。然而，对实际部署的智能体系统的研究表明，生产系统更加关注简洁性、可控性以及推理成本的可预测性等问题。在本文中，我们提出了设计与优化实用智能体系统的原则性方法。我们描述了一个智能体框架，通过定义在受限上下文上递归调用大语言模型的“伪工具”，使设计者能够在智能体系统中实现模块化。利用该框架，我们为多样化的任务手工构建了智能体，并表明与动态规划的工作流相比，手工构建的固定工作流通常成本更低且更准确。随后，我们针对该框架所需的智能体组件（即伪工具）提出了新颖的学习方法。

    arXiv:2606.00189v2 Announce Type: replace-cross  Abstract: Automated design and optimization of agentic LLM-based systems leads to sophisticated systems that substantially improve result quality over off-the-shelf agentic patterns. However, studies of fielded agentic systems show that production systems focus much more on issues such as simplicity, controllability, and predictability of inference costs. In this paper we propose principled approaches to designing and optimizing practical agentic systems. We describe an agent framework that enables designers to enforce modularity in agentic systems, by defining "pseudo-tools" that call LLMs recursively on a restricted context. Using this framework we hand-engineer agents for a diverse set of tasks, and show that relative to dynamically-planned workflows, hand-constructed fixed workflows are generally cheaper and more accurate. We then propose novel learning methods for the agentic components required by this framework, namely pseudo-tool
    
[^588]: LongDS-Bench：论长时程智能体数据分析的失败

    LongDS-Bench: On the Failure of Long-Horizon Agentic Data Analysis

    [https://arxiv.org/abs/2605.30434](https://arxiv.org/abs/2605.30434)

    提出LongDS基准，基于真实Kaggle笔记本构建68个长时程多轮数据分析任务，揭示当前最先进模型在维护和演变分析状态方面存在严重缺陷，最佳模型平均准确率仅48.45%，且长时程错误占失败案例的52%–69%。

    

    现实世界的数据分析本质上是迭代式的，然而现有的基准测试大多评估孤立的或短交互的任务，未能检验智能体在长时程中跟踪不断演变的分析上下文的能力。我们提出了LongDS，这是一个面向长时程、多轮数据分析的基准，要求智能体维护、更新、恢复和组合不断演变的分析状态。LongDS包含从真实Kaggle笔记本构建的68个任务，涵盖地球科学、商业和教育等六个领域，共2,225轮。任务围绕状态演变模式（例如反事实扰动、回滚、多状态组合）设计，平均依赖跨度为11.3轮。通过对五个最先进模型的评估，我们发现表现最好的模型平均准确率仅为48.45%，性能从早期到后期轮次下降近47个百分点，长时程错误占失败案例的52%–69%。

    arXiv:2605.30434v2 Announce Type: replace-cross  Abstract: Real-world data analysis is inherently iterative, yet existing benchmarks mostly evaluate isolated or short interactive tasks, leaving agents' ability to track evolving analytical context over long horizons untested. We introduce LongDS, a benchmark for long-horizon, multi-turn data analysis where agents must maintain, update, restore, and compose evolving analytical states. LongDS comprises 68 tasks constructed from real-world Kaggle notebooks, spanning 2,225 turns across six domains including Geoscience, Business, and Education. Tasks are designed around state-evolution patterns (e.g., counterfactual perturbation, rollback, multi-state composition), with an average dependency span of 11.3 turns. Evaluating five state-of-the-art models, we find that the best model reaches only 48.45% average accuracy, performance drops nearly 47 points from early to late turns, and long-horizon errors account for 52%--69% of failures. Further 
    
[^589]: 在错误回答上进行监督微调后激活表示的线性可分性：大语言模型中合成不诚实行为的研究

    Linear Separability of Activation Representations after Supervised Fine-Tuning on Incorrect Responses: A Study of Synthetic Dishonesty in Large Language Models

    [https://arxiv.org/abs/2605.30381](https://arxiv.org/abs/2605.30381)

    该研究通过构建诚实与不诚实的LoRA微调模型变体，证明对大语言模型进行错误回答的监督微调会在其内部激活中留下结构化且可线性恢复的痕迹，线性探针在多数架构的前几层即可实现近乎完美的诚实/不诚实状态区分，并呈现跨数据集的迁移性。

    

    当一个语言模型被微调为系统性地产生错误回答时，这种训练是否会在其内部激活中留下结构化的、可线性恢复的痕迹？我们在受控的模型生物实验环境中研究这个问题，使用了五个参数量从14亿到90亿的transformer架构。对于每个模型，使用相同的问题分布构建“诚实”与“不诚实”的LoRA微调变体，前者使用正确答案，后者使用看似合理但错误的答案。线性探针在五个架构中的四个里，于前几层即达到接近上限的可分性（AUC ≥ 0.9997）。这种可分性在四个模型中能够从TruthfulQA迁移到留出的MMLU学科上，但对Pythia-1.4B的迁移程度明显较弱。六项几何分析揭示了表示结构中的架构二分性。一项对照实验进一步表明，LoRA微调本身就会留下强烈的激活指纹（摘要内容在此处截断）。

    arXiv:2605.30381v2 Announce Type: replace-cross  Abstract: When a language model is fine-tuned to produce systematically incorrect responses, does this training leave a structured, linearly recoverable trace in its internal activations? We study this question in a controlled model-organism setting using five transformer architectures spanning 1.4 to 9 billion parameters. For each model, an "honest" and a "dishonest" LoRA fine-tuned variant are constructed using identical question distributions but correct versus plausible-but-incorrect answers. Linear probes reach near-ceiling separability (AUC >= 0.9997) within the first few layers in four of five architectures. This separability transfers from TruthfulQA to held-out MMLU subjects for four models, but substantially less so for Pythia-1.4B. Six geometric analyses reveal an architectural dichotomy in representation structure. A control experiment further shows that LoRA fine-tuning itself leaves a strong activation fingerprint that is l
    
[^590]: 基于低秩进化策略的脉冲神经网络无梯度训练

    Gradient-Free Training of Spiking Neural Networks via Low-Rank Evolution Strategies

    [https://arxiv.org/abs/2605.30361](https://arxiv.org/abs/2605.30361)

    该论文提出EGGROLL方法，通过进化策略扰动的低秩分解实现脉冲神经网络的无梯度训练，将每代内存开销从O(mn)降至O(r(m+n))，在N-MNIST上达到79.21%准确率且训练时间减少2.23倍。

    

    脉冲神经网络（SNN）在神经形态硬件上具有显著的能效优势，但由于离散的脉冲发放阈值不可微，其训练仍然面临挑战。代理梯度方法通过近似导数来规避这一问题，但它们依赖反向传播基础设施，这与片上学习不兼容。进化策略（ES）是一种天然的无梯度替代方案，但其计算成本随参数数量增长，使其难以应用于大型权重矩阵。我们提出了一种使用EGGROLL训练SNN的方法，EGGROLL是ES扰动的低秩分解，将每代内存开销从 $\mathcal{O}(mn)$ 降低至 $\mathcal{O}(r(m{+}n))$。将EGGROLL与泄漏积分发放（LIF）SNN结合并在N-MNIST数据集上验证，我们证明无梯度训练达到了79.21%的测试准确率，同时每代墙钟时间相对于传统方法减少了2.23倍。

    arXiv:2605.30361v2 Announce Type: replace-cross  Abstract: Spiking Neural Networks (SNNs) offer compelling energy efficiency on neuromorphic hardware, yet their training remains challenging because the discrete spike threshold is non-differentiable. Surrogate-gradient methods sidestep this by approximating the derivative, but they impose backpropagation infrastructure that is incompatible with on-chip learning. Evolution Strategies (\es) are a natural gradient-free alternative, yet their computational cost scales with the number of parameters, making them impractical for large weight matrices.   We present a method for training SNNs using EGGROLL, a low-rank factorisation of ES perturbations that reduces per-generation memory from $\mathcal{O}(mn)$ to $\mathcal{O}(r(m{+}n))$. Combining EGGROLL with a Leaky Integrate-and-Fire SNN on N-MNIST, we demonstrate that gradient-free training achieves 79.21% test accuracy while reducing per-generation wall-clock time by 2.23$\times$ relative to 
    
[^591]: 《生成式人工智能基础小册子：直观的数学入门指南》

    The Little Book of Generative AI Foundations: An Intuitive Mathematical Primer

    [https://arxiv.org/abs/2605.29713](https://arxiv.org/abs/2605.29713)

    本书以推导为导向，系统串联了从PCA、变分自编码器到扩散模型、GAN和能量模型等主要生成式模型家族的数学基础，为读者构建了一条连贯的生成式建模学习路径。

    

    本书为现代生成式人工智能的数学基础提供了一个紧凑的、以推导为导向的入门介绍。本书并非综述所有最新的架构或实现细节，而是构建了一条连贯的学习路径，将生成式模型主要家族的核心思想串联起来，涵盖从PCA、概率PCA、变分自编码器和扩散模型，到归一化流、自回归分解、生成对抗网络（GAN）、Wasserstein GAN以及基于能量的模型等内容。本书的目标是在保留理解这些模型如何被推导及其相互关联所需数学实质的前提下，使生成式建模的整体结构更易于掌握。本书旨在为对数学有浓厚兴趣的研究人员、从业者和学生提供一本夯实基础的入门读物。

    arXiv:2605.29713v2 Announce Type: replace-cross  Abstract: This book provides a compact, derivation-oriented introduction to the mathematical foundations of modern generative artificial intelligence. Rather than surveying every recent architecture or implementation detail, it develops a coherent route through the ideas connecting major families of generative models, from PCA, probabilistic PCA, variational autoencoders, and diffusion models to normalising flows, autoregressive factorisations, GANs, Wasserstein GANs, and energy-based models. The aim is to make the structure of generative modelling more accessible without removing the mathematical substance needed to understand how these models are derived and related. The book is intended as a foundation-building primer for mathematically curious researchers, practitioners, and students.
    
[^592]: 晚胜于早：通过基于本体的抽取后校正构建神经-符号知识图谱

    Better Later Than Sooner: Neuro-Symbolic Knowledge Graph Construction via Ontology-grounded Post-extraction Correction

    [https://arxiv.org/abs/2605.29168](https://arxiv.org/abs/2605.29168)

    该论文提出一种神经-符号知识图谱构建框架，在抽取完成后依据常识本体约束对大模型抽取的事实进行后验校正，从而解决抽取事实违反本体约束的一致性问题。

    

    问答（QA）是人工智能中的一项核心挑战，尤其是针对需要跨文档多跳推理，或聚合、穷举列举等符号操作的复杂查询。检索增强生成（RAG）已成为问答的主流方法，近期的基于图的变体通过组织知识以更好地支持组合性问题，部分解决了这些难题。然而，大多数基于文本的图结构RAG方法仍然缺乏可靠回答复杂问题所需的、可用于符号操作的结构。这促使了基于符号图的方法的兴起，这类方法抽取知识图谱（KG），其中关系是逻辑谓词，从而支持类似SQL的查询。然而，这些流程通常使用大语言模型（LLM）进行知识图谱抽取，这可能引入一致性问题，即抽取的事实可能违反常识性本体约束。我们提出了一个用于基于本体的知识图谱构建的神经-符号框架，将……（原文摘要在此处截断）

    arXiv:2605.29168v2 Announce Type: replace  Abstract: Question answering (QA) is a core challenge in AI, particularly for complex queries requiring multi-hop reasoning across documents, or symbolic operations like aggregation or exhaustive listing. Retrieval-augmented generation has become the dominant approach to QA, with recent graph-based variants addressing part of these issues by organizing knowledge to better support compositional questions. However, most textual graph-based RAG methods still lack the structure needed for symbolic operations useful to answer complex questions reliably. This motivates symbolic graph-based approaches, which extract knowledge graphs (KGs) whose relations are logic predicates that enable SQL-like querying. Yet these pipelines typically use LLMs for KG extraction, which can introduce consistency issues, where extracted facts may violate commonsense ontology constraints. We propose a neuro-symbolic framework for ontology-grounded KG construction combini
    
[^593]: 澄清、拒答还是回答？基于信念增强生成的对话策略化

    Clarify, Abstain or Answer? Strategising in Conversation with Belief-Augmented Generation

    [https://arxiv.org/abs/2605.25831](https://arxiv.org/abs/2605.25831)

    提出信念增强生成（BAG）方法，通过提示词让大语言模型基于自身采样所得的信念状态进行推理，自主决定并执行澄清、拒答或回答的对话策略，从而在歧义问答任务中同时提升回答准确率和策略决策对信念状态的忠实度。

    

    arXiv:2605.25831v2 公告类型：替换交叉 摘要：大型语言模型（LLMs）定义了文本上的一个概率分布，这可以被视为一种不确定性的概率表示：采样K个响应即可得到一个信念状态——即模型认为合理的响应集合。现有工作仅将这种表示用于狭窄的任务，如解码或选择性预测，且通常需要人工干预，无法直接控制生成过程。我们提出了信念增强生成：通过提示词将大语言模型建立在其自身的信念状态之上，让模型对这些K个采样结果进行推理，以决定并执行相应的对话策略：澄清、拒答或回答。在多轮歧义问答（QA）设置中，我们发现大语言模型在默认情况下很少进行澄清或拒答，忽略了关于输入的偶然不确定性和关于事实的认知不确定性。BAG在六个模型上均提升了问答准确率，且相比仅使用提示词的基线方法，其策略决策更忠实于模型自身的信念状态。

    arXiv:2605.25831v2 Announce Type: replace-cross  Abstract: Large language models (LLMs) define a distribution over text, which can be viewed as a probabilistic representation of uncertainty: sampling K responses yields a belief state - responses a model deems plausible. Existing work exploits this representation for narrow tasks like either decoding or selective prediction, and often requires manual interventions, not controlling generation directly. We propose Belief-Augmented Generation (BAG): grounding LLMs in their own belief state via the prompt and letting them reason over these K samples to decide on and execute a conversational strategy: clarify, abstain, or answer. In a multi-turn ambiguous question answering (QA) setting, we find that LLMs by default rarely clarify or abstain, ignoring uncertainty about the input (aleatoric) or facts (epistemic). BAG improves QA accuracy across six models and yields strategy decisions more faithful to their belief state than prompt-only basel
    
[^594]: WMAttack：面向世界模型智能体对抗性评估的自动化攻击搜索

    WMAttack: Automated Attack Search for Adversarial Evaluation of World-Model Agents

    [https://arxiv.org/abs/2605.23220](https://arxiv.org/abs/2605.23220)

    WMAttack提出了一个自动化攻击搜索框架，通过自校正攻击搜索（SCAS）利用多种反馈信号在有限预算内高效且准确地评估世界模型智能体的对抗鲁棒性。

    

    尽管世界模型作为决策智能体的应用日益增多，但由于缺乏专门的自动化评估方法，其对抗鲁棒性仍未得到充分研究。一个关键障碍在于攻击评估必须同时兼顾准确性与效率：较弱的手动调参攻击可能高估鲁棒性，而穷举式的超参数搜索代价过于高昂，因为每个候选方案都需要通过学习到的潜在动力学进行闭环推演。我们提出了 WMAttack，一个用于世界模型智能体对抗性评估的自动化攻击搜索框架。WMAttack 将鲁棒性评估表述为在攻击配置空间（包括攻击族、扰动预算、优化步数、重启次数和分配规则）上的有限预算搜索问题。为提高搜索准确性，自校正攻击搜索（SCAS）利用奖励退化、动作不稳定性以及运行时开销等反馈信号来不断修正攻击提议分布。

    arXiv:2605.23220v2 Announce Type: replace  Abstract: Despite the growing use of world models as decision-making agents, their adversarial robustness remains underexplored due to the lack of dedicated automated evaluation methods. A key obstacle is that attack evaluation must be both accurate and efficient: weak manually tuned attacks can overestimate robustness, while exhaustive hyperparameter search is prohibitively expensive because each candidate requires closed-loop rollouts through learned latent dynamics. We introduce WMAttack, an automated attack-search framework for adversarial evaluation of world-model agents. WMAttack formulates robustness evaluation as a finite-budget search over attack configurations, including attack families, perturbation budgets, optimization steps, restarts, and allocation rules. To improve search accuracy, Self-Correcting Attack Search (SCAS) refines the attack proposal distribution using feedback from reward degradation, action instability, runtime co
    
[^595]: 更多上下文、更大的模型，还是道德知识？政治文本中施瓦茨价值观检测的系统性研究

    More Context, Larger Models, or Moral Knowledge? A Systematic Study of Schwartz Value Detection in Political Texts

    [https://arxiv.org/abs/2605.22641](https://arxiv.org/abs/2605.22641)

    该研究在受控条件下系统分离了上下文长度、道德知识与模型规模三个因素，发现在政治文本的施瓦茨价值观检测任务中，更多上下文并非总是有益——完整文档输入仅使监督式编码器提升2.5-3.8个宏F1点，而对零样本大语言模型帮助并不一致。

    

    在政治文本中检测施瓦茨价值观十分困难：线索往往隐含，相邻价值观之间仅存在细微差别。人们普遍认为有两种补救措施有效：更多的文档上下文文本，以及显式的道德知识。基于知识的检索已在其他基准测试中带来改进，但两者能否迁移到该任务尚未得到验证，因为已发表的系统中同时改变了上下文、知识和模型系列。我们在ValuesML/Touché ValueEval格式的匹配条件下对这些因素进行了分离。输入范围从目标句子到局部窗口再到完整文档。检索要么缺失，要么来自精心整理的道德知识库，并通过早期、晚期或交叉注意力融合的方式注入。研究将监督式DeBERTa-v3编码器与从12B到123B参数的零样本大语言模型进行比较。结果表明，更多的上下文并非普遍有益：完整文档输入使编码器提升了2.5-3.8个宏F1点，但并不能持续带来帮助。

    arXiv:2605.22641v4 Announce Type: replace-cross  Abstract: Detecting Schwartz values in political texts is hard: cues are often implicit, and neighboring values differ by fine distinctions. Two remedies are widely assumed to help: more surrounding document text, and explicit moral knowledge. Knowledge-based retrieval has improved benchmarks elsewhere, but whether either transfers here is untested, because published systems vary context, knowledge, and model family at once. We separate these factors under matched conditions on the ValuesML/Touch\'e ValueEval format. The input ranges from the target sentence to a local window to the full document. Retrieval is either absent or drawn from a curated moral knowledge base, injected by early, late, or cross-attention fusion. Supervised DeBERTa-v3 encoders are compared against zero-shot LLMs from 12B to 123B. More context is not uniformly better: full-document input improves the encoders by 2.5-3.8 macro-F1 points but does not consistently hel
    
[^596]: EmoTrack：面向基于文本的抑郁严重程度估计的临床-语义建模

    EmoTrack: Clinical-Semantic Modeling for Text-Based Depression Severity Estimation

    [https://arxiv.org/abs/2605.22286](https://arxiv.org/abs/2605.22286)

    本文提出EmoTrack框架，通过临床-语义建模将临床特征分数与话语级表示相融合，从心理咨询文本中预测PHQ-8抑郁严重程度总分，解决了临床评估与对话证据之间关联不足的问题。

    

    基于文本的心理咨询为评估抑郁严重程度提供了宝贵的信息来源。我们研究了如何从咨询对话记录中预测八项患者健康问卷（PHQ-8）的总分，PHQ-8是一种抑郁严重程度的自我报告测量工具。基于临床的方法主要依靠大语言模型（LLM）推理来获得结构化的会话级评估，但这些评估提供的信息有限，无法说明哪些话语支持每个评分。基于训练的方法直接在句子或其语义嵌入上训练预测器，能够保留局部对话细节，但必须从有限的有标签对话记录中学习临床结构。将少量临床特征分数与长序列的高维话语表示相集成极具挑战性，因为简单的融合方法可能会过度强调对话证据，并且无法将临床特征与支持性话语相关联。（原文摘要在此处截断）

    arXiv:2605.22286v2 Announce Type: replace-cross  Abstract: Text-based counseling provides a valuable source of information for assessing depression severity. We study prediction of the total score on the eight-item Patient Health Questionnaire (PHQ-8), a self-report measure of depression severity, from counseling transcripts. Clinical-based methods rely mainly on large language model (LLM) inference to obtain structured session-level assessments, but these assessments provide limited information about which utterances support each score. Training-based methods train predictors directly on sentences or their semantic embeddings and preserve local conversational detail, but must learn clinical structure from limited labeled transcripts. Integrating a small set of clinical feature scores with a long sequence of high-dimensional utterance representations is challenging, as simple fusion can overemphasize dialogue evidence and fail to link clinical features to supporting utterances. Combini
    
[^597]: PUID：一种面向隐藏混杂下推荐系统的个性化去混杂框架

    PUID: A Personalized Deconfounding Framework for Recommender Systems under Hidden Confounding

    [https://arxiv.org/abs/2605.21066](https://arxiv.org/abs/2605.21066)

    论文提出PUID个性化去混杂框架，以个性化的敏感性界限取代全局统一界限来处理推荐系统中的隐藏混杂因素，克服了传统方法忽略用户-物品交互异质性的缺陷。

    

    推荐系统通常依赖于观察性的用户-物品交互数据，这类数据由于用户对物品的选择性交互而容易产生选择偏差。尽管逆倾向加权（IPW）和双重稳健估计器等技术能有效应对来自可观察混杂因素的选择偏差，但当存在隐藏混杂时（即存在同时影响用户点击和反馈却不可观察的混杂因素，例如用户薪资），这些方法便变得不可靠。现有依赖随机对照试验（RCT）或全局敏感性界限的方法在实践中存在局限：RCT需要代价高昂的实验数据，而全局敏感性界限则通过敏感性分析假设未测量混杂因素对倾向的影响被统一约束在固定范围内，从而忽略了用户-物品交互之间的异质性。为克服这一局限，我们提出了一个新颖的框架——个性化未观察混杂因子去混杂框架（PUID）……

    arXiv:2605.21066v2 Announce Type: replace  Abstract: Recommender systems often rely on observational user-item interaction data, which is prone to selection bias due to users' selective interactions with items. While techniques such as inverse propensity weighting (IPW) and doubly robust estimators are effective in addressing selection bias from observed confounding, they become unreliable when hidden confounding exists, meaning that there are confounders that influence both user clicks and feedback but are not observable (e.g., user salary). Existing approaches relying on randomized controlled trials (RCTs) or global sensitivity bounds are constrained in practice: RCTs demand costly experimental data, while global sensitivity bounds presume a uniformly bounded effect of unmeasured confounders on propensities through sensitivity analysis, thereby neglecting heterogeneity across user-item interactions. To overcome this limitation, we propose a novel framework, Personalized Unobserved-Co
    
[^598]: 矛盾图的表达能力

    Expressivity of Contradiction Graphs

    [https://arxiv.org/abs/2605.20434](https://arxiv.org/abs/2605.20434)

    本文证明矛盾图序列能够精确确定概念类的VC维（可区分有限与无限VC维，回答了Alon等人2024年提出的问题），并能进一步确定任意固定长度的可实现数据集，从而刻画由这些数据集决定的学习理论性质。

    

    我们研究与二值概念类相关联的矛盾图。对于一个类 $H\subseteq\{0,1\}^X$，阶为 $m$ 的矛盾图 $G_m(H)$ 以可被 $H$ 实现的长度为 $m$ 的带标签序列作为顶点，当两个序列对某个共同定义域点赋予相反标签时，对应的两个顶点相邻。首先，我们识别出一个图论性质，该性质决定了阈值谓词 $\operatorname{VCdim}(H)\ge m$ 是否成立。因此，序列 $(G_m(H))_{m\ge1}$ 能够确定精确的 VC 维，特别是能够区分有限与无限的 VC 维，这回答了 Alon 等人（2024）提出的一个问题。随后，我们通过证明矛盾图序列在带符号重标记的意义下确定了任意固定长度的可实现数据集，从而推广了这一结果。因此，任何由固定长度的可实现数据集所决定、且在带符号重标记下保持不变的学习理论性质，都可以由矛盾图序列刻画。

    arXiv:2605.20434v2 Announce Type: replace-cross  Abstract: We study the contradiction graphs associated with a binary concept class. For a class $H\subseteq\{0,1\}^X$, the order-$m$ contradiction graph $G_m(H)$ has as vertices the $H$-realizable labeled sequences of length $m$, with two vertices adjacent when the two sequences assign opposite labels to some common domain point. First, we identify a graph-theoretic property that determines the threshold predicate $\operatorname{VCdim}(H)\ge m$. Consequently, the sequence $(G_m(H))_{m\ge1}$ determines the exact VC dimension and, in particular, distinguishes finite from infinite VC dimension, answering a question posed by Alon et al. (2024).   We then generalize this result by proving that the sequence of contradiction graphs determines, up to signed relabeling, the realizable datasets of any fixed length. Thus, any learning-theoretic property determined by the realizable datasets of a fixed length and invariant under signed relabeling ca
    
[^599]: 材料中化学无序的原子尺度建模：连接传统方法与AI辅助方法

    Atomistic Modeling of Chemical Disorder in Materials: Bridging Conventional Methods and AI-Assisted Approaches

    [https://arxiv.org/abs/2605.19124](https://arxiv.org/abs/2605.19124)

    本综述系统梳理了材料中化学无序的原子尺度建模方法，核心在于弥合实验中平均化无序描述与模拟及AI工作流所需的完全指定构型之间的表示鸿沟。

    

    化学无序源于多种元素对晶体学位点的混合占位，广泛存在于合金、陶瓷和成分复杂材料中，其中短程和长程有序强烈影响材料性能。一个核心障碍是实验与模拟之间的表示鸿沟：实验通常以部分占位率和系综平均行为来描述无序，而原子尺度模拟和AI工作流通常需要完全指定的构型。解决这一鸿沟需要能够将平均化无序描述转换为代表性构型系综的计算方法，同时兼顾成本、偏差和保真度。在AI驱动的计算发现中，这一挑战变得更为紧迫，因为忽略无序可能导致AI工作流错误排序稳定性、误判新颖性，并以过于理想化的表示误导实验方向。本综述重点（原文摘要在此处截断）

    arXiv:2605.19124v2 Announce Type: replace-cross  Abstract: Chemical disorder, originating from the mixed occupation of crystallographic sites by multiple elements, is widespread in alloys, ceramics, and compositionally complex materials, where short- and long-range orderings strongly influence properties. A central obstacle is the representation gap between experiments and simulations: experiments often report disorder as partial occupancies and ensemble-averaged behaviors, whereas atomistic simulations and AI workflows usually require fully specified configurations. Tackling this gap requires computational methods that convert averaged disorder descriptions into representative configurational ensembles while balancing cost, bias, and fidelity. This challenge has become more urgent in AI-driven computational discovery, where ignoring disorder may cause AI workflows to misrank stability, misjudge novelty, and misdirect experiments with too-idealized representations. This Review highligh
    
[^600]: 基于最优传输的动力学系统几何字典学习

    Geometric Dictionary Learning of Dynamical Systems with Optimal Transport

    [https://arxiv.org/abs/2605.18276](https://arxiv.org/abs/2605.18276)

    提出DOODL框架，通过最优传输在谱算子空间中学习动力学系统字典，揭示相关动力学系统的共享低维流形结构，并实现从短且部分观测数据中快速、可解释的算子估计

    

    通过算子理论表示来学习动力学系统，为分析复杂动力学提供了一个强大的框架，因为特征值和不变结构等谱量编码了特征时间尺度和长期行为。然而，动力学算子通常是对每个系统独立估计的，这阻碍了对相关动力学之间共享结构的发现。为解决这一局限，我们假设相关的动力学系统位于谱算子空间中的一个低维流形附近。基于这一假设，我们提出了DOODL（Dynamical OperatOr Dictionary Learning，动力学算子字典学习），这是一个学习特征谱动力学字典的框架，其线性组合可近似该流形，并为单个系统产生紧凑且可解释的嵌入。除表示学习之外，DOODL还能从短的、部分观测的轨迹中实现快速且可解释的算子估计

    arXiv:2605.18276v2 Announce Type: replace-cross  Abstract: Learning dynamical systems through operator-theoretic representations provides a powerful framework for analyzing complex dynamics, as spectral quantities such as eigenvalues and invariant structures encode characteristic time scales and long-term behavior. However, dynamical operators are typically estimated independently for each system, preventing the discovery of shared structure across related dynamics. To address this limitation, we posit that related dynamical systems lie near a low-dimensional manifold in spectral operator space. Based on this hypothesis, we introduce DOODL (Dynamical OperatOr Dictionary Learning), a framework that learns a dictionary of characteristic spectral dynamics whose combinations approximate this manifold and yield compact, interpretable embeddings of individual systems. Beyond representation learning, DOODL enables fast and interpretable operator estimation from short and partially observed tr
    
[^601]: 面向神经符号概念模型的简洁且逻辑一致的保形集

    Concise and Logically Consistent Conformal Sets for Neuro-Symbolic Concept-Based Models

    [https://arxiv.org/abs/2605.18202](https://arxiv.org/abs/2605.18202)

    提出事后框架COCOCO，将保形预测联合应用于神经符号概念模型的概念与标签预测，同时满足一致性、覆盖性和简洁性三个条件，解决了模型预测过度自信、难以判断决策可信度的问题。

    

    神经符号概念模型（NeSy-CBMs）是一类将神经网络与符号推理相结合的架构，旨在增强高风险应用中的可靠性。这类模型首先从输入中提取高层概念，然后在给定逻辑约束的前提下从这些概念推断出任务标签。然而，它们的标签和概念预测可能过于自信，使得利益相关者难以判断何时可以信任模型的决策。我们通过融入保形预测（CP）的思想来解决这一问题，保形预测是一个提供严格的、无分布覆盖保证的框架。我们形式化了任何适用于NeSy-CBMs的保形方法都应满足的三个期望条件——一致性、覆盖性和简洁性，并证明现有方法至少未能满足其中之一。随后，我们提出了COCOCO，这是一个事后（post-hoc）框架，它将概念和标签联合进行保形化处理，并调和……（摘要在此处被截断）

    arXiv:2605.18202v2 Announce Type: replace-cross  Abstract: Neuro-Symbolic Concept-based Models (NeSy-CBMs) are a family of architectures that integrate neural networks with symbolic reasoning for enhanced reliability in high-stakes applications. They work by first extracting high-level concepts from the input and then inferring a task label from these compatibly with given logical constraints. Yet, their label and concept predictions can be overconfident, making it difficult for stakeholders to gauge when the model's decisions can be trusted. We address this issue by integrating ideas from Conformal Prediction (CP), a framework providing rigorous, distribution-free coverage guarantees. We formalize three desiderata -- consistency, coverage, and conciseness -- that any conformal method for NeSy-CBMs should satisfy, and show that existing approaches fall short of at least one. We then introduce COCOCO, a post-hoc framework that conformalizes concepts and labels jointly and reconciles the
    
[^602]: 从有机数据生成预训练标记以应对数据受限的扩展

    Generating Pretraining Tokens from Organic Data for Data-Bound Scaling

    [https://arxiv.org/abs/2605.17849](https://arxiv.org/abs/2605.17849)

    SynPro 框架通过对有机数据进行改写和重新格式化并利用强化学习优化生成器，帮助 LLM 在数据受限时更充分地利用有限的有机数据，且不引入外部信息。

    

    大语言模型（LLM）预训练正从算力受限转向数据受限的状态，即可用的人类（有机）文本远远无法满足扩展需求。然而，进入数据受限状态并不意味着模型已经充分利用了其有机语料库。在本文中，我们提出了 SynPro，一个帮助 LLM 从有限的有机数据中更彻底学习的合成数据生成框架。SynPro 应用了两种操作——改写和重新格式化——以多样化的形式呈现相同的有机数据源，从而在不引入外部信息的情况下促进更深入的学习。这两个生成器均通过强化学习进行优化，奖励包括质量、忠实性和数据影响力，并且随着预训练进入平台期而持续更新，以针对模型尚未吸收的内容。我们使用 DCLM-Baseline 数据集中 Chinchilla 最优标记数的 10%（0.8B、2.2B 和 4B）对 400M、1.1B 和 2B 模型进行预训练，反映了现实的数据受限场景……

    arXiv:2605.17849v2 Announce Type: replace-cross  Abstract: LLM pretraining is shifting from a compute-bound to a data-bound regime, where available human (organic) text falls far short of scaling demands. However, reaching the data-bound regime does not mean the model has fully utilized its organic corpus. In this paper, we introduce SynPro, a synthetic data generation framework that helps LLMs more thoroughly learn from limited organic data. SynPro applies two operations, rephrasing and reformatting, that present the same organic source in diverse forms to facilitate deeper learning without introducing external information. Both generators are optimized via reinforcement learning with quality, faithfulness, and data influence rewards, and are continuously updated as pretraining plateaus to target content the model has yet to absorb. We pretrain 400M, 1.1B, and 2B models with 10% of their Chinchilla-optimal tokens (0.8B, 2.2B, and 4B) from DCLM-Baseline, reflecting a realistic data-bou
    
[^603]: 离散扩散模型的词表规模无关收敛性：伴随方程诱导出正确的空间

    Vocabulary-size-independent Convergence of Discrete Diffusion Models: adjoint equations induce the right space

    [https://arxiv.org/abs/2605.17232](https://arxiv.org/abs/2605.17232)

    本文提出基于伴随方程的统一理论框架，首次给出了完全与词表规模无关、适用于任意积分概率度量以及掩码和均匀先验的离散扩散模型收敛保证。

    

    离散扩散已成为包括语言、视觉和生物学在内的多种应用中生成建模的主流框架。然而，现有的收敛理论存在根本性的局限：基于KL散度的分析在诸如掩码分布等奇异先验下发散，而总变差（TV）下的界依赖于词表规模 $S$，对于拥有数十万词元的现代语言任务而言变得毫无意义。我们开发了一个基于伴随方程的统一框架，在任何积分概率度量（IPM）下建立了与词表规模无关的收敛保证。据我们所知，我们的界是首个完全摆脱 $S$ 且同时适用于掩码先验和均匀先验的结果。重要的是，我们的结果可以将现有的步数复杂度保证扩展到任意IPM。此外，我们的理论仅依赖单一标准的速率矩阵正则性假设，并适用于……

    arXiv:2605.17232v5 Announce Type: replace  Abstract: Discrete diffusion has become a leading framework for generative modeling in various applications including language, vision, and biology. Existing convergence theory, however, exhibits fundamental limitations. KL-based analyses diverge under singular priors such as the masked distribution, while bounds in total variation (TV) depend on the vocabulary size $S$ and become vacuous for modern language tasks, where vocabularies contain hundreds of thousands of tokens. We develop a unified adjoint-equation-based framework that establishes vocabulary-size-independent convergence guarantees in any integral probability metric (IPM). To the best of our knowledge, our bounds are the first to be entirely free of $S$ and applicable to both masked and uniform priors. Importantly, our results can extend existing step complexity guarantees to any IPM. Also, our theory relies only on a single standard rate-matrix regularity assumption and applies to
    
[^604]: EmoMind：从人脑fMRI信号解码情感图像描述

    EmoMind: Decoding Affective Captions from Human Brain fMRI

    [https://arxiv.org/abs/2605.16739](https://arxiv.org/abs/2605.16739)

    EmoMind是首个从fMRI信号端到端解码情感图像描述的系统，通过先检索中性场景描述、再利用34维连续情感向量进行重写，并借助无分类器引导实现语义保真与情感表达的灵活平衡。

    

    从大脑活动中解码视觉体验的研究已取得长足进展，但当前的脑到文本系统主要恢复语义内容而丢弃情感信息。此外，语言模型虽然在给定分类标签提示时能够生成情感文本，但这类标签将丰富的个体间差异压缩成粗糙的离散类别。我们提出了EmoMind，这是首个直接从fMRI信号解码情感图像描述的端到端流程。EmoMind首先从脑解码的视觉特征中检索语义基础的中性场景描述，然后使用从同一fMRI记录中解码出的连续34维情感向量对其进行重写。为了控制内容保留与情感表达之间的平衡，我们采用无分类器引导（classifier-free guidance）并结合一个保持身份的空分支来训练重写器，从而实现语义保真度与情感表现力之间的平滑插值。

    arXiv:2605.16739v3 Announce Type: replace-cross  Abstract: Decoding visual experience from brain activity has advanced substantially, but current brain-to-text systems largely recover semantic content while discarding affect. Additionally, language models can generate emotional text when prompted with categorical labels, but such labels collapse rich inter-subject variability into coarse discrete bins. We present EmoMind, the first end-to-end pipeline for decoding affective captions directly from fMRI signals. EmoMind first retrieves a semantically grounded neutral scene description from brain-decoded visual features, then rewrites it using a continuous 34-dimensional emotion vector decoded from the same fMRI recording. To control the balance between content preservation and affective expression, we train the rewriter with classifier-free guidance against an identity-preserving null branch, enabling smooth interpolation between semantic fidelity and affective expressivity. We evaluate 
    
[^605]: 大语言模型是否坚守其价值观？MANTA：一个用于动物福利推理的多轮对抗性基准测试

    Do LLMs Hold Their Values? MANTA: A Multi-Turn Adversarial Benchmark for Animal Welfare Reasoning

    [https://arxiv.org/abs/2605.16301](https://arxiv.org/abs/2605.16301)

    该论文提出了 MANTA 基准，通过 1,088 段五轮多轮对抗对话评估大语言模型在动物福利推理中的价值观稳定性与道德敏感性，弥补了现有单轮基准无法检测持续对抗压力下对齐退化及道德敏感性缺失的不足。

    

    尽管大语言模型已被快速部署于消费者和专业场景中，而在这类场景下，福利考量往往隐含于日常查询之中，但评估大语言模型的动物福利推理仍是一个悬而未决的挑战。现有基准测试（如 AnimalHarmBench）通过单轮、明确框架化的问题进行评估，衡量模型在被直接询问时是否避免产生有害内容。这种方法忽略了两种失效模式：一是持续对抗压力下的对齐退化，二是道德敏感性（即模型是否会在日常查询中自发地呈现福利相关利害关系）。为填补这一空白，我们构建了 MANTA——一个包含 1,088 段五轮对话的基准测试，对话从隐含的第一轮场景开始，经由明确的福利提示，再到三轮对抗压力回合，这些对抗压力源自一个五类分类法：社会型、文化型、经济型、实用型和认知型。我们从两个维度对对话进行评分：动物福利价值观稳定性（A

    arXiv:2605.16301v4 Announce Type: replace-cross  Abstract: Evaluating animal welfare reasoning in LLMs remains an open challenge despite rapid deployment in consumer and professional contexts where welfare considerations appear implicitly in everyday queries. Existing benchmarks such as AnimalHarmBench evaluate this through single-turn, explicitly framed questions, measuring whether models avoid harmful content when directly asked. This approach overlooks two failure modes: alignment degradation under sustained adversarial pressure, and moral sensitivity (whether a model spontaneously surfaces welfare stakes in everyday queries). To fill this gap, we construct MANTA, a benchmark of 1,088 five-turn conversations progressing from an implicit Turn-1 scenario through an explicit welfare prompt to three adversarial pressure rounds drawn from a five-type taxonomy: Social, Cultural, Economic, Pragmatic, and Epistemic. We score conversations on two dimensions: Animal Welfare Value Stability (A
    
[^606]: 潜藏在潜空间中的后门通道：将密码学不可检测性扩展到现代神经网络

    Backdoor Channels Hidden in Latent Space: Extending Cryptographic Undetectability to Modern Neural Networks

    [https://arxiv.org/abs/2605.13214](https://arxiv.org/abs/2605.13214)

    本文通过将后门通道识别为网络潜空间中学习到的潜在方向，把密码学意义上的不可检测后门攻击扩展到现代端到端训练的最先进神经网络，并证明其检测问题可归结为一个在实践中难以处理的假设检验。

    

    近期的密码学研究成果表明，神经网络可以被植入后门，使得任何高效算法都无法将其与干净模型区分开来。然而，这些保障一直局限于实际相关性有限的程式化架构，留下了这样一个悬而未决的问题：同等的不可检测性能否扩展到现代端到端训练的网络中。我们通过将后门通道识别为学习到的潜在方向，为最先进的架构构建了这样一种攻击机制，该机制与密码学意义上的不可检测性高度一致，并证明不可检测性问题可归结为对模型参数上两个未知分布之间的假设检验——我们推测该问题在实践中是难以处理的。这一重新表述的意义重大：如果网络潜空间中可利用的通道在统计上与自然学习到的方向无法区分，攻击者便无需引入（摘要在此处截断）

    arXiv:2605.13214v3 Announce Type: replace-cross  Abstract: Recent cryptographic results establish that neural networks can be backdoored such that no efficient algorithm can distinguish them from a clean model. These guarantees, however, have been confined to stylised architectures of limited practical relevance, leaving open whether comparable undetectability extends to modern, end-to-end trained networks. We construct such an attack mechanism for state-of-the-art architectures, closely aligned to the cryptographic notion of undetectability, by identifying backdoor channels as learned latent directions, and show that the question of undetectability reduces to a hypothesis test between two unknown distributions over model parameters, which we conjecture to be intractable in practice. The consequence of this reframing is significant: if exploitable channels within a network's latent space are statistically indistinguishable from naturally learned directions, an attacker need not introdu
    
[^607]: CRePE：面向统一相机控制视频生成的曲线光线期望位置编码

    CRePE: Curved Ray Expectation Positional Encoding for Unified-Camera-Controlled Video Generation

    [https://arxiv.org/abs/2605.12938](https://arxiv.org/abs/2605.12938)

    提出了曲线光线期望位置编码（CRePE），将图像token表示为沿统一相机模型射线的深度感知分布，并沿弯曲投影路径积分旋转位置相位，从而在冻结的视频扩散模型上实现了包括广角和鱼眼镜头在内的统一相机控制视频生成。

    

    视频世界模型在预测未来外观时，应当与3D场景结构、相机运动和镜头几何保持一致。然而，现有的注意力层面相机编码要么仅通过视线射线来描述每个token——而未能沿该射线定位场景内容——要么假设针孔投影模型，从而限制了广角镜头和鱼眼镜头下的相机控制能力。我们提出了曲线光线期望位置编码，它将每个图像token表示为沿其统一相机模型（UCM）射线的深度感知分布，并将期望的旋转位置编码相位复数沿着该分布投影到每个查询视图时所形成的曲线路径进行积分。CRePE通过在冻结的视频扩散Transformer上添加轻量级的几何注意力适配器来实现，来自单目几何基础模型的伪径向距离监督仅作为稳定化的锚点，而非推理时的依赖。

    arXiv:2605.12938v2 Announce Type: replace-cross  Abstract: Video world models should predict future appearance in a way that remains consistent with 3D scene structure, camera motion, and lens geometry. Existing attention-level camera encodings, however, either describe each token only by its viewing ray---without locating scene content along that ray---or assume pinhole projection, limiting camera control under wide-angle and fisheye lenses. We introduce Curved Ray Expectation Positional Encoding (CRePE), which represents each image token as a depth-aware distribution along its Unified Camera Model (UCM) ray and integrates the expected rotary positional phasor along the curved path this distribution traces when projected into each query view. CRePE is realized through a lightweight Geometric Attention Adapter on a frozen video diffusion transformer, with pseudo radial-distance supervision from a monocular geometry foundation model serving as a stabilizing anchor rather than an inferen
    
[^608]: 作为极坐标状态估计器的Transformer

    The Transformer as a Polar State Estimator

    [https://arxiv.org/abs/2605.11007](https://arxiv.org/abs/2605.11007)

    本文证明Transformer的注意力机制、残差连接和归一化组件可从极坐标下的几何状态估计问题中自然推导得出，并提出保留了这些几何修正项的Polar Transformer。

    

    我们证明，Transformer的核心组件——注意力机制、残差连接和归一化——自然地源于一个单一的几何状态估计问题。在极坐标下对潜在状态进行建模，可以自然地将径向动力学与超球面动力学分离开来，从而产生一种精度加权滤波程序：其中归一化强制执行超球面约束，注意力机制聚合方向证据，残差连接实现增量式状态更新。通过舍弃所得状态估计器中的几何修正项，即可恢复出带有旋转位置编码的标准Transformer块，这表明其架构源自底层的估计问题，而非独立的设计选择。本文提出的“极坐标Transformer”（Polar Transformer）保留了这些几何修正项。

    arXiv:2605.11007v4 Announce Type: replace-cross  Abstract: We show that the core components of the Transformer---attention, residual connections, and normalization---arise naturally from a single geometric state estimation problem. Modeling the latent state in polar coordinates naturally separates radial and hyperspherical dynamics, yielding a precision-weighted filtering procedure in which normalization enforces the hyperspherical constraint, attention aggregates directional evidence, and the residual connection implements an incremental state update. The standard Transformer block with rotary positional encodings is recovered by discarding the geometric correction terms of the resulting state estimator, showing that its architecture follows from the underlying estimation problem rather than from independent design choices. The proposed \textit{Polar Transformer} retains these geometric corrections.
    
[^609]: 揭开在线策略蒸馏的面纱：它何时有效、何时有害以及原因

    Unmasking On-Policy Distillation: Where It Helps, Where It Hurts, and Why

    [https://arxiv.org/abs/2605.10889](https://arxiv.org/abs/2605.10889)

    该论文提出了一个无需训练的高分辨率诊断框架，通过推导理想的逐节点梯度并开发可扩展的定向采样算法，揭示了在线策略蒸馏在训练推理模型时何时有益、何时有害。

    

    在线策略蒸馏为训练推理模型提供了密集的逐token监督信号；然而，这种信号在哪些条件下有益、在哪些条件下有害，目前仍不清楚。应该使用哪个教师模型？在自蒸馏的情况下，哪个特定的上下文应作为监督信号？最优选择是否会因token的不同而变化？目前，回答这些问题通常需要代价高昂的训练运行，而其汇总的性能指标掩盖了单个token层面的动态过程。我们引入了一个无需训练的诊断框架，它以最高分辨率运行：逐token、逐问题、逐教师。我们推导出一个理想的逐节点梯度，其定义为能够最大程度提高学生模型成功概率的参数更新。随后，我们开发了一种可扩展的定向采样算法来高效估计该梯度，即使对于长链（推理过程）也适用……

    arXiv:2605.10889v2 Announce Type: replace-cross  Abstract: On-policy distillation offers dense, per-token supervision for training reasoning models; however, it remains unclear under which conditions this signal is beneficial and under which it is detrimental. Which teacher model should be used, and in the case of self-distillation, which specific context should serve as the supervisory signal? Does the optimal choice vary from one token to the next? At present, addressing these questions typically requires costly training runs whose aggregate performance metrics obscure the dynamics at the level of individual tokens. We introduce a training-free diagnostic framework that operates at the highest resolution: per token, per question, and per teacher. We derive an ideal per-node gradient defined as the parameter update that maximally increases the student's probability of success. We then develop a scalable targeted-rollout algorithm to estimate this gradient efficiently, even for long ch
    
[^610]: RubricRefine：通过无需训练的执行前精炼提升工具使用智能体的可靠性

    RubricRefine: Improving Tool-Use Agent Reliability with Training-Free Pre-Execution Refinement

    [https://arxiv.org/abs/2605.09730](https://arxiv.org/abs/2605.09730)

    RubricRefine提出了一种无需训练的执行前契约检查方法，通过生成任务特定的评分标准、对候选代码进行契约检查评分并迭代修复失败，在零执行尝试的情况下将工具使用智能体在M3ToolEval上的平均性能提升至0.86。

    

    迭代式自我精炼是一种流行的推理时可靠性技术，但其在代码模式工具使用中的有效性很大程度上取决于反馈信号的结构：非结构化的批评在不同模型间的帮助效果不一致，即使使用真实执行反馈进行修订，改进也非常有限。主要的失败模式是工具间契约违规（错误的输出形状、不正确的工具路由、断裂的参数溯源），这些错误会完整运行而不引发任何错误，使得运行时反馈不足以发现问题。我们提出了RubricRefine，这是一种无需训练的执行前契约检查方法，它生成针对特定任务和工具注册表的评分标准，根据明确的契约检查对候选代码进行评分，并在任何执行发生之前迭代修复失败。RubricRefine在零执行尝试的情况下，跨七个模型在M3ToolEval上平均达到0.86，以更低的延迟超越了先前的推理时基线。

    arXiv:2605.09730v5 Announce Type: replace  Abstract: Iterative self-refinement is a popular inference-time reliability technique, but its effectiveness in code-mode tool use depends heavily on the structure of the feedback signal: unstructured critique helps inconsistently across models, and even revision with real execution feedback improves only modestly. The dominant failures are inter-tool contract violations (wrong output shape, incorrect tool routing, broken argument provenance) that run to completion without raising errors, making runtime feedback insufficient. We introduce RubricRefine, a training-free method for pre-execution contract checking that generates task- and registry-specific rubrics, scores candidate code against explicit contract checks, and iteratively repairs failures before any execution occurs. RubricRefine reaches $0.86$, averaged across seven models, on M3ToolEval with zero execution attempts, improving over prior inference-time baselines at lower latency tha
    
[^611]: 通过分类器重构将判别性张成空间作为合成数据效用的预测器

    Discriminative Span as a Predictor of Synthetic Data Utility via Classifier Reconstruction

    [https://arxiv.org/abs/2605.09697](https://arxiv.org/abs/2605.09697)

    该论文提出一种几何驱动的度量方法，通过在预训练基础模型嵌入空间中衡量线性分类器权重向量与样本差分向量所张成子空间之间的相对投影误差，无需训练模型即可预测合成正样本数据对下游二分类任务的效用。

    

    在许多现实世界的计算机视觉应用中，包括医学影像和工业检测，二分类任务的特点是正样本极度稀缺。一种被广泛采用的解决方案是通过对负样本施加图像到图像的变换来生成合成的正样本数据。然而，一个根本性的挑战依然存在：我们如何可靠地评估此类合成数据是否会提升下游模型的性能？在这项工作中，我们提出了一种几何驱动的度量方法，能够在不需要模型训练的情况下预测合成数据的效用。我们的方法在预训练基础模型的嵌入空间中运行，并通过样本之间的差分向量来表示数据集。我们通过测量相对投影误差，来评估线性分类器的权重向量是否可以表示在这些变化所张成的子空间内。直观地说，如果这些变化…

    arXiv:2605.09697v4 Announce Type: replace-cross  Abstract: In many real-world computer vision applications, including medical imaging and industrial inspection, binary classification tasks are characterized by a severe scarcity of positive samples. A widely adopted solution is to generate synthetic positive data using image-to-image transformations applied to negative samples. However, a fundamental challenge remains: how can we reliably assess whether such synthetic data will improve downstream model performance? In this work, we propose a geometry-driven metric that predicts the utility of synthetic data without requiring model training. Our approach operates in the embedding space of a pre-trained foundation model and represents the dataset through difference vectors between samples. We evaluate whether the weight vector of a linear classifier can be expressed within the subspace spanned by these variations by measuring the relative projection error. Intuitively, if the variations i
    
[^612]: 通过定制化智能体推理赋能视觉语言模型实现少样本多模态时间序列分类

    Empowering VLMs for Few-Shot Multimodal Time Series Classification via Tailored Agentic Reasoning

    [https://arxiv.org/abs/2605.09395](https://arxiv.org/abs/2605.09395)

    提出了首个面向少样本多模态时间序列分类的视觉语言模型智能体推理框架，通过生成器、反思器、修改器三角色协同以及自演化知识库的测试时更新策略，有效缓解少样本偏差与分布偏移，显著提升分类性能。

    

    本文提出了首个面向少样本多模态时间序列分类的视觉语言模型智能体推理框架，引入了一个自演化的知识库作为动态上下文，并通过反思性智能体推理对其进行迭代优化。该框架包含三个协作角色：i) 生成器通过推理执行可靠的分类；ii) 反思器诊断推理错误的根本原因，针对生成器所忽视的时序特征产生判别性见解；iii) 修改器将经过验证的更新应用于知识库以防止上下文崩塌。我们进一步引入了一种测试时更新策略，以实现谨慎且持续的知识库优化，从而缓解少样本偏差和分布偏移问题。在多个数据集上的大量实验验证了该框架的有效性。

    arXiv:2605.09395v3 Announce Type: replace  Abstract: In this paper, we propose the first VL\underline{\textbf{M}} \underline{\textbf{a}}gentic \underline{\textbf{r}}easoning framework for few-\underline{\textbf{s}}hot multimodal \underline{\textbf{T}}ime \underline{\textbf{S}}eries \underline{\textbf{C}}lassification (\textsc{MarsTSC}), which introduces a self-evolving knowledge bank as a dynamic context iteratively refined via reflective agentic reasoning. The framework comprises three collaborative roles: i) Generator conducts reliable classification via reasoning; ii) Reflector diagnoses the root causes of reasoning errors to yield discriminative insights targeting the temporal features overlooked by Generator; iii) Modifier applies verified updates to the knowledge bank to prevent context collapse. We further introduce a test-time update strategy to enable cautious, continuous knowledge bank refinement to mitigate few-shot bias and distribution shift. Extensive experiments across 1
    
[^613]: PMCTS：基于粒子蒙特卡洛树搜索的原则性并行推理时间扩展

    PMCTS: Principled Parallelized Inference Time Scaling with Particle Monte Carlo Tree Search

    [https://arxiv.org/abs/2605.08982](https://arxiv.org/abs/2605.08982)

    本文提出了PMCTS，一种专为GPU批处理并行化设计的原则性并行蒙特卡洛树搜索算法，它在保持策略改进保证的同时随并行计算规模良好扩展，并在国际象棋、围棋等MCTS和强化学习评估任务中始终优于或媲美流行的启发式基线方法。

    

    蒙特卡洛树搜索（MCTS）是强化学习中广泛用于策略改进和动作选择的方法。由于其顺序性和确定性的本质，如何利用并行计算对MCTS进行原则性的运行时扩展仍然是一个重大挑战。我们提出了粒子MCTS（PMCTS），这是一种原则性的并行MCTS算法，适用于神经网络评估，并专为GPU加速的批处理并行化而设计。我们为现代MCTS算法建立了策略改进保证，并证明PMCTS能够保持这些保证。实验表明，PMCTS在并行计算下具有良好的扩展性，并在一系列MCTS和强化学习评估领域中始终优于或可与流行的基于启发式的方法相媲美，这些领域包括棋盘游戏国际象棋和围棋，以及流行的离散动作和连续控制基准测试。

    arXiv:2605.08982v3 Announce Type: replace  Abstract: Monte Carlo Tree Search (MCTS) is a widely used approach for policy improvement and action selection in Reinforcement Learning. Due to its sequential and deterministic nature, principled runtime-scaling of MCTS with parallel compute remains a major challenge. We introduce Particle MCTS (PMCTS), a principled parallel MCTS algorithm suited for neural network evaluations and designed for GPU-acceleration with batch-parallelization. We establish policy improvement guarentees for modern MCTS algorithms and show that PMCTS maintains them. Empirically, PMCTS scales well with parallel compute and consistently outperforms or compares well to the popular heuristic-based baselines across a range of MCTS and RL evaluation domains, including the board games chess and Go and popular discrete action and continuous control benchmarks.
    
[^614]: FragileFlow：面向基础模型鲁棒性的“正确但脆弱”预测的谱控制

    FragileFlow: Spectral Control of Correct-but-Fragile Predictions for Foundation Model Robustness

    [https://arxiv.org/abs/2605.08896](https://arxiv.org/abs/2605.08896)

    提出即插即用的FragileFlow正则化器，通过校准边界缓冲区识别“正确但脆弱”的预测并将类别间误差流组织为脆弱风险矩阵，并首次给出该误差流对象的PAC-Bayes上界，从而以谱控制实现基础模型的确定性最差类别鲁棒性。

    

    大语言模型（LLM）和视觉语言模型（VLM）的鲁棒适配通常通过扰动下的平均准确率或平均一致性来评估。然而，这些平均值可能掩盖一种结构化的失败模式：预测可能仍然保持正确，但概率质量已经从特定的真实类别流向决策边界附近的系统性错误竞争类别。在本文中，我们将这种现象形式化为“边界感知误差流”，并引入FragileFlow——一种即插即用的正则化器，它利用校准的边界缓冲区识别“正确但脆弱”的预测，并将其类别外概率质量组织为类别级的脆弱风险矩阵。在理论上，我们为这一边界感知误差流对象提供了首个PAC-Bayes上界，展示了经验谱控制如何在稳定性条件下为确定性最差类别鲁棒性提供一条保守路径。在多项选择题LLM基准和少样本CLIP适配上的实验（摘要在此处截断）……

    arXiv:2605.08896v2 Announce Type: replace-cross  Abstract: Robust adaptation of LLMs and VLMs is often evaluated by average accuracy or average consistency under perturbations. However, these averages can hide a structured failure mode: a prediction may remain correct while probability mass already flows from particular true classes toward systematic wrong competitors near the decision boundary. In this paper, we formalize this phenomenon as margin-aware error flow and introduce FragileFlow, a plug-in regularizer that uses a calibrated margin buffer to identify correct-but-fragile predictions and organize their off-class probability mass into a class-wise vulnerable-risk matrix. Theoretically, we provide the first PAC-Bayes upper bound for this margin-aware error-flow object, showing how empirical spectral control yields a conservative route to deterministic worst-class robustness under a stability condition. Experiments on multiple-choice LLM benchmarks and few-shot CLIP adaptation sh
    
[^615]: 揭示的偏好能否阐明大语言模型的对齐与引导？

    Can Revealed Preferences Clarify LLM Alignment and Steering?

    [https://arxiv.org/abs/2605.08556](https://arxiv.org/abs/2605.08556)

    本文提出一个基于离散选择模型的实证流程，从大语言模型的观测决策中恢复其隐含的成本函数，从而严格评估模型是否表现出一致的目标导向行为、其语言表述的目标是否与实际决策策略相符、以及提示能否可靠地引导模型实现用户指定的成本函数。

    

    大语言模型越来越多地被用于在不确定性下做出或支持高风险决策，在这种情境下，模型的对齐不仅取决于事实准确性，还取决于模型如何在不同的结果之间权衡取舍。我们提出了一个实证流程，用于估计大语言模型的观测选择所隐含优化的偏好：我们引导模型给出对未知量的概率分布以及它在决策任务中所做的选择，然后拟合一个离散选择模型，以恢复最能合理解释模型决策的成本函数。我们展示了这种揭示偏好的描述如何能够严格评估：模型是否以一致的、目标导向的方式行动；模型能否用语言表述出与其揭示的决策策略相匹配的目标描述；以及提示（prompting）能否可靠地引导这些策略以实现用户指定的成本函数。我们将这一评估方法应用于四个医学诊断场景。

    arXiv:2605.08556v2 Announce Type: replace  Abstract: LLMs are increasingly used to make or support high-stakes decisions under uncertainty, where alignment depends not only on factual accuracy but on how models weigh tradeoffs between different outcomes. We present an empirical pipeline for estimating the implied preferences that an LLM's observed choices optimize: we elicit the model's probability distribution over unknowns along with the choice it would make for the decision task and then fit a discrete choice model to recover the cost function that best rationalizes the model's decisions. We show how this revealed-preference description allows rigorous evaluation of whether models behave in a consistently goal-directed way, whether they can verbalize a description of their objectives which matches their revealed decision policy, and whether prompting can reliably steer those policies to implement a user-specified cost function. We apply this evaluation across four medical diagnosis 
    
[^616]: 用于鲁棒优化的多面体保形集学习

    Learning Polyhedral Conformal Sets for Robust Optimization

    [https://arxiv.org/abs/2605.08506](https://arxiv.org/abs/2605.08506)

    该论文提出一个决策感知的保形框架，通过数据驱动方式学习适配鲁棒优化目标的多面体不确定性集，在保持有限样本统计有效性保证的同时降低决策的保守性。

    

    鲁棒优化（RO）为不确定性下的决策提供了一个有原则的框架，但其性能关键取决于不确定性集的选择。虽然较大的集合能确保可靠性，但往往导致过于保守的决策，而较小的集合则有排除真实结果的风险。近年来出现的数据驱动方法，特别是保形预测，提供了有限样本有效性保证，但在很大程度上与任务无关，忽略了下游决策结构。在本文中，我们提出了一个决策感知的保形框架，可以学习为鲁棒优化目标量身定制的不确定性集。我们的方法通过数据驱动的超平面参数化了一类灵活的多面体集合族，并通过直接最小化由此产生的鲁棒损失来学习其几何结构，同时通过保形校准保持统计有效性。为了校正数据依赖的选择偏差，我们引入了一个再校准步骤……

    arXiv:2605.08506v3 Announce Type: replace  Abstract: Robust optimization (RO) provides a principled framework for decision-making under uncertainty, but its performance critically depends on the choice of the uncertainty set. While large sets ensure reliability, they often lead to overly conservative decisions, whereas small sets risk excluding the true outcome. Recent data-driven approaches, particularly conformal prediction, offer finite-sample validity guarantees but remain largely task-agnostic, ignoring the downstream decision structure. In this paper, we propose a decision-aware conformal framework that learns uncertainty sets tailored to robust optimization objectives. Our approach parameterizes a flexible family of polyhedral sets via data-driven hyperplanes and learns their geometry by directly minimizing the induced robust loss, while preserving statistical validity through conformal calibration. To correct for data-dependent selection, we incorporate a re-calibration step on
    
[^617]: 块级可微Sinkhorn注意力：具有间隙感知垃圾箱桥接的尾部精细化梯度

    Block-Wise Differentiable Sinkhorn Attention: Tail-Refinement Gradients with a Gap-Aware Dustbin Bridge

    [https://arxiv.org/abs/2605.08123](https://arxiv.org/abs/2605.08123)

    该论文提出块级可微Sinkhorn注意力方法，通过尾部精细化替代与间隙感知垃圾箱桥接，在TPU上实现了长上下文平衡熵最优传输注意力的精确且高效的反向传播，将块级计算代价降至O((T+R)LW)且仅需O(L)额外HBM内存。

    

    我们通过一种截断基底、固定深度的尾部精细化替代方法，研究TPU硬件上的长上下文平衡熵最优传输（OT）注意力。在执行停止的T步Sinkhorn求解之后，我们展开一个较短的精细化尾部，并对该替代方法进行精确微分。对于所报告的R=2 TPU路径，反向传播包含四个阶梯式传输计划因子。我们证明了一个精确的单参考瓦片调度：R=2得分的余切等于单个参考传输计划瓦片乘以一个由向量余切和对偶差构造的显式修正场。在平衡固定支撑路径上，对于固定的头维度d和带宽W，这带来了块级计算代价O((T+R)LW)、O(Ld)的输入存储以及仅O(L)的额外HBM内存占用。我们还将当前的dustbin_block路径形式化为增广支撑上的相同单位目标替代方法，从而使伴随调度能够提升至TPU运行中使用的单活跃垃圾箱路径；该桥接（摘要内容此处被截断）

    arXiv:2605.08123v3 Announce Type: replace  Abstract: We study long-context balanced entropic optimal transport (OT) attention on TPU hardware through a stopped-base, fixed-depth tail-refinement surrogate. After a stopped $T$-step Sinkhorn solve, we unroll a short refinement tail and differentiate that surrogate exactly. For the reported $R=2$ TPU path, the backward pass contains four staircase plan factors. We prove an exact one-reference-tile schedule: the $R=2$ score cotangent is a single reference plan tile times an explicit modifier field built from vector cotangents and dual differences. This yields block-wise cost $O((T+R)LW)$, $O(Ld)$ input storage, and $O(L)$ additional HBM usage for fixed head dimension $d$ and band width $W$ on the balanced fixed-support path. We also formalize the current \texttt{dustbin\_block} path as the same unit-target surrogate on an augmented support, so the adjoint schedule lifts to the single-active-dustbin path used in our TPU runs; this bridge is 
    
[^618]: 多头注意力的收敛随机训练与理解LoRA

    Convergent Stochastic Training of Multi-Headed Attention and Understanding LoRA

    [https://arxiv.org/abs/2605.07959](https://arxiv.org/abs/2605.07959)

    本文通过统一框架严格证明了多头注意力和LoRA在随机训练方法下的可训练性，其关键在于证明Poincaré常数与数据维度或头的维度无关，从而保证模拟SGD的随机微分方程能收敛并最小化相应的损失。

    

    Transformer已经彻底改变了机器学习领域，在模型中部署注意力层在众多应用中日益成为标准做法。此外，对于大型模型，通常采用低秩适应技术，通过对模型进行因子化参数化训练，来实现令人惊讶的有利的精度与规模权衡。在本工作中，我们通过一个统一的框架严格地证明了此类模型在随机方法下的可训练性。我们证明，对于一类温和的正则化方法，注意力层上的经验回归损失以及浅层神经网络上的LoRA，都会使相应的吉布斯测度满足Poincaré不等式。至关重要的是，我们证明了对于LoRA，Poincaré常数与数据维度无关；对于多头注意力，Poincaré常数与头的维度无关。进而，通过引用近期的相关研究成果，可以得出结论：一个模拟SGD的特定随机微分方程（SDE）能够最小化相应的损失。

    arXiv:2605.07959v2 Announce Type: replace  Abstract: Transformers have revolutionized machine learning and deploying attention layers in the model is increasingly standard across a myriad of applications. Further, for large models, it is common to implement Low Rank Adaptation (LoRA), whereby a factorized parameterization of them is trained, to achieve a surprisingly beneficial accuracy-size trade-off. In this work, via a unified framework we rigorously establish trainability of such models under stochastic methods. We prove that for a class of mild regularizations, the empirical regression loss on a attention layer and LoRA on a shallow neural net, both induce Poincar\'e inequality for the corresponding Gibbs' measure. Crucially, we show that the Poincar\'e constant is free of the data dimension for LoRA and for multi-head attention it is free of the head dimensions, Then it follows, via invoking recent results, that a certain SDE, which mimics the SGD, minimizes the corresponding los
    
[^619]: 半对偶最优传输中 Monge 映射的稳定性

    Stability of the Monge Map in Semi-Dual Optimal Transport

    [https://arxiv.org/abs/2605.05569](https://arxiv.org/abs/2605.05569)

    本文证明最优传输的半对偶形式具有退化鞍点结构且等价于约束优化问题，推导出了 Monge 映射收敛的充分必要条件，并解释了数值算法更新传输映射为何比更新势函数需要更多迭代。

    

    本文证明最优传输问题的半对偶形式具有退化的鞍点结构，且其数值求解等价于求解一个约束优化问题。我们推导出了 Monge 映射收敛的充分必要条件，且无需假设对偶势函数达到最优。这一分析有助于解释为什么在实践中，数值算法更新传输映射通常比更新势函数需要更多的迭代次数。

    arXiv:2605.05569v5 Announce Type: replace-cross  Abstract: This paper shows that the semi-dual formulation of the optimal transport problem has a degenerate saddle-point structure, and that its numerical solution is equivalent to solving a constrained optimization problem. We derive necessary and sufficient conditions for the convergence of Monge maps without requiring optimality of the dual potential. This analysis helps explain why, in practice, numerical algorithms often require more iterations to update the transport map than the potential.
    
[^620]: 基于变分量子线路的量子分层强化学习

    Quantum Hierarchical Reinforcement Learning via Variational Quantum Circuits

    [https://arxiv.org/abs/2605.03434](https://arxiv.org/abs/2605.03434)

    该论文首次将变分量子线路成功应用于基于option-critic架构的分层强化学习，以更少参数实现优于经典方法的性能，并揭示了量子线路在选项价值估计中的架构瓶颈及关键设计原则。

    

    虽然参数化量子计算在标准强化学习（RL）中已展现出成功，但这些优势能否适用于分层强化学习（HRL）仍是一个关键的开放性问题。本工作证明变分量子线路（VQCs）可以有效增强基于option-critic架构的分层强化学习智能体。在标准环境中的评估表明，配备量子特征提取器的混合HRL智能体在使用更少参数的情况下优于经典基线方法。我们还发现了一个架构瓶颈：使用VQCs进行选项价值估计会严重降低学习性能。进一步的消融实验揭示了量子线路设计如何影响性能。我们的工作为参数高效的混合HRL智能体建立了设计原则。

    arXiv:2605.03434v2 Announce Type: replace  Abstract: While parameterized quantum computations have shown success in standard reinforcement learning (RL), whether these advantages adapt to hierarchical RL (HRL) remains a critical open question. This work demonstrates that variational quantum circuits (VQCs) can effectively enhance HRL agents based on the option-critic architecture. Evaluated in standard environments, a hybrid HRL agent with a quantum feature extractor outperforms classical baselines while using fewer parameters. We also identify an architectural bottleneck: using VQCs for option-value estimation severely degrades learning. Further ablations reveal how quantum circuit design affects performance. Our work establishes design principles for parameter-efficient hybrid HRL agents.
    
[^621]: 基于多项式对齐与逆多重二次（IMQ）距离的普适再生核希尔伯特空间

    A Universal Reproducing Kernel Hilbert Space from Polynomial Alignment and IMQ Distance

    [https://arxiv.org/abs/2605.03262](https://arxiv.org/abs/2605.03262)

    本文提出Yat核，通过将IMQ距离与带偏置的平方内积（多项式对齐）相乘，构建了一个在每个紧域上普适且严格扩展IMQ函数空间的再生核希尔伯特空间，并证明其高维激活协方差在高斯输入下保持满秩。

    

    逆多重二次核（IMQ）的核截面在无穷远处会衰减。多项式对齐能否在保持普适逼近能力的同时添加持久的方向性响应？Yat核通过将IMQ距离与一个带偏置的平方内积相乘来回答这个问题。在共享的正偏置和正则化条件下，其再生核希尔伯特空间（RKHS）连续包含IMQ的RKHS，并且在每个紧域上都是普适的。对齐严格地扩大了全局函数空间：Yat截面在无穷远处保留了二次方向性迹。我们通过高维激活协方差量化了第二个区别。对于固定数量的正交单位中心和固定的核参数，各向同性高斯输入使IMQ产生秩一重缩放的极限协方差，而Yat则产生满秩的极限协方差。在半径√d的球面上，两个极限均为满秩，从而将共享的输入范数波动分离出来，解释了高斯坍缩机制。

    arXiv:2605.03262v2 Announce Type: replace  Abstract: Inverse-multiquadric (IMQ) kernel sections decay at infinity. Can polynomial alignment add persistent directional responses while preserving universal approximation? The \yat{} (Yat) kernel answers this question by multiplying IMQ distance by a biased squared inner product. With shared positive bias and regularization, its RKHS continuously contains the IMQ RKHS and is universal on every compact domain. Alignment strictly enlarges the global function space: Yat sections retain a quadratic directional trace at infinity. We quantify a second distinction through high-dimensional activation covariance. For a fixed number of orthogonal unit centers and fixed kernel parameters, isotropic Gaussian inputs yield a rank-one rescaled limiting covariance for IMQ and a full-rank limit for Yat. On the radius-$\sqrt d$ sphere, both limits are full rank, isolating shared input-norm fluctuations as the Gaussian collapse mechanism. The connection is a
    
[^622]: 多用户对决老虎机：一种基于纳什社会福利的公平方法

    Multi-User Dueling Bandits: A Fair Approach using Nash Social Welfare

    [https://arxiv.org/abs/2605.01961](https://arxiv.org/abs/2605.01961)

    该论文针对多用户偏好差异导致的不公平问题，提出在对决老虎机框架中以用户各自的孔多塞赢家为参考点，并采用纳什社会福利目标最大化用户效用乘积，从而保证对少数群体用户的公平性。

    

    从人类偏好数据中学习正成为一种有用的工具，应用范围从微调大型语言模型到训练强化学习智能体。然而，在大多数场景中，模型是基于所有人类评估者的平均偏好进行训练的，当偏好存在较大差异时，这可能对少数群体不公平。在这项工作中，我们研究了对决老虎机（dueling bandits，一种从偏好数据中进行在线学习的标准框架）中的公平性问题。我们假设每个用户都有一个（可能各不相同的）孔多塞赢家（Condorcet winner），即优于其他所有选项的动作。以这些用户特定的孔多塞赢家作为参考点，我们根据每个动作相对于对应赢家的表现来对其进行评估和打分。为了在异构用户之间促进公平性，我们采用了成熟的纳什社会福利（Nash Social Welfare）目标，该目标最大化用户效用的乘积，从而内在地惩罚不平等，并防止（部分用户）被边缘化。

    arXiv:2605.01961v3 Announce Type: replace  Abstract: Learning from human preference data is becoming a useful tool, from fine-tuning large language models to training reinforcement learning agents. However, in most scenarios, the model is trained on the average preference of all human evaluators, which, under large variations of preferences, can be unfair to minority groups. In this work, we consider fairness in dueling bandits, a standard framework for online learning from preference data. We assume that each user has a (potentially distinct) Condorcet winner, which is an arm preferred to every other arm. Using these user-specific Condorcet winners as reference points, we evaluate and score arms according to their performance relative to the corresponding winner. To promote fairness across heterogeneous users, we adopt the well-established Nash Social Welfare objective, which maximizes the product of user utilities, thereby inherently penalizing inequality and preventing the marginali
    
[^623]: S^3-R1：使用合成数据逐步学习检索与回答

    $S^3$-R1: Learning to Retrieve and Answer Step-by-Step with Synthetic Data

    [https://arxiv.org/abs/2605.01248](https://arxiv.org/abs/2605.01248)

    该论文提出S^3-R1框架，通过程序化合成并筛选中等难度多跳问题的数据流水线，结合评估中间搜索步骤的更密集奖励信号，解决了强化学习后训练中稀疏奖励与训练数据单一难度的问题，从而提升模型利用搜索工具收集证据的能力。

    

    arXiv:2605.01248v4 公告类型：替换 摘要：强化学习（RL）后训练为模型带来了新的能力，例如用于搜索的智能体工具使用。然而，这些模型面临的主要困难源于基于最终结果的稀疏奖励以及缺乏涵盖不同难度问题的训练数据，这导致模型无法通过工具进行更深入的搜索以收集问答所需的证据。为了解决这些局限性，我们提出了S^3-R1（合成数据与稳定化搜索R1），这是一个将以数据为中心的方法与更密集的学习信号相结合的框架。我们首先开发了一个合成生成与筛选流水线，从现有文档中以程序化方式派生出多样化的多跳问题。该流水线包含一个基于检索的验证步骤，专门筛选出中等难度的问题。然后，我们将这个扩展后的训练集与一种奖励结构相配对，该奖励结构同时评估中间搜索步骤和最终答案。

    arXiv:2605.01248v4 Announce Type: replace  Abstract: Reinforcement learning (RL) post-training has enabled newer capabilities in models, such as agentic tool-use for search. However, these models struggle primarily due to limitations with sparse outcome-based rewards and a lack of training data that encapsulates questions of differing hardness, which results in models not performing deeper searches with tools to collect evidence for question-answering. To address these limitations, we introduce S^3-R1 (Synthetic data and stabilized Search R1), a framework that couples a data-centric approach with denser learning signals. We first develop a synthetic generation and curation pipeline that programmatically derives diverse, multi-hop questions from existing documents. This pipeline incorporates a retrieval-based verification step to specifically isolate questions of intermediate difficulty. We then pair this expanded training set with a reward structure that evaluates both intermediate sea
    
[^624]: 近地板几何是普遍的：训练过的过完备码中的杠杆离散度

    Near-Floor Geometry Is Generic: Leverage Dispersion in Trained Overcomplete Codes

    [https://arxiv.org/abs/2605.01192](https://arxiv.org/abs/2605.01192)

    该论文从理论上精确证明与秩-迹地板的距离本质上是特征杠杆的离散度（杠杆均衡的i.i.d.码普遍即可达到地板），并在40个实际发布的训练解码器矩阵上发现它们均偏离地板且更早出现保证的仿射失效。

    

    过完备码将F个特征打包进d<F维的空间中，因此对一个特征的线性读取会拾取来自其他特征的串扰，其下界由秩-迹地板 W(F,d)=(F-d)/(d(F-1)) 限定。与该地板的接近程度通常被解读为高效排列的证据，但事实并非如此：同形状的独立同分布（i.i.d.）码在我们测量的每种形状和负载下都能达到该下界（在7个匹配对照组中达到1.0000-1.0005），且其原因是精确的：对于增益校准的伪逆，实现比率为 d/(F(F-d))(Σᵢ hᵢ⁻¹ - F)，其中 Σᵢ hᵢ=d，因此当且仅当杠杆 hᵢ 在各特征间均衡时该比率才等于一——距地板的距离就是杠杆离散度。在40个已发布的解码器矩阵上——包括24个稀疏自编码器解码器以及16个混合模型的MLP下投影——全部位于其匹配对照组之上（1.0055-1.2018），且全部提前4到40个稀疏度水平达到保证的仿射失效：当同形状的随机码……（摘要在此处截断）

    arXiv:2605.01192v2 Announce Type: replace  Abstract: An overcomplete code packs $F$ features into $d<F$ dimensions, so linear readout of one feature picks up cross-talk from the others, bounded below by the rank-trace floor $W(F,d)=(F-d)/(d(F-1))$. Proximity to this floor is read as evidence of an efficient arrangement. It is not: an i.i.d. code of the same shape attains it at every shape and load we measure (1.0000-1.0005 over 7 matched controls), and the reason is exact. For the gain-calibrated pseudoinverse the attainment ratio is $\frac{d}{F(F-d)}(\sum_i h_i^{-1}-F)$ with $\sum_i h_i=d$, so it equals one precisely when the leverage $h_i$ is equalised across features: distance from the floor IS leverage dispersion.   On 40 released decoder matrices - 24 sparse-autoencoder decoders plus 16 MLP down-projections of a hybrid model - all sit above their matched control (1.0055-1.2018), and all reach guaranteed affine failure 4 to 40 sparsity levels earlier: where a random code of the sam
    
[^625]: 共形化量子DeepONet集成：迈向具有无分布保证的可扩展算子学习

    Conformalized Quantum DeepONet Ensembles: Towards Scalable Operator Learning with Distribution-Free Guarantees

    [https://arxiv.org/abs/2605.00330](https://arxiv.org/abs/2605.00330)

    本文提出共形化量子DeepONet集成框架，利用量子正交神经网络将隐层计算成本从二次方降至近线性，并结合分割共形校准实现具有有限样本、无分布保证的不确定性量化。

    

    算子学习能够实现对高维动力系统的快速代理建模，但现有方法面临两个根本性局限：稠密神经层的二次方计算成本，以及安全关键场景中不可靠的不确定性量化。我们提出了共形化量子DeepONet集成，这是一个同时应对这两个挑战的框架。利用量子正交神经网络（QOrthoNNs），我们刻画了其已有的 $\widetilde{\mathcal{O}}(n)$ 隐层运行时间标度优于经典稠密层 $\mathcal{O}(n^2)$ 成本的资源区间。为了量化不确定性，我们将集成预测与分割共形校准相结合。对于与校准数据对联合可交换的新输入-输出函数对，我们证明了关于覆盖查询位置期望比例的有限样本、无分布下界。作为次要的概念验证，我们探...

    arXiv:2605.00330v3 Announce Type: replace  Abstract: Operator learning enables fast surrogate modelling of high-dimensional dynamical systems, but existing approaches face two fundamental limitations: the quadratic cost of dense neural layers and unreliable uncertainty quantification in safety-critical settings. We propose Conformalized Quantum DeepONet Ensembles, a framework that addresses both challenges simultaneously. Using Quantum Orthogonal Neural Networks (QOrthoNNs), we characterize the resource regime in which their established $\widetilde{\mathcal{O}}(n)$ hidden-layer running-time scaling improves on the $\mathcal{O}(n^2)$ cost of a classical dense layer. To quantify uncertainty, we combine ensemble predictions with split conformal calibration. For a new input-output function pair jointly exchangeable with the calibration pairs, we prove a finite-sample, distribution-free lower bound on the expected fraction of covered query locations. As a secondary proof-of-concept, we expl
    
[^626]: 秩一矩阵分解的状态依赖李雅普诺夫分析

    State-Dependent Lyapunov Analysis of Rank-1 Matrix Factorization

    [https://arxiv.org/abs/2604.26993](https://arxiv.org/abs/2604.26993)

    该论文提出了一种基于参数化二次证书嵌套下水平集的状态依赖李雅普诺夫分析框架，证明了秩一矩阵分解中梯度下降即使在所有相关驻点均不稳定时，也能在显式区域内对几乎所有初始化收敛到全局极小值点或平衡的周期2轨道。

    

    我们为秩一矩阵分解上的梯度下降开发了一个状态依赖的李雅普诺夫框架。一个参数化的二次证书 I(δ; ·) 生成严格嵌套的下水平集，其排序为每个点分配一个状态 δ，而边界内向性质使该状态沿梯度下降轨迹保持单调。结合内部链传递性，这一几何结构即使在所有相关驻点均不稳定的情况下也能识别极限动力学。对于标量和秩一分解，该框架证明：在显式区域中对几乎所有初始化，当参数低于稳定性阈值时梯度下降收敛到全局极小值点，而在临界后区间内则收敛到平衡的周期2轨道。我们通过结构公理和动力学公理将这一机制形式化。在以原点为中心、具有交换对称性的二次类中，这些公理在单调重标记的意义下唯一确定了有序水平集几何。

    arXiv:2604.26993v3 Announce Type: replace-cross  Abstract: We develop a state-dependent Lyapunov framework for gradient descent on rank-1 matrix factorization. A parameterized quadratic certificate $I(\delta;\,\cdot)$ generates strictly nested sublevel sets. Their ordering assigns each point a state $\delta$, while a boundary-inward property makes this state monotone along gradient-descent trajectories. Together with internal chain transitivity, this geometry identifies the limiting dynamics even when all relevant stationary points are unstable. For scalar and rank-1 factorization, it yields convergence to a global minimizer below the stability threshold and to a balanced period-$2$ orbit in a post-critical interval, for almost every initialization in an explicit region.   We formalize the mechanism through structural and dynamical axioms. Within the origin-centered, exchange-symmetric quadratic class, the axioms determine the ordered level-set geometry uniquely up to a monotone relabe
    
[^627]: 过滤反馈下面向多智能体大语言模型的边际贡献策略梯度

    Marginal-Contribution Policy Gradients under Filtered Feedback for Multi-Agent LLMs

    [https://arxiv.org/abs/2604.22785](https://arxiv.org/abs/2604.22785)

    本文提出了多智能体大语言模型强化学习训练中唯一满足一致性要求的边际贡献功劳分配理论，通过从过滤反馈中识别梯度正确的边际贡献信号并最优分配反事实评估预算，在GRPO中无需额外生成成本即提升了路由GSM8K准确率。

    

    我们为多智能体大语言模型系统中的强化学习训练提出了一种统一的功劳分配处理方法。我们证明，仅凭观测到的奖励无法区分真正决定该奖励的智能体与从未影响该奖励的智能体，且标准的共享奖励训练实际上是在对每个智能体的私有效用而非系统性能进行精确的梯度上升。此外，我们证明，一旦智能体之间存在交互，任何单一的每个智能体标量都无法一致地解释联合性能。因此，我们发展出了唯一满足自然一致性要求的、依赖于背景的边际贡献概念。由此我们推导出梯度正确的边际贡献训练信号，从过滤反馈中识别这些信号，并针对学习信号的误差最优地分配精确反事实评估的预算。在GRPO中实例化后，我们的信号在不增加额外生成成本的情况下，相较于赢家通吃训练提升了路由GSM8K的准确率。

    arXiv:2604.22785v2 Announce Type: replace  Abstract: We develop a unified treatment of credit assignment for RL training in multi-agent LLM systems. We show that observed reward alone cannot distinguish an agent that determines it from one that never affects it, and that standard shared-reward training performs exact gradient ascent on each agent's private utility rather than system performance. Moreover, we prove no single scalar per agent can consistently account for joint performance once agents interact. We thus develop the unique background-dependent notion of marginal contribution satisfying natural consistency requirements. From it we derive gradient-correct marginal contribution training signals, identify them from filtered feedback, and optimally allocate a budget of exact counterfactual evaluations against learned-signal error. Instantiated in GRPO, our signal improves routed GSM8K accuracy over winner-take-all training at no extra generation cost.
    
[^628]: 无需对抗训练的学习：一种面向虚假数据注入攻击下安全电力系统状态估计的物理信息神经网络

    Learning Without Adversarial Training: A Physics-Informed Neural Network for Secure Power System State Estimation under False Data Injection Attacks

    [https://arxiv.org/abs/2604.22784](https://arxiv.org/abs/2604.22784)

    本文提出一种能联合学习同方差不确定性参数从而自适应平衡监督损失与物理损失的物理信息神经网络，在无需对抗训练的情况下实现了对虚假数据注入攻击具有鲁棒防御能力的电力系统状态估计。

    

    电力系统状态估计（PSSE）将地理上分布的测量数据转换为电网监控和控制所需的电压幅值与相角。学习型估计器可以快速完成这一映射，但具备模型感知能力的虚假数据注入攻击（FDIA）可能在保持交流合理性并规避基于残差检测的情况下破坏其输入。物理信息神经网络（PINN）通过潮流一致性来限制候选状态；然而，其鲁棒性依赖于监督损失与物理损失之间的平衡，而这些损失的尺度和梯度贡献在训练过程中会不断演变。本文提出一种联合学习同方差不确定性参数的PINN，并利用这些参数对两个目标进行自适应调整。该方案为有功功率、无功功率、电压和相角损失分配可训练的对数不确定性参数，同时防止物理目标被低估。该估计器仅在干净的稳态数据上训练……（原文摘要此处截断）

    arXiv:2604.22784v2 Announce Type: replace  Abstract: Power System State Estimation (PSSE) converts geographically distributed measurements into the voltage magnitudes and phase angles needed for grid monitoring and control. Learned estimators can perform this mapping rapidly, but model-aware False Data Injection Attacks (FDIAs) may corrupt their inputs while retaining AC plausibility and residual-based stealth. Physics-Informed Neural Networks (PINNs) limit candidate states through power-flow consistency; however, their robustness depends on balancing supervised and physics losses whose scales and gradient contributions evolve during training. This paper proposes a PINN that jointly learns homoscedastic uncertainty parameters and uses them to adapt the two objectives. The formulation assigns trainable log-uncertainties to the active-power, reactive-power, voltage, and angle losses while safeguarding against an underweighted physics objective. The estimator is trained only on clean stea
    
[^629]: 保形化超级学习器

    Conformalized Super Learner

    [https://arxiv.org/abs/2604.22391](https://arxiv.org/abs/2604.22391)

    本文提出将保形预测与超级学习器自然结合的框架，通过加权组合各学习器的保形分数来构建具有有限样本覆盖保证的预测区间。

    

    超级学习器（SL）是一种广泛使用的集成学习方法，它根据各学习器的预测性能，将来自学习器库中的点预测进行组合。区间预测具有重要的实际意义，因为它能够量化单个学习器或集成模型所产生的预测中的不确定性。目前已提出多种基于SL构建区间预测的方法，然而这些方法通常仅通过渐近性论证来支撑其有效性，或依赖于诸如自助法等计算密集型的程序。保形预测（CP）是一种机器学习框架，能够在温和的条件下构建具有有限样本和渐近覆盖保证的预测区间。我们提出通过一种自然的构造将保形预测与超级学习器相结合，该构造呼应了原始SL框架的设计，利用个体学习器的权重，并通过加权方式组合各学习器特定的保形分数。

    arXiv:2604.22391v2 Announce Type: replace-cross  Abstract: The Super Learner (SL) is a widely used ensemble method that combines point predictions from a library of learners based on their predictive performance. Interval predictions are of considerable practical interest because they allow uncertainty in predictions produced by an individual learner or an ensemble to be quantified. Several methods have been proposed for constructing interval predictions based on the SL, however, these approaches are typically justified using asymptotic arguments or rely on computationally intensive procedures such as the bootstrap. Conformal prediction (CP) is a machine learning framework for constructing prediction intervals with finite-sample and asymptotic coverage guarantees under mild conditions. We propose coupling CP with the SL through a natural construction that mirrors the original SL framework, using individual learner weights and combining learner-specific conformity scores via a weighted 
    
[^630]: 基于伙伴感知的肽-蛋白质相互作用预测与靶标条件化肽生成

    Partner-aware Peptide-Protein Interaction Prediction and Target-conditioned Peptide Generation

    [https://arxiv.org/abs/2604.18467](https://arxiv.org/abs/2604.18467)

    该论文提出了一个集成框架，将伙伴感知的肽-蛋白质相互作用预测与结合位点定位模型ConGA-PepPI和靶标条件化肽生成模型TC-PepGen相结合，实现了相互作用预测、结合位点解读和靶标条件化肽生成的统一，用于加速早期肽类药物筛选。

    

    动机：肽-蛋白质相互作用（PepPIs）是细胞调控和肽类治疗的核心，但其实验表征对于大规模筛选而言仍然过于缓慢。现有方法通常只侧重于相互作用预测或肽生成，导致候选肽优先级排序、残基级解读和靶标条件化肽扩展未能充分整合。结果：我们提出了一个用于早期肽筛选的集成框架，该框架结合了伙伴感知的预测与定位模型以及靶标条件化生成模型。ConGA-PepPI采用非对称编码、双向交叉注意力以及从配对预测到结合位点定位的渐进式迁移学习，而TC-PepGen通过逐层条件化在自回归解码过程中持续保留靶标信息。在五折交叉验证中，ConGA-PepPI达到了0.839的准确率和0.92（摘要在此处截断）

    arXiv:2604.18467v3 Announce Type: replace-cross  Abstract: Motivation: Peptide-protein interactions (PepPIs) are central to cellular regulation and peptide therapeutics, but experimental characterization remains too slow for large-scale screening. Existing methods usually emphasize either interaction prediction or peptide generation, leaving candidate prioritization, residue-level interpretation, and target-conditioned expansion insufficiently integrated. Results: We present an integrated framework for early-stage peptide screening that combines a partner-aware prediction and localization model (ConGA-PepPI) with a target-conditioned generative model (TC-PepGen). ConGA-PepPI uses asymmetric encoding, bidirectional cross-attention, and progressive transfer from pair prediction to binding-site localization, while TC-PepGen preserves target information throughout autoregressive decoding via layerwise conditioning. In five-fold cross-validation, ConGA-PepPI achieved 0.839 accuracy and 0.92
    
[^631]: AdaExplore：面向高效内核生成的失败驱动适应与保持多样性搜索

    AdaExplore: Failure-Driven Adaptation and Diversity-Preserving Search for Efficient Kernel Generation

    [https://arxiv.org/abs/2604.16625](https://arxiv.org/abs/2604.16625)

    提出 AdaExplore 智能体框架，通过失败驱动的适应与保持多样性的搜索两个互补阶段，利用积累的执行反馈实现 Triton 内核代码生成的自我改进，无需额外微调或外部知识即可同时提升代码正确性与优化性能。

    

    近期的大语言模型（LLM）智能体在使用执行反馈进行测试时适应方面展现出潜力。然而，稳健的自我改进仍远未解决：大多数方法仍然独立地处理每个问题实例，而不积累可复用的知识。这一局限在诸如 Triton 这样的领域特定语言中尤为明显，因为这类语言在 LLM 预训练数据中代表性不足。其严格的约束和非线性的优化空间进一步使朴素的生成与局部精炼变得不可靠。我们提出 AdaExplore，这是一个智能体框架，通过两个互补的阶段——失败驱动的适应和保持多样性的搜索——利用积累的执行反馈实现面向性能关键内核代码生成的自我改进，在无需额外微调或外部知识的情况下，同时提升正确性与优化性能。在适应阶段，智能体……

    arXiv:2604.16625v2 Announce Type: replace-cross  Abstract: Recent large language model (LLM) agents have shown promise in using execution feedback for test-time adaptation. However, robust self-improvement remains far from solved: most approaches still treat each problem instance independently, without accumulating reusable knowledge. This limitation is particularly pronounced in domain-specific languages such as Triton, which are underrepresented in LLM pretraining data. Their strict constraints and non-linear optimization landscape further make naive generation and local refinement unreliable. We propose AdaExplore, an agent framework that enables self-improvement via accumulated execution feedback for performance-critical kernel code generation through two complementary stages: failure-driven adaptation and diversity-preserving search, jointly improving correctness and optimization performance without additional fine-tuning or external knowledge. In the adaptation stage, the agent s
    
[^632]: PAC-CF：在LLM引导搜索中校准不可逆前沿剪枝

    PAC-CF: Calibrating Irreversible Frontier Pruning in LLM-Guided Search

    [https://arxiv.org/abs/2604.14345](https://arxiv.org/abs/2604.14345)

    提出PAC-CF方法，将LLM引导搜索中的前沿剪枝形式化为具有PAC保证的保形决策问题，通过Native-Trace路径校准得到保形边际来过滤候选，避免因不可约评估偏差误删所有可通向有效解的分支，从而在多种领域和预算下提升搜索效用。

    

    LLM引导的搜索通常通过基于评估器分数对top-$K$候选进行排序和剪枝来解决复杂任务。然而，即使采用重复采样等流行方法来降低方差，不可约偏差依然存在。因此，剪枝可能会误删所有能够到达有效解的后续分支。在本文中，我们提出了可能近似正确保形过滤（PAC-CF），它将树剪枝形式化为一个具有PAC保证的决策问题。理论分析阐明了不可约偏差如何降低用于认证性淘汰的分数区分度。Native-Trace路径校准从保留的Native轨迹上验证器有效后续分支的分数差距中推导出一个保形边际。在部署阶段，PAC-CF在直接的分数差距过滤规则中使用这一校准后的边际。在多个领域和最先进的控制器上，PAC-CF在各种预算下均提升了效用。

    arXiv:2604.14345v4 Announce Type: replace-cross  Abstract: LLM-guided search is usually adopted to solve complex tasks by ranking and pruning top-$K$ candidates based on evaluator scores. However, irreducible bias still exists even if popular methods, such as repeated sampling, are applied to reduce variance. Consequently, pruning may remove every continuation that can reach a valid solution. In this paper, we propose Probably Approximately Correct Conformal Filtering (PAC-CF), which formulates tree pruning as a PAC-guaranteed decision problem. Theoretical analysis establishes how irreducible bias reduces the score separation for certified elimination. Native-Trace path calibration derives a conformal margin from the score deficit of verifier-valid continuations on held-out Native traces. During deployment, PAC-CF uses this calibrated margin in a direct score-gap filtering rule. Across diverse domains and state-of-the-art controllers, PAC-CF improves utility at various budgets while re
    
[^633]: 面向语义恢复的令牌编码

    Token Encoding for Semantic Recovery

    [https://arxiv.org/abs/2604.12931](https://arxiv.org/abs/2604.12931)

    该论文提出TokCode框架，利用轻量级适配器将大语言模型改造为令牌编码器，并通过信道质量感知的蒸馏方法CADET进行训练，通过在语义域中重构冗余实现抗令牌丢失的鲁棒语义恢复。

    

    在生成式语义通信中，语义令牌引导接收端的生成模型合成高维内容。然而，在具有挑战性的网络环境中，频繁的令牌丢失会使所传递的语义失真，超出接收端恢复能力所能修复的范围。本文提出了一种面向鲁棒语义恢复的令牌编码框架，通过在语义域中重构冗余来实现抗擦除能力。TokCode使用一个轻量级适配器将发射端的通用大语言模型（LLM）改造为令牌编码器，利用LLM预训练获得的语义先验，从而避免引入专用的深度模型。为了高效优化该适配器并使其能够适用于多种不同信道，我们开发了一种信道质量感知的蒸馏方法用于令牌编码器训练（CADET）。CADET使用可微分的句子级语义代理对T5基础模型进行调优……（摘要内容在此处被截断）

    arXiv:2604.12931v2 Announce Type: replace-cross  Abstract: In generative semantic communication, semantic tokens guide receiver-side generative models to synthesize high-dimensional content. In challenging network environments, however, frequent token erasure distorts the conveyed semantics beyond what receiver-side recovery can restore. In this paper, we propose a token encoding framework (TokCode) for robust semantic recovery, achieving erasure resilience by restructuring redundancy in the semantic domain. TokCode uses a lightweight adapter to recast a general-purpose large language model (LLM) at the transmitter into a token encoder, exploiting the LLM's pretrained semantic prior to avoid introducing a dedicated deep model. To optimize the adapter efficiently and make it applicable across diverse channels, we develop a channel-quality-aware distillation approach for token encoder training~(CADET). Using a differentiable sentence-level semantic surrogate, CADET tunes T5 foundation mo
    
[^634]: 面向KV缓存压缩的注意力加权值投影

    Attention-Weighted Value Projection for KV-Cache Compression

    [https://arxiv.org/abs/2604.11501](https://arxiv.org/abs/2604.11501)

    提出了一种面向KV缓存压缩的注意力加权值投影方法，通过以重建注意力输出（而非仅重建值本身）为目标，利用 V^⊤α^⊤αV 的主特征向量得到具有精确最优解的秩约减方案，其误差恰好等于被丢弃特征值之和，并将该目标扩展至校准数据集与分组查询注意力场景。

    

    秩约减会丢弃维度；量化则以更低精度保留维度。比较这两种方法需要先确定压缩应该保留什么。针对注意力值，我们研究的是注意力输出的重建，而不仅仅是值本身的重建。在固定注意力权重的条件下，最优的正交秩-r投影使用 V^⊤α^⊤αV 的主特征向量，其误差恰好等于被丢弃的特征值之和。我们将这一目标扩展到校准数据集和分组查询注意力（GQA）场景，并描述了在加性局部误差预算下的秩分配策略。我们还检验了利用局部误差预测下游损失的局限性。历史上的权重扰动实验更倾向于系数舍入而非所测试的值投影，但并未在相同缓存存储条件下建立公平比较。由此产生的区别具有实用性：投影目标存在精确解，

    arXiv:2604.11501v2 Announce Type: replace-cross  Abstract: Rank reduction discards dimensions; quantization keeps them at lower precision. Comparing the two requires a choice of what compression should preserve. For attention values, we study reconstruction   of the attention output rather than reconstruction of the values alone. With fixed attention weights, the optimal orthogonal rank-$r$ projection uses the leading eigenvectors of   $V^\top\alpha^\top\alpha V$, and its error is exactly the discarded eigenvalue sum. We extend this objective to calibration datasets and grouped query attention, and describe rank allocation under   an additive local error budget. We also examine the limits of using local error to predict downstream loss. Historical weight-perturbation experiments favor coefficient rounding over the value   projections tested, but do not establish a comparison at equal cache storage. The resulting distinction is practical: the projection objective has an exact solution, 
    
[^635]: 基于相关性引导电路设计的领域感知混合量子学习用于犯罪模式分析

    Domain-Aware Hybrid Quantum Learning via Correlation-Guided Circuit Design for Crime Pattern Analytics

    [https://arxiv.org/abs/2604.07389](https://arxiv.org/abs/2604.07389)

    本研究提出一种基于相关性引导电路设计的领域感知混合量子-经典学习框架用于犯罪模式分析，其中QAOA方法以更少的可训练参数实现了高达84.6%的准确率，在内存受限的边缘部署场景中展现出实际优势。

    

    犯罪模式分析对执法和预测性警务至关重要，然而快速城市化带来的犯罪活动激增产生了高维、不平衡的数据集，这对传统分类方法构成了挑战。本研究提出了一种用于犯罪分析的量子-经典比较框架，评估了四种计算范式：量子模型、经典基线机器学习模型以及两种混合量子-经典架构。利用16年的犯罪统计数据，我们在严格的交叉验证方法下系统评估了分类性能和计算效率。实验结果表明，量子启发方法（特别是QAOA）实现了高达84.6%的准确率，同时所需的可训练参数比经典基线更少，这表明其在内存受限的边缘部署方面具有实际优势。所提出的相关性感知电路设计展示了……的潜力（原文在此处截断）

    arXiv:2604.07389v3 Announce Type: replace  Abstract: Crime pattern analysis is critical for law enforcement and predictive policing, yet the surge in criminal activities from rapid urbanization creates high-dimensional, imbalanced datasets that challenge traditional classification methods. This study presents a quantum-classical comparison framework for crime analytics, evaluating four computational paradigms: quantum models, classical baseline machine learning models, and two hybrid quantum-classical architectures. Using 16-year crime statistics, we systematically assess classification performance and computational efficiency under rigorous cross-validation methods. Experimental results show that quantum-inspired approaches, particularly QAOA, achieve up to 84.6% accuracy, while requiring fewer trainable parameters than classical baselines, suggesting practical advantages for memory-constrained edge deployment. The proposed correlation-aware circuit design demonstrates the potential o
    
[^636]: 学习排序：通过学习重要性排序实现视觉归因

    Learn to Rank: Visual Attribution by Learning Importance Ranking

    [https://arxiv.org/abs/2604.05819](https://arxiv.org/abs/2604.05819)

    该论文提出了一种将视觉归因中的重要性排序框架化为排列学习的训练方案，直接优化删除和插入指标，从而克服了现有基于传播、扰动和学习的方法在效率、因果性和解释质量之间的三重权衡。

    

    解释复杂计算机视觉模型的决策对于建立信任和问责制至关重要，尤其是在安全攸关的领域。一种成熟的可解释性方法是生成视觉归因图，突出显示输入中与模型预测最相关的区域。然而，现有方法面临三重权衡。基于传播的方法效率高，但可能存在偏差且依赖于特定架构。同时，基于扰动的方法具有因果基础，但计算成本高昂，且在视觉Transformer上往往只能产生粗糙的补丁级解释。基于学习的解释器速度快，但通常优化的是代理目标函数或从启发式教师模型中蒸馏知识。我们提出了一种新的学习方案，直接优化删除和插入指标。由于这些指标依赖于不可微分的排序操作，我们将其框架化为排列学习问题，并用……（原文摘要在此处截断）

    arXiv:2604.05819v2 Announce Type: replace-cross  Abstract: Interpreting the decisions of complex computer vision models is crucial to establish trust and accountability, especially in safety-critical domains. An established approach to interpretability is generating visual attribution maps that highlight regions of the input most relevant to the model's prediction. However, existing methods face a three-way trade-off. Propagation-based approaches are efficient, but they can be biased and architecture-specific. Meanwhile, perturbation-based methods are causally grounded, yet they are expensive and for vision transformers often yield coarse, patch-level explanations. Learning-based explainers are fast but usually optimize surrogate objectives or distill from heuristic teachers. We propose a learning scheme that instead optimizes deletion and insertion metrics directly. Since these metrics depend on non-differentiable sorting and ranking, we frame them as permutation learning and replace 
    
[^637]: 基于神经路由算法的近实时遥测感知路由

    Towards Near-Real-Time Telemetry-Aware Routing with Neural Routing Algorithms

    [https://arxiv.org/abs/2604.02927](https://arxiv.org/abs/2604.02927)

    本文将遥测感知路由建模为延迟感知的闭环控制问题，提出了一个显式建模通信与推理延迟的训练评估框架，并在此基础上提出可扩展的图神经路由算法LOGGIA，实现近实时的遥测感知路由。

    

    路由算法对于计算机网络的高效运行至关重要，在许多场景下，路由算法必须能够在毫秒级时间内对流量突发做出反应。实时遥测数据可以为路由算法提供有价值的信号，近期的研究已经训练神经网络来利用这些信号进行流量感知路由。然而，聚合全网信息会受到通信延迟的影响，现有的神经方法要么假设不现实的零延迟全局状态，要么将路由器限制为仅使用纯本地遥测信息，这使得它们在真实环境中的可部署性尚不明确。我们将遥测感知路由建模为一个延迟感知的闭环控制问题，并引入了一个框架，用于训练和评估神经路由算法，同时显式地对通信和推理延迟进行建模。在此框架之上，我们提出了 LOGGIA，一种可扩展的图神经路由算法，该算法预测对数空间的链路权重……

    arXiv:2604.02927v3 Announce Type: replace  Abstract: Routing algorithms are crucial for efficient computer network operations, and in many settings they must be able to react to traffic bursts within milliseconds. Live telemetry data can provide informative signals to routing algorithms, and recent work has trained neural networks to exploit such signals for traffic-aware routing. Yet, aggregating network-wide information is subject to communication delays, and existing neural approaches either assume unrealistic delay-free global states, or restrict routers to purely local telemetry. This leaves their deployability in real-world environments unclear. We cast telemetry-aware routing as a delay-aware closed-loop control problem and introduce a framework that trains and evaluates neural routing algorithms, while explicitly modeling communication and inference delays. On top of this framework, we propose LOGGIA, a scalable graph neural routing algorithm that predicts log-space link weight
    
[^638]: Apriel-Reasoner：面向通用且高效推理的强化学习后训练方法

    Apriel-Reasoner: RL Post-Training for General-Purpose and Efficient Reasoning

    [https://arxiv.org/abs/2604.02007](https://arxiv.org/abs/2604.02007)

    提出 Apriel-Reasoner，一个基于 150 亿参数开源基座模型、采用可复现的多领域强化学习后训练方案与自适应领域采样技术训练而成的通用且高效推理模型。

    

    使用可验证奖励的强化学习（RLVR）在多个领域构建通用推理模型的方法，已被前沿开源权重模型广泛采用。然而，这些模型的训练方案和领域混合策略往往不予公开。跨领域联合优化面临重大挑战：各领域在 rollout 长度、问题难度和样本效率方面差异巨大。此外，具有长思维链的模型会增加推理成本和延迟，使得效率成为实际部署中的关键因素。我们提出了 Apriel-Reasoner，它在 Apriel-Base（一个 150 亿参数的开源权重大型语言模型）上，采用可复现的多领域强化学习后训练方案，基于公开数据集在五个领域进行训练：数学、代码生成、指令遵循、逻辑谜题和函数调用。我们引入了自适应领域采样方法，尽管各领域的 rollout 动态存在异质性，仍能保持目标已完成 rollout 的比例……

    arXiv:2604.02007v3 Announce Type: replace  Abstract: Building general-purpose reasoning models using reinforcement learning with verifiable rewards (RLVR) across diverse domains has been widely adopted by frontier open-weight models. However, their training recipes and domain mixtures are often not disclosed. Joint optimization across domains poses significant challenges: domains vary widely in rollout length, problem difficulty, and sample efficiency. Further, models with long chain-of-thought traces increase inference cost and latency, making efficiency critical for practical deployment. We present Apriel-Reasoner, trained with a reproducible multi-domain RL post-training recipe on Apriel-Base, a 15B-parameter open-weight LLM, across five domains using public datasets: mathematics, code generation, instruction following, logical puzzles, and function calling. We introduce adaptive domain sampling that preserves target completed-rollout ratios despite heterogeneous rollout dynamics, a
    
[^639]: 多项式群卷积神经网络的几何学

    The Geometry of Polynomial Group Convolutional Neural Networks

    [https://arxiv.org/abs/2603.29566](https://arxiv.org/abs/2603.29566)

    本文基于分次群代数为多项式群卷积神经网络提出了新的数学框架，给出了 Hadamard 和 Kronecker 两种自然参数化方法，并证明了其神经流形维度仅取决于网络层数和群的大小。

    

    我们研究了针对任意有限群 $G$ 的多项式群卷积神经网络（PGCNNs）。特别地，我们使用分次群代数的语言为 PGCNNs 引入了一个新的数学框架。该框架基于 Hadamard 积和 Kronecker 积给出了该架构的两种自然参数化方法，二者通过一个线性映射相互关联。我们计算了相关神经流形的维度，验证了其仅取决于网络层数和群的大小。我们还描述了 Kronecker 参数化在正则群作用和重缩放意义下的一般纤维，并对 Hadamard 参数化提出了类似的猜想。该猜想得到了针对小群和浅层网络的显式计算的支持。

    arXiv:2603.29566v2 Announce Type: replace  Abstract: We study polynomial group convolutional neural networks (PGCNNs) for an arbitrary finite group $G$. In particular, we introduce a new mathematical framework for PGCNNs using the language of graded group algebras. This framework yields two natural parametrizations of the architecture, based on Hadamard and Kronecker products, related by a linear map. We compute the dimension of the associated neuromanifold, verifying that it depends only on the number of layers and the size of the group. We also describe the general fiber of the Kronecker parametrization up to the regular group action and rescaling, and conjecture the analogous description for the Hadamard parametrization. Our conjecture is supported by explicit computations for small groups and shallow networks.
    
[^640]: 面向无反向传播持续测试时自适应的子空间优化方法

    Subspace Optimization for Backpropagation-Free Continual Test-Time Adaptation

    [https://arxiv.org/abs/2603.28678](https://arxiv.org/abs/2603.28678)

    PACE利用带Fastfood投影的协方差矩阵自适应进化策略，在低维子空间中优化归一化层的高维仿射参数，实现了无需反向传播的高效持续测试时自适应，并通过自适应停止准则和域专用向量库将运行时间减少50%以上。

    

    我们提出了PACE，一个无反向传播的持续测试时自适应系统，它直接优化归一化层的仿射参数。现有的无导数方法难以在运行效率与学习能力之间取得平衡，因为它们要么将更新限制在输入提示词上，要么无论域是否稳定都需要持续进行资源密集型的自适应。为了解决这些局限性，PACE利用带Fastfood投影的协方差矩阵自适应进化策略（CMA-ES）在低维子空间中优化高维仿射参数，从而实现卓越的自适应性能。此外，我们通过引入自适应停止准则和域专用向量库来消除冗余计算，从而提升运行时效率。我们的框架在持续分布偏移下的多个基准测试中取得了最先进的准确率，运行时间减少了50%以上。

    arXiv:2603.28678v2 Announce Type: replace  Abstract: We introduce PACE, a backpropagation-free continual test-time adaptation system that directly optimizes the affine parameters of normalization layers. Existing derivative-free approaches struggle to balance runtime efficiency with learning capacity, as they either restrict updates to input prompts or require continuous, resource-intensive adaptation regardless of domain stability. To address these limitations, PACE leverages the Covariance Matrix Adaptation Evolution Strategy with the Fastfood projection to optimize high-dimensional affine parameters within a low-dimensional subspace, leading to superior adaptive performance. Furthermore, we enhance the runtime efficiency by incorporating an adaptation stopping criterion and a domain-specialized vector bank to eliminate redundant computation. Our framework achieves state-of-the-art accuracy across multiple benchmarks under continual distribution shifts, reducing runtime by over 50% c
    
[^641]: 基于可微分有限元与结构保持神经网络的超弹性本构模型发现

    Hyperelastic constitutive model discovery with differentiable finite elements and structure-preserving neural networks

    [https://arxiv.org/abs/2603.26517](https://arxiv.org/abs/2603.26517)

    该论文提出一种将非线性有限元平衡问题直接嵌入学习循环的可微分有限元框架，结合结构保持神经网络，仅从边界位移和整体反力等部分观测数据中即可发现物理上合理的超弹性本构模型。

    

    从实验可获得的测量数据中发现本构规律是非线性计算力学中的一个核心问题。许多数据驱动的本构识别方法要么依赖于成对的应变-应力数据，要么依赖于全场位移测量，而这两者在现实的三维场景中都难以获取。我们提出了一个可微分有限元框架，用于从部分观测数据中发现超弹性材料规律，这些观测数据包括仅边界处的位移测量和整体反力。该方法将非线性有限元平衡问题直接嵌入学习循环中，使得候选应变能密度可以通过其诱导的变形场和反力来评估。这一表述将力学平衡作为约束条件加以强制执行，并允许损失函数仅在观测位置进行评估。为确保物理可容许性

    arXiv:2603.26517v2 Announce Type: replace-cross  Abstract: The discovery of constitutive laws from experimentally accessible measurements is a central problem in nonlinear computational mechanics. Many data-driven constitutive identification approaches rely either on paired strain-stress data or on full-field displacement measurements, both of which are difficult to obtain in realistic three-dimensional settings. We present a differentiable finite element framework for the discovery of hyperelastic material laws from partial observations, including boundary-only displacement measurements and global reaction forces. The method embeds the nonlinear finite element equilibrium problem directly into the learning loop, so that candidate strain-energy densities are assessed through the deformation fields and reactions they induce. This formulation enforces mechanical equilibrium as a constraint and allows the loss function to be evaluated only at observed locations. To ensure physical admissi
    
[^642]: EcoFair：数据退化下面向边缘AI的能效推理路由

    EcoFair: Energy-Efficient Inference Routing for Edge AI under Data Degradation

    [https://arxiv.org/abs/2603.26483](https://arxiv.org/abs/2603.26483)

    EcoFair提出了一种垂直分区推理框架，让边缘设备保留原始图像和表格数据、仅传输轻量的模态特定表示进行服务器端融合，从而在数据退化场景下平衡皮肤病分类的能耗与诊断可靠性。

    

    医疗边缘AI系统必须在一种困难的张力下运行：一方面要提供可靠的诊断推理，另一方面设备又受限于电池容量、内存和计算能力。在皮肤病学领域，这一问题因现实世界的图像退化而进一步加剧，包括智能手机拍摄、光线不足、模糊、压缩以及异构边缘传感器造成的影响。为处理这些退化输入，部署大型模型可以提高可靠性，但会迅速加重资源受限设备的能量负担；相反，始终使用轻量级模型虽然节省能量，但在模糊或退化的输入上可靠性可能较低。本文提出了EcoFair，一个面向皮肤病分类的垂直分区推理框架，其中图像和表格输入保留在边缘客户端本地，仅传输学习到的模态特定表示用于服务器端融合。EcoFair首先使用轻量级模型处理每个样本（摘要在此处截断）

    arXiv:2603.26483v2 Announce Type: replace  Abstract: Medical edge-AI systems must operate under a difficult tension: delivering reliable diagnostic inference while running on devices with limited battery capacity, memory, and compute. In dermatology, this problem is amplified by real-world image degradation caused by smartphone capture, poor lighting, blur, compression, and heterogeneous edge sensors. To handle these degraded inputs, deploying a heavyweight model can improve reliability, but it rapidly increases the energy burden on resource-constrained devices. Conversely, always using a lightweight model saves energy but may be less reliable on ambiguous or degraded inputs. This paper introduces EcoFair, a vertically partitioned inference framework for dermatology classification in which image and tabular inputs remain local to edge clients while only learned modality-specific representations are transmitted for server-side fusion. EcoFair first processes each sample using a lightwei
    
[^643]: 神经网络中同时实现泛化与记忆的规则-事实模型

    The Rules-and-Facts Model for Simultaneous Generalization and Memorization in Neural Networks

    [https://arxiv.org/abs/2603.25579](https://arxiv.org/abs/2603.25579)

    本文提出规则-事实模型，将师生泛化框架与Gardner容量分析相结合，从理论上刻画了神经网络何时能够同时泛化底层规则并记忆例外事实，并量化了过参数化在实现这一双重能力中的作用。

    

    现代神经网络的一个关键能力是能够同时学习底层规则并记忆特定的事实或例外。然而，对这种双重能力的理论理解仍然有限。我们提出了规则-事实模型，这是一个最小可解的设定，通过连接统计学习物理中的两条经典研究路线——用于泛化的师生框架和用于记忆的Gardner式容量分析——来精确刻画这一现象。在RAF模型中，训练标签中有比例为 $1-\varepsilon$ 的部分由结构化的教师规则生成，而比例为 $\varepsilon$ 的部分由具有随机标签的非结构化事实构成。我们刻画了学习器何时能够同时恢复底层规则（从而实现对新数据的泛化）并记忆非结构化的样本。我们的结果量化了过参数化如何同时实现这一双重能力。

    arXiv:2603.25579v2 Announce Type: replace-cross  Abstract: A key capability of modern neural networks is their capacity to simultaneously learn underlying rules and memorize specific facts or exceptions. Yet, theoretical understanding of this dual capability remains limited. We introduce the Rules-and-Facts (RAF) model, a minimal solvable setting that enables precise characterization of this phenomenon by bridging two classical lines of work in the statistical physics of learning: the teacher-student framework for generalization and Gardner-style capacity analysis for memorization. In the RAF model, a fraction $1 - \varepsilon$ of training labels is generated by a structured teacher rule, while a fraction $\varepsilon$ consists of unstructured facts with random labels. We characterize when the learner can simultaneously recover the underlying rule - allowing generalization to new data - and memorize the unstructured examples. Our results quantify how overparameterization enables the si
    
[^644]: 用于多月份轨迹异常检测的高光谱轨迹图像

    Hyperspectral Trajectory Image for Multi-Month Trajectory Anomaly Detection

    [https://arxiv.org/abs/2603.25255](https://arxiv.org/abs/2603.25255)

    提出TITAnD方法，通过将轨迹表示为高光谱轨迹图像（HTI）把轨迹异常检测转化为视觉问题，首次实现了多月份密集GPS轨迹的异常检测，并统一了密集与稀疏两种场景以实现知识迁移。

    

    轨迹异常检测支撑着从欺诈检测到城市出行分析等多种应用。密集GPS方法能够保留细粒度证据，例如异常速度和短时事件，但其二次方计算代价使得多月分析难以实现；因此，目前尚无现有方法能够在多月份的密集GPS轨迹上检测异常。该领域转而依赖可扩展的稀疏停留点方法，但这类方法会丢弃上述证据，导致每种场景都需要单独的架构，并阻碍了知识迁移。密集与稀疏轨迹共享一个沿“日内”与“跨天”两个轴的二维循环结构。因此，我们提出了TITAnD（用于异常检测的轨迹图像Transformer），通过将轨迹表示为高光谱轨迹图像（HTI）——一个由“天×一天内时刻”构成的网格，其通道编码空间、语义、时间等信息——将轨迹异常检测重新表述为一个视觉问题。（注：原摘要此处被截断）

    arXiv:2603.25255v2 Announce Type: replace-cross  Abstract: Trajectory anomaly detection underpins applications from fraud detection to urban mobility analysis. Dense GPS methods preserve fine-grained evidence such as abnormal speeds and short-duration events, but their quadratic cost makes multi-month analysis intractable; consequently, no existing approach detects anomalies over multi-month dense GPS trajectories. The field instead relies on scalable sparse stay-point methods that discard this evidence, forcing separate architectures for each regime and preventing knowledge transfer. Dense and sparse trajectories share a two-dimensional cyclic structure along within-day and across-day axes. We therefore propose TITAnD (Trajectory Image Transformer for Anomaly Detection), which reformulates trajectory anomaly detection as a vision problem by representing trajectories as a Hyperspectral Trajectory Image (HTI): a day x time-of-day grid whose channels encode spatial, semantic, temporal, a
    
[^645]: 基于等变神经网络的关联离散选择模型的摊销推断

    Amortized Inference for Correlated Discrete Choice Models via Equivariant Neural Networks

    [https://arxiv.org/abs/2603.24705](https://arxiv.org/abs/2603.24705)

    本文提出一种基于等变神经网络架构的摊销推断方法，可为具有相关误差的一般离散选择模型快速逼近选择概率与似然函数，突破了传统Logit模型对随机效用分布的严格假设限制。

    

    离散选择模型是管理科学、经济学和市场营销中用于理解和预测决策行为的基础工具。基于Logit的模型在实际应用中占据主导地位，这主要归功于其选择概率具有便捷的闭式表达式。然而，这些模型对随机效用成分施加了严格的假设，限制了模型捕捉现实替代模式的能力。我们提出了一种摊销推断方法，该方法依赖神经网络模拟器来逼近一般误差分布（包括误差相关的分布）下的选择概率。我们设计了一种专门的神经网络架构，使其符合离散选择模型的等变性/不变性性质，并为该架构提供了群论基础，包括基于最小不变特征集的万能逼近定理证明。模型训练完成后，该模拟器能够实现快速的似然评估（摘要原文此处截断）。

    arXiv:2603.24705v4 Announce Type: replace-cross  Abstract: Discrete choice models are fundamental tools in management science, economics, and marketing for understanding and predicting decision-making. Logit-based models are dominant in applied work, largely due to their convenient closed-form expressions for choice probabilities. However, they impose restrictive assumptions on the stochastic utility component, constraining our ability to capture realistic substitution patterns. We propose an amortized inference approach that relies on a neural network emulator to approximate choice probabilities for general error distributions, including those with correlated errors. We develop a specialized neural network architecture designed to respect the invariance properties of discrete choice models. We provide group-theoretic foundations for the architecture, including a proof of universal approximation given a minimal set of invariant features. Once trained, the emulator enables rapid likelih
    
[^646]: DSPA：面向数据高效偏好对齐的动态稀疏自编码器引导方法

    DSPA: Dynamic SAE Steering for Data-Efficient Preference Alignment

    [https://arxiv.org/abs/2603.21461](https://arxiv.org/abs/2603.21461)

    DSPA是一种推理时偏好对齐方法，通过提示条件化的稀疏自编码器引导在不更新模型权重的情况下实现偏好对齐，在保持性能的同时将对齐阶段计算量最多降低4.47倍。

    

    偏好对齐通常通过在偏好数据上进行权重更新的训练来实现，这种方式会增加大量的对齐阶段计算，且机制可解释性有限。我们提出了用于偏好对齐的动态SAE引导方法（DSPA），这是一种推理时方法，使稀疏自编码器（SAE）引导具备提示条件性。DSPA从偏好三元组中计算出条件差分映射，将提示特征与生成控制特征关联起来；在解码过程中，它仅修改令牌激活的潜在变量，无需更新基础模型权重。在Gemma-2-2B/9B和Qwen3-8B模型上，DSPA提升了MT-Bench成绩，在AlpacaEval上具有竞争力，同时保持了多项选择任务的准确率。在偏好数据受限的情况下，DSPA依然保持稳健，能够与两阶段RAHF-SCIT流程相媲美，同时所需的对齐阶段FLOPs最多减少4.47倍。最后，我们审计了DSPA所修改的SAE特征，发现偏好方向主要由……

    arXiv:2603.21461v2 Announce Type: replace-cross  Abstract: Preference alignment is usually achieved by weight-updating training on preference data, which adds substantial alignment-stage compute and provides limited mechanistic visibility. We propose Dynamic SAE Steering for Preference Alignment (DSPA), an inference-time method that makes sparse autoencoder (SAE) steering prompt-conditional. From preference triples, DSPA computes a conditional-difference map linking prompt features to generation-control features; during decoding, it modifies only token-active latents, without base-model weight updates. Across Gemma-2-2B/9B and Qwen3-8B, DSPA improves MT-Bench and is competitive on AlpacaEval while preserving multiple-choice accuracy. Under restricted preference data, DSPA remains robust and can rival the two-stage RAHF-SCIT pipeline while requiring up to $4.47\times$ fewer alignment-stage FLOPs. Finally, we audit the SAE features DSPA modifies, finding that preference directions are do
    
[^647]: 使用扩散模型从离散分布中生成数据：来自随机约束满足问题的洞察

    Generating from Discrete Distributions Using Diffusions: Insights from Random Constraint Satisfaction Problems

    [https://arxiv.org/abs/2603.20589](https://arxiv.org/abs/2603.20589)

    本研究以随机k-SAT问题作为生成模型基准，发现连续扩散模型优于掩码离散扩散模型，学习到的扩散模型可达到理论理想精度，且智能变量排序能显著提升生成精度。

    

    从离散分布中生成数据对于文本、表格数据和基因组数据等众多应用领域都至关重要。近期有多个研究团队使用随机k-可满足性（k-SAT）问题作为新式生成技术的合成基准。在本文中，我们表明随机约束满足问题理论中的基本洞察对生成技术在此类基准上的行为具有可观察的影响（有时甚至与直觉相悖）。更确切地说，我们研究了为给定的（随机）k-SAT或k-XORSAT公式生成均匀随机解的问题。在诸多发现中，我们观察到：(i) 连续扩散模型优于掩码离散扩散模型；(ii) 学习到的扩散模型可以达到理论上“理想”的精度；(iii) 对变量进行智能排序可以显著提升精度，尽管这种排序并非遵循流行的启发式方法。

    arXiv:2603.20589v2 Announce Type: replace  Abstract: Generating data from discrete distributions is important for a number of application domains including text, tabular data, and genomic data. Several groups have recently used random $k$-satisfiability ($k$-SAT) as a synthetic benchmark for new generative techniques. In this paper, we show that fundamental insights from the theory of random constraint satisfaction problems have observable implications (sometime contradicting intuition) on the behavior of generative techniques on such benchmarks. More precisely, we study the problem of generating a uniformly random solution of a given (random) $k$-SAT or $k$-XORSAT formula. Among other findings, we observe that: $(i)$~Continuous diffusions outperform masked discrete diffusions; $(ii)$~Learned diffusions can match the theoretical `ideal' accuracy; $(iii)$~Smart ordering of the variables can significantly improve accuracy, although not following popular heuristics.
    
[^648]: FILT3R：用于流式三维重建的潜在状态自适应卡尔曼滤波器

    FILT3R: Latent State Adaptive Kalman Filter for Streaming 3D Reconstruction

    [https://arxiv.org/abs/2603.18493](https://arxiv.org/abs/2603.18493)

    FILT3R提出了一种无需训练的潜在滤波层，将循环状态更新建模为token空间中的随机状态估计，通过在线估计过程噪声并计算卡尔曼风格增益，自适应平衡记忆保持与新观测，从而解决流式三维重建中状态更新不稳定的问题。

    

    流式三维重建维护一个持久的潜在状态，该状态从传入的帧中在线更新，从而实现恒定内存推理。一个关键的失效模式在于状态更新规则：激进的覆盖会遗忘有用的历史信息，而保守的更新则无法跟踪新的证据，并且这两种行为在超出训练范围后都会变得不稳定。为了应对这一挑战，我们提出了FILT3R，这是一种无需训练的潜在滤波层，它将循环状态更新建模为token空间中的随机状态估计。FILT3R为每个token维护一个方差，并计算卡尔曼风格的增益，以自适应地平衡记忆保持与新观测之间的关系。过程噪声——即控制潜在状态在帧之间预期变化程度的参数——通过候选token的EMA归一化时间漂移在线估计。通过大量实验，我们证明FILT3R提供了一个可解释的、即插即用的更新规则，该规则能够……

    arXiv:2603.18493v2 Announce Type: replace-cross  Abstract: Streaming 3D reconstruction maintains a persistent latent state that is updated online from incoming frames, enabling constant-memory inference. A key failure mode is the state update rule: aggressive overwrites forget useful history, while conservative updates fail to track new evidence, and both behaviors become unstable beyond the training horizon. To address this challenge, we propose FILT3R, a training-free latent filtering layer that casts recurrent state updates as stochastic state estimation in token space. FILT3R maintains a per-token variance and computes a Kalman-style gain that adaptively balances memory retention against new observations. Process noise -- governing how much the latent state is expected to change between frames -- is estimated online from EMA-normalized temporal drift of candidate tokens. Using extensive experiments, we demonstrate that FILT3R yields an interpretable, plug-in update rule that genera
    
[^649]: 通过潜在逻辑增强、鲁棒降噪与混合奖励建模适配技术服务类LLM智能体

    Adapting Technical-Service LLM Agents with Latent Logic Augmentation, Robust Noise Reduction, and Hybrid Reward Modeling

    [https://arxiv.org/abs/2603.18074](https://arxiv.org/abs/2603.18074)

    本文提出一种耦合潜在逻辑增强、鲁棒降噪与混合奖励建模的后训练框架，通过规划感知轨迹建模增广微调数据、构建双重过滤多重真值、并结合重排序器与LLM评审的混合奖励进行策略训练，使适配后的Qwen3-4B在真实云技术服务任务上取得最优表现且显著降低奖励成本。

    

    技术服务类LLM智能体正在进入生产工作流程，其价值取决于工程师是否采纳所生成的回复。服务工单隐藏了决策逻辑，包含带噪声的单一参考响应，且奖励评估成本高昂，这使得标准的后训练方法变得脆弱。现有的后训练和LLM-as-a-Judge（大模型作为评审）方法虽然改进了接地性或反馈，但并未联合建模潜在决策逻辑、响应多样性与奖励成本。我们通过耦合潜在逻辑增强、鲁棒降噪与混合奖励建模来填补这一空白。该框架通过规划感知轨迹建模与推理增强来增广监督微调数据，构建双重过滤的多重真值，并采用结合重排序器与LLM-as-a-Judge的混合奖励来训练策略。在真实的云技术服务任务上，适配后的Qwen3-4B取得了最高的Multi-ECS分数（0.441）、更低的奖励成本，并且……

    arXiv:2603.18074v2 Announce Type: replace-cross  Abstract: Technical-service LLM agents are entering production workflows, where value depends on whether engineers adopt generated replies. Service tickets hide decision logic, contain noisy single-reference responses, and make reward evaluation costly, making standard post-training brittle. Existing post-training and LLM-as-a-Judge approaches improve grounding or feedback, but do not jointly model latent decision logic, response diversity, and reward cost. We address this gap by coupling latent logic augmentation, robust noise reduction, and hybrid reward modeling. The framework augments supervised fine-tuning data with Planning-Aware Trajectory Modeling and Reasoning Augmentation, builds dual-filtered Multiple Ground Truths, and trains the policy with a hybrid reward that combines a Reranker with an LLM-as-a-Judge. On real Cloud technical-service tasks, the adapted Qwen3-4B achieves the highest Multi-ECS (0.441), lower reward cost, and
    
[^650]: 通过逆向事故概率建模实现实时驾驶员安全评分

    Real-Time Driver Safety Scoring Through Inverse Crash Probability Modeling

    [https://arxiv.org/abs/2603.14841](https://arxiv.org/abs/2603.14841)

    本文提出SafeDriver-IQ框架，通过融合全国事故统计数据与自动驾驶车辆自然驾驶数据，将二元事故分类器转化为连续的0-100分驾驶员安全评分，从而实现对驾驶员的实时安全反馈。

    

    道路交通事故仍然是可预防死亡的主要原因之一。现有的预测模型主要产生二元结果，这对实时驾驶员反馈提供的可操作洞察十分有限。这些方法通常缺乏连续的风险量化、可解释性，以及对易受伤道路使用者（VRU，如行人和骑行者）的明确考虑。本研究提出了SafeDriver-IQ框架，通过将全国事故统计数据与自动驾驶车辆的自然驾驶数据相结合，将二元事故分类器转换为连续的0-100分安全评分。该框架融合了美国国家公路交通安全管理局（NHTSA）的事故记录与Waymo开放运动数据集场景，设计了基于领域知识驱动的特征，并引入了一个基于交通安全文献的校准层。在15项互补分析中的评估表明，该框架能够可靠地区分……

    arXiv:2603.14841v4 Announce Type: replace-cross  Abstract: Road crashes remain a leading cause of preventable fatalities. Existing prediction models predominantly produce binary outcomes, which offer limited actionable insights for realtime driver feedback. These approaches often lack continuous risk quantification, interpretability, and explicit consideration of vulnerable road users (VRUs), such as pedestrians and cyclists. This research introduces SafeDriver-IQ, a framework that transforms binary crash classifiers into continuous 0-100 safety scores by combining national crash statistics with naturalistic driving data from autonomous vehicles. The framework fuses National Highway Traffic Safety Administration (NHTSA) crash records with Waymo Open Motion Dataset scenarios, engineers domain-informed features, and incorporates a calibration layer grounded in transportation safety literature. Evaluation across 15 complementary analyses indicates that the framework reliably differentiate
    
[^651]: 混合性使马尔可夫上下文在线性老虎机中变得廉价

    Mixing Makes Markovian Contexts Cheap for Linear Bandits

    [https://arxiv.org/abs/2603.12530](https://arxiv.org/abs/2603.12530)

    该论文将“上下文廉价”归约扩展到马尔可夫上下文过程的线性老虎机问题，证明在一致几何遍历性（混合性）条件下，可通过构造平稳替代动作集并使用标准线性老虎机算法求解时间相关的上下文问题。

    

    arXiv:2603.12530v3 公告类型：替换。摘要：近期研究表明，当上下文是独立同分布抽取时，线性上下文老虎机问题可以归约为单上下文的线性老虎机问题。这种“上下文是廉价的”视角极具优势，因为它支持更精细的有限时间分析，并能借助线性老虎机文献中的成熟技术，例如处理模型误设和对抗性污染的技术。然而，这一归约关键地依赖于上下文的独立性，无法扩展到上下文具有时间相关性（如马尔可夫性）的情形，而这种情况在实践中频繁出现。受上下文以时间相关方式出现的应用所启发，我们将这一视角扩展到具有马尔可夫上下文过程的线性老虎机问题，其中动作集通过一个外生马尔可夫链进行演化。我们的主要贡献是一个在一致几何遍历性条件下成立的归约。我们构造了一个平稳的替代动作集，从而能够使用标准的线性老虎机……

    arXiv:2603.12530v3 Announce Type: replace  Abstract: Recent work shows that when contexts are drawn i.i.d., linear contextual bandits can be reduced to single-context linear bandits. This ``contexts are cheap'' perspective is highly advantageous, as it allows for sharper finite-time analyses and leverages mature techniques from the linear bandit literature, such as those for misspecification and adversarial corruption. However, this reduction crucially relies on the independence of contexts and does not extend to settings with temporally correlated (e.g., Markovian) contexts, which arise frequently in practice. Motivated by applications with temporally correlated availability, we extend this perspective to linear bandits with Markovian context processes, where the action set evolves via an exogenous Markov chain. Our main contribution is a reduction that applies under uniform geometric ergodicity. We construct a stationary surrogate action set to solve the problem using a standard line
    
[^652]: AI驱动的交通流模式与土地利用交互的时空异质性：基于GeoAI的多模式城市出行分析

    Spatiotemporal Heterogeneity of AI-Driven Traffic Flow Patterns and Land Use Interaction: A GeoAI-Based Analysis of Multimodal Urban Mobility

    [https://arxiv.org/abs/2603.05581](https://arxiv.org/abs/2603.05581)

    本研究提出一种依次整合MGWR、随机森林与时空图卷积网络的GeoAI混合框架，有效刻画了机动车、公共交通和主动出行三种模式下城市交通流的时空异质性及其与土地利用的交互作用。

    

    城市交通流受土地利用配置与时空异质性出行需求之间复杂非线性相互作用的支配。传统的全局回归模型和时间序列模型无法同时捕捉多种出行方式下的多尺度动态。本研究提出一种GeoAI混合分析框架，依次整合多尺度地理加权回归（MGWR）、随机森林（RF）和时空图卷积网络（ST-GCN），以建模机动车、公共交通和主动出行三种交通方式下交通流模式的时空异质性及其与土地利用的交互作用。将该框架应用于跨越六座城市、涵盖两种对比鲜明城市形态、包含350个交通分析区的实证校准数据集后，得出四项关键发现：（i）GeoAI混合模型实现了0……

    arXiv:2603.05581v2 Announce Type: replace-cross  Abstract: Urban traffic flow is governed by the complex, nonlinear interaction between land use configuration and spatiotemporally heterogeneous mobility demand. Conventional global regression and time-series models cannot simultaneously capture these multi-scale dynamics across multiple travel modes. This study proposes a GeoAI Hybrid analytical framework that sequentially integrates Multiscale Geographically Weighted Regression (MGWR), Random Forest (RF), and Spatio-Temporal Graph Convolutional Networks (ST-GCN) to model the spatiotemporal heterogeneity of traffic flow patterns and their interaction with land use across three mobility modes: motor vehicle, public transit, and active transport. Applying the framework to an empirically calibrated dataset of 350 traffic analysis zones across six cities spanning two contrasting urban morphologies, four key findings emerge: (i) the GeoAI Hybrid achieves a root mean squared error (RMSE) of 0
    
[^653]: FuseDiff：面向双靶点结构药物设计的保持对称性的联合扩散模型

    FuseDiff: Symmetry-Preserving Joint Diffusion for Dual-Target Structure-Based Drug Design

    [https://arxiv.org/abs/2603.05567](https://arxiv.org/abs/2603.05567)

    FuseDiff是一个端到端的对称性保持扩散模型，能够以两个靶点口袋为条件，联合生成配体分子及其对应的两个口袋特异性结合构象，从而实现双靶点结构药物设计。

    

    双靶点结构药物设计旨在生成单一配体分子及其与两个口袋特异性结合的构象，每个构象都与相应的靶点口袋兼容，从而实现疗效更佳、耐药性更低的多药理学治疗。现有方法通常依赖分阶段的流水线，要么通过条件独立性假设将两个结合构象解耦，要么强制施加过于刚性的相关性约束，因此无法联合生成两种靶点特异性的结合模式。为解决这一问题，我们提出FuseDiff，一个端到端的扩散模型，它以两个口袋为条件，联合生成配体分子图以及两个口袋特异性的结合构象。FuseDiff采用带有双靶点局部上下文融合模块的消息传递骨干网络，融合每个配体原子来自两个口袋的局部上下文信息，在保持所需对称性的同时实现富有表现力的联合建模。配合显式键……

    arXiv:2603.05567v2 Announce Type: replace  Abstract: Dual-target structure-based drug design aims to generate a single ligand together with two pocket-specific binding poses, each compatible with a corresponding target pocket, enabling polypharmacological therapies with improved efficacy and reduced resistance. Existing approaches typically rely on staged pipelines, which either decouple the two poses via conditional-independence assumptions or enforce overly rigid correlations, and therefore fail to jointly generate two target-specific binding modes. To address this, we propose FuseDiff, an end-to-end diffusion model that jointly generates a ligand molecular graph and two pocket-specific binding poses conditioned on both pockets. FuseDiff features a message-passing backbone with Dual-target Local Context Fusion (DLCF), which fuses each ligand atom's local context from both pockets to enable expressive joint modeling while preserving the desired symmetries. Together with explicit bond 
    
[^654]: TimeWarp：通过重访过去来评估网页智能体

    TimeWarp: Evaluating Web Agents by Revisiting the Past

    [https://arxiv.org/abs/2603.04949](https://arxiv.org/abs/2603.04949)

    提出了模拟网页UI演变的TimeWarp基准测试，揭示现有网页智能体对界面变化的脆弱性，并通过TimeTraj标注方法与BC变体训练显著提升了智能体跨网页版本的泛化能力。

    

    随着网页智能体在基准测试中逐渐缩小与人类的差距，一个问题随之而来：今天的智能体在明天的网页上是否也能表现得同样出色？我们提出了TimeWarp，一个模拟不断演变的网页环境的基准测试。TimeWarp包含三个网页环境，每个环境有六个UI版本，涵盖了互联网不同时代的UI设计、前端代码和工作流程。我们为TimeWarp配套了一系列复杂、真实的任务，涵盖不同形式的网页导航。我们的实验表明，基于视觉的智能体容易受到网页变化的影响，而基于文本的智能体一旦在单一版本上微调就会变得脆弱。为了解决这个问题，我们提出了TimeTraj，一种新的标注方法，通过计划蒸馏来收集跨多个版本的轨迹。通过使用我们的BC变体方法在教师rollout上训练智能体，我们取得了显著的性能提升：Qwen-3 4B模型从20.4%提升到37.7%，Llama-3.1 8B模型从0%提升到27.0%。

    arXiv:2603.04949v2 Announce Type: replace  Abstract: As web agents close the gap with humans on benchmarks, one question arises: Do today's agents perform just as well on tomorrow's web? We introduce TimeWarp, a benchmark that emulates the evolving web. TimeWarp consists of three web environments, each with six UI versions spanning UI design, frontend code, and workflows from different eras of the internet. We pair TimeWarp with a set of complex, realistic tasks covering different forms of web navigation. Our experiments reveal that vision-based agents are vulnerable to changes, while text-based agents become brittle once fine-tuned on a single version. To address this, we propose TimeTraj, a new annotation method that uses plan distillation to collect trajectories across multiple versions. By training agents on teacher rollouts using our BC-variant, we achieve substantial performance gains: 20.4% to 37.7% for Qwen-3 4B and 0% to 27.0% for Llama-3.1 8B models. Our work helps study gene
    
[^655]: 时序一致性提升情境式离线元强化学习的泛化能力

    Temporal Consistency Improves Generalization in Contextual Offline Meta Reinforcement Learning

    [https://arxiv.org/abs/2603.02935](https://arxiv.org/abs/2603.02935)

    该论文提出在潜在空间中强制多步预测的时序一致性约束，使任务表示既能捕捉任务特定的动力学又能避免表示坍塌，从而提升情境式离线元强化学习对新任务的泛化能力。

    

    离线元强化学习旨在学习一个能够在线泛化到新的相关任务的策略。基于情境（context）的方法从转移历史中推断任务表示，然而在无监督的情况下学习有效的任务表示仍然具有挑战性。现有依赖对比学习的方法能够学习具有判别性的任务表示，但无法识别任务特定的动力学；而依赖重构的方法可能不足以建模长时程依赖关系，从而限制了对新任务的泛化能力。我们研究了潜在空间中的时序一致性对任务表示学习的影响，结果表明在潜在空间中强制执行多步预测能够促使任务表示捕捉任务相关的动力学，同时防止表示坍塌。我们提供了刻画价值估计误差来源的理论分析，并通过在MuJoCo上的大量实验验证了方法的有效性。

    arXiv:2603.02935v2 Announce Type: replace  Abstract: Offline meta-reinforcement learning seeks to learn a policy that generalizes to new related tasks online. Context-based methods infer a task representation from transition histories, yet learning an effective task representation without supervision remains challenging. Existing methods relying on contrastive learning learn discriminative task representations, but fail to identify task-specific dynamics, while relying on reconstruction can be insufficient to model long-horizon dependencies, limiting generalization to new tasks. We investigate the impact of temporal consistency in latent space on task representation learning, showing that enforcing multi-step predictions in latent space encourages task representations that are able to capture task-dependent dynamics while preventing representation collapse. We provide theoretical analysis characterizing sources of error in value estimation and show through extensive experiments on MuJo
    
[^656]: KDFlow：一个面向大语言模型的用户友好且高效的知识蒸馏框架

    KDFlow: A User-Friendly and Efficient Knowledge Distillation Framework for Large Language Models

    [https://arxiv.org/abs/2603.01875](https://arxiv.org/abs/2603.01875)

    KDFlow提出了一种解耦架构的大语言模型知识蒸馏框架，结合SGLang进行Teacher推理与PyTorch FSDP2进行Student优化，并通过Ray对象存储传输Teacher隐藏状态、在Student端重算Teacher logits来实现高效的全词表蒸馏。

    

    知识蒸馏（KD）被广泛用于压缩和后训练大语言模型（LLM），然而许多现有框架使用与Student优化相同的面向训练的后端来执行Teacher推理，导致效率欠佳。在本文中，我们提出了KDFlow，一个新颖的LLM蒸馏框架，其特点是采用解耦架构并使用SGLang进行Teacher推理。KDFlow将用于Teacher推理的SGLang与用于Student优化的PyTorch FSDP2相结合，使每个模型都能在适合其工作负载的后端上运行。为了在这种解耦架构下实现高效的全词表蒸馏，KDFlow通过Ray的对象存储传输Teacher的最终隐藏状态，并使用Teacher输出头的冻结副本在每个Student工作节点上重新计算Teacher的logits。此外，我们的框架同时支持离策略和在线蒸馏，并融合了跨分词器算法……

    arXiv:2603.01875v4 Announce Type: replace-cross  Abstract: Knowledge distillation (KD) is widely used to compress and post-train large language models (LLMs), yet many existing frameworks execute teacher inference with the same training-oriented backend as student optimization, leading to suboptimal efficiency. In this paper, we propose KDFlow, a novel framework for LLM distillation that features a decoupled architecture and employs SGLang for teacher inference. KDFlow combines SGLang for teacher inference with PyTorch FSDP2 for student optimization, allowing each model to run on a backend tailored to its workload. To enable efficient full-vocabulary distillation in this decoupled architecture, KDFlow transfers the teacher's final hidden states via Ray's object store and recomputes teacher logits on each student worker using a frozen copy of the teacher's output head. Furthermore, our framework supports both off-policy and on-policy distillation and incorporates cross-tokenizer algorit
    
[^657]: CARE：面向可靠大语言模型评估的混淆因子感知聚合框架

    CARE: Confounder-Aware Aggregation for Reliable LLM Evaluation

    [https://arxiv.org/abs/2603.00039](https://arxiv.org/abs/2603.00039)

    提出CARE框架，通过将LLM评审分数显式建模为潜在真实质量信号与共享混淆因子的叠加，在无需真实标签的情况下实现质量与混淆因子的分离，从而获得更可靠的大语言模型评估聚合结果。

    

    以大语言模型作为评审（LLM-as-a-judge）的集成系统是可扩展评估的标准范式，但其聚合机制存在一个根本性缺陷：它们隐含地假设各评审者对真实质量提供相互独立的估计。然而在实践中，LLM评审者会因共享的潜在混淆因子——例如冗长性、风格偏好或训练痕迹——而产生相关性错误，导致多数投票或平均等标准聚合规则收益甚微，甚至放大系统性错误。为解决这一问题，我们提出了CARE，一个混淆因子感知的聚合框架，它将LLM评审分数显式建模为来源于潜在的真实质量信号与共享的混淆因子。CARE并非启发式地对评审者重新加权，而是在无需真实标签的情况下实现质量与混淆因子的分离。我们提供了在共享混淆因子条件下的可辨识性保证与有限样本恢复的理论保证。

    arXiv:2603.00039v2 Announce Type: replace-cross  Abstract: LLM-as-a-judge ensembles are the standard paradigm for scalable evaluation, but their aggregation mechanisms suffer from a fundamental flaw: they implicitly assume that judges provide independent estimates of true quality. However, in practice, LLM judges exhibit correlated errors caused by shared latent confounders -- such as verbosity, stylistic preferences, or training artifacts -- causing standard aggregation rules like majority vote or averaging to provide little gain or even amplify systematic mistakes. To address this, we introduce CARE, a confounder-aware aggregation framework that explicitly models LLM judge scores as arising from both a latent true-quality signal and shared confounding factors. Rather than heuristically re-weighting judges, CARE separates quality from confounders without access to ground-truth labels. We provide theoretical guarantees for identifiability and finite-sample recovery under shared confoun
    
[^658]: 通过混沌量子扩散模型学习量子数据分布

    Learning Quantum Data Distribution via Chaotic Quantum Diffusion Model

    [https://arxiv.org/abs/2602.22061](https://arxiv.org/abs/2602.22061)

    该论文提出了一种通过混沌哈密顿量时间演化生成投影系综的混沌量子扩散模型，仅需全局时不变控制即可在多种模拟量子平台上大幅降低实现开销，同时保持与现有量子去噪扩散概率模型相当的精度。

    

    arXiv:2602.22061v3 公告类型：replace-cross 摘要：量子数据的生成模型面临重大挑战，但在化学信息学和量子物理等领域具有巨大潜力。量子去噪扩散概率模型（QuDDPMs）通过逐步扰乱和去噪量子态，能够高效地学习量子数据分布。然而，现有的实现通常依赖于基于线路的随机酉动力学，其实现成本高昂，且对控制缺陷十分敏感，尤其是在模拟量子硬件上。我们提出了混沌量子扩散模型，这是一个通过混沌哈密顿量时间演化来生成投影系综的框架，提供了一种灵活且与硬件兼容的扩散机制。我们的方法仅需要全局的、与时间无关的控制，因此在多种模拟量子平台上大幅降低了实现开销，同时达到了与QuDDPM相当的精度。该方法改进了……

    arXiv:2602.22061v3 Announce Type: replace-cross  Abstract: Generative models for quantum data pose significant challenges but hold immense potential in fields such as chemoinformatics and quantum physics. Quantum denoising diffusion probabilistic models (QuDDPMs) enable efficient learning of quantum data distributions by progressively scrambling and denoising quantum states. However, existing implementations typically rely on circuit-based random unitary dynamics, which can be costly to implement and sensitive to control imperfections, particularly on analog quantum hardware. We propose the chaotic quantum diffusion model, a framework that generates projected ensembles via chaotic Hamiltonian time evolution, providing a flexible and hardware-compatible diffusion mechanism. Requiring only global, time-independent control, our approach substantially reduces implementation overhead across diverse analog quantum platforms while achieving accuracy comparable to QuDDPMs. This method improves
    
[^659]: US-JEPA：一种面向超声影像的联合嵌入预测架构

    US-JEPA: A Joint Embedding Predictive Architecture for Ultrasound

    [https://arxiv.org/abs/2602.19322](https://arxiv.org/abs/2602.19322)

    US-JEPA提出了一种采用静态教师非对称潜在训练（SALT）目标的自监督框架，利用冻结的领域特定教师模型提供稳定的潜在目标，解决了超声影像低信噪比环境下的表征学习难题，同时避免了传统JEPA中在线教师模型超参数敏感和高计算开销的缺陷。

    

    arXiv:2602.19322v2 公告类型：replace-cross 摘要：超声成像因其固有噪声较大的采集过程，为表征学习带来了独特的挑战。低信噪比和随机散斑模式阻碍了依赖像素级重建目标的标准自监督学习方法。联合嵌入预测架构（JEPAs）通过预测被掩码的潜在表示而非原始像素来解决这一缺陷。然而，标准方法依赖于通过指数移动平均更新的在线教师模型，这类模型对超参数敏感且计算开销大。我们提出了US-JEPA，一种采用静态教师非对称潜在训练（SALT）目标的自监督框架。通过使用冻结的、特定领域的教师模型提供稳定的潜在目标，US-JEPA解耦了师生模型的优化过程，并推动学生模型在教师模型的语义先验基础上进行扩展。此外，我们还提供了首个严格的……（原文截断）

    arXiv:2602.19322v2 Announce Type: replace-cross  Abstract: Ultrasound (US) imaging poses unique challenges for representation learning due to its inherently noisy acquisition process. The low signal-to-noise ratio and stochastic speckle patterns hinder standard self-supervised learning methods relying on a pixel-level reconstruction objective. Joint-Embedding Predictive Architectures (JEPAs) address this drawback by predicting masked latent representations rather than raw pixels. However, standard approaches depend on hyperparameter-brittle and computationally expensive online teachers updated via exponential moving average. We propose US-JEPA, a self-supervised framework that adopts the Static-teacher Asymmetric Latent Training (SALT) objective. By using a frozen, domain-specific teacher to provide stable latent targets, US-JEPA decouples student-teacher optimization and pushes the student to expand upon the semantic priors of the teacher. In addition, we provide the first rigorous co
    
[^660]: 固定证据池下LLM重排序器行为的诊断研究

    Diagnosing LLM Reranker Behavior Under Fixed Evidence Pools

    [https://arxiv.org/abs/2602.18613](https://arxiv.org/abs/2602.18613)

    本文提出一种以Multi-News聚类为固定证据池的受控诊断方法，发现LLM重排序器在冗余模式和词汇覆盖方面与BM25、MMR基线行为显著不同，且不同LLM之间也表现出各异的冗余策略。

    

    标准的重排序评估研究的是重排序器如何对上游检索器返回的候选结果进行排序。这种设置将排序行为与检索质量耦合在一起，因此输出差异不能仅归因于排序策略本身。我们引入了一种使用Multi-News聚类作为固定证据池的重排序受控诊断方法。我们将每个证据池限制为八个文档，并向所有排序器传递相同的输入。在此设置中，BM25和MMR作为词汇匹配和多样性优化的可解释参考点。在345个聚类上的实验中，我们发现冗余模式因模型而异：一个LLM在较大的选择预算下会隐式地实现多样化，而另一个LLM则增加了冗余。相比之下，LLM在较小选择预算下的词汇覆盖率表现不佳。因此，LLM的排序结果与两个基线方法均存在显著差异，而非始终近似于其中任何一种策略。通过减少检索方差的干扰……

    arXiv:2602.18613v2 Announce Type: replace  Abstract: Standard reranking evaluations study how a reranker orders candidates returned by an upstream retriever. This setup couples ranking behavior with retrieval quality, so differences in output cannot be attributed to the ranking policy alone. We introduce a controlled diagnostic for reranking that uses Multi-News clusters as fixed evidence pools. We limit each pool to eight documents and pass identical inputs to all rankers. Within this setup, BM25 and MMR serve as interpretable reference points for lexical matching and diversity optimization. Across 345 clusters, we find that redundancy patterns vary by model: one LLM implicitly diversifies at larger selection budgets, while another increases redundancy. In contrast, LLMs underperform on lexical coverage at small selection budgets. As a result, LLM rankings diverge substantially from both baselines rather than consistently approximating either strategy. By reducing retrieval variance t
    
[^661]: MGD：矩引导扩散实现最大熵生成

    MGD: Moment Guided Diffusion for Maximum Entropy Generation

    [https://arxiv.org/abs/2602.17211](https://arxiv.org/abs/2602.17211)

    提出矩引导扩散（MGD），基于随机插值框架通过求解随机微分方程在有限时间内将矩引导至预设值，从而高效采样最大熵分布并避免高维慢混合问题。

    

    从有限信息中生成样本是科学各领域的一个基本问题。经典的最大熵方法能够基于矩约束提供有原则的不确定性量化，但需要通过MCMC或朗之万动力学进行采样，这通常在高维情形下会表现出指数级的减速。相比之下，基于扩散模型和流匹配的生成模型能够高效地将噪声传输至数据分布，但理论保证有限，且在数据稀缺时容易过拟合。我们提出了矩引导扩散（MGD），它结合了两种方法的优点。基于随机插值框架，MGD通过求解一个随机微分方程来采样最大熵分布，该方程在有限时间内将矩引导至预设值，从而避免了基于平衡态方法中的缓慢混合问题。在大波动性极限下，我们形式化地证明了MGD收敛于最大熵分布。

    arXiv:2602.17211v2 Announce Type: replace-cross  Abstract: Generating samples from limited information is a fundamental problem across scientific domains. Classical maximum entropy methods provide principled uncertainty quantification from moment constraints but require sampling via MCMC or Langevin dynamics, which typically exhibit exponential slowdown in high dimensions. In contrast, generative models based on diffusion and flow matching efficiently transport noise to data but offer limited theoretical guarantees and can overfit when data is scarce. We introduce Moment Guided Diffusion (MGD), which combines elements of both approaches. Building on the stochastic interpolant framework, MGD samples maximum entropy distributions by solving a stochastic differential equation that guides moments toward prescribed values in finite time, thereby avoiding slow mixing in equilibrium-based methods. We formally obtain, in the large-volatility limit, convergence of MGD to the maximum entropy dis
    
[^662]: LGQ：面向图像标记化的可学习几何量化

    LGQ: Learnable Geometric Quantization for Image Tokenization

    [https://arxiv.org/abs/2602.16086](https://arxiv.org/abs/2602.16086)

    提出可学习几何量化LGQ，通过保留可学习码本并结合温度退火与多样性和尖锐度两个正则化项，在无需EMA、重置启发式或重参数化的情况下有效防止码本坍缩，实现图像标记化的稳定训练。

    

    近年来的免坍缩量化器（如FSQ）通过用工程化的几何结构——即一个由码本大小K决定的固定标量网格——取代可学习码本来实现稳定训练。我们证明这种权衡是不必要的。我们提出了可学习几何量化（LGQ），它保留了可学习的码本，并通过温度退火实现从软分配到硬分配的过渡，同时由两个廉价的正则化项进行约束：一个按码本大小缩放的多样性项，用于惩罚批次平均使用过于集中的情况，这是抵抗坍缩的主要驱动力；另一个是尖锐度项，将每个token的软分配锐化为one-hot形式。两者共同防止码本坍缩，无需EMA更新、重置启发式或码本重参数化。在固定的VQ-GAN骨干网络下，我们在ImageNet 256x256上以K=16,384将LGQ与RotVQ、FSQ、LFQ、SimVQ和IBQ进行基准对比，并在K从4096到65,536的范围内对LGQ进行了扫描评估。

    arXiv:2602.16086v4 Announce Type: replace-cross  Abstract: Recent collapse-free quantizers such as FSQ achieve stable training by replacing the learnable codebook with an engineered geometry: a fixed scalar grid whose structure is dictated by the codebook size K. We show this trade-off is unnecessary. We introduce Learnable Geometric Quantization (LGQ), which retains a learnable codebook of codes and performs soft-to-hard assignment via temperature annealing, regularized by two cheap terms: a diversity term scaled by codebook size that penalizes concentrated batch-average usage is the primary driver of collapse resistance, complemented by a peakedness term that sharpens each token's soft-assignment toward one-hot; together they prevent codebook collapse without EMA, reset heuristics, or codebook reparameterization. Under a fixed VQ-GAN backbone, we benchmark LGQ against RotVQ, FSQ, LFQ, SimVQ, and IBQ on ImageNet 256x256 at K = 16,384, and sweep LGQ over K in {4096, ..., 65,536} withou
    
[^663]: 恒定步长随机逼近：有限时间收敛、高斯近似与尾界

    Constant-Stepsize Stochastic Approximation: Finite-Time Convergence, Gaussian Approximation, and Tail Bounds

    [https://arxiv.org/abs/2602.13960](https://arxiv.org/abs/2602.13960)

    该论文为独立同分布和马尔可夫噪声下的恒定步长随机逼近建立了显式的极限前理论刻画，证明了平稳分布的存在唯一性及几何Wasserstein收敛速率，并在 $\sqrt{\alpha}$ 波动尺度上推导出高阶定量高斯近似与尾界，从而量化了经典渐近结果无法捕捉的有限时间、有限步长误差。

    

    恒定步长随机逼近（SA）因其在计算效率方面的优势而在学习领域被广泛使用，然而其迭代点的分布通常难以解析处理。经典的渐近结果给出了 $X_k^{(\alpha)} \approx X^{(\alpha)} \approx x^\star+\sqrt{\alpha}Y$，其中 $X^{(\alpha)}$ 为稳态，$Y$ 为相应的高斯极限，这是通过令时间 $k\uparrow\infty$ 和步长 $\alpha\downarrow0$ 逐步逼近得到的。然而，这类极限结果并未量化有限时间、有限步长下的误差。我们针对具有独立同分布噪声和马尔可夫噪声的SA发展了一种显式的极限前刻画。我们建立了平稳分布的存在性与唯一性、以几何速率收敛到平稳态的Wasserstein收敛性，以及稳态到根 $x^\star$ 的几乎必然收敛和 $L^3$ 收敛，并确定了 $\sqrt{\alpha}$ 作为一阶波动的尺度。在该尺度上，我们推导了高阶定量高斯近似……

    arXiv:2602.13960v2 Announce Type: replace  Abstract: Constant-stepsize stochastic approximation (SA) is widely used in learning for computational efficiency, yet the distribution of the iterates is typically intractable. Classical asymptotics results give $X_k^{(\alpha)} \approx X^{(\alpha)} \approx x^\star+\sqrt{\alpha}Y$, where $X^{(\alpha)}$ is the steady state and $Y$ is an appropriate Gaussian limit, by progressively taking the time $k\uparrow\infty$ and stepsize $\alpha\downarrow0$. Such limit results, however, do not quantify finite-time, finite-stepsize errors.   We develop an explicit pre-limit characterization for SA with i.i.d.\ and Markovian noise. We establish existence and uniqueness of the stationary law, a geometric Wasserstein convergence to stationarity, and almost-sure and $L^3$ convergence of the steady state to the root $x^\star$, identifying the scale $\sqrt{\alpha}$ as first-order fluctuation. At this scale, we derive a higher-order quantitative Gaussian approxim
    
[^664]: 文本具有曲率

    Text Has Curvature

    [https://arxiv.org/abs/2602.13418](https://arxiv.org/abs/2602.13418)

    本文提出Texture——一种文本原生、词级的离散曲率信号，通过实证和理论证明自然语言文本确实具有内在曲率，并给出了该曲率的定义与检测方法。

    

    自然语言文本是否具有内在曲率？语言越来越多地被建模在弯曲几何中——用双曲空间表示层级结构，用混合曲率流形表示组合结构——然而一个基础科学问题仍未解决：曲率对文本本身而言意味着什么，并且这种意义是语言原生的，而非我们所选择的嵌入空间所带来的人为产物？我们认为文本确实具有曲率，并展示了如何检测、定义和使用它。为此，我们提出了Texture，一种文本原生的、词级离散曲率信号，并做出了三项贡献：（a）存在性：我们提供了实证和理论证明，表明自然语料库中的语义推断是非平坦的；（b）定义：我们将Texture定义为上下文中词信念场的带符号双轴曲率——即前缀与后缀之间调和作用的微分——通过去偏的薛定谔传输散度来度量（摘要在此处截断）。

    arXiv:2602.13418v2 Announce Type: replace  Abstract: Does natural language text have an intrinsic curvature? Language is increasingly modeled in curved geometries - hyperbolic spaces for hierarchy, mixed-curvature manifolds for compositional structure - yet a basic scientific question remains unresolved: what does curvature mean for text itself, in a way that is native to language rather than an artifact of the embedding space we choose? We argue that text does indeed have curvature, and show how to detect it, define it, and use it. To this end, we propose Texture, a text-native, word-level discrete curvature signal, and make three contributions. (a) Existence: We provide empirical and theoretical certificates that semantic inference in natural corpora is non-flat. (b) Definition: We define Texture as a signed two-axis curvature of the word-in-context belief field - the differential of reconciliation between prefix and suffix - measuring, via a debiased Schr\"odinger transport divergen
    
[^665]: 利用神经网络从数据中学习偏微分方程的函数分量

    Learning functional components of PDEs from data using neural networks

    [https://arxiv.org/abs/2602.13174](https://arxiv.org/abs/2602.13174)

    本文提出将神经网络嵌入偏微分方程框架的方法，能够从稳态观测数据中高精度地学习PDE中的未知函数分量（如相互作用核和外部势），并系统分析了影响重建精度的关键因素。

    

    偏微分方程（PDE）模型常常包含无法直接测量的未知函数项，这限制了其预测能力。虽然用于估计标量PDE参数的数据驱动方法已经相当成熟，但未知函数的恢复仍然相对缺乏探索。在本研究中，我们展示了标准的参数估计工作流程可以被扩展，以直接从数据中推断PDE的函数分量。我们的方法将神经网络嵌入到PDE框架中，使未知函数能够在训练过程中以高精度被学习。以非局部聚合-扩散方程作为案例研究，我们从稳态观测数据中推断出相互作用核和外部势。我们系统地研究了重建精度如何依赖于可用解的数量与多样性、采样密度以及测量噪声等因素。所得框架保留了原有方法的优势。

    arXiv:2602.13174v2 Announce Type: replace  Abstract: Partial differential equation (PDE) models frequently contain unknown functional terms that cannot be measured directly, limiting their predictive utility. While data-driven methods for estimating scalar PDE parameters are well established, the recovery of unknown functions remains comparatively underexplored. Here, we show that standard parameter estimation workflows can be extended to infer functional components of PDEs directly from data. Our approach embeds neural networks within the PDE framework, allowing unknown functions to be learned during training with high accuracy. Using nonlocal aggregation-diffusion equations as a case study, we infer interaction kernels and external potentials from steady-state observations. We systematically examine how reconstruction accuracy depends on factors such as the number and diversity of available solutions, sampling density, and measurement noise. The resulting framework retains the advant
    
[^666]: 优惠券营销中去偏CTR与增益的联合优化：一个统一的因果框架

    Jointly Optimizing Debiased CTR and Uplift for Coupons Marketing: A Unified Causal Framework

    [https://arxiv.org/abs/2602.12972](https://arxiv.org/abs/2602.12972)

    本文提出统一多值处理网络UniMVT，通过解耦混淆因素与处理敏感表示，并利用全空间反事实推断，联合优化优惠券营销场景下去偏CTR预测与增益估计。

    

    在在线广告中，优惠券等营销干预会给点击率预测引入显著的混淆偏差。观察到的点击实际上是用户内在偏好与这些干预所带来增益的混合体现。这导致传统模型对基础CTR的校准出现偏差，进而扭曲下游的排序和计费决策。此外，营销干预通常以不同强度的多值处理形式运作，这给CTR预测带来了额外的复杂性。为解决这些问题，我们提出了统一多值处理网络（UniMVT）。具体而言，UniMVT将混淆因素与处理敏感表示解耦，并通过全空间反事实推断模块联合重建去偏的基础CTR和强度-响应曲线。为应对多值处理的复杂性，UniMVT采用了辅助强度估计机制。

    arXiv:2602.12972v3 Announce Type: replace-cross  Abstract: In online advertising, marketing interventions such as coupons introduce significant confounding bias into Click-Through Rate (CTR) prediction. Observed clicks reflect a mixture of users' intrinsic preferences and the uplift induced by these interventions. This causes conventional models to miscalibrate base CTRs, which distorts downstream ranking and billing decisions. Furthermore, marketing interventions often operate as multi-valued treatments with varying magnitudes, introducing additional complexity to CTR prediction.   To address these issues, we propose the Unified Multi-Valued Treatment Network (UniMVT). Specifically, UniMVT disentangles confounding factors from treatment-sensitive representations, enabling a full-space counterfactual inference module to jointly reconstruct the debiased base CTR and intensity-response curves. To handle the complexity of multi-valued treatments, UniMVT employs an auxiliary intensity esti
    
[^667]: 受扩散启发的Transformer重构方法用于不确定性校准

    Diffusion-Inspired Reconfiguration of Transformers for Uncertainty Calibration

    [https://arxiv.org/abs/2602.08920](https://arxiv.org/abs/2602.08920)

    提出一种受扩散启发的Transformer重构方法，将特征变换块建模为概率映射并重组为扩散过程，在保持预测性能的同时实现预训练模型中不确定性的原则性传播与校准。

    

    预训练Transformer中的不确定性校准对于其在风险敏感应用中的可靠部署至关重要。然而，大多数现有的预训练Transformer缺乏在其特征变换堆栈中实现不确定性传播的原则性机制。在本工作中，我们提出了一种受扩散启发的Transformer重构方法，将每个特征变换块建模为概率映射。将这些概率映射进行组合会形成一条概率路径，该路径模拟了扩散过程的结构，将数据质量从输入分布输送到预训练的特征分布。随后，这条概率路径可以在具有统一转移模型的扩散过程上重新编译，从而在整个预训练模型架构中实现表示不确定性的原则性传播，同时保持其原有的预测性能。跨多种任务的实证结果表明……

    arXiv:2602.08920v3 Announce Type: replace  Abstract: Uncertainty calibration in pre-trained transformers is critical for their reliable deployment in risk-sensitive applications. Yet, most existing pre-trained transformers do not have a principled mechanism for uncertainty propagation through their feature transformation stack. In this work, we propose a diffusion-inspired reconfiguration of transformers in which each feature transformation block is modeled as a probabilistic mapping. Composing these probabilistic mappings reveals a probability path that mimics the structure of a diffusion process, transporting data mass from the input distribution to the pre-trained feature distribution. This probability path can then be recompiled on a diffusion process with a unified transition model to enable principled propagation of representation uncertainty throughout the pre-trained model's architecture while maintaining its original predictive performance. Empirical results across a variety o
    
[^668]: 学习的热力学理论（第二部分）：依赖历史的可达性与持续学习

    A Thermodynamic Theory of Learning Part II: History-Dependent Reachability and Continual Learning

    [https://arxiv.org/abs/2602.07950](https://arxiv.org/abs/2602.07950)

    该论文将神经可塑性建模为依赖历史动力学下的最小能量可达性问题，提出任务条件化的适应代价理论，证明了学习代价可在不发生秩损失的情况下持续增长，从而为持续学习中学习与记忆保持之间的权衡提供了理论刻画。

    

    我们利用标准的最小能量控制理论，将可塑性表述为在依赖历史的动力学下、目标依赖的有限时程可达性问题。扩展状态中包含影响未来更新的参数和内部变量。围绕参考轨迹，输入到端点的响应及其可控性格拉姆矩阵（controllability Gramian）决定了每个可达位移所需的最小输入能量。在联合任务目标上最小化该能量，定义了一个任务条件化的适应代价，该代价对于任意输入的线性模型是精确的。一个可解的学习-保持（learning-retention）示例表明，这种代价可以在没有任何秩损失的情况下增加，并能区分小的方向性增益与完全不可达的方向。Wasserstein输运提供了一个互补的全局下界，而学习映射的雅可比矩阵描述的是初始扰动的形变，而非对未来输入的响应。由此得到的学习-保持前沿依赖于

    arXiv:2602.07950v3 Announce Type: replace  Abstract: We formulate plasticity as target-dependent, finite-horizon reachability under history-dependent dynamics, using standard minimum-energy control theory. An extended state includes parameters and internal variables that affect future updates. Around a reference trajectory, the input-to-endpoint response and its controllability Gramian determine the minimum input energy for each reachable displacement. Minimizing this energy over the joint task target defines a task-conditioned adaptation cost, exact for the arbitrary-input linear model. A solvable learning-retention example shows that this cost can increase without any rank loss, and distinguishes small directional gain from an inaccessible direction. Wasserstein transport supplies a complementary global lower bound, while learning-map Jacobians describe deformation of initial perturbations rather than response to future inputs. The resulting learning-retention frontier depends on the
    
[^669]: 奇偶性、敏感度与Transformer

    Parity, Sensitivity, and Transformers

    [https://arxiv.org/abs/2602.05896](https://arxiv.org/abs/2602.05896)

    本论文证明transformer计算奇偶性任务至少需要两层，从而否定地解决了“单层transformer能否计算奇偶性”这一开放问题，并提出了一种去除不切实际假设的新构造方法。

    

    arXiv:2602.05896v3 公告类型：replace-cross 摘要：理解神经架构能够计算什么、不能计算什么，是人工智能理论中的核心挑战。在这一背景下，一个基础性问题就是奇偶性（PARITY）任务，即判断二进制输入序列中1的个数是偶数还是奇数。PARITY是计算理论中研究的核心任务之一，然而关于transformer在何种条件下能够或不能够解决该问题，目前仍然出人意料地不明确。在本文中，我们证明了transformer计算PARITY所需的最小层数为两层。特别地，我们解决了一个开放性问题：单层transformer是否能够计算PARITY。我们给出了否定的答案，其证明方法是表明单层transformer的平均敏感度增长速度慢于PARITY任务所需的水平。此外，我们提出了一种计算PARITY的transformer新构造，通过移除若干不切实际的假设，改进了现有的构造。

    arXiv:2602.05896v3 Announce Type: replace-cross  Abstract: Understanding what neural architectures can and cannot compute is a central challenge in the theory of AI. One of the fundamental problems in this context is the PARITY task, which asks whether the number of 1s in a binary input sequence is even or odd. PARITY is one of the central tasks studied in the theory of computation, yet it remains surprisingly unclear under which conditions transformers can or cannot solve it.   In this paper, we show that the minimal number of layers a transformer needs to compute PARITY is two. In particular, we solve the open problem asking whether a one-layer transformer can compute PARITY. We answer it negatively by showing that average sensitivity of a one-layer transformer grows slower than that of PARITY.   Furthermore, we show a new construction for transformer that computes PARITY, which improves on the existing constructions by removing a number of impractical assumptions. In particular, the
    
[^670]: 面向多目标图自监督学习的反馈控制

    Feedback Control for Multi-Objective Graph Self-Supervision

    [https://arxiv.org/abs/2602.05036](https://arxiv.org/abs/2602.05036)

    该论文提出ControlG框架，将多目标图自监督学习中的目标协调重新表述为反馈控制的时间分配问题，通过控制论方法解决传统混合方式导致的目标干扰与训练不稳定问题。

    

    图上的多任务自监督学习能否在没有目标之间常见拉锯战的情况下实现协调？图自监督学习（SSL）提供了日益丰富的前置任务目标工具箱，如互信息、重建和对比学习，然而由于目标干扰和训练不稳定性，可靠地组合这些目标仍然充满挑战。大多数多前置任务流程采用每次更新混合的方式，迫使每一次参数更新都成为一种妥协，并导致三种失败模式：分歧（冲突引起的负迁移）、漂移（目标效用的非平稳性）和干涸（被忽视目标的隐性饥饿）。我们认为，协调本质上是一个时间分配问题：决定每个目标何时获得优化预算，而不仅仅是如何对它们进行加权。我们提出了ControlG，一个控制理论框架，将多目标图SSL重新构建为反馈控制的时间分配问题（摘要在此处被截断）。

    arXiv:2602.05036v2 Announce Type: replace  Abstract: Can multi-task self-supervised learning on graphs be coordinated without the usual tug-of-war between objectives? Graph self-supervised learning (SSL) offers a growing toolbox of pretext objectives like mutual information, reconstruction, and contrastive learning, yet combining them reliably remains challenging due to objective interference and training instability. Most multi-pretext pipelines use per-update mixing, forcing every parameter update to be a compromise and leading to three failure modes: Disagreement (conflict-induced negative transfer), Drift (nonstationary objective utility), and Drought (hidden starvation of underserved objectives). We argue that coordination is fundamentally a temporal allocation problem: deciding when each objective receives optimization budget, not merely how to weigh them. We introduce ControlG, a control-theoretic framework that recasts multi-objective graph SSL as feedback-controlled temporal a
    
[^671]: 梯度变化下赌博机凸优化的维度依赖性改进

    Improved Dimension Dependence for Bandit Convex Optimization with Gradient Variations

    [https://arxiv.org/abs/2602.04761](https://arxiv.org/abs/2602.04761)

    本文通过对非连续梯度变化的精细化分析，改进了带两点反馈的赌博机凸优化中凸函数和强凸函数梯度变化遗憾界的维度依赖性，并首次实现了单点赌博机反馈下的梯度变化界。

    

    梯度变化在线学习由于与博弈论和优化的深刻联系而受到越来越多的关注。该问题已在全信息设置下得到广泛研究，但在赌博机反馈下的研究仍然不足。在这项工作中，我们关注带两点反馈的赌博机凸优化（BCO）中的梯度变化问题。通过对非连续梯度变化（赌博机反馈下梯度变化中的一个基本量）提出精细化的分析，我们相比已知最佳结果（Chiang et al., 2013）改进了凸函数和强凸函数情形下的维度依赖性。我们对非连续梯度变化的改进分析还蕴含了其他有利的问题依赖性保证，例如梯度方差遗憾界和小损失遗憾界。除两点反馈设置之外，我们通过在单点赌博机反馈下实现首个梯度变化界，展示了我们技术的通用性。

    arXiv:2602.04761v2 Announce Type: replace  Abstract: Gradient-variation online learning has drawn increasing attention due to its deep connections to game theory and optimization. It has been studied extensively in the full-information setting, but is underexplored with bandit feedback. In this work, we focus on gradient variation in Bandit Convex Optimization (BCO) with two-point feedback. By proposing a refined analysis of the non-consecutive gradient variation, a fundamental quantity in gradient variation with bandit feedback, we improve the dimension dependence for both convex and strongly convex functions compared with the best known results (Chiang et al., 2013). Our improved analysis of the non-consecutive gradient variation also implies other favorable problem-dependent guarantees, such as gradient-variance and small-loss regret bounds. Beyond the two-point setup, we demonstrate the versatility of our technique by achieving the first gradient-variation bound for one-point bandi
    
[^672]: 通过人类启发的奖励塑形提升大语言模型推理能力

    Boosting LLM Reasoning via Human-Inspired Reward Shaping

    [https://arxiv.org/abs/2602.04265](https://arxiv.org/abs/2602.04265)

    本文提出受人类学习过程启发的T2T动态奖励框架，通过“由厚到薄”的双阶段机制，让模型在未掌握的问题上鼓励广泛探索、在已掌握的问题上鼓励推理凝练与知识抽象，从而克服现有RLVR奖励设计将探索与巩固混为一谈的缺陷，提升大语言模型的推理能力。

    

    可验证奖励强化学习（RLVR）已成为提升大语言模型（LLM）推理能力的一种有前景的范式。然而，现有的奖励设计通常将探索与巩固视为一个单一的整体过程，导致分阶段的学习动态相互纠缠，这与人类学习者的自然学习行为相悖。在人类学习中，个体对已掌握的问题与陌生的问题会采取截然不同的行为模式：面对尚未掌握的挑战时，人类会优先进行广泛探索以寻求可行的解决方案；相比之下，对于已经熟练掌握的问题，他们则专注于推理的凝练与知识的抽象，以提炼出简洁的底层原理。受这一差距的启发，我们提出了T2T（Thickening-to-Thinning，由厚到薄），一个受人类学习过程启发的动态奖励框架。具体而言，它实现了一个双阶段机制：(1) 在回答错误时……（原文摘要在此处被截断）

    arXiv:2602.04265v4 Announce Type: replace-cross  Abstract: Reinforcement Learning with Verifiable Rewards (RLVR) has emerged as a promising paradigm for enhancing reasoning in Large Language Models (LLMs). However, existing reward formulations typically treat exploration and consolidation as a monolithic process, resulting in entangled stage-wise learning dynamics. This contradicts the natural learning behavior of human learners. In human learning, individuals adopt distinct behavioral patterns toward mastered versus unfamiliar problems. When confronting unmastered challenges, humans prioritize broad exploration to seek viable solutions. By contrast, for well-mastered problems, they focus instead on reasoning condensation and knowledge abstraction to distill concise underlying principles. Motivated by this gap, we introduce T2T(Thickening-to-Thinning), a dynamic reward framework inspired by human learning processes. Specifically, it implements a dual-phase mechanism: (1) On incorrect a
    
[^673]: 回声状态网络用于时间序列预测：超参数扫描与基准测试

    Echo State Networks for Time Series Forecasting: Hyperparameter Sweep and Benchmarking

    [https://arxiv.org/abs/2602.03912](https://arxiv.org/abs/2602.03912)

    该研究通过系统的超参数扫描和样本外基准测试表明，经过合理配置的简单回声状态网络（ESN）可以在M4竞赛数据集的月度和季度时间序列预测中与ARIMA、ETS、Theta等主流方法相竞争。

    

    本文研究了回声状态网络（ESN）在M4预测竞赛数据集中月度和季度时间序列单变量预测方面的性能。我们评估了简单的以一阶自回归方式驱动的ESN能否成为广泛使用的预测方法的有竞争力替代方案。该研究采用两阶段设计：使用一个参数数据集分析ESN模型在泄漏率、谱半径、储备池规模和正则化选择等方面的配置，同时保留一个不相交的预测数据集用于样本外基准测试。预测精度通过平均绝对标度误差（MASE）、对称平均绝对百分比误差（sMAPE）和总体加权平均值（OWA）来衡量。ESN与简单基准方法、统计模型（包括自回归积分移动平均模型ARIMA、指数平滑状态空间模型ETS、Theta方法和TBATS）以及多层模型进行了比较。

    arXiv:2602.03912v5 Announce Type: replace  Abstract: This paper investigates the performance of Echo State Networks (ESNs) for univariate forecasting of monthly and quarterly time series from the M4 Forecasting Competition dataset. We evaluate whether a simple first-order autoregressive ESN can serve as a competitive alternative to widely used forecasting methods. The study uses a two-stage design: a Parameter dataset is used to analyze ESN model configurations over leakage rate, spectral radius, reservoir size, and regularization selection, while a disjoint Forecast dataset is reserved for out-of-sample benchmarking. Forecast accuracy is measured using mean absolute scaled error (MASE), symmetric mean absolute percentage error (sMAPE), and the overall weighted average (OWA). The ESN is compared with simple benchmarks, statistical models including autoregressive integrated moving average (ARIMA), exponential smoothing state space (ETS), the Theta method, and TBATS, as well as multilaye
    
[^674]: 自回归与扩散语言模型中的逐步拒答动力学

    Step-Wise Refusal Dynamics in Autoregressive and Diffusion Language Models

    [https://arxiv.org/abs/2602.02600](https://arxiv.org/abs/2602.02600)

    该论文提出逐步拒答内部动力学（SRI）信号，揭示了扩散语言模型的重掩码采样机制能够从有害的中间生成内容中恢复，从而显著提升越狱鲁棒性，且这一优势在固定模型权重、仅切换采样方式时依然成立。

    

    扩散语言模型（DLM）近来已成为自回归（AR）模型的有力竞争替代方案，具备并行解码、具有竞争力的生成质量，以及改进越狱鲁棒性的初步证据。尽管取得了这些进展，采样机制在塑造拒答行为中所扮演的角色仍鲜为人知。为填补这一空白，我们对逐步拒答动力学进行了全面研究。我们证明扩散重掩码（remasking）能够促进模型从有害的中间生成内容中恢复，提供证据表明该行为与采样机制紧密相关，并证明从自回归采样切换到扩散采样可以提升越狱鲁棒性，即使在模型权重固定的情况下也是如此。为了捕捉文本层面无法观察到的生成动力学，我们提出了逐步拒答内部动力学（Step-Wise Refusal Internal Dynamics, SRI）信号。与文本层面的发现相一致，SRI表明恢复失败主要发生在自回归采样下。

    arXiv:2602.02600v4 Announce Type: replace-cross  Abstract: Diffusion language models (DLMs) have recently emerged as a competitive alternative to autoregressive (AR) models, offering parallel decoding, competitive generation quality, and initial evidence of improved jailbreak robustness. Despite this progress, the role of sampling mechanisms in shaping refusal behavior remains poorly understood. To address this gap, we present a comprehensive study of step-wise refusal dynamics. We show that diffusion remasking can promote recovery from harmful intermediate generations, provide evidence that this behavior is tied to the sampling mechanism, and demonstrate that switching from AR to diffusion sampling improves jailbreak robustness, including under fixed model weights. To capture generation dynamics not observable at the text level, we propose the Step-Wise Refusal Internal Dynamics (SRI) signal. Consistent with our text-level findings, SRI shows that recovery fails primarily under AR sam
    
[^675]: 基于线性循环单元的旋转无关在线手写字符识别

    Rotation-free Online Handwritten Character Recognition Using Linear Recurrent Units

    [https://arxiv.org/abs/2602.01533](https://arxiv.org/abs/2602.01533)

    本论文提出利用滑动窗口路径签名（SW-PS）提取局部结构特征，并结合轻量级线性循环单元（LRU）作为分类器，实现了对手写体字符在±180°任意旋转下仍保持高准确率的鲁棒识别。

    

    在线手写字符识别利用笔画顺序和动态特征，与离线识别相比通常能提供更高的准确率和鲁棒性。然而，在实际应用中，旋转变形会破坏笔画的空间布局，显著降低识别准确率。因此，提取旋转不变特征仍然是一个具有挑战性的开放问题。在本工作中，我们采用滑动窗口路径签名（SW-PS）来捕获字符的局部结构特征，并引入轻量级的线性循环单元（LRU）作为分类器。LRU结合了循环神经网络（RNN）的快速增量处理能力和状态空间模型（SSM）的高效并行训练，同时能够可靠地建模动态笔画特征。我们在CASIA-O数据集的三个子集上进行了随机旋转角度高达±180°的识别实验。

    arXiv:2602.01533v2 Announce Type: replace-cross  Abstract: Online handwritten character recognition leverages stroke order and dynamic features, which generally provide higher accuracy and robustness compared with offline recognition. However, in practical applications, rotational deformations can disrupt the spatial layout of strokes, substantially reducing recognition accuracy. Extracting rotation-invariant features therefore remains a challenging open problem. In this work, we employ the Sliding Window Path Signature (SW-PS) to capture local structural features of characters, and introduce the lightweight Linear Recurrent Units (LRU) as the classifier. The LRU combine the fast incremental processing capability of recurrent neural networks (RNN) with the efficient parallel training of state space models (SSM), while reliably modelling dynamic stroke characteristics. We conducted recognition experiments with random rotation angle up to $\pm 180^{\circ}$ on three subsets of the CASIA-O
    
[^676]: 基于贝叶斯不确定性量化与自适应残差学习的多保真物理信息神经网络用于参数化偏微分方程的高效求解

    Multi-Fidelity Physics-Informed Neural Networks with Bayesian Uncertainty Quantification and Adaptive Residual Learning for Efficient Solution of Parametric Partial Differential Equations

    [https://arxiv.org/abs/2602.01176](https://arxiv.org/abs/2602.01176)

    本文提出MF-BPINN框架，通过融合物理信息神经网络、贝叶斯不确定性量化和自适应残差学习，利用低保真模拟与稀疏高保真数据的分层架构，实现了参数化偏微分方程的高效求解。

    

    物理信息神经网络（PINNs）通过将物理定律直接嵌入到神经网络训练中，已成为求解偏微分方程（PDEs）的一种强大范式。然而，求解高保真PDE在计算上仍然代价高昂，特别是对于需要在多种参数配置下进行多次评估的参数化系统。本文提出了MF-BPINN，这是一种新颖的多保真框架，将物理信息神经网络与贝叶斯不确定性量化和自适应残差学习协同结合。我们的方法通过分层神经架构，利用丰富的低保真模拟与稀疏的高保真数据，学习不同保真度级别之间的非线性相关性。我们引入了一种具有可学习门控机制的自适应残差网络，能够动态平衡线性与非线性保真度差异。此外，我们还开发了一个严格的...

    arXiv:2602.01176v2 Announce Type: replace  Abstract: Physics-informed neural networks (PINNs) have emerged as a powerful paradigm for solving partial differential equations (PDEs) by embedding physical laws directly into neural network training. However, solving high-fidelity PDEs remains computationally prohibitive, particularly for parametric systems requiring multiple evaluations across varying parameter configurations. This paper presents MF-BPINN, a novel multi-fidelity framework that synergistically combines physics-informed neural networks with Bayesian uncertainty quantification and adaptive residual learning. Our approach leverages abundant low-fidelity simulations alongside sparse high-fidelity data through a hierarchical neural architecture that learns nonlinear correlations across fidelity levels. We introduce an adaptive residual network with learnable gating mechanisms that dynamically balances linear and nonlinear fidelity discrepancies. Furthermore, we develop a rigorou
    
[^677]: 单次测试时数据增强能否帮助泛化？

    Can One-Shot Test-Time Data Augmentation Help with Generalization?

    [https://arxiv.org/abs/2602.00114](https://arxiv.org/abs/2602.00114)

    提出了名为1S-DAug的单次测试时数据增强方法，仅从单张原始图像通过受控噪声注入的几何扰动和图像条件去噪生成增强样本，无需模型训练或参数访问即可实现高达20%的相对准确率提升。

    

    数据增强对模型泛化至关重要，但现有方法大多集中在训练阶段。测试时数据增强虽然研究不足，但可以在避免额外模型参数或微调的情况下，对泛化产生实际有效的提升。鉴于训练成本日益增加以及相关文献的空白，我们研究了是否有可能仅从单张原始图像通过图像生成来执行有效的测试时数据增强。我们首先分析了测试时数据增强的重要性，然后设计并研究了一个简单而自然的算子，命名为1S-DAug，它包含带受控噪声注入的几何扰动和基于图像条件的去噪。我们在四个数据集和多个模型的标准图像分类基准上获得了积极的结果，在不进行模型训练或访问模型参数的情况下，实现了高达20%的相对准确率提升。代码将会发布。

    arXiv:2602.00114v5 Announce Type: replace-cross  Abstract: Data augmentation is crucial for model generalization, but existing methods are mostly centered on the training stage. Test-time augmentation, while underexplored, can be practically effective for generalization while avoiding extra model parameters or fine-tuning. Given the increasing training cost and the literature gap, we study whether it is possible to perform effective test-time augmentation using image generation from just the single original image. We first analyze the importance of test-time augmentation, and then design and study a simple yet natural operator named 1S-DAug, which comprises geometric perturbations with controlled noise injection and image-conditioned denoising. We obtain positive results on well-established image-classification benchmarks across four datasets and multiple models, achieving up to 20\% relative accuracy improvement without model training or parameter access. Code will be released.
    
[^678]: 利用遥感影像与地理上下文引导的视觉Transformer观测健康结果

    Observing Health Outcomes Using Remote Sensing Imagery and Geo-Context Guided Visual Transformer

    [https://arxiv.org/abs/2602.00110](https://arxiv.org/abs/2602.00110)

    本文提出一种地理上下文引导的视觉Transformer，通过将异构地理空间变量嵌入为与图像块对齐的表示来增强遥感影像分析，从而实现基于遥感影像的健康结果观测。

    

    视觉Transformer推动了遥感图像分析的重大进展，尤其是在目标检测和分割方面。近期的视觉-语言与多模态模型通过整合辅助信息（包括描述文本、问答对和元数据）进一步扩展了这些能力，将应用范围拓宽至传统计算机视觉任务之外。然而，这些模型通常针对视觉内容与文本内容之间的语义对齐进行优化，而非针对地理空间理解，因此并不适合表示或推理结构化的地理空间图层。在本研究中，我们提出了地理上下文引导的视觉Transformer（Geo-Context Guided Visual Transformer），利用辅助地理空间信息增强遥感影像的处理。所提出的方法引入了一种地理空间嵌入机制，将异构的地理空间变量转换为与图像块对齐的表示，以及一种非对称

    arXiv:2602.00110v2 Announce Type: replace-cross  Abstract: Visual transformers have driven major progress in remote sensing image analysis, particularly in object detection and segmentation. Recent vision-language and multimodal models further extend these capabilities by incorporating auxiliary information, including captions, question and answer pairs, and metadata, which broadens applications beyond conventional computer vision tasks. However, these models are typically optimized for semantic alignment between visual and textual content rather than geospatial understanding, and therefore are not well suited for representing or reasoning with structured geospatial layers. In this study, we propose Geo-Context Guided Visual Transformer that enhances remote sensing imagery processing with auxiliary geospatial guidance. The proposed approach introduces a geospatial embedding mechanism that converts heterogeneous geospatial variables into patches-aligned representations and an asymmetric
    
[^679]: 面向终身机器人学习的持续策略巩固

    Continual Policy Consolidation for Lifelong Robot Learning

    [https://arxiv.org/abs/2601.22475](https://arxiv.org/abs/2601.22475)

    提出持续策略巩固（CPC）教师-学生框架，通过持续策略蒸馏、优先经验回放和可扩展专家，将强化学习驱动的技能习得与有监督的策略巩固相分离，使机器人能够终身学习新技能的同时保留已习得的行为。

    

    构建通用机器人策略需要在持续集成新技能的同时保留已习得的行为。由于机器人交互成本高昂、任务分布异构、且顺序更新会引发干扰，直接在持续增长的任务流上优化单一策略十分困难。为解决这些问题，我们提出了持续策略巩固（CPC），这是一种结合持续策略蒸馏、优先经验回放和可扩展专家的教师-学生框架。该架构将技能习得与策略巩固相分离：教师通过强化学习独立训练，其行为被持续蒸馏到一个中心化的通用学生策略中。这种分解既保留了强化学习在任务专用训练方面的实用优势，又将学生端的策略巩固转化为一个有监督的策略学习问题。

    arXiv:2601.22475v2 Announce Type: replace  Abstract: Building a generalist robot policy requires continuously integrating new skills while preserving previously acquired behaviors. Directly optimizing a single policy over a growing task stream is difficult because robotic interaction is expensive, task distributions are heterogeneous, and sequential updates induce interference. To address these problems, we propose continual policy consolidation (CPC), a teacher--student framework that combines continual policy distillation with prioritized experience replay and expandable experts. This architecture separates skill acquisition from policy consolidation: teachers are trained independently through reinforcement learning, and their behaviors are continually distilled into a central generalist student. This decomposition retains the practical strength of reinforcement learning for task-specialized training while casting student-side consolidation as a supervised policy-learning problem. To
    
[^680]: 增长表示与环境维度下的低秩加稀疏矩阵迁移学习

    Low-Rank Plus Sparse Matrix Transfer Learning under Growing Representations and Ambient Dimensions

    [https://arxiv.org/abs/2601.21873](https://arxiv.org/abs/2601.21873)

    该论文提出了一个在环境维度与潜在表示同时增长情形下的低秩加稀疏矩阵迁移学习框架，通过锚定交替投影估计器在保留已迁移子空间的同时仅估计低维创新与稀疏修改，实现了严格更优的误差率。

    

    学习系统往往会随时间扩展其环境特征或潜在表示，将早期表示嵌入到更大的空间中，而仅引入有限的新潜在结构。我们研究了在环境维度与内在表示同时增长情况下的结构化矩阵估计迁移学习问题，其中估计良好的源任务被嵌入为更高维目标任务的一个子空间。我们提出了一个通用的迁移框架，其中目标参数分解为嵌入的源分量、低维低秩创新分量以及稀疏编辑分量，并开发了一种锚定交替投影估计器，该估计器在保留已迁移子空间的同时，仅估计低维创新和稀疏修改。我们建立了确定性的误差界，将目标噪声、表示增长和源估计误差的贡献分离开来，当秩与稀疏度增量受限时可获得严格更优的误差率。

    arXiv:2601.21873v2 Announce Type: replace  Abstract: Learning systems often expand their ambient features or latent representations over time, embedding earlier representations into larger spaces with limited new latent structure. We study transfer learning for structured matrix estimation under simultaneous growth of the ambient dimension and the intrinsic representation, where a well-estimated source task is embedded as a subspace of a higher-dimensional target task.   We propose a general transfer framework in which the target parameter decomposes into an embedded source component, low-dimensional low-rank innovations, and sparse edits, and develop an anchored alternating projection estimator that preserves transferred subspaces while estimating only low-dimensional innovations and sparse modifications. We establish deterministic error bounds that separate target noise, representation growth, and source estimation error, yielding strictly improved rates when rank and sparsity increm
    
[^681]: 物有所值：使用重置与丢弃（ReD）方法在固定预算下提升大语言模型的推理能力

    More Bang for the Buck: Improving the Inference of Large Language Models at a Fixed Budget using Reset and Discard (ReD)

    [https://arxiv.org/abs/2601.21522](https://arxiv.org/abs/2601.21522)

    本文提出重置与丢弃（ReD）查询方法，克服了pass@k幂律行为导致的边际收益递减问题，在固定预算下显著提升大语言模型回答独特问题的数量（coverage@cost），并能定量预测节省的尝试次数。

    

    大语言模型（LLM）在可验证任务上的性能通常用pass@k来衡量，即在k次尝试中至少一次正确回答问题的概率。在固定预算下，更合适的指标是coverage@cost，即作为总尝试次数函数的已正确回答的独特问题平均数量。我们建立了这两个指标之间的联系，并证明在经验上观察到的pass@k幂律行为会导致coverage@cost的次线性增长（即边际收益递减）。为了解决这个问题，我们提出了重置与丢弃（ReD），这是一种大语言模型查询方法，无论pass@k的具体形式如何，都能在给定预算下提高coverage@cost。此外，在给定pass@k的情况下，我们可以定量预测使用ReD所能节省的总尝试次数。如果模型的pass@k不可用，ReD可以推断其幂律指数。我们在三个大语言模型上进行了实验，涵盖编程（HumanEval）、数学（GSM8K）和推理（MMLU-Pro）任务。

    arXiv:2601.21522v3 Announce Type: replace-cross  Abstract: The performance of large language models (LLMs) on verifiable tasks is usually measured by pass@k, the probability of answering a question correctly at least once in k trials. At a fixed budget, a more suitable metric is coverage@cost, the average number of unique questions answered as a function of the total number of attempts. We connect the two metrics and show that the empirically-observed power-law behavior in pass@k leads to a sublinear growth of the coverage@cost (diminishing returns). To solve this problem, we propose Reset-and-Discard (ReD), a query method of LLMs that increases coverage@cost for a given budget, regardless of the pass@k form. Moreover, given a pass@k, we can quantitatively predict the savings in the total number of attempts using ReD. If pass@k is not available for the model, ReD can infer its power-law exponent. Experiments on three LLMs across coding (HumanEval), math (GSM8K), and reasoning (MMLU-Pro
    
[^682]: Hypersolid：通过短程排斥涌现视觉表征

    Hypersolid: Emergent Vision Representations via Short-Range Repulsion

    [https://arxiv.org/abs/2601.21255](https://arxiv.org/abs/2601.21255)

    提出Hypersolid自监督学习方法，通过将视图对齐与短程排斥（局部碰撞避免）相结合，诱导出低各向异性、语义对齐的紧凑潜在几何结构，在无监督聚类和细粒度区分任务上表现尤为出色。

    

    自监督学习的一个核心问题是防止表征坍缩。大多数方法通过全局机制来避免这一问题，例如对比扩展、方差约束、维度去相关或强制特定的输出分布。在本工作中，我们研究了另一种不同的设计：短程排斥。我们提出了Hypersolid，这是一种将视图对齐与局部碰撞避免相结合的自监督目标函数。我们的方法诱导出一种由紧凑的、语义对齐的邻域构成的低各向异性潜在几何结构。这种几何结构在无监督聚类和细粒度区分方面尤为有效，尽管其代价是迁移能力较弱。

    arXiv:2601.21255v2 Announce Type: replace-cross  Abstract: A central problem in self-supervised learning is preventing representation collapse. Most methods avoid it through global mechanisms, such as contrastive expansion, variance constraints, decorrelating dimensions, or enforcing certain output distributions. In this work, we study a different design: short-range repulsion. We introduce Hypersolid, a self-supervised objective that combines view alignment with local collision avoidance.   Our method induces a latent geometry of compact, semantically aligned neighborhoods with low anisotropy. This geometry is especially effective for unsupervised clustering and fine-grained separation, although it comes at the cost of weaker transferability.
    
[^683]: 求解非光滑次模-凹函数的离线与在线极小极大问题：一种零阶方法

    Solving the Offline and Online Min-Max Problem of Non-smooth Submodular-Concave Functions: A Zeroth-Order Approach

    [https://arxiv.org/abs/2601.21243](https://arxiv.org/abs/2601.21243)

    本文提出一种结合Lovász扩展次梯度与高斯平滑的零阶方法，解决了非光滑次模-凹函数的离线与在线极小极大问题，证明了离线情形收敛到ε-鞍点，并在在线情形达到O(√(N(1+P̄_N)))的对偶间隙。

    

    我们考虑目标函数可能非光滑、关于极小化变量为次模函数且关于极大化变量为凹函数的极大-极小和极小-极大问题。我们研究了应用于该问题的零阶方法的性能。该方法基于目标函数关于极小化变量的Lovász扩展的次梯度，并基于高斯平滑来估计关于极大化变量的平滑函数梯度。在期望意义下，我们证明了该算法在离线情形下收敛到ε-鞍点。此外，我们证明在期望意义下，在在线设置中，该算法实现了O(√(N(1+P̄_N)))的在线对偶间隙，其中N是迭代次数，P̄_N是最优决策序列的路径长度。我们为所有情形给出了复杂度分析和超参数选择。

    arXiv:2601.21243v4 Announce Type: replace-cross  Abstract: We consider max-min and min-max problems with objective functions that are possibly non-smooth, submodular with respect to the minimiser and concave with respect to the maximiser. We investigate the performance of a zeroth-order method applied to this problem. The method is based on the subgradient of the Lov\'asz extension of the objective function with respect to the minimiser and based on Gaussian smoothing to estimate the smoothed function gradient with respect to the maximiser. In expectation sense, we prove the convergence of the algorithm to an $\epsilon$-saddle point in the offline case. Moreover, we show that, in the expectation sense, in the online setting, the algorithm achieves $O(\sqrt{N(1+\bar{P}_N)})$ online duality gap, where $N$ is the number of iterations and $\bar{P}_N$ is the path length of the sequence of optimal decisions. The complexity analysis and hyperparameter selection are presented for all the cases
    
[^684]: PatchFormer：一种基于补丁的时间序列基础模型，采用层次化掩码重建与跨域迁移学习实现零样本多步预测

    PatchFormer: A Patch-Based Time Series Foundation Model with Hierarchical Masked Reconstruction and Cross-Domain Transfer Learning for Zero-Shot Multi-Horizon Forecasting

    [https://arxiv.org/abs/2601.20845](https://arxiv.org/abs/2601.20845)

    PatchFormer是一种基于补丁的时间序列基础模型，通过层次化掩码重建自监督预训练、跨时间尺度可学习聚合、跨域知识蒸馏以及轻量级适配器迁移，在24个基准数据集上实现了最先进的零样本多步预测并显著降低均方误差。

    

    时间序列预测是一个基础性问题，在气候、能源、医疗健康和金融等领域具有广泛应用。许多现有方法需要针对特定领域的特征工程以及大量的标注数据。我们提出了PatchFormer，这是一种基于补丁的时间序列基础模型，利用层次化掩码重建进行自监督预训练，并通过轻量级适配器实现高效迁移。PatchFormer将时间序列分割为补丁，通过跨时间尺度的可学习聚合来学习多尺度时间表示。预训练采用带动态掩码的掩码补丁重建，其目标函数同时鼓励局部准确性和全局一致性，随后进行跨域知识蒸馏。在涵盖天气、能源、交通、金融和医疗健康领域的24个基准数据集上的实验表明，该模型实现了最先进的零样本多步预测性能，将均方误差降低了2（原文摘要在此处截断）。

    arXiv:2601.20845v2 Announce Type: replace  Abstract: Time series forecasting is a fundamental problem with applications in climate, energy, healthcare, and finance. Many existing approaches require domain-specific feature engineering and substantial labeled data for each task. We introduce PatchFormer, a patch-based time series foundation model that uses hierarchical masked reconstruction for self-supervised pretraining and lightweight adapters for efficient transfer. PatchFormer segments time series into patches and learns multiscale temporal representations with learnable aggregation across temporal scales. Pretraining uses masked patch reconstruction with dynamic masking and objectives that encourage both local accuracy and global consistency, followed by cross-domain knowledge distillation. Experiments on 24 benchmark datasets spanning weather, energy, traffic, finance, and healthcare demonstrate state-of-the-art zero-shot multi-horizon forecasting, reducing mean squared error by 2
    
[^685]: 从人工标注到文献：大规模核磁共振化学位移的半监督学习

    From Human Labels to Literature: Semi-Supervised Learning of NMR Chemical Shifts at Scale

    [https://arxiv.org/abs/2601.18524](https://arxiv.org/abs/2601.18524)

    本文提出一种半监督学习框架，利用数百万个无需原子级标注的文献谱图大规模学习核磁共振化学位移预测，显著提升了预测准确性、鲁棒性和泛化能力。

    

    核磁共振（NMR）化学位移的准确预测是谱图分析和分子结构解析的基础，然而现有的机器学习方法依赖于有限的、需要大量人工劳动的原子级标注数据集。我们提出了一种半监督框架，能够从数百万个文献提取的谱图中学习NMR化学位移，而无需显式的原子级标注，将少量标注数据与大规模未标注谱图相结合。我们将从文献谱图中预测化学位移的问题表述为置换不变的集合监督问题，并证明在损失函数满足通常可满足的条件下，最优二分匹配可以简化为基于排序的损失，从而实现超越传统精选数据集的稳定大规模半监督训练。我们的模型在准确性和鲁棒性方面显著优于最先进的方法，并展现出更强的泛化能力。

    arXiv:2601.18524v2 Announce Type: replace  Abstract: Accurate prediction of nuclear magnetic resonance (NMR) chemical shifts is fundamental to spectral analysis and molecular structure elucidation, yet existing machine learning methods rely on limited, labor-intensive atom-assigned datasets. We propose a semi-supervised framework that learns NMR chemical shifts from millions of literature-extracted spectra without explicit atom-level assignments, integrating a small amount of labeled data with large-scale unassigned spectra. We formulate chemical shift prediction from literature spectra as a permutation-invariant set supervision problem, and show that under commonly satisfied conditions on the loss function, optimal bipartite matching reduces to a sorting-based loss, enabling stable large-scale semi-supervised training beyond traditional curated datasets. Our models achieve substantially improved accuracy and robustness over state-of-the-art methods and exhibit stronger generalization 
    
[^686]: SFO：通过谱滤波学习偏微分方程算子

    SFO: Learning PDE Operators via Spectral Filtering

    [https://arxiv.org/abs/2601.17090](https://arxiv.org/abs/2601.17090)

    提出SFO神经算子，利用基于希尔伯特矩阵特征模态的通用谱基参数化积分核，仅学习快速衰减特征值的谱系数即可高效捕捉偏微分方程解映射中的长程非局部相互作用，在六个基准测试中达到最先进精度。

    

    偏微分方程（PDE）控制着复杂系统，然而神经算子往往难以高效地捕捉其解映射中固有的长程、非局部相互作用。我们提出了谱滤波算子（SFO），这是一种利用通用谱基（USB）来参数化积分核的神经算子，通用谱基是由谱滤波理论中希尔伯特矩阵的特征模态导出的固定全局正交基。基于我们的理论发现——平移不变的偏微分方程离散化的离散格林函数表现出空间线性动力系统（LDS）结构——我们证明了这些核在通用谱基中容许紧凑的近似。通过仅学习快速衰减特征值的谱系数，SFO实现了高度高效的表示。在包括反应扩散、流体动力学和三维电磁学在内的六个基准测试中，SFO达到了最先进的精度。

    arXiv:2601.17090v3 Announce Type: replace-cross  Abstract: Partial differential equations (PDEs) govern complex systems, yet neural operators often struggle to efficiently capture the long-range, nonlocal interactions inherent in their solution maps. We introduce Spectral Filtering Operator (SFO), a neural operator that parameterizes integral kernels using the Universal Spectral Basis (USB), a fixed, global orthonormal basis derived from the eigenmodes of the Hilbert matrix in spectral filtering theory. Motivated by our theoretical finding that the discrete Green's functions of shift-invariant PDE discretizations exhibit spatial Linear Dynamical System (LDS) structure, we prove that these kernels admit compact approximations in the USB. By learning only the spectral coefficients of rapidly decaying eigenvalues, SFO achieves a highly efficient representation. Across six benchmarks, including reaction-diffusion, fluid dynamics, and 3D electromagnetics, SFO achieves state-of-the-art accur
    
[^687]: 战略条令语言模型：一个面向条令一致性与地缘政治预测的学习系统框架

    Strategic Doctrine Language Models (sdLM): A Learning-System Framework for Doctrinal Consistency and Geopolitical Forecasting

    [https://arxiv.org/abs/2601.14862](https://arxiv.org/abs/2601.14862)

    提出了战略条令语言模型框架，通过融合多文档注意力、时间编码和条令一致性层，在多文档战略推理中实现条令一致性与校准不确定性，其战略质量和长程地缘政治预测能力超越通用大语言模型基线，并可与人类专家媲美。

    

    我们提出了战略条令语言模型，这是一个面向多文档战略推理的学习系统框架，具备条令一致性约束和校准的不确定性。该方法结合了多文档注意力机制、时间编码和条令一致性层，以提升长程预测能力和计划合理性，同时减少严重的条令违规。我们通过以下三种方式评估sdLM：(i) 对战略情景进行专家小组评分（N=47）；(ii) 在336份条令出版物（12,847条陈述）上检验条令一致性；(iii) 在127个历史反事实案例（1945-2020年）上进行跨越12-60个月时间范围的地缘政治预测。在这些基准测试中，sdLM相比强大的通用大语言模型基线实现了更高的战略质量和更好的校准性，并且在长程判断方面与人类专家保持竞争力。我们还进一步报告了消融实验、扩展趋势以及面向部署的性能/延迟表现。

    arXiv:2601.14862v2 Announce Type: replace  Abstract: We introduce Strategic Doctrine Language Models (sdLM), a learning-system framework for multi-document strategic reasoning with doctrinal consistency constraints and calibrated uncertainty. The approach combines multi-document attention, temporal encoding, and a doctrine-consistency layer to improve long-horizon forecasting and plan plausibility while reducing severe doctrinal violations. We evaluate sdLM using (i) expert-panel scoring of strategic scenarios (N=47), (ii) doctrine consistency on 336 doctrine publications (12,847 statements), and (iii) geopolitical forecasting on 127 historical counterfactuals (1945-2020) across 12-60 month horizons. Across these benchmarks, sdLM achieves higher strategic quality and better calibration than strong general-purpose LLM baselines, and remains competitive with human experts on long-horizon judgments. We further report ablations, scaling trends, and deployment-oriented performance/latency c
    
[^688]: SWE-Tester：训练开源大语言模型在真实世界代码仓库中进行问题复现

    SWE-Tester: Training Open-Source LLMs for Issue Reproduction in Real-World Repositories

    [https://arxiv.org/abs/2601.13713](https://arxiv.org/abs/2601.13713)

    提出 SWE-Tester 流程，通过从2600个开源仓库构建的4.1万实例高质量数据集微调开源大语言模型，使其能够自动生成问题复现测试，成功率最高提升10%。

    

    软件测试对于确保软件系统的正确性和可靠性至关重要。从自然语言问题描述中自动生成问题复现测试，能够通过简化根本原因分析来提高开发人员的工作效率，促进测试驱动开发——“先写测试，后写代码”，并可用于提升编码智能体等自动化问题解决系统的有效性。针对该任务提出的现有方法主要依赖于闭源大语言模型，对开源模型的探索十分有限。为解决这一问题，我们提出了 SWE-Tester——一个用于训练开源大语言模型生成问题复现测试的新型流程。首先，我们从2600个开源GitHub仓库中精心构建了一个包含4.1万个实例的高质量训练数据集，并用它训练了不同规模和不同系列的大语言模型。微调后的模型在成功率和变更方面分别实现了高达10%和21%的绝对提升。

    arXiv:2601.13713v2 Announce Type: replace-cross  Abstract: Software testing is crucial for ensuring the correctness and reliability of software systems. Automated generation of issue reproduction tests from natural language issue descriptions enhances developer productivity by simplifying root cause analysis, promotes test-driven development -- "test first, write code later", and can be used for improving the effectiveness of automated issue resolution systems like coding agents. Existing methods proposed for this task predominantly rely on closed-source LLMs, with limited exploration of open models. To address this, we propose SWE-Tester -- a novel pipeline for training open-source LLMs to generate issue reproduction tests. First, we curate a high-quality training dataset of 41K instances from 2.6K open-source GitHub repositories and use it to train LLMs of varying sizes and families. The fine-tuned models achieve absolute improvements of up to 10\% in success rate and 21\% in change 
    
[^689]: 人工智能的新策略：直接在人类大脑数据上训练基础模型

    A New Strategy for Artificial Intelligence: Training Foundation Models Directly on Human Brain Data

    [https://arxiv.org/abs/2601.12053](https://arxiv.org/abs/2601.12053)

    本文提出了一种人工智能新策略——直接在人类大脑数据（神经影像数据）上训练基础模型，以获取文本等人类生成数据无法体现的认知信息，从而克服基础模型的现有局限性。

    

    尽管基础模型已经在众多领域取得了显著成果，但它们仍然依赖人类生成的数据（如文本）作为知识的基本来源。然而，这些数据归根结底是人类大脑的产物，是更深层次神经复杂性的过滤投影。在本文中，我们探索一种人工智能的新策略：通过直接在人类大脑数据上训练基础模型，超越表层的统计规律。我们假设神经影像数据可以为我们打开一扇窗口，揭示通过可观察行为无法获取的人类认知要素，并论证这些额外知识可以与传统训练数据相结合，用以克服基础模型当前的一些局限性。虽然此前的研究已经证明了在神经模式上训练经典机器学习、深度学习或强化学习模型的可能性……

    arXiv:2601.12053v2 Announce Type: replace-cross  Abstract: While foundation models have achieved remarkable results across a diversity of domains, they still rely on human-generated data, such as text, as a fundamental source of knowledge. However, this data is ultimately the product of human brains, the filtered projection of a deeper neural complexity. In this paper, we explore a new strategy for artificial intelligence: moving beyond surface-level statistical regularities by training foundation models directly on human brain data. We hypothesize that neuroimaging data could open a window into elements of human cognition that are not accessible through observable actions, and argue that this additional knowledge could be used, alongside classical training data, to overcome some of the current limitations of foundation models. While previous research has demonstrated the possibility to train classical machine learning, deep learning, or reinforcement learning models on neural patterns
    
[^690]: GIFT：通过变分有限温度Gibbs初始化调和后训练目标

    GIFT: Reconciling Post-Training Objectives via Variational Finite-Temperature Gibbs Initialization

    [https://arxiv.org/abs/2601.09233](https://arxiv.org/abs/2601.09233)

    该论文提出GIFT方法，通过token级变分代理目标引入有限温度Gibbs初始化，使SFT目标与后续RL阶段结构兼容，避免分布坍缩并保留探索空间，从而提升大型推理模型的后训练效果。

    

    大型推理模型（LRM）主流的后训练范式——先监督微调（SFT）后强化学习（RL）——存在固有的优化失配问题：SFT中刚性的似然最大化会导致分布坍缩，从而耗尽后续RL所必需的探索空间。受KL正则化RL的Gibbs最优解启发，我们推导出一个token级别的变分代理目标，使SFT目标在结构上与后续RL阶段兼容，并提出了有限温度Gibbs初始化（GIFT）。标准SFT是该代理目标的退化零温极限，而有限温度则能保留结构多样性。我们的实验表明，GIFT优于标准SFT，并在用作RL初始化时超越了具有竞争力的基线方法。我们的代码可在 https://github.com/zzy1127/GIFT 获取。

    arXiv:2601.09233v3 Announce Type: replace-cross  Abstract: The prevailing post-training paradigm for Large Reasoning Models (LRMs)---Supervised Fine-Tuning (SFT) followed by Reinforcement Learning (RL)-suffers from an intrinsic optimization mismatch: the rigid likelihood maximization in SFT induces distributional collapse, thereby exhausting the exploration space necessary for subsequent RL. Motivated by the Gibbs optimum of KL-regularized RL, we derive a token-level variational surrogate that makes the SFT target structurally compatible with the subsequent RL stage, and propose Gibbs Initialization with Finite Temperature (GIFT). Standard SFT emerges as a degenerate zero-temperature limit of this surrogate, while a finite temperature preserves structural diversity. Our experiments demonstrate that GIFT outperforms standard SFT and surpasses competitive baselines when utilized for RL initialization. Our code is available at https://github.com/zzy1127/GIFT.
    
[^691]: 面向非线性动力系统的最小距离分类

    Minimum distance classification for nonlinear dynamical systems

    [https://arxiv.org/abs/2601.04058](https://arxiv.org/abs/2601.04058)

    本文提出了Dynafit方法，通过逼近Koopman算子并结合核技巧学习训练数据与底层动力学之间的距离度量，实现对非线性动力系统生成轨迹的最小距离分类。

    

    我们研究了将由非线性动力系统生成的轨迹或序列进行分类的问题，其中每个类别对应一个不同的动力系统。我们提出了Dynafit，一种基于核方法的技术，用于学习训练数据与底层动力学之间的距离度量。根据学习到的度量，新的观测数据被分配到其动力学最能拟合该观测数据的类别。该学习算法通过逼近Koopman算子，在与核函数相关的（可能是无限维的）特征空间中对动力学进行全局线性化。通过利用机器学习中常用的核技巧，距离度量可以在特征空间中计算，且与特征空间的维度无关。当可以获得动力学的先验知识时，还可以对核函数进行定制以融入这些先验信息。我们考虑了一个经典的测试例子——作为离散动力系统的逻辑斯蒂映射（logistic map），并推导了…

    arXiv:2601.04058v3 Announce Type: replace  Abstract: We address the problem of classifying trajectories or sequences generated by nonlinear dynamical systems, where each class corresponds to a distinct dynamical system. We propose Dynafit, a kernel-based method that learns a distance metric between training data and the underlying dynamics. New observations are assigned to the class whose dynamics best fit the observations according to the learned metric.The learning algorithm approximates the Koopman operator, which globally linearizes the dynamics in a (potentially infinite-dimensional) feature space associated with a kernel function. The distance metric is computed in the feature space independently of its dimensionality by exploiting the kernel trick commonly used in machine learning. The kernel function can be tailored to incorporate prior knowledge of the dynamics when available. We consider a classical test example, the logistic map as a discrete dynamical system, and derive ana
    
[^692]: 电力价格预测：连接线性模型、神经网络与在线学习

    Electricity Price Forecasting: Bridging Linear Models, Neural Networks and Online Learning

    [https://arxiv.org/abs/2601.02856](https://arxiv.org/abs/2601.02856)

    本文提出了一种结合线性与非线性前馈神经网络结构的多元混合神经网络架构，并整合了一种新颖的部分在线学习策略（将热启动与各训练阶段的不同超参数配置相结合），从而在计算成本与预测精度之间取得平衡，提升日前电力价格预测的准确性。

    

    精确的日前电力价格预测对于确保高效的投资组合管理、支持电厂运营的战略决策以及实现有效的电池优化至关重要。然而，在不确定且波动的市场环境中开发准确的预测模型极具挑战性。例如，虽然线性模型通常在预测电力价格方面表现出具有竞争力的性能，且计算需求极小，但它们无法捕捉相关的非线性关系。另一方面，非线性模型能够在计算成本大幅增加的情况下提高预测精度。我们采用了一种多元混合神经网络架构，该架构结合了线性和非线性前馈神经网络结构。随后，我们将该架构与一种新颖的部分在线学习策略相结合，该策略将热启动与各训练阶段不同的超参数配置相融合。这一……

    arXiv:2601.02856v4 Announce Type: replace  Abstract: Precise day-ahead forecasts for electricity prices are crucial to ensure efficient portfolio management, support strategic decision-making for power plant operations, and enable effective battery optimization. However, developing an accurate prediction model is highly challenging in an uncertain and volatile market environment. For instance, although linear models generally exhibit competitive performance in predicting electricity prices with minimal computational requirements, they fail to capture relevant nonlinear relationships. Nonlinear models, on the other hand, can improve forecasting accuracy with a surge in computational costs. We employ a multivariate hybrid neural architecture that combines linear and nonlinear feed-forward neural structures. We then integrate this architecture with a novel partial online learning strategy that combines warm-starting with distinct hyperparameter configurations for each training stage. This
    
[^693]: 用于高频限价订单簿预测的时序Kolmogorov-Arnold网络（T-KAN）：效率、可解释性与阿尔法衰减

    Temporal Kolmogorov-Arnold Networks (T-KAN) for High-Frequency Limit Order Book Forecasting: Efficiency, Interpretability, and Alpha Decay

    [https://arxiv.org/abs/2601.02310](https://arxiv.org/abs/2601.02310)

    本文提出时序Kolmogorov-Arnold网络（T-KAN），用可学习的B样条激活函数替代LSTM的固定线性权重来预测限价订单簿，在k=100时间范围内F1分数相对提升19.1%，实现132.48%的回报并显著缓解阿尔法衰减问题，同时具备良好的可解释性。

    

    高频交易（HFT）环境的特点是存在海量的限价订单簿（LOB）数据，这些数据以噪声大和非线性著称。阿尔法衰减是一个重大挑战，传统模型如DeepLOB随着预测时间范围（k）的增加而逐渐失去预测能力。本文基于FI-2010数据集的数据，引入时序Kolmogorov-Arnold网络（T-KAN），用可学习的B样条激活函数取代标准LSTM中固定的线性权重。这使得模型能够学习市场信号的“形状”，而不仅仅是其幅度。该方法在k = 100时间范围内使F1分数相对提升了19.1%。T-KAN网络的有效性不容小觑，在1.0个基点交易成本下产生了132.48%的回报，而DeepLOB的回撤为-82.76%。此外，T-KAN模型被证明具有良好的可解释性，“死区”清晰可见。

    arXiv:2601.02310v2 Announce Type: replace  Abstract: High-Frequency trading (HFT) environments are characterised by large volumes of limit order book (LOB) data, which is notoriously noisy and non-linear. Alpha decay represents a significant challenge, with traditional models such as DeepLOB losing predictive power as the time horizon (k) increases. In this paper, using data from the FI-2010 dataset, we introduce Temporal Kolmogorov-Arnold Networks (T-KAN) to replace the fixed, linear weights of standard LSTMs with learnable B-spline activation functions. This allows the model to learn the 'shape' of market signals as opposed to just their magnitude. This resulted in a 19.1% relative improvement in the F1-score at the k = 100 horizon. The efficacy of T-KAN networks cannot be understated, producing a 132.48% return compared to the -82.76% DeepLOB drawdown under 1.0 bps transaction costs. In addition to this, the T-KAN model proves quite interpretable, with the 'dead-zones' being clearly
    
[^694]: 混杂因素下奇异线性模型的可微分因果发现

    Differentiable Causal Discovery for Singular Linear Models under Confounding

    [https://arxiv.org/abs/2601.01368](https://arxiv.org/abs/2601.01368)

    该论文基于奇异学习理论提出了一个可微分因果发现框架，验证了WBIC在线性高斯ADMG奇异模型上的一致性，通过ADVI实现其可扩展近似，并利用Gumbel-Softmax松弛实现了在有向与双向邻接结构上的可微分搜索，从而在存在未观测混杂因素时实现高效的线性因果发现。

    

    在存在未观测混杂因素的情况下，基于评分的因果发现方法既需要一致的评分准则，也需要在图结构上进行高效搜索。具有相关误差的线性因果模型自然地可以用有向无环混合图（ADMG）来表示，其诱导的模型族包含奇异统计模型，而对于这些奇异模型，标准的贝叶斯信息准则（BIC）是非一致的。我们基于奇异学习理论，开发了一个针对线性高斯ADMG的基于评分的因果发现框架。我们首先验证了线性因果模型类满足广泛适用贝叶斯信息准则（WBIC）一致性的条件。随后，我们通过自动微分变分推断（ADVI）提出了WBIC的一种可扩展近似方法。最后，我们通过对有向邻接和双向邻接进行Gumbel-Softmax松弛，引入了一种针对ADMG的可微分搜索方法。实验……

    arXiv:2601.01368v2 Announce Type: replace  Abstract: Score-based causal discovery in the presence of unobserved confounders requires both a consistent scoring criterion and an efficient search over graph structures. Linear causal models with correlated errors are naturally represented by acyclic directed mixed graphs (ADMGs), whose induced model families include singular statistical models for which the standard Bayesian Information Criterion is inconsistent. We develop a score-based framework for causal discovery over linear Gaussian ADMGs grounded in singular learning theory. We first verify that the conditions for consistency of the Widely Applicable Bayesian Information Criterion (WBIC) are satisfied by the linear causal model class. We then propose a scalable approximation of the WBIC via Automatic Differentiation Variational Inference (ADVI). Finally, we introduce a differentiable search over ADMGs through Gumbel-Softmax relaxations of both directed and bidirected adjacency. Expe
    
[^695]: Alpha-R1：基于强化学习的LLM推理Alpha筛选

    Alpha-R1: Alpha Screening with LLM Reasoning via Reinforcement Learning

    [https://arxiv.org/abs/2512.23515](https://arxiv.org/abs/2512.23515)

    Alpha-R1提出了一个强化学习对齐的LLM框架，通过“语义门控”机制将候选因子的经济逻辑与动态构建的市场状态描述进行匹配，实现了情境感知的Alpha因子筛选，突破了传统方法仅依赖数值时间序列、忽视经济语义的局限。

    

    信号衰减和市场状态转换是非平稳市场中数据驱动投资策略反复面临的挑战，传统时间序列和机器学习方法往往难以超越历史相关性进行泛化。尽管大型语言模型（LLM）在处理非结构化信息方面展现出强大能力，但其通过显式经济推理支持量化因子筛选的潜力仍未被充分探索。现有基于因子的方法通常将Alpha简化为数值时间序列，忽视了决定因子何时具有经济相关性的语义逻辑。我们提出了Alpha-R1，一个通过强化学习对齐的LLM框架，用于情境感知的Alpha筛选。其核心机制——语义门控，将每个候选因子的语义特征与动态构建的市场状态描述进行对比评估，从而筛选出经济逻辑与当前市场环境相契合的稀疏因子子集。

    arXiv:2512.23515v2 Announce Type: replace-cross  Abstract: Signal decay and regime shifts pose recurring challenges for data-driven investment strategies in non-stationary markets, where conventional time-series and machine learning approaches often struggle to generalize beyond historical correlations. While large language models (LLMs) offer strong capabilities for processing unstructured information, their potential to support quantitative factor screening through explicit economic reasoning remains underexplored. Existing factor-based methods typically reduce alphas to numerical time series, overlooking the semantic rationale that determines when a factor is economically relevant. We present Alpha-R1, an RL-aligned LLM framework for context-aware alpha screening. Its core mechanism, semantic gating, evaluates each candidate factor's semantic profile against a dynamically constructed market state description, selecting a sparse subset of factors whose economic rationale aligns with 
    
[^696]: 面向低比特率神经语音压缩的解码端语义条件化方法

    Decoder-Side Semantic Conditioning for Low-Bitrate Neural Speech Compression

    [https://arxiv.org/abs/2512.21653](https://arxiv.org/abs/2512.21653)

    SemDAC通过在残差向量量化中引入由HuBERT蒸馏得到的语义条件化机制，使语音编解码器在仅0.95 kbps的比特率下即可达到甚至超越2.5 kbps DAC基线的客观与主观性能。

    

    语音编解码器通常以波形保真度为优化目标，将比特分配给那些可以从语言结构中推断出来的声学细节，这导致了压缩效率低下和识别性能下降。我们提出了SemDAC，一种语义感知的神经语音编解码器，它在残差向量量化（RVQ）中引入了层次化的语义条件化机制。第一个RVQ量化器从HuBERT特征蒸馏而来，产生能够捕捉语音内容的语义标记，而后续的量化器则编码残余的声学信息。解码器通过特征级线性调制（FiLM）以语义标记为条件，将重建过程引导至语义抽象未能解释的信息上。在0.95 kbps的比特率下，SemDAC在PESQ、STOI、SI-SNR和Whisper WER指标上达到或超越了2.5 kbps的DAC基线，ViSQOL表现相当，并且其主观MOS高于更高比特率的DAC基线。结果表明，显式的语义条件化……

    arXiv:2512.21653v2 Announce Type: replace-cross  Abstract: Speech codecs are usually optimized for waveform fidelity, allocating bits to acoustic detail that can be inferred from linguistic structure. This leads to inefficient compression and degraded recognition performance. We propose SemDAC, a semantic-aware neural speech codec that adds hierarchical semantic conditioning to residual vector quantization (RVQ). The first RVQ quantizer is distilled from HuBERT features to produce semantic tokens capturing phonetic content, while later quantizers encode residual acoustics. The decoder is conditioned on semantic tokens via feature-wise linear modulation (FiLM), steering reconstruction toward information not explained by semantic abstraction. At 0.95 kbps, SemDAC matches or surpasses a 2.5 kbps DAC baseline on PESQ, STOI, SI-SNR, and Whisper WER, with comparable ViSQOL, and achieves higher subjective MOS than higher-bitrate DAC baselines. Results show that explicit semantic conditioning,
    
[^697]: 用于概率偏微分方程的残差增强流匹配算子

    Residual-augmented flow matching operators for probabilistic partial differential equations

    [https://arxiv.org/abs/2512.12749](https://arxiv.org/abs/2512.12749)

    提出了一种残差增强的概率算子学习框架，在无限维函数空间中利用流匹配生成建模并借助廉价低保真解算子作为归纳偏置，通过学习刻画低保真与高保真解之间差异的概率残差算子，在数据稀缺条件下实现对偏微分方程系统的高效不确定性量化代理建模。

    

    arXiv:2512.12749v3 公告类型：replace-cross 摘要：在数据稀缺的场景下，为具有潜在不确定性的物理系统学习代理模型仍然充满挑战：确定性神经算子无法刻画不确定性，而生成式方法则需要大量高保真解算子模拟的样本集合，且往往会牺牲分辨率上的泛化能力。在本工作中，我们提出了一种残差增强的概率算子学习框架，该框架将基于流匹配的生成建模置于无限维函数空间中，同时利用廉价的低保真解算子作为归纳偏置。与直接学习完整的高保真随机解算子不同，所提出的框架学习刻画低保真解与高保真解之间差异的概率残差算子。通过使用以已知系统输入和低保真解为条件的神经算子对流匹配中的向量场进行参数化，该框架能够在数据稀缺条件下实现高效且具备不确定性量化的偏微分方程代理建模。

    arXiv:2512.12749v3 Announce Type: replace-cross  Abstract: Learning surrogate models for physical systems with latent uncertainty remains challenging in data-scarce regimes: deterministic neural operators fail to characterize uncertainty, while generative approaches require large ensembles of high-fidelity solution operator simulations and often sacrifice resolution generalizability. In this work, we propose a residual-augmented probabilistic operator learning framework that casts flow-matching-based generative modeling in infinite-dimensional function spaces while leveraging inexpensive low-fidelity solution operators as an inductive bias. Rather than learning the full high-fidelity stochastic solution operator directly, the proposed framework learns probabilistic residual operators that characterize the discrepancy between low- and high-fidelity solutions. By parameterizing the vector field in flow matching using neural operators conditioned on both the known system input and low-fid
    
[^698]: 模态逻辑神经网络

    Modal Logic Neural Networks

    [https://arxiv.org/abs/2512.03491](https://arxiv.org/abs/2512.03491)

    提出了一种端到端可微分的模态逻辑神经网络，通过可学习的可达关系和赋值函数实现可微分克里普克语义，统一支持认知、信念、义务和时间等模态解读，并能处理不一致性。

    

    神经网络对自然科学与社会而言不可或缺，其影响从公共卫生应用延伸至劳动力生产率。本文提出了模态逻辑神经网络（MLNNs）——模态逻辑的一种端到端可微分的逻辑神经网络实现，它在可能世界语义上评估一个可学习的真值函数。该神经架构通过可学习的世界可达关系和赋值函数来处理次一致性与不一致性。由于模态由关系所满足的框架公理而非算子本身决定，一个可微分引擎即可涵盖认知、信念、义务和时间等多种模态解读，其应用涵盖从反应式和分布式系统的验证到法律话语以及微观经济效用模型。本文提出了可微分克里普克语义的模型，并建立了其可靠性、收敛性和结构性（质）。

    arXiv:2512.03491v3 Announce Type: replace  Abstract: Neural Networks are indispensable to natural sciences and society. Their impact extends from applications in public health to workforce productivity. Here, we introduce Modal Logic Neural Networks (MLNNs) -- an end-to-end differentiable logical neural network realisation of modal logic which evaluates a learnable truth function across possible-world semantics. This neural architecture handles para-consistency and inconsistency via a learnable world accessibility relation and valuation function. Because the modality is fixed by which frame axioms the relation satisfies rather than by the operator, one differentiable engine covers the epistemic, doxastic, deontic and temporal readings, with applications from verification of reactive and distributed systems to legal discourse and microeconomic utility models. In this paper, we introduce a model of differentiable Kripke semantics, and establish their soundness, convergence, and structura
    
[^699]: 背景分布偏移下的开集域自适应：挑战与一种可证明高效的解决方案

    Open-Set Domain Adaptation Under Background Distribution Shift: Challenges and A Provably Efficient Solution

    [https://arxiv.org/abs/2512.01152](https://arxiv.org/abs/2512.01152)

    本文提出CoLOR方法，首次在背景分布发生偏移的挑战性条件下为开集识别提供可证明的理论保证，证明其在新类别可分离的良性假设下优于代表性基线方法并具备可扩展性。

    

    随着我们将机器学习系统部署到现实世界中，一个核心挑战是即使在数据发生偏移时也能保持模型的性能。这种偏移可以有多种形式：可能出现训练期间不存在的新类别，这被称为开集识别问题；同时已知类别的分布也可能发生变化。关于开集识别的保证大多是在已知类别的分布（我们称之为背景分布）固定不变的假设下推导出来的。在本文中，我们开发了CoLOR，一种即使在背景分布发生偏移这一具有挑战性的情况下也能保证解决开集识别问题的方法。我们证明了该方法在良性假设下有效，即新类别与非新类别是可分离的，并在简化的过参数化设置中提供了理论保证，证明其优于代表性基线方法。我们还开发了使CoLOR具备可扩展性的技术。

    arXiv:2512.01152v5 Announce Type: replace-cross  Abstract: As we deploy machine learning systems in the real world, a core challenge is to maintain a model that is performant even as the data shifts. Such shifts can take many forms: new classes may emerge that were absent during training, a problem known as open-set recognition, and the distribution of known categories may change. Guarantees on open-set recognition are mostly derived under the assumption that the distribution of known classes, which we call the background distribution, is fixed. In this paper we develop CoLOR, a method that is guaranteed to solve open-set recognition even in the challenging case where the background distribution shifts. We prove that the method works under benign assumptions that the novel class is separable from the non-novel classes, and provide theoretical guarantees that it outperforms a representative baseline in a simplified overparameterized setting. We develop techniques to make CoLOR scalable 
    
[^700]: 通过问题解决过程追踪数学能力

    Tracing Mathematical Proficiency Through Problem-Solving Processes

    [https://arxiv.org/abs/2512.00311](https://arxiv.org/abs/2512.00311)

    该论文提出利用学生问题解决过程的知识追踪方法KT-PSP及配套数据集KT-PSP-25，并构建StatusKT框架，通过教师-学生-教师三阶段大语言模型流水线将学生数学能力作为中间表示进行提取，突破了传统知识追踪仅依赖答题正确性、缺乏可解释性的局限。

    

    知识追踪（Knowledge Tracing, KT）旨在建模学生的知识状态并预测其未来表现，以实现智能辅导系统中的个性化学习。然而，传统的知识追踪方法在可解释性方面存在根本性局限，因为它们仅依赖于答题的正确性，而忽略了学生问题解决过程中蕴含的丰富信息。为弥补这一空白，我们提出了利用问题解决过程的知识追踪方法（KT-PSP），该方法结合学生的问题解决过程来捕捉数学能力的多维度特征。我们还介绍了KT-PSP-25，一个专门为KT-PSP设计的新数据集。在此基础上，我们提出了StatusKT，一个采用教师-学生-教师三阶段大语言模型流水线的知识追踪框架，用于提取学生的数学能力（MP）作为中间表示。在该流水线中，教师大语言模型首先提取特定问题的能力信息……

    arXiv:2512.00311v3 Announce Type: replace-cross  Abstract: Knowledge Tracing (KT) aims to model student's knowledge state and predict future performance to enable personalized learning in Intelligent Tutoring Systems. However, traditional KT methods face fundamental limitations in explainability, as they rely solely on the response correctness, neglecting the rich information embedded in students' problem-solving processes. To address this gap, we propose Knowledge Tracing Leveraging Problem-Solving Process (KT-PSP), which incorporates students' problem-solving processes to capture the multidimensional aspects of mathematical proficiency. We also introduce KT-PSP-25, a new dataset specifically designed for KT-PSP. Building on this, we present StatusKT, a KT framework that employs a teacher-student-teacher three-stage LLM pipeline to extract students' Mathematical Proficiency (MP) as intermediate representation. In this pipeline, the teacher LLM first extracts problem-specific proficien
    
[^701]: 多项选择视觉问答中未被探索的缺陷使基准测试不可靠

    Unexplored flaws in multiple-choice VQA make benchmarking unreliable

    [https://arxiv.org/abs/2511.22341](https://arxiv.org/abs/2511.22341)

    该论文揭示了MC-VQA基准测试中存在严重的提示格式敏感性问题——即使消除了选项顺序的影响，仅仅改变选项ID、分隔符等语义无关的格式元素就会导致模型性能排名频繁逆转，这表明当前MC-VQA作为多模态大语言模型评估基准的可靠性存在根本性缺陷。

    

    先前的研究指出对选项顺序的敏感性是多项选择视觉问答（MC-VQA）评估中的一个关键问题，并提出了一些协议来缓解这种影响。我们证明这种缓解措施不足以确保MC-VQA作为多模态大语言模型（MLLMs）可靠基准的有效性：性能对语义中性的提示格式选择仍然高度敏感，而当前的基准测试并未控制这些格式选择。在一项涵盖七个MLLM和五个MC-VQA数据集的大规模研究中，我们发现即使在顺序不变的评估条件下，排名逆转仍然频繁发生。当我们系统地改变选项ID集合、分隔符和分离符，从而产生48种语义等价的提示格式时，这些排名逆转就会出现。机制分析将这种不稳定性追溯到低层次的语言建模效应：分词器引起的选项ID标记的融合或移除会在输入序列中引入损坏的选项ID标记，而选择……

    arXiv:2511.22341v2 Announce Type: replace-cross  Abstract: Previous works identify sensitivity to option order as a key issue in multiple-choice VQA (MC-VQA) evaluation and propose protocols to mitigate this effect. We show that such mitigation is insufficient to ensure the validity of MC-VQA as a reliable benchmark for Multimodal Large Language Model (MLLMs): performance remains highly sensitive to semantically neutral prompt format choices that are not controlled by current benchmarks. In a large-scale study spanning seven MLLMs and five MC-VQAs datasets, we find frequent rank reversals even under order-invariant evaluation. These reversals arise when we systematically vary option ID sets, delimiters, and separators, yielding 48 semantically equivalent prompt formats. Mechanistic analyses trace this instability to low-level language modeling effects: tokenizer-induced fusion or removal of option ID tokens introduces corrupted option ID tokens into the input sequence, while the choice
    
[^702]: 迭代GRPO：借助单轮RLHF实现多轮强化学习的批在线策略迭代

    Iterative GRPO: Batch-Online Policy Iteration for Multi-Turn RL via Single-Turn RLHF

    [https://arxiv.org/abs/2511.21638](https://arxiv.org/abs/2511.21638)

    提出Iterative GRPO方法，利用批在线部署模式收集的交互数据，将经典的近似策略迭代算法与单轮RLHF相结合，从而无需在训练循环中接入真实或模拟用户即可实现多轮强化学习。

    

    arXiv:2511.21638v2 公告类型：替换。摘要：实际的LLM智能体通常运行在多轮对话场景中，其成功与否只有在整个交互过程结束后才能确定。大多数多轮RL方法通过同策略 rollout 进行训练，但与单轮RLHF不同，策略无法独自产生完整轨迹，因为在智能体的每一轮之后，外部环境必须做出响应。对于对话式智能体而言，这个环境就是用户，但真实用户通常无法嵌入训练循环中，而忠实地构建模拟用户也十分困难。另一方面，现实世界的部署很少是完全在线或完全离线的。常见的生产模式被称为“批在线”，即先部署当前策略收集一批交互数据，然后在该批数据上重新训练并重新部署。我们证明，这种批在线设置为应用经典的近似策略迭代算法创造了机会。我们的核心观察是，运行标准的token级…

    arXiv:2511.21638v2 Announce Type: replace  Abstract: Practical LLM agents often operate over multi-turn conversations where success is determined only after the full interaction ends. Most multi-turn RL methods train via on-policy rollouts, but unlike in single-turn RLHF, the policy cannot produce a trajectory alone, since an external environment must respond after each agent turn. For conversational agents, this environment is a user, but real users are generally unavailable inside the training loop and simulated users are difficult to build faithfully. Separately, real-world deployment is rarely fully online or fully offline. The common production pattern is called "batch online," where the current policy is deployed to collect a batch of interaction data, then retrained on that batch and redeployed. We show that this batch-online setting creates an opportunity for applying the classical approximate policy iteration algorithm. Our central observation is that running standard token-le
    
[^703]: 解析AI天气预报中不确定性的来源

    Resolving sources of uncertainty in AI weather forecasting

    [https://arxiv.org/abs/2511.14218](https://arxiv.org/abs/2511.14218)

    提出Pangu-Bayes概率预报框架，将大气状态不确定性与学习模型不确定性作为独立随机变量分开建模，实现了误差来源的可解析归因，并在热带气旋预报中显著降低路径、气压和风速误差。

    

    天气预报的不确定性来源于不完美的分析场和预报模式，但仅靠集合离散度无法揭示不同来源与下游预报目标之间的关系。我们提出了Pangu-Bayes，这是一个概率预报层级框架，将大气状态不确定性与学习模型不确定性视为不同的随机变量，将随流演变的状态扰动与贝叶斯参数样本进行交叉组合。这一构造产生了由模式定义的、可区分来源的方差分量以及匹配的路径评估方法。在90个2023年留出的热带气旋上，Pangu-Bayes将路径、气压和风速误差分别降低了54.2%、17.2%和24.9%，同时提高了对快速增强过程的检测能力。在88个支持路径比较的气旋中，大气状态变率与改进的路径预报更为一致地相关，而学习模型变率则更多地与改进的强度预报相关。

    arXiv:2511.14218v2 Announce Type: replace-cross  Abstract: Weather forecast uncertainty arises from imperfect analyses and forecast models, but ensemble spread alone does not reveal how distinct sources relate to downstream targets. We introduce Pangu-Bayes, a probabilistic forecasting hierarchy that treats atmospheric-state and learned-model uncertainty as distinct stochastic variables, crossing flow-dependent perturbations of the evolving state with Bayesian parameter samples. This construction yields model-defined source-resolved variance components and matched pathway evaluation. Across 90 held-out 2023 tropical cyclones, Pangu-Bayes reduces track, pressure and wind errors by 54.2%, 17.2% and 24.9%, respectively, while improving rapid-intensification detection. Among 88 cyclones supporting pathway comparison, atmospheric-state variability is more consistently associated with improved track prediction, whereas learned-model variability is more often associated with improved intensit
    
[^704]: 基于自监督的专利表示学习

    Patent Representation Learning via Self-supervision

    [https://arxiv.org/abs/2511.10657](https://arxiv.org/abs/2511.10657)

    提出一种利用专利内部结构（如权利要求、背景等）作为自监督信号的混合dropout-部分正样本策略，有效提升了专利表示学习在多种检索任务中的迁移性。

    

    我们研究了基于对比学习目标的自监督专利表示学习。一个标准基线方法通过对同一文本应用两次独立的dropout掩码来构建正样本，但将这一方法应用于长篇幅、结构化的专利文档时，需要仔细校准。我们证明，通过调节温度和dropout率，仅使用dropout的训练方法可以显著增强，但其最佳配置依赖于评估任务，并且无法从标题-摘要检索统一迁移到权利要求-说明书检索。我们提出混合dropout-部分正样本策略，这是一种专利特定的视图构建方法：锚点为标题-摘要视图，正样本要么来自同一视图的dropout重编码，要么来自同一专利的其他部分（如权利要求、摘要、背景、附图或说明书）。该方法利用专利内部结构作为训练时信号，无需IPC标签。

    arXiv:2511.10657v2 Announce Type: replace-cross  Abstract: We study self-supervised patent representation learning with contrastive objectives. A standard baseline constructs positives by encoding the same text twice under independent dropout masks, but applying this recipe to long, structured patent documents requires careful calibration. We show that dropout-only training can be substantially strengthened by tuning temperature and dropout rate, yet its best configuration is evaluation-dependent and does not transfer uniformly from title--abstract retrieval to claim-to-disclosure retrieval. We propose mixed dropout--section positives, a patent-specific view construction strategy in which the anchor is the title--abstract view and the positive is sampled either from a dropout re-encoding of the same view or from another section of the same patent, such as claims, summary, background, drawings, or description. This uses patent-internal structure as a training-time signal without IPC lab
    
[^705]: 广义无限维基于Alpha-Procrustes的几何

    Generalized infinite dimensional Alpha-Procrustes based geometries

    [https://arxiv.org/abs/2511.09801](https://arxiv.org/abs/2511.09801)

    本文通过引入基于单位化Hilbert-Schmidt算子和扩展Mahalanobis范数的新形式体系，将Alpha-Procrustes黎曼度量族扩展至可容纳广义Bures-Wasserstein和Log-Hilbert-Schmidt距离的无限维推广，并通过可学习正则化参数增强高维比较中的几何稳定性。

    

    本工作通过纳入广义Bures-Wasserstein（GBW）、Log-Euclidean和Wasserstein距离，扩展了最近提出的用于对称正定（SPD）矩阵的Alpha-Procrustes黎曼度量族。虽然Alpha-Procrustes框架已经在有限维和无限维设置中统一了许多经典度量，但它此前缺乏实现这些广义形式所需的结构组件。我们引入了一种基于单位化Hilbert-Schmidt算子和扩展Mahalanobis范数的形式体系，使得能够构建稳健的GBW和Log-Hilbert-Schmidt距离的无限维推广。我们的方法还纳入了一个可学习的正则化参数，以增强高维比较中的几何稳定性。复现文献中基准测试的初步实验证明了我们广义方法的性能提升。

    arXiv:2511.09801v3 Announce Type: replace-cross  Abstract: This work extends the recently introduced Alpha-Procrustes family of Riemannian metrics for symmetric positive definite (SPD) matrices by incorporating generalized versions of the Bures-Wasserstein (GBW), Log-Euclidean, and Wasserstein distances. While the Alpha-Procrustes framework has unified many classical metrics in both finite- and infinite- dimensional settings, it previously lacked the structural components necessary to realize these generalized forms. We introduce a formalism based on unitized Hilbert-Schmidt operators and an extended Mahalanobis norm that allows the construction of robust, infinite-dimensional generalizations of GBW and Log-Hilbert-Schmidt distances. Our approach also incorporates a learnable regularization parameter that enhances geometric stability in high-dimensional comparisons. Preliminary experiments reproducing benchmarks from the literature demonstrate the improved performance of our generalize
    
[^706]: PAN：面向通用、可交互、长时程世界模拟的世界模型

    PAN: A World Model for General, Actionable, and Long-Horizon World Simulation

    [https://arxiv.org/abs/2511.09057](https://arxiv.org/abs/2511.09057)

    该论文提出了PAN——一个通用、可行动交互且具备长时程一致性的世界模型，旨在突破现有世界模型在开放域行动驱动预测和抽象推理规划方面的局限，推动AI实现超越大语言模型的物理与具身智能。

    

    世界模型是对真实世界环境的认知模拟器，使生物智能体能够推理世界如何演化——无论是自发演化还是在自身行动的响应下演化——并据此进行规划与策略制定。在构建人工智能（AI）系统方面，世界模型代表着超越大语言模型（LLM）的下一个前沿方向，旨在赋予AI智能体物理智能与具身智能，使其能够通过模拟推理进行决策制定，并通过模拟试验进行强化学习。世界建模领域的最新进展在视频生成、三维场景演化、机器人动力学和游戏模拟等方面取得了令人瞩目的成果，但在通用的、开放域的、行动驱动的预测、长时程一致性，以及抽象推理与规划方面仍存在局限。此外，还存在着一些根本性的架构问题，例如状态表示、信息流或训练……

    arXiv:2511.09057v4 Announce Type: replace-cross  Abstract: A world model is a cognitive simulator of the real-world environment allowing biological agents to reason about how the world evolves, whether spontaneously or in response to their actions, and accordingly to plan and strategize. In building Artificial Intelligence (AI) systems, world models represent the next frontier beyond large language models (LLMs) to enable physical and embodied intelligence in AI agents, allowing them to perform decision-making through simulative reasoning and reinforcement-learning through simulative trials. Recent advancements in world modeling have yielded impressive progress in video generation, 3-D scene evolution, robotic dynamics, and game simulation, but limitations persist in general, open-domain, action-driven prediction, long-horizon consistency, and abstract reasoning and planning. Moreover, fundamental architectural questions, whether it be state representation, information flow, or trainin
    
[^707]: 利用离散函数可分解性进行科学设计

    Leveraging Discrete Function Decomposability for Scientific Design

    [https://arxiv.org/abs/2511.03032](https://arxiv.org/abs/2511.03032)

    该论文提出利用科学中属性预测函数所具有的离散可分解结构，将离散设计空间上的分布优化问题分解为更易处理的子问题，从而更高效地设计满足用户指定属性的离散对象（如蛋白质、电路和材料）。

    

    在AI驱动的科学与工程时代，我们常常希望根据用户指定的属性在计算机中设计离散对象。例如，我们可能希望设计一种能与靶标结合的蛋白质，排列电路中的组件以最小化延迟，或寻找具有特定性质的材料。在给定属性预测模型的情况下，计算机辅助设计通常需要在设计空间（如蛋白质序列空间）上训练一个生成模型，使其聚焦于具有所需属性的设计。分布优化——其可以被形式化为分布估计算法或强化学习策略优化——旨在寻找在期望意义上最大化目标函数的生成模型。由于设计空间的组合特性，对离散值设计上的分布进行优化在总体上具有挑战性。然而，科学领域中的许多属性预测器（摘要在此处被截断）

    arXiv:2511.03032v3 Announce Type: replace  Abstract: In the era of AI-driven science and engineering, we often want to design discrete objects in silico according to user-specified properties. For example, we may wish to design a protein to bind its target, arrange components within a circuit to minimize latency, or find materials with certain properties. Given a property predictive model, in silico design typically involves training a generative model over the design space (e.g., protein sequence space) to concentrate on designs with the desired properties. Distributional optimization$\unicode{x2013}$which can be formalized as an estimation of distribution algorithm or as reinforcement learning policy optimization$\unicode{x2013}$finds the generative model that maximizes an objective function in expectation. Optimizing a distribution over discrete-valued designs is in general challenging because of the combinatorial nature of the design space. However, many property predictors in scie
    
[^708]: 加速Frank-Wolfe算法：互补条件与稀疏性

    Accelerated Frank-Wolfe Algorithms: Complementarity Conditions and Sparsity

    [https://arxiv.org/abs/2511.02821](https://arxiv.org/abs/2511.02821)

    本文提出了基于互补条件刻画解稀疏性的新型加速Frank-Wolfe算法，在多面体和矩阵域约束下实现了复杂度仅依赖解稀疏度（而非环境维度）的最优一阶oracle复杂度。

    

    我们为在紧凸集上最小化光滑凸函数的问题开发了Frank-Wolfe（FW）族中新的加速一阶算法，重点关注两类重要的约束集合：（1）多面体和（2）由谱面体（spectrahedron）和核范数球给出的矩阵域。一个关键技术要素是刻画解稀疏性的互补条件——对多面体而言是面的维度，对矩阵而言是秩。我们提出了两种算法：（1）一种针对多面体的纯线性优化oracle（LOO）方法，具有最优的最坏情况一阶（FO）oracle复杂度，除有限的“预热”（burn-in）阶段和至多一个对数因子外，其LOO复杂度随 $r/\sqrt{\epsilon}$ 缩放，其中 $\epsilon$ 是目标精度，$r$ 是解的稀疏度（与环境维度无关）；（2）一种将FW与稀疏投影oracle（例如矩阵域的低秩SVD）相结合的混合方案。

    arXiv:2511.02821v2 Announce Type: replace-cross  Abstract: We develop new accelerated first-order algorithms in the Frank-Wolfe (FW) family for minimizing smooth convex functions over compact convex sets, with a focus on two prominent constraint classes: (1) polytopes and (2) matrix domains given by the spectrahedron and nuclear-norm balls. A key technical ingredient is a complementarity condition that captures solution sparsity---face dimension for polytopes and rank for matrices. We present two algorithms: (1) a purely linear optimization oracle (LOO) method for polytopes that has optimal worst-case first-order (FO) oracle complexity and, aside of a finite \emph{burn-in} phase and up to a logarithmic factor, has LOO complexity that scales with $r/\sqrt{\epsilon}$, where $\epsilon$ is the target accuracy and $r$ is the solution sparsity (independently of the ambient dimension), and (2) a hybrid scheme that combines FW with a sparse projection oracle (e.g., low-rank SVDs for matrix dom
    
[^709]: CDFlow：基于循环矩阵与对角矩阵构建可逆层

    CDFlow: Building Invertible Layers with Circulant and Diagonal Matrices

    [https://arxiv.org/abs/2510.25323](https://arxiv.org/abs/2510.25323)

    本文提出基于循环矩阵与对角矩阵乘积的新型可逆线性层CDFlow，利用快速傅里叶变换在保持表达能力的同时，将参数复杂度降至O(mn)，并将矩阵求逆和对数行列式计算的时间复杂度分别降至O(mn log n)和O(mn)。

    

    标准化流是一类深度生成模型，通过可逆变换实现高效的似然估计和采样。一个关键挑战在于设计既能增强表达能力、又能高效计算雅可比行列式和逆矩阵的线性层。我们提出了一种基于循环矩阵与对角矩阵乘积的新型可逆线性层。这种分解方法使用 m 个对角矩阵和 m-1 个循环矩阵，在仍能逼近一般线性变换的同时，将参数复杂度从 O(n²) 降低到 O(mn)。通过利用快速傅里叶变换，我们的方法将矩阵求逆的时间复杂度从 O(n³) 降低到 O(mn log n)，将对数行列式计算的时间复杂度从 O(n³) 降低到 O(mn)，其中 n 为输入维度。我们基于该层构建了循环-对角流（CDFlow）。

    arXiv:2510.25323v4 Announce Type: replace  Abstract: Normalizing flows are deep generative models that enable efficient likelihood estimation and sampling through invertible transformations. A key challenge is to design linear layers that enhance expressiveness while maintaining efficient computation of the Jacobian determinant and inverse. We introduce a novel invertible linear layer based on the product of circulant and diagonal matrices. This decomposition reduces parameter complexity from $\mathcal{O}(n^2)$ to $\mathcal{O}(mn)$ using $m$ diagonal matrices and $m-1$ circulant matrices while still approximating general linear transformations. By leveraging the Fast Fourier Transform, our approach reduces the time complexity of matrix inversion from $\mathcal{O}(n^3)$ to $\mathcal{O}(mn\log n)$ and that of computing the log-determinant from $\mathcal{O}(n^3)$ to $\mathcal{O}(mn)$, where $n$ is the input dimension. We build upon this layer to develop Circulant-Diagonal Flow (CDFlow), w
    
[^710]: 经典模型与模拟量子模型在非线性预测中的持续性能与能耗核算

    Sustained Performance and Energy Accounting for Nonlinear Forecasting Across Classical and Simulated Quantum Models

    [https://arxiv.org/abs/2510.25183](https://arxiv.org/abs/2510.25183)

    该论文通过在经典模型与模拟量子模型（包括量子储备池计算）上开展大规模非线性时间序列预测基准测试，从误差、延迟、内存、能耗和碳排放等多个维度全面核算了AI系统的持续性能与能效。

    

    高能效人工智能应当在完整的应用流程中进行评估，而不仅仅以最低误差或最短训练时间为标准。我们通过非线性时间序列预测来研究这一问题，并将模拟量子储备池计算（QRC）作为新兴计算技术的案例研究。我们的评估涵盖33种预测配置以及在NARMA-10、NARMA-20、Mackey-Glass、Lorenz-63和Santa Fe激光数据集上完成的825次运行。核心基准包括775次全程监测的运行，涵盖统计模型、线性模型、储备池模型、神经网络模型、连续变量高斯QRC以及基于门的态矢量QRC模型，另有50次变分量子神经网络（QNN）运行以扩展可训练量子模型的比较。我们测量了NRMSE、RMSE、MAE、实际运行时间、推理延迟、CPU/GPU峰值内存、参数量、运行能耗及碳排放。由于固定的QRC编码器可以为多个读出层生成可复用的特征，我们同时报告了冷启动成本和摊销成本。

    arXiv:2510.25183v2 Announce Type: replace-cross  Abstract: Energy-efficient AI should be evaluated across the full application pipeline, not only by lowest error or shortest training time. We study this through nonlinear time-series forecasting using simulated quantum reservoir computing (QRC) as an emerging-computing case study. Our evaluation spans 33 forecasting configurations and 825 completed runs on NARMA-10, NARMA-20, Mackey--Glass, Lorenz-63, and Santa Fe laser data. The core benchmark includes 775 fully instrumented runs across statistical, linear, reservoir, neural, continuous-variable Gaussian QRC, and gate-based statevector QRC models, with 50 additional variational QNN runs extending the trainable-quantum comparison.   We measure NRMSE, RMSE, MAE, wall time, inference latency, peak CPU/GPU memory, parameter count, operational energy, and carbon. Since a fixed QRC encoder can generate reusable features for multiple readouts, we report both cold-start and amortized costs. We
    
[^711]: 基于扩散策略与多时间尺度世界模型的深度主动推断用于真实世界探索与导航

    Deep Active Inference with Diffusion Policy and Multiple Timescale World Model for Real-World Exploration and Navigation

    [https://arxiv.org/abs/2510.23258](https://arxiv.org/abs/2510.23258)

    该论文提出了一个将扩散策略与多时间尺度循环状态空间世界模型相集成的深度主动推断框架，通过最小化期望自由能实现了真实世界机器人探索与目标导向导航的统一。

    

    真实环境中的自主机器人导航既需要通过探索来获取环境信息，也需要目标导向的导航以到达指定目标。基于自由能原理的主动推断通过最小化期望自由能，将认知价值与外在价值相结合，为这些行为提供了一个统一框架。为了在实践中实现这一理念，我们提出了一个深度主动推断框架，该框架集成了扩散策略作为策略模型，以及多时间尺度循环状态空间模型作为世界模型。扩散策略生成多样化的候选动作，而多时间尺度循环状态空间模型通过潜在想象预测这些动作的长期后果，从而实现最小化期望自由能的动作选择。真实世界导航实验，包括基线对比、组件消融和鲁棒性评估，表明我们的框架取得了更高的性能。

    arXiv:2510.23258v2 Announce Type: replace-cross  Abstract: Autonomous robotic navigation in real-world environments requires exploration to acquire environmental information as well as goal-directed navigation in order to reach specified targets. Active inference (AIF) based on the free-energy principle provides a unified framework for these behaviors by minimizing the expected free energy (EFE), thereby combining epistemic and extrinsic values. To realize this practically, we propose a deep AIF framework that integrates a diffusion policy as the policy model and a multiple timescale recurrent state-space model (MTRSSM) as the world model. The diffusion policy generates diverse candidate actions while the MTRSSM predicts their long-horizon consequences through latent imagination, enabling action selection that minimizes EFE. Real-world navigation experiments, including baseline comparisons, component ablations, and robustness evaluations, demonstrated that our framework achieved higher
    
[^712]: 基于结构感知特征学习与不确定性量化的晶体结构可合成性预测

    Synthesizability Prediction of Crystalline Structures with Structure-Aware Feature Learning and Uncertainty Quantification

    [https://arxiv.org/abs/2510.19251](https://arxiv.org/abs/2510.19251)

    提出SyntheFormer框架，通过结构感知特征学习与不确定性量化直接从晶体结构预测可合成性，在时间严格分离的前瞻性评估中，对仅占1.02%的极稀少可合成正类样本实现了高召回率预测，并通过新发现材料的前瞻性验证证实了其有效性。

    

    预测哪些假设性无机晶体能够在实验上被实现，仍然是加速材料发现过程中的核心挑战。SyntheFormer是一个正例-未标注（positive-unlabeled）框架，它直接从晶体结构中学习可合成性，将傅里叶变换晶体性质（FTCP）表示与结构感知特征提取、随机森林特征选择以及紧凑的深度多层感知机分类器相结合。该模型在2011年至2018年的历史数据上训练，并在2019年至2025年的未来年份上进行前瞻性评估，其中正类样本仅占总样本的1.02%。在这种时间分离的评估条件下，SyntheFormer实现了0.735的测试AUC、0.099的AUPRC，并在双阈值校准下以94.2%的覆盖率达到了97.6%的召回率。直接的前瞻性验证支持了这一结果，因为在数据整理时尚未被标注的两种材料Y6Fe(SiS7)2……（原文摘要在此处截断）

    arXiv:2510.19251v2 Announce Type: replace-cross  Abstract: Predicting which hypothetical inorganic crystals can be experimentally realized remains a central challenge in accelerating materials discovery. SyntheFormer is a positive-unlabeled framework that learns synthesizability directly from crystal structure, combining Fourier-transformed crystal properties (FTCP) representation with structure-aware feature extraction, Random-Forest feature selection, and a compact deep MLP classifier. The model is trained on historical data from 2011 through 2018 and evaluated prospectively on future years from 2019 to 2025, where the positive class constitutes only 1.02 percent of samples. Under this temporally separated evaluation, SyntheFormer achieves a test AUC of 0.735, AUPRC of 0.099 and 97.6 percent recall at 94.2 percent coverage with dual-threshold calibration. Direct prospective validation supports this result as two materials that were unlabeled at the time of data curation, Y6Fe(SiS7)2 
    
[^713]: RePro：训练语言模型忠实地回收网络数据用于预训练

    RePro: Training Language Models to Faithfully Recycle the Web for Pretraining

    [https://arxiv.org/abs/2510.10681](https://arxiv.org/abs/2510.10681)

    RePro通过强化学习训练仅1B参数的小型语言模型，忠实地改写回收网络数据用于预训练，在22个下游任务上相比仅使用原始数据实现了3.7%-14.5%的相对准确率提升。

    

    高质量数据是大语言模型（LLM）预训练的基石，然而其增长速度未能跟上前沿模型的需求。在本文中，我们提出了RePro，一种新颖的网络数据回收方法，该方法通过强化学习训练一个相对较小的语言模型来生成有效且忠实的预训练数据改写。具体而言，我们设计了一个质量奖励和三个忠实度奖励，优化语言模型改写器将原始数据转换为高质量改写，同时保持其核心语义和结构。在实验中，我们训练了一个仅有1B参数的改写器，从DCLM-RefinedWeb数据集中回收了720亿个token。在400M、1.4B和2.8B模型上的预训练结果表明，RePro在22个下游任务上相比仅使用原始数据的基线实现了3.7%-14.5%的相对准确率提升，是使用提示词驱动70B模型的最先进网络回收方法所获性能提升的两倍。

    arXiv:2510.10681v2 Announce Type: replace-cross  Abstract: High-quality data is a cornerstone of large language model (LLM) pretraining, yet its growth has not kept pace with the needs of frontier models. In this paper, we introduce RePro, a novel web recycling method that trains a relatively small LM with reinforcement learning to generate effective and faithful rephrasings of pretraining data. Specifically, we design one quality reward and three faithfulness rewards, optimizing the LM rephraser to convert organic data into high-quality rephrasings while maintaining its core semantics and structure. In our experiment, we train a rephraser as small as 1B parameters to recycle 72B tokens sampled from DCLM-RefinedWeb. Pretraining results on 400M, 1.4B, and 2.8B models demonstrate that RePro delivers 3.7%-14.5% relative accuracy gains over organic-only baseline on 22 downstream tasks, doubling the performance gains achieved by the state-of-the-art web recycling method that prompts a 70B r
    
[^714]: DAGLFNet：深度特征注意力引导的全局与局部特征融合用于伪图像点云分割

    DAGLFNet: Deep Feature Attention Guided Global and Local Feature Fusion for Pseudo-Image Point Cloud Segmentation

    [https://arxiv.org/abs/2510.10471](https://arxiv.org/abs/2510.10471)

    提出DAGLFNet框架，通过深度特征注意力引导的全局与局部特征融合，解决伪图像表示与原始三维信息不一致导致的特征判别性差的问题，从而提升点云语义分割性能。

    

    环境感知系统对于高精度建图和自主导航至关重要，激光雷达作为核心传感器提供精确的三维点云数据。如何在高效处理非结构化点云的同时提取结构化语义信息仍然是一项重大挑战。近年来，出现了许多基于伪图像的表示方法，通过将三维点云与二维网格融合来平衡效率与性能。然而，伪图像表示与原始三维信息之间的根本不一致性严重削弱了2D-3D特征融合，构成了一致性信息融合的主要障碍，并导致特征判别能力较差。本工作提出了DAGLFNet，一个旨在提取判别性特征的基于伪图像的语义分割框架。它包含三个关键组件：首先，全局-局部特征融合编码

    arXiv:2510.10471v3 Announce Type: replace-cross  Abstract: Environmental perception systems are crucial for high-precision mapping and autonomous navigation, with LiDAR serving as a core sensor providing accurate 3D point cloud data. Efficiently processing unstructured point clouds while extracting structured semantic information remains a significant challenge. In recent years, numerous pseudo-image-based representation methods have emerged to balance efficiency and performance by fusing 3D point clouds with 2D grids. However, the fundamental inconsistency between the pseudo-image representation and the original 3D information critically undermines 2D-3D feature fusion, posing a primary obstacle for coherent information fusion and leading to poor feature discriminability. This work proposes DAGLFNet, a pseudo-image-based semantic segmentation framework designed to extract discriminative features. It incorporates three key components: first, a Global-Local Feature Fusion Encoding (GL-F
    
[^715]: CogniDir：通过自适应分布学习对抗认知型恶意评论，实现鲁棒的假新闻检测

    CogniDir: Combating Cognitive Malicious Comments via Adaptive Distributional Learning for Robust Fake News Detection

    [https://arxiv.org/abs/2510.09712](https://arxiv.org/abs/2510.09712)

    CogniDir 提出了一种自适应分布学习框架，通过将鲁棒假新闻检测重构为动态数据混合优化问题，并利用信息论评分指导自适应采样，有效对抗大语言模型生成的认知型恶意评论攻击。

    

    大语言模型（LLMs）的扩散催生了一类新的基于心理学原理的恶意评论，使假新闻攻击从表面层面的文本噪声转向深层的认知与逻辑操控。这一转变严重削弱了现有检测器的效能，因为这些检测器通常依赖于静态攻击假设和固定的训练分布。为弥合这一差距，我们提出了 CogniDir，一个自适应分布学习框架，它将鲁棒检测重新表述为面向社交媒体内容安全的动态数据混合优化问题。基于认知心理学，我们首先形式化了针对特定机制的认知对抗范式，以系统性暴露检测器深层次的脆弱性。为应对脆弱性的异质性，CogniDir 推导出一个将经验准确率与概率置信度相耦合的信息论评分，该评分随后被映射为自适应采样比例。

    arXiv:2510.09712v4 Announce Type: replace-cross  Abstract: The proliferation of Large Language Models (LLMs) has enabled a new class of psychologically grounded malicious comments, shifting fake news attacks from surface-level textual noise to deep cognitive and logical manipulation. This shift severely undermines existing detectors, which conventionally rely on static attack assumptions and fixed training distributions. To bridge this gap, we introduce CogniDir, an adaptive distributional learning framework that reformulates robust detection as a dynamic data mixture optimization problem for social media content safety. Grounded in cognitive psychology, we first formalize mechanism-specific cognitive adversarial paradigms to systematically expose deep-seated detector vulnerabilities. To address the vulnerability heterogeneity, CogniDir derives an information-theoretic score coupling empirical accuracy with probabilistic confidence, which is then mapped to adaptive sampling proportions
    
[^716]: BaNEL：仅使用负奖励的生成建模探索后验方法

    BaNEL: Exploration Posteriors for Generative Modeling Using Only Negative Rewards

    [https://arxiv.org/abs/2510.09596](https://arxiv.org/abs/2510.09596)

    BaNEL算法仅利用失败尝试（负奖励）对生成模型进行后训练，并最小化奖励评估次数，从而解决了基础模型奖励信号接近零且奖励评估成本高昂这一严峻挑战。

    

    当今的生成模型在大量监督数据和刻画生成质量的信息性奖励函数的支持下蓬勃发展。它们的工作假设是：监督数据为模型预训练提供知识，而奖励函数提供关于如何进一步提高生成质量和正确性的密集信息。然而，在重要问题的最难实例中，出现了两个问题：（1）基础生成模型获得的奖励信号接近于零，（2）调用奖励预言机的成本高昂。这种设置构成了与标准基于奖励的后训练根本不同的学习挑战。为了解决这一问题，我们提出了BaNEL（贝叶斯负证据学习），这是一种仅使用失败尝试来后训练模型的算法，同时最小化奖励评估次数（NREs）。我们的方法基于这样一个核心思想：学习规律的...

    arXiv:2510.09596v2 Announce Type: replace-cross  Abstract: Today's generative models thrive with large amounts of supervised data and informative reward functions characterizing the quality of the generation. They work under the assumptions that the supervised data provides knowledge to pre-train the model, and the reward function provides dense information about how to further improve the generation quality and correctness. However, in the hardest instances of important problems, two problems arise: (1) the base generative model attains a near-zero reward signal, and (2) calls to the reward oracle are expensive. This setting poses a fundamentally different learning challenge than standard reward-based post-training. To address this, we propose BaNEL (Bayesian Negative Evidence Learning), an algorithm that post-trains the model using failed attempts only, while minimizing the number of reward evaluations (NREs). Our method is based on the idea that the problem of learning regularities 
    
[^717]: SQS：基于稀疏量化子分布的贝叶斯深度神经网络压缩

    SQS: Bayesian DNN Compression through Sparse Quantized Sub-distributions

    [https://arxiv.org/abs/2510.08999](https://arxiv.org/abs/2510.08999)

    该论文提出SQS框架，利用贝叶斯变分学习统一权重剪枝与低比特量化——通过尖峰-厚板先验诱导稀疏性、用高斯混合模型建模量化权重，并给出高效近似方法与理论一致性保证，从而在保持模型性能的同时实现比现有方法更高的压缩率。

    

    压缩大规模神经网络对于在资源受限设备上部署模型至关重要。大多数现有方法单独采用权重剪枝或低比特量化，往往导致为了保持可接受的性能损失而只能获得不理想的压缩率。我们提出了一个统一的框架（\method），通过贝叶斯变分学习同时进行剪枝和低比特量化，在保持相当性能的同时实现了比先前基线更高的压缩率。其核心思想是采用尖峰-厚板（spike-and-slab）先验来诱导稀疏性，并使用高斯混合模型（GMMs）对量化权重进行建模，从而实现低比特精度。由于包含尖峰-厚板先验与高斯混合模型的目标函数难以直接求解，我们推导了一种高效的近似方法，能够在最小精度损失的情况下实现有效压缩。在理论方面，我们为所提出的变分方法提供了一致性结果。

    arXiv:2510.08999v2 Announce Type: replace-cross  Abstract: Compressing large-scale neural networks is essential for deploying models on resource-constrained devices. Most existing methods adopt weight pruning or low-bit quantization individually, often resulting in suboptimal compression rates to preserve acceptable performance drops. We introduce a unified framework for simultaneous pruning and low-bit quantization via Bayesian variational learning (\method), which achieves higher compression rates than prior baselines while maintaining comparable performance. The key idea is to employ a spike-and-slab prior to induce sparsity and model quantized weights using Gaussian Mixture Models (GMMs) to enable low-bit precision. Due to the intractability of the objective involving spike-and-slab priors with GMMs, we derive an efficient approximation that facilitates effective compression with minimal accuracy loss. In theory, we provide a consistent result for our proposed variational approach 
    
[^718]: 付费观测下线性上下文老虎机的两全其美（Best-of-Both-Worlds）

    Best-of-Both Worlds for linear contextual bandits with paid observations

    [https://arxiv.org/abs/2510.07424](https://arxiv.org/abs/2510.07424)

    提出了 Agg-SPB 和 CE-SPB 两种“两全其美”的正则化领导者跟随算法，用于带付费观测的线性上下文老虎机，在对抗环境中达到 $T^{2/3}$ 遗憾率，在随机环境中达到对数或多对数级遗憾。

    

    我们研究带有付费观测的线性上下文老虎机问题，其中在每一轮中，学习者观察一个上下文，选择一个动作，并可以支付固定成本来观察来自部分臂的反馈。我们提出了两种具有“两全其美”保证的正则化领导者跟随算法。第一种是 Agg-SPB，它通过聚合与上下文相关的稳定性项，扩展了 Tsuchiya 和 Ito（2024）的 SPB-matching 框架，在对抗性环境中实现了特征的 $T^{2/3}$ 遗憾率，并在随机环境中实现了对 $T$ 的对数级依赖。第二种是 CE-SPB，它将依赖于臂的观测概率与受 Kuroki 等人（2024）启发的熵自适应学习率相结合，实现了熵自适应的 $\widetilde{O}(T^{2/3})$ 对抗性保证和多对数级的随机遗憾，同时避免了 Agg-SPB 随机分析中出现的最小上下文质量依赖。两种算法均进一步扩展到带污染观测的场景。

    arXiv:2510.07424v3 Announce Type: replace  Abstract: We study linear contextual bandits with paid observations, where at each round the learner observes a context, selects an action, and may pay a fixed cost to observe feedback from a subset of arms. We propose two Follow-the-Regularized-Leader algorithms with Best-of-Both-Worlds guarantees. The first, Agg-SPB, extends the SPB-matching framework of Tsuchiya and Ito (2024) by aggregating context-dependent stability terms, achieving the characteristic $T^{2/3}$ adversarial regret rate and logarithmic dependence on $T$ in stochastic environments. The second, CE-SPB, combines arm-dependent observation probabilities with an entropy-adaptive learning rate inspired by Kuroki et al. (2024). It achieves an entropy-adaptive $\widetilde{O}(T^{2/3})$ adversarial guarantee and polylogarithmic stochastic regret, while avoiding the minimum-context-mass dependence arising in the stochastic analysis of Agg-SPB. Both algorithms further extend to corrupt
    
[^719]: 基于超核岭回归的多指标模型学习

    Learning Multi-Index Models with Hyper-Kernel Ridge Regression

    [https://arxiv.org/abs/2510.02532](https://arxiv.org/abs/2510.02532)

    本文提出融合神经网络与核方法的超核岭回归（HKRR）方法，并通过样本复杂度理论证明其能够自适应地学习多指标模型，克服维度灾难。

    

    深度神经网络在高维问题中表现出色，优于核方法等因维度灾难而受限的模型。然而，这一成功背后的理论基础仍知之甚少。我们遵循这样一个观点：学习任务的复合结构是决定深度网络何时优于其他方法的关键因素。为了将这一想法形式化，我们考虑了一个简单的复合模型，即多指标模型（MIM）。在此背景下，我们引入并研究了超核岭回归（HKRR），这是一种融合神经网络与核方法的方法。我们的主要贡献是一个样本复杂度结果，证明HKRR能够自适应地学习多指标模型，从而克服维度灾难。此外，我们利用该估计量的核特性开发了专门的优化方法。实际上，我们对比了交替最小化和交替梯度下降（原文在此处被截断）。

    arXiv:2510.02532v2 Announce Type: replace-cross  Abstract: Deep neural networks excel in high-dimensional problems, outperforming models such as kernel methods, which suffer from the curse of dimensionality. However, the theoretical foundations of this success remain poorly understood. We follow the idea that the compositional structure of the learning task is the key factor determining when deep networks outperform other approaches. Taking a step towards formalizing this idea, we consider a simple compositional model, namely the multi-index model (MIM). In this context, we introduce and study hyper-kernel ridge regression (HKRR), an approach blending neural networks and kernel methods. Our main contribution is a sample complexity result demonstrating that HKRR can adaptively learn MIM, overcoming the curse of dimensionality. Further, we exploit the kernel nature of the estimator to develop ad hoc optimization approaches. Indeed, we contrast alternating minimization and alternating gra
    
[^720]: 融合序列基序与泛基因组特征：基于可解释轻量级一维CNN-XGBoost集成模型的抗菌素耐药性预测

    Fusing Sequence Motifs and Pan-Genomic Features: Antimicrobial Resistance Prediction using an Explainable Lightweight 1D CNN-XGBoost Ensemble

    [https://arxiv.org/abs/2509.23552](https://arxiv.org/abs/2509.23552)

    提出了AMR-EnsembleNet集成框架，将轻量级1D CNN学习的序列基序与XGBoost的泛基因组特征相融合，在中等规模数据集上实现了可解释且高效的抗菌素耐药性预测。

    

    抗菌素耐药性（AMR）是一场迅速升级的全球健康危机。虽然基因组测序能够快速预测耐药表型，但目前的计算方法存在局限性。标准机器学习模型将基因组视为无序的特征集合，忽略了单核苷酸多态性的序列上下文。而像Transformer这样的最先进序列模型通常需要大量数据且计算成本高昂，不适合该领域中典型的中等规模数据集。为了应对这些挑战，我们提出了AMR-EnsembleNet，这是一个协同结合基于序列和基于特征学习的集成框架。我们开发了一个轻量级的定制一维卷积神经网络（CNN），以从高维SNP数据中高效学习预测性序列基序。这个序列感知模型与XGBoost模型（一个强大的梯度提升系统）进行集成……

    arXiv:2509.23552v2 Announce Type: replace-cross  Abstract: Antimicrobial Resistance (AMR) is a rapidly escalating global health crisis. While genomic sequencing enables rapid prediction of resistance phenotypes, current computational methods have limitations. Standard machine learning models treat the genome as an unordered collection of features, ignoring the sequential context of Single Nucleotide Polymorphisms (SNPs). State-of-the-art sequence models like Transformers are often too data-hungry and computationally expensive for the moderately sized datasets that are typical in this domain. To address these challenges, we propose AMR-EnsembleNet, an ensemble framework that synergistically combines sequence-based and feature-based learning. We developed a lightweight, custom 1D Convolutional Neural Network (CNN) to efficiently learn predictive sequence motifs from high-dimensional SNP data. This sequence-aware model was ensembled with an XGBoost model, a powerful gradient boosting syst
    
[^721]: AudioFuse：通过ViT-1D CNN混合架构实现统一的频谱-时域学习，用于鲁棒的心音图分类

    AudioFuse: Unified Spectral-Temporal Learning via a Hybrid ViT-1D CNN Architecture for Robust Phonocardiogram Classification

    [https://arxiv.org/abs/2509.23454](https://arxiv.org/abs/2509.23454)

    AudioFuse提出了一种混合架构，用宽而浅的ViT处理频谱图、浅层1D CNN处理原始波形，同时学习心音图的频域和时域互补特征，在从零训练的条件下达到最先进的分类性能（ROC-AUC 0.8608），并对域偏移展现出优异的鲁棒性。

    

    心音图（PCG）等生物医学音频信号本质上具有节律性，其诊断信息同时蕴含在频域和时域中。标准的2D频谱图虽然提供了丰富的频谱特征，但会损失1D波形的相位信息和时间精度。我们提出了AudioFuse，这是一种能够同时从两种互补表示中学习的架构，用于心音图分类。为了降低融合模型中常见的过拟合风险，我们集成了一种定制的宽而浅的视觉Transformer（ViT）来处理频谱图，并结合一个浅层1D CNN来处理原始波形。在PhysioNet 2016数据集上，AudioFuse在从零开始训练的情况下实现了具有竞争力的最先进ROC-AUC 0.8608，优于其频谱图基线（0.8066）和波形基线（0.8223）。此外，它在具有挑战性的PASCAL数据集上展现出对域偏移的卓越鲁棒性，保持ROC-AUC为0.7181

    arXiv:2509.23454v2 Announce Type: replace-cross  Abstract: Biomedical audio signals, such as phonocardiograms (PCG), are inherently rhythmic and contain diagnostic information in both their spectral (tonal) and temporal domains. Standard 2D spectrograms provide rich spectral features but compromise the phase information and temporal precision of the 1D waveform. We propose AudioFuse, an architecture that simultaneously learns from both complementary representations to classify PCGs. To mitigate the overfitting risk common in fusion models, we integrate a custom, wide-and-shallow Vision Transformer (ViT) for spectrograms with a shallow 1D CNN for raw waveforms. On the PhysioNet 2016 dataset, AudioFuse achieves a state-of-the-art competitive ROC-AUC of 0.8608 when trained from scratch, outperforming its spectrogram (0.8066) and waveform (0.8223) baselines. Moreover, it demonstrates superior robustness to domain shift on the challenging PASCAL dataset, maintaining an ROC-AUC of 0.7181 whi
    
[^722]: S³F-Net：一种通过空间-频谱摘要融合网络实现医学图像分类的多模态方法

    S$^3$F-Net: A Multi-Modal Approach to Medical Image Classification via Spatial-Spectral Summarizer Fusion Network

    [https://arxiv.org/abs/2509.23442](https://arxiv.org/abs/2509.23442)

    提出了双分支网络S³F-Net，将深度空间CNN与浅层频谱编码器SpectraNet相融合，其中SpectralFilter层基于卷积定理通过逐元素乘法直接在完整傅里叶频谱上应用可学习滤波器以获得全局感受野，从而同时利用空间与频域表示提升医学图像分类性能。

    

    卷积神经网络因其在学习层次化空间特征方面的出色能力，已成为医学图像分析的基石。然而，这种对单一域的关注在捕获全局、整体模式方面效率低下，且未能显式建模图像的频域特性。为了应对这些挑战，我们提出了空间-频谱摘要融合网络（S³F-Net），这是一个同时从空间和频谱表示中学习的双分支框架。S³F-Net 将深度空间 CNN 与我们提出的浅层频谱编码器 SpectraNet 进行融合。SpectraNet 的特色在于所提出的 SpectralFilter 层，该层利用卷积定理，通过计算高效的逐元素乘法直接对图像的完整傅里叶频谱应用一组可学习的滤波器。这使得 SpectralFilter 层能够获得全局感受野……

    arXiv:2509.23442v2 Announce Type: replace-cross  Abstract: Convolutional Neural Networks have become a cornerstone of medical image analysis due to their proficiency in learning hierarchical spatial features. However, this focus on a single domain is inefficient at capturing global, holistic patterns and fails to explicitly model an image's frequency-domain characteristics. To address these challenges, we propose the Spatial-Spectral Summarizer Fusion Network (S$^3$F-Net), a dual-branch framework that learns from both spatial and spectral representations simultaneously. The S$^3$F-Net performs a fusion of a deep spatial CNN with our proposed shallow spectral encoder, SpectraNet. SpectraNet features the proposed SpectralFilter layer, which leverages the Convolution Theorem by applying a bank of learnable filters directly to an image's full Fourier spectrum via a computation-efficient element-wise multiplication. This allows the SpectralFilter layer to attain a global receptive field ins
    
[^723]: SingLEM：单通道大型脑电图模型

    SingLEM: Single-Channel Large EEG Model

    [https://arxiv.org/abs/2509.17920](https://arxiv.org/abs/2509.17920)

    提出了SingLEM，一个自监督脑电图基础模型，通过混合卷积-Transformer编码器独立处理每个通道，摆脱了预定义多通道输入和电极布局假设的限制，并在71个公开数据集（约9,200名受试者、357,000小时单通道数据）上训练，显著提升了模型的通用适应性。

    

    当前用于脑电图（EEG）的深度学习模型通常是任务特定的，并依赖大量标注数据，这限制了其适应性。尽管脑电图基础模型试图实现更广泛的适用性，但许多模型仍依赖预定义的多通道输入、电极布局假设或模型特定的通道处理方式。为解决这些局限，我们提出了单通道大型脑电图模型，这是一个自监督基础模型，其混合卷积-Transformer编码器将每个通道独立映射为可复用的表示，从而捕获局部和长程时间结构。这些表示既可以单独使用，也可以通过通道级特征拼接进行组合。我们收集了71个公开脑电图数据集，包含约9,200名受试者和357,000小时的单通道数据。为进行防数据泄漏的评估，下游实验结果是使用在排除了相应数据集后、由68个数据集预训练得到的模型获得的。

    arXiv:2509.17920v3 Announce Type: replace  Abstract: Current deep learning models for electroencephalography (EEG) are often task-specific and depend on large labeled datasets, limiting their adaptability. Although EEG foundation models seek broader applicability, many still rely on predefined multi-channel inputs, electrode-layout assumptions, or model-specific channel handling. To address these limitations, we introduce the Single-Channel Large EEG Model (SingLEM), a self-supervised foundation model whose hybrid convolutional--Transformer encoder maps each channel independently to a reusable representation capturing local and long-range temporal structure. These representations can be used individually or combined through channel-wise feature concatenation. We assembled 71 public EEG datasets comprising approximately 9,200 subjects and 357,000 single-channel hours. For leakage-controlled evaluation, downstream results were obtained with a model pretrained on 68 datasets after excludi
    
[^724]: 可解释的网络辅助随机森林+

    Interpretable Network-assisted Random Forest+

    [https://arxiv.org/abs/2509.15611](https://arxiv.org/abs/2509.15611)

    本文提出了一种基于随机森林泛化（RF+）的可解释网络辅助模型族，在实现极具竞争力的预测精度的同时，通过直接从模型参数和结构推导出的内在可解释性度量，弥合了网络辅助预测中预测性能与可解释性之间的差距。

    

    机器学习算法通常假设训练样本是相互独立的。当数据点通过网络相互连接时，样本之间产生的依赖性既是挑战（降低了有效样本量），也是机遇（可以利用网络邻居的信息来改进预测）。目前已有多种利用这一机遇的方法，但其中许多方法（包括图神经网络）不易解释，限制了其在理解模型如何进行预测方面的实用性。其他方法，如网络辅助线性回归，虽然具有可解释性，但预测性能往往较差。我们通过提出一族基于随机森林泛化（RF+）构建的灵活的网络辅助模型来弥合这一差距，该模型实现了极具竞争力的预测精度，并且可以通过直接从模型参数和结构推导出的内在可解释性度量来加以理解。

    arXiv:2509.15611v2 Announce Type: replace-cross  Abstract: Machine learning algorithms often assume that training samples are independent. When data points are connected by a network, the induced dependency between samples is both a challenge, reducing effective sample size, and an opportunity to improve prediction by leveraging information from network neighbors. Multiple methods taking advantage of this opportunity are available, but many, including graph neural networks, are not easily interpretable, limiting their usefulness for understanding how models make predictions. Others, such as network-assisted linear regression, are interpretable but often yield worse prediction performance. We bridge this gap by proposing a family of flexible network-assisted models built upon a generalization of random forests (RF+), which achieves highly-competitive prediction accuracy and can be understood through intrinsic interpretability measures, derived directly from the model parameters and stru
    
[^725]: WhisTLE：面向小型预训练语音识别Transformer的深度监督纯文本领域自适应方法

    WhisTLE: Deeply Supervised, Text-Only Domain Adaptation for Small Pretrained Speech Recognition Transformers

    [https://arxiv.org/abs/2509.10452](https://arxiv.org/abs/2509.10452)

    WhisTLE提出了一种深度监督的纯文本领域自适应方法，通过训练VAE从文本建模编码器输出并微调解码器，使预训练语音识别模型无需收集语音数据即可适配新领域，且推理时不产生额外开销。

    

    预训练自动语音识别（ASR）模型（如Whisper）表现良好，但仍需要领域自适应来处理未见过的语言表达。在许多实际场景中，收集语音数据并不现实，因此需要进行纯文本自适应。我们提出了WhisTLE，这是一种面向预训练编码器-解码器ASR模型的深度监督纯文本自适应方法。WhisTLE训练一个变分自编码器（VAE）来从文本建模编码器输出，并使用学习到的文本到潜在表示的编码器来微调解码器，还可以选择性地与文本转语音（TTS）自适应相结合。在推理时，恢复原始编码器，因此不会产生额外的运行时开销。在四个数据集和四个ASR模型的实验中，仅使用WhisTLE在32个案例中的28个（88%）中带来改善，平均相对词错误率（WER）下降22%。单独使用WhisTLE或与其他自适应方法结合使用时，在144个案例中的116个（81%）中带来改善，平均额外WER下降15%。

    arXiv:2509.10452v3 Announce Type: replace-cross  Abstract: Pretrained automatic speech recognition (ASR) models such as Whisper perform well but still need domain adaptation to handle unseen parlance. In many real-world settings, collecting speech data is impractical, necessitating text-only adaptation. We propose WhisTLE, a deeply supervised, text-only adaptation method for pretrained encoder-decoder ASR models. WhisTLE trains a variational autoencoder (VAE) to model encoder outputs from text and fine-tunes the decoder using the learned text-to-latent encoder, optionally combined with text-to-speech (TTS) adaptation. At inference, the original encoder is restored, incurring no extra runtime cost. Across four datasets and four ASR models, WhisTLE alone helps in 28 of 32 (88%) cases, with an average relative WER drop of 22%. Using WhisTLE alone or in combination with other adaptation approaches helps in 116 of 144 (81%) cases, with an average additional WER drop of 15%. Given all treatm
    
[^726]: 稀疏性的代价：使用稀疏与稀疏化测量进行稀疏恢复的充分条件

    The Price of Sparsity: Sufficient Conditions for Sparse Recovery using Sparse and Sparsified Measurements

    [https://arxiv.org/abs/2509.01809](https://arxiv.org/abs/2509.01809)

    本文确定了在高信噪比区间使用稀疏高斯测量矩阵进行稀疏二元信号支撑恢复所需最小样本量的充分条件，给出了阶为 $s\log(p/s)/\log(ds/p)$ 的信息论阈值，明确量化了测量稀疏性的代价，并指出存在一个测量稀疏导致对数级样本复杂度损失却换来近线性计算增益的区间。

    

    我们研究从含噪线性测量中对稀疏二元信号进行支撑恢复的问题。对于稀疏高斯测量矩阵，我们在高信噪比区间 $ds/p \to \infty$ 下确定了最大似然恢复所需最小样本量的充分条件，其中 $p$ 表示信号维度，$s$ 表示信号非零分量的数量，$d$ 表示测量矩阵每行非零分量的期望数量。结合已知的下界，这给出了阶为 $s\log(p/s) / \log(ds/p)$ 的信息论阈值，从而明确揭示了测量稀疏性的代价。特别地，我们强调了一种区间，在该区间中测量稀疏性导致的样本复杂度损失是对数级的，而计算增益接近线性。其次，我们研究了对原始稠密高斯设计进行稀疏化后的恢复问题：观测数据由稠密设计生成，而估计则使用一个独立的（摘要在此处截断）。

    arXiv:2509.01809v2 Announce Type: replace-cross  Abstract: We consider the problem of support recovery for sparse binary signals from noisy linear measurements. For sparse Gaussian measurement matrices we identify sufficient conditions on the minimal sample size for maximum-likelihood recovery in the high-SNR regime $ds/p \to \infty$, where $p$ denotes the signal dimension, $s$ the number of non-zero components of the signal, and $d$ the expected number of non-zero components per row of measurement. Combined with known lower bounds, this yields an information-theoretic threshold of order $s\log(p/s) / \log(ds/p)$, making explicit the price of measurement sparsity. In particular, we highlight a regime where the sample-complexity loss from measurement sparsity is logarithmic while the computational gain is nearly linear.   Second, we study recovery after sparsifying an originally dense Gaussian design: the observations are generated from the dense design, while estimation uses an indepen
    
[^727]: ReST-RL：通过统一自训练与价值引导搜索强化大语言模型推理

    ReST-RL: Reinforcing LLM Reasoning through Unified Self-Training and Value-Guided Search

    [https://arxiv.org/abs/2508.19576](https://arxiv.org/abs/2508.19576)

    该论文提出统一策略-价值框架ReST-RL，通过ReST-GRPO重塑轨迹分布以放大奖励信号提升策略训练效率，并结合基于MCTS自收集数据训练价值模型的VM-MCTS解码搜索方法，协同增强大语言模型的推理能力。

    

    在提高大语言模型（LLM）推理准确率方面，代表性的强化学习（RL）方法——组相对策略优化（GRPO）——已取得关键性成功，但其仍存在奖励信号不显著的问题。本文提出了ReST-RL，这是一个统一的强化自训练策略-价值框架，它将策略优化与价值引导搜索重新连接起来，以提升LLM的推理能力。首先，ReST-GRPO采用一种优化的ReST风格算法，通过增加GRPO采样的奖励方差，并让策略接触信息更丰富的部分状态，来重塑策略诱导的轨迹分布，从而提升训练的效率和有效性。随后，我们进一步引入一种解码优化方法VM-MCTS，该方法基于自收集的蒙特卡洛树搜索（MCTS）目标训练价值模型（VM），并通过改进的MCTS算法部署该模型以提供[价值引导的搜索]。

    arXiv:2508.19576v3 Announce Type: replace  Abstract: With respect to improving the reasoning accuracy of LLMs, the representative reinforcement learning (RL) method - Group Relative Policy Optimization (GRPO) - has achieved critical success, yet it still suffers from the issue of insignificant reward signals. This paper introduces ReST-RL, a unified Reinforced Self-Training (ReST) policy-value framework that reconnects policy optimization and value-guided search to improve LLM reasoning ability. Firstly, ReST-GRPO adopts an optimized ReST-style algorithm to reshape the policy-induced trajectory distribution by increasing the reward variance of GRPO sampling and exposing the policy to more informative partial states, thereby improving training efficiency and effectiveness. Then, we further introduce a decoding optimization method, VM-MCTS, which trains a Value Model (VM) from self-collected Monte-Carlo Tree Search (MCTS) targets and deploys it through an adapted MCTS algorithm to provid
    
[^728]: 从偏好中学习特技飞行

    Learning Acrobatic Flight from Preferences

    [https://arxiv.org/abs/2508.18817](https://arxiv.org/abs/2508.18817)

    本文提出REC框架，通过集成分布奖励模型显式建模每个时间步的奖励不确定性，解决了手工设计奖励函数与人类判断不一致的问题，使智能体能够仅凭人类偏好学会执行高难度的特技飞行动作。

    

    基于偏好的强化学习使智能体能够在无需人工设计奖励函数的情况下学习控制策略，因此非常适合那些目标难以形式化或本质上具有主观性的任务。特技飞行是一个特别具有挑战性的问题，因为其动力学复杂、运动速度快，且精确执行至关重要。然而，为此类任务人工设计的奖励函数往往无法捕捉真正重要的品质：我们发现手工设计的奖励与人类判断仅有60.7%的一致率，这凸显了基于偏好方法的必要性。在本工作中，我们提出了置信度下的奖励集成，这是一种用于偏好强化学习的概率性奖励学习框架，通过分布奖励模型的集成显式地对每个时间步的奖励不确定性进行建模。该方法将不确定性传播到偏好损失中，并利用模型间的分歧（来指导策略学习）。

    arXiv:2508.18817v3 Announce Type: replace-cross  Abstract: Preference-based reinforcement learning (PbRL) enables agents to learn control policies without requiring manually designed reward functions, making it well-suited for tasks where objectives are difficult to formalize or inherently subjective. Acrobatic flight poses a particularly challenging problem due to its complex dynamics, rapid movements, and the importance of precise execution. However, manually designed reward functions for such tasks often fail to capture the qualities that matter: we find that hand-crafted rewards agree with human judgment only 60.7% of the time, underscoring the need for preference-driven approaches. In this work, we propose Reward Ensemble under Confidence (REC), a probabilistic reward learning framework for PbRL that explicitly models per-timestep reward uncertainty through an ensemble of distributional reward models. By propagating uncertainty into the preference loss and leveraging disagreement 
    
[^729]: ComplicitSplat：下游模型容易受到3D高斯泼溅伪装的黑盒攻击

    ComplicitSplat: Downstream Models are Vulnerable to Blackbox Attacks by 3D Gaussian Splat Camouflages

    [https://arxiv.org/abs/2508.11854](https://arxiv.org/abs/2508.11854)

    ComplicitSplat是首个利用3D高斯泼溅中视角特定伪装对下游目标检测器发起黑盒攻击的方法，无需了解模型架构或权重即可成功攻击多种检测器，揭示了自动驾驶等安全关键应用面临的新型风险。

    

    随着3D高斯泼溅（3DGS）因能够从静态图像高效合成新视角而在安全关键任务中快速获得应用，攻击者可能如何篡改图像以造成危害？我们提出了ComplicitSplat，这是首个利用标准3DGS着色方法创建视角特定伪装的攻击——即随观察角度变化的颜色和纹理——将对抗性内容嵌入场景物体中，这些内容仅在特定视角下可见，且无需访问模型架构或权重。我们的大量实验表明，ComplicitSplat能够成功泛化并攻击多种流行的检测器——包括单阶段、多阶段和基于Transformer的模型，涵盖真实世界物理物体拍摄和合成场景。据我们所知，这是首个使用3DGS对下游目标检测器发起的黑盒攻击，暴露了自动驾驶等应用中的一种新型安全风险。

    arXiv:2508.11854v3 Announce Type: replace-cross  Abstract: As 3D Gaussian Splatting (3DGS) gains rapid adoption in safety-critical tasks for efficient novel-view synthesis from static images, how might an adversary tamper images to cause harm? We introduce ComplicitSplat, the first attack that exploits standard 3DGS shading methods to create viewpoint-specific camouflage - colors and textures that change with viewing angle - to embed adversarial content in scene objects that are visible only from specific viewpoints and without requiring access to model architecture or weights. Our extensive experiments show that ComplicitSplat generalizes to successfully attack a variety of popular detector - both single-stage, multi-stage, and transformer-based models on both real-world capture of physical objects and synthetic scenes. To our knowledge, this is the first black-box attack on downstream object detectors using 3DGS, exposing a novel safety risk for applications like autonomous navigatio
    
[^730]: D-Wave量子退火与Gibbs蒙特卡洛在受限玻尔兹曼机概率分布采样中的比较

    Comparison of D-Wave Quantum Annealing and Gibbs Monte Carlo for Sampling from a Probability Distribution of a Restricted Boltzmann Machine

    [https://arxiv.org/abs/2508.10228](https://arxiv.org/abs/2508.10228)

    本研究将基于局部谷的采样质量评估方法应用于最新一代D-Wave量子退火机，发现在RBM采样中D-Wave覆盖的局部谷数量略多于Gibbs采样，但两者找到的局部谷差异明显，且在高概率状态上重叠度高、互补性不足。

    

    本研究将一种以局部谷（LV）为中心的受限玻尔兹曼机（RBM）采样质量评估方法应用于最新一代的D-Wave量子退火机。在与基于对比散度的RBM学习相关的条件下，从经典训练的RBM中获取了D-Wave和Gibbs采样样本。通过样本所属局部谷的数量以及相应局部极小值的能量对两种采样结果进行了比较。结果表明，减少D-Wave退火时间并未带来局部谷数量的显著（理想的）增加。在每个训练周期中，D-Wave采样的状态所属的局部谷数量略高于Gibbs采样。然而，两种技术找到的许多局部谷并不相同。对于高概率采样状态，两种技术（不利地）表现出较低的互补性和较高的重叠度。尽管如此，许多潜在“重要”的局部极小值……（摘要在此截断）

    arXiv:2508.10228v3 Announce Type: replace  Abstract: A local-valley (LV) centered approach to assessing the quality of sampling from Restricted Boltzmann Machines (RBMs) was applied to the latest generation of the D-Wave quantum annealer. D-Wave and Gibbs samples from a classically trained RBM were obtained at conditions relevant to the contrastive-divergence-based RBM learning. The samples were compared for the number of the LVs to which they belonged and the energy of the corresponding local minima. No significant (desirable) increase in the number of the LVs has been achieved by decreasing the D-Wave annealing time. At any training epoch, the states sampled by the D-Wave belonged to a somewhat higher number of LVs than in the Gibbs sampling. However, many of those LVs found by the two techniques differed. For high-probability sampled states, the two techniques were (unfavorably) less complementary and more overlapping. Nevertheless, many potentially "important" local minima, i.e., t
    
[^731]: 基于Johnson-Lindenstrauss变换的差分隐私Model-X Knockoffs方法

    Differentially Private Model-X Knockoffs via Johnson-Lindenstrauss Transform

    [https://arxiv.org/abs/2508.04800](https://arxiv.org/abs/2508.04800)

    该论文提出了一种基于Johnson-Lindenstrauss变换的新颖差分隐私化框架，通过降维技术对knockoff矩阵进行隐私化处理，在差分隐私约束下实现了严格的错误发现率（FDR）控制，并从理论上刻画了隐私参数和样本量等因素对变量选择功效的渐近影响。

    

    我们为高维受控变量选择引入了一种新颖的隐私化框架。该框架能够在差分隐私约束下实现严格的错误发现率（FDR）控制。虽然Model-X knockoff程序通过构造可证明可交换的“阴性对照”特征来提供FDR保证，但现有的隐私机制（如高斯噪声注入）会破坏其核心的可交换性条件。在这项工作中，我们考虑通过Johnson-Lindenstrauss变换（JLT）对数据knockoff矩阵进行隐私化处理，JLT是一种降维技术，通过对(ε,δ)-差分隐私的近似等距性，同时保留协变量之间的关系。我们从理论上渐近地刻画了所提出的隐私变量选择程序的FDR和统计功效。我们的理论分析刻画了隐私参数、样本量等不同因素所起的作用。

    arXiv:2508.04800v2 Announce Type: replace-cross  Abstract: We introduce a novel privatization framework for high-dimensional controlled variable selection. Our framework enables rigorous False Discovery Rate (FDR) control under differential privacy constraints. While the Model-X knockoff procedure provides FDR guarantees by constructing provably exchangeable ``negative control" features, existing privacy mechanisms like Gaussian noise injection disrupt its core exchangeability conditions. In this work we consider privatizing the data knockoff matrix through Johnson--Lindenstrauss Transform (JLT), a dimension reduction technique that simultaneously preserves covariate relationships through approximate isometry for $(\epsilon,\delta)$-differential privacy.   We theoretically characterize both FDR and the power of the proposed private variable selection procedure asymptotically. Our theoretical analysis characterizes the role of different factors, such as the privacy parameters, sample si
    
[^732]: 检索增强解码：提升开放式生成中的真实性

    Retrieval-augmented Decoding for Improving Truthfulness in Open-ended Generation

    [https://arxiv.org/abs/2508.02184](https://arxiv.org/abs/2508.02184)

    提出检索增强解码方法RAD，仅用10个标注示例构建参考空间，在推理时通过检索动态塑造logits分布，无需模型重训练即可显著提升开放式生成中语言模型输出的真实性。

    

    确保大型语言模型（LLM）的真实性仍然是实现可靠文本生成的关键挑战。虽然监督微调（SFT）和基于人类反馈的强化学习（RLHF）已展现出潜力，但它们需要大量的标注数据和计算资源，限制了可扩展性。相比之下，解码时干预提供了一种无需重新训练模型的轻量级替代方案。然而，现有的解码策略往往面临提示敏感、泛化能力有限或依赖模型内部状态等问题。我们提出了检索增强解码（RAD），这是一种上下文感知的自适应解码方法，它利用一个紧凑的参考基础空间（仅需10个标注示例即可构建，其中包含来自真实响应的上下文嵌入与下一词元logits的配对数据），从而在推理过程中实现基于检索的logits塑形。在每个解码步骤，RAD检索高质量的语义……

    arXiv:2508.02184v3 Announce Type: replace  Abstract: Ensuring truthfulness in large language models (LLMs) remains a critical challenge for reliable text generation. While supervised fine-tuning and reinforcement learning with human feedback have shown promise, they require a substantial amount of annotated data and computational resources, limiting scalability. In contrast, decoding-time interventions offer lightweight alternatives without model retraining. However, existing decoding strategies often face issues like prompt sensitivity, limited generalization, or dependence on internal model states. We propose Retrieval-Augmented Decoding (RAD), a context-aware adaptive decoding method that leverages a compact reference grounding space built from as few as 10 annotated examples and comprising pairs of context embeddings and next-token logits from truthful responses, to enable retrieval-based logit shaping during inference. At each decoding step, RAD retrieves high-quality semantically
    
[^733]: 基于总风险先验的正式贝叶斯迁移学习

    Formal Bayesian Transfer Learning via the Total Risk Prior

    [https://arxiv.org/abs/2507.23768](https://arxiv.org/abs/2507.23768)

    该论文提出了一种新的贝叶斯迁移学习方法，其核心创新在于使用以源参数为条件的风险最小化器（而非传统的经验损失最小化器）来构建源数据集与目标数据集所有参数的单一联合先验分布，从而实现完整的贝叶斯不确定性量化以及通过Gibbs采样对源数据集进行选择和模型平均。

    

    现有的迁移学习方法难以应对源数据集有限且无法保证与目标数据集良好对齐的情况。一种典型的策略是将源数据上的经验损失最小化器用作目标参数的先验均值。我们的关键概念性贡献是改为使用以源参数为条件的风险最小化器。这使得我们能够基于源数据集和目标数据集为所有参数构建一个单一的联合先验分布。因此，我们可以获得完整的贝叶斯不确定性量化，并可以通过对控制每个源数据集是否被纳入的指示变量进行吉布斯采样（Gibbs sampling）来执行模型平均。我们展示了该先验的一种特定实例化如何在变换后的坐标系中导出贝叶斯Lasso，并讨论了将我们的方法扩展到中等规模数据集的计算技术。我们讨论了……

    arXiv:2507.23768v2 Announce Type: replace-cross  Abstract: Existing methods for transfer learning struggle to deal with situations where the source datasets are limited and not guaranteed to be well-aligned with the target dataset. A typical strategy is to use the empirical loss minimizer on the source data as a prior mean for the target parameters. Our key conceptual contribution is to use a risk minimizer conditional on source parameters instead. This allows us to construct a single joint prior distribution for all parameters from the source datasets as well as the target dataset. As a consequence, we benefit from full Bayesian uncertainty quantification and can perform model averaging via Gibbs sampling over indicator variables governing the inclusion of each source dataset. We show how a particular instantiation of our prior leads to a Bayesian Lasso in a transformed coordinate system and discuss computational techniques to scale our approach to moderately sized datasets. We discus
    
[^734]: HALC简介：一个用于计算社会科学中大语言模型自动编码的提示词系统化、可靠化构建的通用流程

    Introducing HALC: A general pipeline for the systematic and reliable construction of prompts for automated coding with LLMs in the computational social sciences

    [https://arxiv.org/abs/2507.21831](https://arxiv.org/abs/2507.21831)

    本文提出了HALC通用流程，可为计算社会科学中的LLM自动编码任务系统、可靠地构建提示词，并通过超过三百万次LLM请求在不同数据集上验证了其适用性。

    

    大型语言模型（LLM）正被广泛用于任务自动化，包括社会科学中的自动编码。然而，尽管研究人员已经提出了不同的提示策略，但其有效性因LLM和任务的不同而各异，试错式的实践仍然普遍存在。我们的研究旨在填补这一空白，评估如何以系统和透明的方式使用LLM，从而在内容分析中产生可靠的编码结果。我们提出了HALC——一个通用流程，能够为任何给定的编码任务和模型进行系统化、可靠化的提示词构建。我们基于现有文献以及一项调查LLM编码一致性和影响因素的预研究结果开发了该流程。我们还将HALC应用于另外两个数据集，涵盖不同的主题背景、文档类型、语言和编码单元，以检验其适用性。基于超过三百万次LLM请求，我们的结果表明……

    arXiv:2507.21831v2 Announce Type: replace-cross  Abstract: LLMs are seeing widespread use for task automation, including automated coding in the social sciences. However, even though researchers have proposed different prompting strategies, their effectiveness varies across LLMs and tasks. Often trial and error practices are still widespread. Our study aims to fill this gap and evaluate how LLMs can be used in a systematic and transparent way to produce reliable codings in content analyses. We propose HALC-a general pipeline that allows for the systematic and reliable construction of prompts for any given coding task and model. We develop this pipeline based on current literature and findings of a prestudy investigating consistency and influencing factors of LLM codings. We also apply HALC on two other datasets covering different thematic contexts, document types, languages, and coding units to test its applicability. Based on more than three million LLM requests, our results demonstra
    
[^735]: 通过不动点薛定谔型激活学习潜在图几何：一项理论研究

    Learning Latent Graph Geometry via Fixed-Point Schr\"odinger-Type Activation: A Theoretical Study

    [https://arxiv.org/abs/2507.20088](https://arxiv.org/abs/2507.20088)

    本文提出将神经网络隐藏层建模为学习潜在图上耗散薛定谔型动力学的稳态，证明了多层稳态网络等价于超图上的精确全局稳态问题，并建立了带惩罚全局松弛的收敛性理论及基于伴随系统的反向微分框架。

    

    我们研究了一类神经架构，其中每个隐藏层由学习到的潜在图上耗散薛定谔型动力学的稳态所定义。在稳定分支上，局部稳态问题定义了一个可微的隐式图层。为了学习图本身，我们在加权图的分层模态空间上进行优化，并为每个分层配备非退化的凯勒-黑塞（Kähler-Hessian）度量，从而保证自然梯度下降和面交叉问题的良定性。我们进而证明，多层稳态网络等价于超图（supra-graph）上的精确全局稳态问题，且该问题容许一个带惩罚的全局松弛，其稳态在惩罚参数趋于无穷时收敛到精确解。反向模式微分被恢复为精确全局系统的伴随系统，带惩罚的伴随在相同极限下收敛到它。最后，在有限维强单调性条件下……

    arXiv:2507.20088v4 Announce Type: replace  Abstract: We study neural architectures in which each hidden layer is defined by the stationary state of a dissipative Schr\"odinger-type dynamics on a learned latent graph. On stable branches, the local stationary problem defines a differentiable implicit graph layer. To learn the graph itself, we optimize over the stratified moduli space of weighted graphs and equip each stratum with a non-degenerate K\"ahler-Hessian metric that keeps natural-gradient descent and face crossing well posed. We then show that a multilayer stationary network is equivalent to an exact global stationary problem on a supra-graph, and that it admits a penalized global relaxation whose stationary states converge to the exact one as the penalty parameter tends to infinity. Reverse-mode differentiation is recovered as the adjoint of the exact global system, and the penalized adjoint converges to it in the same limit. Finally, under finite-dimensional strong-monotonicit
    
[^736]: Squeeze10-LLM：通过分阶段混合精度量化方法将大语言模型权重压缩10倍

    Squeeze10-LLM: Squeezing LLMs' Weights by 10 Times via a Staged Mixed-Precision Quantization Method

    [https://arxiv.org/abs/2507.18073](https://arxiv.org/abs/2507.18073)

    提出了Squeeze10-LLM分阶段混合精度训练后量化框架，通过将80%权重量化为1比特、20%量化为4比特实现平均每权重1.6比特（即16比特权重压缩10倍），并借助二值化后激活鲁棒性（PBAR）和全信息激活监督（FIAS）两项创新在极端压缩下保持模型性能。

    

    部署大语言模型（LLM）因其庞大的参数量和高昂的计算成本而面临巨大挑战。超低比特量化可以显著减少存储需求并加速推理，但极端压缩（即平均比特宽度≤2）通常会导致严重的性能下降。为解决这一问题，我们提出了Squeeze10-LLM，有效地将16比特大语言模型的权重“压缩”10倍。具体而言，Squeeze10-LLM是一个分阶段混合精度的训练后量化（PTQ）框架，通过将80%的权重量化为1比特、20%的权重量化为4比特，实现了平均每权重1.6比特的压缩。我们引入了Squeeze10-LLM的两项关键创新：二值化后激活鲁棒性（PBAR）和全信息激活监督（FIAS）。PBAR是一种精细化的权重重要性度量指标，它考虑了量化对激活的影响，从而提高了低比特设置下的准确性。FIAS是一种保留完整激活信息的策略……（原文摘要在此处截断）

    arXiv:2507.18073v2 Announce Type: replace  Abstract: Deploying large language models (LLMs) is challenging due to their massive parameters and high computational costs. Ultra low-bit quantization can significantly reduce storage and accelerate inference, but extreme compression (i.e., mean bit-width <= 2) often leads to severe performance degradation. To address this, we propose Squeeze10-LLM, effectively "squeezing" 16-bit LLMs' weights by 10 times. Specifically, Squeeze10-LLM is a staged mixed-precision post-training quantization (PTQ) framework and achieves an average of 1.6 bits per weight by quantizing 80% of the weights to 1 bit and 20% to 4 bits. We introduce Squeeze10LLM with two key innovations: Post-Binarization Activation Robustness (PBAR) and Full Information Activation Supervision (FIAS). PBAR is a refined weight significance metric that accounts for the impact of quantization on activations, improving accuracy in low-bit settings. FIAS is a strategy that preserves full ac
    
[^737]: 吸引子神经网络中密集模式的动力学稳定性

    Dynamical stability for dense patterns in attractor neural networks

    [https://arxiv.org/abs/2507.10383](https://arxiv.org/abs/2507.10383)

    该研究利用随机矩阵理论，为存在噪声且具有分级神经活动的吸引子神经网络发展了离散不动点的局部稳定性理论，突破了以往研究条件的限制，为理解生物记忆中密集存储模式的动力学稳定性提供了新途径。

    

    递归神经网络是生物记忆的经典模型。在这些模型中，记忆由神经活动的分布式模式表示，并存储在神经元之间的递归连接中，使其成为网络动力学的吸引子。在记忆回忆过程中，当网络从嘈杂或不完整的线索开始演化时，网络动力学会收敛到这些记忆模式之一。因此，记忆性能关键取决于存储模式的动力学稳定性。然而，以往的理论方法仅在高度限制性的条件下研究动力学稳定性，这些条件不易适用于生物神经回路。本文针对一大类具有分级神经活动且存在噪声的网络，发展了其中离散不动点的局部稳定性理论。利用随机矩阵理论的方法，我们分析了网络特征值谱的连续谱主体部分和离群特征值……

    arXiv:2507.10383v5 Announce Type: replace-cross  Abstract: Recurrent neural networks are canonical models of biological memory. In these models, memories are represented by distributed patterns of neural activity that are stored in the recurrent connections between neurons, such that they become attractors of the network's dynamics. During memory recall, network dynamics thus converge toward one of these memory patterns when started from a noisy or partial cue. Therefore, memory performance critically hinges on the dynamical stability of the stored patterns. However, previous theoretical approaches only studied dynamical stability under highly restrictive conditions that do not readily apply to biological neural circuits. Here, we develop a theory of the local stability of discrete fixed points in a broad class of networks with graded neural activities and in the presence of noise. Using methods from random matrix theory, we analyze the bulk and outliers of the eigenvalue spectra of th
    
[^738]: 注意间隙：利用最优传输映射进行推断导航

    Mind the Gap: Navigating Inference with Optimal Transport Maps

    [https://arxiv.org/abs/2507.08867](https://arxiv.org/abs/2507.08867)

    该论文提出了一种基于最优传输的模型校准方法，首次应用于高维模拟以解决粒子物理中机器学习面临的模拟与实验数据不一致问题，并通过喷注标记任务验证了其有效性。

    

    机器学习（ML）技术近来在科学领域对新现象的探测灵敏度方面带来了巨大的提升。在粒子物理学中，这些进展很大程度上依赖于对广泛物理过程的优质模拟。然而，由于现代机器学习算法的复杂性及其对高质量训练样本的依赖，模拟与实验数据之间的差异可能会显著限制它们的有效性。在这项工作中，我们提出了一种解决这一“错误设定”问题的方案：一种基于最优传输的模型校准方法，并首次将其应用于高维模拟。我们通过喷注标记任务展示了该方法的表现，所使用的数据集受大型强子对撞机（LHC）CMS实验启发。我们研究了一个来自强大通用分类器的128维内部喷注表示；在对此内部表示进行校准之后……

    arXiv:2507.08867v3 Announce Type: replace-cross  Abstract: Machine learning (ML) techniques have recently enabled enormous gains in sensitivity to new phenomena across the sciences. In particle physics, much of this progress has relied on excellent simulations of a wide range of physical processes. However, due to the sophistication of modern machine learning algorithms and their reliance on high-quality training samples, discrepancies between simulation and experimental data can significantly limit their effectiveness. In this work, we present a solution to this ``misspecification'' problem: a model calibration approach based on optimal transport, which we apply to high-dimensional simulations for the first time. We demonstrate the performance of our approach through jet tagging, using a dataset inspired by the CMS experiment at the Large Hadron Collider. A 128-dimensional internal jet representation from a powerful general-purpose classifier is studied; after calibrating this interna
    
[^739]: 自适应非线性向量自回归：面向含噪混沌时间序列的稳健预测

    Adaptive Nonlinear Vector Autoregression: Robust Forecasting for Noisy Chaotic Time Series

    [https://arxiv.org/abs/2507.08738](https://arxiv.org/abs/2507.08738)

    提出了一种数据自适应的非线性向量自回归模型，通过将浅层MLP与线性读出层进行梯度联合训练来学习数据驱动的非线性特征，从而在含噪混沌时间序列预测中实现更强的稳健性和可扩展性。

    

    非线性向量自回归（NVAR）和储备池计算（RC）在预测混沌动力系统方面展现出了良好的前景，例如Lorenz-63模型和厄尔尼诺-南方涛动现象。然而，这些方法依赖于固定的非线性变换——NVAR中的多项式展开或RC中的随机特征映射——这限制了它们对高噪声或复杂现实数据的适应能力。此外，由于优化过程中需要进行代价高昂的矩阵求逆，这些方法在高维场景下的可扩展性也较差。我们提出了一种数据自适应的NVAR模型，该模型将延迟嵌入的线性输入与由浅层可训练多层感知机（MLP）生成的特征相结合。与标准的NVAR和RC模型不同，该MLP与线性读出层通过基于梯度的优化进行联合训练，使模型能够学习数据驱动的非线性特性，同时保持简单的读出结构并提升了可扩展性。初步实验

    arXiv:2507.08738v3 Announce Type: replace-cross  Abstract: Nonlinear vector autoregression (NVAR) and reservoir computing (RC) have shown promise in forecasting chaotic dynamical systems, such as the Lorenz-63 model and El Nino-Southern Oscillation. However, their reliance on fixed nonlinear transformations - polynomial expansions in NVAR or random feature maps in RC - limits their adaptability to high noise or complex real-world data. Furthermore, these methods also exhibit poor scalability in high-dimensional settings due to costly matrix inversion during optimization. We propose a data-adaptive NVAR model that combines delay-embedded linear inputs with features generated by a shallow, trainable multilayer perceptron (MLP). Unlike standard NVAR and RC models, the MLP and linear readout are jointly trained using gradient-based optimization, enabling the model to learn data-driven nonlinearities, while preserving a simple readout structure and improving scalability. Initial experiments
    
[^740]: 基于强化学习信息策略先验的Q引导Stein变分模型预测控制

    Q-Guided Stein Variational Model Predictive Control via RL-informed Policy Prior

    [https://arxiv.org/abs/2507.06625](https://arxiv.org/abs/2507.06625)

    该论文提出Q-SVMPC方法，将基于学习的模型预测控制转化为轨迹级后验推断，通过强化学习软Q值引导和策略先验下的Stein变分梯度下降保留多样化轨迹解，从而避免传统方法的模式坍缩问题。

    

    模型预测控制（MPC）能够在动力学约束下实现可靠的轨迹优化，但通常依赖于精确的动力学模型和精心手工设计的代价函数。近年来基于学习的MPC方法旨在通过学习动力学、先验或与价值相关的引导信号来减轻这些建模与代价设计的负担。然而，许多现有方法仍依赖于确定性的基于梯度的求解器（如可微分MPC）或参数化的基于采样的更新（如CEM/MPPI），这可能导致模式坍缩并收敛到单一的主导解。我们提出Q-SVMPC，一种具有强化学习信息策略先验的Q引导Stein变分MPC方法，该方法将基于学习的MPC转化为轨迹级后验推断，并在学习到的软Q值引导下通过SVGD对轨迹粒子进行细化，以显式地保留多样化的解。在导航、机器人操作以及真实世界水果采摘（实验）……（摘要截断）

    arXiv:2507.06625v4 Announce Type: replace-cross  Abstract: Model Predictive Control (MPC) enables reliable trajectory optimization under dynamics constraints, but often depends on accurate dynamics models and carefully hand-designed cost functions. Recent learning-based MPC methods aim to reduce these modeling and cost-design burdens by learning dynamics, priors, or value-related guidance signals. Yet many existing approaches still rely on deterministic gradient-based solvers (e.g., differentiable MPC) or parametric sampling-based updates (e.g., CEM/MPPI), which can lead to mode collapse and convergence to a single dominant solution. We propose Q-SVMPC, a Q-guided Stein variational MPC method with an RL-informed policy prior, which casts learning-based MPC as trajectory-level posterior inference and refines trajectory particles via SVGD under learned soft Q-value guidance to explicitly preserve diverse solutions. Experiments on navigation, robotic manipulation, and a real-world fruit-p
    
[^741]: 深度学习实现二维晶体管参数提取与模型拟合的自动化

    Deep Learning to Automate Parameter Extraction and Model Fitting of Two-Dimensional Transistors

    [https://arxiv.org/abs/2507.05134](https://arxiv.org/abs/2507.05134)

    提出一种结合数据增强与预训练的深度学习方法，仅需约500个器件的物理仿真数据即可自动提取二维晶体管的物理参数并完成严格的TCAD模型拟合，数据需求比以往方法减少40倍以上。

    

    我们提出了一种深度学习方法，可以从电学测量中提取二维晶体管的物理参数（例如迁移率、肖特基接触势垒高度、缺陷分布），实现自动化参数提取和工艺计算机辅助设计（TCAD）拟合。为了促进这一任务，我们实施了一种简单的数据增强和预训练方法，即训练一个辅助神经网络来逼近基于物理的器件仿真器。该方法使得在仅使用约500个器件的基于物理的仿真所生成的电学数据训练神经网络后，即可实现高质量拟合，所需数据量比其他近期工作减少了40倍以上。因此，拟合可以通过在物理上严谨的TCAD模型（包括复杂几何结构、自洽输运和静电效应）上训练来实现，而不局限于计算成本较低的紧凑模型。我们将我们的方法应用于逆向工程……（摘要在此处被截断）

    arXiv:2507.05134v2 Announce Type: replace  Abstract: We present a deep learning approach to extract physical parameters (e.g., mobility, Schottky contact barrier height, defect profiles) of two-dimensional (2D) transistors from electrical measurements, enabling automated parameter extraction and technology computer-aided design (TCAD) fitting. To facilitate this task, we implement a simple data augmentation and pre-training approach by training a secondary neural network to approximate a physics-based device simulator. This method enables high-quality fits after training the neural network on electrical data generated from physics-based simulations of ~500 devices, a factor >40$\times$ fewer than other recent efforts. Consequently, fitting can be achieved by training on physically rigorous TCAD models, including complex geometry, self-consistent transport, and electrostatic effects, and is not limited to computationally inexpensive compact models. We apply our approach to reverse-engin
    
[^742]: 检测并解释临床-组学数据不一致性以改进患者队列分层：在帕金森病中的应用

    Detecting and explaining clinical-omics inconsistencies to improve patient cohort stratification: an application to Parkinson's disease

    [https://arxiv.org/abs/2507.03656](https://arxiv.org/abs/2507.03656)

    提出MLASDO工具，通过无监督方法检测临床诊断与分子组学数据之间的不一致性（包括离群值和分子特征与临床类别相反的异常样本），从而在下游分析前识别误诊、隐藏亚群或前驱疾病状态，改进帕金森病等疾病的患者队列分层质量。

    

    在已明确表征的队列中，临床诊断与组学数据之间的差异可能反映误诊、隐藏亚群或疾病前驱状态。我们提出了MLASDO，一种用于在下游分析之前检测和表征此类差异的工具。MLASDO：(1) 使用两种无监督方法检测离群值，从而标记潜在的劣质样本；(2) 识别并表征异常样本（ASs），即其分子谱特征与相反临床类别相似的个体。我们将MLASDO应用于帕金森病进展标志物计划（PPMI）和帕金森病生物标志物计划（PDBP）的帕金森病（PD）队列。在PPMI中，MLASDO检测到26个离群值和12个异常样本：其中5个为异常健康对照（AHCs），7个为异常PD病例（APDs）。AHCs表现出比对照组更高的脑脊液（CSF）Aβ1-42水平（P < 0.0396），提示其对认知衰退具有抵抗力。AHC特异性基因w（原文摘要在此处被截断）

    arXiv:2507.03656v2 Announce Type: replace  Abstract: Discrepancies between clinical diagnoses and omics profiles within a characterized cohort may reflect misdiagnosis, hidden subgroups or prodromal disease states. We propose MLASDO, a tool to detect and characterize such discrepancies before downstream analyses. MLASDO (1) detects outliers using two unsupervised methods, thereby flagging potential poor-quality samples; and (2) identifies and characterizes anomalous samples (ASs), i.e., individuals whose molecular profile resembles the opposite clinical class. We applied MLASDO to the Parkinson's Progression Markers Initiative (PPMI) and Parkinson's Disease Biomarkers Program (PDBP) Parkinson's disease (PD) cohorts. In PPMI, it detected 26 outliers and 12 ASs: 5 anomalous healthy controls (AHCs) and 7 anomalous PD cases (APDs). AHCs exhibited higher cerebrospinal fluid (CSF) A\b{eta}1-42 levels than controls (P < 0.0396), suggesting resistance to cognitive decline. AHC-specific genes w
    
[^743]: 论 z 变换方法在二次优化中的有效性

    On the Effectiveness of the z-Transform Method in Quadratic Optimization

    [https://arxiv.org/abs/2507.03404](https://arxiv.org/abs/2507.03404)

    该论文展示了 z 变换方法作为推导优化算法新渐近收敛结果的有效工具，从无限维希尔伯特空间中的梯度下降出发，将分析依次扩展至 Nesterov 加速、平均技术和随机梯度下降，并用谱维数刻画收敛行为。

    

    序列的 z 变换是信号处理、控制理论、计算机科学和电气工程中广泛使用的经典工具。它使得人们能够从生成函数的角度研究序列，且许多运算可以在原始序列及其 z 变换上等价地定义。特别地，z 变换方法专注于渐近行为，并允许使用泰勒展开。我们针对线性模型和优化算法提出了一系列重要性和难度逐级递增的结果，展示了 z 变换方法在推导新渐近结果方面的有效性与通用性。从无限维希尔伯特空间中最简单的梯度下降迭代出发，我们展示了谱维数如何刻画收敛行为，随后将分析扩展到 Nesterov 加速、平均技术和随机梯度下降。

    arXiv:2507.03404v3 Announce Type: replace  Abstract: The z-transform of a sequence is a classical tool used within signal processing, control theory, computer science, and electrical engineering. It allows for studying sequences from their generating functions, with many operations that can be equivalently defined on the original sequence and its $z$-transform. In particular, the z-transform method focuses on asymptotic behaviors and allows the use of Taylor expansions. We present a sequence of results of increasing significance and difficulty for linear models and optimization algorithms, demonstrating the effectiveness and versatility of the z-transform method in deriving new asymptotic results. Starting from the simplest gradient descent iterations in an infinite-dimensional Hilbert space, we show how the spectral dimension characterizes the convergence behavior. We then extend the analysis to Nesterov acceleration, averaging techniques, and stochastic gradient descent.
    
[^744]: 一种针对高斯过程模型的鲁棒自适应模型预测控制方法

    A robust and adaptive MPC formulation for Gaussian process models

    [https://arxiv.org/abs/2507.02098](https://arxiv.org/abs/2507.02098)

    本文提出了一种基于收缩度量推导高斯过程鲁棒预测的鲁棒自适应模型预测控制框架，以高概率保证递归可行性、鲁棒约束满足和收敛性，并通过在线学习实现显著性能提升。

    

    本文针对受有界扰动和未建模非线性影响的非线性不确定系统，提出了一种鲁棒且自适应的模型预测控制（MPC）框架。我们使用高斯过程（GP）基于含噪测量数据（包括系统运行过程中采集的数据）来学习不确定的动力学特性。作为一项关键贡献，我们利用收缩度量推导出高斯过程模型的鲁棒预测，并将其纳入MPC设计中。所提出的设计以高概率保证递归可行性、鲁棒约束满足以及收敛到参考状态。我们提供了一个受难以建模的地面效应影响的平面四旋翼飞行器数值算例，展示了所提出的鲁棒预测方法和在线学习所取得的显著性能提升。

    arXiv:2507.02098v3 Announce Type: replace-cross  Abstract: In this paper, we present a robust and adaptive model predictive control (MPC) framework for uncertain nonlinear systems affected by bounded disturbances and unmodeled nonlinearities. We use Gaussian Processes (GPs) to learn the uncertain dynamics based on noisy measurements, including those collected during system operation. As a key contribution, we derive robust predictions for GP models using contraction metrics, which are incorporated in the MPC formulation. The proposed design guarantees recursive feasibility, robust constraint satisfaction and convergence to a reference state, with high probability. We provide a numerical example of a planar quadrotor subject to difficult-to-model ground effects, which highlights significant improvements achieved through the proposed robust prediction method and through online learning.
    
[^745]: MILAAP：基于注意力预测的移动链路分配

    MILAAP: Mobile Link Allocation via Attention-based Prediction

    [https://arxiv.org/abs/2506.19947](https://arxiv.org/abs/2506.19947)

    提出MiLAAP框架，利用基于自注意力的深度学习预测信道占用情况，使信道跳变系统无需节点间状态共享即可适应干扰变化和节点移动，从而降低通信开销并提升网络吞吐量效率。

    

    信道跳变（CS）通信系统必须适应无线网络中的干扰变化和节点移动性，以维持吞吐量效率。最优调度需要最新的网络状态信息（即信道占用情况），以便为干扰区域内的链路选择互不重叠的信道。然而，节点间的状态共享会引入显著的通信开销，随着网络规模或节点移动性的扩大，这种开销会进一步降低本已容量受限的网络吞吐量效率。在本文中，我们摒弃了状态共享，转而基于学习式的信道占用预测来调整CS调度。我们提出了MiLAAP——一个基于注意力的预测框架，用于对网络节点之间的频谱、空间和时间依赖关系进行机器学习建模。MiLAAP采用自注意力机制，使每个节点能够捕获其干扰区域内的时空频谱CS模式。

    arXiv:2506.19947v2 Announce Type: replace-cross  Abstract: Channel hopping (CS) communication systems must adapt to interference changes in the wireless network and to node mobility for maintaining throughput efficiency. Optimal scheduling requires up-to-date network state information (i.e., of channel occupancy) to select non-overlapping channels for links in interference regions. However, state sharing among nodes introduces significant communication overhead, especially as network size or node mobility scale, thereby decreasing throughput efficiency of already capacity-limited networks. In this paper, we eschew state sharing while adapting the CS schedule based on a learning-based channel occupancy prediction. We propose the MiLAAP attention-based prediction framework for machine learning models of spectral, spatial, and temporal dependencies among network nodes. MiLAAP uses a self-attention mechanism that lets each node capture the temporospectral CS pattern in its interference reg
    
[^746]: 时间序列分析中长程预测问题综述

    A Review of the Long Horizon Forecasting Problem in Time Series Analysis

    [https://arxiv.org/abs/2506.12809](https://arxiv.org/abs/2506.12809)

    本综述系统梳理了35年来时间序列长程预测（LHF）问题的发展，总结了深度学习如何结合趋势/季节性分解、傅里叶与小波变换以及卷积、注意力、SSM等架构创新来提升长程预测性能，并通过ETTm2数据集上的消融实验加以验证。

    

    长程预测（LHF）问题在过去约35年的时间里持续出现在时间序列文献中。本综述涵盖了这一时期内LHF问题的各个方面，以及深度学习如何融合趋势、季节性、傅里叶变换和小波变换的变体、误设偏差降低和带通滤波器等技术，同时通过卷积、残差连接、稀疏性降低、步幅卷积、注意力掩码、状态空间模型（SSM）、归一化方法、低秩近似和门控机制等架构创新作出贡献。我们重点介绍了能够提升性能的时间序列分解技术、输入数据预处理以及数据集窗口划分方案。文中描述了改进和/或解决LHF问题性能的多层感知机模型、循环神经网络混合模型以及自注意力模型，并着重讨论了特征空间的构建。消融实验在ETTm2数据集上的多变量（原文在此处截断）

    arXiv:2506.12809v2 Announce Type: replace  Abstract: The long horizon forecasting (LHF) problem has come up in the time series literature for over the last 35 years or so. This review covers aspects of LHF in this period and how deep learning has incorporated variants of trend, seasonality, fourier and wavelet transforms, misspecification bias reduction and bandpass filters while contributing using convolutions, residual connections, sparsity reduction, strided convolutions, attention masks, SSMs, normalization methods, low-rank approximations and gating mechanisms. We highlight time series decomposition techniques, input data preprocessing and dataset windowing schemes that improve performance. Multi-layer perceptron models, recurrent neural network hybrids, self-attention models that improve and/or address the performances of the LHF problem are described, with an emphasis on the feature space construction. Ablation studies are conducted over the ETTm2 dataset in the multivariate and
    
[^747]: 从代理数据到场：基于稀疏传感器序列的全球辐射时空重建

    From Proxies to Fields: Spatiotemporal Reconstruction of Global Radiation from Sparse Sensor Sequences

    [https://arxiv.org/abs/2506.12045](https://arxiv.org/abs/2506.12045)

    本文提出时间辐射算子网络（TRON），一种时空神经算子架构，能够仅从稀疏、非均匀的代理测量序列中实时重建连续的全球标量场，无需密集网格输入和未来观测数据。

    

    从大气科学、地球物理到公共卫生和航空航天安全，从稀疏、间接的观测中准确重建潜在环境场是各科学领域面临的一项根本性挑战。现有方法通常依赖于基于物理的模拟或密集的传感器网络；然而，这些方法受到高计算成本、延迟和有限空间覆盖范围的制约。我们在此提出时间辐射算子网络（TRON），这是一种时空神经算子架构，能够仅从稀疏、非均匀的代理测量序列中推断出连续的全球标量场。与近期需要密集网格输入来预测系统状态的预测模型不同，TRON 解决了稀疏到密集的跨域场重建问题。它从稀疏的、随时间演化的传感器数据中实时重建当前的全球场，而无需获取任何未来观测数据。

    arXiv:2506.12045v2 Announce Type: replace-cross  Abstract: Accurate reconstruction of latent environmental fields from sparse, indirect observations is a fundamental challenge across scientific domains, from atmospheric science and geophysics to public health and aerospace safety. Existing approaches typically rely on physics-based simulations or dense sensor networks; however, these methods are hampered by high computational cost, latency, and limited spatial coverage. Here we introduce the \textbf{Temporal Radiation Operator Network (TRON)}, a spatiotemporal neural operator architecture that infers continuous global scalar fields solely from sequences of sparse, non-uniform proxy measurements. Unlike recent prediction models that require dense, gridded inputs to predict system states, TRON tackles sparse-to-dense, cross-domain field reconstruction. It reconstructs the current global field in real time from sparse, temporally evolving sensor data, without access to any future observat
    
[^748]: 基于数据融合的轴承与感应电机故障诊断深度学习方法

    Deep Learning Approach to Bearing and Induction Motor Fault Diagnosis via Data Fusion

    [https://arxiv.org/abs/2506.11032](https://arxiv.org/abs/2506.11032)

    本文提出利用CNN评估加速度计与麦克风数据，并通过LSTM循环神经网络实现多传感器数据融合，从而实现轴承与感应电机的高效故障诊断。

    

    本文采用卷积神经网络（CNN）对加速度计和麦克风数据进行评估，用于轴承和感应电机的故障诊断。研究使用长短期记忆（LSTM）循环神经网络有效融合传感器信息，凸显了数据融合的优势。该方法通过提出一种综合运用深度学习与传感器融合的全面方案，鼓励研究人员关注恒定转速数据采集下的多模型诊断，并鼓励数据科学家收集更多多传感器数据，包括声学与加速度计数据集。

    arXiv:2506.11032v2 Announce Type: replace  Abstract: Convolutional Neural Networks (CNNs) are used to evaluate accelerometer and microphone data for bearing and induction motor diagnosis. A Long Short-Term Memory (LSTM) recurrent neural network is used to combine sensor information effectively, highlighting the benefits of data fusion. This approach encourages researchers to focus on multi model diagnosis for constant speed data collection by proposing a comprehensive way to use deep learning and sensor fusion and encourages data scientists to collect more multi-sensor data, including acoustic and accelerometer datasets.
    
[^749]: NoLoCo：一种无需全归约的大模型低通信训练方法

    NoLoCo: No-all-reduce Low Communication Training Method for Large Models

    [https://arxiv.org/abs/2506.10911](https://arxiv.org/abs/2506.10911)

    提出了无需全归约和集合通信的优化方法 NoLoCo，通过 Nesterov 动量的新颖变体隐式同步模型权重，从而大幅降低大规模语言模型训练的通信成本。

    

    训练大型语言模型通常需要在包含数千个加速器的集群上进行，这些加速器通过高带宽互连进行通信。扩展这类集群的成本高昂，甚至可能变得不切实际，从而限制了可训练模型的规模。近期的一些研究提出了通信密集度较低的训练方法，以避免对具有极高互连速度的计算集群的需求。然而，这些低通信训练方法仍需要对模型参数进行全局同步步骤，当参与者数量较多时，该步骤的代价可能过于高昂，因为通信成本会随组规模的平方增长。在本工作中，我们提出了一种新颖的优化方法 NoLoCo，该方法在训练过程中不显式同步所有模型参数，且不需要任何集合通信。NoLoCo 通过一种新颖的 Nesterov 动量变体隐式同步模型权重（摘要原文在此处截断）。

    arXiv:2506.10911v2 Announce Type: replace  Abstract: Training large language models is generally done on clusters containing thousands of accelerators, communicating over a high-bandwidth interconnect. Scaling up these clusters is expensive and can become impractical, imposing limits on the size of models that can be trained. Several recent studies have proposed training methods that are less communication intensive, avoiding the need for compute clusters with extremely high interconnect speeds. These low communication training methods still employ a global synchronization step for model parameters, which can be too costly with a high number of participants, as the communication cost scales quadratically with group size. In this work, we propose a novel optimization method, NoLoCo, that does not explicitly synchronize all model parameters during training and does not require any collective communication. NoLoCo implicitly synchronizes model weights via a novel variant of the Nesterov m
    
[^750]: N元知识表示学习方法的二维分类法

    Two-dimensional Taxonomy for N-ary Knowledge Representation Learning Methods

    [https://arxiv.org/abs/2506.05626](https://arxiv.org/abs/2506.05626)

    本综述针对知识图谱将复杂n元关系简化为三元组而丢失高阶关系细节的问题，提出了N元知识表示学习方法的二维分类体系，系统梳理了知识超图（KHG）和超关系知识图谱（HKG）等融合知识图谱与超图优势的新兴方法。

    

    现实世界的知识可以呈现多种形式，包括结构化、半结构化和非结构化数据。其中，知识图谱是将异构数据源整合为结构化表示的结构化表示形式。然而，知识图谱通常将复杂的n元关系简化为简单的三元组，从而丢失了高阶关系的细节。相比之下，超图通过直接连接多个实体的超边自然地表示n元关系。近年来的研究进展催生了许多超图表示学习方法；然而，这些方法往往忽略了实体在超边中的角色，限制了细粒度的语义建模。为了解决这些问题，知识超图和超关系知识图谱结合了知识图谱和超图的优势，以更好地捕捉现实世界知识的复杂关系结构和角色特定语义。本综述提供了一个结构化的回顾……（摘要截断）

    arXiv:2506.05626v3 Announce Type: replace  Abstract: Real-world knowledge can take various forms, including structured, semi-structured, and unstructured data. Among these, Knowledge Graphs (KGs) are structured representations that integrate heterogeneous data sources into structured representations. However, KGs typically reduce complex n-ary relations to simple triples, thereby losing higher-order relational details. In contrast, hypergraphs naturally represent n-ary relations with hyperedges that directly connect multiple entities. Recent advances have led to many hypergraph representation learning methods; however, they often overlook entity roles in hyperedges, limiting fine-grained semantic modelling. To address these issues, Knowledge Hypergraphs (KHGs) and Hyper-relational Knowledge Graphs (HKGs) combine the advantages of KGs and hypergraphs to better capture complex relational structures and role-specific semantics of real-world knowledge. This survey provides a structured rev
    
[^751]: 基于协变量重叠正则化的多任务学习

    Multi-Task Learning with Covariate-Overlap Regularization

    [https://arxiv.org/abs/2505.24281](https://arxiv.org/abs/2505.24281)

    提出了一种协变量重叠正则化多任务学习方法（COVER），通过共享神经表示与低维任务特定系数，并利用基于逐任务二阶矩矩阵的协变量重叠惩罚，有效解决了多任务学习中协变量分布与响应关系异质性的问题。

    

    多任务学习通过在相关任务之间共享信息来提高数据效率，但当各任务的协变量分布和响应关系存在差异时，不加区分的共享可能是有害的。我们提出了协变量重叠正则化多任务学习方法（COVER），以解决协变量和后验异质性问题。该模型将一个公共分量函数与共享神经表示以及低维的任务特定系数相结合。逐任务的二阶矩矩阵用于总结协变量的异质性，并决定每个表示方向上系数整合的强度。我们通过最小化两个任务预测器在系数被替换为单一辅助系数时的总平方变化，推导出了协变量重叠惩罚项。一种等价的辅助形式支持无需矩阵求逆的端到端训练。精确的固定表示偏差—方差分解量化了协变量……（原文摘要在此处截断）

    arXiv:2505.24281v2 Announce Type: replace-cross  Abstract: Multi-task learning improves data efficiency by sharing information across related tasks, but indiscriminate sharing can be harmful when their covariate distributions and response relationships differ. We propose COVariate-ovERlap regularized multi-task learning (COVER) to address covariate and posterior heterogeneity. The model combines a common component function with a shared neural representation and low-dimensional task-specific coefficients. Taskwise second-moment matrices summarize covariate heterogeneity and determine the strength of coefficient integration in each representation direction. We derive a covariate-overlap penalty by minimizing the total squared change in two task predictors when their coefficients are replaced by one auxiliary coefficient. An equivalent auxiliary formulation supports end-to-end training without matrix inversion. An exact fixed-representation bias--variance decomposition quantifies how cov
    
[^752]: Kronecker 分解提升稀疏自编码器的效率与可解释性

    Kronecker Factorization Improves Efficiency and Interpretability of Sparse Autoencoders

    [https://arxiv.org/abs/2505.22255](https://arxiv.org/abs/2505.22255)

    KronSAE 通过 Kronecker 分解将稀疏自编码器的潜在空间结构化为低维成分的组合，并利用可微分的类 AND 交互建模特征共激活，在兼容标准 SAE 目标的同时提升了效率与可解释性。

    

    稀疏自编码器（SAE）能够将语言模型的激活值分解为稀疏、可解释的特征，但标准编码器通常将潜在字典视为一组平坦的独立坐标，使得层次结构与特征交互只能隐式地涌现。我们提出 KronSAE，该设计将潜在空间分解为多个头，并通过 mAND（一种可微分的类 AND 交互）将低维前潜在表示进行成对组合，从而形成后潜在特征。这种方式在施加组合式共同激活先验的同时，仍与标准 SAE 目标函数以及 TopK、Matryoshka 和 Switch SAE 等变体保持兼容。KronSAE 在 EV-FLOPs 前沿上与强基线方法表现相当，提升了潜在表示的可解释性，更好地捕捉了潜在的相关特征结构，并且还额外降低了编码器的计算成本。代码可在 https://github.com/corl-team/kronsae 获取。

    arXiv:2505.22255v4 Announce Type: replace-cross  Abstract: Sparse Autoencoders (SAEs) decompose language-model activations into sparse, interpretable features, but standard encoders usually treat the latent dictionary as a flat set of independent coordinates, leaving hierarchy and feature interactions to emerge only implicitly. We propose KronSAE, a design that factorizes the latent space into heads and forms post-latent features as pairwise compositions of lower-dimensional pre-latents using mAND, a differentiable AND-like interaction. This imposes a compositional co-activation prior while remaining compatible with standard SAE objectives and variants such as TopK, Matryoshka, and Switch SAEs. KronSAE matches strong baselines on the EV-FLOPs frontier, improves interpretability of the latents, better captures the underlying correlated feature structure, and reduces encoder computational cost as an additional benefit. Code is available at https://github.com/corl-team/kronsae.
    
[^753]: 高效且麦克风容错的3D声源定位

    Efficient and Microphone-Fault-Tolerant 3D Sound Source Localization

    [https://arxiv.org/abs/2505.20961](https://arxiv.org/abs/2505.20961)

    本文提出了一种结合稀疏交叉注意力、预训练和自适应信号相干性度量的3D声源定位框架，在减少麦克风数量和计算成本的同时实现精确定位，并能容错未知位置的麦克风。

    

    声源定位（SSL）是确定复杂环境中声源位置的关键技术。然而，现有方法面临计算成本高和需要精确校准等挑战，限制了其在动态或资源受限环境中的部署。本文介绍了一种新颖的3D声源定位框架，该框架利用稀疏交叉注意力、预训练和自适应信号相干性度量，以更少的输入麦克风实现精确且计算高效的定位。该框架还支持位于未知位置的正常工作的麦克风：其录音仍然可用，并被用于估计声源和麦克风的位置。初步实验证明了该框架在无需额外硬件的情况下对多声源定位的可扩展性。这项工作通过平衡模型性能与效率并提高其鲁棒性，推动了声源定位技术的发展。

    arXiv:2505.20961v2 Announce Type: replace-cross  Abstract: Sound source localization (SSL) is a critical technology for determining the position of sound sources in complex environments. However, existing methods face challenges such as high computational costs and precise calibration requirements, limiting their deployment in dynamic or resource-constrained environments. This paper introduces a novel 3D SSL framework, which uses sparse cross-attention, pretraining, and adaptive signal coherence metrics, to achieve accurate and computationally efficient localization with fewer input microphones. The framework also supports operational microphones at unknown positions: their recordings remain available and are used to estimate both source and microphone positions. Preliminary experiments demonstrate its scalability for multi-source localization without requiring additional hardware. This work advances SSL by balancing the model's performance and efficiency and improving its robustness f
    
[^754]: ControlTac：通过物理可控的触觉图像生成扩展触觉数据规模

    ControlTac: Scaling Tactile Data with Physically Controlled Tactile Image Generation

    [https://arxiv.org/abs/2505.20498](https://arxiv.org/abs/2505.20498)

    ControlTac提出了一种两阶段可控触觉图像生成框架，以单个参考触觉图像、接触力和接触位姿作为物理先验条件来生成逼真的触觉图像，从而实现跨传感器的触觉数据低成本扩展，并有效提升真实世界下游任务的表现。

    

    基于视觉的触觉传感被广泛应用于感知、重建和机器人操作中，然而由于传感器与物体交互的多样性以及不同传感器实例之间的不一致性，收集大规模触觉数据仍然成本高昂。现有的触觉数据扩展方法——仿真和自由形式的触觉生成——通常会产生不真实的渲染信号，难以迁移到高度动态的真实世界任务中。我们提出ControlTac，一个两阶段可控触觉图像生成框架，它以单个参考触觉图像、接触力和接触位姿为条件生成逼真的触觉图像。通过将生成过程建立在这些重要的物理先验之上，ControlTac能够在不同传感器之间合成逼真的样本，同时有效捕捉与任务相关的变化。在一系列下游任务和真实世界实验中，例如物体插入、模仿学习和物体重称量任务中，

    arXiv:2505.20498v3 Announce Type: replace-cross  Abstract: Vision-based tactile sensing is widely used in perception, reconstruction, and robotic manipulation, yet collecting large-scale tactile data remains costly due to diverse sensor-object interactions and inconsistencies across sensor instances. Existing approaches to scaling tactile data---simulation and free-form tactile generation---often yield unrealistically rendered signals with poor transfer to highly dynamic real-world tasks. We propose \name, a two-stage controllable tactile image generation framework that generates realistic tactile images conditioned on a single reference tactile image, contact force, and contact pose. By grounding generation in these important physical priors, \name synthesizes realistic samples across different sensors while effectively capturing task-relevant variations. Across a series of downstream tasks and real-world experiments, such as object insertion, imitation learning, and object weighting,
    
[^755]: 因果数据融合中的聚类与剪枝

    Clustering and Pruning in Causal Data Fusion

    [https://arxiv.org/abs/2505.15215](https://arxiv.org/abs/2505.15215)

    本文提出剪枝与聚类作为因果数据融合的预处理操作，通过移除不必要变量和合并变量来简化复杂因果模型，在保留关键特征的同时降低计算开销，使因果效应的可识别性分析更加高效。

    

    数据融合是将观测数据和实验数据相结合的过程，能够使原本无法识别的因果效应变得可识别。尽管针对特定场景已经开发了一些识别算法，但do-演算仍然是因果数据融合唯一通用的工具，特别是当某些变量仅存在于部分数据源时。然而，随着变量数量的增加和因果图复杂性的增长，基于do-演算的方法可能面临计算上的挑战。因此，需要在保留模型基本特征的同时减小此类模型的规模。为此，我们提出剪枝（移除不必要的变量）和聚类（合并变量）作为因果数据融合的预处理操作。我们将早期针对单一数据源的结果进行了推广，并推导了应用剪枝和聚类的条件。

    arXiv:2505.15215v4 Announce Type: replace-cross  Abstract: Data fusion, the process of combining observational and experimental data, can enable the identification of causal effects that would otherwise remain non-identifiable. Although identification algorithms have been developed for specific scenarios, do-calculus remains the only general-purpose tool for causal data fusion, particularly when variables are present in some data sources but not others. However, approaches based on do-calculus may encounter computational challenges as the number of variables increases and the causal graph grows in complexity. Consequently, there exists a need to reduce the size of such models while preserving the essential features. For this purpose, we propose pruning (removing unnecessary variables) and clustering (combining variables) as preprocessing operations for causal data fusion. We generalize earlier results on a single data source and derive conditions for applying pruning and clustering in 
    
[^756]: 神经网络中可证明组合泛化的理论分析：一个充分必要条件

    A Theoretical Analysis of Provable Compositional Generalization in Neural Networks: A Necessary and Sufficient Condition

    [https://arxiv.org/abs/2505.02627](https://arxiv.org/abs/2505.02627)

    本文首次建立了神经网络可证明组合泛化的充分必要条件——即“结构对齐”与“无歧义最小化表示”两个原则，并在Lean 4中完成了机器验证，该条件即使在少样本和单样本情形下依然成立。

    

    组合泛化——即系统性地处理已知组件的新颖组合的能力——是人类智能的重要标志；然而，其在神经网络中的理论基础尚未得到充分理解。本文建立了可证明组合泛化的充分必要条件，精确刻画了其边界。从概念上讲，该条件包含两个原则：（i）结构对齐，即模型的计算图与任务的真实组合层次结构保持一致；（ii）无歧义的最小化表示，即每个组件对训练数据编码的信息充分但不冗余。该结果在Lean 4中得到了完整证明和机器验证，并且即使在小样本和单样本情形下依然成立。必要性方向证明了可证明组合泛化无法绕过这些要求，而……（摘要在此处不完整）

    arXiv:2505.02627v2 Announce Type: replace-cross  Abstract: Compositional generalization$\unicode{x2013}$the ability to systematically process novel combinations of known components$\unicode{x2013}$is a hallmark of human intelligence; however, its theoretical foundation in neural networks is not yet well understood. This paper establishes a necessary and sufficient condition for provable compositional generalization, precisely characterizing its boundary. Conceptually, the condition consists of two principles: (i) structural alignment, where a model's computational graph aligns with a task's true compositional hierarchy, and (ii) unambiguous minimized representations, where each component encodes adequate but not redundant information on the training data. The result is fully proved and machine-verified in Lean 4 and holds even in few-shot and one-shot regimes. The necessity direction establishes that provable compositional generalization cannot circumvent these requirements, while the 
    
[^757]: 如何对图像知识蒸馏进行后门攻击

    How to Backdoor Image Knowledge Distillation

    [https://arxiv.org/abs/2504.21323](https://arxiv.org/abs/2504.21323)

    本文提出了一种针对图像知识蒸馏的新型后门攻击方法，通过向蒸馏数据集注入被干净教师模型预测为目标标签的触发图像，使学生在教师保持干净的情况下学到后门，且在10%中毒率下仍然有效。

    

    知识蒸馏被广泛用于将大型教师模型的行为迁移到较小的学生模型。当教师模型是干净的时，知识蒸馏通常被认为是安全的，因为经典的后门攻击依赖于监督训练中的中毒标签和触发器，而蒸馏则是训练学生模型去匹配教师模型的输出。我们表明，当蒸馏数据集本身被投毒时，这一假设可能会失效。我们的攻击注入带有触发器且经过操纵的图像，这些图像会被干净的教师模型预测为攻击者选定的目标标签，从而使学生在教师模型不受影响的情况下学到后门。我们在多种操纵策略下评估了这一威胁，包括针对性的对抗扰动和基于GAN的针对性类别转换，并研究了蒸馏设置如何同时影响准确率和攻击成功率。我们表明，即使在10%的中毒率下，该攻击仍然有效。

    arXiv:2504.21323v3 Announce Type: replace-cross  Abstract: Knowledge distillation is widely used to transfer behavior from a large teacher model to a smaller student. It is often assumed to be safe when the teacher is clean, because classic backdoor attacks rely on poisoned labels and triggers in supervised training, whereas distillation trains the student to match a teacher's outputs. We show that this assumption can fail when the distillation dataset itself is poisoned. Our attack injects triggered and manipulated images that a clean teacher already predicts as an attacker chosen target label, which causes the student to learn a backdoor even though the teacher remains unaffected. We evaluate this threat across multiple manipulation strategies, including targeted adversarial perturbations and targeted GAN based class transitions, and study how distillation settings influence both accuracy and attack success. We show that the attack remains effective even at a 10% poisoning rates. The
    
[^758]: 深度学习中泛化的动力学

    The Dynamics of Generalization in Deep Learning

    [https://arxiv.org/abs/2504.16450](https://arxiv.org/abs/2504.16450)

    该论文推导出一个刻画泛化差距演化的微分方程，将泛化差距表示为依赖于训练轨迹的“有效Gram矩阵”与初始化残差构成的二次型，为一般深度网络提供了统一的泛化理论框架。

    

    我们推导出了一个微分方程，用于刻画模型在基于梯度下降的方法训练时泛化差距的演化过程。该微分方程由两个关键量驱动：一个是收缩因子，它使对应于略有不同数据集的训练轨迹相互靠近；另一个是扰动因子，它刻画了在不同数据集上训练所产生的差异。收缩与扰动的耦合衰减保证了训练过程中泛化差距的可控累积。我们通过分析该微分方程表明，泛化差距可表示为一个二次型，该二次型由一个依赖于训练轨迹的“有效Gram矩阵”以及预测器在初始化时的某种残差构成。我们的框架适用于一般的深度网络和光滑损失函数。在不同神经网络架构、数据集和样本量上的数值实验中，我们展示了……

    arXiv:2504.16450v4 Announce Type: replace  Abstract: We derive a differential equation that governs the evolution of the generalization gap when a model is trained by gradient descent-based methods. This differential equation is driven by two key quantities, a contraction factor that brings together trajectories corresponding to slightly different datasets, and a perturbation factor that accounts for them training on different datasets. The coupled decay of contraction and perturbation guarantees a controlled accumulation of generalization gap during training. We analyze this differential equation to show that the generalization gap is given by a quadratic form that consists of an ``effective Gram matrix'' that depends upon the training trajectory and a certain residual of the predictor at initialization. Our framework is applicable to general deep networks and smooth loss functions. In numerical experiments on different neural network architectures, datasets and sample sizes, we show 
    
[^759]: 基于特征值及其单位根的神经状态空间模型可观测性条件

    Observability conditions for neural state-space models with eigenvalues and their roots of unity

    [https://arxiv.org/abs/2504.15758](https://arxiv.org/abs/2504.15758)

    本文借助常微分方程与控制理论，为神经状态空间模型和Mamba架构提出了利用特征值和单位根来保证可观测性的高效计算策略。

    

    我们通过常微分方程和控制理论的视角，研究神经状态空间模型和Mamba架构中的可观测性概念。我们提出了强制可观测性的策略，这些策略专为学习场景量身定制，特别是隐藏状态在初始时刻可学习、在其连续区间上可学习以及高维的情形。我们还强调我们的方法侧重于特征值、单位根，或两者兼有。我们的方法在强制可观测性时实现了计算效率，有时可在大规模下运作。我们基于经典控制理论制定了机器学习中的可观测性条件，并讨论了它们的计算复杂度。我们的非平凡结果有五个方面：我们讨论了在具有可学习矩阵且无需高精度的神经应用中通过置换实现可观测性；我们提出了基于傅里叶变换的两个结果……

    arXiv:2504.15758v3 Announce Type: replace  Abstract: We operate through the lens of ordinary differential equations and control theory to study the concept of observability in the context of neural state-space models and the Mamba architecture. We develop strategies to enforce observability, which are tailored to a learning context, specifically where the hidden states are learnable at initial time, in conjunction to over its continuum, and high-dimensional. We also highlight our methods emphasize eigenvalues, roots of unity, or both. Our methods effectuate computational efficiency when enforcing observability, sometimes at great scale. We formulate observability conditions in machine learning based on classical control theory and discuss their computational complexity. Our nontrivial results are fivefold. We discuss observability through the use of permutations in neural applications with learnable matrices without high precision. We present two results built upon the Fourier transfor
    
[^760]: SAE可以改进机器遗忘：用于大语言模型精准遗忘的动态稀疏自编码器防护栏

    SAEs Can Improve Unlearning: Dynamic Sparse Autoencoder Guardrails for Precision Unlearning in LLMs

    [https://arxiv.org/abs/2504.08192](https://arxiv.org/abs/2504.08192)

    本文提出动态稀疏自编码器防护栏（DSG），通过有原则的特征选择和动态分类器实现大语言模型的精准遗忘，其表现显著优于现有基于梯度的遗忘方法。

    

    机器遗忘是一种很有前景的方法，通过从模型中移除不需要的知识来提高大语言模型的安全性。然而，目前主流的基于梯度的遗忘方法存在诸多问题，如高计算成本、超参数不稳定、顺序遗忘能力差、易受再学习攻击、数据效率低以及缺乏可解释性。虽然稀疏自编码器（SAE）非常适合通过实现基于激活的定向遗忘来改善这些问题，但此前的方法表现不如基于梯度的方法。本工作表明，与这些早期发现相反，当动态使用时，SAE可以显著改善遗忘效果。我们提出了动态DAE防护栏（DSG），这是一种新颖的精准遗忘方法，利用有原则的特征选择和动态分类器。我们的实验表明，DSG大幅超越了领先的遗忘方法，取得了卓越的遗忘表现。

    arXiv:2504.08192v2 Announce Type: replace-cross  Abstract: Machine unlearning is a promising approach to improve LLM safety by removing unwanted knowledge from the model. However, prevailing gradient-based unlearning methods suffer from issues such as high computational costs, hyperparameter instability, poor sequential unlearning capability, vulnerability to relearning attacks, low data efficiency, and lack of interpretability. While Sparse Autoencoders are well-suited to improve these aspects by enabling targeted activation-based unlearning, prior approaches underperform gradient-based methods. This work demonstrates that, contrary to these earlier findings, SAEs can significantly improve unlearning when employed dynamically. We introduce $\textbf{Dynamic DAE Guardrails}$ (DSG), a novel method for precision unlearning that leverages principled feature selection and a dynamic classifier. Our experiments show DSG substantially outperforms leading unlearning methods, achieving superior 
    
[^761]: DDPM分数匹配与分布学习

    DDPM Score Matching and Distribution Learning

    [https://arxiv.org/abs/2504.05161](https://arxiv.org/abs/2504.05161)

    本文提出一个将分数估计归约为参数估计与密度估计这两个经典统计任务的框架，从而在DDPM分数匹配与经典统计学习理论之间建立了联系。

    

    分数估计是基于分数的生成模型（SGM）的支柱，尤其是去噪扩散概率模型（DDPM）。该领域的一个关键结果表明，凭借准确的分数估计，SGM能够高效地从任何现实的数据分布中生成样本（Chen等人，ICLR'23；Lee等人，ALT'23）。然而，这一分布学习结果中，学习到的分布隐含地是采样器输出的分布，它并未解释分数估计与经典的参数估计和密度估计任务之间的关系。本文引入了一个框架，将分数估计归约为这两个经典任务，并对统计与计算学习理论产生了多方面的启示。在参数估计方面：Koehler等人（ICLR'23）证明了一种分数匹配变体在实践中常见的多峰密度的参数估计上具有统计低效性；相比之下，我们证明在温和的条件下……

    arXiv:2504.05161v2 Announce Type: replace-cross  Abstract: Score estimation is the backbone of score-based generative models (SGMs), especially denoising diffusion probabilistic models (DDPMs). A key result in this area shows that with accurate score estimates, SGMs can efficiently generate samples from any realistic data distribution (Chen et al., ICLR'23; Lee et al., ALT'23). This distribution learning result, where the learned distribution is implicitly that of the sampler's output, does not explain how score estimation relates to classical tasks of parameter and density estimation.   This paper introduces a framework that reduces score estimation to these two tasks, with various implications for statistical and computational learning theory:   Parameter Estimation: Koehler et al. (ICLR'23) demonstrate that a score-matching variant is statistically inefficient for the parametric estimation of multimodal densities common in practice. In contrast, we show that under mild conditions, d
    
[^762]: 少而关键：减少基于Transformer的时间序列预测中的冗余标记依赖

    Fewer yet critical: Reducing Redundant Token Dependencies for Transformer-based Time Series Forecasting

    [https://arxiv.org/abs/2503.06867](https://arxiv.org/abs/2503.06867)

    提出一种联合利用注意力熵约束与预测误差约束的标记依赖选择策略，使基于Transformer的时间序列预测模型能够识别更少但更关键的标记间依赖，从而避免冗余依赖的干扰并提升泛化能力。

    

    时间序列预测（TSF）在现实世界的应用中非常重要。近年来，基于Transformer的方法通过注意力机制对标记（token）依赖进行建模，取得了出色的性能。然而，现有方法通常主要使用预测误差损失进行训练，这可能导致模型同时利用关键的和冗余的标记依赖。这种冗余依赖会引入无关信息并削弱模型的泛化能力。为了解决这一问题，我们提出了一种简单而有效的标记依赖选择策略。具体而言，通过联合引入注意力熵约束和预测误差约束，模型能够识别出更少但更关键的标记间依赖，并基于这些依赖进行预测，从而避免冗余依赖的干扰。所提出的方法可以轻松扩展到各种基于Transformer的时间序列预测模型。在多个时间序列预测数据集上的实验表明……（摘要内容不完整）

    arXiv:2503.06867v2 Announce Type: replace  Abstract: Time series forecasting (TSF) is important in real-world applications. Recently, Transformer-based methods have achieved strong performance by modeling token dependencies through attention mechanisms. However, existing methods are usually trained mainly with prediction error losses, which may cause models to exploit both critical and redundant token dependencies. Such redundant dependencies can introduce irrelevant information and weaken generalization. To address this issue, we propose a simple yet effective token dependency selection strategy. Specifically, by jointly introducing the attention entropy constraint and prediction error constraint, the model can identify fewer but more critical inter-token dependencies and perform forecasting based on them, thus avoiding the interference of redundant dependencies. The proposed method can be easily extended to various Transformer-based TSF models. Experiments on multiple TSF datasets de
    
[^763]: 仅通过翻转标签即可接近梯度攻击的危害

    Approaching the Harm of Gradient Attacks While Only Flipping Labels

    [https://arxiv.org/abs/2503.00140](https://arxiv.org/abs/2503.00140)

    本文证明，即使攻击者只能翻转有限数量的训练标签而无法修改特征、注入样本或控制梯度，仍能对基于均值聚合的分布式学习造成与梯度攻击相当的破坏。

    

    部署在分布式或联邦环境中的机器学习系统极易受到对抗性操纵，尤其是可用性攻击——即导致训练出的模型无法正常使用。此前分布式机器学习领域的研究已通过注入梯度或数据投毒的方式证明了这种对抗性效果。在本工作中，我们提出这样一个问题：在实质上更为受限的行动空间下，攻击者是否仍能造成类似的性能下降——即攻击者仅能翻转现有训练样本中的有限数量标签，而不能修改特征、注入样本或直接控制梯度。我们分析了这种受限标签翻转攻击针对采用均值聚合（研究和生产中的主导基线方法）的分布式学习所能造成的损害程度。聚焦于分类问题，(1) 我们提出了一种新颖的标签翻转攻击形式化方法，将其建模为每轮受限优化问题……

    arXiv:2503.00140v3 Announce Type: replace-cross  Abstract: Machine learning systems deployed in distributed or federated environments are highly susceptible to adversarial manipulations, particularly availability attacks -- rendering the trained model unavailable. Prior research in distributed ML has demonstrated such adversarial effects through the injection of gradients or data poisoning. In this work, we ask whether comparable degradation is still possible under a substantially more constrained action space: the adversary may only flip a limited number of labels of existing training examples, without modifying features, injecting samples, or directly controlling gradients. We analyze the extent of damage caused by constrained label flipping attacks against distributed learning under mean aggregation -- the dominant baseline in research and production. Focusing on classification problems, (1) we propose a novel formalization of label flipping attacks as a per-round constrained optimi
    
[^764]: TSMini：一个简单却极其高效的轨迹相似性学习模型

    TSMini: A Simple Yet Highly Effective Trajectory Similarity Learning Model

    [https://arxiv.org/abs/2502.00285](https://arxiv.org/abs/2502.00285)

    提出了TSMini模型，通过子视图建模机制和基于k近邻的损失函数，同时学习轨迹的绝对相似值和相对相似排序，实现了高精度的轨迹相似性逼近。

    

    轨迹相似性是许多时空数据挖掘应用的基础。最近的研究提出了深度学习模型来逼近传统的轨迹相似性度量，利用其训练后的快速推理时间。尽管已有研究表明其推理效率很高，但由于在轨迹粒度建模以及利用训练数据中的相似性信号方面存在困难，相似性逼近精度仍面临挑战。为填补这一空白，我们提出了TSMini，一个高效的轨迹相似性模型，它包含子视图建模机制和基于k近邻的损失函数。前者使模型能够学习多粒度的轨迹模式，后者引导TSMini不仅学习轨迹之间的绝对相似性值，还学习它们的相对相似性排序。这些创新共同实现了高精度的轨迹相似性逼近。实验表明，TSMini的性能优于……（原文摘要至此截断）

    arXiv:2502.00285v3 Announce Type: replace  Abstract: Trajectory similarity is fundamental to many spatio-temporal data mining applications. Recent studies propose deep learning models to approximate conventional trajectory similarity measures, exploiting their fast inference time once trained. Although efficient inference has been reported, challenges remain in similarity approximation accuracy due to difficulties in trajectory granularity modeling and in exploiting similarity signals in training data. To fill this gap, we propose TSMini, a highly effective trajectory similarity model with a sub-view modeling mechanism and a k nearest neighbor-based loss. The former enables learning multi-granularity trajectory patterns, while the latter guides TSMini to learn not only absolute similarity values between trajectories but also their relative similarity ranks. Together, these innovations enable highly accurate trajectory similarity approximation. Experiments show that TSMini outperforms t
    
[^765]: 关于协作式AI在真实世界医学应用中成本效益的警示故事

    A cautionary tale on the cost-effectiveness of collaborative AI in real-world medical applications

    [https://arxiv.org/abs/2412.06494](https://arxiv.org/abs/2412.06494)

    本文通过对7个医学数据集、3种机器学习任务和8种数据模态的大规模基准测试，系统比较了联邦学习与基于共识的学习方法在多中心医学数据分析中的准确性和成本效益，为协作式AI在真实医疗场景中的部署提供了警示性见解。

    

    背景：联邦学习（FL）作为一种协作学习范式已获得广泛关注，能够在敏感的医疗应用中实现协作式人工智能。然而，联邦学习的实际实施面临技术和组织方面的挑战，因为它通常需要复杂的通信基础设施。在这种背景下，基于共识的学习（CBL）可能是一种有前景的协作学习替代方案，它能够将本地知识整合到联邦决策系统中，同时可能降低部署开销。方法：在这项工作中，我们对一系列FL和CBL方法在广泛的协作式医学数据分析场景中的准确性和成本效益进行了大规模基准测试。该基准测试包括7个不同的医学数据集，涵盖3种机器学习任务、8种不同的数据模态，以及涉及3到23个客户端的多中心设置。发现：（原文摘要至此截断）

    arXiv:2412.06494v2 Announce Type: replace  Abstract: Background. Federated learning (FL) has gained wide popularity as a collaborative learning paradigm enabling collaborative AI in sensitive healthcare applications. Nevertheless, the practical implementation of FL presents technical and organizational challenges, as it generally requires complex communication infrastructures. In this context, consensus-based learning (CBL) may represent a promising collaborative learning alternative, thanks to the ability of combining local knowledge into a federated decision system, while potentially reducing deployment overhead. Methods. In this work we propose an extensive benchmark of the accuracy and cost-effectiveness of a panel of FL and CBL methods in a wide range of collaborative medical data analysis scenarios. The benchmark includes 7 different medical datasets, encompassing 3 machine learning tasks, 8 different data modalities, and multi-centric settings involving 3 to 23 clients. Findings
    
[^766]: DUA-D2C：一种用于解决深度学习过拟合问题的动态不确定性感知方法

    DUA-D2C: Dynamic Uncertainty Aware Method for Overfitting Remediation in Deep Learning

    [https://arxiv.org/abs/2411.15876](https://arxiv.org/abs/2411.15876)

    本文提出DUA-D2C方法，通过基于子集模型在共享验证集上的表现动态加权其贡献，改进了D2C的聚合过程，从而更有效地缓解深度学习中的过拟合问题。

    

    arXiv:2411.15876v3 公告类型： replace-cross 摘要：过拟合仍然是深度学习中的一个重大挑战，其成因通常包括数据异常值、噪声以及训练数据有限。为解决这一问题，我们此前提出了分而治之（Divide2Conquer，D2C）方法，该方法将训练数据划分为多个子集，并在每个子集上独立训练相同的模型。这一策略使模型能够学习到更一致的模式，同时最小化单个异常值和噪声的影响。然而，D2C的标准聚合方式通常对所有子集模型一视同仁，或仅基于固定启发式规则（如数据规模）进行处理，这可能未能充分利用子集模型泛化能力各不相同的信息。在此基础上，我们提出了动态不确定性感知分而治之（Dynamic Uncertainty-Aware Divide2Conquer，DUA-D2C），这是一种对聚合过程进行优化的先进技术。DUA-D2C根据各子集模型在共享验证集上的表现，动态地为它们的贡献分配权重，并采用一种新颖的复合评分机制……

    arXiv:2411.15876v3 Announce Type: replace-cross  Abstract: Overfitting remains a significant challenge in deep learning, often arising from data outliers, noise, and limited training data. To address this, we previously proposed the Divide2Conquer (D2C) method, which partitions training data into multiple subsets and trains identical models independently on each. This strategy enables learning more consistent patterns while minimizing the influence of individual outliers and noise. D2C's standard aggregation typically treats all subset models equally or based on fixed heuristics (like data size), potentially underutilizing information about their varying generalization capabilities. Building upon this foundation, we introduce Dynamic Uncertainty-Aware Divide2Conquer (DUA-D2C), an advanced technique that refines the aggregation process. DUA-D2C dynamically weights the contributions of subset models based on their performance on a shared validation set, employing a novel composite score 
    
[^767]: 用于学习受约束动力学的投影神经微分方程

    Projected Neural Differential Equations for Learning Constrained Dynamics

    [https://arxiv.org/abs/2410.23667](https://arxiv.org/abs/2410.23667)

    本文提出投影神经微分方程（PNDEs），通过将预测速度投影到约束流形的切空间上来强制满足物理约束，在混沌系统和电网模型等测试中比现有方法表现更优、所需超参数更少且计算效率更高。

    

    神经微分方程为从数据中学习动力系统提供了一种强大的方法。然而，它们本身并不能遵循已知的约束条件，例如学习到的动力学应当遵守的守恒定律。众所周知，在数据驱动模型中强制施加约束能够提升模型的泛化能力和数值稳定性。本文提出了投影神经微分方程（PNDEs），这是一种通过将预测速度投影到满足约束的流形的切空间上来约束神经微分方程的方法。在来自不同领域的多个实例（包括混沌动力系统和电网模型）的测试中，PNDEs优于现有的约束学习方法，所需超参数更少，且计算效率更高。该方法展示了增强受约束动力学建模的潜力。

    arXiv:2410.23667v2 Announce Type: replace  Abstract: Neural differential equations offer a powerful approach for learning dynamical systems from data. However, they do not inherently respect known constraints, such as conservation laws, that should be obeyed by the learned dynamics. It is well known that enforcing constraints in data-driven models can enhance their generalizability and numerical stability. In this paper, we introduce projected neural differential equations (PNDEs), a method for constraining neural differential equations based on projection of the predicted velocities onto the tangent space of the manifold that fulfills the constraint. In tests on several examples from different fields, including chaotic dynamical systems and power grid models, PNDEs outperform existing methods for constraining learned dynamics, require fewer hyperparameters, and are computationally more efficient. The proposed approach demonstrates potential for enhancing the modeling of constrained dy
    
[^768]: 论Transformer的泛化误差界

    On Generalisation Error Bounds for Transformers

    [https://arxiv.org/abs/2410.11500](https://arxiv.org/abs/2410.11500)

    本文通过建立多种范数和秩约束下的覆盖数界，为单层Transformer导出了与输入序列长度无关、以O(1/√n)速率衰减的改进泛化误差界，并刻画了低秩结构对泛化性能的影响。

    

    在本文中，我们针对线性函数类在输入和矩阵的各种范数约束下建立了一系列覆盖数界。然后，我们将这些结果与现有的覆盖数界相结合，推导出改进的估计，并基于这些估计为单层Transformer建立了泛化误差界。所得到的泛化界改进了文献中的若干现有结果，特别是，它们与输入序列长度无关。此外，我们的泛化误差界以 $O(1/\sqrt{n})$ 的速率衰减（其中 $n$ 表示样本量），从而改进了现有按 $O((\log n)/\sqrt{n})$ 速率扩展的界。此外，我们的覆盖数分析明确纳入了对底层矩阵类的秩约束，使我们能够刻画低秩结构如何影响度量熵，进而影响最终的泛化界。

    arXiv:2410.11500v2 Announce Type: replace-cross  Abstract: In this paper, we establish a collection of covering number bounds for linear function classes under various norm constraints on the inputs and matrices. We then combine these results with existing covering number bounds to derive improved estimates and, based on these estimates, develop generalization error bounds for single-layer Transformers. The resulting generalization bounds improve upon several existing results in the literature and, in particular, are independent of the input sequence length. Moreover, our generalization error bound decays at the rate $O(1/\sqrt{n})$, where $n$ denotes the sample size, thereby improving upon existing bounds that scale as $O((\log n)/\sqrt{n})$. Furthermore, our covering number analysis explicitly incorporates rank constraints on the underlying matrix classes, allowing us to characterize how low-rank structures affect the metric entropy and, consequently, the resulting generalization bou
    
[^769]: 强化学习中累积前景理论的策略梯度

    Policy Gradients for Cumulative Prospect Theory in Reinforcement Learning

    [https://arxiv.org/abs/2410.02605](https://arxiv.org/abs/2410.02605)

    本文为强化学习中的累积前景理论（CPT）目标推导了策略梯度定理，并基于次序统计量的蒙特卡洛估计器设计了具有渐近收敛性和样本复杂度保证的一阶策略梯度算法。

    

    我们在有限时域强化学习（RL）中为累积前景理论（CPT）目标推导了策略梯度定理，该定理推广了标准策略梯度定理，并将基于失真的风险目标作为特例纳入其中。受行为经济学的启发，CPT将围绕参考点的不对称效用变换与概率失真相结合。基于我们的定理，我们设计了一种用于CPT-RL的一阶策略梯度算法，该算法使用基于次序统计量的蒙特卡洛梯度估计器。我们为该估计器建立了统计保证，并证明了所得算法渐近收敛于（通常是非凸的）CPT目标的一阶稳定点。我们通过非渐近的总样本复杂度分析来补充渐近分析，以达到近似的一阶稳定策略。仿真结果展示了CPT所引起的定性行为特征。

    arXiv:2410.02605v5 Announce Type: replace-cross  Abstract: We derive a policy gradient theorem for Cumulative Prospect Theory (CPT) objectives in finite-horizon Reinforcement Learning (RL), generalizing the standard policy gradient theorem and encompassing distortion-based risk objectives as special cases. Motivated by behavioral economics, CPT combines an asymmetric utility transformation around a reference point with probability distortion. Building on our theorem, we design a first-order policy gradient algorithm for CPT-RL using a Monte Carlo gradient estimator based on order statistics. We establish statistical guarantees for the estimator and prove asymptotic convergence of the resulting algorithm to first-order stationary points of the (generally nonconvex) CPT objective. We complement our asymptotic analysis with a non-asymptotic total sample complexity analysis to reach an approximate first-order stationary policy. Simulations illustrate qualitative behaviors induced by CPT an
    
[^770]: Softmax混合模型中的EM算法与矩方法

    The EM-algorithm and the Method of Moments in Softmax Mixture Models

    [https://arxiv.org/abs/2409.09903](https://arxiv.org/abs/2409.09903)

    本文对Softmax混合模型进行了理论与方法研究，证明在log K量级的原子分离条件下EM算法仅需O(log N)次迭代即可恢复混合原子，并为参数和子空间估计开发了矩方法。

    

    Softmax混合模型（SMMs）是离散的K分量混合模型，用于刻画异质总体中选择p个候选特征向量 $X_1,\ldots,X_p\in\mathbb{R}^L$ 之一的概率，广泛应用于计量经济学和各类科学应用中。相关的softmax混合机制也出现在现代大语言模型（LLM）架构中。我们对SMMs进行了理论与方法论研究，重点关注期望最大化（EM）算法和矩方法（MoM）。我们证明，在混合原子分离度至少为 $\log K$ 量级的条件下，EM算法经过 $\mathcal{O}(\log N)$ 次迭代后，能以参数速率（至多相差对数因子）恢复混合模型的原子。这改进了现有针对高维高斯混合模型EM分析中的分离条件。我们还开发了用于参数和子空间估计的矩方法程序。尽管MoM参数估计的收敛速度比EM更慢，且性能可能出现退化……

    arXiv:2409.09903v3 Announce Type: replace-cross  Abstract: Softmax Mixture Models (SMMs) are discrete $K$-component mixture models for the probabilities of selecting one of $p$ candidate feature vectors $X_1,\ldots,X_p\in\mathbb{R}^L$ in heterogeneous populations and are widely used in econometrics and scientific applications. Related softmax mixture mechanisms also appear in modern LLM architectures. We provide a theoretical and methodological study of SMMs, focusing on the Expectation-Maximization (EM) algorithm and the Method of Moments (MoM). We show that EM recovers the mixture atoms at the parametric rate, up to logarithmic factors, after $\mathcal{O}(\log N)$ iterations, provided atom separation is at least of order $\log K$. This improves on separation conditions in existing analyses of EM for high-dimensional Gaussian mixtures. We also develop MoM procedures for parameter and subspace estimation. Although MoM parameter estimates converge more slowly than EM and can deteriorate
    
[^771]: Tempora-Fusion：具有高效可验证同态线性组合的时间锁谜题

    Tempora-Fusion: Time-Lock Puzzle with Efficient Verifiable Homomorphic Linear Combination

    [https://arxiv.org/abs/2406.15070](https://arxiv.org/abs/2406.15070)

    提出 Tempora-Fusion，首个支持对单个谜题解和同态线性组合进行高效公开验证、且无需可信设置的同态时间锁谜题方案。

    

    我们提出了 Tempora-Fusion，这是首个支持对单个谜题解以及同态线性组合进行高效公开验证的同态时间锁谜题（TLP）方案。Tempora-Fusion 允许客户端独立生成谜题，随后授权一个带有自身发布时间的线性组合，并使任何一方无需可信设置或昂贵的非对称密钥证明系统即可验证发布的结果。在技术上，我们的构造将独立生成的基于 RSA 的谜题映射到公共有限域中，在求值过程中使用不经意线性求值来刷新盲化因子，并通过将消息编码为具有已承诺秘密根的多项式来嵌入隐藏的验证结构。我们形式化了可验证同态线性组合 TLP，在该模型中证明了隐私性与解的有效性，并刻画了求值结果可能在底层客户端谜题被打开之前发布的场景。

    arXiv:2406.15070v3 Announce Type: replace-cross  Abstract: We present Tempora-Fusion, the first homomorphic TLP scheme with efficient public verification of both individual puzzle solutions and homomorphic linear combinations. Tempora-Fusion lets clients generate puzzles independently, later authorize a linear combination with its own release time, and enables any party to verify the released result without trusted setup or costly asymmetric-key proof systems. Technically, our construction maps independently generated RSA-based puzzles into a common finite field, uses oblivious linear evaluation to refresh blinding factors during evaluation, and embeds a hidden verification structure by encoding messages as polynomials with committed secret roots. We formalize verifiable homomorphic linear-combination TLPs, prove privacy and solution validity in this model, and capture the setting in which the evaluation result may be released before the underlying client puzzles are opened. Our protot
    
[^772]: 基于Q函数差分正交估计的以决策为中心的状态抽象

    Decision-Centered Abstractions via Orthogonal Estimation of Difference-of-Q Functions

    [https://arxiv.org/abs/2406.08697](https://arxiv.org/abs/2406.08697)

    该论文提出以保留Q函数差分为目标的状态抽象方法，通过因果机器学习与统计稀疏学习实现，并开发了R学习器的动态泛化版本来估计Q函数差分，从而在离线强化学习中构建更简洁的以决策为中心的状态表示。

    

    离线强化学习使得在因安全、成本等因素而无法在线部署新策略的情况下，能够从历史数据中对序列决策进行评估和优化。大数据技术的进步提供了丰富的状态信息，但其中可能天真地包含与奖励和动作无关的动态信息，而这些信息对于学习最优动作最终是不必要的。我们引入了以保留Q函数差分为目标的状态抽象方法，并提出通过Q函数差分的因果机器学习以及标准的统计稀疏学习来学习这些抽象。在非参数加性奖励模型下，我们刻画了以决策为中心的抽象何时比完整状态空间更简洁，这为我们的估计方法提供了理论动机。我们开发了R学习器（R learner，Nie等人2021年、Lewis和Syrgkanis 2021年）的动态泛化版本，用于估计Q函数的差分，针对离散……（摘要在此处截断）

    arXiv:2406.08697v4 Announce Type: replace-cross  Abstract: Offline reinforcement learning enables evaluation and optimization of sequential decisions from historical data, when it is not possible to deploy new policies online due to safety, cost, and other concerns. Big data advances enable rich state information, but may naively include reward- and action- irrelevant dynamics that are ultimately unnecessary for learning optimal actions. We introduce state abstractions that target preservation of the difference-of-Q functions, and we propose to learn these abstractions via causal machine learning of the difference-of-Q function and standard statistical sparse learning. Under a nonparametric additive-rewards model, we characterize when decision-centered abstractions are simpler than the full state space, motivating our estimation procedure. We develop a dynamic generalization of the R learner (Nie et al. 2021, Lewis and Syrgkanis 2021) for estimating difference of Q-functions, for discr
    
[^773]: 通过逐点互信息实现合理的数据集估值

    Proper Dataset Valuation by Pointwise Mutual Information

    [https://arxiv.org/abs/2405.18253](https://arxiv.org/abs/2405.18253)

    本文提出了一个基于信息论的数据筛选方法评估框架，通过逐点互信息和Blackwell信息序将数据集质量定义为对真实模型参数的信息量，以避免古德哈特定律导致的度量失效问题。

    

    arXiv:2405.18253v4 公告类型：替换 摘要：数据在现代人工智能的发展中扮演着核心角色，高质量数据已成为提升模型性能的关键驱动力。这促使近年来涌现出一系列有原则且有效的数据筛选方法。然而，现有方法在很大程度上依赖于启发式规则，它们是否真正有效仍不清楚。例如，通过评估训练后的模型在特定基准上的表现来评价数据的标准方法，可能会激励系统给那些仅仅与测试集相似的数据赋予高分。这一问题正体现了古德哈特定律（Goodhart's law）：当一项度量成为目标时，它就不再是一个好的度量。为解决这一问题，我们提出了一个用于评估数据筛选方法的信息论框架。我们将数据集的质量定义为它对真实模型参数的信息量，并使用Blackwell信息序对其进行形式化。在该序关系下，Blackwell定理

    arXiv:2405.18253v4 Announce Type: replace  Abstract: Data plays a central role in advancements in modern artificial intelligence, with high-quality data emerging as a key driver of model performance. This has prompted the development of principled and effective data curation methods in recent years. However, existing methods largely rely on heuristics, and whether they are truly effective remains unclear. For instance, standard evaluation methods that assess a trained model's performance on specific benchmarks may incentivize assigning high scores to data that merely resembles the test set. This issue exemplifies Goodhart's law: when a measure becomes a target, it ceases to be a good measure. To address this issue, we propose an information-theoretic framework for evaluating data curation methods. We define dataset quality in terms of its informativeness about the true model parameters, formalized using the Blackwell ordering of informativeness. Under this ordering, Blackwell's theorem
    
[^774]: CausalBN-Bench：一个用于评估大语言模型因果学习能力的综合基准

    CausalBN-Bench: A Comprehensive Benchmark for Causal Learning Capability of LLMs

    [https://arxiv.org/abs/2404.06349](https://arxiv.org/abs/2404.06349)

    本文提出了CausalBN-Bench基准，通过相关性、因果骨架和因果关系识别三项任务的评估发现，大语言模型在简单因果关系上具有潜力，但在大规模网络和碰撞结构上仍显著落后于传统因果学习算法。

    

    理解因果关系的能力显著影响着大语言模型（LLM）在输出解释和反事实推理方面的表现，因为因果性揭示了数据的潜在分布。然而，目前缺乏一个全面的基准来评估大语言模型的因果学习能力，这限制了相关研究的发展。为了填补这一空白，本文基于因果研究社区的数据开发了CausalBN-Bench，使大语言模型能够与传统因果学习算法进行对比评估。为了提供全面的调查，我们设计了三个不同难度的任务，包括相关性识别、因果骨架识别和因果关系识别。对19个领先大语言模型的评估表明，虽然闭源大语言模型在简单因果关系上展现出潜力，但在较大规模网络（超过50个节点）上，它们明显落后于传统算法。具体而言，大语言模型在处理碰撞结构（collider structures）时表现不佳。

    arXiv:2404.06349v3 Announce Type: replace  Abstract: The ability to understand causality significantly impacts the competence of large language models (LLMs) in output explanation and counterfactual reasoning, as causality reveals the underlying data distribution. However, the lack of a comprehensive benchmark currently limits the evaluation of LLMs' causal learning capabilities. To fill this gap, this paper develops CausalBN-Bench based on data from the causal research community, enabling comparative evaluations of LLMs against traditional causal learning algorithms. To provide a comprehensive investigation, we offer three tasks of varying difficulties, including correlation, causal skeleton, and causality identification. Evaluations of 19 leading LLMs reveal that, while closed-source LLMs show potential for simple causal relationships, they significantly lag behind traditional algorithms on larger-scale networks ($>50$ nodes). Specifically, LLMs struggle with collider structures but 
    
[^775]: 具有多元人类反馈的可证明多方协作强化学习

    Provable Multi-Party Reinforcement Learning with Diverse Human Feedback

    [https://arxiv.org/abs/2403.05006](https://arxiv.org/abs/2403.05006)

    该研究首次提出了多方协作强化学习的理论研究，通过整合多个个体不同偏好的元学习与不同社会福利函数的采用，克服了传统RLHF方法无法捕捉并平衡多个个体偏好的局限性。

    

    用人类反馈进行强化学习（RLHF）是一种新兴范式，旨在将模型与人类偏好进行匹配。我们的工作探索了明确建模多个个体不同偏好的多方RLHF的理论研究。我们展示了传统RLHF方法如何失败，因为学习单一奖励函数无法捕捉和平衡多个个体的偏好。为了克服这些局限性，我们结合元学习来学习多个偏好，并采用不同的社会福利函数来整合多方的偏好。我们关注离线学习设置，并为优化不同社会福利函数（如Nash、Utilitarian和Leximin福利）建立样本复杂度界限，同时提供效率和公平性保证。

    arXiv:2403.05006v1 Announce Type: cross  Abstract: Reinforcement learning with human feedback (RLHF) is an emerging paradigm to align models with human preferences. Typically, RLHF aggregates preferences from multiple individuals who have diverse viewpoints that may conflict with each other. Our work \textit{initiates} the theoretical study of multi-party RLHF that explicitly models the diverse preferences of multiple individuals. We show how traditional RLHF approaches can fail since learning a single reward function cannot capture and balance the preferences of multiple individuals. To overcome such limitations, we incorporate meta-learning to learn multiple preferences and adopt different social welfare functions to aggregate the preferences across multiple parties. We focus on the offline learning setting and establish sample complexity bounds, along with efficiency and fairness guarantees, for optimizing diverse social welfare functions such as Nash, Utilitarian, and Leximin welfa
    
[^776]: 数据驱动的有理函数神经网络：一种生成岩石物理解析模型的新方法

    Data-driven rational function neural networks: a new method for generating analytical models of rock physics

    [https://arxiv.org/abs/2109.08813](https://arxiv.org/abs/2109.08813)

    提出了一种数据驱动的有理函数神经网络方法，能够从观测数据中自动生成既符合实际数据分布又具有物理意义的岩石速度解析模型。

    

    地下岩石的地震波速度在探测地球内部结构中起着重要作用。岩石物理模型长期以来一直是预测波速的研究重点。然而，构建理论模型需要仔细的物理考量和数学推导，这意味着一个漫长的研究过程。此外，实践中经常出现各种复杂情况，这给理论模型的应用带来了很大困难。另一方面，存在许多基于真实数据的经验公式，这些经验模型通常简单易用，但可能缺乏物理原理基础，没有恰当的物理表述。这项工作提出了一种用于数据驱动岩石物理建模的有理函数神经网络方法。基于观测数据集，该方法可以推导出一个既满足实际数据分布、又具有恰当物理形式的速度模型。

    arXiv:2109.08813v1 Announce Type: cross  Abstract: Seismic wave velocity of underground rock plays important role in detecting internal structure of the Earth. Rock physics models have long been the focus of predicting wave velocity. However, construction of a theoretical model requires careful physical considerations and mathematical derivations, which means a long research process. In addition, various complicated situations often occur in practice, which brings great difficulties to the application of theoretical models. On the other hand, there are many empirical formulas based on real data. These empirical models are often simple and easy to use, but may be not based on physical principles and lack a proper formulation of physics. This work proposed a rational function neural networks (RafNN) for data-driven rock physics modeling. Based on the observation data set, this method can deduce a velocity model which not only satisfies the actual data distribution, but also has a proper 
    
[^777]: 有向混合隶属度随机块模型

    Directed mixed membership stochastic blockmodel

    [https://arxiv.org/abs/2101.02307](https://arxiv.org/abs/2101.02307)

    本文提出有向混合隶属度随机块模型及配套的高效谱估计算法DiSP，能够分别刻画有向网络中行、列节点的重叠社区归属，并在温和稀疏条件下给出节点级理论误差保证。

    

    过去几年中，无向网络的混合隶属度建模在网络科学领域得到了广泛探索。尽管无向网络的研究已取得重大进展，但处理有向网络中的混合隶属度结构仍然面临巨大困难。为填补这一空白，我们提出了有向混合隶属度随机块模型，这是一个专为具有重叠社区的有向网络量身定制的新框架。DiMMSB的一个关键特性是能够将邻接矩阵的行节点和列节点视为不同的实体，每个实体都可能遵循各自的社区组织结构。基于该模型，我们开发了DiSP，一种高效的谱方法程序，用于估计两组节点的混合隶属度。通过细致的分析，我们在温和的稀疏性条件下推导出了DiSP的节点级误差界。仿真结果支持了理论结果，表明……

    arXiv:2101.02307v4 Announce Type: replace-cross  Abstract: Mixed membership modeling for undirected networks has been extensively explored in network science over the past few years. Despite the substantial progress made for undirected cases, handling mixed membership structures in directed networks continues to pose substantial difficulties. To address this gap, we introduce the Directed Mixed Membership Stochastic Blockmodel (DiMMSB), a novel framework tailored for directed networks with overlapping communities. A key feature of DiMMSB is its ability to treat the row and column nodes of the adjacency matrix as distinct entities, each potentially following its own community organization. Building on this model, we develop DiSP, an efficient spectral procedure to estimate mixed memberships for both sets of nodes. Through delicate analysis, we derive node-specific error bounds of DiSP under mild sparsity conditions. Simulation results support the theoretical results, demonstrating that 
    
[^778]: 线性判别分析与二次判别分析：教程

    Linear and Quadratic Discriminant Analysis: Tutorial

    [https://arxiv.org/abs/1906.02590](https://arxiv.org/abs/1906.02590)

    本教程系统讲解了LDA和QDA两种基本分类方法的决策边界优化、参数估计及其与多种机器学习方法的内在联系，并证明了LDA与Fisher判别分析的等价性。

    

    本教程将线性判别分析（LDA）和二次判别分析（QDA）作为统计与概率学习中的两种基本分类方法进行讲解。我们从后验概率相等的决策边界优化入手，然后针对二分类和多分类推导了LDA和QDA，并介绍了两者中参数的估计方法。接着，我们解释了LDA和QDA与度量学习、核主成分分析、马氏距离、逻辑回归、贝叶斯最优分类器、高斯朴素贝叶斯以及似然比检验之间的关系。我们还证明了LDA与Fisher判别分析是等价的。最后，我们通过提供的仿真实验阐明了部分理论概念。

    arXiv:1906.02590v2 Announce Type: replace-cross  Abstract: This tutorial explains Linear Discriminant Analysis (LDA) and Quadratic Discriminant Analysis (QDA) as two fundamental classification methods in statistical and probabilistic learning. We start with the optimization of decision boundary on which the posteriors are equal. Then, LDA and QDA are derived for binary and multiple classes. The estimation of parameters in LDA and QDA are also covered. Then, we explain how LDA and QDA are related to metric learning, kernel principal component analysis, Mahalanobis distance, logistic regression, Bayes optimal classifier, Gaussian naive Bayes, and likelihood ratio test. We also prove that LDA and Fisher discriminant analysis are equivalent. We finally clarify some of the theoretical concepts with simulations we provide.
    
[^779]: 具有背包约束的高维线性赌臂问题研究

    High-dimensional Linear Bandits with Knapsacks. (arXiv:2311.01327v1 [cs.LG])

    [http://arxiv.org/abs/2311.01327](http://arxiv.org/abs/2311.01327)

    本文研究了具有背包约束的高维线性赌臂问题，利用稀疏结构实现改进遗憾。通过开发在线硬阈值算法和原始-对偶框架结合的方法，实现了对特征维度的对数改进的次线性遗憾。

    

    我们研究了在特征维度较大的高维设置下的具有背包约束的上下文赌臂问题。每个手臂拉动的奖励等于稀疏高维权重向量与当前到达的特征的乘积，加上额外的随机噪声。在本文中，我们研究如何利用这种稀疏结构来实现CBwK问题的改进遗憾。为此，我们首先开发了一种在线的硬阈值算法的变体，以在线方式进行稀疏估计。我们进一步将我们的在线估计器与原始-对偶框架结合起来，在每个背包约束上分配一个对偶变量，并利用在线学习算法来更新对偶变量，从而控制背包容量的消耗。我们证明了这种集成方法使我们能够实现对特征维度的对数改进的次线性遗憾，从而改进了多项式相关性。

    We study the contextual bandits with knapsack (CBwK) problem under the high-dimensional setting where the dimension of the feature is large. The reward of pulling each arm equals the multiplication of a sparse high-dimensional weight vector and the feature of the current arrival, with additional random noise. In this paper, we investigate how to exploit this sparsity structure to achieve improved regret for the CBwK problem. To this end, we first develop an online variant of the hard thresholding algorithm that performs the sparse estimation in an online manner. We further combine our online estimator with a primal-dual framework, where we assign a dual variable to each knapsack constraint and utilize an online learning algorithm to update the dual variable, thereby controlling the consumption of the knapsack capacity. We show that this integrated approach allows us to achieve a sublinear regret that depends logarithmically on the feature dimension, thus improving the polynomial depend
    
[^780]: 通过正则化谱聚类进行潜在类别分析

    Latent class analysis by regularized spectral clustering. (arXiv:2310.18727v1 [cs.LG])

    [http://arxiv.org/abs/2310.18727](http://arxiv.org/abs/2310.18727)

    本文提出了两种新的算法用于分类数据的潜在类别模型，并通过新定义的正则化拉普拉斯矩阵计算潜在类别分析，结果表明算法具有理论收敛速度和稳定的一致性。同时提出了一个衡量潜在类别分析强度的度量标准及相关程序，通过模拟实验验证了算法的效率和准确性，并应用于实际分类数据。

    

    潜在类别模型是在社会、心理和行为科学领域识别具有共同特征的潜在类别的强大工具。在本文中，我们提出了两种新的算法来估计用于分类数据的潜在类别模型。我们的算法利用响应矩阵计算出的新定义的正则化拉普拉斯矩阵进行开发。我们通过考虑稀疏参数给出了算法的理论收敛速度，并表明在适度条件下我们的算法稳定地产生一致的潜在类别分析结果。此外，我们提出了一个衡量潜在类别分析强度的度量标准，并基于该度量标准设计了几个推断在实际分类数据中应该使用多少潜在类别的程序。通过广泛的模拟实验验证了我们算法的效率和准确性，同时将我们的算法进一步应用于实际分类数据。

    The latent class model is a powerful tool for identifying latent classes within populations that share common characteristics for categorical data in social, psychological, and behavioral sciences. In this article, we propose two new algorithms to estimate a latent class model for categorical data. Our algorithms are developed by using a newly defined regularized Laplacian matrix calculated from the response matrix. We provide theoretical convergence rates of our algorithms by considering a sparsity parameter and show that our algorithms stably yield consistent latent class analysis under mild conditions. Additionally, we propose a metric to capture the strength of latent class analysis and several procedures designed based on this metric to infer how many latent classes one should use for real-world categorical data. The efficiency and accuracy of our algorithms are verified by extensive simulated experiments, and we further apply our algorithms to real-world categorical data with pro
    
[^781]: 对于模型解释的神经网络实例化线性化

    Instance-wise Linearization of Neural Network for Model Interpretation. (arXiv:2310.16295v1 [cs.LG])

    [http://arxiv.org/abs/2310.16295](http://arxiv.org/abs/2310.16295)

    这项研究提出了一种实例化线性化的方法，用于解释神经网络模型。通过给模型内部的每个输入特征分配重要得分，揭示了模型如何使用特征做出决策。这种方法有助于解决当前特征归因方法中的局限性，并提高了模型解释的准确性。

    

    神经网络在许多科学领域取得了显著的成功。然而，神经网络模型的可解释性仍然是将这种技术应用于我们日常生活的主要瓶颈。挑战在于神经网络的非线性行为，这提出了一个关键性问题，即模型如何使用输入特征进行决策。解决这一挑战的经典方法是特征归因，它为每个输入特征分配一个重要得分，并揭示其对当前预测的重要性。然而，当前的特征归因方法经常指示每个输入特征的重要性，而没有详细说明它们在模型内部实际上是如何处理的。这些归因方法常常引发一个关注点，即它们是否正确地强调了模型预测的特征。对于神经网络模型，非线性行为通常是由模型的非线性激活单元引起的。然而，预测的计算行为往往是复杂的，这使得解释和理解模型的决策变得困难。

    Neural network have achieved remarkable successes in many scientific fields. However, the interpretability of the neural network model is still a major bottlenecks to deploy such technique into our daily life. The challenge can dive into the non-linear behavior of the neural network, which rises a critical question that how a model use input feature to make a decision. The classical approach to address this challenge is feature attribution, which assigns an important score to each input feature and reveal its importance of current prediction. However, current feature attribution approaches often indicate the importance of each input feature without detail of how they are actually processed by a model internally. These attribution approaches often raise a concern that whether they highlight correct features for a model prediction.  For a neural network model, the non-linear behavior is often caused by non-linear activation units of a model. However, the computation behavior of a predict
    
[^782]: 理解不确定性采样

    Understanding Uncertainty Sampling. (arXiv:2307.02719v1 [cs.LG])

    [http://arxiv.org/abs/2307.02719](http://arxiv.org/abs/2307.02719)

    本研究通过系统研究流式和池式主动学习下的不确定性采样算法，提出了一个等效损失的概念，并证明不确定性采样算法实质上是针对该等效损失进行优化。

    

    不确定性采样是一种常见的主动学习算法，它顺序地查询当前预测模型对数据样本的不确定性。然而，不确定性采样的使用往往是启发式的：（i）关于在特定任务和特定损失函数下对“不确定性”的准确定义没有共识；（ii）没有理论保证能够给出一个标准协议来实施该算法，例如，在随机梯度下降等优化算法框架下如何处理顺序到达的注释数据。在本研究中，我们系统地研究了流式和池式主动学习下的不确定性采样算法。我们提出了一个等效损失的概念，该概念取决于使用的不确定性度量和原始损失函数，并确立了不确定性采样算法本质上是针对这种等效损失进行优化。这一观点验证了算法的适当性。

    Uncertainty sampling is a prevalent active learning algorithm that queries sequentially the annotations of data samples which the current prediction model is uncertain about. However, the usage of uncertainty sampling has been largely heuristic: (i) There is no consensus on the proper definition of "uncertainty" for a specific task under a specific loss; (ii) There is no theoretical guarantee that prescribes a standard protocol to implement the algorithm, for example, how to handle the sequentially arrived annotated data under the framework of optimization algorithms such as stochastic gradient descent. In this work, we systematically examine uncertainty sampling algorithms under both stream-based and pool-based active learning. We propose a notion of equivalent loss which depends on the used uncertainty measure and the original loss function and establish that an uncertainty sampling algorithm essentially optimizes against such an equivalent loss. The perspective verifies the properne
    

