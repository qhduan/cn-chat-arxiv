{
    "title": "What a Deletion Certificate Covers, and Where It Expires: Auditable Removal from a Support-Vector Memory",
    "abstract": "arXiv:2607.12204v3 Announce Type: replace  Abstract: A dense key--value cache gives an operator no way to verify a deletion: it does not say which stored entries currently contribute nothing to the output, and it offers no reference state that the edited memory should match. We build a context memory whose entries carry explicit weights and ask what a deletion certificate over it can cover and where it expires. A one-class support-vector boundary fit around the keys of a context at test time supplies the coefficients used in the readout, so that each key is active (positive coefficient) or reserve (zero coefficient). Removing a reserve key leaves the readout unchanged without a re-solve, and deleting an active key with a decremental solver reaches the same state as re-solving on the remaining keys at the same coefficient cap. A three-key construction shows the limit of both properties: an inert key can acquire positive weight after one more token is admitted, so reserve status certifie",
    "link": "https://arxiv.org/abs/2607.12204",
    "context": "Title: What a Deletion Certificate Covers, and Where It Expires: Auditable Removal from a Support-Vector Memory\nAbstract: arXiv:2607.12204v3 Announce Type: replace  Abstract: A dense key--value cache gives an operator no way to verify a deletion: it does not say which stored entries currently contribute nothing to the output, and it offers no reference state that the edited memory should match. We build a context memory whose entries carry explicit weights and ask what a deletion certificate over it can cover and where it expires. A one-class support-vector boundary fit around the keys of a context at test time supplies the coefficients used in the readout, so that each key is active (positive coefficient) or reserve (zero coefficient). Removing a reserve key leaves the readout unchanged without a re-solve, and deleting an active key with a decremental solver reaches the same state as re-solving on the remaining keys at the same coefficient cap. A three-key construction shows the limit of both properties: an inert key can acquire positive weight after one more token is admitted, so reserve status certifie",
    "path": "papers/26/07/2607.12204.json",
    "total_tokens": 1373,
    "translated_title": "删除证书覆盖什么，又在哪里失效：支持向量记忆中的可审计移除",
    "translated_abstract": "密集键值缓存使操作者无法验证一次删除操作：它既不能说明哪些已存储条目目前对输出没有贡献，也不提供编辑后的记忆应当匹配的参考状态。我们构建了一种条目携带显式权重的上下文记忆，并探讨其上的删除证书能覆盖什么范围、又在何处失效。在测试时对上下文的键拟合一个单类支持向量边界，由其给出读取时使用的系数，使每个键处于激活状态（正系数）或保留状态（零系数）。移除一个保留键无需重新求解即可保持读取结果不变；而通过减量求解器删除一个激活键，可达到与在相同系数上限下对剩余键重新求解完全一致的状态。一个三键构造展示了这两种性质的极限：一个惰性键在再多接纳一个词元之后可能获得正权重，因此保留状态证书……（原文在此处截断）",
    "tldr": "该论文提出一种带显式权重的支持向量上下文记忆，利用测试时的单类支持向量边界将键划分为激活或保留状态，从而提供可验证的删除证书——保留键移除无需重新求解、激活键删除可通过减量求解达到与重新求解一致的状态——并揭示了这些证书在新词元加入后可能失效的边界。",
    "en_tdlr": "This paper proposes a support-vector context memory with explicit weights, where a test-time one-class boundary classifies keys as active or reserve, enabling auditable deletions—reserve-key removal requires no re-solve and active-key deletion via a decremental solver matches re-solving—while demonstrating the limits of these certificates as new tokens can flip reserve keys to active."
}